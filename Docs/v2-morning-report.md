# Утренняя сдача: полный интерфейс V2 (предпросмотр)

Сформировано скриптом `scripts/gen_v2_morning_report.py`, 2026-09-21 02:08.

## Кратко и честно

- **Проход А (полный охват) — «ВИЗУАЛЬНЫЙ ОХВАТ ЗАВЕРШЁН»:** все 73 экрана V1 достижимы из навигации V2, каждый с составом формы и переходом в V1 с контекстом (объект, рабочее место, пункт меню). Это НЕ «весь интерфейс работает».
- Проход Б (операции) начат: читают данные 29 экранов, подключены операции у 12 (проверка не завершена), остальные 32 — каркас с переходом в V1.
- Ни один экран не отмечен «рабочим и проверенным» (статус 5): итоговая регрессия и полная живая приёмка не проведены.
- Слияний, push и deploy не было. V1 не изменён, кроме расширения разбора параметров запуска (`applyStartupDeepLink`).

## Где смотреть

- Настоящий backend на тестовой копии БД: `http://127.0.0.1:8041/v2` (вход выполняет человек), прежний интерфейс: `http://127.0.0.1:8041/?ui=v1`.
- Ветка: `worktree-v2-full-interface`, версия: `f763393` (база `a3e8a92`).
- Стенд без БД (имитация API, наборы сценариев): `python3 scripts/v2_test_server.py 8051` → `http://127.0.0.1:8051/tests/run.html`; сам интерфейс на имитации — `…/tests/app.html`.
- **Маршрут просмотра:** начальная страница `#/` → группы «Рабочие места», «Учёт по блокам», «Контрактация и график», «Отчёты», «Справочники», «Обмен данными», «Администрирование», «Настройки», «Обучение и справка»; поиск по названию слева; выбор объекта в шапке; маршрут любого экрана — `#/<идентификатор>` (список — `Docs/v2-interface-coverage.md`).
- Реестр охвата: `Docs/v2-interface-coverage.md` (генерируется, сверка полноты пройдена: каждый пункт меню, модалка и кнопка панели V1 закреплены). Дизайн-основа: `Docs/v2-design-notes.md` (прототип `#ua-concept` в репозитории отсутствует). Передача работы: `Docs/v2-night-handoff.md`.
- Снимки ключевых форм (стенд, фейковые данные; 1920×1080 и 1366×768, проверка 1920×900 — сценариями VIS): `Docs/v2-full-shots/`.

## Сводка по статусам

| Статус | Экранов |
| --- | --- |
| 1 — не начат | 0 |
| 2 — визуально готов (каркас) | 32 |
| 3 — подключено чтение | 29 |
| 4 — операции подключены, проверка не завершена | 12 |
| 5 — рабочий и проверенный | 0 |
| 6 — заблокирован внешней причиной | 0 |

## 1. Рабочие и проверенные (статус 5)

Нет. Причины — в разделе «Известные дефекты и ограничения».

## 2. Операции подключены, проверка не завершена (статус 4) (12)

- `#/counterparties` — Контрагенты (Контрактация и график)
- `#/projects-objects` — Проекты и объекты (Справочники)
- `#/project-card` — Карточка объекта (Справочники). Ограничения: Ключевые события, задачи и вопросы правятся на экране «События, задачи, вопросы»
- `#/dict-smu` — СМУ (Справочники). Ограничения: Удаление записи, на которую ссылаются объекты (с заменой), — только V1
- `#/dict-individuals` — Физлица (Справочники). Ограничения: Удаление записи, на которую ссылаются объекты (с заменой), — только V1
- `#/report-notes` — События, задачи, вопросы (Справочники). Ограничения: Дата существующей редакции не меняется
- `#/mark-prefixes` — Префиксы марок (Справочники). Ограничения: Тип выбирается из типов объекта и уже заданных; префикс определяет тип только при следующих импортах чертежей
- `#/users-access` — Пользователи и доступ (Администрирование)
- `#/sessions` — Сеансы пользователей (Администрирование). Ограничения: Сеансы других пользователей и завершение всех сеансов сервиса — V1
- `#/status-colors` — Цвета статусов (Настройки). Ограничения: Изменение общее для всех пользователей
- `#/zone-colors` — Цвета зон (Настройки). Ограничения: Стоянки отдельного цвета не имеют — наследуют цвет крана (как в V1)
- `#/late-threshold` — Порог опоздания поставки (Настройки). Ограничения: Только числовой порог; права других ролей на настоящем backend не проверялись

## 3. Подключено чтение (статус 3) (29)

Чтение — из существующих GET-API (отчёты — POST-чтение `/reports/*`), только просмотр; изменение — переход в V1.

- `#/map` — Карта проектов (Рабочие места). Ограничения: Карта MapLibre остаётся в V1; здесь — таблица тех же данных
- `#/blocks` — Учёт по блокам (Учёт по блокам). Ограничения: Только просмотр; правка блоков, работ, сроков и факта — V1 (многоэтапные операции, отдельная проверка)
- `#/fact-journal` — Журнал факта (Учёт по блокам). Ограничения: Только просмотр; отчёты о выполнении (создание, правка, удаление, построчный аудит) — V1
- `#/contracts` — Контракты (Контрактация и график). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/supplier-change` — Документы контрактации (Контрактация и график). Ограничения: Только просмотр; создание, проведение и отмена документов — V1 (массово переносят привязки изделий, высокий риск)
- `#/schedule` — График СМР (Контрактация и график). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/report-status` — Статус монтажа (Отчёты). Ограничения: Только просмотр и выгрузка файла; печать, график, отбор фильтром схемы — V1
- `#/report-dynamics` — Динамика поставки и монтажа (Отчёты). Ограничения: Только просмотр и выгрузка файла; печать, график, отбор фильтром схемы — V1
- `#/report-completion` — Статус комплектации (Отчёты). Ограничения: Только просмотр и выгрузка файла; печать, график, отбор фильтром схемы — V1
- `#/report-mywork` — Моя работа (Отчёты). Ограничения: Только просмотр и выгрузка файла; печать, график, отбор фильтром схемы — V1
- `#/report-analytics` — Аналитическая справка (Отчёты). Ограничения: Только просмотр и выгрузка файла; печать, график, отбор фильтром схемы — V1
- `#/report-block-status` — Учёт по блокам: статусы (Отчёты). Ограничения: Только просмотр; правка процентов в ячейках — V1
- `#/report-block-schedule` — График работ по блокам (Отчёты). Ограничения: Только просмотр и выгрузка файла; печать, график, отбор фильтром схемы — V1
- `#/report-linear-track` — Линейный трек (Отчёты). Ограничения: Только просмотр и выгрузка XLSX; учёт прогресса по трекам — V1
- `#/zones` — Зоны: захватки, краны, стоянки (Справочники). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/subtypes` — Типы и подтипы элементов (Справочники). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/element-catalog` — Элементы (Справочники). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/export-xls` — Экспорт в XLS (Обмен данными). Ограничения: Отбор фильтром схемы (выгрузка только видимых элементов) — V1
- `#/export-pdf` — Экспорт в PDF (Обмен данными). Ограничения: Отбор фильтром схемы (выгрузка только видимых элементов) — V1
- `#/ldap` — Доменная авторизация (Администрирование). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/backups` — Резервные копии (Администрирование). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/address-classifier` — Адресный классификатор (Администрирование). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/map-admin` — Карта: подложка и источник (Администрирование). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/activity` — Журнал действий (Администрирование). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/db-status` — Состояние БД (Администрирование). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/admin-guide` — Памятка администратора (Администрирование). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/marker-shapes` — Форма маркеров (Настройки). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта
- `#/training-history` — Как учатся сотрудники (Обучение и справка). Ограничения: История попыток сотрудника — в V1
- `#/changelog` — Что нового (Обучение и справка). Ограничения: Только просмотр: изменяющие операции — переход в V1 с контекстом объекта

## 4. Визуально готовы: каркас и переход в V1 (статус 2) (32)

Каркас — состав формы V1 (из разметки, из шаблонов JS или по описанию функции; источник указан на экране), неактивный; данные не загружаются. Работа с функцией — переходом в V1.

- `#/ws-model` — Модель: схема 2D/3D (Рабочие места). Ограничения: Схемный движок V1 (2D/3D, выбор элементов, координатные преобразования, семантика действий) по заданию не переписывается ради редизайна: рабочее место открывается в V1 с объектом и режимом
- `#/element-ops` — Операции над элементами (Рабочие места). Ограничения: Смена статуса, плановая дата и привязка контракта пишут историю статусов и работают на выборке из схемы — остаются в V1 до подключения схемы к V2
- `#/ws-mfr` — Модель МФР (Рабочие места). Ограничения: Схемный движок V1 (2D/3D, выбор элементов, координатные преобразования, семантика действий) по заданию не переписывается ради редизайна: рабочее место открывается в V1 с объектом и режимом
- `#/ws-picker` — АРМ комплектовщика (Рабочие места). Ограничения: Схемный движок V1 (2D/3D, выбор элементов, координатные преобразования, семантика действий) по заданию не переписывается ради редизайна: рабочее место открывается в V1 с объектом и режимом; панель строится скриптом V1
- `#/ws-foreman` — АРМ прораба (Рабочие места). Ограничения: Схемный движок V1 (2D/3D, выбор элементов, координатные преобразования, семантика действий) по заданию не переписывается ради редизайна: рабочее место открывается в V1 с объектом и режимом
- `#/chess-flat` — Плоская шахматка (Рабочие места). Ограничения: Доска и массовый ввод факта (атомарно, с проверкой конфликта) остаются в V1: нужна отдельная проверка безопасности пакетной записи
- `#/report-delivery` — График поставки (в разработке) (Отчёты). Ограничения: Экран показывает состав формы V1; данные и операции — в V1
- `#/report-contracting` — График контрактации и поставки (в разработке) (Отчёты). Ограничения: Экран показывает состав формы V1; данные и операции — в V1
- `#/fill-scope` — Заполнить пустые «Объект» и «Проект» (временная) (Справочники). Ограничения: Временная массовая административная обработка — остаётся в V1
- `#/upload-drawing` — Загрузить чертёж (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/shaft-panels` — Панели облицовки шахты (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/revit-import` — Загрузить из Revit (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/pdf-import` — Загрузить из PDF (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/external-models` — Загрузить из FBX (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/history-import` — Импорт истории из XLS (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/status-restore` — Восстановить статусы из выгрузки (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/contracting-import` — Импорт контрактации из XLS (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/objects-import` — Загрузка справочника объектов из Excel (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/schedule-import` — Импорт графика MS Project из XLS (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/import-input` — Загрузить из папки Input (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/settings-io` — Экспорт/импорт настроек (Обмен данными). Ограничения: Загрузка и импорт файлов не включены в V2: нужна отдельная проверка безопасности (предпросмотр изменений, границы, откат) — выполняются в V1
- `#/bulk-edit` — Массовая правка через Excel (Обмен данными). Ограничения: Массовые операции не включены в V2: нужна проверка видимого состава выборки и частичных результатов — выполняются в V1
- `#/blk-bulk` — Массовая правка ЗР через Excel (МФР) (Обмен данными). Ограничения: Массовые операции не включены в V2: нужна проверка видимого состава выборки и частичных результатов — выполняются в V1
- `#/access-matrix` — Права пользователей (Администрирование). Ограничения: Матрица прав и правка ролей ячейки — в V2 доступна выдача доступа в «Пользователи и доступ»; сама матрица остаётся в V1
- `#/reset-history` — Очистить историю статусов (Администрирование). Ограничения: Необратимая очистка истории статусов: в V2 не включается; в V1 она вызывается только осознанным кликом в меню
- `#/appearance` — Внешний вид (Настройки). Ограничения: Изменение данных пока выполняется в V1 (операция не подключена в V2)
- `#/label-color` — Цвет подписей марок (Настройки). Ограничения: Изменение данных пока выполняется в V1 (операция не подключена в V2)
- `#/mfr-colors` — Цвета модели МФР (Настройки). Ограничения: Изменение данных пока выполняется в V1 (операция не подключена в V2)
- `#/change-password` — Сменить пароль (Настройки). Ограничения: Пароль вводит только человек: смена пароля выполняется в V1 (и на экране входа V2)
- `#/training` — Обучение: инструкция и тест (Обучение и справка). Ограничения: Изменение данных пока выполняется в V1 (операция не подключена в V2)
- `#/statuslog` — Сообщения за сеанс (Обучение и справка). Ограничения: В V2 сообщения показываются рядом с действием (область статуса, озвучивается программами экрана); отдельной ленты нет — V1
- `#/shell-context` — Общие элементы оболочки (Начало). Ограничения: Экран показывает состав формы V1; данные и операции — в V1

## 5. Заблокированные внешней причиной (статус 6)

Нет. Отдельно — функции, **сознательно не включённые в V2 по риску** (импорт, массовые и административные операции, схемный движок 2D/3D): причина и путь в V1 указаны в столбце «Ограничения» реестра.

## Результаты проверок

**Стенд с имитацией API** (`Docs/v2-acceptance-results/all.json`, версия `f763393`, чистое дерево, 2026-09-21 02:08:27): 390 PASS / 0 FAIL из 390.

**Настоящий backend** (тестовая копия БД), выполнено:

- Оболочка: вход в /v2 под сессией администратора, начальная страница (65 доступных экранов, 8 скрыто по типу объекта), выбор объекта, права по объекту.
- Чтение (29 экранов): каждый открыт, данные без ошибок; счётчики сверены с прямым SQL по копии БД (СМУ 6, физлица 42, префиксы 12, контракты 27, зоны 5, таблицы БД 71, версии графика 2, элементов 14 506; «Моя работа» 156 = SQL с исключением служебных действий).
- Отчёты «Статус монтажа», «Комплектация» (9 422 позиций), «Аналитическая справка» (смена горизонта перезапрашивает отчёт), «Динамика», «Моя работа» — значения совпали с ответами API.
- «СМУ»: реальный ввод и клики — добавление (пробелы обрезаны), переименование, удаление своей записи, отказ в удалении используемой записи; SQL и журнал действий подтвердили каждый шаг; справочник возвращён к 6 записям.
- «Порог опоздания поставки»: 0→2→0 для объекта 2 реальным вводом; SQL и журнал подтвердили, исходное значение восстановлено.
- «Цвета зон» (объект 2) и «Цвета статусов» (общие): изменение одного цвета, сохранение только изменённой строки, SQL и журнал подтвердили, значения восстановлены (значение цвета выставлено программно — системная палитра не открывалась).
- «Префиксы марок»: реальный ввод и клики — добавление QATEST, замена типа с подтверждением, удаление через план последствий; SQL и журнал подтвердили, справочник возвращён к 12 записям.
- «Карточка объекта» (объект 1): реальный ввод и клики — правка наименования; SQL показал целый JSON (вехи и списки не тронуты), журнал `project_card`, исходный JSON восстановлен без отличий. «События, задачи, вопросы» (объект 1): создание редакции на 2026-09-15, отказ занятой даты, удаление с подтверждением; SQL и журнал подтвердили, состав редакций восстановлен (2).
- Объект МФР (объект 4, данные есть): «Учёт по блокам» (блоки 42, работы 210, виды работ 137), «Журнал факта» (32 отчёта), «Линейный трек» (233), «График работ по блокам» (210), матрица «Учёт по блокам: статусы» (42 блока × 233 операции, 51 с данными) — счётчики сверены с SQL; «Документы контрактации» (2 = SQL), «Карта проектов» (131 объект).
- Выгрузки: XLSX и PDF отчёта «Статус монтажа», XLSX «история»/«на дату» и PDF схемы формируются с верными типами файлов; сохранение на диск не выполнялось (клик по ссылке подменён).
- Переход из V2 в V1 к форме пользователя (`?open=user-security&user_id=`) открывает форму на вкладке «Вход и безопасность» (UA-C-08 живьём); `?object_id=` переключает объект (PO-23 живьём).
- Три ранее перенесённых раздела монтируются в новой оболочке (пользователи: 8 записей; дерево проектов: 193 узла; контрагенты открываются), без ошибок в консоли.
- Переходы в V1 (регрессия глубоких ссылок, живой V1): из 71 пункта меню «Действия» 69 открывают свою форму по `?open=menu&item=<id>` (62 — на объекте ЖБИ, 7 пунктов МФР — на объекте МФР), 2 не открываются по замыслу (`menu-ui-v2` — переключатель версии; `menu-reset-history` — опасный пункт, отклонён с сообщением); рабочие места `?ws=` (model, picker, foreman, mfr) открываются, `?view=3d` переключает вид.
- V1: загрузка `/?ui=v1` (запросы 200), глубокие ссылки `?object_id=&open=menu&item=menu-dict-smu` (объект переключён, форма открыта, параметры стёрты, «Что нового» не перекрывает), опасный пункт `menu-reset-history` и недопустимый id отклонены с сообщением и без действия, недоступное рабочее место не открывается.

Классификация: «имитация API» (стенд) и «настоящий backend» — разные уровни, один за другой не засчитывался; в столбце «Проверки» реестра для каждого экрана указано, что именно и на чём проверялось.

## Известные дефекты и ограничения

- Ни один экран не имеет статуса 5: не проведена итоговая регрессия V1 на всех экранах и полная живая приёмка каждого экрана; статусы 3–4 честно отражают выполненное.
- Права других ролей (не администратор) на настоящем backend не проверялись — только на стенде с имитацией API.
- Ограничение инструмента браузера: клавиша Enter и Backspace на полях ввода не срабатывают нативно; сценарии Enter/клавиатуры проверены только на стенде.
- Отчёты «График поставки» и «График контрактации» не подключены (в V1 помечены «в разработке»). Графики отчётов, печать и отбор фильтром схемы — только в V1.
- Операции учёта по блокам и журнала факта (создание/правка отчётов, сроки, проценты, массовая правка), шахматка, схемные рабочие места 2D/3D, импорт файлов, восстановление копий, права ячейкой, смена поставщика — не включены в V2 (высокий риск/движок схем), доступны переходом в V1.
- Ранее отложенная приёмка трёх разделов (Пользователи и доступ / Проекты и объекты / Контрагенты) не завершена: остатки — `Docs/v2-night-progress.md`.

## Состояние тестовой среды и данных

- Рабочая БД и реальные данные не затрагивались; всё выполнялось на обезличенной копии `data/zhbi.anon.db` этого дерева (копия `.backup` рядом — исходное состояние).
- Собственные тестовые записи ночи удалены/восстановлены (СМУ «QA-СМУ-Ночь…» создан и удалён — `smu_catalog` = 6; порог опоздания объекта 2 возвращён к 0). В журнале действий копии остались события этих проверок.
- Сервер `127.0.0.1:8041` (настоящий backend этой копии) и стенд `127.0.0.1:8051` оставлены запущенными; порты 8000/8020/8010 и старые стенды 8021/8031/8032/8033 не трогались.
- Старый worktree `v2-overnight-migration` (приёмка трёх разделов) — без изменений, его БД возвращена к baseline.

## Следующая точка продолжения

1. Проход Б: сеансы (завершение), подтипы и зоны (правка), затем операции учёта по блокам и журнала факта на объекте МФР (данные есть на объекте 4) — по зависимостям и риску; высокорисковое (импорт, восстановление копий, права ячейкой) — только после отдельной проверки.
2. Проход В: полная живая приёмка каждого экрана статуса 3–4 на настоящем backend (включая права не-администраторов через вход человека), итоговая регрессия V1 по всем меню, затем присвоение статуса 5 только при выполнении всех критериев.
3. Отложенная приёмка трёх ранее перенесённых разделов — по списку остатков в `Docs/v2-night-progress.md`.

