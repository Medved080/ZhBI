# Рабочие места со схемой в V2

Постоянный отчёт проекта: по нему можно восстановить состояние без переписки. Дата обновления — 2026-09-21. Пароли, токены и содержимое рабочих
данных в документ не входят.

Ветка `feature/v2-workspaces` (от `main` `0c6aba6`, ограниченный выпуск V1 + экспериментальный V2). Push и развёртывание на корпоративный сервер не
выполнялись. Локально обновлён экземпляр на 8000 (§0). V1 остаётся самостоятельным: его файлы менялись только режимом кадра (см. ниже) и переходом в V2
с объектом и рабочим местом; обычный запуск V1 остальное не задевает.

Что сделано: четыре РАБОЧИХ места внутри V2 — с настоящей схемой выбранного объекта, а не страницы-заглушки:
«Модель: схема 2D/3D» (ЖБИ), «Модель МФР», «АРМ комплектовщика», «АРМ прораба». Открываются из левой навигации V2
(`/v2#/ws-model`, `#/ws-mfr`, `#/ws-picker`, `#/ws-foreman`), объект берётся из шапки V2.

## 0. Состояние (актуально на 2026-09-21; править при каждой публикации)

| Что | Значение |
| --- | --- |
| **Опубликовано на 8000** | **кандидат `candidate/allocation` (`bff4dfb`) — опубликован 2026-09-21 по согласованию пользователя** (`git merge --ff-only`; `main` = `bff4dfb`, прежний `main` — `093ce8d`, прежняя сборка — `247ab78`). Воркер перезапущен `--reload` (PID 16512, старт 17:42:48), в логе старта ошибок нет, `/health` 200, `/` и `/v2` отдаются, метка сборки `197f2d5`, `ALLOCATION_ENABLED = true` в отдаваемом `write-gate.js`, `POST /contracts/1/allocations` без входа — 401. Штатная копия БД перед обновлением: `zhbi_20260921_144250_auto_before_update.db`. Сквозной сценарий распределения на 8000 НЕ выполнялся (запись на боевых данных — только по отдельному решению пользователя); после публикации проверены только чтение и доступность маршрута |
| Рабочая копия, обслуживающая 8000 | `/Users/max/zhbi-tool`, ветка `main`; `uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`, боевая `data/zhbi.db` (окружение и путь к данным не менялись); дерево чистое, чужой `stash@{0}` не тронут |
| **Единый кандидат на публикацию** | ветка **`candidate/allocation`**, рабочая копия `/Users/max/zhbi-candidate` (ВНЕ каталога, который отслеживает `--reload` сервера 8000). HEAD и состав — §0.1 |
| Код кандидата | коммит `4307204` (последний коммит с кодом: включение распределения в шлюзе); перед ним `f5eea12` (документы), `9bc977d` (проверки), `e8af8d2` (интерфейс распределения), слияние `8d406e2` (backend), backend-коммиты `a1eb746 3e0538b 3614cf6 5d5f2db 4572d64 9512f00 3902237`. Дальше — только метка сборки и записи о публикации (без кода) |
| Ветка интерфейса (прежняя) | `feature/v2-workspaces` `7240f43` (worktree внутри каталога сервера, НЕ менялся). Кандидат содержит её историю целиком |
| Ветка backend | `proposal/atomic-contract-guard` `3902237` (`/Users/max/zhbi-atomic-guard`); кандидат = она + интерфейс |
| Незакоммиченные изменения, влияющие на сборку | нет (в кандидате всё закоммичено; включение распределения — отдельный последний коммит) |
| Push / корпоративный сервер | не выполнялись и запрещены в этом задании |

### 0.1. Состав кандидата (`candidate/allocation`) и что нужно для публикации

Кандидат = `main` (`093ce8d`) + 25 файлов (в т.ч. метка сборки) (`git diff --name-only 093ce8d..candidate/allocation`). **Меняются 9 файлов backend (`app/*.py`) — публикация ТРЕБУЕТ перезапуска воркера** (`--reload` перезапустит его сам; активные запросы в момент перезапуска штатно завершаются/обрываются — публиковать в тихий момент, без идущих импортов и массовых правок). Схема БД и данные не меняются; миграций нет.

| Слой | Файлы | Что |
| --- | --- | --- |
| backend | `app/db.py` (`begin_write`), `app/main.py`, `app/contracts.py`, `app/supplier_change.py`, `app/dict_delete.py` | блокировка записи первым действием во ВСЕХ писателях привязки/количества (§9); исправлена утечка блокировки в `update_contract` |
| backend | `app/activity.py`, `app/activity_actions.py` | журнал подтверждает только зафиксированное: события после commit (`defer_*`), код `contract_allocate` |
| backend | `app/contract_guard.py` | страж отклоняет контракт другого объекта при смене статуса |
| backend | `app/allocation.py` (новый) | `POST /contracts/{id}/allocations` — атомарное распределение пачки (§9) |
| интерфейс | `app/static/v2/workspace.js`, `write-gate.js` | вкладка «Распределение» на новой операции; шлюз: строка `element.allocate` с проверкой формы тела; **флаг `ALLOCATION_ENABLED = true` — отдельный последний коммит кандидата** |
| проверки | `scripts/verify_*.py`, `scripts/_guard_harness.py`, `scripts/serve_test_backend.py`, `scripts/v2_tests/*` | воспроизводимые проверки (§10) |
| документы | `Docs/TZ.md`, `Docs/v2-workspaces.md`, `Docs/v2-write-policy.md` | |

**Порядок публикации** (после вашего согласования; я его не выполняю без него):
1. Убедиться, что нет идущих долгих операций (`tail logs/uvicorn-8000.log`: нет свежих `POST /import-*`, `/bulk-edit/apply`, `/objects/*/upload`).
2. `git -C /Users/max/zhbi-tool status --short` (чисто) → `git -C /Users/max/zhbi-tool merge --ff-only candidate/allocation`. `--reload` перезапустит воркер (штатная копия БД `..._auto_before_update.db` снимается при старте — так было и раньше; миграций нет).
3. `GET /health`, в логе «Application startup complete» без ошибок; вход (человек), V1 и V2 открываются, метка сборки в шапке сменилась; чтение схем/карточек/остатков.
4. Запись на 8000 я НЕ выполняю: сквозной сценарий распределения на боевых данных — по вашему решению (каждое распределение необратимо в этом интерфейсе: история статусов).

**Откат кода без отката данных:** `git -C /Users/max/zhbi-tool reset --hard 093ce8d` (метка `pre-v2-workspaces-8000` — ещё раньше, `0c6aba6`). Воркер перезапустится снова; статусы/контракты, изменённые пользователями за время работы новой версии, остаются — это обычные данные (схема не менялась). Откат только интерфейса (без backend) невозможен для `write-gate.js`, если backend уже откатили: вкладка «Распределение» получит 404 от старого сервера — поэтому откатывать обе части вместе.

### 0.2. Что опубликовано на 8000 и что только в разработке

| Возможность | На 8000 (`247ab78`) | В кандидате |
| --- | --- | --- |
| четыре рабочих места на чтение, поиск, фильтры, показатели | ✔ | ✔ |
| смена статуса ОДНОГО изделия | ✔ (прежняя форма: пункт «Запланирован» предлагается, текст про контракт неточен) | ✔ исправлено: «Запланирован» не предлагается (возврат на него снимает контракт), текст точен |
| распределение изделий на контракт | ✘ отключено шлюзом | ✔ включено; серверная операция атомарна |
| атомарность остатка при одновременных операциях | ✘ (гонка, §9) | ✔ во всех писателях привязки/количества |
| журнал не подтверждает откатившееся | ✘ | ✔ |
| групповая смена статуса без изменения контракта | ✘ (и не будет: §9б) | ✘ |
| «смена планируемого поставщика» | ✘ (нет сущности: §9в) | ✘ |

Побочное событие прошлой публикации (для истории): новый файл `scripts/stamp_v2_build.py` перезапустил воркер 8000 (`--reload` следит за всеми `*.py`). В этом задании ничего на 8000 не публиковалось.

## 0.3. Матрица сценариев V1 → V2

Обозначения: **✔** реализован и проверен в V2 (браузер на настоящем backend); **◐** реализован, проверен только автотестами/частично; **✘** отсутствует (в V2 — переход в V1 с объектом); **⛔** заблокирован конкретной причиной.

| Рабочее место | Сценарий | Статус | Примечание |
| --- | --- | --- | --- |
| Модель ЖБИ | схема объекта 2D / 3D / 3D лёгкий, масштаб, «вписать» | ✔ | сдвиг проверен событиями мыши в кадре, а не физическим перетаскиванием инструментом |
| | выбор элемента → карточка (размещение, контракт, даты, история) | ✔ | |
| | множественный выбор рамкой, сводка | ◐ | сводка проверена автотестом |
| | фильтры (захватка, кран/стоянка, этаж, отметка, тип/подтип/марка, статус, контрагент/контракт), сброс, поиск по марке | ✔ | |
| | смена статуса одного элемента | ✔ | форма, права, двойной клик, сбой сети, история — на копии БД |
| | массовая смена статуса рамкой | ✘ | в V1 (контекстное меню/панель выделения); безопасная пачка требует выбора контракта по каждой позиции |
| | смена контракта / плановой даты / комментария элемента | ✘ | в V1; шлюзом отключены |
| Комплектовщик | схема с собственным отбором, показатели, контракты с остатками | ✔ | |
| | распределение: поставщик → контракт → марка → изделия на схеме → подтверждение (пачка «Запланирован» и смешанные статусы, статусы сохраняются) | ✔ в кандидате; на 8000 ⛔ | серверная операция `POST /contracts/{id}/allocations` и интерфейс проверены на копии БД в браузере на настоящем backend, по HTTP и на стенде (§10–§11). На 8000 (`247ab78`) выключено до публикации кандидата (§0.1) |
| | смена «планируемого поставщика» по марке | ⛔ | такой сущности в модели данных нет (§9в): существуют «Замена поставщика» и «Обмен привязками» — документы перепривязки контракта в V1 (ссылка в «Контракты» есть); для новой операции нужна схема БД |
| Прораб | схема, отбор слева, показатели, поиск | ✔ | |
| | смена статуса одного элемента | ✔ | как в модели ЖБИ |
| | групповая смена статуса | ⛔ | `PATCH /elements/bulk-status` перезаписывает/снимает контракт (§9б) — «только статус» не даёт; остаётся в V1 |
| МФР | план 2D/3D, стартовый вид «1 этаж · все секции», отбор по этажам/секциям/категориям, слои | ✔ | без отбора — явно подписанный режим «все этажи» |
| | карточка блока и элемента | ✔ | |
| | отбор по видам работ / статусу выполнения / срокам / периоду, динамика, «Шахматка» | ✘ | в V1 |
| | факт, настройки, сроки работ блока | ✘ | в V1; шлюз разрешает лишь срок ЗР на экране «Учёт по блокам» |

Заглушки, ссылки и неактивные кнопки реализацией не считаются: все строки ✘ — именно «нет в V2».

## 1. Как устроено

Схему рисует движок V1 (`app/static/app.js`, ~36 тыс. строк: SVG 2D, Three.js 3D, выбор, координаты, семантика). Он завязан на
глобальный DOM документа V1, поэтому его нельзя вынуть, не переписывая, — а переписывать его задание запрещает. Поэтому:

* **Сцена в кадре.** V2 создаёт `<iframe>` с документом V1 в специальном режиме «только сцена» (`EMBED_SCENE` в начале `app.js`,
  стили `embed-scene.css`): шапки, меню, боковых панелей, строки состояния и панелей рабочих мест V1 в кадре нет — они скрыты
  и не управляют ничем. В кадре остаётся холст схемы (2D/3D), его жесты (колесо — масштаб, перетаскивание — сдвиг, клик —
  выбор, Shift + рамка — групповой выбор) и подсветка выбранного.
* **Всё остальное — в V2** (`app/static/v2/workspace.js`): компактная верхняя панель (режим 2D/3D, поиск), инструменты рядом со
  сценой (вписать, приблизить, отдалить, снять выбор), правая панель (свойства / статус / фильтры / вид), строка состояния
  (показано, выбрано, ограничения, режим), сворачиваемая глобальная навигация и правая панель, ширина панели — мышью и стрелками.
* **Кадр строится из `srcdoc`, а не по адресу.** Сервер запрещает показ своих страниц в кадрах (`X-Frame-Options: DENY`,
  `frame-ancestors 'none'` — `app/main.py`, `security_headers`). Заголовки НЕ менялись (backend по заданию не трогаем):
  V2 читает страницу `/` обычным GET и кладёт её в `srcdoc` кадра (тот же origin, та же CSP, `<base href="/">`). Параметры режима
  (объект, рабочее место) передаются атрибутом `data-zhbi-scene` самого `<iframe>` — `app.js` читает их через `frameElement`.
  Если позже заголовок будет ослаблен до `frame-ancestors 'self'`, можно перейти на обычный адрес `/?embed=scene` — движок
  понимает оба способа. (Это единственное место, где нужна отдельная просьба к владельцу backend; без неё всё работает.)
* **Единый источник состояния.** Объект — в шапке V2 (`main.js`): смена объекта → команда `setObject` тому же кадру (V1 сам сбрасывает
  выбор и фильтры прежнего объекта, запоздавшие смены схлопываются до последней) либо переход на парное рабочее место при смене
  типа учёта (ЖБИ ↔ МФР). Выбор, фильтры и режим 2D/3D живут в движке (это его модель) — V2 отображает их снимком и меняет командами,
  своей копии не ведёт. Права на смену статуса — от `/me/permissions?object_id=`.
* **Уход с экрана.** Кадр обнуляется и удаляется (вместе с документом уходят WebGL-контексты, таймеры и слушатели), слушатель
  сообщений снимается, навигация возвращается в прежнее состояние. Проверено: 8 циклов «вход → 3D → выход» — кадр один/ноль, слушатель
  один/ноль, память возвращается к исходной (25 → 30 МБ после выхода), предупреждений о лишних WebGL-контекстах нет.

## 2. Канал «кадр ↔ V2» — протокол `zhbi-scene/1`

Только `postMessage` между кадром и его родителем; обе стороны проверяют `e.source` (только «свой» кадр / «свой» родитель),
`e.origin` и поле `proto`, всё остальное молча игнорируют. HTML и код не передаются: кадр → V2 — числа, строки, даты (карточка
выбранного берётся из ФИКСИРОВАННОГО списка полей); V2 → кадр — команды из белого списка с проверкой типов и границ.

| Направление | Сообщение |
| --- | --- |
| кадр → V2 | `ready`, `state` (снимок: объект, режим, загрузка, ошибка, счётчики, выбор), `filters` (модель фильтров ЖБИ), `picker` (срезы/показатели/контракты), `search-result`, `refreshed`, `notice`, `cmd-error` |
| V2 → кадр (ЖБИ) | `setObject`, `setView{2d\|3d\|3d-light}`, `fit`, `zoom{factor}`, `select{id\|null}`, `locate{id}`, `clearSelection`, `setFilter{changes}`, `resetFilters`, `setZoneVisible`, `search{text}`, `refreshElement{id}`, `getFilters`, `reload` |
| V2 → кадр (МФР) | `mfrPick{kind,id}`, `mfrCategory{category,on}`, `mfrLayer{layer,on}`, `mfrReset`, `mfrSelect{kind,id,additive}` (+ общие `setView{2d\|3d}`, `fit`, `zoom`, `clearSelection`) |
| V2 → кадр (комплектовщик) | `pickerToggle{key,value}`, `pickerSet{key,values,on}`, `pickerClear{key}`, `pickerMetric{key,on}`, `pickerHighlight{on}`, `pickerCandidates{elementType,mark}` (ответ — событие `candidates`), `pickerSelectIds{ids}` (выделение на схеме), `applyElements{items}` (применить подтверждённый сервером ответ к схеме: только скалярные поля статуса/контракта/дат) |

Реализация: `app/static/embed-bridge.js` (классический скрипт кадра: вызывает функции движка, а не копирует его логику).
Отбор и выбор считают сами обработчики V1 (`onPlacementFilterChange`, `pickerElementPasses`, клики по `.revit-pick` и т.д.) —
поэтому числа и состав схемы совпадают с V1.

## 3. Защита данных

* **Кадр работает только на чтение.** В режиме кадра `window.fetch` отсекает любой запрос, кроме GET/HEAD и чтения геометрии
  `POST /plan-data`, ещё ДО сети (ответ 403 «в окне схемы запись отключена»; журнал действий `POST /activity` — тихий 204;
  `sendBeacon` отключён). Значит, контекстное меню сцены, двойной клик, горячие клавиши, групповые действия, диалоги V1 и прямые вызовы
  старых обработчиков не могут ничего записать. Проверено на настоящем backend: `POST/PATCH/DELETE/PUT` из кадра → 403 без обращения к серверу.
* **Запись — только оболочка V2 через шлюз** (`write-gate.js`, запрет по умолчанию; шлюз НЕ ослаблялся). Записи рабочих мест (на 8000 сейчас только первая; вторая — в кандидате, §0):
  **смена статуса ОДНОГО элемента** (`PATCH /elements/{id}/status`), добавлена в таблицу политики строкой `element.status` (теперь 21
  разрешённая операция; `Docs/v2-write-policy.md` пересобран). В теле разрешены только `status`, `changed_at`, `comment`: контракт не
  передаётся (со `contract_id` шлюз отказывает), массовая смена статусов, смена контракта, плановой даты, история, поля элемента — отключены.
* Форма («Свойства» выбранного элемента ЖБИ) показывается только при праве «статусы: изменение» на объекте; запрос идёт на ID
  выбранного элемента; на время записи форма заблокирована, повторная отправка невозможна (двойной клик — один запрос); при отказе
  сервера текст показан, введённое осталось; при сетевом сбое/5xx исход считается НЕИЗВЕСТНЫМ: автоповтора нет, элемент перечитывается
  и говорится, что видит сервер; после успеха схема обновляется тем, что подтвердил сервер (`refreshElement` — GET элемента и штатное
  точечное обновление V1), история перечитывается. Несохранённый ввод при уходе — сторож «Остаться / Не сохранять / Сохранить и продолжить».
* Недоступность записи не мешает просмотру, фильтрам и выбору.

## 4. Что работает

| Рабочее место | На чтение и в интерфейсе | Запись |
| --- | --- | --- |
| Модель: схема 2D/3D (`ws-model`) | реальная схема объекта в 2D, 3D и 3D лёгком; масштаб колесом, сдвиг, «вписать», ± ; выбор элемента → карточка (размещение, контракт, даты, история статусов с сервера); множественный выбор (сводка); поиск по марке/адресу; фильтры (захватка, кран/стоянка, этаж, отметка, тип/подтип/марка, статус, контрагент/контракт, «без контракта») со счётчиками и сбросом; статусы; слои зон | смена статуса одного элемента |
| АРМ прораба (`ws-foreman`) | та же схема; отбор постоянной панелью слева; полоса показателей (число элементов, доля смонтированных и принятых, статусы) | смена статуса одного элемента |
| АРМ комплектовщика (`ws-picker`) | схема с собственным отбором: срезы (тип, подтип, марка, захватка, кран, стоянка, этаж) с колонками «модель / контракт / Δ»; плитки показателей (в модели, запланировано, законтрактовано, без контракта, с плановой датой, доставлено, смонтировано) — кросс-фильтр; контракты по контрагентам: всего / привязано / остаток, выбор, «без контракта», подсветка несвязанных, «только с остатком» | смена статуса одного элемента |
| Модель МФР (`ws-mfr`) | план по этажам (2D) и модель (3D) из выгрузки Revit; отбор по этажам, секциям, категориям; слои (элементы, блоки, оси, фасады, планы); выбор блока или элемента → карточка (для блока — состав, габарит, виды работ; для элемента — свойства и параметры Revit) | нет |

## 5. Ограничения (что НЕ перенесено)

* Записи на МФР и у прораба, кроме смены статуса одного изделия ЖБИ, не перенесены; у комплектовщика — только распределение (кандидат): групповая смена статуса без изменения контракта, «планируемый поставщик» (§9б, §9в), плановые даты, комментарии — в V1. Факт/настройки/сроки работ блока — в V1.
* МФР: не перенесены отборы по видам работ / статусу выполнения / сроку / периоду, «Динамика за период» и раскраска «Шахматка»
  (остаются в V1); показ элемента на плане («Показать на схеме») для МФР не поддержан.
* Комплектовщик: не перенесены развороты контракта по маркам и связь позиций с элементами; прораб — поиск только по марке/адресу.
* Кадр по-прежнему выполняет весь фоновый запуск V1 (несколько лишних GET при входе: права, справочники, опрос изменений) — время
  входа сопоставимо с V1 (на объекте 9580 элементов: V2 ≈ 6 с, V1 ≈ 10 с от начала навигации в свёрнутой панели браузера; без
  сверки в видимом окне цифры ориентировочные).
* Заголовки `X-Frame-Options`/`frame-ancestors` не менялись (см. §1); переход на кадр по адресу возможен только по решению владельца backend.
* Скриншоты в файлы из встроенной панели браузера сохранить нельзя (инструмент возвращает изображение только в ответе) — при приёмке
  смотрите рабочие места по ссылкам из отчёта.

## 6. Проверка

Живая — на настоящем backend (`uvicorn`, свободный порт, обезличенная копия БД `data/zhbi.anon.db`, объекты 1 — 9580 элементов ЖБИ,
2 — 4926, 3 и 4 — модели МФР на 30078 и 11308 элементов), встроенный браузер:

1. ЖБИ: схема загружена (геометрия видна), клик по элементу → карточка + история; колесо → масштаб (100 → 115 %); фильтр «Статус →
   Контрактация» снимает 2 из 4926 (счётчик 4924); 2D → 3D → 3D лёгкий; смена объекта 2 → 1 → МФР → ЖБИ без перезагрузки страницы;
   8 циклов входа/выхода без накопления слушателей и кадров.
2. Кадр только читает: POST/PATCH/DELETE/PUT из кадра → 403 (без сети); неизвестные команды, неверные параметры, чужие сообщения — отбраковка.
3. Смена статуса элемента 40558 (объект 2): «Запланирован» → «Контрактация» → «В производстве»; в БД ровно по одной записи истории на каждое
   подтверждённое сохранение, `contract_id` не тронут; имитация обрыва сети — один запрос, ввод сохранён, исход проверен чтением, ручной
   повтор — один запрос; двойная отправка — один запрос.
4. МФР (объект 4, 11308): план блоков, фильтр «3 этаж» → 642 элемента / 2 блока, клик по блоку → карточка (С01 · Этаж 3, 293 элемента,
   29272×16406×3000 мм, работы: план 5), слои, 3D с подсветкой выбранного блока; объект 3 (30078 элементов) — загружается.
5. Комплектовщик (объект 1): срез «Колонна» → 1328 из 9580, «законтрактовано 751, дефицит 577», Δ −577; плитки и контракты
   (остаток) считаются теми же функциями, что панель V1.
6. Прораб: полоса показателей (3 % смонтировано и принято), поиск «П» → 30 результатов, панель отбора слева.
7. V1: `/?ui=v1&object_id=1&ws=picker` — рабочее место V1 открывается, ни `embed-*` файлы, ни кадр не запрашиваются.

Автоматически (стенд без геометрии, `scripts/v2_test_server.py`, наборы в `scripts/v2_tests/`): новый набор `ws` — 12 сценариев
(`WS-01…WS-12`: кадр из srcdoc и геометрия на 1920×1080, 1920×900, 1366×768 без прокрутки страницы; выбор ↔ панель и запоздавший
ответ; отбраковка чужих/неверных сообщений и белый список команд; фильтры; ошибка/пустота/повтор; смена объекта и переход ЖБИ↔МФР; уход
без накопления слушателей; смена статуса — отказ, сетевой сбой, двойная отправка, тело запроса, шлюз; сторож ухода; панели прораба и
комплектовщика). В кадр подставляется заглушка с тем же протоколом — настоящий движок и геометрия проверяются живой проверкой выше.
Полный прогон стенда (см. §10): 377 PASS / 15 FAIL / 77 «отключено шлюзом» из 469; 15 FAIL — карта MapLibre в безголовом Chrome (VIS-po-*, VIS-dialog-confirm-danger),
идентично падают на базовом коммите `0c6aba6` (не регрессия; в обычном браузере проходят).

## 7. Как посмотреть (короткий маршрут)

Пользователь проверяет результат на **http://127.0.0.1:8000/v2** (V1 — `http://127.0.0.1:8000/`); запускать сервер и переходить на другие порты не нужно.

1. Войти (тот же логин и пароль, что в V1) → выбрать объект в шапке → «Рабочие места» → «Модель: схема 2D/3D».
2. Щёлкнуть по элементу схемы (справа карточка и история) → вкладка «Фильтры» → снять значение (счётчик «Показано» уменьшится) → «Сбросить все» → 3D.
3. «АРМ комплектовщика» → вкладка «Распределение»: поставщик → контракт → марка → «Выбрать N на схеме» → итог перед подтверждением → «Распределить» (в кандидате; на 8000 сейчас
   кнопка недоступна — причина и ссылка в V1 показаны на экране, §0.2).
4. «Модель МФР» (объект с моделью Revit) — стартует на «1 этаж · все секции»; «АРМ прораба» — отбор слева.
5. «← Текущий интерфейс» в шапке V2 возвращает в V1 на тот же объект и рабочее место; в V1 обратно — «Действия → Настройки → Общие → Новый интерфейс — экспериментальный».

## 9. Backend: атомарное распределение и защита остатка (ветка `proposal/atomic-contract-guard`, в кандидате; НЕ на 8000)

Схема БД не менялась; изменения только в `app/*.py`.

### 9.1. Что было не так

Страж `app/contract_guard.py` считал занятые места запросом ВНЕ транзакции и потом писал: параллельные запросы оба видели свободное место и оба записывали (на остатке 1 проходили все одновременные запросы). Тот же разрыв — у проведения замены поставщика и правки количества контракта. Кроме того, после первого исправления найдена новая ошибка: в `update_contract` `begin_write`, `_guard_contract` и `_guard_specification` стояли ДО `try/finally` — отказ в правах оставлял открытую пишущую транзакцию (второй писатель: «database is locked»).

### 9.2. Что сделано (коммиты `a1eb746`, `3e0538b`, `5d5f2db`, `4572d64`, `9512f00`, `3902237`)

| Часть | Решение |
| --- | --- |
| Блокировка | `app/db.py: begin_write(conn)` — `BEGIN IMMEDIATE` первым действием ВНУТРИ `try/finally` обработчика; чтение исходного состояния, проверка стража и запись — под одной блокировкой; ожидание дольше `BUSY_TIMEOUT_MS` (15 с) — 503 «База занята другой операцией — ничего не изменено» (соединение НЕ закрывается внутри `begin_write`: его закрывает `finally` вызывающего — иначе обработчики с `rollback` в `except` подменяли 503 на «closed database») |
| Где подключена | ВСЕ найденные писатели привязки/количества: `update_status`, `update_status_bulk`, `set_element_contract`, `update_contract`, `post_supplier_change`, `unpost_supplier_change`, `update_element_fields`, `update_history_entry`, `delete_history_entry`, `bulk_edit_apply` (Excel-правки), `import_contracting_xlsx` (разбор файла — до блокировки), `delete_entry` (замена записей справочников), новый `allocate` |
| Журнал | `app/activity.py: defer_begin/defer_flush/defer_end`: события операции копятся в буфере потока и уходят в очередь ОДНИМ вызовом после подтверждённого commit; при откате/исключении отбрасываются (счётчик `discarded_count`). Автор, режим «от имени» и время фиксируются в момент события — как раньше; порядок сохранён; ошибки очереди прежние (переполнение считается в `_dropped` и видно в состоянии журнала). Применено в `update_status`, `update_status_bulk`, `post_supplier_change`, `allocate` |
| Объект контракта | `contract_guard.link_problem` отклоняет контракт другого объекта (раньше это проверял только `set_element_contract`) |
| **Новая операция** | `POST /contracts/{contract_id}/allocations` (`app/allocation.py`), тело `{object_id, element_type, mark, items:[{element_id, expected_status}]}` — см. §9.3 |

### 9.3. Семантика `POST /contracts/{id}/allocations`

Одна серверная операция на всю пачку (≤ 500 изделий), под одной блокировкой записи:

1. права `status: write` на ВСЮ пачку (`_guard_elements` по объектам изделий) — отказ целиком (403); контракт существует (404), не архивный (409), того же объекта, что и пачка (400);
2. все изделия найдены (404), выбранного объекта, актуального чертежа и выбранной позиции (тип + марка, сопоставление стража); дубли идентификаторов — 400;
3. **состояние на момент сохранения** сверяется с тем, что видел клиент: у каждого изделия сейчас нет контракта и статус равен `expected_status`; расхождение — **409 с перечнем** (`contract_assigned`, `status_changed`, `other_position`, `other_object`, `not_current`, `partly_applied`) и БЕЗ каких-либо изменений — подходящая часть пачки молча не распределяется, чужой контракт не перезаписывается;
4. изделия «Запланирован» → «Контрактация» и контракт — тем же `apply_status_change`, что и в V1 (история, журнал, страж остатка); изделия в других статусах — статус СОХРАНЯЕТСЯ, контракт назначается тем же `sync_element_contract` под тем же стражем остатка, что у `PATCH /elements/{id}/contract` (записи в историю статусов при этом нет — как у той операции); даты, комментарии и прочие поля не меняются;
5. остаток проверяет страж под той же блокировкой; превышение — 409 и откат ВСЕЙ пачки (в том числе изделий, обработанных до отказа);
6. повторная отправка (потерянный ответ): если ВСЕ изделия уже на этом контракте — 200 `already_applied` без повторной истории и без второго расхода остатка; если только часть — 409 `partly_applied`;
7. ответ: фактически сохранённые изделия (`id`, статус, контракт, контрагент, даты) и остаток позиции после операции (`quantity`, `fact`, `damaged`, `remaining`).

### 9.4. Пути, которые могут менять привязки/количество: защищены ли от чтения устаревшего состояния

Проверка `scripts/verify_writer_paths.py` (трасса SQL: первым выражением каждого соединения обработчика является `BEGIN IMMEDIATE`, до него нет ни одного чтения; при занятой БД — 503 без изменений). До исправления все, кроме `update_contract` (его сверка «до» шла после первого `UPDATE`), начинали с чтения.

| Путь | Защита | Замечание |
| --- | --- | --- |
| `PATCH /elements/{id}/status`, `PATCH /elements/bulk-status`, `PATCH /elements/{id}/contract` | блокировка + страж остатка | |
| `POST /contracts/{id}/allocations` | блокировка + страж + сверка состояния | новый |
| `PATCH /contracts/{id}` (количество/позиции/инциденты) | блокировка + сверка покрытия «до/после» | |
| `POST /supplier-changes/{id}/post`, `/unpost` | блокировка (+ сверка «до/после» у проведения) | |
| `PATCH /elements/{id}/fields` | блокировка | сам путь при смене марки только ПРЕДУПРЕЖДАЕТ о позиции вне спецификации (по подтверждению) — не запрет; это политика пути, а не гонка |
| `PATCH|DELETE /elements/{id}/history/{hid}` | блокировка | пересчёт статуса/контракта из истории под блокировкой |
| `POST /bulk-edit/apply` (Excel-правки: поля, история, контрактация) | блокировка + страж (`link_problem`) | |
| `POST /import-contracting-xlsx` | блокировка | проверки «до/после» остатка в самом импорте нет (по политике пути; не гонка) |
| `POST /dictionaries/{kind}/{key}/delete` (замена записей справочников) | блокировка + сверка «до/после» | |
| импорт истории/DXF-переимпорт | не исследовались отдельно | ссылки на контракты не создают; при необходимости — тем же `begin_write` |

### 9.5. Результаты проверок на копии обезличенной БД (2026-09-21)

Все скрипты работают на КОПИИ (`scripts/_guard_harness.py` копирует источник во временный файл; источник не меняется); запуск: `.venv/bin/python scripts/<имя>.py <копия_БД>`; копия — штатный `sqlite3 backup` (не `cp`: WAL).

| Проверка | Скрипт | Результат |
| --- | --- | --- |
| гонки (30 раундов; вызов обработчиков в потоках, отдельное соединение у каждого) | `verify_contract_guard_concurrency.py` S1–S5, S12, S13 | 0 нарушений во всех; без исправления (`093ce8d`, 40 раундов): S1 40, S3 40, S4 18, S5 31; S2 не различает. Исходы проигравших в наблюдавшихся раундах — 409 |
| откат, ожидание блокировки, свойства пачки | там же S6, S7, S8, S10, S11 | S6 полный откат; S7 503 за ≈0,3 с и успех после освобождения; S10 контракт чужого объекта — 409; S8/S11 — наблюдения (§9б) |
| **освобождение блокировки при любом исходе** (регрессия найденной ошибки) | `verify_lock_release.py` | 31 проверка, 0 нарушений: права (403), нет сущности (404), исключение ПОСЛЕ записи, таймаут (503), ошибка валидации — для `update_contract`, `update_status`, `update_status_bulk`, `set_element_contract`, `post/unpost_supplier_change`, `allocate`. Соединения обработчиков удерживаются тестом (проверка не зависит от сборщика мусора): закрыты/без открытой транзакции, другой писатель сразу начинает транзакцию, отпечаток таблиц не изменился. На старом `update_contract` тест даёт 6 нарушений |
| **журнал** | `verify_activity_journal.py` | 0 нарушений: откатившаяся пачка — 0 событий (отброшено 2), успешная — ровно 3 без дублей с автором и `planned → contracting`, режим «от имени» сохраняется, одиночная смена (успех 1 / отказ 0), проведение замены (откат 0 / успех 1 + итоговое), переполнение очереди при сбросе засчитывается. Без исправления — 4 нарушения |
| писатели: блокировка первой | `verify_writer_paths.py` | 13 путей, 0 нарушений (в т.ч. поля, история, Excel-правка, импорт, замена записей справочника) |
| **новая операция по HTTP** (настоящий сервер uvicorn, маршруты, пул потоков, ответы об ошибках; авторизация подменяется заголовком ТОЛЬКО в тестовом процессе на копии, без заголовка — настоящее 401) | `verify_allocation.py` | 0 нарушений: пачка «Запланирован»; смешанная пачка (статусы сохраняются); превышение остатка (откат, журнал пуст, `discarded=2`); контракт назначен другим после открытия формы (409, чужой не затёрт); статус изменился; недопустимая пачка (дубли, пустая, нет изделия, чужая позиция, чужой объект, архивный, нет контракта); 403 и 401; повтор и двойной клик (история не задваивается); два пользователя на последнее место (5 раундов: 200+409); 503 при занятой БД, 422; совместимость прежних операций V1 (bulk-status, одиночная смена, назначение контракта отгруженному, GET) |

Ограничения проверки: вызовы обработчиков и HTTP идут в ОДНОМ процессе-тесте (не два процесса сервера); вход по паролю в копию не выполнялся; отсутствие нарушений в конечном числе раундов не доказательство — доказательство даёт блокировка `BEGIN IMMEDIATE` до чтения. Исходы «все остальные 409» подтверждены только наблюдавшимися раундами; 503 при ожидании дольше 15 с в гонках не наблюдался (проверен отдельно с малым таймаутом).

**Не решено этим набором (осознанно):** архивный контракт в теле СТАРЫХ операций смены статуса по-прежнему принимается (V1 его не предлагает; новая операция его отклоняет); журнал отражает не всё: события операций, не перечисленных в §9.2, по-прежнему ставятся в очередь до commit.

## 9а. Интерфейс распределения (кандидат)

* Кандидаты — изделия позиции (тип + марка) **без контракта в любом статусе**. «Выбрать N» берёт не больше остатка, запланированные первыми; либо рамкой на схеме (Shift + перетаскивание).
* Перед подтверждением показано, что изменится: поставщик, контракт, позиция, количество, «доступно A → B шт.», «Запланирован → Контрактация: P шт.; остальные сохраняются: Q шт.».
* Неподходящее выделение (другая позиция, уже с контрактом) НЕ сужается молча: пачка не отправляется, причина названа, «Оставить только подходящие (N)» — явное действие человека.
* Запись — одна операция `POST /contracts/{id}/allocations` (§9.3); шлюз: строка `element.allocate` (флаг `ALLOCATION_ENABLED`) проверяет форму тела (объект, позиция, пачка ≤ 500, `expected_status` у каждого, без дублей и лишних полей); общей разрешающей настройки нет.
* Двойной клик — один запрос (кнопка блокируется, повторный вызов отсекается); конфликт 409 — текст с числом расхождений по причинам, ничего не применено, схема перечитывается; отказ сервера — ввод и выбор сохранены; **потеря ответа** — автоповтора нет: сверка с сервером по первому и последнему изделию (пачка атомарна): применено — «сервер подтвердил» и перечитывание; не применено — сообщение и повтор только по решению человека; неоднозначно — сообщение без записи.
* После успеха схема, карточки и остатки обновляются по ФАКТИЧЕСКОМУ ответу сервера (`applied`/`already`, `position.remaining`), затем контракты перечитываются (`GET /contracts`).
* Возврат в «Запланирован» и смена статуса без контракта группой — вне распределения (§9б).

## 9б. Групповая смена статуса — чем она НЕ является

Проверено (S8, копия БД): `PATCH /elements/bulk-status` в каждой строке принимает `contract_id` и применяет его ЯВНО (`contract_explicit=True`):
* **устаревший контракт в теле молча перезаписывает** фактический (изделие на контракте A, клиент прислал B → станет B; проверка есть только по остатку/позиции B);
* `contract_id: null` **снимает** контракт;
* переход в «Запланирован» **снимает** контракт независимо от тела;
* архивный контракт принимается; контракт другого объекта — принимался (исправлено в `3e0538b`).

Поэтому групповая смена статуса — это НЕ «операция без изменения контракта»: чтобы не изменить контракт, клиент обязан прислать актуальный контракт каждого изделия, а при конкурирующем изменении контракта другим пользователем пачка его затрёт.
Безопасной «только статус» пачки backend не даёт; в V2 групповая смена **не реализована и не планируется до появления такого режима** (в V1 она остаётся, с выбором контракта по строкам).

## 9в. «Смена планируемого поставщика по марке» — модель данных

Установлено по схеме БД (`elements`, `contract_lines`, `counterparties`, `supplier_change_docs`, `supplier_change_items`) и `Docs/TZ.md` §5.6: **сущности «планируемый поставщик» нет.** У изделия нет поля поставщика; поставщик выводится из контракта
(`elements.contract_id → contracts → specifications → agreements → counterparties`); изделие в статусе «Запланирован» контракта иметь не может (инвариант `sync_element_contract`), то есть у запланированного изделия поставщика нет вовсе. Существуют две другие операции:
«Замена поставщика» — документ (черновик → проведение → отмена проведения), переводящий НЕпоставленные изделия (статус ниже «Отгружен») одного контракта на другой в пределах одного объекта, по выбранным изделиям, в пределах свободного количества нового контракта, с записью в историю;
«Обмен привязками» — попарный обмен контрактом, плановой датой и историей. Обе — **перепривязка контракта**, а не смена «планируемого» поставщика. Запрошенная операция («выбрать все изделия марки, в том числе не привязанные, и сменить планируемого поставщика») **существующей моделью данных не поддерживается**:
для непривязанных изделий менять нечего, для привязанных это уже замена поставщика документом. Реализация потребовала бы новой сущности (например, плановый контракт/резерв позиции у изделия) — изменение схемы БД, без согласования не делается. В V2 — только переход в V1 к документу «Замена поставщика».

## 10. Тесты интерфейса: результаты и сравнение с базой

Стенд без геометрии (`scripts/v2_test_server.py`, фейковый бэкенд повторяет правила `app/allocation.py` и `contract_guard`), безголовый Chrome; запуск: `bash run_stand.sh <порт> "suite=…" <имя> 1920 1080`.

| Прогон (кандидат) | Результат |
| --- | --- |
| `suite=ws` при ВЫКЛЮЧЕННОМ распределении (реальный шлюз) | 12 PASS / 0 FAIL (WS-11: на экране кнопка отключена, причина и ссылка в V1) |
| `suite=ws,shell,gt,ua,a11y,lc,ap,rd`, распределение ВКЛЮЧЕНО (реальный шлюз: тела запросов проходят настоящую проверку формы) | 131 PASS / 0 FAIL / 29 «отключено шлюзом» из 160 |
| `suite=ws` на стенде с открытым шлюзом | 12 PASS / 0 FAIL |
| полный прогон всех наборов, кандидат (распределение включено) | 379 PASS / 15 FAIL / 77 «отключено шлюзом» из 471 |

**15 падений** — те же, что раньше: `VIS-po-object`, `VIS-po-object-long`, `VIS-po-project-long`, `VIS-po-new`, `VIS-dialog-confirm-danger` × 3 размера (карта MapLibre в безголовом Chrome). Воспроизводятся на базовом `0c6aba6`: `bash run_stand.sh 8083 "suite=vis&only=VIS-po,VIS-dialog-confirm-danger"` в worktree `release-v2-limited` → 3 PASS / 15 FAIL, те же тексты. Полный прогон зелёным не объявляется. Новые регрессии этого задания не найдены; ранее найденная (индикатор сборки 11 px) исправлена.
WS-11 (полный сценарий с включённой записью): смешанная пачка, конфликт после открытия формы (подходящая часть НЕ применена, чужой контракт цел), потеря ответа до commit (сверка, автоповтора нет), двойной клик (один запрос), тело запроса и статусы у «сервера», обновление схемы из ответа, остатки; потеря ответа ПОСЛЕ commit (сверка подтверждает, записи не повторяются). WS-12: серверные правила на фейковом бэкенде.

## 11. Что проверено и чем (2026-09-21)

| Способ | Что проверено |
| --- | --- |
| **Лично в браузере, настоящий backend кандидата на копии обезличенной БД** (`scripts/serve_test_backend.py`, порт 8075; вход подменён ТОЛЬКО в тестовом процессе на копии: по умолчанию администратор копии, cookie `test_user` переключает пользователя) | реальная схема объекта 1 (9580 изделий); сценарий «объект → поставщик → контракт → марка → выбор на схеме → подтверждение → сохранение → обновление остатков и карточек → перезагрузка» на реальных данных; **пачка запланированных** (3 шт., остаток 3 → 0, статусы и история в БД); **смешанные статусы** (отгружен, доставлен, два запланированных: запланированные → «Контрактация» + контракт, «Отгружен» и «Доставлен» сохранены с контрактом, история +1 только у запланированных, карточка изделия показывает контракт сразу и после перезагрузки; остатки после перезагрузки 5/5/0); **превышение остатка** (выделено 4 при доступных 3 — кнопка недоступна, сообщение); **два пользователя на последнее место** (второй получает отказ сервера, его изделие не тронуто, изделие первого распределено); **изделие изменилось после открытия формы** (другой пользователь сменил статус → 409, «изменили статус», ни одно изделие пачки не применено); **отказ внутри пачки** (другой пользователь занял место → третье изделие не помещается → все три без изменений, журнал пуст); **двойной клик** (один POST, история +1); **потеря ответа после commit** (запись состоялась, ответ оборван: интерфейс сверился с сервером, повторного POST нет); **недостаточные права** (пользователь без грантов: 403 от сервера; в V2 «Нет объектов», рабочих мест нет). V1 на том же backend: рабочее место комплектовщика открывается, показывает результат V2 (статусы и контракты), собственная операция V1 (смена статуса `doApplyStatus`) работает |
| **По HTTP** (настоящий сервер и маршруты, `verify_allocation.py`) | все сценарии §9.5, включая совместимость прежних операций V1 и настоящий отказ 401 |
| **Вызовом функций** (обработчики в потоках) | гонки S1–S13, освобождение блокировки, журнал, блокировка первой на 13 путях |
| **Только на стенде** (фейковый бэкенд) | WS-11/WS-12 (интерфейс), недостаточные права при частичной роли (WS-09), сторож ухода |
| **Не проверено** | на 8000 в этом задании ничего не проверялось (там прежняя сборка `247ab78`); физическое перетаскивание рамкой (выбор на схеме делался командой `pickerSelectIds` — тем же путём, что кнопка «Выбрать N»); вход по паролю в копию (вход подменён в тестовом процессе); работа кандидата на боевых данных; долгая нагрузка и время отклика на 30-тысячном объекте; частичные роли в браузере на настоящем backend |

Как воспроизвести браузерную проверку: `.venv/bin/python scripts/serve_test_backend.py <копия_БД> <порт>` (в `/Users/max/zhbi-candidate`), открыть `http://127.0.0.1:<порт>/v2?object_id=1#/ws-picker`.

## 12. Проверка на 8000 (только чтение)

Проверено без входа (чтение статических файлов и заголовков, 2026-09-21 15:12): `GET /health` → 200; `GET /v2` → 200; `GET /` (V1) → 200; `GET /me` без сеанса → 401;
файлы `workspace.js`, `write-gate.js`, `embed-bridge.js`, `main.js`, `styles.css`, `build.json`, `app.js` побайтно (SHA-1 по содержимому) совпадают с деревом
`feature/v2-workspaces` на коммите `94432cd`; `ALLOCATION_ENABLED = false` в отдаваемом `write-gate.js`; экран входа V2 на 8000 открывается.

**Проверено на 8000 после входа (2026-09-21, только чтение, встроенный браузер, 1920×1080; вход выполнил пользователь):**

* индикатор в шапке V2 — «сборка 247ab78»; выбор объекта по умолчанию — последний объект пользователя (МФР, 11308 элементов);
* «Модель МФР»: загрузка модели (689 элементов и 40 блоков на 1 этаже), стартовый вид «1 этаж · все секции», вкладки «Фильтры» (32 значения этажей/секций, 5 категорий),
  выбор блока → карточка (состав, габарит), схема занимает основную площадь (кадр 1575×945), страница не прокручивается;
* смена объекта на ЖБИ (9580 элементов) переключает на «Модель: схема 2D/3D» без перезагрузки страницы (~4,6 с до показа схемы), карточка выбранного элемента с историей, фильтр
  снимает 2470 элементов (9580 → 7110) и «Сбросить все» возвращает 9580, 3D включается;
* «АРМ комплектовщика»: 7 срезов отбора, вкладка «Распределение» — 10 поставщиков, контракты, 66 позиций у выбранного контракта, «Выбрать N на схеме» доступна, «Распределить»
  отключена с названной причиной и ссылкой в V1; «АРМ прораба»: панель отбора слева, полоса показателей;
* на объекте МФР «Модель: схема 2D/3D» скрыта в навигации (тип учёта не подходит), ЖБИ-места на МФР не открываются;
* V2 → V1 («← Текущий интерфейс») открывает V1 на том же объекте и в том же рабочем месте (МФР, 11308 элементов), V1 → V2 (меню «Новый интерфейс — экспериментальный»)
  возвращает на `/v2#/ws-mfr` с тем же объектом; в журнале сервера нет запросов `PUT /me/last-object` (объект V1 совпадал с последним, поэтому V1 ничего не записал);
* воркер 8000 в ходе проверок не перезапускался (запущен 14:48:48).

Замечание: сразу после смены рабочего места полоса показателей прораба один раз показала «0 элементов», через 2 с заполнилась (гонка первой отрисовки; данные верны, в очередь на доработку).

Записей на 8000 я не выполнял (смена статуса, распределение, переключение объекта в V1 — нет). В журнале сервера присутствуют старые строки `POST /users/1/set-password` (200 и 403) — эти запросы я не отправлял.

## 13. Точка продолжения

1. **Одно согласование публикации** кандидата `candidate/allocation` (состав, порядок, перезапуск и откат — §0.1). До него на 8000 остаётся `247ab78`.
2. После публикации и ВАШЕГО решения о записи на боевых данных — сквозная проверка распределения на 8000 (я выполняю только чтение; распределение необратимо в этом интерфейсе).
3. Оставшиеся ограничения (не задерживают распределение): журнал действий по операциям вне перечня §9.2 по-прежнему ставит события до commit; групповая смена статуса без изменения контракта (§9б) и «планируемый поставщик» (§9в) — в V1; архивный контракт в старых операциях смены статуса принимается.
4. Перенос прочих операций прораба/МФР (факт и сроки работ блока, отборы МФР по видам работ/срокам, «Шахматка») — каждая по проверке и отдельной строке политики шлюза.
5. Время входа/отзывчивость в видимом окне на 30-тысячном объекте; скриншоты в файлах для приёмки.
