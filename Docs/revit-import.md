# Загрузка данных из Revit — проект решения

Статус: **предложение**, код не начат, ветка не заведена (2026-08-10).
Первый шаг сознательно узкий: довести данные модели до сервиса и ПОКАЗАТЬ
их. Статусы монолита, объёмы, контрактация новых типов и зоны из Revit — вне
этого шага, см. «Что отложено».

Конкретные имена файлов и пути заказчика в этот файл не выносятся — он в
git (см. CLAUDE.md, раздел про обезличенную копию).

## 1. Исходные данные

Один объект = девять моделей Revit 2022 (русская локаль), суммарно ~2,7 ГБ.
Разбиение — **по разделам проекта**, а не по частям здания:

| Раздел | Порядок размера | Отношение к трекеру |
|---|---|---|
| КР | ~125 МБ | конструктив: монолит и сборный ЖБИ — главный |
| АР | ~410 МБ | архитектура: кладка, ГВЛ, перегородки |
| А | ~35 МБ | оформление/общие виды |
| ВК, ИТП, ОВ1, ОВ2, СС, ЭОМ | ~1,37 ГБ суммарно | инженерия, к конструкциям отношения не имеет |

Три факта, установленных чтением самих файлов (служебные потоки OLE
читаются без Revit), которые прямо определяют конструкцию:

* **Имя файла не идентификатор.** У одного из разделов внутренний путь
  центральной модели отличается от имени выданного файла: между выдачами
  файлы переименовывают.
* **`Central Model Identity` не идентификатор тоже** — у всех девяти он
  одинаковый (следствие detach/save-as при подготовке выдачи). Привязывать
  «модель ↔ область элементов» к нему нельзя.
* **Содержимое читается, но не разбирается.** Уточнено 2026-08-10: первый
  вывод «закрыто наглухо» был ОШИБОЧНЫМ — он опирался на поиск открытых
  строк, а потоки просто сжаты gzip. Файл КР разложен: 12 потоков OLE,
  из них главный (`252`, 121 МБ) после восьмибайтового заголовка —
  обычный gzip; распаковалось 30,9 МБ, и в них нашлись предметные термины
  (Level 1228, Бетон 413, Стена 365, Плита 319, Балка 136, Марка 119,
  Арматура 79, Пилон 26, Секция 9). То есть прочитать `.rvt` можно.

  Полностью разложено: поток `252` — это 9101 gzip-блок по 16-байтовому
  заголовку каждый; распаковалось 7290 блоков, 539 МБ из 121 МБ сжатых,
  1811 блоков (20%) не взялись.

  **Что оттуда извлекается:** каталог семейств и типоразмеров
  (`Перекрытие_Плита_Бетон_180`, `Пилон монолитный`, `Пилон
  Пс.200.30.290.В1`), имена параметров (`Мрк.МаркаКонструкции`,
  `Мрк.МаркаИзделия`, `Мтрл.МаркаБетонаW/F`, `Арм.МаркаТипаАрмирования`),
  общие параметры `MCY_*`, правила фильтров видов и значения-строки —
  например, секции корпуса: **С01 и С02**. Всего 11 441 уникальная
  кириллическая строка.

  **Что НЕ извлекается — и это блокирует:** связка «элемент → его
  параметры → его геометрия». Строки лежат отдельно, элементы — двоичными
  записями со ссылками по идентификаторам; чтобы получить строку нашей
  таблицы импорта, надо декодировать сериализацию объектов Revit, включая
  геометрию (солиды, выдавливания, трансформации). Формат
  недокументирован и меняется между версиями Revit. Плюс 20%
  нераспаковавшихся блоков — это дыры в данных ещё до разбора.

  Итог: прямое чтение `.rvt` годится, чтобы ЗАРАНЕЕ наполнить справочники
  (секции, уровни, каталог типов, правила сопоставления) до первой
  выгрузки, и как перекрёстная проверка выгрузки. Заменить экспортёр оно
  не может. Граница системы остаётся прежней — по причине «дорого и
  ненадёжно», а не «невозможно».

Отсюда первый инвариант: **область-источник элемента приходит явным полем
в самой выгрузке** (код раздела), а не выводится из файла.

## 2. Граница системы

`.rvt` на сервер не попадает никогда. Извлечение данных происходит там, где
есть Revit — на машине проектировщика; наружу выходит компактный **пакет
выгрузки**. Сервису от элемента нужно около полутора десятков полей, это
единицы мегабайт против сотен.

Передача — обычным файлом (почта, сетевая папка), а не HTTP-запросом от
плагина. Причина не в простоте: проектировщик внешний по отношению к
сервису, учётной записи в нём у него нет, а сам сервис живёт только внутри
корпоративного контура и наружу не смотрит. Прямая отправка по API с
сервисным токеном — возможное продолжение, когда выгрузку будет запускать
сотрудник, у которого доступ и так есть.

Экспортёр запускается на отсоединённой копии, а не на центральном файле.

## 3. Пакет выгрузки

Один пакет = один раздел. Файл `*.zhbi.json.gz`.

Почему один раздел на файл, а не архив из девяти: раздел — это область, в
границах которой считается «элемент исчез из модели». Один файл = одна
область = единица, которую можно перевыгрузить отдельно. Загружать при этом
можно сразу несколько.

Почему gzip-JSON, а не xlsx: контуры — вложенные массивы точек, в таблицу
не ложатся; и объём в 5–10 раз меньше. Порядок: элемент ≈ 600–900 байт,
50 000 элементов ≈ 40 МБ JSON ≈ 5 МБ в gzip. Лимит загрузки 200 МБ
(`app/upload_limits.py`) с запасом.

```
{
  "формат": "zhbi-revit-package",
  "версия_схемы": 1,
  "выгрузка": {
    "раздел": "КР",              // область-источник, задаёт экспортёр
    "модель": "<имя файла>",
    "дата": "2026-08-10T12:00:00",
    "экспортёр": "1.0",
    "revit": "2022 20241107_1515",
    "единицы": "мм",
    "координаты": "общие"
  },
  "оси": [ {"имя": "1", "тип": "цифровая", "точки": [[x,y],[x,y]]} ],
  "уровни": [ {"имя": "Этаж 3", "отметка": 12000} ],
  "элементы": [ { ... } ]
}
```

**Оси (`"оси"`)** сохраняются в `object_grids` объекта (2026-08-26,
`app/revit_catalog._apply_grids`) — свойство ЗДАНИЯ, а не раздела:
последний применённый пакет, в котором встретилась метка, побеждает. До
этой даты поле парсилось (`app/revit_package.Package.grids`), но нигде не
писалось — только считалось в сводке загрузки. Используются для
геометрии блока (`Docs/TZ.md`, «Геометрия блока») — привязка секции к
двум осям даёт горизонтальную границу параллелепипеда.

Элемент:

| Поле | Источник в Revit | Зачем |
|---|---|---|
| `uid` | `Element.UniqueId` | идентичность между выгрузками |
| `категория`, `семейство`, `типоразмер` | BuiltInCategory, FamilyName, TypeName | сопоставление с нашим словарём типов |
| `марка` | параметр «Марка» | у сборных изделий |
| `уровень`, `отметка_низа` | Level + смещение | ярус |
| `высота` | габарит | снимает наши эвристики расчёта высот |
| `контур` | проекция низа в плане, замкнутая, мм | 2D-схема и 3D-выдавливание |
| `точка` | точка вставки x,y,z | |
| `объём`, `площадь` | штатные параметры | монолит и кладка (пока просто хранятся) |
| `материал` | класс бетона/материал | |
| `рабочий_набор` | Workset | фильтр |
| `секция`, `корпус`, `тип_этажа`, `часть_здания` | общие параметры `MCY_*` | адресация блоков, см. ниже |
| `параметры` | свободный словарь | всё остальное, без правки экспортёра |

### Адресация уже стандартизована заказчиком

Вычитано из модели КР 2026-08-10: в ней заведено семейство общих
параметров с префиксом `MCY_` (МСУ-1), и адресный набор среди них —
готовый:

| Параметр | Роль |
|---|---|
| `MCY_Корпус` | корпус |
| `MCY_Секция` | **секция** — то самое измерение, которого не было у нас |
| `MCY_Этаж` | этаж |
| `MCY_Тип этажа` | значения: Автостоянка, 1 этаж, Типовой этаж, Кровля |
| `MCY_Часть здания` | часть здания |
| `MCY_Раздел` | раздел проекта |

В модели встречается и строка-перечисление «MCY_Этаж, MCY_Секция,
MCY_Тип этажа, MCY_Часть здания» — эти четыре параметра используются как
единый адресный набор.

Значит договариваться с проектировщиками о том, ЧЕМ задавать секцию, не
нужно: параметр уже есть. Остаётся проверить, ЗАПОЛНЕН ли он у элементов —
имена параметров видны в файле, а заполненность по строкам не определяется.
Это первое, что должна показать пробная выгрузка.

Прочие параметры семейства (`MCY_Площадь`, `MCY_Площадь застройки`,
`MCY_Вид объекта`, `MCY_Кадастровый номер участка`, `MCY_Номер ГПЗУ`,
`MCY_Описание`, `MCY_Назначение вида`) относятся к объекту и участку, а
`MCY_Аннотация_*` — к оформлению; в выгрузку элементов они не нужны.

Обязательные условия: **общие координаты** на все разделы (иначе они не
совместятся) и миллиметры.

Сопоставление «категория/семейство Revit → наш тип» живёт правилами **на
стороне сервиса**, а не в экспортёре: эти правила будут меняться чаще
всего, а менять их в сервисе может администратор, тогда как правку
экспортёра надо доносить до проектировщиков.

## 4. Идентичность и области

* Ключ элемента — `uid` (`Element.UniqueId`), устойчив к правкам,
  пересохранению и detach.
* Геометрическая сверка `app/element_identity.py` остаётся ЗАПАСНЫМ уровнем
  — на случай, если модель пересобрали и uid обнулились (ровно то, что уже
  дважды случалось с `dxf_handle`).
* «Исчез из модели» считается **строго внутри раздела**, пришедшего в этом
  комплекте. Без этого загрузка АР списала бы весь КР.

## 5. Путь данных

1. **Экспорт.** Кнопка в Revit на открытой отсоединённой копии → пакет
   рядом с моделью. Ни сети, ни прав, ни настроек.
2. **Передача.** Пакет уезжает как обычный файл.
3. **Приём.** Форма «Загрузка из Revit»: явно выбирается Проект · Объект,
   прикладываются один или несколько пакетов. Комплект = то, что загрузили
   за один заход.
4. **Анализ.** В базу не пишется ничего. Разбор, сопоставление по `uid`,
   сводка.
5. **Применение.** По подтверждению.

Повторяет двухфазную схему `analyze → apply` из `app/dxf_import.py`
(решение И3) с тем же токеном отложенного разбора. Отличия: принимается
СПИСОК пакетов, сверка по `uid`, списание ограничено разделами комплекта.

**Первичная загрузка** и **дозагрузка** — одна и та же операция. Первый раз
кладут все нужные разделы, дальше — только тот, что менялся. Дельту на
стороне Revit считать не нужно: выгрузка всегда полная по своему разделу,
расхождения считает сервер.

## 6. Экран загрузки

Три шага, как у DXF (`app/static/app.js`, «Загрузка чертежа»):

1. Объект — явно, без догадок по имени файла.
2. Пакеты — можно несколько. Таблица: раздел, модель, дата выгрузки,
   элементов, результат разбора.
3. Сводка и применение.

Сводка — четыре блока:

* **Что приехало** — по каждому разделу: всего элементов, разбивка по
  категориям Revit и по нашим типам, отдельно — сколько НЕ сопоставилось с
  типом. Несопоставленное не отбрасывается молча: первый шаг называется
  «просто показать», значит показываем и то, для чего у нас типа пока нет.
* **Что изменится** — новых / изменённых / исчезнувших, по каждому разделу
  отдельно, потому что списание ограничено разделом.
* **Предупреждения** — единицы не мм; координаты не общие; дубли `uid`;
  элементы без контура; отметки вне диапазона уровней; этот раздел уже
  загружен выгрузкой более свежей даты.
* **Превью геометрии** — план контуров комплекта, наложенный на то, что уже
  есть в объекте, разными цветами: имеющееся / новое / исчезающее. Это
  главный визуальный контроль. Девять разделов обязаны стоять в одних
  координатах, и промах с системой координат ловится только глазом — до
  записи в базу, а не после.

## 7. Минимальные изменения в базе

* `elements`: `revit_uid`, `source_section` (код раздела).
* Реестр загруженных пакетов: объект, раздел, имя модели, дата выгрузки,
  дата загрузки, актуальность — по образцу `object_drawings`.
* Справочник правил «категория/семейство Revit → тип», редактируемый в UI.

Всё, что новая версия должна сделать с накопленными данными, — записью в
`app/release_tasks.py`, а не скриптом (стоячее правило проекта).
## 8. Второй контур учёта: блоки «этаж + секция»

Уточнено 2026-08-10 по документам заказчика («WBS МФР типовой»,
«Концепция автоматизации календарного планирования v3.1»). Помимо
поштучного учёта сборных ЖБИ-изделий появляется ОТДЕЛЬНАЯ ветка учёта:
вид работ × блок (этаж + секция) → План / В работе / Выполнено. Контуры
не влияют друг на друга.

Проект этого контура — `Docs/block-accounting.md`. Здесь важно только то,
что он требует от выгрузки Revit:

* поле `секция` у элемента (новая сущность, захваткой не является);
* секция пакета `помещения` (Rooms) — для квартир, под 8 операций отделки
  в единице `кв.эт/сек`;
* уровни — уже предусмотрены.

Снятая развилка: монолит выше нуля меряется в `эт/сек`, а не в
кубометрах, поэтому отдельные статусные цепочки монолита не нужны.

## 9. Что отложено

Необязательность марки и контракта для невыпускаемых изделий, зоны из
Revit, инженерные разделы как источник геометрии.

## 10. Открытые вопросы

* ~~Чем секция задаётся в Revit~~ — закрыто 2026-08-10: общий параметр
  `MCY_Секция`, уже заведённый в модели. Открытым остаётся, **заполнен ли
  он у элементов** — проверяется первой пробной выгрузкой.
* Порядок числа элементов неизвестен — выяснится на первой пробной выгрузке
  КР. От него зависит, нужна ли работа по производительности 2D-схемы.
* Нужны ли инженерные разделы вообще.

## 11. Результаты выгрузки КР (2026-08-25)

Модель КР выгружена целиком: **3693 элемента, 3,7 МБ**. Порядок числа
элементов — открытый вопрос с самого начала — закрыт: он МЕНЬШЕ нынешних
9422 из DXF, работа по производительности 2D-схемы не нужна.

Что подтвердилось:

* **контур настоящий у 3693 из 3693**, габаритным не подменился ни один,
  без геометрии — ноль. `ExtrusionAnalyzer` оправдал себя полностью;
* координаты общие; база проекта (внутренний ноль в общих координатах) —
  `[16550609.4, 17963708.4, 157680.0]`, то есть площадка стоит на
  абсолютной отметке +157,68 м;
* 26 уровней, 33 оси, помещений нет (они в АР).

### Инвариант: этаж и секция берутся из ИМЕНИ УРОВНЯ

Уровни названы по единому шаблону, несущему всю адресацию:

```
С01-02_2_этаж_основной_+4.950
С01-С02_-1_подземный этаж_основной_-3.300
С01_крыша_основной_+71.340
```

24 уровня из 26 разбираются одним выражением; исключения — две крыши, где
вместо номера слово «крыша». Отсюда видна и форма здания: С02 — низкая
часть (крыша +29,4), С01 — высокая (крыша +71,34), подземный этаж общий.

**`MCY_Этаж` использовать нельзя**: 26% пусто, 28 разных значений, разнобой
в регистре («кровля», «Подземный этаж» / «подземный этаж»), и по смыслу это
назначение, а не номер — элемент на уровне «10 этаж» помечен «кровля».
**`MCY_Тип этажа` пуст у 100%** элементов: параметр заведён, но не заполнен.

**Секция — гибрид.** Основной источник `MCY_Секция` (заполнена у 99%), но у
89 элементов туда записан тип этажа («Автостоянка» / «автостоянка»), а у
5 пусто. Кроме того, в КР рядом с `С01`/`С02` стоят **`СО1` (904) и `СО2`
(342) — с кириллической буквой «О» вместо нуля**. На экране неотличимо, для
программы это разные секции; нормализация приводит их к общей форме, иначе
1246 элементов уехали бы в «без секции».

**Секция из модели расходится с расположением у 475 элементов** (448 из
них помечены С02, а стоят в зоне С01: лестнично-лифтовой узел у стыка,
продублированный по всем этажам). Параметр остаётся главным — это данные
проектировщика, — но расхождение считается и называется в итоге загрузки:
при отборе по секции такие элементы показываются в чужой половине дома, и
без объяснения это выглядит как ошибка программы. Запасной источник — имя уровня, но у общих уровней (`С01-02_…`) оно
неоднозначно. Правило: берём `MCY_Секция`, если значение похоже на секцию;
иначе — из имени уровня, если оно однозначно; иначе — в «требует внимания».

### Состав модели: три разные сущности в одном файле

| Префикс типоразмера | Что это | Штук | С маркой |
|---|---|---|---|
| `СТм` | стены монолитные | 1617 | 1400 |
| `ППм` | пенополистирол, термовкладыши | 994 | 66 |
| `Бм` | балки монолитные | 511 | 0 |
| `Вс` | **вентблоки сборные** | 358 | 358 |
| `МЛс`, `ФПм`, `Перекрытие*` | лестницы, фундаменты, перекрытия | ~200 | — |

Три вывода:

1. **Сборные изделия в модели есть** — вентблоки `Вс_БВ 30.11.4-1 …`, марка
   заполнена у всех 358. Это существующий поштучный контур трекера.
2. **Монолит** (`СТм`, `Бм`, `Пм`) — контур блочного учёта.
3. **994 термовкладыша и гидроизоляция — не конструкции вообще.** Категория
   Revit у них «Обобщенные модели», семейства `231/232_Термовкладыш`,
   `222_Гидроизоляция…`. Их надо отсекать правилом сопоставления типов, а не
   тащить в трекер: это 27% выгрузки.

Отсюда же следует, что «категория Revit» плохой признак типа: в
«Обобщенных моделях» лежат и вкладыши, и гидроизоляция, и дефшвы. Правило
сопоставления надо строить на СЕМЕЙСТВЕ и префиксе типоразмера.

### Мелочи

* gzip в среде Dynamo не сработал, выгрузка ушла в запасной путь и легла
  несжатым `.json`. При 3,7 МБ это приемлемо, чинить не стали.
* В именах уровней отметка написана то через точку, то через запятую
  (`+13,950` рядом с `+22.950`) — на разбор номера этажа не влияет.

## 12. Результаты выгрузки АР (2026-08-25)

**25 131 элемент, 24,4 МБ, 1091 помещение, 37 уровней.** Вместе с КР по
объекту выходит **28 824 элемента** — втрое больше нынешних 9422 из DXF.
Вывод раздела 11 про производительность был сделан по одному КР и его надо
пересмотреть: после отсева неконструктивного останется меньше, но порядок
уже другой.

### Квартиры есть — квартирографией на помещениях

Первый вывод «квартир в модели нет» был ПОСПЕШНЫМ: он сделан по составу
выгрузки, а не по модели. Прямое чтение `.rvt` (тем же читателем, что в
разделе 1) показало в АР полный набор квартирографии — параметры
ПОМЕЩЕНИЙ, которых экспортёр просто не читал:

| Параметр | Что несёт |
|---|---|
| `ROOM_КВ_Номер`, `3_ROOM_Номер квартиры` | номер квартиры |
| `ROOM_КВ_Кол.Комнат` | число комнат |
| `ROOM_КВ_Площадь`, `ROOM_КВ_Жил.Площадь`, `ROOM_КВ_Пл.+Неот.` | площади |
| `ROOM_Тип планировки`, `ROOM_Категория`, `ROOM_Тип` | классификация |

Значения живые: «2х комнатная квартира» (495), «1х комнатная» (274),
«3х комнатная» (158). Есть и слово «Квартирография» — то есть в модели
заведена соответствующая схема.

**Квартира в Revit не отдельный объект: она собирается по номеру,
проставленному у комнат.** Восемь видов работ WBS в единице `кв.эт/сек`
привязывать есть к чему, просить проектировщиков ничего не нужно.

Сами `помещения` — это комнаты: Спальня (270), Прихожая (150), Лоджия
(150), Кухня (134), Ванная (92), плюс МОП — лестничные клетки,
межквартирные коридоры, тамбур-шлюзы. По 70 на этаж, сквозная нумерация
`1…1091`. Квартира = группа комнат с одним `ROOM_КВ_Номер`.

Перевыгрузка АР подтвердила: **квартир 238**, комнат с номером квартиры
1051 из 1091 (остальные 40 — МОП: лестничные клетки, коридоры,
тамбур-шлюзы).

**Ключ квартиры — тройка (секция, этаж, номер).** Номер НЕ сквозной: «4»
повторяется на разных этажах. Считать квартиру по одному номеру нельзя —
уникальных номеров всего 135, и агрегация по ним даёт «квартиры» из 35
комнат, то есть один номер на всех типовых этажах сразу.

Это буквально совпадает с единицей `кв.эт/сек` из WBS: квартира внутри
блока «этаж + секция». Единица измерения заказчика и ключ модели сошлись
один в один, без переходников.

Что приезжает на комнату: `квартира`, `комнат`, `тип_планировки`
(`3.1`, `2.2*`, `1.2`, `1.3*`), `площадь_квартиры`, `жилая_площадь`,
`площадь_с_неотапливаемыми` (переведены в м2), `категория_помещения`
(`1` — квартирное, 960; `0` — прочее, 131).

### Секции названы по-разному в разных разделах

| Раздел | Значения `MCY_Секция` | Пусто |
|---|---|---|
| КР | `С01`, `С02` (+ мусор «Автостоянка») | 5 |
| АР | `Секция 1`, `Секция 2` (+ одно `С1`) | 4374 (17%) |

Один и тот же параметр, один объект, разные словари. **Нормализация
значений при загрузке обязательна**, иначе справочник секций сразу
задвоится.

### Уровни: разные имена И разные отметки

Один этаж в двух разделах — это `С01-02_8_этаж_основной_+22.950` (КР,
180630 мм) и `С01-02_8_этаж` (АР, 178660 мм). Расхождение
систематическое, −1970 мм на надземных этажах, но у подземного +480 мм —
значит это не сдвиг системы координат, а разные отметки (похоже, верх
плиты против чистого пола).

Отсюда инвариант: **разделы сшиваются по НОМЕРУ ЭТАЖА из имени уровня**,
не по отметке и не по имени целиком.

В АР ещё и 11 мусорных уровней: `С01-02_21_этаж_основной_+61,951` …
`+61,960` стоят на отметках 2-го, 11-го и 23-го этажей. Ошибка модели,
загрузка должна их показать, а не проглотить.

### Отделка смоделирована отдельными элементами

13 173 «стены» АР — это в основном не стены. По префиксу типоразмера:

| Префикс | Что это | Штук (топ) |
|---|---|---|
| `ОВ_` | отделка: обои, штукатурка, окраска | 1614 + 1225 + 1095 + … |
| `КЖ_` | железобетон | 1547 |
| `СВ_` | стены внутренние: газоблок, ПГП | 940 + 604 |
| `СТК` | каркасные перегородки ГКЛВ | 494 |
| `УН_` | утеплитель наружный | 327 |

Это возможность: виды работ WBS «Стены / Пол / Потолок» можно привязать к
РЕАЛЬНЫМ элементам, а не только к блоку. И одновременно обязанность:
правило сопоставления должно раскладывать «Стены» Revit на конструктив,
перегородки, утеплитель и отделку — по префиксу типоразмера.

«Обобщенные модели» АР — доборные элементы и отверстия:
`MCY_АР_Откос` (648), `MCY_АР_Подоконник` (549), `MCY_АР_Отлив` (547),
`MCY_АР_Порог` (481), отверстия в перекрытиях (790).

### Контур габаритом — 6873 из 25 131 (27%)

Распределение объясняет само себя: Обобщенные модели 3287 из 4217, Двери
1834 из 3036, Окна 1516 из 1998, а **Стены — лишь 191 из 13 173**. Двери,
окна, откосы и подоконники — плоские вставки в стену, вертикальной
проекции у них по сути нет, и габарит для них приемлем. Там, где контур
важен, он настоящий.

## 13. Отметки этажей объекта 80-0924-ОКЭФ-1/Н-3 — из PDF, не из Revit (2026-08-25)

**С 2026-08-26 таблица не только справочная — она буквально код**
(`app/pdf_rooms.py`, `_SPEC`/`FLOOR_PLANS`): кнопка «Загрузить из PDF»
(`Docs/TZ.md` §3а) использует ровно эти отметки и номера листов для
выдавливания контуров помещений по этажам. Правка одной без другой
рассинхронизирует документацию с поведением кнопки.

Другой объект, чем ЖУ30 в разделах 11–12 выше. Модели Revit у него ещё нет
— только архитектурный PDF (`80-0924-ОКЭФ-1-Н-3-АР1.pdf`, комплект
марки АР1). Отметки сняты с разреза (лист 18 комплекта) — единственный
лист, где у каждого этажа своя подпись `N этаж` рядом с отметкой. Метод:
слова с координатами (`page.get_text("words")`), подпись этажа сопоставлена
с ближайшей по расстоянию отметкой вида `±X,XXX` — для этажей 2–24 это
даёт чистое совпадение (типовое смещение 5–8 pt, у случайных совпадений на
порядок больше). Этаж 22 проверен визуально по прямому запросу
пользователя (скриншот листа), этаж 1 — рендером фрагмента листа в
изображение: своей отметки рядом с подписью нет, использован датум
`+0,000`, размеченный пунктирной линией на той же высоте.

Пригодится, если модель Revit по этому объекту появится позже: отметки
уровней в выгрузке обязаны сойтись с этой таблицей (или разойтись
объяснимо, как КР/АР у ЖУ30 в разделе 11 — верх плиты против чистого пола).

Секция 1 (этажи 1–8, дальше не растёт): подземный этаж выведен из текста
плана (`-6,450`), не с этого листа.

| Этаж | Низ, мм | Верх, мм |
|---|---|---|
| подземный | −6450 | 0 |
| 1 | 0 | 4650 |
| 2 | 4650 | 7650 |
| 3 | 7650 | 10650 |
| 4 | 10650 | 13650 |
| 5 | 13650 | 16650 |
| 6 | 16650 | 19650 |
| 7 | 19650 | 22650 |
| 8 | 22650 | 25650 |
| технический (секция 1) | 25650 | 29400 |
| кровля (секция 1) | 29400 | 32400 |

Секция 2 продолжает тот же шаг (2–8 этажи общие с секцией 1, ровно
3000 мм на этаж) до этажа 24, дальше свой технический этаж и кровля:

| Этаж | Низ, мм | Верх, мм |
|---|---|---|
| 9 | 25650 | 28650 |
| 10 | 28650 | 31650 |
| 11–20 (по 3000 мм) | 31650 | 61650 |
| 21 | 61650 | 64650 |
| 22 | 64650 | 67650 |
| 23 | 67650 | 70650 |
| 24 | 70650 | 73650 |
| технический (секция 2) | 73650 | 76800 |
| кровля (секция 2) | 76800 | 80400 |

Итоговая высота здания — 80 400 мм от нулевой отметки входа.
