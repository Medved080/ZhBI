# Бэклог замечаний по тестированию

Замечания от заказчика по итогам тестирования интерактивного инструмента.
Пункты 1–12 реализованы 2026-07-14. Нумерация — как в исходном списке
замечаний.

## 1. Хранение истории статусов: дата, статус, кто установил — ✅ реализовано

Карточка элемента при клике подгружает `GET /elements/{id}` и показывает
таблицу "История статусов" (дата, статус, кто изменил), сначала новые.
`status_history.changed_by_user_id` теперь ссылается на аккаунт (п.8), а
`changed_by` хранит текстовый снимок ФИО на момент изменения (переживёт
переименование пользователя).

## 2. В XLS-экспорт истории добавить дату изменения — ✅ уже было реализовано

Подтверждено при повторной проверке — колонка "Изменено" в листе "История
статусов" (`app/export.py`) на месте.

## 3. Импорт истории статусов из XLS — перенос прогресса на другой сервер — ✅ реализовано

`POST /import-history-xlsx` (`app/history_import.py`, только admin).
Сопоставление элементов — по `(source_file, dxf_handle)`, как и договорились.
Перед импортом пользователь выбирает режим (диалог в UI, меню "Настройки →
Импорт истории из XLS"):
- **Дополнить** — история добавляется, дубли (тот же статус в ту же дату у
  того же элемента) пропускаются.
- **Заменить** — история сопоставленных элементов удаляется перед импортом.

После импорта `current_status` элемента пересчитывается по последней записи
истории. Несопоставленные `dxf_handle` (элемента с таким handle нет в целевой
БД) попадают в сводку `unmatched_handles`, ничего не импортируется молча.
Round-trip проверен: экспорт → импорт того же файла (100% дублей, 0 вставок)
→ импорт файла с новой записью (корректно добавилась, `current_status`
пересчитался) → режим "заменить" (старая история вытеснена).

## 4. Подписи элементов накладываются друг на друга — ✅ реализовано

`computeLabelOffsets()` в `app/static/index.html`: для каждого элемента при
загрузке схемы перебираются 6 кандидатных направлений подписи (право,
право-верх, право-низ, верх, низ, лево), выбирается первое без пересечения
с уже размещёнными (пространственная сетка бакетов, а не полный перебор —
не тормозит на тысяче+ элементов). На реальных данных (332 колонны) 32
подписи получили нестандартное направление из-за коллизий — проверено
проверкой распределения `state.labelOffsetById`, не просто "выполняется
вхолостую".

## 5. Ограничить рост шрифта подписей при масштабировании — ✅ реализовано

## 6. Подсвечивать выбранный элемент на схеме — ✅ реализовано

Клик по элементу добавляет класс `.selected` (синяя жирная обводка),
сбрасывается при выборе другого элемента или клике мимо. Карточка и
подсветка всегда синхронны.

## 7. Настройка видимости подписей по типу элемента — ✅ реализовано

Таблица `label_visibility` (ключ — `element_type`) самозаполняется при
каждом импорте DXF новыми типами (`ensure_label_visibility` в
`scripts/import_elements.py`) — ничего не нужно заводить руками при
появлении ригелей/плит. Быстрые чекбоксы в сайдбаре + полноценный экран в
"Настройки → Видимость подписей" (`GET/PUT /label-visibility`, запись —
только admin).

## 8. Список пользователей: ФИО, должность, подразделение, доменное имя, роль — ✅ реализовано

Полноценная модель (`app/schema.sql`: таблица `users`) + аутентификация по
логину/паролю с серверной сессией (`app/auth.py`, cookie `zhbi_session`,
30 дней). Уточнено в разговоре и реализовано так:
- **Администратор** — полный доступ.
- **Пользователь** — схема + смена статусов; меню "Настройки" не видно и
  недоступно совсем (403 на бэкенде, не только скрыто в UI).
- **Просмотр** — только чтение, контекстное меню смены статуса не
  открывается.

Роли проверены не только в UI, но и на уровне API (403 при прямом вызове
в обход интерфейса) — см. curl-тесты в истории разработки.

## 9. Меню "Настройки" — ✅ реализовано

Выпадающее меню в тулбаре, видимое только администратору. Подпункты: Цвета
статусов, Видимость подписей, Пользователи, Импорт истории из XLS,
Экспорт/импорт настроек. "Загрузить чертёж"/"Экспорт в XLS" остались
обычными кнопками (доступны admin+user, это не "настройки", а рабочие
действия) — осознанное разграничение, см. п.8.

## 10. Экспорт/импорт настроек проекта — ✅ реализовано

`GET /settings/export` / `POST /settings/import` (JSON, только admin).
Содержит пользователей (включая хэш+соль пароля — осознанно, чтобы не
заставлять всех заново задавать пароли при переносе на другой сервер; файл
нужно беречь как резервную копию БД), цвета статусов, видимость подписей.
Импорт — upsert (по `domain_login` для пользователей), ничего не удаляет
молча. Проверено сквозным сценарием: экспорт с "боевого" состояния →
свежая БД (только дефолтный админ) → импорт → пользователь `ivanov` вошёл
на "новом" сервере **тем же паролем**, что был на старом.

## 11. Ограничить максимальный размер маркеров при масштабировании — ✅ реализовано

Радиус маркера и размер шрифта подписи пересчитываются в `updateSizesForZoom()`
при каждом изменении `viewBox` (колесо мыши/перетаскивание), с верхним
пределом в экранных пикселях: `MAX_MARKER_PX = 9`, `MAX_LABEL_FONT_PX = 12`
(константы в начале `<script>`, легко подправить). Проверено: при сильном
приближении радиус фиксируется ровно на 9px, шрифт — ровно на 12px,
дальнейший zoom их не увеличивает.

## 12. Пользователь-администратор по умолчанию — ✅ реализовано

`app/schema.sql` сеет пользователя "Администратор" (`domain_login=admin`,
роль `admin`, пароль не задан) при первой инициализации БД. Пароль
устанавливается тем же механизмом, что и для остальных пользователей
(`POST /users/{id}/set-password`), плюс self-service смена собственного
пароля доступна любому залогиненному пользователю (кнопка "Пароль" в
тулбаре) без необходимости прав администратора.

---

## Новое: найдено при тестировании (не входило в исходный список из 12 пунктов)

### 13. Утечка форматирования MTEXT в марке (найдено 2026-07-14)

При разводке подписей на реальном файле `260713_Чертежи для WEB_2.dxf`
обнаружена марка вида `{\C10;9КН3}` — это код цветового форматирования
MTEXT (color override), который не был вырезан при извлечении текста
марки через выноску (`MULTILEADER.get_mtext_content()` в
`scripts/parse_zhbi.py`). Затрагивает не только отображение на схеме, но
и все выгрузки/экспорты, где используется поле `mark`. Нужно отдельно
разобраться, для скольких элементов в реальных файлах это актуально, и
почему `ezdxf` не стрипает этот конкретный паттерн форматирования
(возможно, нужна ручная постобработка регулярным выражением поверх
`get_mtext_content()`).

---

## Сводка по статусу

| № | Тема | Готовность |
|---|---|---|
| 1 | Хранение истории статуса | ✅ Готово |
| 2 | Дата в XLS-экспорте истории | ✅ Готово |
| 3 | Импорт истории из XLS | ✅ Готово |
| 4 | Коллизии подписей | ✅ Готово |
| 5 | Предел размера шрифта при zoom | ✅ Готово |
| 6 | Подсветка выбранного элемента | ✅ Готово |
| 7 | Видимость подписей по типу элемента | ✅ Готово |
| 8 | Пользователи и роли | ✅ Готово |
| 9 | Меню "Настройки" | ✅ Готово |
| 10 | Экспорт/импорт настроек | ✅ Готово |
| 11 | Предел размера маркеров при zoom | ✅ Готово |
| 12 | Админ по умолчанию, управление паролем | ✅ Готово |
| 13 | Утечка форматирования MTEXT в марке | Не начато (новый пункт) |

---

# Второй раунд замечаний (2026-07-15)

Нумерация — своя для этого раунда (1–12), не путать с п.1–13 выше.

## 1. Зафиксированные подписи осей: комфортный размер + прореживание при коллизии — ✅ реализовано

`updateAxisLabelSizing()` в `app/static/index.html`: размер шрифта подписи
оси капается в экранных пикселях (`MAX_AXIS_FONT_PX = 13`), как и у марок.
При мелком масштабе, если соседние подписи по минимальному шагу между
осями не помещаются без наложения, включается прореживание — динамически
считается шаг (каждая 2-я/3-я/... подпись) по формуле
`ceil(нужный_px / фактический_шаг_px)`, независимо для числовых и
буквенных осей. Сами линии сетки остаются видимы всегда, прореживаются
только подписи. Проверено визуально на общем виде схемы — вместо всех
39 числовых осей видны только "1, 11, 21, 29, 33/1, 38" при удалении.

## 2. Редизайн — современный UI в духе gosuslugi.ru — ✅ реализовано

Полностью переработан `<style>` в `app/static/index.html`: CSS-переменные
для акцентного синего (`--color-primary: #1353d6`), поверхностей, границ,
теней; скруглённые кнопки (`.btn`/`.btn-primary`/`.btn-secondary`) с
состояниями hover/focus; модалки с мягкой тенью и увеличенными отступами;
таблицы, дропдауны, чекбоксы — везде единая система. Тёмная тема
переведена на те же переменные (отдельный блок значений в
`@media (prefers-color-scheme: dark)`), проверена скриншотом — не менее
контрастна и читаема, чем светлая. Логин-форма и рабочая область
визуально согласованы (единый шрифт, скругления, акцентный цвет).

## 3. Контекстное меню — по правому клику/двойному клику, не по обычному — ✅ реализовано

`svg-root` теперь слушает три события отдельно: `click` — только
`selectElement()` (карточка в сайдбаре, подсветка), без контекстного
меню; `dblclick` и `contextmenu` (с `preventDefault()`, чтобы не всплывало
нативное меню браузера) — открывают контекстное меню смены статуса.
Проверено прямым `dispatchEvent` нативных `dblclick`/`contextmenu`
событий на элементе — оба корректно открывают меню; обычный клик
подтверждённо не открывает.

## 4. Экспорт в XLS/PDF — перенесены в "Настройки → Обмен данными" — ✅ реализовано

Кнопки "Экспорт в XLS" и "Экспорт в PDF" убраны из тулбара и перенесены в
выпадающее меню "Настройки" отдельным подразделом "Обмен данными"
(визуальный разделитель `.dropdown-divider` над ними), вместе с уже
существовавшими "Импорт истории из XLS" и "Экспорт/импорт настроек". В
тулбаре из операционных кнопок остались только "Загрузить чертёж" (частое
действие) и сам "Настройки ▾".

## 5. Закрытие всех форм настроек по Esc — ✅ реализовано

Общий `keydown`-обработчик на `document`: по `Escape` закрываются все
открытые `.modal-backdrop.open`, контекстное меню статуса и выпадающее
меню "Настройки". Проверено на реальном примере (открыт экран
"Контракты" → Esc → закрылся).

## 6. Удвоить максимальный размер маркеров и подписей при zoom — ✅ реализовано

`MAX_MARKER_PX` 9→18, `MAX_LABEL_FONT_PX` 12→24. Проверено численно при
сильном приближении: радиус маркера ровно 18px, шрифт подписи ровно 24px
на экране — предел работает, не просто увеличенное базовое значение.

## 7. Выпадающий список пользователей на форме входа — ✅ реализовано

Новый публичный эндпоинт `GET /login-users` (`app/auth.py`, без
авторизации — так и должно быть, форма входа работает до логина) отдаёт
только ФИО и `domain_login`, без ролей/паролей/прочих полей — сознательно
урезанная версия полного `/users` (тот admin-only). `#login-domain` стал
`<select>`, заполняется этим списком при показе экрана входа.

**Компромисс, о котором стоит знать**: список валидных логинов теперь
виден без аутентификации — приемлемо для внутреннего инструмента, но
станет проблемой, если сервис когда-нибудь будет доступен из интернета
широкому кругу (упрощает подбор пароля по известному логину). Отмечено
в коде.

## 8. Рабочая дата в тулбаре — ✅ реализовано

Уточнено и сделано так, как согласовали: дата **и точное время**
(`<input type="datetime-local">`), значение запоминается в
`localStorage` между заходами (не сбрасывается на "сегодня" каждый раз).
Кнопка "Сейчас" сбрасывает на текущий момент. Пока рабочая дата
установлена, поле подсвечивается акцентным цветом — чтобы не забыть, что
работаешь задним числом. При смене статуса элемента (и только тогда —
привязка контракта к элементу временем не датируется) выбранное значение
передаётся как `changed_at` в `PATCH /elements/{id}/status`.

**Важный смежный баг, найденный и исправленный по ходу**: `current_status`
элемента раньше слепо выставлялся в значение только что отправленного
статуса. Если рабочая дата — в прошлом, раньше уже существующей более
поздней записи в истории, это было НЕВЕРНО (задний статус "откатывал"
текущий). Исправлено: `current_status` теперь всегда пересчитывается по
самой поздней по `changed_at` записи в истории, та же логика, что уже
использовалась при импорте истории из XLS (`app/history_import.py`).
Проверено сценарием: элемент переведён в "Смонтирован" сегодня, затем
задним числом (2020 год) отправлен статус "Отгружен" — `current_status`
корректно остался "Смонтирован".

## 9. Контроль контрактов — ✅ реализовано

Обсуждено и реализовано согласно ответам:
- **Точка учёта "факта"**: любой статус элемента, кроме "Запланирован".
- **Привязка элемента к контракту**: контракт по умолчанию задаётся на
  раздел "Настройки → Контракты" (по типу элемента), подставляется
  автоматически при первом уходе элемента со статуса "Запланирован", если
  у элемента ещё нет контракта. Всегда можно переназначить вручную —
  выпадающий список прямо в карточке элемента (раздел "Контракт"),
  отфильтрованный по совпадающему типу.
- **Предупреждение**: и отдельный экран "Контракты" (`Настройки →
  Контракты`) с таблицей план/факт/остаток, превышение подсвечено красным
  построчно; и неблокирующий тост в правом верхнем углу схемы сразу после
  действия, из-за которого контракт превышен.

Бэкенд — `app/contracts.py` (`GET/POST/PATCH /contracts`, `GET/PUT
/contracts/default-map`), новые таблицы `contracts`/`default_contracts`,
колонка `elements.contract_id` (миграция). Проверено сквозным сценарием:
контракт на 2 шт., статус выставлен 3 элементам одного типа → на третьем
корректно появляется тост "законтрактовано 2, отмечено на схеме 3", в
таблице контрактов `fact=3, remaining=-1, exceeded=true`.

## 10. Изменяемая ширина сайдбара — ✅ реализовано

Перетаскиваемый разделитель `#resize-handle` между схемой и сайдбаром,
ширина сохраняется в `localStorage` (`zhbi_sidebar_width`) и
восстанавливается при следующей загрузке страницы. Ограничена диапазоном
220–600px, чтобы не схлопнуть/не раздуть до неюзабельного состояния.
Проверено: перетаскивание на 100px увеличило сайдбар с 320 до 423px,
значение сохранилось.

## 11. Группировка свойств элемента в карточке по темам — ✅ реализовано

Карточка элемента теперь выводится блоками с подзаголовками: **Идентификатор**
(id, dxf handle, слой), **Тип и марка** (тип, марка, источник марки),
**Адрес по осям** (адрес, статус адресации, оси, смещения), **Статус**
(текущий статус) + отдельный блок **Контракт** (выпадающий список,
описан в п.9), **Координаты** (x, y). Точная разбивка — по логике полей,
можно скорректировать по фидбэку после первого просмотра.

## 12. Логотип компании на странице входа и в рабочей области — ✅ частично реализовано

Логотип (`app/static/logo.svg`, скачан с mcy-1.ru и сохранён локально —
не тянется с внешнего сайта при каждом открытии) добавлен на страницу
входа в натуральном размере (169×37, "масштаб 100%" — это исходный размер
SVG, без увеличения/уменьшения) и маленьким значком (высота 24px,
пропорционально) слева в тулбаре рабочей области. `#login-box` сделан
белым в обеих темах (тёмная/светлая) — логотип тёмно-бордовый, на тёмном
фоне терялся бы контраст.

Не реализовано: выпадающий список пользователей на форме входа (это
отдельный п.7 этого раунда, не про логотип) — сейчас ещё текстовое поле.

## 13. Экспорт схемы в PDF как отчёт с легендой цветов — ✅ реализовано (2026-07-15)

Шаблон согласован в обсуждении и реализован в `app/pdf_export.py`
(`GET /export.pdf?source_file=...&date=...`, кнопка "Экспорт в PDF" в
тулбаре):
- **Шапка**: логотип компании (встроен как вектор через `svglib` — не
  растеризован, чёткий на любом масштабе печати) + "Статус по <чертёж>" +
  дата отчёта. Дата — выбираемая в модалке экспорта; если указана,
  статусы элементов считаются актуальными на эту дату (тот же принцип,
  что у режима "снимок на дату" в XLS-экспорте) — элементы, которых на
  эту дату ещё не существовало, показаны нейтральным серым, а не текущим
  статусом задним числом.
- **Подпись/футер**: автоподпись системы ("Сформировано автоматически
  системой ЖБИ — контроль поставки и монтажа, <дата/время> (<кто
  сформировал>)") и блок для ручной подписи (Составил/Проверил/Утвердил,
  пустые строки под ФИО и дату).
- **Содержимое схемы**: весь чертёж целиком, авто-fit по осям и
  элементам, не зависит от масштаба на экране пользователя на момент
  экспорта.
- **Формат**: альбомная A3, подписи марок показаны, легенда цветов
  статусов отдельной строкой над футером.

**Важный технический момент, найденный при тестировании**: стандартные
PDF-шрифты (Helvetica и т.п.) не содержат кириллицу — без явного
шрифта весь русский текст в отчёте отображался чёрными прямоугольниками.
Исправлено регистрацией DejaVu Sans (свободная лицензия, полная
поддержка кириллицы, зашита в репозиторий — `app/fonts/`, ~2МБ) через
`reportlab.pdfbase.ttfonts`. Это гарантирует одинаковый результат
независимо от того, какие шрифты установлены на сервере развёртывания —
раньше эта проблема могла остаться незамеченной на macOS/Windows (там
обычно есть системный Arial с кириллицей), но проявилась бы на "голом"
Linux-сервере без такого шрифта.

---

## Сводка по второму раунду

| № | Тема | Готовность |
|---|---|---|
| 1 | Фиксированный размер подписей осей + прореживание | ✅ Готово |
| 2 | Редизайн под gosuslugi.ru | ✅ Готово |
| 3 | Контекстное меню по ПКМ/двойному клику | ✅ Готово |
| 4 | Экспорт в XLS/PDF → меню "Настройки/Обмен данными" | ✅ Готово |
| 5 | Esc закрывает формы настроек | ✅ Готово |
| 6 | Удвоить предел размера маркеров/подписей | ✅ Готово |
| 7 | Выпадающий список пользователей на входе | ✅ Готово |
| 8 | Рабочая дата в тулбаре | ✅ Готово |
| 9 | Контроль контрактов | ✅ Готово |
| 10 | Изменяемая ширина сайдбара | ✅ Готово |
| 11 | Группировка свойств элемента по темам | ✅ Готово |
| 12 | Логотип компании | ✅ Готово |
| 13 | Экспорт схемы в PDF-отчёт | ✅ Готово |

---

# Третий раунд замечаний (2026-07-15)

Нумерация — своя для этого раунда (1–10), не путать с предыдущими раундами.
Перед реализацией проведено обсуждение (два уточняющих вопроса, ответы см.
в п.4 и п.5 ниже), как и попросил заказчик.

## 1. Меню "Настройки" открывалось под схемой — ✅ исправлено

Причина: `.dropdown-menu` был `position: absolute` внутри `#toolbar`, а у
`#toolbar` стоял `overflow-x: auto; overflow-y: hidden;` (добавлено в
предыдущем раунде, чтобы кнопка "Выйти" не обрезалась на узких экранах) —
это же правило обрезало и выпадающее меню по нижней границе тулбара.
Исправлено переводом `.dropdown-menu` на `position: fixed` с координатами,
вычисляемыми в JS при открытии (тот же приём, что уже использовался для
`#ctx-menu`) — меню больше не зависит от overflow родителя. Проверено через
`document.elementFromPoint()` в точке пункта меню — до исправления возвращал
`<svg>` (клик проваливался сквозь), после — корректно возвращает сам пункт
меню.

## 2-3. Контроль объёмов контрактации / выбор контракта по умолчанию не видны — ✅ подтверждено, что это следствие п.1

Оба экрана ("Контракты" с планом/фактом/остатком и назначением контракта по
умолчанию по типу элемента) были реализованы в предыдущем раунде и работали
корректно — они были просто недостижимы через обрезанное меню "Настройки"
(см. п.1). После исправления п.1 оба экрана открываются и работают, включая
таблицу контрактов и селекторы контракта по умолчанию — проверено.

## 4. Контракт не спрашивается при уходе со статуса "Запланирован" — ✅ реализовано

Обсуждено: раньше контракт по умолчанию подставлялся молча. Заказчик
подтвердил, что нужен активный диалог при каждой смене СО статуса
"Запланирован" (а не молчаливая подстановка). Реализовано: при выборе
любого статуса, отличного от "Запланирован", для элемента, у которого
`current_status === "planned"`, вместо немедленного применения статуса
открывается модальное окно "Выбор контракта" с выпадающим списком
(предзаполнен контрактом по умолчанию для типа элемента, если он задан) —
статус применяется только после подтверждения. При последующих сменах
статуса у уже "непланового" элемента диалог не переспрашивает (элемент уже
один раз прошёл эту точку) — проверено сценарием: колонна
"Запланирован → В производстве" (диалог показан, статус не менялся до
подтверждения) → "В производстве → Отгружен" (диалог НЕ показан, статус
сразу применился).

## 5. Подписи осей не зафиксированы при скроллинге/масштабировании — ✅ реализовано

Уточнено: в предыдущем раунде (see раунд 2, п.1) был реализован только
предел размера шрифта + прореживание при коллизии, но не само закрепление
подписей на границах видимой области, как изначально просили ("зафиксировать
наименование осей на границах рабочей области"). Заказчик подтвердил: нужны
подписи, всегда видимые у края экрана, как замороженные заголовки
строки/столбца в таблице, независимо от прокрутки.

Реализовано в `updateAxisLabelSizing()`: на каждый вызов `setView()`
(колесо мыши/перетаскивание) видимая в мировых координатах область
вычисляется из `state.view`, подписи числовых осей закрепляются на
постоянном отступе в пикселях от верхней/нижней границы видимой области
(верхняя и нижняя копия), подписи буквенных осей — от левой границы;
отступ считается в мировых единицах динамически из текущего масштаба
(`px / pxPerUnit`), поэтому визуально остаётся постоянным при любом zoom.
Видимость подписи (нужно ли её вообще показывать) и прореживание при
коллизии теперь считаются только по подмножеству осей, реально видимых в
текущем видовом окне — раньше это считалось по всем осям чертежа сразу.
Линии сетки по-прежнему рисуются на всю длину, закрепление касается только
текстовых подписей. Проверено численно через CDP: при zoom+pan в середину
чертежа и последующей прокрутке ещё дальше подписи остаются на одном и том
же экранном Y (≈85.6px от верха рабочей области) и X (≈15px от левого края)
во всех трёх проверенных положениях просмотра, при этом сам набор видимых
подписей (какие именно оси показаны) корректно меняется под текущую
прокрутку.

## 6. Схема "подпрыгивает" при клике по элементу — ✅ исправлено (найдена настоящая причина)

При диагностике найдены и исправлены **две независимые причины**:

1. **Отсутствие порога перед перетаскиванием**: реальный клик мышью почти
   всегда сопровождается микро-дрожанием на 1-2px между `mousedown` и
   `mouseup`, а обработчик перетаскивания схемы трактовал любое движение
   мыши как намеренный pan. Добавлен порог `DRAG_THRESHOLD_PX = 4` — schema
   не двигается, пока движение мыши не превысит порог; клик с обычным
   дрожанием больше не приводит к микро-панорамированию. Дополнительно клик
   на `svg-root`, случившийся сразу после настоящего перетаскивания (движение
   ≥ порога), больше не выбирает элемент под финальной точкой курсора.

2. **Более существенная причина**: `#app-root` не был полноценным
   flex-контейнером — CSS содержал только `#app-root.hidden { display: none; }`,
   без базового `display: flex`. Из-за этого `#body { flex: 1 }` не имел
   эффекта (родитель не flex), и высота `#stage`/`#sidebar` фактически
   определялась содержимым сайдбара, а не вьюпортом: при выборе элемента
   карточка в сайдбаре становилась выше (доп. поля, история статусов,
   выпадающий список контракта) — и **вся рабочая область реально меняла
   размер**, а `<svg preserveAspectRatio="xMidYMid meet">` пересчитывал
   позицию содержимого под новый размер бокса — отсюда ощущение "подпрыгивает
   и возвращается обратно" даже при клике без единого пикселя перетаскивания.

   Исправлено добавлением `#app-root { display: flex; flex-direction: column; height: 100vh; overflow: hidden; }`.
   Проверено численно: до исправления `getBoundingClientRect()` у `#svg-root`
   менял высоту с 811px до 1098px при обычном клике по элементу (без
   единого пикселя движения мыши); после исправления bounding rect
   `#svg-root` остаётся идентичным на всех этапах — выбор элемента,
   открытие контекстного меню, диалог контракта, подтверждение статуса.

## 7. Кнопка "Сейчас" не выполняла видимого действия — ✅ исправлено

Кнопка технически работала и до исправления (очищала рабочую дату,
`localStorage`), но само по себе опустошение уже могло выглядеть пустым —
никакой видимой обратной связи не было. Добавлена короткая текстовая
вспышка рядом с кнопкой ("Сброшено"), которая появляется на 1.2 секунды при
клике — теперь нажатие явно заметно.

## 8. Кнопка "Загрузить чертёж" перенесена к полю выбора чертежа — ✅ реализовано

Переставлена в DOM сразу после `#source-select`, до разделителя `.spacer` —
теперь обе кнопки, относящиеся к выбору/загрузке чертежа, находятся рядом
в левой части тулбара.

## 9. Итоговое количество элементов в разделе "Статусы" — ✅ реализовано

`renderLegend()` добавляет строку "Итого" с суммой по всем статусам, визуально
отделённую верхней границей. Проверено: сумма построчных значений (331+1+0+0+0+0)
совпадает со строкой "Итого 332" и с фактическим числом элементов чертежа.

## 10. Смена пароля перенесена из тулбара в меню "Настройки" — ✅ реализовано

Кнопка "Пароль" убрана из `#user-box`. Само меню "Настройки" перестало быть
видимым только администратору целиком — теперь оно доступно всем ролям
(иначе пользователи с ролью "Пользователь"/"Просмотр" потеряли бы
самообслуживание смены пароля), а админ-специфичные пункты (цвета статусов,
видимость подписей, пользователи, контракты, экспорт/импорт) помечены
классом `.admin-only` и скрываются адресно по роли — как и раньше, доступны
только администратору. Пункт "Сменить пароль" — первый в списке, доступен
всем ролям.

---

## Сводка по третьему раунду

| № | Тема | Готовность |
|---|---|---|
| 1 | Меню "Настройки" обрезалось под схемой | ✅ Готово |
| 2 | Контроль объёмов контрактации не виден | ✅ Готово (следствие п.1) |
| 3 | Выбор контракта по умолчанию не виден | ✅ Готово (следствие п.1) |
| 4 | Активный диалог контракта при уходе со статуса "Запланирован" | ✅ Готово |
| 5 | Подписи осей закреплены на границах рабочей области | ✅ Готово |
| 6 | Схема "подпрыгивает" при клике | ✅ Готово (2 независимые причины) |
| 7 | Кнопка "Сейчас" без видимой обратной связи | ✅ Готово |
| 8 | "Загрузить чертёж" рядом с выбором чертежа | ✅ Готово |
| 9 | Итого элементов в разделе "Статусы" | ✅ Готово |
| 10 | Смена пароля — в меню "Настройки" | ✅ Готово |

---

# Четвёртый раунд замечаний (2026-07-15)

Нумерация — своя для этого раунда (1–14). Перед реализацией проведено
обсуждение по архитектурно значимым развилкам (см. п.1/2, п.7, п.8, п.13,
п.9 ниже) — вопросы и ответы см. в истории разработки.

## 1-2. Статус "Контрактация" + наследование контракта следующими статусами — ✅ реализовано

Новый статус `contracting` ("Контрактация") добавлен между "Запланирован" и
"В производстве" в `STATUS_ORDER`/`STATUS_LABELS_RU` (`app/models.py`), свой
цвет по умолчанию (`#eab308`). Уточнено в обсуждении: прямой переход
"Запланирован" → любой статус ДАЛЬШЕ "Контрактации" (пропуская её) —
**разрешён**, диалог выбора контракта показывается в любом случае (тот же
диалог, что и раньше — условие показа не изменилось: уход со статуса
"planned"). Предзаполнение диалога — из `element.contract_id` (кэш
последнего известного контракта), а не только из карты "по умолчанию".

Наследование контракта всеми статусами ПОСЛЕ контрактации реализовано на
уровне БД: `contract_id` теперь хранится в каждой записи `status_history`
(`app/contracts.py:resolve_contract_for_new_row`) — если контракт не выбран
явно в диалоге, новая запись наследует `contract_id` от самой свежей
предыдущей записи истории этого элемента, где он был задан. Проверено
сценарием: элемент явно получает контракт на "Контрактации" → следующий
переход "В производстве" без диалога (не первый уход с "Запланирован") →
`contract_id` сохранился неизменным.

## 3. Удаление записи из истории статусов — ✅ реализовано

`DELETE /elements/{id}/history/{history_id}` (`app/main.py`), кнопка "✕" у
каждой строки истории в карточке элемента (только для ролей admin/user).
После удаления `current_status` и кэш `contract_id` у элемента
пересчитываются по самой поздней ПО ВРЕМЕНИ оставшейся записи (та же логика,
что при обычной смене статуса) — корректно работает и при удалении записи
не из конца истории. Защита от обнуления: если у элемента осталась ровно
одна запись истории, её удаление отклоняется (400) — история никогда не
становится полностью пустой. Проверено: удаление записи "Контрактация" из
середины истории корректно пересчитало `current_status` на актуальный более
поздний статус.

## 4. Подписи элементов не должны накладываться при масштабировании — ✅ улучшено

На присланном скриншоте видно, что при экстремальной плотности (3-4 круга
почти в одной точке) ни одно из 6 кандидатных направлений подписи (см. п.4
первого раунда) не может не пересекаться — математически не хватает места.
Добавлено прореживание по аналогии с подписями осей (`updateLabelCollisionVisibility()`
в `app/static/index.html`): при каждом изменении масштаба подписи, чьи
экранные боксы (в уже выбранном для них направлении) пересекаются с ранее
размещёнными, скрываются; по мере приближения боксы "сжимаются" (шрифт
считается в мировых единицах, кэпается по экранным пикселям — та же формула,
что и раньше), поэтому часть подписей естественно открывается без отдельной
логики. Проверено численно: на плотной реальной сетке (332 колонны) при
среднем зуме скрыто 38 из 332 подписей, при полном виде чертежа — тоже 38
(геометрия коллизий не зависит от абсолютного масштаба, только от
относительной плотности).

**Побочно найдено и исправлено**: на том же скриншоте виден старый
известный баг из бэклога первого раунда (п.13, тогда отложен) — марка
`{\C10;9КН3}` вместо `9КН3` (утечка кода форматирования MTEXT из
выноски-мультилидера). Исправлено в `scripts/parse_zhbi.py`: сырой текст из
`MULTILEADER.get_mtext_content()` теперь пропускается через
`ezdxf.tools.text.plain_mtext()`, которая как раз и предназначена для
удаления таких inline-кодов. Проверено на реальном файле — 0 марок с
остаточными кодами форматирования (было минимум 1 подтверждённое на
скриншоте).

## 5, 13. Выбор произвольного набора слоёв внутри файла и совмещение нескольких файлов — ✅ реализовано

Согласовано в обсуждении: выбор — чекбоксами (несколько слоёв/файлов сразу),
сеансовый (не сохраняется ни на сервере, ни в `localStorage` — сбрасывается
на дефолт при каждой перезагрузке страницы). П.13 реализован как обобщение
п.5 — один и тот же механизм: `state.selection` — `Map(source_file → Set(layer) | null)`,
`null` означает "все слои файла".

Поле выбора чертежа в тулбаре стало выпадающей панелью (`#file-select-dropdown`):
верхний уровень — чекбоксы файлов (`GET /source-files`), под каждым отмеченным
файлом — чекбоксы его слоёв (`GET /layers?source_file=...`, новый эндпоинт).
`GET /plan-data` заменён на `POST /plan-data` с телом
`{selection: [{source_file, layers}, ...]}` — элементы и сетки осей всех
выбранных (файл, слои) объединяются в одну схему, bbox/радиус маркера
считаются по объединённому набору точек. Экспорт в XLS/PDF и импорт истории
остались одно-файловыми операциями (используют "основной" файл —
последний включённый в выборке), это не менялось.

## 6. Кнопка "Сейчас" по-прежнему не заполняла рабочую дату — ✅ исправлено (верно на этот раз)

В предыдущем раунде было ошибочно реализовано как "очистить поле + вспышка
подтверждения" — заказчик явно поправил: кнопка должна ЗАПОЛНЯТЬ поле
текущими датой и временем, не очищать его. Исправлено буквально по
формулировке: `workdate-reset` теперь пишет в поле `<input type="datetime-local">`
реальные текущие дату/время (`new Date()`), а не сбрасывает на пусто.

## 7. Поле "Контракт" у элемента — только для чтения — ✅ реализовано

Выпадающий список в карточке элемента убран, вместо него — обычный текст
(название + поставщик), вычисляемый из `element.contract_id`. Уточнено в
обсуждении: контракт хранится в каждой записи истории (см. п.1-2), поэтому
`elements.contract_id` теперь — денормализованный кэш, всегда равный
`contract_id` самой последней по `changed_at` записи истории
(`app/contracts.py:recompute_element_contract_cache`, пересчитывается при
каждой смене статуса и при удалении записи истории). Отдельный эндпоинт
`PATCH /elements/{id}/contract` для прямого назначения удалён — единственный
способ поменять контракт элемента теперь — диалог подтверждения при смене
статуса (п.1-2).

## 8. Новая структура хранения контрактов: контракт + строки по типам — ✅ реализовано

Согласовано: `Контракт (название, поставщик)` → несколько строк
`(тип элемента, количество)` внутри него. Новые таблицы `contracts` (id,
name, supplier) + `contract_lines` (id, contract_id, element_type,
quantity, `UNIQUE(contract_id, element_type)`), взамен единственной таблицы
на строку "контракт=тип+кол-во" из второго раунда. "Факт"/"остаток"/
"превышение" теперь считаются по СТРОКЕ (пара contract_id+element_type), а
не по контракту целиком. Экран "Настройки → Контракты" перерисован
блоками — заголовок контракта (название+поставщик) с кнопкой "Изменить",
под ним таблица его строк (план/факт/остаток, превышение подсвечено).
Форма редактирования — динамический список строк (кнопка "+ строка",
удаление строки крестиком).

**Миграция существующих данных** (`app/db.py:_migrate_contracts_structure`):
старые строки группируются в новые контракты по `(name, supplier)`, ссылки
из `elements.contract_id` и `default_contracts.contract_id` переносятся на
новые id. **Найден и исправлен реальный баг при разработке миграции**:
`ALTER TABLE contracts RENAME TO contracts_old` в SQLite молча переписывает
FK-констрейнты в ДРУГИХ таблицах (`elements.contract_id`,
`default_contracts.contract_id`), которые ссылались на `contracts`, — они
начинают ссылаться на `contracts_old`. Без отключения `PRAGMA foreign_keys`
на время миграции последующий `DROP TABLE contracts_old` запускает
`ON DELETE SET NULL` по этим констрейнтам и обнуляет то, что миграция
только что аккуратно перенесла на новые id — воспроизведено и подтверждено
эмпирически в изолированном тесте до исправления, после исправления
`PRAGMA foreign_key_check` не находит нарушений, а перенесённые ссылки
сохраняют правильные значения.

## 9. Формат дат ДД.ММ.ГГГГ во всех формах — ✅ подтверждено, изменений не потребовалось

Обсуждено: решили полагаться на локаль системы, а не переводить все
`<input type="date">`/`<input type="datetime-local">` на кастомные
текстовые поля с маской (это был бы существенный объём работы на ~6+ полей
по всему приложению). Формат отображения нативных полей определяется
локалью браузера/ОС, не HTML/CSS — если локаль русская, поля уже
показывают ДД.ММ.ГГГГ без дополнительного кода.

## 10. Полосы прокрутки рабочей области — ✅ реализовано

Кастомные (не нативные — нативный overflow-скролл не применим к
SVG-схеме, управляемой через `viewBox`) вертикальная и горизонтальная
полосы прокрутки в правом и нижнем краю рабочей области
(`#vscroll`/`#hscroll` + перетаскиваемые ползунки). Функциональные:
перетаскивание ползунка панорамирует схему, клик по треку мимо ползунка —
прыжок к этой позиции. Ползунок отражает текущий видовой прямоугольник
(`state.view`) относительно вида "вся схема целиком" (`state.initialView`)
и скрывается, когда весь охват уже виден (нечего прокручивать). Проверено:
при масштабировании до 250% ползунки корректно появляются и меняют
размер/положение, при сбросе на 100% (вид умещает всю схему) — скрываются.

## 11. Выбор формы маркера по (слой, тип элемента) — ✅ реализовано

Настройка буквально по описанию — группировка по ПАРЕ (слой, тип элемента),
не только по типу. Новая таблица `element_shapes` (layer, element_type,
shape; отсутствие строки = круг по умолчанию). Экран "Настройки → Форма
маркеров" (`GET /layer-type-combinations`, `PUT /element-shapes`, только
admin) — по одному выпадающему списку на каждую встреченную в данных пару
слой/тип, с вариантами круг/квадрат/треугольник/ромб/шестиугольник.
Рендеринг схемы (`app/static/index.html`) переведён с жёстко зашитого
`<circle>` на диспетчер по имени формы (`SHAPE_TAGS`/`shapeGeometryAttrs`) —
круг рисуется как раньше, остальные формы — как `<polygon>`/`<rect>` с
геометрией, пересчитываемой при каждом изменении масштаба той же функцией,
что и раньше кэпала радиус круга. Проверено: смена формы на "треугольник"
в настройках сразу отражается на схеме (маркер элемента стал `<polygon>` с
тремя вершинами).

## 12. Индикатор текущего зума + кнопка сброса на 100% — ✅ реализовано

В правом нижнем углу рабочей области — текущий масштаб в процентах и кнопка
сброса. Уточнено (без вопроса пользователю, помечено как разумное умолчание):
"100%" — это вид "вся схема целиком" (как при первом открытии чертежа), а не
буквальный масштаб 1px=1мм чертежа (для строительных объектов такой
масштаб был бы бессмысленно крупным приближением). Кнопка сброса
возвращает `state.view` к `state.initialView`. Проверено: при зуме до 250%
индикатор показывает "250%", после сброса — ровно "100%".

## 14. Убрать роль пользователя из панели — ✅ реализовано

`#user-box` теперь показывает только имя пользователя, без "(роль)" в
скобках рядом с ним.

---

## Сводка по четвёртому раунду

| № | Тема | Готовность |
|---|---|---|
| 1-2 | Статус "Контрактация" + наследование контракта | ✅ Готово |
| 3 | Удаление записи истории статусов | ✅ Готово |
| 4 | Подписи элементов не накладываются при масштабировании | ✅ Готово (+ попутно исправлен старый баг MTEXT) |
| 5, 13 | Выбор слоёв внутри файла + совмещение нескольких файлов | ✅ Готово |
| 6 | Кнопка "Сейчас" действительно заполняет дату/время | ✅ Готово |
| 7 | Контракт у элемента — только для чтения | ✅ Готово |
| 8 | Новая структура контрактов (контракт + строки) | ✅ Готово |
| 9 | Формат дат ДД.ММ.ГГГГ | ✅ Подтверждено (полагаемся на локаль) |
| 10 | Полосы прокрутки рабочей области | ✅ Готово |
| 11 | Форма маркера по (слой, тип элемента) | ✅ Готово |
| 12 | Индикатор зума + сброс на 100% | ✅ Готово |
| 14 | Убрать роль из панели пользователя | ✅ Готово |

---

# Запланировано (не начато)

## Автоматическое распознавание новых слоёв при загрузке DXF

**Найдено**: 2026-07-15, при разборе `260714_Чертежи для WEB.dxf` — в файле
оказалось 5 слоёв, но система "видела" только один (колонны), т.к. список
распознаваемых слоёв жёстко зашит в `LAYER_CONFIG`
(`scripts/parse_zhbi.py`) — слои, отсутствующие в этом словаре, тихо
пропускаются без ошибок и предупреждений. Слой ригелей в этом файле
пришлось прописывать вручную (см. запись о ригелях в четвёртом раунде
выше) — то же самое повторится для любого следующего файла с новыми
именами слоёв.

**Согласовано с заказчиком**: сделать распознавание существенно более
автоматическим, а не просто предупреждать о пропущенных слоях. План:

1. При загрузке DXF показывать пользователю **все слои файла, содержащие
   геометрию**, а не только уже распознанные — включая слои, которых нет в
   `LAYER_CONFIG`.
2. Для слоёв, похожих на элементы (блоки-вставки, или много однотипных
   замкнутых контуров схожего размера — та же эвристика, что уже
   используется для колонн/ригелей), предлагать включить их в импорт одним
   кликом, без правки кода.
3. **Тип элемента по умолчанию — имя самого слоя** (не нужно заранее знать
   слово "column"/"beam" и т.п. — это просто строка, используется только
   для отображения, счётчиков и назначения формы маркера), с возможностью
   переименовать в интерфейсе.
4. Поиск марки — по уже обобщённой логике (атрибут блока → ближайшая
   выноска LEADER/MULTILEADER → ближайший "голый" текст без выноски),
   применимой к любому слою, а не только к заранее прописанным.
5. Слои, явно НЕ похожие на элементы (мало объектов, или несколько очень
   крупных контуров — типичный признак рамки листа/осей/размеров),
   по умолчанию не предлагать — чтобы не замусорить базу линиями осей,
   штриховкой и служебными пометками (`Defpoints`, `-- РАЗМЕРЫ` и т.п.) со
   статусом "запланирован".

**Статус**: не начато, запланировано на будущую итерацию.

---

## Отображение ригелей на схеме реальной формой (вытянутый прямоугольник) — ✅ реализовано (2026-07-15)

Запрошено сразу после появления поддержки ригелей: показывать их на схеме
не условным маркером фиксированного размера, а настоящим контуром — так же,
как они выглядят в исходном DXF.

Реализовано как расширение уже существующей настройки "Форма маркеров"
(`Настройки → Форма маркеров`) новым вариантом **"как в оригинале
(контур)"**, а не отдельной хардкод-веткой для ригелей — работает для
любого слоя/типа, где есть сохранённая геометрия:

- При парсинге (`scripts/parse_zhbi.py`) элементы, извлечённые из
  LWPOLYLINE (не из INSERT-блока), теперь сохраняют список вершин контура
  в мировых координатах (`ElementRecord.outline`) — у элементов из
  INSERT-блоков контура нет (`None`), т.к. их геометрия не извлекается.
- Новая колонка `elements.outline_json` (миграция), JSON-список точек.
  Заполняется/обновляется при каждом импорте (в т.ч. повторном — старые
  элементы получают контур "задним числом" при переучёте того же файла).
- `/plan-data` отдаёт распакованный `outline` в каждом элементе.
- На схеме: если элементу назначена форма "outline" и контур есть —
  рисуется `<polygon>` напрямую по мировым координатам, без привязки к
  условному радиусу маркера — масштабируется вместе со всей схемой через
  `viewBox`, как сетка осей, а не пересчитывается на каждый zoom, в отличие
  от круга/квадрата/etc. Если "outline" назначен, а контура нет (например,
  колонна из INSERT-блока в другом файле) — тихий откат на круг, без
  ошибки.

Проверено на `260714_Чертежи для WEB.dxf`: ригели отображаются
вытянутыми прямоугольниками точно по сетке осей (визуально соответствует
исходному чертежу), колонны без назначенной формы остаются кружками;
геометрия не искажается при масштабировании; на файле без сохранённого
контура (`sample.dxf`, элементы из INSERT-блоков) форма "outline"
корректно откатывается на круг без ошибок.

---

## Исправления по безопасности (найдено при подготовке `Docs/ARCHITECTURE.md`, 2026-07-15)

**✅ Все 3 пункта ниже реализованы 2026-07-17** — см. раздел
"Исправления по безопасности — ✅ реализовано (2026-07-17)" ближе к
концу файла для деталей реализации/проверки. Текст ниже оставлен как
исходный план/анализ риска для истории.

Найдено при документировании архитектуры для анализа безопасности —
полный разбор см. `Docs/ARCHITECTURE.md`, раздел 9. Ниже — то, что
было предложено исправить (план на момент обнаружения).

### 1. Сохранённый XSS через `innerHTML` без экранирования

Марка элемента (текст, извлечённый из содержимого DXF-файла — то есть
контролируется тем, кто загружает чертёж, роль `user` или `admin`) и
ряд других полей (ФИО пользователя, название/поставщик контракта)
вставляются в DOM через `innerHTML` с шаблонными литералами без
экранирования (`app/static/index.html`, начиная с `fieldRowsHtml()` —
затрагивает порядка 13 мест использования `innerHTML`). Сама схема
(SVG) рендерится безопасно, через `textContent` — уязвима только
сайдбар-часть интерфейса (карточка элемента, таблица пользователей,
список контрактов, история статусов).

**Риск**: пользователь с правом загрузки DXF может внедрить payload в
марку элемента; при открытии карточки этого элемента другим
пользователем (включая `admin`) скрипт выполнится в его браузере и
сможет выполнять действия от его имени через `fetch()` к тому же origin
(cookie сессии `httponly`, поэтому прямая кража токена невозможна, но
эскалация действий — возможна).

**План исправления**: один хелпер экранирования (`escapeHtml`, замена
`& < > " '` на HTML-сущности), применить ко всем интерполяциям в
`innerHTML`-шаблонах, где значение приходит не от кода приложения, а
из БД/пользовательского ввода. Там, где вставляется единственное
текстовое значение внутрь уже готовой разметки — предпочесть замену на
`textContent` вместо экранирования (меньше риск регрессии).

### 2. Нет ограничения частоты попыток входа

`POST /login` не ограничен по частоте — ничего не мешает автоматическому
перебору паролей, кроме затрат на PBKDF2 (260к итераций замедляют
каждую попытку, но не останавливают целенаправленный перебор). Усугубляется
тем, что список валидных логинов публично доступен через
`GET /login-users` (уже осознанный компромисс ради удобства входа, см.
`app/auth.py`).

**План исправления**: простой лимит по IP и/или по `domain_login`
(например, экспоненциальная задержка или блокировка на N минут после K
неудачных попыток подряд) — без внешних зависимостей, счётчик можно
хранить в самой SQLite или в памяти процесса.

### 3. Cookie сессии без флага `secure`

`response.set_cookie(...)` в `app/auth.py` не выставляет `secure=True` —
cookie передаётся и по обычному HTTP, не только HTTPS. Приемлемо при
развёртывании за HTTPS-терминирующим прокси в закрытой сети, реальный
риск — при прямой публикации сервиса по HTTP на незащищённой сети.

**План исправления**: выставлять `secure=True` при определении, что
сервис работает через HTTPS (например, по переменной окружения/настройке
развёртывания), не ломая локальную разработку по HTTP.

**Статус всех трёх пунктов**: обсуждено, приоритет и порядок исправления —
по решению заказчика, не реализовано.

---

## Разбор структурированных имён слоёв DWG/DXF + привязка элементов к захваткам/кранам/стоянкам (поставлено 2026-07-15)

Новый стандарт именования слоёв зафиксирован заказчиком (см. текст задачи
целиком — ниже приведён в исходном виде для точности формулировок, детали
согласования см. в обсуждении после этой записи):

### Стандарт именования

**Слои ЖБИ:**
```
WEB_<Тип>[_<Подтип>][_ОТМ<знак><высота_мм_5цифр>]_<Роль>
```
- Тип: `Колонна` | `Ригель` | `Плита` | `Панель`
- Подтип: опционален (ярус для колонн; "Периметральный" для ригелей;
  Цоколь/ЛифтоваяШахта/ШахтаПодъемника для панелей) — справочник
  допустимых подтипов должен быть конфигурируемым списком, не зашитым в
  логику разбора (полный перечень ярусов ещё уточняется у заказчика).
- ОТМ: буква `П` (плюс) или `М` (минус) перед числом, без буквы — ноль;
  число — высота в мм, дополненная нулями слева до 5 знаков (примеры:
  `ОТМ00000`, `ОТМП15800`, `ОТММ02500`).
- Роль: `Элемент` | `Марка` (последний токен имени).

**Вспомогательные слои (зоны):**
```
WEB_доп_<Категория>[_ОТМ<знак><высота_мм_5цифр>]_<Роль>
```
- Категория: `Захватка` | `Кран` | `Стоянка`
- Захватка и Кран — без отметки (сквозные, на всю высоту здания)
- Стоянка — обязательно с отметкой (свой набор стоянок крана на каждом уровне)
- Роль: `Зона` | `Наименование`

### Требуемая логика

1. **Разбор имени слоя** — функция, принимающая имя слоя, возвращающая
   структуру: группа (`zhbi`/`zone`), тип, подтип (или отсутствует),
   отметка в мм со знаком (или отсутствует), роль. Строгая валидация:
   слой, начинающийся на `WEB_` и не подходящий ни под один из двух
   паттернов — не пропускать молча, явно сообщать об ошибке с именем
   проблемного слоя (чтобы сразу ловить отклонения от стандарта, когда
   проектировщики начнут переименовывать слои).

2. **Реестр зон** — прочитать геометрию (полигоны) из каждого слоя-"Зона",
   прочитать текстовые объекты из парного слоя-"Наименование",
   сопоставить название с полигоном по принципу "точка вставки текста
   лежит внутри полигона". Если текст не попал ни в один полигон или
   попал сразу в несколько — не гадать, помечать как требующее ручной
   проверки, выводить отдельным списком. Реестр зоны: идентификатор,
   категория, отметка (для стоянок), название, геометрия/границы полигона.

3. **Привязка элементов к зонам** — для каждого элемента ЖБИ определить
   захватку, кран, стоянку:
   - точечные элементы (колонна) — точка вставки внутри полигона зоны;
   - протяжённые/площадные элементы (ригель, плита, панель) — доля
     площади/длины элемента, попадающей в полигон зоны; присваивается
     зона с максимальным перекрытием; если максимальное перекрытие
     меньше ~50% или несколько зон дают близкий результат — статус
     "требует проверки", зона не присваивается автоматически;
   - для стоянок — дополнительно фильтровать по совпадению отметки
     элемента с отметкой слоя стоянки (элемент сравнивается только со
     стоянками своего уровня).
   - Результат на элемент (в дополнение к уже существующим марке/адресу
     по осям): тип, подтип, отметка (из имени слоя), захватка+статус,
     кран+статус, стоянка+статус.

4. **Тестовые данные** — сначала синтетические DXF (несколько
   зон-полигонов разной формы + несколько элементов: полностью внутри
   зоны, на границе между двумя зонами, в зоне без сопоставленного
   названия), проверить логику на них, и только потом пробовать на
   реальном чертеже — файла от заказчика, переименованного по новому
   стандарту, пока нет.

**Статус**: ✅ реализовано полностью (ядро логики + интеграция в
БД/пайплайн импорта/API/интерфейс), проверено на синтетических данных и
регрессией на всех реальных файлах старого стандарта.

### Согласованные решения по итогам обсуждения

1. Старый `LAYER_CONFIG` и новый строгий парсер сосуществуют параллельно —
   новый парсер применяется только к слоям с префиксом `WEB_`, реально
   подходящим под новую грамматику; старые файлы продолжают работать как
   работали.
2. Справочник допустимых подтипов — таблица в БД + экран настроек для
   `admin` (как цвета статусов/форма маркеров), не хардкод.
3. Метрика перекрытия для ригелей/плит/панелей — единая по площади
   (`intersection.area / element.area` через `shapely`), не отдельная
   метрика по длине для ригелей — используется уже сохранённый контур
   элемента (см. запись про ригели выше в этом же раунде).
4. Реализовывать сразу оба слоя — бэкенд-логику и отображение в
   интерфейсе (границы зон на схеме, захватка/кран/стоянка в карточке
   элемента) — не только бэкенд.

### Что уже сделано (ядро логики, до интеграции)

- **`scripts/layer_naming.py`** — разбор имени слоя по обеим грамматикам
  (`parse_layer_name`), строгая валидация (`LayerNameError` с именем
  слоя и причиной на любом `WEB_`-слое вне стандарта). Справочник
  подтипов принимается параметром — источник (БД/настройки) появится на
  шаге интеграции.
- **`scripts/zone_parser.py`** — построение реестра зон
  (`build_zone_registry`): группировка слоёв по (категория, отметка),
  геометрия зон из замкнутых `LWPOLYLINE` (переиспользует
  `is_effectively_closed` из `parse_zhbi.py`), сопоставление
  названия с полигоном через `shapely` `Polygon.contains(Point(...))`.
  Зоны без названия и названия без однозначной зоны — оба явно попадают
  в список `review`, не выбираются "на удачу".
- **`scripts/zone_binding.py`** — привязка элемента к зонам
  (`bind_element_to_zones`): точка для колонн, доля площади пересечения
  для протяжённых/площадных типов; порог 50% на минимальное перекрытие
  и 10 п.п. на "близкий результат" (`OVERLAP_MIN_FRACTION`/
  `OVERLAP_CLOSE_MARGIN`, оба — константы, легко перекалибровать);
  стоянка дополнительно фильтруется по совпадению отметки элемента и
  зоны, элемент без известной отметки получает статус `not_applicable`
  (не `unmatched` — это разные по смыслу ситуации: "нельзя определить"
  vs "определили и не попал").
- **`scripts/generate_test_zones_dxf.py`** — синтетический тестовый DXF
  (`test_data/synthetic_zones.dxf`): 3 захватки (включая одну без
  сопоставленного названия), Г-образная (невыпуклая) зона крана, 2
  стоянки на разных отметках с одинаковой геометрией в плане (проверка,
  что элемент не цепляется к стоянке чужого уровня), 3 элемента (колонна
  полностью внутри зоны; ригель ровно на границе двух захваток 50/50;
  колонна внутри зоны без названия).
- **`scripts/verify_zone_pipeline.py`** — прогоняет весь конвейер на
  этом файле, 16 явных проверок (`assert`-подобных), все проходят.
  Ключевые проверенные сценарии: строгая валидация ловит некорректное
  имя слоя; зона без сопоставленного названия всё равно участвует в
  геометрической привязке элементов; элемент на границе двух зон
  получает `needs_review`, а не произвольный выбор; стоянка на "своей"
  отметке не путается со стоянкой другого уровня при идентичной
  геометрии в плане; невыпуклый полигон (кран) обрабатывается корректно.

### Интеграция (завершена)

- **БД**: новые таблицы `zones` (полигон+категория+отметка+название+статус
  сопоставления) и `allowed_subtypes` (с сидом известных подтипов —
  Периметральный/Цоколь/ЛифтоваяШахта/ШахтаПодъемника), новые поля
  `elements`: `subtype`, `elevation_mm`, `zone_zakhvatka_id`+`_status`,
  `zone_crane_id`+`_status`, `zone_stance_id`+`_status` — все через
  аддитивные миграции, без риска FK-ловушки, найденной ранее для контрактов
  (здесь только `ADD COLUMN`, без `RENAME TABLE`).
- **`scripts/new_standard_pipeline.py`** — оркестратор: находит слои с
  префиксом `WEB_`, ещё не занятые старым конвейером
  (`LAYER_CONFIG`/`ANNOTATION_LAYERS`/`AXIS_LAYER="WEB_Оси"`), и только их
  прогоняет через строгую валидацию нового стандарта — **найден и
  исправлен реальный баг** при первой регрессии на старых файлах:
  `zone_parser.build_zone_registry()` заново разбирал ВСЕ слои файла с
  нуля (включая уже занятые старым конвейером), из-за чего
  `WEB_Колонна_нижняя` ошибочно попадал под строгую валидацию нового
  стандарта и ронял импорт старых файлов. Исправлено — классификация
  слоёв выполняется один раз на уже отфильтрованном наборе
  (`build_zone_registry_from_classified`), результат переиспользуется.
  Марка элемента нового стандарта ищется среди аннотаций ТОЛЬКО на парном
  слое роли "Марка" внутри той же группы (тип/подтип/отметка), не
  глобально — разные группы (например, ригели разных ярусов) не путают
  друг другу выноски.
- **`app/dxf_import.py`**: старый и новый конвейеры запускаются
  последовательно на одном открытом DXF-документе, результаты
  объединяются на уровне записи в БД; ошибка валидации нового стандарта
  (`LayerNameError`) прерывает импорт с понятным сообщением (422), как и
  требовалось ("явно сообщать об ошибке с именем проблемного слоя").
  Сводка импорта (`DxfImportResult.zones`) — сколько зон по категориям,
  сколько зон и сколько привязок элементов требуют проверки.
- **API**: `GET/POST/DELETE /allowed-subtypes` (справочник подтипов,
  редактирование только `admin`), зоны и расширенные поля элементов —
  в `POST /plan-data` (зоны не фильтруются выбором слоёв — показываются
  целиком для каждого выбранного файла).
- **Интерфейс**: новый SVG-слой `#zones-layer` (полигоны зон между
  сеткой осей и элементами — сверху видны маркеры, снизу — зоны),
  раскраска по категории, пунктир для несопоставленных/неоднозначных
  зон, подпись названия (или причины, почему его нет) по центроиду
  полигона; переключатели видимости по категории в сайдбаре
  ("Зоны"); в карточке элемента — блок "Зоны" (Захватка/Кран/Стоянка,
  каждая — название зоны либо статус: не определено/требует
  проверки/неприменимо), показывается только у элементов нового
  стандарта; экран "Настройки → Справочник подтипов" — чипы с
  подтипами по типу элемента, добавление/удаление без отдельного
  сохранения (сразу применяется).

**Проверено**: полный цикл (импорт → БД → API → интерфейс) на
синтетическом файле через реальный запущенный сервер и управление
браузером; регрессия — все 4 реальных файла старого стандарта
(`260713_2`, `260714`, `Чертежи для WEB-1`, `sample`) дают ровно те же
числа элементов, что и до этой доработки, `zones: null` в ответе
импорта (корректно — в этих файлах нет данных нового стандарта), 0
ошибок в консоли браузера при загрузке.

**Файлы**: созданы `scripts/layer_naming.py`, `scripts/zone_parser.py`,
`scripts/zone_binding.py`, `scripts/new_standard_pipeline.py`,
`scripts/generate_test_zones_dxf.py`, `scripts/verify_zone_pipeline.py`,
`test_data/synthetic_zones.dxf`; изменены `requirements.txt`,
`app/schema.sql`, `app/db.py`, `app/models.py`, `app/main.py`,
`app/dxf_import.py`, `scripts/parse_zhbi.py`, `scripts/import_elements.py`,
`app/static/index.html`.

### Ревизия стандарта имён слоёв по боевому чертежу (2026-07-15)

После поставки первого реального чертежа, переименованного по новому
стандарту (`260715_Чертежи для WEB.dxf`), выяснилось, что фактическая
грамматика отличается от исходной спецификации выше — заказчик поменял
формат ещё раз до того, как прислал первый файл. Перед реализацией
чертёж и приложенный скриншот диспетчера слоёв были сверены напрямую
через `ezdxf` (полный список реальных имён слоёв — см. историю
разработки), затем заданы 2 уточняющих вопроса, оба отвечены:

1. Сравнение токенов имени слоя — без учёта регистра (не точное
   совпадение написания).
2. Старый словарь зон (`доп`, `Захватка`/`Стоянка` без уточнения) отменён
   окончательно, обратная совместимость под него не нужна.

**Что изменилось в грамматике** (актуальная версия — см. `Docs/TZ.md`,
раздел 3.7):
- У слоёв ЖБИ появился обязательный второй токен `констр`:
  `WEB_констр_<Тип>[_<Подтип>][_ОТМ...]_<Роль>` (было без него).
- У служебных слоёв (зон) `доп` заменён на `тех`:
  `WEB_тех_<Категория>[_ОТМ...]_<Роль>`.
- Словарь категорий зон: `Захватки`/`Стоянки крана`/`Кран` в имени слоя
  (внутреннее каноническое имя в БД/коде осталось прежним — `Захватка`/
  `Стоянка`/`Кран`, меняется только распознавание, не хранимое значение —
  так не пришлось трогать схему БД/CSS/фронтенд).
- Роль зоны: `область` (было `Зона`), `наименование` (было
  `Наименование`, только регистр).
- Слой сетки осей: `WEB_Оси` → `WEB_тех_Оси`. Система теперь пробует оба
  варианта по очереди (`assign_axes.build_axis_grid_auto`,
  `AXIS_LAYER_CANDIDATES`) — старые файлы с `WEB_Оси` продолжают работать.
- Cправочник `allowed_subtypes` пополнен реальными значениями: Колонна —
  `нижняя`, `средняя нижний ярус`; Ригель — `на отм. +15.800`,
  `периметральный`.

**Два бага найдены и исправлены при регрессии на реальном файле**
(не были видны на синтетических данных, т.к. синтетический генератор
использовал `TEXT` и точную геометрию):

1. **Название зоны — `MULTILEADER`, не `TEXT`/`MTEXT`.** На реальном
   чертеже подписи "Захватка 1", "Стоянка 12" и т.п. — это выноски
   (`MULTILEADER`), которые `zone_parser.py` раньше не читал вовсе (искал
   только `TEXT MTEXT`), из-за чего ни одна зона не находила названия.
   Исправлено: `zone_parser._multileader_name()` разворачивает
   `MULTILEADER` так же, как это уже делает `parse_zhbi.collect_annotations`
   для марок элементов, и берёт **остриё выноски** как точку для
   сопоставления с полигоном (не место текстового блока — оно обычно
   вынесено за пределы зоны).
2. **Точная геометрия (`contains`/`covers`) не срабатывала на точках,
   геометрически совпадающих с границей полигона.** Координаты остриёв
   выносок и вершин полигонов в DXF независимо округляются CAD-системой —
   расстояние между геометрически "одной и той же" точкой и границей
   полигона получается порядка 1e-11, а не 0, и точный shapely-предикат
   такую точку не засчитывает. Заменено на проверку расстояния с допуском
   1 мм (`zone_parser.NAME_MATCH_TOLERANCE_MM`,
   `zone_binding.POINT_MATCH_TOLERANCE_MM` — второй допуск применён и к
   привязке точечных элементов/колонн к зоне по той же причине, хотя на
   имеющихся данных проблема проявилась только для названий зон).

**Проверено**: `scripts/verify_zone_pipeline.py` (переведён на новую
грамматику, синтетический DXF перегенерирован) — 18/18 проверок; регрессия
на всех 6 ранее проверенных файлах старого стандарта
(`260713_2`, `260713`, `260714`, `Чертежи для WEB.dxf`, `Чертежи для WEB-1`,
`sample`) — те же числа элементов, что и раньше, `zones: null`; целевой
файл `260715_Чертежи для WEB.dxf` — 1401 элемент, 34 зоны (Захватка: 4,
Кран: 3, Стоянка: 27), **0 зон и 0 привязок элементов требуют проверки**.

**Найдено, но не исправлено (отдельная проблема, вне рамок этой задачи)**:
файл `260714_Чертежи для WEB_2.dxf` (не входил в ранее проверенный набор
регрессии) содержит слой `WEB_Колонна_Средняя нижний ярус_отм 15.800`
(996 сущностей) — не подходит ни под `LAYER_CONFIG` старого способа
(`parse_zhbi.py`), ни под новую грамматику (нет токена `констр`, нет
суффикса `_Элемент`/`_Марка` в правильном формате). Похоже на слой ещё
одной, промежуточной ревизии стандарта. Импорт этого конкретного файла
сейчас завершается ошибкой 422 с понятным указанием проблемного слоя —
осознанно не угадывалось, куда его отнести, нужно уточнение у заказчика.

**Файлы**: изменены `scripts/layer_naming.py`, `scripts/zone_parser.py`,
`scripts/zone_binding.py`, `scripts/assign_axes.py`, `app/dxf_import.py`,
`app/schema.sql`, `scripts/generate_test_zones_dxf.py`,
`scripts/verify_zone_pipeline.py`, `test_data/synthetic_zones.dxf`.

## Правки схемы: масштаб, выбор слоёв, форма по умолчанию, подложка зон (2026-07-15)

Пакет из 7 связанных правок интерактивной схемы, все проверены статически
(код + юнит-вызов `plan_data()` напрямую в Python — живого браузера в
этой сессии не было, см. примечание в конце).

1. **Масштаб/пан больше не сбрасывается** при изменении состава слоёв —
   `loadPlan()` получил параметр `preserveView` (по умолчанию `true`):
   сбрасывает вид на "вся схема целиком" только при самой первой загрузке
   приложения и сразу после загрузки нового чертежа; все остальные
   перезагрузки данных (переключение слоёв/файлов, сохранение настроек,
   импорт истории) масштаб не трогают.
2. **Список слоёв в выпадающей панели** (`#file-select-menu`) визуально
   не был списком "друг под другом" — **найден реальный CSS-баг**:
   `.layer-list label.checkbox` не задавал `display`, а `<label>` по
   умолчанию `inline`, и правило `.modal label.checkbox { display:flex }`
   не применялось (панель — `.dropdown-menu`, не `.modal`) — чекбоксы
   слоёв рендерились в строку, обтекая друг друга как текст, а не
   списком. Исправлено (`display:flex` + `flex-direction:column` на
   контейнере), плюс разделители между файлами для ясности иерархии.
3. **Форма отображения элемента по умолчанию — «как в оригинале»
   (контур)**, было `circle`. Изменено в двух местах (должны совпадать):
   `shapeNameFor()` на фронтенде и дефолт в `GET /layer-type-combinations`
   на бэкенде (экран настроек показывал `circle` как текущее значение для
   ещё не настроенных пар, теперь тоже `outline`). Переопределение по
   (слой, тип элемента) — в «Настройки → Форма маркеров», уже сохранялось
   на сервере (`element_shapes`) и продолжает.
4. **Реальный баг в `POST /plan-data`**: `if item.layers:` — пустой
   список (`[]`, т.е. "сняты все галочки слоёв") ложный в Python, поэтому
   неотличим от `None` ("все слои") — при снятии всех галочек сервер
   возвращал ВСЕ элементы файла вместо нуля. Исправлено на явную проверку
   `is not None` c отдельной веткой для пустого списка (ноль элементов).
   Заодно правило "хотя бы один элемент, иначе 404" ослаблено: пусто по
   элементам, но есть сетка осей — не ошибка (именно так теперь себя
   ведёт "сняты все галочки" — остаются только оси/зоны), 404 только если
   вообще нечего показать. Проверено юнит-вызовом `plan_data()`: `layers=None`
   → 1401 элементов, `layers=[]` → 0 элементов (27 осей, 34 зоны на месте),
   `layers=[один слой]` → 332. Полная регрессия по всем 7 тестовым файлам
   не нарушена.
   (Жалоба "при выборе всех слоёв кажется что не всё выводится" явно не
   воспроизведена отдельным багом — вероятная причина эффекта: п.1, дёргающийся
   масштаб при каждом клике по чекбоксу создавал именно такое впечатление;
   должна исчезнуть вместе с этим исправлением.)
5. **Зоны — подложка**: `#zones-layer` уже рисуется до `#elements-layer`
   в DOM (элементы поверх), `pointer-events:none` уже стоял; добавлена
   `stroke-opacity:.5` на полигоны зон (была полностью непрозрачная 2px
   обводка при `fill-opacity:.07`) — граница зоны больше не "спорит"
   визуально с маркерами элементов у самого края зоны.
6. **Название зоны вынесено за контур** — было по центроиду полигона
   (гарантированно перекрывало марки элементов внутри зоны), теперь над
   верхним краем полигона (`max(outline.y) + отступ`), тем же приёмом,
   что и подписи осей. Полного алгоритма разводки коллизий между СОСЕДНИМИ
   зонами (например, у плотной решётки из 27 стоянок) не добавлено — вне
   рамок этой правки, отдельная возможная доработка при необходимости.
7. **Общий предел размера шрифта распространён на подписи зон** —
   раньше `renderZones()` считал `fontSize` один раз в мировых единицах
   и никогда не пересчитывал при зуме (в отличие от подписей марок/осей,
   которые ограничены константой в ЭКРАННЫХ пикселях — `MAX_LABEL_FONT_PX`/
   `MAX_AXIS_FONT_PX` — и пересчитываются в `updateSizesForZoom()` на
   каждый зум). При сильном приближении название зоны могло раздуваться
   на весь экран. Добавлена `MAX_ZONE_FONT_PX` (20px) и та же логика
   пересчёта; элементы подписей зон теперь отслеживаются в
   `state.zoneLabelEls` для обновления `font-size` без полного
   перерендера.

**Не проверено вживую**: в этой сессии не было доступного инструмента
браузерной автоматизации (в отличие от предыдущих раундов, где
использовался CDP) — все правки проверены чтением кода, статическим
JS-синтаксисом (`node --check`) и прямым юнит-вызовом бэкенд-функции
`plan_data()` в Python с проверкой на всех 7 тестовых DXF-файлов (числа
элементов не изменились). Визуальную проверку (масштаб при переключении
слоёв, вид списка слоёв, положение подписей зон при разном зуме) нужно
сделать в браузере.

**Файлы**: изменены `app/main.py`, `app/schema.sql`, `app/static/index.html`.

## Каталог Input/, очистка при пустом выборе, масштаб при смене файлов (2026-07-15)

Продолжение предыдущего пакета правок схемы, по итогам дальнейшей
проверки. Уточняющий вопрос был задан и отвечен: каталог `Input/` должен
не только фильтровать список доступных файлов, но и **автоматически
импортировать** новые DXF из него на каждом старте сервера (выбран
рекомендованный вариант).

1. **`INPUT_DIR` (`Input/` в корне проекта) — теперь источник истины для
   списка доступных чертежей.** На старте сервера (`@app.on_event
   ("startup")`) все `.dxf` из `Input/` (пере)обрабатываются через
   `process_upload()` — тот же путь, что и ручная загрузка через UI,
   идемпотентно (повторный прогон того же файла не плодит дубли, не
   сбрасывает статусы — сопоставление по `dxf_handle`). Ошибка одного
   файла логируется в консоль и не блокирует старт сервера/обработку
   остальных файлов. `GET /source-files` дополнительно фильтрует
   результат живым сканированием `Input/*.dxf` на каждый запрос — файл,
   убранный из папки без перезапуска сервера, тоже сразу перестаёт
   предлагаться (данные из БД при этом не удаляются, только скрываются
   из списка выбора). Кнопка «Загрузить чертёж» (сохраняет в `uploads/`)
   осталась как есть — но если её файл не совпадает по имени ни с чем в
   `Input/`, его данные обработаются, а в списке выбора не появятся, пока
   файл не окажется в `Input/`.
2. **Снятие всех галочек файлов теперь по-настоящему очищает экран** —
   раньше `loadPlan()` при пустом `state.selection` просто молча
   ничего не делал (`if (!state.selection.size) return;`), оставляя
   последний отрисованный чертёж висеть на экране. Добавлена
   `clearWorkspace()`: очищает все три SVG-слоя, легенду, тумблеры
   подписей/зон, карточку элемента (плейсхолдер), сбрасывает
   `state.view`/`state.initialView` в `null`.
3. **Масштаб теперь пересчитывается при изменении состава ФАЙЛОВ** (в
   отличие от слоёв внутри уже выбранного файла — тот случай, наоборот,
   не должен трогать масштаб, см. предыдущую запись). Чекбокс файла в
   `renderFileSelectMenu` — `loadPlan(false)` (рефит) и при включении, и
   при выключении файла, независимо от порядка кликов.
4. **Найдена и объяснена причина смещения подписей осей к центру при
   переключении файлов**: `updateAxisLabelSizing()` пинит подписи осей к
   границам ТЕКУЩЕГО `state.view` (а не к границам данных). Предыдущая
   правка ("не менять масштаб при смене состава слоёв") по ошибке
   применялась и к переключению ФАЙЛОВ целиком — у каждого файла своя
   сетка осей с другим охватом, и если вид не пересчитывался при смене
   набора файлов, `state.view` переставал соответствовать реальному
   охвату новых данных, и подписи осей, честно пинуясь к краям СТАРОГО
   вида, визуально оказывались смещены от истинного края к центру
   относительно НОВОГО содержимого. П.3 выше — прямое исправление: при
   любом изменении состава файлов вид принудительно пересчитывается по
   текущему выбору, поэтому `state.view` всегда актуален и подписи осей
   всегда ровно по краю.

**Проверено**: юнит-вызовом `_input_dir_filenames()`/`list_source_files()`
напрямую в Python — с `Input/`, содержащим только
`260715_Чертежи для WEB.dxf`, `/source-files` возвращает ровно один этот
файл (1401 элемент), несмотря на то что в БД остаются данные ещё 6 файлов
из более ранних раундов тестирования (`uploads/`/`test_data/`) — они
корректно скрыты, не удалены. Повторный вызов `_import_input_dir()` —
идемпотентен (0 новых, 1401 обновлено). `plan_data()` для этого файла
по-прежнему возвращает 1401 элемент и 34 зоны — регрессия не задета.
Живой браузерной проверки в этой сессии не было (см. примечание в
предыдущей записи) — масштаб/подписи осей при переключении файлов стоит
перепроверить глазами.

**Файлы**: изменены `app/main.py`, `app/static/index.html`.

## Легенда по типам, вкладки сайдбара, фильтр по размещению, XLS-состав (2026-07-15)

1. **Легенда «Статусы» — колонки по типам элементов.** Была одна колонка
   (статус → количество), теперь таблица статус×тип, столбцы — не зашитый
   список, а `element_type`, реально встретившиеся в текущей выборке
   (у старого стандарта имён слоёв это "column"/"beam", у нового —
   "Колонна"/"Ригель"/... — оба варианта корректно работают, т.к. список
   строится динамически). Плюс итоговый столбец/строка.
2. **Сайдбар разбит на вкладки «Свойства»/«Фильтры».** «Свойства» —
   Статусы + карточка элемента; «Фильтры» — переехавшие сюда переключатели
   зон и подписей + новый фильтр по размещению (п.3). Клик по элементу на
   схеме автоматически переключает на «Свойства», если была открыта
   «Фильтры» — иначе карточка обновлялась бы невидимо для пользователя.
3. **Новый фильтр по размещению** — 5 независимых групп чекбоксов:
   Захватка, Кран, Стоянка крана, Отметка, Тип элемента. Значения — не
   хардкод, собираются из `state.elements` на клиенте (включая сентинел
   "не определено"/"без отметки" для элементов без привязки к этой
   категории — например, у старого стандарта имён слоёв зон и отметок не
   бывает вовсе). По умолчанию всё отмечено (группа не фильтрует); кнопки
   "все"/"ничего" на группу. Комбинация — И между группами, ИЛИ внутри
   группы. Применяется полностью на клиенте — скрытием уже отрисованных
   `<g>`-групп элементов (`display:none`) через `applyPlacementFilters()`,
   без обращения к серверу, поэтому мгновенно реагирует на клик по
   чекбоксу. Легенда статусов (п.1) сознательно НЕ учитывает этот фильтр —
   показывает сводку по всей текущей выборке файлов/слоёв, а не только по
   видимому на схеме.
4. **XLS-экспорт: аудит и добор состава колонок.** `ELEMENT_COLUMNS` в
   `app/export.py` не включал `subtype`, `elevation_mm`, привязку к
   зонам и контракт вовсе — оба отчёта (снимок на дату, история статусов)
   молчаливо теряли эти свойства при выгрузке. Добавлены: "Подтип",
   "Отметка, мм", "Захватка"/"Кран"/"Стоянка" (резолвится в название зоны
   через отдельный `SELECT id, name FROM zones`, та же логика, что у
   `zoneBindingText()` на фронтенде — не сырой id), "Контракт" (резолвится
   в "название (поставщик)"). В отчёте истории статусов контракт берётся
   **на момент события** (`status_history.contract_id`), а не текущий
   (`elements.contract_id`) — это отчёт про то, что было тогда, а не что
   сейчас; зоны/подтип/отметка в истории по-прежнему текущие — для них
   отдельной истории не ведётся, хранится только последнее состояние.
   `app/history_import.py` (обратная операция) менять не пришлось — он
   читает только 3 обязательных заголовка по имени, лишние колонки в
   экспортированном файле не мешают повторному импорту той же истории.

**Проверено**: юнит-вызовом `build_snapshot_xlsx`/`build_history_xlsx`
напрямую (без сервера) на `260715_Чертежи для WEB.dxf` — оба файла
открываются `openpyxl`, все новые колонки на месте, "Захватка" резолвится
в реальное название ("Захватка 1", а не голый id), 1401 строка в обоих
отчётах. Фронтенд-часть (легенда/вкладки/фильтры) проверена статическим
JS-синтаксисом (`node --check`) и чтением кода — живой браузерной проверки
в этой сессии не было (нет инструмента автоматизации браузера), стоит
перепроверить глазами: таблицу легенды на разных наборах типов, вид
вкладок, поведение фильтра по размещению на реальных значениях (Захватка
1-4/Кран 1-3/Стоянка 1-21/отметки 0,5000,15800).

**Файлы**: изменены `app/export.py`, `app/static/index.html`.

## Доработки фильтров, зон, статусов и справочников (запрошено 2026-07-15)

Список из 10 пунктов, зафиксирован для будущей итерации. **Ничего из
этого не реализовано** — заказчик явно попросил сначала записать в
бэклог. Нумерация — как в исходном запросе, для трассируемости.

### 1. Каскадные фильтры (сужение значений по уже применённым)

Сейчас (см. предыдущую запись) `distinctPlacementValues()` в
`app/static/index.html` строит список доступных значений каждой группы
фильтра (Захватка/Кран/Стоянка/Отметка/Тип элемента) по **всем**
`state.elements`, не учитывая, что уже отфильтровано в ДРУГИХ группах.
Нужно: при построении списка значений группы X учитывать только элементы,
прошедшие фильтры ВСЕХ ОСТАЛЬНЫХ групп (не X — иначе группа может скрыть
сама себя). Пример: если в фильтре "Кран" выбран только "Кран 2", то в
фильтре "Стоянка крана" должны остаться только стоянки, реально
относящиеся к "Кран 2" (см. п.2/п.3 ниже). Отдельный вопрос на
реализацию: что делать с уже снятой галочкой значения, которое временно
пропадает из списка при таком сужении — вероятно, не терять её (просто не
показывать чекбокс, но не забывать состояние в `excludedSet`), чтобы при
возврате других фильтров назад выбор не "терялся".

### 2. Связь "стоянка → кран" при разборе файла

Сейчас в `zones` (таблица БД, см. `app/schema.sql`) у категории
"Стоянка" нет связи с "Кран" — они просто два независимых списка зон.
Нужно на этапе `scripts/zone_parser.py`/`new_standard_pipeline.py`
после построения обоих реестров вычислять для каждой зоны-стоянки, в
зоне работы какого крана она физически находится (скорее всего —
point-in-polygon по центроиду стоянки относительно полигонов "Кран", тем
же приёмом, что уже используется для привязки элементов к зонам, см.
`scripts/zone_binding.py`), и сохранять эту связь — потребуется новая
колонка в `zones` (например `parent_zone_id INTEGER REFERENCES
zones(id)`, заполняется только для `category='Стоянка'`). Нужно решить,
как показывать стоянку, не попавшую ни в одну зону крана или попавшую в
несколько сразу — по аналогии с уже существующим `match_status`
зон/`needs_review` у привязки элементов, не молчать.

### 3. Иерархия "Стоянка" внутри "Кран" в фильтре

Зависит от п.2. В `renderPlacementFilters()` объединить группы "Кран" и
"Стоянка крана" в одну иерархическую группу: чекбокс крана + под ним, с
отступом, чекбоксы его стоянок (используя связь из п.2). Клик по
чекбоксу крана — ставит/снимает галочки со всех его стоянок разом (и,
видимо, сам считается отдельным значением фильтра по кранам — элемент,
геометрически относящийся к стоянке крана, но не факт что относится к
самой зоне крана один-в-один, нужно уточнить семантику на реализации).
Стоянки без известного крана (см. "не попала ни в одну зону" в п.2) —
отдельным пунктом вне иерархии ("не определено").

### 4. Фильтр по статусам + переименование статуса

Добавить 6-ю группу в фильтр по размещению — по `current_status`, той же
логикой чекбокс-группы, что и остальные (набор доступных статусов — из
факта присутствия у текущей выборки элементов, как и другие фильтры).
Отдельно, не связано с фильтром напрямую: переименовать статус
`Status.DELIVERED` в `app/models.py` (`STATUS_LABELS_RU`) — сейчас
"Доставлен на площадку", должно быть "Доставлен". Простая правка в
одном месте, но нужно проверить все места, где это название выводится
текстом (легенда, карточка элемента, история статусов, экспорт XLS,
контекстное меню смены статуса) — насколько известно, все они берут
подпись из `STATUS_LABELS_RU`/`state.statusLabels`, так что правки в
одном месте должно быть достаточно, но стоит перепроверить при
реализации.

### 5. Названия зон — обратно внутрь контура

**Разворот предыдущего решения**: в записи "Легенда по типам, вкладки
сайдбара, фильтр по размещению..." (она же — предыдущий раунд, п.5 того
списка) название зоны было намеренно вынесено ЗА контур (над верхним
краем полигона), чтобы не перекрываться с подписями элементов внутри
зоны. Сейчас запрошено обратное — вернуть подпись внутрь контура.
Причина смены решения не указана; вероятно, связана с п.7 (свой цвет на
каждый кран) — с различимой заливкой по кранам название внутри зоны,
возможно, снова читается нормально. Стоит при реализации свериться,
не возвращается ли проблема перекрытия с марками элементов внутри
зоны, из-за которой подпись в своё время и вынесли наружу — если
возвращается, обсудить с заказчиком, а не молча делать полумеру.

### 6. Единый (русский) словарь типов элементов

Сейчас два источника `element_type` дают РАЗНЫЕ языковые словари для
одних и тех же по сути типов — старый стандарт слоёв (`LAYER_CONFIG` в
`scripts/parse_zhbi.py`) отдаёт `"column"`/`"beam"` (английские
строки), новый стандарт (`scripts/layer_naming.py`, `ZHBI_TYPES`) —
`"Колонна"`/`"Ригель"`/`"Плита"`/`"Панель"` (русские). Отсюда и
"лишние" английские строки, которые заказчик увидел в "Настройки →
Контракты → контракт по умолчанию" и "Настройки → Видимость подписей"
— эти экраны просто показывают все `element_type`, когда-либо
встретившиеся в данных, не различая язык.

Решение — конвертировать в русский **на этапе разбора файла**: заменить
`LAYER_CONFIG`, чтобы он отдавал `"Колонна"`/`"Ригель"` напрямую, а не
`"column"`/`"beam"` (простая правка словаря в `parse_zhbi.py`).

**Важный нюанс для реализации** — этого НЕДОСТАТОЧНО само по себе:
1. У элементов (`elements.element_type`), уже загруженных со старым
   значением, "column"/"beam" сами по себе не станут "Колонна"/"Ригель"
   задним числом — но т.к. `upsert_elements` обновляет существующую
   строку по `(source_file, dxf_handle)`, при следующей переобработке
   того же файла (а `Input/` теперь переобрабатывается на каждом старте
   сервера, см. соответствующую запись) значение обновится само —
   если файл всё ещё лежит в `Input/`. Для файлов НЕ в `Input/` (старые
   архивные источники) значение останется "как было" без ручной миграции.
2. А вот `label_visibility`, `default_contracts`, `contract_lines`,
   `element_shapes` — это ОТДЕЛЬНЫЕ таблицы, ключ в них — сама строка
   `element_type` (`PRIMARY KEY`/`UNIQUE`), они НЕ выводятся заново из
   `elements` при каждом запросе, а хранят свои строки постоянно. Просто
   исправить парсер эти таблицы не почистит — старые строки с "column"/
   "beam" останутся там висеть осиротевшими (ключ, на который больше
   ничего не ссылается) и по-прежнему будут отображаться в
   соответствующих экранах настроек. Нужна явная разовая миграция
   (переименовать или удалить строки с английским ключом в этих 4
   таблицах) как часть этой доработки, а не только правка парсера.

После этого исправления "Настройки → Контракты по умолчанию" и
"Настройки → Видимость подписей" должны сами показывать только 2 строки
(Колонна/Ригель) вместо 4 — отдельно ничего чинить в этих экранах не
нужно, они уже строятся динамически по факту встретившихся типов.

### 7. Свой цвет зоны на каждый кран

Сейчас цвет зоны — общий на всю категорию (`#zones-layer polygon.zone-
Кран { fill: #c0392b }` и т.п. — фиксированная CSS-подстановка по
категории, см. `app/static/index.html`), а не персональный на
конкретную зону. Нужно: свой цвет на каждую ИНДИВИДУАЛЬНУЮ зону-кран, с
наследованием тем же цветом у её стоянок (см. связь из п.2) — то есть
"Кран 1" и все его стоянки — один цвет, "Кран 2" — другой.

**Важный нюанс** — `upsert_zones` (см. `scripts/import_elements.py`)
делает `DELETE FROM zones WHERE source_file = ?` + заново `INSERT` при
каждой обработке файла (как и сетка осей) — `zones.id` НЕ стабилен
между перезагрузками одного и того же файла (новый `AUTOINCREMENT` id
каждый раз). Значит, привязывать цвет к `zones.id` — ошибка, при
следующей переобработке (а Input/ переобрабатывается на каждом старте
сервера) все настроенные цвета слетят. Ключ для хранения цвета должен
быть стабильным — вероятно `(source_file, category, name)` (имя зоны,
"Кран 1" и т.п.) или просто `name`, если имена кранов в рамках проекта
считаются достаточно уникальными.

При первой загрузке — автоназначение цветов "на усмотрение" (соседние,
визуально различимые цвета из фиксированной палитры, циклически по
порядку кранов). В настройках — новый экран (по аналогии с "Цвета
статусов") для ручного переназначения цвета на кран.

### 8. Фильтр по подтипам внутри фильтра по типам

По аналогии с иерархией "Кран → Стоянка" (п.3) — сделать "Тип элемента"
в фильтре по размещению иерархическим: под каждым типом (Колонна/Ригель/
...) — вложенные чекбоксы его подтипов (`subtype`, справочник которых
уже есть — "Настройки → Справочник подтипов", см. раздел 3.7 ТЗ).
Клик по типу — ставит/снимает все его подтипы разом, аналогично п.3.
Разумно реализовывать п.3 и п.8 через один переиспользуемый компонент
"иерархическая группа фильтра", а не дублировать логику дважды.

### 9. Кнопка сброса всех фильтров

Одна кнопка на вкладке "Фильтры" — очищает все `excludedSet` всех групп
фильтра по размещению (включая новую группу статусов из п.4) разом,
без необходимости жать "все" в каждой группе по отдельности.

### 10. Легенда статусов — по текущим фильтрам

**Разворот предыдущего решения**: в предыдущей записи явно
зафиксировано, что легенда "Статусы" **сознательно** не учитывает
фильтр по размещению — показывает сводку по всей текущей выборке
файлов/слоёв, а не по тому, что видно на схеме сейчас (см. ТЗ, раздел
6.5). Теперь запрошено обратное: легенда должна пересчитываться по
элементам, реально прошедшим текущие фильтры (`passesPlacementFilters`),
а не по полной выборке. Раздел 6.5 ТЗ придётся поправить в этой части
при реализации.

**Статус**: ✅ реализовано (2026-07-15) — см. запись "Реализация 10
пунктов доработок фильтров/зон/статусов/справочников" ниже.

---

## Реализация 10 пунктов доработок фильтров/зон/статусов/справочников (2026-07-15)

Все 10 пунктов из предыдущей записи реализованы в одной сессии, по
согласованному в переписке решению двух неоднозначностей (без отдельного
уточняющего вопроса — оба вывода обоснованы ниже):

- **Иерархия Кран→Стоянка и Тип→Подтип (п.3, п.8)**: реализована КАК ДВЕ
  независимые категории фильтра (свой `excludedSet` у каждой), а не
  слияние в одну булеву группу — клик по родителю просто программно
  ставит/снимает чекбоксы потомков. Проверено рассуждением, что это даёт
  тот же результат, что и настоящее слияние (элемент, привязанный к
  стоянке крана, но не к самому крану напрямую, всё равно корректно
  скрывается при снятии крана, потому что снимается и чекбокс его
  стоянки) — при этом не требует переписывать булеву семантику
  "И между категориями".

### 1. Единый русский словарь `element_type` (было п.6)

`scripts/parse_zhbi.py` `LAYER_CONFIG` теперь отдаёт `"Колонна"`/
`"Ригель"` вместо `"column"`/`"beam"`. Добавлена идемпотентная миграция
`app/db._normalize_element_type_vocabulary()` (вызывается на каждом
старте, как и `_apply_migrations`) — приводит уже загруженные данные
к тому же словарю: `elements.element_type` — простой `UPDATE`;
`label_visibility`/`default_contracts`/`element_shapes`/`contract_lines`
(element_type — часть уникального ключа) — переименование с проверкой
коллизии: если строка под русским именем уже есть, английская просто
удаляется, иначе переименовывается на месте. Проверено на боевой БД:
`label_visibility` был `{column, beam, Колонна, Ригель}` (4 строки) →
стал `{Колонна, Ригель}` (2), `element_shapes` — 7 строк с английскими
element_type корректно переименованы (коллизий по `(layer, element_type)`
не оказалось — старый и новый стандарт используют разные имена слоёв),
`elements.element_type`: было 3988+634 (column/beam) + 664+737
(Колонна/Ригель) → стало 4652 (Колонна) + 1371 (Ригель), числа сошлись
день-в-день. Повторный запуск миграции — no-op (проверено).

### 2. Связь "стоянка → кран" при разборе файла (было п.2)

`scripts/zone_parser._link_stances_to_cranes()` — после построения
полного реестра зон вычисляет для каждой "Стоянки" её кран
(point-in-polygon центроида стоянки относительно полигонов "Кран", с тем
же допуском по расстоянию, что и у сопоставления названий зон). Новые
колонки `zones.parent_zone_id`/`parent_match_status`. `upsert_zones()` в
`scripts/import_elements.py` пишет `parent_zone_id` вторым проходом
(`UPDATE` после того, как все зоны файла уже вставлены и получили id) —
не нужно гарантировать порядок вставки кран-раньше-стоянки. Проверено на
`260715_Чертежи для WEB.dxf`: все 27 стоянок сопоставились однозначно
(0 needs_review), распределение по кранам — Кран 1: 21, Кран 2: 3,
Кран 3: 3.

### 3-4. Фильтр по статусам + переименование + иерархия Кран/Стоянка (было п.3, п.4)

Добавлена 6-я группа фильтра по размещению — "Статус" (значения — из
факта присутствия в текущей выборке, та же логика, что у остальных
групп). `STATUS_LABELS_RU[DELIVERED]` в `app/models.py`: "Доставлен на
площадку" → "Доставлен" (единственное место хранения подписи, все экраны
берут её оттуда). "Кран"/"Стоянка крана" в фильтре объединены в одну
иерархическую группу (`buildHierarchicalFilterGroup` в
`app/static/index.html`) — заголовки-краны, под каждым вложенные
чекбоксы его стоянок (используя связь из п.2, найденную через
`craneNameForStanceValue()`), клик по крану синхронизирует чекбоксы всех
его стоянок.

### 5. Названия зон — обратно внутрь контура (было п.5, разворот решения)

`renderZones()` — подпись снова по центроиду полигона, как было
изначально (до правки, вынесшей её над верхним краем специально против
перекрытия с марками элементов внутри зоны). Причина разворота — как и
предполагалось в предыдущей записи, появление индивидуального цвета на
каждый кран (п.7) сделало чтение подписи внутри зоны приемлемым.

### 6. Иерархия Тип/Подтип в фильтре (было п.8)

Тем же компонентом `buildHierarchicalFilterGroup`, что и Кран/Стоянка —
под каждым типом элемента вложены чекбоксы его подтипов (группировка —
по факту встречающихся у элементов `subtype`, через
`typesForSubtypeValue()`, не по справочнику `allowed_subtypes` напрямую —
показываются только реально присутствующие в текущей выборке значения).

### 7. Каскадное сужение значений фильтров (было п.1)

`distinctValuesFor(key)` — список значений категории теперь считается по
элементам, прошедшим фильтры **всех остальных** категорий
(`elementPassesExcept`), а не по всей выборке. После любого изменения
любого чекбокса (`onPlacementFilterChange()`) весь блок фильтров
перерисовывается заново (`renderPlacementFilters()`) — состояние
(`excludedSet`) хранится в `state.placementFilters`, а не в DOM, поэтому
перерисовка ничего не теряет, только отражает новый каскад. Значение,
временно пропавшее из видимой выборки из-за фильтра в другой категории,
не сбрасывает свою галочку — просто не рендерится, пока не станет
актуальным снова.

### 8. Свой цвет зоны на каждый кран (было п.7)

Новая таблица `zone_colors` (`source_file`, `category`, `name`, `color`)
— ключ сознательно НЕ включает `zones.id` (не стабилен между
переимпортами файла, `upsert_zones` делает `DELETE`+`INSERT` заново
каждый раз). `_ensure_zone_colors()` в `scripts/import_elements.py`
автоназначает цвет каждому новому крану из фиксированной палитры (10
цветов, дальше циклически), `INSERT OR IGNORE` — уже настроенный цвет
при повторном импорте не трогается (проверено). `plan_data()` в
`app/main.py` резолвит цвет в ответе: у зон категории "Кран" — напрямую,
у "Стоянка" — через `parent_zone_id` из п.2 (наследует цвет своего
крана), у "Захватка" — `null` (остаётся общая раскраска по категории).
Новый экран "Настройки → Цвета зон" (`GET`/`PUT /zone-colors`, только
admin) — список кранов по всем файлам с `<input type="color">`, собран
через `createElement`/`textContent` (не `innerHTML`) — имя крана приходит
из текста на чертеже (MULTILEADER), как и в остальных местах, где на
схему могло бы попасть что угодно из DXF, здесь сознательно не
допускается инъекция через шаблонную строку. Проверено на реальном
файле: 3 крана получили 3 разных цвета автоматически, стоянки корректно
унаследовали цвет родителя, повторный импорт не изменил уже назначенные
цвета.

### 9. Кнопка сброса всех фильтров (было п.9)

Кнопка в верхней части раздела "Фильтр по размещению" — очищает все 7
`excludedSet` разом (`zakhvatka`/`crane`/`stance`/`elevation`/
`elementType`/`subtype`/`status`).

### 10. Легенда статусов по текущим фильтрам (было п.10, разворот решения)

`renderLegend()` теперь считает только по `state.elements.filter
(passesPlacementFilters)`, вызывается заново при каждом изменении
фильтра через `onPlacementFilterChange()` — сознательный разворот
предыдущего решения (легенда была намеренно независима от фильтра по
размещению), запрошено явно.

**Проверено**: полная регрессия по всем 6 файлам старого стандарта
(`test_data/Архив/`) — числа элементов не изменились (332/1328/966/664/
1328/4); целевой файл `260715_Чертежи для WEB.dxf` — 1401 элемент, 34
зоны, 0 needs_review, 3 крана с разными автоцветами, 27 стоянок с
корректной привязкой к кранам. Миграция словаря типов проверена на
боевой БД без потери данных (см. п.1) и на идемпотентность (повторный
прогон — no-op). JS проверен статическим синтаксисом (`node --check`) —
живой браузерной проверки в этой сессии не было (нет инструмента
браузерной автоматизации), стоит перепроверить глазами: каскад значений
фильтра при реальном клике, вложенность Кран/Стоянка и Тип/Подтип,
таблицу легенды при активном фильтре, экран "Цвета зон".

**Файлы**: изменены `scripts/parse_zhbi.py`, `scripts/zone_parser.py`,
`scripts/import_elements.py`, `scripts/render_plan.py`, `app/db.py`,
`app/schema.sql`, `app/models.py`, `app/main.py`, `app/static/index.html`;
обновлены `Docs/TZ.md` (разделы 3.1, 3.3, 6.1, 6.5, новый 6.6, 8, таблица
статусов в разделе 4).

## Баг: каскад фильтров сужал список значений вместо disabled (2026-07-15)

Заказчик сообщил два симптома сразу после предыдущей записи: (1) у Крана 2
не было стоянок в иерархии фильтра, хотя на схеме они отмечены; (2) при
снятии галочки с "Колонна" в фильтре сам пункт "Колонна" исчезает из
формы, и вернуть его нельзя без полного сброса всех фильтров. Плюс явно
запрошено изменить сам принцип: не убирать из формы фильтры, ставшие
неактуальными при выборе других фильтров, а делать их недоступными
(disabled), не трогая список. Отдельно попросили: сортировать стоянки по
числовому номеру ("Стоянка 2" перед "Стоянка 10"), а не лексикографически.

**Диагностика (2)**: подтверждена прямым запросом к БД — у стоянок Крана 2
реально привязаны элементы (20+4+4), это не проблема данных. Причина —
в архитектуре предыдущей версии `distinctValuesFor(key)`: список значений
категории считался по элементам, прошедшим фильтры ВСЕХ ОСТАЛЬНЫХ
категорий. Клик по родителю в иерархической группе (Кран→Стоянка,
Тип→Подтип) каскадом исключал ВСЕХ потомков через `childExcludedSet`
(например, снятие "Колонна" исключало все подтипы колонны из
`state.placementFilters.subtype`). При следующем пересчёте
`distinctValuesFor("elementType")` — список значений категории "Тип
элемента" — учитывал, среди прочего, фильтр "Подтип" как "остальной", и
раз все подтипы колонны только что оказались исключены, ни один элемент
с типом "Колонна" больше не проходил проверку "остальных фильтров" →
"Колонна" переставала попадать в список значений СВОЕЙ ЖЕ категории —
самоблокирующийся цикл, разорвать который можно было только сбросом всех
фильтров разом. Тот же механизм работал и для Кран→Стоянка: снятие "Кран
2" каскадом исключало все его стоянки из фильтра "Стоянка", а список
значений "Стоянка" (в свою очередь) при расчёте учитывал фильтр "Кран" —
и, что важнее в данном случае, все элементы, привязанные к стоянкам Крана
2, переставали проходить проверку по категории "Кран" (она ведь тоже
исключена) → стоянки Крана 2 пропадали из списка значений категории
"Стоянка" целиком, хотя сам заголовок "Кран 2" оставался виден (доступность
категории "Кран" не проверяет саму себя, поэтому Кран 2 не исчезал — но
дочерние стоянки исчезали из СВОЕЙ категории).

**Исправление — смена принципа**, как и попросил заказчик:
- `allValuesFor(key)` — новая функция, список значений категории теперь
  считается БЕЗ учёта каких-либо текущих фильтров вообще (ни своего, ни
  чужих) — просто полный перечень значений, встречающихся у элементов
  текущей выборки. Для зон (Захватка/Кран/Стоянка) список ещё и
  объединяется с именами зон из `state.zones` — иначе зона без единого
  привязанного элемента (пустая стоянка) не попала бы в список вовсе,
  хотя видна на схеме. Список, возвращаемый этой функцией, никогда не
  меняется от кликов по чекбоксам — только от смены выбора файлов/слоёв.
- `isValueEnabled(key, value)` — используется ТОЛЬКО для визуального
  disabled-состояния чекбокса (серый + не кликается), не для состава
  списка. Считается по элементам, проходящим "остальные" фильтры — но с
  важной поправкой: для категорий, связанных иерархическим каскадом
  (`PLACEMENT_CASCADE_GROUPS = [["elementType","subtype"], ["crane",
  "stance"]]`), при расчёте доступности ОДНОЙ категории игнорируется
  фильтр ОБЕИХ категорий пары — это и разрывает порочный круг: снятие
  "Колонна" по-прежнему каскадом исключает её подтипы, но при проверке
  доступности самой "Колонна" фильтр подтипов больше не учитывается,
  поэтому "Колонна" остаётся ENABLED и кликабельной для возврата.
- `buildFilterGroup`/`buildHierarchicalFilterGroup` — рендерят ВСЕ
  значения из `allValuesFor` всегда, `input.disabled`+CSS-класс
  `.filter-disabled` (`opacity:.4`, `cursor:not-allowed`) — по
  `isValueEnabled`, состав списка никогда не меняется.
- Сортировка (`placementComparator`) — добавлен `{ numeric: true }` в
  `localeCompare` (родная возможность Intl-сравнения строк в JS) —
  "Стоянка 2" теперь сортируется перед "Стоянка 10".

**Проверено**: логика вынесена в изолированный Node-скрипт с точной
копией функций из `index.html` (сверено построчным diff, чтобы
гарантировать идентичность тестируемого и реального кода) и
воспроизведён ровно сценарий из обоих багов на смоделированных данных
(2 крана, стоянки "Стоянка 1"/"Стоянка 2"/"Стоянка 10", тип "Колонна" с
подтипом) — 10/10 проверок пройдено: значение не пропадает из списка
после снятия галочки, чекбокс остаётся кликабельным (не самоблокируется),
элемент корректно скрывается/возвращается на схеме, стоянки Крана 2
остаются сгруппированными под ним в иерархии, натуральная сортировка
подтверждена. Полная регрессия по всем 6 файлам старого стандарта и
целевому файлу не запускалась повторно в этом раунде (изменений в
бэкенд-парсинге не было, только клиентская логика фильтров) — сервер
перезапущен, стартовый автоимпорт `Input/` прошёл штатно.
Живой браузерной проверки не было (см. предыдущие записи).

**Файлы**: изменены `app/static/index.html`, `Docs/TZ.md` (раздел 6.5).

## Баги от заказчика по скриншотам: цвет зон, дублирующиеся имена стоянок (2026-07-16)

Заказчик прислал 3 скриншота (сохранены в `test_data/`) и словами описал три
симптома: (1) у всех зон кранов на схеме один и тот же цвет, хотя в
«Настройки → Цвета зон» видно, что каждому крану назначен свой; (2) в
иерархии фильтра у Крана 2 и Крана 3 нет ни одной стоянки, хотя на схеме
они отмечены, и на скриншоте схемы видно, что у всех трёх кранов есть
СВОЯ "Стоянка 1"; (3) отбор Кран/Стоянка = "не определено" не показывает
ни одного элемента. Попросили сначала объяснить причину и план
исправления, потом делать правки — так и было сделано.

### 1. Цвет зон

`renderZones()` красил полигон через `poly.setAttribute("fill", zone.color)`
— SVG-атрибут `fill="..."` **всегда проигрывает** CSS-правилу класса
(`.zone-Кран { fill: #c0392b }`), независимо от того, что подставлено
через JS и в каком порядке идёт DOM — это общее свойство каскада: обычный
презентационный атрибут имеет более низкий приоритет, чем любое реальное
правило в таблице стилей. Поэтому все краны красились в общий цвет из
CSS, а индивидуальные цвета из БД просто игнорировались на отрисовке.
**Исправлено**: `poly.style.fill`/`poly.style.stroke` (инлайн-стиль,
который, наоборот, побеждает правило класса) вместо `setAttribute`.

### 2-3. Одинаковые имена стоянок у разных кранов + путаница "нет крана"/"без стоянки"

Проверено прямым запросом к БД: "Стоянка 1"/"2"/"3" физически существуют
**трижды** — у каждого крана своя нумерация с единицы (id 620/641/644 —
три разные зоны с именем "Стоянка 1", каждая под своим краном). Фильтр
использовал **имя** зоны как значение/идентификатор — из-за этого
одноимённые зоны разных кранов схлопывались в один пункт списка, а
группировка "какой стоянке какой кран" (`.find()` по имени) всегда
находила первую совпавшую по имени зону — почти всегда одну из зон
Крана 1. В итоге Крану 2 и Крану 3 не доставалось ни одной стоянки в
дереве, хотя данные были верны (см. предыдущую запись, где привязка
стоянок к кранам уже была проверена и работала правильно на уровне БД).

**Исправлено**: значением фильтра для зональных категорий (Захватка/
Кран/Стоянка) теперь служит **id зоны**, а не имя — у элемента он уже
есть готовым (`zone_crane_id`/`zone_stance_id`/`zone_zakhvatka_id`),
резолвить зону для получения значения фильтра не нужно вовсе. Имя
используется только для подписи чекбокса, резолвится отдельной функцией
`zoneNameById()` — поэтому "Стоянка 1" теперь корректно появляется трижды,
по разу под каждым настоящим краном, как одноимённые файлы в разных
папках. Заодно поправлена сортировка (раньше "числовое" сравнение
включалось для любых числовых значений, включая id зон — для id это
бессмысленно, id не несёт порядка; теперь "сырое" числовое сравнение
применяется только к Отметке, остальное сортируется по подписи).

Заодно найдена и устранена путаница ДВУХ разных смыслов "не определено" у
стоянок, из-за чего отбор из п.3 (жалоба заказчика) давал пустой
результат: (а) элемент вообще не привязан ни к одной стоянке (обычный
случай — 1236 из 1401 элементов в целевом файле, большинство элементов
не стоят точно на месте парковки) — раньше ошибочно попадал в дерево ПОД
несуществующим заголовком "Кран: не определено"; (б) зона-стоянка не
сопоставилась ни с одним краном при разборе файла (для этого файла таких
нет — все 27 стоянок сопоставились, см. предыдущую запись). Проверка в БД
подтвердила: `zone_crane_status` — `'matched'` у 100% элементов, то есть
"Кран: не определено" для этого файла — легитимно пустое множество, а не
баг; настоящая проблема была именно в том, что (а) визуально пряталось
под ложным заголовком крана. **Исправлено**: (а) вынесено отдельным
плоским пунктом «Стоянка крана — без привязки» вне иерархии кранов;
заголовок «Кран: не определено» теперь появляется только если в файле
реально есть элементы вне всех зон кранов, либо стоянка без крана-хозяина
(б) — и в этом случае корректно показывается как disabled/пустой, а не
ловушка с нулевым результатом без объяснения.

**Проверено**: логика вынесена в изолированный Node-скрипт с моделью
данных, ТОЧНО повторяющей реальный случай (21 стоянка у Крана 1, по 3
одноимённые "Стоянка 1/2/3" у Крана 2 и Крана 3, id из реальной БД) —
12/12 проверок: все 28 стоянок (не 22) присутствуют как разные значения,
Кран 2 и Кран 3 получили по 3 стоянки каждый (было 0), одноимённые
"Стоянка 1" у разных кранов не путают друг друга при фильтрации,
"без привязки" не задевает фильтр по крану. Все проверенные функции
сверены построчным diff'ом с реальным кодом. Живой браузерной проверки
не было (нет инструмента автоматизации браузера в этой сессии) — стоит
перепроверить глазами: реальные цвета кранов на схеме, полный список
стоянок под Краном 2/3 в фильтре, поведение пункта «без привязки».

**Файлы**: изменены `app/static/index.html`, `Docs/TZ.md` (раздел 6.5).

## Уточнение: «без стоянки» — дочерний пункт под своим краном, не общий (2026-07-16)

Заказчик поправил моё понимание предметной области: «Стоянка крана» — не
точка, а рабочая зона крана в конкретной позиции (кран, стоя на стоянке
N, монтирует ЖБК в пределах этой зоны). Элементы без привязки к
конкретной стоянке (в целевом файле — 88% элементов, обычный случай) всё
равно физически находятся в рабочей зоне какого-то ОДНОГО, известного
крана. Предыдущая реализация показывала такие элементы одним общим
пунктом «Стоянка крана — без привязки» ВНЕ иерархии — по сути отрывая их
от крана, к которому они на самом деле относятся, хотя эта связь была
прекрасно известна (`zone_crane_status`).

**Исправлено**: элемент без конкретной стоянки, но с известным краном,
теперь получает не общий `PLACEMENT_NONE`, а псевдо-значение вида
`no-stance:<id крана>` (`stanceFilterValue()`), которое корректно ложится
ДОЧЕРНИМ пунктом «— без конкретной стоянки —» под СВОЙ кран в дереве
фильтра — рядом с обычными пронумерованными стоянками этого крана.
Настоящий `PLACEMENT_NONE` (общий пункт вне иерархии) остался только для
случая «и кран, и стоянка не определены одновременно» — для целевого
файла таких элементов нет вовсе, пункт не показывается.

**Проверено**: на реальных данных файла — у каждого из 3 кранов теперь
есть свой дочерний пункт «без конкретной стоянки» (Кран 1: 21 обычная
стоянка + 1 такой пункт = 22 элемента в списке; Кран 2/3: по 3 + 1 = 4);
общий `PLACEMENT_NONE` для категории "stance" в этом файле больше не
встречается вовсе (у всех элементов известен хотя бы кран). Сценарий
«показать элементы Крана 1 без конкретной стоянки» (выключить остальные
краны + все конкретные стоянки Крана 1, оставив только новый дочерний
пункт) корректно даёт 365 элементов, все — из Крана 1 и без стоянки.
Все проверенные функции сверены построчным diff'ом с реальным кодом.
Живой браузерной проверки не было.

**Файлы**: изменены `app/static/index.html`, `Docs/TZ.md` (раздел 6.5).

## 3D-режим схемы (2026-07-16)

Запрошено заказчиком: возможность покрутить схему в 3D, с той же
функциональностью по клику на элемент, что и в 2D. Уточнения от
заказчика, определившие реализацию: колонна — высота от своей отметки до
следующего уровня; ригель — квадратное сечение (высота = ширина); старый
стандарт входных данных больше не будет использоваться (можно не
поддерживать в 3D).

**Библиотека**: Three.js 0.160.0 — единственный реалистичный вариант для
WebGL 3D в браузере без написания слоя проекции/освещения/raycasting с
нуля. Подключение подтверждено пользователем отдельно (сработала защита
на внесение стороннего кода без явного согласия — правильно сработала,
пользователь подтвердил после объяснения). Файлы (`three.module.min.js`,
`OrbitControls.js`) скачаны через `npm install three@0.160.0` и
скопированы локально в `app/static/vendor/three/` — **не CDN**, чтобы
инструмент не зависел от интернета в рабочем режиме. Подключение — через
`<script type="importmap">` в `<head>` (резолвит bare-специфайер `three`
внутри `OrbitControls.js`) и ленивый динамический `import()` при первом
включении кнопкой «3D» — библиотека (~700 КБ) не грузится, пока
пользователь ни разу не открыл 3D.

**Архитектурное решение**: весь новый код — в существующем классическом
(не `type="module"`) `<script>`, используя `import()` как ВЫРАЖЕНИЕ (не
отдельный `<script type="module">`) — это позволило 3D-коду напрямую
использовать `state`/`selectElement`/`colorFor`/`passesPlacementFilters`
и т.п. без переноса в отдельный модуль и без "мостика" через `window` —
классический и модульный скрипт на одной странице НЕ делят lexical-
scope, а `let state = {...}` не становится свойством `window`
автоматически (в отличие от объявлений `function`), так что раздельный
`<script type="module">` потребовал бы это исправлять.

**Расчёт высоты — найдена и исправлена реальная ошибка в процессе
проверки**: изначально список "ярусов" для колонны считался по отметкам
ВСЕХ элементов (включая ригели). На реальном файле это давало неверный
результат: колонна с отметки 0 "видела" ближайшую отметку — не следующий
этаж колонн (+15800), а отметку периметрального ригеля-парапета на
промежуточной высоте (+5000) — и получала высоту всего 5000мм вместо
верных 15800мм. Исправлено — список ярусов теперь строится только по
отметкам, встречающимся у КОЛОНН (`computeColumnLevels()`), не у всех
элементов. Ширина сечения ригеля считается не по габаритному
прямоугольнику (bounding box — на повёрнутых элементах завышает обе
стороны), а из площади и периметра контура по формуле шнурков в
предположении прямоугольной формы — устойчиво к повороту (проверено на
синтетическом прямоугольнике 5000×400мм, повёрнутом на 37°: формула дала
точные 400мм, bounding-box ошибочно дал бы 3328мм).

**Интерактивность**: клик/двойной клик/правый клик по 3D-мешу —
raycasting → та же `selectElement()`/`openCtxMenu()`, что у 2D, без
дублирования логики карточки/контекстного меню. Фильтр по размещению
(`applyPlacementFilters`) и смена цвета при смене статуса/выборе
(`styleShape`) — расширены на пять строк каждая, чтобы синхронно
обновлять и 2D-фигуру, и 3D-меш (если сцена уже построена). При загрузке
новых данных (`loadPlan`) 3D-сцена пересобирается, только если 3D уже
была включена в этом сеансе — иначе просто ничего не делается, сцена
соберётся с нуля при первом включении.

**Проверено** (без браузера — нет инструмента автоматизации в этой
сессии): вычисления высот и ширин сечений — изолированным Node-тестом на
реальных данных целевого файла (уровни колонн — точно [0, 15800], высота
колонны нижнего яруса — 15800мм, ширина сечения реального ригеля —
450мм, что физически разумно) плюс синтетическая проверка устойчивости
формулы ширины к повороту контура. Все проверенные функции сверены
построчным diff'ом с реальным кодом. JS-синтаксис всего файла проверен
(`node --check`). Раздача новых статических файлов (`/static/vendor/
three/...`) проверена напрямую через сервер (200 OK). **Не проверено**:
собственно рендер в браузере (сцена/камера/свет/raycasting/OrbitControls
— вся часть, требующая реального WebGL-контекста) — стоит открыть
приложение и почовать: вращение камеры, клик по элементу в 3D (карточка
должна открыться так же, как в 2D), смену статуса через контекстное меню
в 3D, визуальную вменяемость высот колонн/ригелей.

**Файлы**: созданы `app/static/vendor/three/three.module.min.js`,
`app/static/vendor/three/OrbitControls.js`; изменены
`app/static/index.html`, `Docs/TZ.md` (новый раздел 6.7).

## 3D: зоны (захватка/кран/стоянка) (2026-07-16)

Продолжение предыдущей записи — заказчик уточнил, что в 3D не хватает зон.
Правило от заказчика: захватка — объём на всю высоту здания (подсветить
так, чтобы элементы внутри остались видны); кран и стоянка — плоские
полигоны на уровне 0 (не объём).

**Реализовано**: `build3DZoneMesh()` — захватка строится экструзией
контура зоны от нижнего яруса колонн до верха здания
(`computeBuildingHeightRange()` — верх считается тем же запасным
приёмом, что и высота самой верхней колонны: последний ярус + высота
последнего пролёта), материал полупрозрачный (7% непрозрачности — то же
значение, что у подложки зон в 2D, `fill-opacity:.07`). Кран/Стоянка —
плоский `ShapeGeometry` на Y≈0 (небольшой отступ 10мм от нуля — чтобы не
мерцало от наложения с полом/сеткой при рендере), непрозрачность 35%.
Цвет — свой у каждого крана и унаследованный его стоянками (та же связь
"стоянка → кран" и та же палитра, что уже резолвится на бэкенде для 2D,
см. `zone.color` в ответе `/plan-data`), у захватки своего цвета нет —
фолбэк на тот же оттенок, что и в 2D CSS (`#1353d6`). И захватка, и
кран/стоянка — `depthWrite:false`, чтобы несколько наложенных
полупрозрачных зон не давали артефактов сортировки глубины. Видимость
зон синхронизирована с существующими тумблерами "Зоны" (сайдбар →
Фильтры) — `apply3DZoneVisibility()` вызывается из того же обработчика,
что и 2D `renderZones()`, без пересборки геометрии. Зоны не входят в
список объектов для raycasting (там участвуют только меши элементов) —
клик всегда выбирает элемент, а не зону, даже если зона визуально сверху.

**Проверено**: расчёт диапазона высоты здания и парсинг цвета — Node-
тестом на реальных данных целевого файла (диапазон 0-31600мм: нижний
ярус колонн 0, верх = 15800 + (15800-0) = 31600; цвет захватки —
фолбэк `#1353d6`, цвет крана — собственный `#c0392b` из БД, корректно
распарсен в число для `THREE.Color`); все 34 зоны файла имеют валидный
контур (≥3 точки). Функции сверены построчным diff'ом с реальным кодом,
JS-синтаксис проверен. Живой браузерной проверки рендера по-прежнему не
было (нет инструмента автоматизации браузера в этой сессии).

**Файлы**: изменены `app/static/index.html`, `Docs/TZ.md` (раздел 6.7).

## Баг: зеркальность 3D-плана (2026-07-16)

Заказчик прислал скриншоты 2D и 3D одного и того же чертежа — по
ощущению 3D выглядела зеркальной. Проверка подтвердила: реальный баг в
переносе координат.

**Причина**: контур элемента/зоны переносился в 3D через
`shapeY = -y` (инверсия) ПЕРЕД `geometry.rotateX(-90°)`. Раскладка
матрицы поворота на -90° вокруг X сама по себе даёт `мировая Z = -shapeY`
— то есть с уже применённой инверсией `shapeY=-y` итоговое `мировая
Z = -(-y) = +y` — прямое (без инверсии) соответствие мировой Z и
исходного Y чертежа. Формально это похоже на "ничего не сломано" (просто
перенос осей), но у право-ориентированной системы координат с осью Y
вверх (как в WebGL/Three.js, так и вообще везде в 3D-графике) перенос
плоского 2D-плана (X вправо, Y вверх на бумаге) в горизонтальную
плоскость X-Z **математически не может** одновременно сохранить И
"право" (X), И "верх" (Y→Z) без инверсии одной из осей — иначе при
взгляде на сцену сверху (или под углом, как в скриншотах) картинка
неизбежно получается ЛИБО зеркальной по одной оси, ЛИБО перевёрнутой по
другой. Прежний код (без нужной инверсии) давал именно зеркальность.

Численно проверено на асимметричном тестовом контуре (прямоугольник со
срезанным углом — форма, аналогичная реальному зданию на скриншотах,
которое имеет срез в правом верхнем углу плана): полный цикл
"dxf-координаты → построение shape → поворот геометрии → мировые
координаты → проекция через базис камеры (метод lookAt, идентичный
внутреннему устройству Three.js)" — с исходным кодом срезанный угол
проецировался на ЛЕВУЮ сторону экрана вместо правой (зеркальность
подтверждена эмпирически, не только теоретическим выводом); после
исправления — корректно на правую сторону.

**Исправлено**: убрана лишняя инверсия при построении контура (`shapeY
= y` вместо `-y`) — у элементов и у зон. Побочный эффект — меняется
порядок обхода контура (winding), а с ним направление нормалей граней
после экструзии; чтобы это не привело к отбраковке "невидимых" граней
(`MeshStandardMaterial` по умолчанию рисует только лицевые грани),
материал элементов дополнительно переведён на `side: THREE.DoubleSide`
(у материала зон это уже было сделано раньше, по другой причине —
прозрачность). Также поправлен расчёт центра камеры
(`fit3DCameraToData`) — цель по Z теперь берётся с тем же знаком, что и
новое соответствие осей, иначе камера целилась бы не в центр модели, а в
его зеркальное отражение.

**Проверено**: изолированным Node-скриптом, реализующим ТОЧНО ту же
математику (матрица поворота -90° вокруг X + базис камеры lookAt) на
синтетическом асимметричном контуре — до фикса срез оказывался слева
(неверно), после — справа (верно, совпадает с положением в исходных
dxf-координатах). Само число проверок формально невелико, но проверка —
не на правдоподобие, а на прямое сравнение "было/стало" с одинаковой
методикой, что даёт высокую уверенность в правильности причины и фикса.
JS-синтаксис всего файла проверен. Живой браузерной проверки по-прежнему
не было.

## Раунд доработок UI/3D по итогам тестирования (2026-07-16)

Девять замечаний одним списком: подсказка при наведении в 3D,
переключатель 2D/3D, ширина поля рабочей даты, переименования подписей
в фильтрах, объединение "Зоны"+"Подписи" в один блок, сворачиваемая
иерархия кран/стоянка (и тип/подтип), медленный зум в 3D.

### 1. Подсказка при наведении в 3D — ✅ реализовано

При задержке курсора на элементе дольше 1с (без вращения камеры)
показывается подсказка: тип, подтип, марка, ID, адрес по осям, отметка.
`on3DMouseMove` на каждое движение мыши делает raycasting
(`pick3DElementAt`, переиспользован от клика), при смене элемента под
курсором сбрасывает таймер и планирует новый через `setTimeout(…, 1000)`
— подсказка показывается, только если элемент под курсором не изменился
к моменту срабатывания таймера. Скрывается: при вращении камеры
(`threeDragging`), уходе курсора с холста (`mouseleave`), клике/
контекстном меню, переключении в 2D. Текст подсказки строится через
`createElement`/`textContent` (не `innerHTML`) — те же соображения
безопасности, что и у остального кода, работающего со строками из DXF
(марка и т.п.).

**Проверено**: JS-синтаксис файла (`node --check`) — чисто. Формирование
строк подсказки — Node-тестом на реальных данных целевого файла:
корректный вывод марки/ID, и отдельно — что нулевая отметка
(`elevation_mm === 0`) показывается как "0 мм", а не как "—" (типичная
ошибка truthy-проверки `value || "—"`, в реализации так не сделано —
explicit `=== null || === undefined`).

### 2. Переключатель 2D/3D — ✅ реализовано

Залипающая кнопка "3D" заменена на сегментированный контрол с обеими
подписями видимыми одновременно ("2D"/"3D"), активный режим подсвечен
заливкой. `setViewMode(mode)` — общая функция для обоих направлений
переключения вместо прежнего `toggle3D()`.

### 3. Ширина поля рабочей даты — ✅ реализовано

`#workdate-input`: `172px` → `192px` — кнопка вызова календаря
перестала перекрывать последнюю цифру.

### 4–5, 8. Переименования подписей в фильтрах — ✅ реализовано

- "— без конкретной стоянки —" → "вне стоянок" (`stanceLabelFor`).
- Группа "Отметка" → "Отметка (высота)" (заголовок группы в
  `buildFilterGroup`).
- Группа "Фильтр по размещению" → "Фильтры" (заголовок `<h3>` в разметке
  вкладки).

### 6. Объединение "Зоны" + "Подписи" в блок "Настройка отображения" — ✅ реализовано

Разметка вкладки "Фильтры" перестроена: общий `<h3>Настройка
отображения</h3>`, под ним `.display-settings-row` (flex, `gap:20px`) с
двумя колонками `.display-settings-col` — "Отображение зон" (бывшие
тумблеры "Зоны") и "Подписи", элементы внутри каждой колонки по-прежнему
вертикальным списком, как раньше. Технически это только визуальная
перегруппировка разметки — оба набора тумблеров остаются независимыми
DOM-контейнерами (`#zone-toggles`, `#label-toggles`) с прежней логикой
рендера/обработчиков.

### 7. Сворачиваемая иерархия "Кран → Стоянка" и "Тип → Подтип" — ✅ реализовано

По умолчанию у родителя (крана / типа элемента) видна только его
строка — это и есть выбор "всей группы" целиком, как и было раньше по
смыслу. Рядом со строкой родителя — кнопка-стрелка (▸/▾), если у него
вообще есть потомки; клик разворачивает список конкретных
стоянок/подтипов для точечного выбора. Состояние развёрнутости хранится
в `state.placementGroupsExpanded = {crane: Set, elementType: Set}` (а не
только в DOM) — иначе разворот сбрасывался бы при каждой полной
перерисовке фильтров (`onPlacementFilterChange` пересобирает DOM целиком
на любой клик, это обязательное свойство текущей архитектуры фильтров,
см. выше по документу). Сбрасывается кнопкой "Сбросить все фильтры" и
`clearWorkspace()` (смена чертежа), как и остальные фильтры. Клик по
чекбоксу родителя по-прежнему каскадно ставит/снимает всех потомков
независимо от того, развёрнута сейчас группа или нет.

**Проверено**: JS-синтаксис файла. Логика построения `stancesByCrane`/
`craneHeadings` (наполнение дочерних списков под правильным краном,
включая псевдо-значение "вне стоянок") не менялась в этом раунде —
только визуальное сворачивание уже существующей и ранее проверенной
иерархии; сверено построчным просмотром итогового кода на предмет
согласованности с остальной архитектурой фильтров (allValuesFor/
isValueEnabled/PLACEMENT_CASCADE_GROUPS).

### 9. Медленный зум в 3D — ✅ найдена причина, исправлено

**Причина**: `OrbitControls.getZoomScale()` нормализует шаг колеса через
`event.deltaY / (100 * devicePixelRatio)`. Трекпад Mac при плавной
инерциальной прокрутке шлёт много событий с очень маленьким `deltaY` —
с библиотечным дефолтом `zoomSpeed=1` каждое такое событие меняло
масштаб на доли процента, из-за чего заметное приближение/отдаление
требовало очень долгой прокрутки. Не связано с масштабом самой сцены
(мм) — чувствительность к вводу, а не к единицам мира.

**Исправлено**: `controls.zoomSpeed = 4` в `init3DScene()` — без правки
самой библиотеки.

**Общая проверка раунда**: JS-синтаксис всего файла (`node --check`) —
чисто, `python3 -m py_compile` бэкенда — чисто, сервер перезапущен и
отвечает `200` на `/health` и на статику 3D-библиотеки. Живой браузерной
проверки по-прежнему не было (нет инструмента автоматизации браузера в
этой сессии) — все проверки выполнены методом изолированного
воспроизведения логики на реальных данных из БД плюс построчная сверка
кода, как и в предыдущих раундах этой сессии.

**Файлы**: изменены `app/static/index.html`, `Docs/TZ.md` (разделы 6.5,
6.7).

## Лесенка сужения зоны стоянки крана с высотой (2026-07-16)

Заказчик указал на пробел: элементы на отметках выше 0 практически никогда
не попадали ни в одну стоянку крана (`zone_stance_status` был либо
`not_applicable`, либо `unmatched` почти у всех — 88% элементов файла), хотя
кран физически монтирует их именно со своей стоянки. Причина —
`zone_binding._candidates_for_category` требовала точного совпадения
отметки элемента с отметкой полигона стоянки, а полигоны стоянок в реальном
файле нарисованы только на отметке 0 (земля) — выше нигде совпадать было
не с чем.

Заказчик описал реальную физику: кран стоит у одного (ближнего к себе) края
своей стоянки и наклоняет стрелу к началу ряда стоянок (см. фото крана в
`test_data/package-lock.jpeg` — стрела резко задрана от крана вверх-в
сторону). Стрела — прямая линия от низкого основания (у крана) до высокой
точки (у дальнего от крана края), поэтому с каждым этажом именно ближний к
крану участок первым выходит из зоны охвата, а дальний остаётся доступен
дольше — "лесенка": на каждом следующем ярусе колонн ближний к крану край
зоны стоянки отступает на 1 реальный пролёт сетки осей, а освободившееся
место подхватывает следующая по номеру стоянка того же крана. Первая
стоянка — дальний край (начало ряда) никогда не отступает; последняя —
ближний к крану край никогда не отступает (забирает всё, что осталось).
Перпендикулярная ряду сторона не меняется ни на одном ярусе.

Согласование заняло несколько раундов уточнений (числовые примеры от
заказчика проверялись на реальных осях/координатах Крана 1 из БД — оси
файла в этом ряду скачут через 2, 1/3/5/7/9/…, что и дало несовпадение
первого черновика формулы с примером, пока не подтвердилось: "отступить на
один реальный пролёт сетки, номера осей могут быть пропущены" + на каком
крае лесенка отступает — "это не дальний, а ближний к крану" край, крану
у своей стоянки).

**Реализовано**:
- `scripts/zone_binding.compute_column_tier_elevations` — ярусы здания
  (уникальные отметки элементов "Колонна", по возрастанию — тот же приём,
  что и `computeColumnLevels()` в 3D, см. `Docs/TZ.md` §6.7).
- `scripts/zone_binding.build_stance_level_polygons` — для каждой стоянки
  крана готовит список shapely-полигонов по ярусам: ярус 0 — исходный
  полигон стоянки без изменений (подрезка по сетке на нулевом ярусе срезала
  бы небольшой технологический зазор между контуром и осью без всякой
  причины); ярус k>0 — окно вдоль оси, вдоль которой физически расположен
  ряд стоянок этого крана (числовая или буквенная, определяется автоматически
  по тому, вдоль какой координаты стоянки одного крана расходятся сильнее),
  подрезанное полигоном ЦЕЛОЙ рабочей зоны крана (не отдельной стоянки — не
  из чего иначе подхватывать уступленное соседом). Стоянки без однозначного
  крана или без номера в названии — исключение, лесенка не применяется.
- `bind_element_to_zones` — новые опциональные параметры
  `stance_level_polys`/`tier_elevations`; если оба переданы, категория
  "Стоянка" использует лесенку по ярусу отметки элемента (зажимается к
  крайнему известному ярусу, чтобы ни один элемент не остался без
  возможности отнесения из-за отметки чуть вне диапазона), иначе (для
  старых вызовов — `scripts/verify_zone_pipeline.py`) — прежнее поведение
  без изменений.
- `app/dxf_import.py` — сетка осей теперь строится ДО привязки элементов
  к зонам (раньше — после; лесенке нужны реальные оси заранее),
  `new_standard_pipeline.process()` получает её новым параметром
  `axis_grid`.

**Проверено**: изолированным Python-скриптом на реальных зонах/сетке осей
Крана 1 из БД — числовой пример заказчика (Стоянка 1 = оси 1–9, Стоянка 2 =
оси 9–11, этаж 2: Стоянка 1 → 1–7, Стоянка 2 → 7–9) воспроизведён точно;
проверена непрерывность окон на этаже 2 по всем 21 стоянкам Крана 1 (ни
разрывов, ни наложений); перпендикулярная сторона окна не меняется между
ярусами. `scripts/verify_zone_pipeline.py` (старый синтетический тест на
точное совпадение отметки) — все проверки пройдены без изменений, обратная
совместимость подтверждена. После переобработки реального файла через
перезапуск сервера (тот же путь, что и загрузка через UI) — доля
`zone_stance_status='matched'` на отметке 15800 выросла с 0% до 100%
(966 из 966 элементов), сводка по всем 1401 элементам файла: 1194
matched / 207 unmatched (без единого `needs_review` — окна стоянок не
пересекаются по построению), `needs_review` не выросло. Оставшиеся
unmatched — все на отметках 0 и 5000, у всех при этом кран определён
корректно, стоянка не найдена из-за небольших зазоров между нарисованными
контурами стоянок на плане (тот же класс проблемы, что и раньше, не
регрессия от этого раунда) — заказчик подтвердил, что исходный файл пока
не наполнен всеми данными.

**Файлы**: изменены `scripts/zone_binding.py`,
`scripts/new_standard_pipeline.py`, `app/dxf_import.py`, `Docs/TZ.md`
(раздел 3.7).

## Баг: включение стоянки не работало при выключенном кране (2026-07-16)

Заказчик описал неудобный сценарий: снять галочки со всех кранов, затем
включить одну конкретную стоянку — на схеме ничего не появлялось, потому
что галочка с крана оставалась снятой (`passesPlacementFilters` — И между
всеми категориями, крана достаточно, чтобы исключить элемент независимо
от стоянки). Обходной путь (включить кран целиком — каскадом включаются
все его стоянки — и вручную выключать все ненужные) слишком долгий.

**Исправлено**: в `buildHierarchicalFilterGroup` (`app/static/index.html`)
обработчик клика по дочернему чекбоксу (стоянка/подтип) — при включении
потомка, если его родитель (кран/тип) сейчас исключён, родитель тоже
включается, но остальные потомки родителя не трогаются (это делает только
прямой клик по самому родителю — каскадом на всех). Общая функция для
обеих иерархий (Кран/Стоянка и Тип/Подтип) — фикс действует одинаково в
обоих местах.

**Проверено**: изолированным JS-тестом на Set-логике, воспроизводящим
точный сценарий заказчика (все краны/стоянки выключены → включить одну
стоянку одного крана) — кран включился, кликнутая стоянка включилась,
остальные стоянки этого крана и другой кран не изменились. JS-синтаксис
файла проверен, сервер перезапущен и отвечает `200` на `/health`.

**Файлы**: изменены `app/static/index.html`.

## Групповая смена статуса + фильтр «Поставщик/Контракт» (2026-07-16)

Заказчик: нужна возможность быстро менять статус у большого количества
объектов сразу. Согласовано (несколько уточняющих вопросов через
AskUserQuestion): выделение рамкой в 2D (Shift+перетаскивание, обычное
перетаскивание остаётся панорамированием), после выделения — модалка с
таблицей выбранных элементов, у каждой строки свой контракт, в шапке
можно одним действием заполнить контракт всем пока незаполненным
строкам, статус меняется только когда контракт указан у ВСЕХ строк.
Позже, уже во время реализации, заказчик добавил: возможность убрать
лишнюю строку из таблицы (если рамка захватила лишнее) и цветовой
индикатор текущего статуса в строке — оба пункта естественно легли в ту
же таблицу, что и строилась.

**Backend** (`app/contracts.py`, `app/main.py`, `app/models.py`):
- Извлечена общая функция `apply_status_change()` (`app/contracts.py`) —
  тело одиночного `PATCH /elements/{id}/status`, вынесенное как есть
  (INSERT history → пересчёт `current_status` → пересчёт кэша
  `contract_id` → `contract_line_warning`), НЕ коммитит сама — коммит на
  стороне вызывающего. Одиночный эндпоинт переписан на вызов этой
  функции без изменения поведения (проверено вручную — идентичный ответ).
- Новый `PATCH /elements/bulk-status` — принимает список
  `{element_id, contract_id}` + один статус на всех, для каждого элемента
  вызывает `apply_status_change` (контракт всегда `explicit=True` — фронт
  гарантирует явный выбор в каждой строке, включая осмысленное «без
  контракта»), один `conn.commit()` после всего цикла — вся пачка
  применяется атомарно. Отсутствующие id — 404 со списком; пустой список
  — 400. Права — `require_editor`, как и у одиночного эндпоинта.

**Frontend** (`app/static/index.html`):
- Выделение рамкой — `Shift+mousedown+drag` на `#stage` (новая ветка в
  существующих pan-обработчиках, `rubberBandActive`), визуальный оверлей
  `#rubber-band`. На отпускании — экранный прямоугольник переводится в
  мировые координаты (`screenRectToWorldBBox`) и все элементы, чей центр
  внутри и которые сейчас проходят фильтры (`passesPlacementFilters`),
  попадают в `state.multiSelectedIds`. Подсветка — CSS-класс
  `.multi-selected` (оранжевый пунктир), независимая от одиночного
  `.selected` (обе могут быть на разных элементах одновременно).
- Плавающая панель `#multi-select-panel` — счётчик + «Изменить статус…» +
  «✕».
- Модалка `#bulk-status-backdrop` — таблица строк (ID/марка/тип/статус с
  цветным бейджем/контракт-select/удалить), в шапке — новый статус и
  контракт для массового заполнения пустых строк (только по тем строкам,
  чей тип элемента подходит выбранному контракту — остальные пропускает).
  Кнопка «Применить» заблокирована, пока хоть одна строка не имеет
  явного выбора контракта.
- Новая иерархическая группа фильтра «Поставщик / Контракт» — тем же
  приёмом, что «Кран/Стоянка» и «Тип/Подтип»
  (`buildHierarchicalFilterGroup`, третья пара в `PLACEMENT_CASCADE_
  GROUPS`), источник значений — `state.contracts` (из `/plan-data`), не
  элементы схемы.
- Заодно (см. предыдущую запись выше) — тот же принцип «включение
  потомка включает и выключенного родителя» уже действовал на
  Кран/Стоянка и Тип/Подтип, теперь автоматически работает и для
  Поставщик/Контракт, т.к. использует ту же функцию.

**Важное открытие при вёрстке рамки выделения**: у `#svg-root` дочерняя
группа `<g id="flip" transform="scale(1,-1)">` — все элементы схемы
рисуются РЕАЛЬНЫМИ мировыми координатами без инверсии
(`renderElements` кладёт `element.x/element.y` как есть), а видимую
ориентацию «Y вверх» даёт именно этот flip. Значит `viewBox` сам живёт в
стандартном SVG-пространстве (Y вниз), и экран→мир для Y требует
инверсии: `worldY = -(viewBoxY)`, тогда как X — без инверсии. Подтверждено
`state.initialView = {x: b.min_x, y: -b.max_y, ...}` (та же связь). Без
этого разбора рамка выделения работала бы отзеркаленной по вертикали.

**Проверено**:
- JS-синтаксис файла (`node --check`), `python3 -m py_compile` бэкенда.
- Изолированный Node-тест формулы экран→мир (`screenRectToWorldBBox`) —
  round-trip на синтетических точках и реалистичный сценарий (прямоугольник
  вокруг известной мировой области) — совпадение с точностью до 0.01мм.
- Backend end-to-end через живой сервер и `curl` с реальной сессией:
  `PATCH /elements/bulk-status` на 3 реальных элементах — статус и
  контракт применились корректно, история корректна; повторная проверка с
  реальным тестовым контрактом (создан через `POST /contracts`,
  подтверждено `element_types` в `/plan-data`) — контракт корректно
  проставился через bulk-эндпоинт; 404 на несуществующий id; 400 на
  пустой список; одиночный `PATCH /elements/{id}/status` — не изменился
  после рефакторинга (сравнение до/после). Все тестовые изменения статуса
  и тестовый контракт **удалены после проверки** (через штатные
  `DELETE /elements/{id}/history/{id}` — не сырым SQL, чтобы кэши
  `current_status`/`contract_id` пересчитались корректно; тестовый
  контракт без DELETE-эндпоинта — убран напрямую из БД, к этому моменту
  на него уже ничего не ссылалось).
- Сервер перезапущен, `/health`/`/static/index.html` — `200`.
- Живой браузерной проверки (сама рамка мышью, модалка) по-прежнему не
  было — нет инструмента автоматизации браузера в этой сессии, нужна
  ручная проверка golden path.

**Файлы**: изменены `app/static/index.html`, `app/contracts.py`,
`app/main.py`, `app/models.py`, `Docs/TZ.md` (разделы 4, 4.2 новый, 5,
6.2, 6.5).

## Раунд из 6 пунктов — запрошено 2026-07-16, реализовано 2026-07-16

Заказчик сначала попросил зафиксировать список без реализации, затем
дал добро ("делай"). Пункты 1, 2, 4, 5, 6 реализованы и проверены в этом
же раунде; пункт 3 остаётся отложенным (меняет схему БД, см. его
собственную пометку) — нумерация ниже сохранена как в исходном списке.

### 1. Проверка остатка контракта при групповой установке — ✅ реализовано

В модалке массовой смены статуса (`#bulk-status-backdrop`,
`app/static/index.html`) — когда контракт назначается группе элементов
(построчно или через «Заполнить пустые»), нужно проверить: по каждой
связке (контракт, тип элемента), затронутой в текущей пачке, сколько
элементов этого типа в пачке получат этот контракт, и сравнить с
остатком контракта по этой строке (`remaining = quantity - fact`, уже
считается в `app/contracts.py`, `_to_contract_out`/`ContractLineOut`).
Если запрашиваемое количество превышает остаток — предупредить и явно
сообщить, сколько элементов ДОСТУПНО присвоить этому контракту по этой
строке прямо сейчас.

**Решено (уточняющие вопросы 2026-07-16):**
- Предупреждение **неблокирующее** — как существующий
  `contract_line_warning` для одиночной смены статуса, применение
  разрешено даже при превышении.
- Пересчитывать и показывать **сразу при любом изменении строки**
  (ручной выбор контракта в строке ИЛИ «Заполнить пустые») — не
  откладывать до нажатия «Применить».

**Реализовано**: `state.contracts` (из `/plan-data`) не содержит
`quantity`/`fact`/`remaining` — при открытии bulk-модалки (`openBulk
StatusModal`, теперь `async`) отдельно грузится снимок `GET /contracts`
(уже возвращает эти поля) в `bulkContractLines`. Новая функция
`updateBulkContractWarning()` (вызывается из `updateBulkStatusValidation`
— т.е. при каждом изменении строки и при "Заполнить пустые") по каждой
паре (контракт, тип элемента) в текущей пачке считает `newlyRequested` =
сколько строк ВПЕРВЫЕ получат этот контракт (строки, у которых этот же
контракт УЖЕ был — не в счёт, они и так уже в `fact`), сравнивает с
`remaining` строки контракта. Текст — в новый `.warning-text`
`#bulk-status-contract-warning` (жёлтый, отдельно от блокирующего
красного `#bulk-status-error`), не блокирует кнопку "Применить".

**Проверено**: изолированным Node-тестом чистой логики (объединение
requested/already по Map, сравнение с остатком) — 4 сценария: превышение
(3 запрошено, остаток 2 → предупреждение с точным числом), не превышение
(часть элементов уже имела этот контракт → newlyRequested в пределах
остатка → без предупреждения), "без контракта" не учитывается, тип без
строки в контракте не проверяется. JS-синтаксис файла, живой сервер +
`curl` — `GET /contracts` отдаёт `remaining` в ожидаемом формате,
`/plan-data` не сломан.

### 2. Убрать дублирующий пункт "Видимость подписей" из настроек — ✅ реализовано

`#menu-labels` (кнопка в выпадающем меню настроек, `app/static/
index.html:439`) + модалка `#labels-backdrop` (~3084) — та же
функциональность уже есть на вкладке "Фильтры" (блок "Подписи" внутри
"Настройка отображения", см. `#label-toggles`, Docs/TZ.md §6.5).

**Уточнение при реализации**: выяснилось, что это не полные дубли —
пункт в настройках был АДМИНСКОЙ персистентной настройкой (`PUT
/label-visibility`, только admin, общая для всех, переживает
перезагрузку), а тумблеры на "Фильтры" — сессионные, только визуальные,
ни для кого не сохраняются. Заказчик подтвердил: не менять
функциональность, просто убрать точку входа из настроек — сделано без
переноса персистентности (backend-эндпоинт `PUT /label-visibility`
оставлен как есть, просто больше не используется из UI).

**Реализовано**: убраны кнопка `#menu-labels`, модалка `#labels-backdrop`
и её JS-обработчики (`labelsBackdrop`, click-листенеры) целиком.
`renderLabelToggles()` (используется вкладкой "Фильтры") не тронут.

### 3. Учёт повреждённых элементов + переработка формы контракта — ✅ реализовано

(Повтор более раннего запроса в этой же сессии — заказчик подтвердил,
что реализации ещё не видит, значит откладывается до отдельного плана,
не забыто.) У контракта — учёт инцидентов повреждения на стройке: каждый
инцидент — дата, описание, тип элемента, количество; привязан к
конкретному контракту. Остаток "доступно для монтажа" по строке
контракта (`ContractLineOut.remaining`, `app/contracts.py`) должен
дополнительно вычитать количество повреждённых элементов этого типа по
этому контракту. Форма редактирования контракта
(`#contract-edit-backdrop`/`openContractEdit`, `app/static/index.html`)
переделывается в полноценную форму с ДВУМЯ табличными частями: (а) уже
существующие строки план-поставки (тип элемента + количество), (б) новая
таблица инцидентов повреждений. Требует новую таблицу в БД (например
`contract_incidents`: id, contract_id, element_type, quantity, date,
description) + новые API-эндпоинты. **Меняет схему БД — начинать со
`EnterPlanMode`, не реализовывать с ходу.**

**Решено (уточняющие вопросы 2026-07-16):**
- Инцидент — это **только количество** (тип элемента + число), БЕЗ
  привязки к конкретным ID элементов схемы. Не нужно выбирать, какие
  именно `elements` пострадали — `contract_incidents` не ссылается на
  `elements.id`.
- **Без отдельного статуса подтверждения** — любая внесённая запись
  инцидента сразу считается и сразу вычитается из остатка контракта
  (правка/удаление ошибочной записи — как правка строк контракта
  сейчас, тем же человеком, кто её внёс).

**Реализовано** (план — `.claude/plans/distributed-singing-rossum.md`,
через `EnterPlanMode`, как и договаривались из-за смены схемы БД):
переиспользован СУЩЕСТВУЮЩИЙ паттерн `contract_lines` — инциденты не
получили отдельных API-эндпоинтов, а стали ещё одним дочерним списком
контракта (`ContractIn.incidents`/`ContractOut.incidents`),
сохраняются/заменяются в той же транзакции и тем же `POST /contracts`/
`PATCH /contracts/{id}`, что и строки плана (`DELETE FROM
contract_incidents WHERE contract_id=?` + заново INSERT всех присланных
при каждом сохранении — полная замена, как у строк). Это заодно и
решило вопрос "у нового контракта ещё нет id, куда привязывать
инцидент до сохранения" — не нужно, инциденты уходят на сервер вместе
с созданием контракта одним запросом.

- `app/schema.sql` — новая таблица `contract_incidents` (id, contract_id
  → CASCADE, element_type, quantity, incident_date, description). Новая
  ТАБЛИЦА, не колонка существующей — `_COLUMN_MIGRATIONS`/
  `_apply_migrations` (`app/db.py`) не нужны, `init_db()` и так
  перевыполняет весь `schema.sql` на каждом старте.
- `app/contracts.py` — `ContractIncidentIn`/`ContractIncidentOut`,
  `ContractLineOut.damaged`, новая `_line_damaged()` (симметрично
  `_line_fact()`). `_to_contract_out`: `remaining = quantity - fact -
  damaged`, `exceeded = (fact + damaged) > quantity`. `contract_line_
  warning` — тот же учёт `damaged` в пороге срабатывания + поле
  `damaged` в возвращаемом предупреждении.
- `app/models.py` — `ContractWarning.damaged: int = 0`.
- Форма контракта (`#contract-edit-backdrop`, теперь `.modal.wide`) —
  вторая табличная часть "Инциденты повреждений на стройке":
  `addContractIncidentRow()` (дата/тип/количество/описание, тип — тот же
  `datalist` `#ce-known-types`, что уже подключён к строкам плана).
  **Написана через `createElement`/`.value=`, БЕЗ `innerHTML` с
  шаблонными литералами** — в отличие от соседнего `addContractLineRow`
  (существующий, не трогали), который использует непроверенный
  innerHTML — это отдельный документированный техдолг (см. раздел
  "Исправления по безопасности" ниже по файлу), не стали его попутно
  чинить, но новый код сразу написан безопасно.
- `renderContractsList` — колонка "Повреждено" в таблице контракта.
  `maybeWarnContract` — тост дописывает "повреждено N", если есть.
- **Бонус без доп. работы**: bulk-модалка массовой смены статуса (прошлый
  раунд) уже брала `remaining` из `GET /contracts` — автоматически
  учла повреждения в проверке остатка при групповой установке контракта,
  без единой правки в её собственном коде.

**Проверено**: `python3 -m py_compile`/`node --check`. Живой сервер +
`curl` с реальной сессией — создан тестовый контракт (строка Колонна,
план=20) с инцидентом (Колонна, повреждено=3) → `GET /contracts` вернул
`remaining=17` (20−0−3), `damaged=3` — точное совпадение с формулой.
`PATCH .../contracts/{id}` с `incidents: []` корректно стёр старый
инцидент, `remaining` вернулся к 20. Реальная смена статуса элемента
(Колонна, contract_id этого тестового контракта после увеличения
инцидента до quantity=20) вызвала `contract_line_warning` с полем
`damaged: 20` в ответе (1 факт + 20 повреждено > 20 плана) — предупреждение
корректно учитывает повреждения, не только факт. Все тестовые
изменения (запись истории элемента, тестовый контракт/строка/инцидент)
удалены после проверки — реальный контракт заказчика (id=2, "123",
ДСК1) не тронут, поля `damaged`/`incidents` на нём корректно вернулись
0/пусто. Сервер перезапущен, `/health` — `200`. Живой браузерной
проверки формы по-прежнему не было.

**Файлы**: изменены `app/schema.sql`, `app/contracts.py`,
`app/models.py`, `app/static/index.html`, `Docs/TZ.md` (раздел 5).

### 4. Подсказка типа элемента при добавлении строки контракта — ✅ реализовано

`addContractLineRow()` (`app/static/index.html:~3400`) — поле
`.ce-line-type` сейчас обычный `<input type="text">` со свободным
вводом. Нужно предлагать варианты из уже известных после загрузки файла
типов элементов (`ZHBI_ELEMENT_TYPES` = Колонна/Ригель/Плита/Панель,
`app/models.py`, либо `Object.keys(state.labelVisibility)` — тот же
источник, что уже используют `renderDefaultContracts`).

**Решено (уточняющие вопросы 2026-07-16)**: `<input>` + `<datalist>` —
подсказка известными типами, но поле остаётся свободным текстовым
вводом (не строгий `<select>`), можно ввести и что-то нестандартное.

**Реализовано**: `<datalist id="ce-known-types">` в разметке модалки
`#contract-edit-backdrop`, поле `.ce-line-type` получило `list="ce-known-
types"`. Заполняется в `openContractEdit()` из `Object.keys(state.
labelVisibility)` при каждом открытии формы (актуальные типы текущего
чертежа).

### 5. Сократить название приложения до "ЖБИ" — ✅ реализовано

Сейчас "ЖБИ — контроль поставки и монтажа" встречается в трёх местах:
`app/static/index.html:5` (`<title>`), `app/static/index.html:413`
(`<h1>` в шапке), `app/main.py:41` (`FastAPI(title=...)`, влияет на
OpenAPI-документацию).

**Решено (уточняющие вопросы 2026-07-16)**: менять **все три места** на
просто "ЖБИ", для консистентности.

**Реализовано**: заодно нашлось и поправлено четвёртое место —
`app/pdf_export.py`, подпись "Сформировано автоматически системой ЖБИ —
контроль поставки и монтажа..." в подвале PDF-экспорта. Все четыре
места — теперь просто "ЖБИ".

**Проверено**: `curl .../openapi.json` → `info.title == "ЖБИ"`.

### 6. Накопительное выделение рамкой (несколько групп подряд) — ✅ реализовано

Сейчас `finishRubberBand()` → `setMultiSelection(ids)`
(`app/static/index.html`) ПОЛНОСТЬЮ заменяет `state.multiSelectedIds`
при каждом новом Shift+перетаскивании — второй раз выделить рамкой
где-то ещё сбрасывает первую группу. Нужно: повторное
Shift+перетаскивание ДОБАВЛЯЕТ новую группу к уже выделенной (объединение
множеств), а не заменяет её. Кнопка "✕" на плавающей панели
(`#multi-select-clear`) остаётся единственным способом сбросить всё и
начать заново.

**Решено (уточняющие вопросы 2026-07-16)**: простое объединение множеств
(не toggle) — рамка никогда не снимает уже выделенное.

**Реализовано**: `finishRubberBand()` теперь стартует с
`new Set(state.multiSelectedIds)` вместо пустого набора, дальше как
раньше добавляет захваченные элементы — итог передаётся в
`setMultiSelection`, которая уже корректно (де)подсвечивает только
изменившиеся элементы.

**Общая проверка раунда (п.1, 2, 4, 5, 6)**: JS-синтаксис (`node
--check`), `python3 -m py_compile` бэкенда, сервер перезапущен и отвечает
`200` на `/health`/`/static/index.html`, живой `curl` с реальной сессией
— `GET /contracts` (уже существующий реальный контракт заказчика в БД,
не тестовые данные) отдаёт ожидаемую форму с `remaining`, `/plan-data`
не сломан. Живой браузерной проверки (сама модалка, datalist,
накопительное выделение) по-прежнему не было.

**Файлы**: изменены `app/static/index.html`, `app/main.py`,
`app/pdf_export.py`.

## Установка/обновление на Windows 11 — документация и скрипты (2026-07-16)

Заказчик попросил расписать, что нужно для установки на компьютер с
Windows 11 и как дальше обновлять, не затирая внесённые данные. Не
функциональное изменение приложения — операционная документация +
несколько вспомогательных `.bat`/`.ps1`-скриптов.

**Ключевой факт, определивший подход**: у проекта пока нет git-репозитория
(`git log` — "no commits yet", `git remote -v` — пусто), поэтому
документ описывает ДВА пути обновления: (А) через git, если позже
заведут приватный репозиторий (рекомендуется, безопаснее), и (Б) через
копирование файлов вручную (то, что реально доступно прямо сейчас) — с
явным списком "что копировать / что НИКОГДА не копировать", т.к. самый
опасный сценарий — случайно распаковать новый архив поверх рабочей
папки и затереть `data\zhbi.db`/`Input\`/`uploads\`.

**Добавлено**:
- `Docs/DEPLOYMENT_WINDOWS.md` — установка (Python, venv, зависимости,
  первый запуск, смена пароля admin, доступ по LAN, брандмауэр,
  автозапуск), резервное копирование, оба варианта обновления,
  диагностика типичных проблем.
- `scripts/windows/install.bat` — создаёт `.venv`, ставит
  `requirements.txt`.
- `scripts/windows/run.bat` — запуск `uvicorn --host 0.0.0.0` (LAN-доступ
  из коробки).
- `scripts/windows/backup.bat` + `backup.ps1` — копия `data\zhbi.db` с
  меткой даты/времени в `data\backups\` (дата — через PowerShell
  `Get-Date`, не через batch `%date%`/`%time%`, т.к. их формат зависит от
  региональных настроек Windows и ненадёжен для скриптов).
- `scripts/windows/update-git.bat` — `git pull` + переустановка
  зависимостей, с резервной копией перед обновлением.
- `.gitignore` — добавлены `Input/` (исходные DXF конкретного
  развёртывания) и `output/` (черновой вывод CLI-скриптов) — были
  открытием при подготовке этого раунда: не мешали работе сейчас (в БД
  до сих пор не было ни одного коммита), но были бы западнёй при
  первом же `git init`/`git add .` без этих исключений.
- Все `.bat`-файлы — `chcp 65001` сразу после `@echo off`, иначе
  кириллица в выводе рискует превратиться в "кракозябры" в стандартной
  кодовой странице `cmd.exe`.

**Проверено**: `python3 -m py_compile`/структура файлов — да, но
**реального запуска на Windows в этой сессии не было** (среда
разработки — macOS, нет `pwsh`/`cmd.exe` для проверки синтаксиса
`.bat`/`.ps1` вживую) — весь контент выверен построчно вручную по
описанной логике `cmd.exe`/PowerShell, но не исполнен. Документ явно
предупреждает об этом отдельным разделом ("Не проверялось вживую") —
перед боевым использованием нужен один сквозной прогон на тестовом
компьютере с Windows 11.

**Файлы**: новые `Docs/DEPLOYMENT_WINDOWS.md`, `scripts/windows/
install.bat`, `run.bat`, `backup.bat`, `backup.ps1`, `update-git.bat`;
изменены `.gitignore`, `CLAUDE.md`.

## Git-репозиторий запущен + модель "main + теги релизов" (2026-07-16)

Заказчик подтвердил, что готов организовать разработку через git
(есть аккаунт GitHub, VS Code) — попросил расписать, как обновлять через
git и как правильно организовать работу между тестовой и продуктовой
установками.

**Сделано на компьютере разработчика**:
- `git config user.name/user.email` (локально для этого репозитория,
  не глобально) — `Max Medvedev` / `max.v.medvedev@gmail.com`, взято из
  контекста сессии; поправить одной командой (`git config user.name
  "..."`), если нужно другое имя для авторства коммитов.
- Первый коммит на ветке `main` — весь код (`app/`, `scripts/`,
  `Docs/`, `requirements.txt`, `CLAUDE.md`, `.gitignore`) — 44 файла.
  **Осознанно НЕ включено**: `data/`, `Input/`, `uploads/` (уже были в
  `.gitignore`), `test_data/` (147 МБ реальных чертежей — решение
  отложено, см. ниже), `.venv/`, `output/`.
- Ни на какой GitHub-remote код НЕ отправлен — это требует входа
  пользователя в его аккаунт, сделать за него нельзя. Полная инструкция
  (проще всего — кнопка "Publish to GitHub" в VS Code, раз он уже
  установлен) — `Docs/DEPLOYMENT_WINDOWS.md` §3.1.

**Модель обновления** (задокументирована, скрипты готовы):
`main` — рабочая ветка разработки; теги релизов (`vГГГГ.ММ.ДД`) —
конкретные точки, отправляемые на продакшен. Тестовая установка
всегда подтягивает `main` целиком; продуктовая — только выбранный
вручную тег, никогда не обновляется "сама по себе".
- `scripts/windows/update-git.bat` → переименован в
  `update-git-test.bat` (просто `git pull`, для тестовой установки).
- Новый `scripts/windows/update-git-prod.bat` — `git fetch --tags`,
  показывает список тегов, спрашивает, какой ставить, `git checkout
  <тег>` (репозиторий переходит в detached HEAD — ожидаемо для
  установки, закреплённой за релизом).
- `Docs/DEPLOYMENT_WINDOWS.md` — раздел 3 переписан целиком: разовая
  настройка репозитория (через VS Code), подключение тестовой/
  продуктовой установок (`git clone`, Git Credential Manager для входа
  на Windows), повседневный цикл разработки → тест → тег релиза →
  продакшен. Раздел 1.2 (первая установка) обновлён — `git clone`
  теперь основной путь, копирование ZIP — запасной (раздел 4).

**Открытый вопрос, требует решения пользователя** (не блокирует
остальное, второй коммит без него уже возможен): включать ли
`test_data/` в git — большие бинарные файлы, попав в историю
репозитория, не освобождают место даже после удаления файла без
отдельной перезаписи истории. Подробности и оба варианта —
`Docs/DEPLOYMENT_WINDOWS.md` §3.7.

**Проверено**: `python3 -m py_compile` бэкенда (без изменений в этом
раунде, но перепроверено на всякий случай), `git log`/`git status`
после коммита — ожидаемый состав файлов, ничего похожего на секреты
(`.env`, пароли, ключи) в staged-файлах не найдено при ручной проверке
перед коммитом. Само подключение к GitHub и клонирование на
Windows-машины — не проверено (требует шагов пользователя вне этой
сессии).

**Файлы**: новые `scripts/windows/update-git-prod.bat`; переименован
`update-git.bat` → `update-git-test.bat`; изменены
`Docs/DEPLOYMENT_WINDOWS.md`, `CLAUDE.md`.

## Новый файл — плиты перекрытия, новые ярусы (2026-07-17)

Заказчик положил в `Input/` новый чертёж (`260716_Чертежи для WEB.dxf`,
18 МБ, заменил `260715_...` в `Input/`, старый файл остался доступен —
данные обоих сосуществуют в БД по `source_file`) и заранее предупредил:
должны появиться плиты перекрытия, толщина 300 мм.

**Диагностика (до какого-либо кода)** — прогнал реальный парсер слоёв
на файле: 24 "непойманных старым конвейером" WEB_-слоя, из них **10 не
проходили строгую валидацию**, три независимые причины:
1. `Плита перекрытия` — целиком новый токен типа (в имени слоя одно
   словосочетание через пробел, `WEB_констр_Плита перекрытия_..._элемент`)
   — не входил в словарь типов (`Колонна`/`Ригель`/`Плита`/`Панель`).
   1997 элементов, только отметка +25.800.
2. Колонна — новые подтипы `верхняя` (ОТМ+34.700, 332 шт.) и `средняя
   верхний ярус` (ОТМ+25.800, 333 шт.) — здание выросло на 2 яруса.
3. Ригель — новые значения `на отм. +25.800` (633 шт.) и `на отм.
   +34.700` (634 шт.) — тот же паттерн именования, что уже был для
   `на отм. +15.800`, просто для новых этажей.

Зоны (захватки/краны/стоянки) — без изменений (4+3+27, как и раньше) —
"лесенка" сужения стоянки по высоте не требовала никаких правок, чтобы
подхватить 2 новых яруса — она уже была рассчитана как полностью
динамическая (`compute_column_tier_elevations` берёт РЕАЛЬНЫЙ список
отметок колонн текущей выборки, не захардкожен на 2 яруса).

**Уточняющие вопросы (2026-07-17) и решения**:
- «Плита перекрытия» — **новый, самостоятельный 5-й тип** словаря (не
  подтип «Плита»; «Плита» остаётся отдельным, всё ещё не встречавшимся
  в реальных данных типом).
- Фиксированная толщина 300 мм в 3D — на **весь тип «Плита перекрытия»
  целиком** (любой будущий подтип/отметка), не только на подтип
  `на отм. +25.800`. Тип «Плита» (генерик) под это исключение не
  подпадает — при появлении будет считаться по старому допущению
  «квадратное сечение».
- Верхний ярус колонн (`верхняя`, ОТМ+34.700) — подтверждено, что это
  весь верх здания в этом файле, новых ярусов выше не будет (пока).

**Реализовано**:
- `scripts/layer_naming.py`: `ZHBI_TYPES` — добавлен `"Плита перекрытия"`
  (парсинг токена типа не менялся — словосочетание с пробелом и так
  остаётся одним token'ом при разбиении имени слоя по `_`).
- `app/models.py`: `ZHBI_ELEMENT_TYPES` — то же добавление (используется
  отдельно от `layer_naming.ZHBI_TYPES`, см. комментарий в файле —
  валидация `POST /allowed-subtypes` и заполнение „Справочника подтипов“
  в UI).
- `app/static/index.html`: `elementExtrusionHeight()` — новая ветка
  `element.element_type === "Плита перекрытия"` → константа
  `FLOOR_SLAB_THICKNESS_MM = 300`, проверяется ДО общего допущения
  «квадратное сечение» (которое иначе тоже сработало бы, просто с другим
  числом).
- `allowed_subtypes` — добавлены 5 строк: Колонна/верхняя, Колонна/
  средняя верхний ярус, Ригель/на отм. +25.800, Ригель/на отм. +34.700,
  Плита перекрытия/на отм. +25.800 (через прямой SQL, эквивалент того,
  что сделал бы админ через «Настройки → Справочник подтипов» — эндпоинт
  тот же, просто без похода через живой UI).
- Видимость подписей (`label_visibility`) для нового типа — ничего
  вручную заполнять не пришлось: `ensure_label_visibility()`
  (`scripts/import_elements.py`) уже сама заводит запись `visible=1`
  для каждого фактически встреченного `element_type` при каждом импорте
  — сработала автоматически.

**Проверено**: изолированный прогон `parse_layer_name` на всех 24
проблемных слоях реального файла — 0 ошибок после правок (было 10).
Полная переобработка через живой сервер (`[startup] ... 5330 элементов,
5330 новых, 0 обновлено`) — счётчики по (тип, подтип, отметка) сошлись
день-в-день с тем, что показал прямой разбор DXF (1997 плит перекрытия,
332/333/332/332 колонны по 4 ярусам, 634/633/634/103 ригеля). Марки
плит перекрытия — 1997 из 1997 распознаны через выноску (`mark_source
='leader'`, 0 unresolved). "Лесенка" стоянок по высоте — 100% matched на
новых ярусах +25800 (2963 элемента) и +34700 (966), без единой правки в
её собственном коде — подтверждает, что она действительно была
рассчитана заранее на "файл дополнится новыми этажами". `python3 -m
py_compile`, `node --check`, живой `curl` (`/plan-data` по новому
файлу — 200). Живой браузерной проверки (сама 3D-модель, толщина плит
на глаз) не было.

**Файлы**: изменены `scripts/layer_naming.py`, `app/models.py`,
`app/static/index.html`, `Docs/TZ.md` (разделы 3.3, 3.7, 6.7); БД —
5 новых строк в `allowed_subtypes` (не файл, но часть изменения).

## Плиты перекрытия — марка из нескольких текстовых фрагментов (2026-07-17)

Заказчик сообщил: марки плит перекрытия не ассоциировались. Причина —
слой марок для этой группы (`WEB_констр_Плита перекрытия_на отм.
+25.800_ОТМП25800_марка`) не содержит вообще ни одной выноски
(LEADER/MULTILEADER) — только 5947 отдельных `TEXT` на 1997 плит.
Проверено: **всего 5 уникальных строк текста** во всём слое (`-`, `1`,
`4П`, `4Пд`, `4ПИ-1`) — марка "4П-1" нарисована как ТРИ отдельных TEXT
("4П","-","1"), а не одной надписью. Старый механизм резолвинга
(`resolve_via_leaders`/`build_leader_pool`) трактует каждый "голый" TEXT
как отдельного самостоятельного кандидата на марку — жадно отдавал
каждой плите ближайший ОДИНОЧНЫЙ фрагмент вместо всех, что ей
принадлежат (отсюда марки вида `'1'` на сотнях плит).

**Реализовано**: новая `scripts/parse_zhbi.resolve_via_polygon_texts()`
— марка элемента = все текстовые фрагменты, чья точка вставки лежит
ВНУТРИ его контура, отсортированные по направлению чтения (проекция на
поворот текста: `x*cos(rot) + y*sin(rot)` — общая формула, не
специальные случаи для 0°/90°) и склеенные без разделителя. Использует
`shapely.strtree.STRtree` (пространственный индекс) — наивный двойной
перебор (1997 плит × 5947 фрагментов = ~11.9М проверок) занимал десятки
секунд, с STRtree — единицы секунд.

**Побочно найдено и исправлено (в процессе, до финальной версии)**:
1. Первая версия (без индекса) была недопустимо медленной — переписана
   на STRtree.
2. Первая версия С индексом, но БЕЗ эксклюзивности — не давала гарантии,
   что один фрагмент достаётся только одному элементу: пограничные
   фрагменты (точка вставки ровно на общей стороне двух соседних плит —
   **подтверждено**: `polygon.distance(point) == 0.0` до ОБЕИХ соседних
   плит одновременно, не отличить чисто геометрически) доставались
   ОБЕИМ, давая мусорную склейку вроде `"4Пд4П--11"` (120 плит из 1997).
   Исправлено: финальное присвоение — глобальный жадный алгоритм по
   возрастанию расстояния до ЦЕНТРОИДА элемента, каждый фрагмент занят
   только одним элементом (тот же принцип, что уже был в
   `resolve_via_leaders`, просто применён и здесь).
3. **Настоящий регресс, пойман до релиза**: изначально
   `resolve_via_polygon_texts` вызывалась ВСЕГДА, для любого слоя марок
   нового стандарта. На реальном файле это ЛОМАЛО Ригель (было 2004/2004
   через выноску, стало 1746 выноска + 130 мусорных склеек по контуру +
   128 unresolved) — часть "голых" TEXT, обслуживающих старые LEADER
   (`find_nearest_text_entry` в `build_leader_pool` ищет текст для
   LEADER среди ровно этого же списка), уходила в группировку по контуру
   раньше, чем до неё доходила очередь резолвинга через выноски, и
   переставала быть доступна. Исправлено: группировка по контуру
   применяется **только если на слое марок нет вообще ни одной
   выноски** — иначе используется прежний, немодифицированный путь через
   `resolve_via_leaders`. Найдено сравнением с реальным поведением ДО
   правки (`git stash` двух изменённых файлов, повторный прогон на том
   же файле, сравнение чисел) — без этого шага регресс остался бы
   незамеченным, т.к. тестировался в основном именно случай плит.

**Проверено**: изолированные прогоны реального парсера на файле
`260716_Чертежи для WEB.dxf` (не на срезах/выдумках) на каждом шаге —
финальный результат: Плита перекрытия — 1996/1997 через `grouped_text` +
1/1997 через `leader` (резерв), **всего 3 уникальные марки**
(`4П-1`: 1726, `4Пд-1`: 249, `4ПИ-1`: 22, сумма 1997) — без единой
мусорной склейки. Ригель — 2004/2004 через `leader`, Колонна — 1327
через `leader` + 2 unresolved — **оба идентичны показателям ДО всех
правок этого раунда** (тот самый `git stash`-прогон), подтверждена
нулевая регрессия. Полный прогон через живой сервер
(`app.dxf_import.process_upload`, не только изолированный парсер) —
числа в БД совпали с изолированным тестом день-в-день. `python3 -m
py_compile`, `scripts/verify_zone_pipeline.py` — без изменений, все
проверки пройдены. Один "зависший" запуск через uvicorn (2+ минуты без
ответа на `/health`) оказался разовой помехой окружения — прямой вызов
`process_upload()` в изоляции отработал за 19 секунд, повторный чистый
перезапуск uvicorn ответил мгновенно; не связано с кодом этого раунда.

**Файлы**: изменены `scripts/parse_zhbi.py` (новая
`resolve_via_polygon_texts`, `_to_polygon_or_none`, поле `rotation` у
собираемых TEXT-аннотаций), `scripts/new_standard_pipeline.py` (условный
выбор пути резолвинга марки), `Docs/TZ.md` (раздел 3.4).

## Раунд из 3 пунктов (2026-07-17)

Запрошено заказчиком одним сообщением: 1) группировка по маркам как
3-й уровень фильтра Тип/Подтип, 2) подписи марок пропали на схеме,
3) вывод марок в 3D-режиме.

### 1. Подписи марок пропали на плотных многоярусных файлах — ✅ исправлено

**Причина**: базовый радиус маркера (и производный от него размер
шрифта подписи, см. 6.1 в `Docs/TZ.md`) сервер оценивает как медианное
расстояние до ближайшего соседа среди ВСЕХ элементов выборки
(`estimate_marker_radius`, `app/main.py`) — раньше без исключений.
Многоярусные колонны/ригели (одна и та же координата X,Y на разных
отметках — тот же след на 2D-плане) дают "соседей" на расстоянии,
близком к нулю (миллиметровые расхождения округления между полилиниями
разных ярусов одного контура, не ровный ноль). На файле
`260716_Чертежи для WEB.dxf` (5330 элементов, до 4 ярусов) доля таких
"нулевых" соседей выросла до ~46% — медиана рухнула до 0.0013 мм,
маркер/подпись стали фактически невидимы на любом масштабе (реальный
эффект уже проявлялся частично и на предыдущем файле `260715...` — 49%
"нулевых" соседей, медиана 14 мм, просто не было замечено/сообщено).

**Исправлено**: сосед на расстоянии ≤ 50 мм (`SAME_FOOTPRINT_TOLERANCE_MM`)
больше не считается настоящим соседом при оценке медианы — точки одного
следа сначала схлопываются в одного представителя (решётка с ячейкой
50 мм), медиана считается по представителям. Заодно переписано с
наивного перебора пар (O(n²), ~4.2 с на 5330 элементах) на
`shapely.strtree.STRtree` (O(n log n), ~0.03 с) — тот же приём
пространственного индекса, что уже применялся для группировки марок
плит перекрытия в предыдущем раунде.

**Проверено**: прямой вызов `estimate_marker_radius`/`plan_data` на
реальных данных `data/zhbi.db` для всех загруженных файлов — новый файл
(`260716...`, 5330 эл.) даёт разумный радиус 278 мм вместо 0.00033 мм;
старые разреженные файлы (`260713...`, `260714...`) дают ТОЧНО те же
значения, что и до правки (1387.5 мм и 941.8 мм) — нулевая регрессия.
Время расчёта упало с ~4.2 с до ~0.03–0.14 с на запрос.

**Файлы**: `app/main.py` (`estimate_marker_radius` переписана,
константа `SAME_FOOTPRINT_TOLERANCE_MM`), `Docs/TZ.md` (раздел 6.1).

### 2. Группировка по маркам как 3-й уровень фильтра (Тип → Подтип → Марка) — ✅ реализовано

**Уточнено с заказчиком**: марка внутри подтипа — обычный список
чекбоксов, БЕЗ строки поиска (как у подтипов сейчас), несмотря на то,
что у некоторых подтипов (например, "Колонна"/подтип не определён) до
340+ уникальных марок.

**Реализовано**: `buildHierarchicalFilterGroup` (`app/static/index.html`)
генерализована — новый опциональный последний параметр `grandchildConfig`
добавляет 3-й уровень (та же UI-механика: сворачиваемый список, кнопка
разворота, тот же класс `.filter-children`). Два других вызова (Кран/
Стоянка крана, Поставщик/Контракт) НЕ передают этот параметр и работают
без единого изменения поведения — регресс-проверка ниже. Каскады
расширены симметрично: клик по марке при выключенных предках включает
ОБА уровня вверх (подтип и тип) — иначе элемент всё равно не проходит
фильтр (`passesPlacementFilters` — AND по всем категориям); клик по
подтипу/типу каскадом трогает и марки. `PLACEMENT_CASCADE_GROUPS` первая
пара расширена до тройки `["elementType", "subtype", "mark"]` — вся
остальная логика доступности (`cascadeGroupFor`/`isValueEnabled`) уже
была длина-независимой, менять не пришлось. Новая `markFilterValue`,
запись `mark` в `PLACEMENT_FILTER_DEFS`/`state.placementFilters`.

**Проверено**: изолированный Node-тест (минимальный DOM-стаб +
извлечённые реальные функции из файла) на реальных
`(element_type, subtype, mark)` тройках из `260716_Чертежи для WEB.dxf`
(5330 элементов) — 13 проверок каскада (выключение типа тянет за собой
все подтипы и все марки; включение одной марки при выключенных предках
поднимает оба уровня вверх; «показать все» чистит все 3 уровня разом).
Отдельный регресс-тест — 2-уровневый вызов БЕЗ `grandchildConfig` ведёт
себя ИДЕНТИЧНО (9 проверок): нет ни одного grandchild-элемента в DOM,
каскад родитель↔потомок работает как раньше. Все 22 проверки прошли.

**Файлы**: `app/static/index.html` (`buildHierarchicalFilterGroup`,
`PLACEMENT_FILTER_DEFS`, `PLACEMENT_CASCADE_GROUPS`,
`state.placementFilters`/`placementGroupsExpanded`, вызов для группы
"Тип элемента / Подтип / Марка", CSS `.filter-grandchild`),
`Docs/TZ.md` (раздел 6.5).

### 3. Вывод марок элементов в 3D-режиме — ✅ реализовано

**Уточнено с заказчиком**: постоянные подписи (не только тултип), тем
же переключателем видимости по типу, что и в 2D; рисовать сразу для
ВСЕХ элементов (не только видимых в кадре камеры) — простой вариант для
первой итерации.

**Реализовано**: новая `build3DLabelSprite(element, topY)` —
`THREE.Sprite` с canvas-текстурой (билборд к камере автоматический,
ничего не обновляется per-frame в `animate3D()`). Без нового
вендоринга: используется только уже провендоренное ядро Three.js
(`Sprite`/`SpriteMaterial`/`CanvasTexture`) — `CSS2DRenderer`/
`TextGeometry` НЕ провендорены, а по правилу проекта любой новый
сторонний код требует отдельного подтверждения заказчика (см.
`CLAUDE.md`), которого для этого раунда не запрашивалось. Масштаб —
через `state.baseMarkerRadius` (тот же параметр, что у 2D-маркеров/
подписей, см. п.1 выше) — единая логика между режимами, ничего заново
не подбиралось. Видимость — переиспользует `state.labelVisibility`
(тот же тумблер "Подписи", что в 2D, без отдельного 3D-состояния, тем
же приёмом, что уже был у `apply3DZoneVisibility`/`zoneVisibility`) и
текущий фильтр по размещению. Новая `state.view3d.labelSpriteById`,
диспоуз старых спрайтов при пересборке сцены (`build3DScene`) —
симметрично уже существующей очистке мешей/зон. Без логики скрытия
пересечений (заказчик выбрал простой вариант) — риск наложения ниже,
чем в 2D, поскольку элементы одного следа на разных ярусах в 3D
физически разнесены по высоте.

**Проверено**: изолированный Node-тест (`build3DLabelSprite` со
стаб-версией `THREE`/`document.createElement("canvas")`) — 12 проверок:
элемент без марки → `null`, позиция/масштаб корректны для реального
элемента (world.X=dxf.x, world.Z=-dxf.y, та же конвенция, что у мешей),
масштаб пропорционален длине марки, `userData.elementId` проставлен.
Живой сервер — статика отдаётся, синтаксис (`node --check`) корректен,
`/health` 200 после всех правок. **Живой визуальной/перформанс-проверки
в браузере (плавность вращения на файле 5330 элементов, реальный вид
подписей) не было — нет инструмента автоматизации браузера в этой
среде.** Это открытый пробел, как и у всех UI-правок этой сессии —
явно сообщается пользователю.

**Файлы**: `app/static/index.html` (`build3DLabelSprite`, `build3DScene`,
`apply3DLabelVisibility`, `applyPlacementFilters`, `renderLabelToggles`,
`state.view3d.labelSpriteById`), `Docs/TZ.md` (раздел 6.7).

## Исправления по безопасности — ✅ реализовано (2026-07-17)

Все 3 пункта из раздела "Исправления по безопасности" (2026-07-15,
ранее "обсуждено, не реализовано") реализованы по явной просьбе
заказчика в рамках этого раунда.

**1. XSS через `innerHTML` без экранирования** — новый хелпер
`escapeHtml()` (`app/static/index.html`), обёрнут вокруг каждого места
интерполяции untrusted-данных (из БД/DXF/пользовательского ввода) в
шаблонных литералах `innerHTML` — марка, названия слоёв/файлов DXF,
контракты (название/поставщик/тип элемента), ФИО/должность/логин
пользователя, история статусов (кто менял), админ-конфигурируемые
подписи статусов, подтипы. Хардкодные (не интерполируемые) innerHTML-
вызовы и код, уже написанный через `createElement`/`textContent`
(инциденты контракта, крановые подписи — паттерн из предыдущего
раунда), не тронуты — уже безопасны. Проверено вручную (`escapeHtml`
на пейлоаде `<script>alert(1)</script>"onmouseover="alert(2)"` —
корректно экранирует и теги, и разрыв атрибута) и `node --check` всего
файла.

**2. Нет rate-limit на `POST /login`** — блокировка по IP-адресу
клиента (не по логину — логины публично видны через
`GET /login-users`, блокировка по логину позволила бы намеренно
запереть чужого пользователя), 5 попыток за 5 минут, 429 при
превышении, счётчик сбрасывается при успешном входе. In-memory (один
процесс `uvicorn`). Проверено: изолированный тест (`_check_login_rate_limit`/
`_record_login_failure`) и живой сервер (`curl` — 6 неверных попыток
подряд с одного IP → 6-я получает 429, другой IP не заблокирован,
успешный вход после сброса счётчика проходит).

**3. Cookie сессии без флага `secure`** — добавлен через переменную
окружения `ZHBI_SECURE_COOKIES` (по умолчанию выключено — текущий
деплой на обычном HTTP внутри локальной сети, `Docs/DEPLOYMENT_WINDOWS.md`
нигде не упоминает TLS/reverse-proxy, включить безусловно сломало бы
вход). Проверено: без переменной — `Set-Cookie` без `Secure` (живой
`curl -i` на `/login`), с `ZHBI_SECURE_COOKIES=1` — флаг выставлен.
Добавлена заметка в `Docs/DEPLOYMENT_WINDOWS.md` §1.5 — как включить,
если/когда появится HTTPS.

**Файлы**: `app/auth.py` (rate-limit, `SECURE_COOKIES`),
`app/static/index.html` (`escapeHtml` + места применения),
`Docs/DEPLOYMENT_WINDOWS.md` (§1.5), `Docs/TZ.md` (раздел 7).

## Партия — учёт по маркам внутри контракта (2026-07-17)

Заказчик попросил расширить учёт контрактов: детализация до **марки**
(не только тип элемента, как раньше) и планирование конкретных поставок
во времени — новая сущность **Партия** (заказчик явно отклонил название
«Отгрузка»). Согласовано на уточняющих вопросах: овербукинг — мягкое
предупреждение, не блокирует; назначить элемент на партию можно только
по точному совпадению строки (тип/подтип/марка) — иначе недоступно;
назначение партии — независимое от смены статуса действие, но из
единого контекстного меню с ней; идентификация партии — без ручного
названия, метка по маске «Контрагент. Номер и дата контракта. Дата»
(потребовало новое поле «дата контракта»). Плюс, при показе плана,
заказчик добавил: поле «Код» у контрагента, допстрока подписи на схеме
по статусу+партии, партия кликабельна в карточке; и отдельно, по
скриншоту из `test_data/` — границы элементов в 3D были не видны на
однотипных примыкающих элементах, подписи марок сливались по цвету с
элементом.

### Реализовано

**Схема**: новые таблицы `batches` (id, contract_id → CASCADE,
planned_date) и `batch_lines` (element_type, subtype, mark, quantity;
NULL-safe уникальность через `COALESCE`-индекс — обычный `UNIQUE` в
SQLite не считает `NULL=NULL`, без этого две "одинаковые" строки без
подтипа/марки продублировались бы беспрепятственно). Новые колонки:
`contracts.contract_date`, `contracts.code`, `elements.batch_id` (→
SET NULL, простое живое поле, БЕЗ версионирования по `status_history` —
осознанный компромисс, назначение партии не привязано к статусу),
`users.label_color`.

**Backend**: новый модуль `app/batches.py` (свой роутер, как
`app/contracts.py`) — `compose_batch_label` (формула метки, сервер —
единственный источник форматирования), `find_matching_batch_line`
(NULL-safe, жёсткий отказ при несовпадении), `batch_line_warning`
(мягкое предупреждение, без фильтра по статусу — само назначение уже
есть факт), `validate_and_resolve_batch_assignment` (общая точка для
одиночного/массового назначения), `clear_stale_batch_assignment` (хук в
`apply_status_change` — партия чужого контракта не остаётся молча
привязанной при смене контракта), `enrich_element_row` (денормализует
`contract_code`/`batch_planned_date` на КАЖДЫЙ ответ с элементом —
нужно для допстроки подписи без отдельного запроса на каждый элемент).
Новые эндпоинты: `GET/POST/PATCH/DELETE /batches[/{id}]` (CRUD,
полная замена строк на PATCH — тот же приём, что у `contract_lines`),
`PATCH /elements/{id}/batch` и `/elements/bulk-batch` (bulk — атомарно,
сначала валидирует все элементы пачки, при ошибке — ничего не
применяется). `PATCH /users/{id}/label-color` — тот же
self-service-guard, что у `set-password` (менять можно только себе, не
admin — 403).

**Найденный и исправленный по пути баг**: `PATCH /batches/{id}` с двумя
строками (тип, без подтипа, без марки) в одном запросе давал сырой 500
(`sqlite3.IntegrityError` из-за UNIQUE-индекса) вместо понятного ответа
— обёрнуто в try/except с rollback, теперь чистый 400.

**Frontend**: контракт-модалка — поля дата/код; новая модалка «Партии
контракта» (список + добавление/изменение/удаление) и форма партии
(каскадный выбор тип→подтип→марка тремя `<select>`, НЕ текст+datalist,
как у строк контракта — нужно точное совпадение для контроля, опечатка
в свободном тексте сломала бы его; построено сканированием
`state.elements`, тем же приёмом, что уже использует
`renderPlacementFilters` для фильтра); единое контекстное меню элемента
— пункт «Выбрать партию…» рядом со статусами; массовое назначение —
новая кнопка и модалка, по образцу массовой смены статуса; допстрока
подписи на схеме (2D — отдельный `<text>` со своей мировой Y, НЕ
`tspan`+`dy` внутри существующего — риск ошибки знака под уже
применённым контрфлипом текста без визуальной проверки; 3D — вторая
строка на том же canvas-спрайте); карточка элемента — партия строкой
внутри блока «Контракт», кликабельна для admin, открывает форму
редактирования.

**Контраст (по скриншоту)**: 3D — рёбра силуэта каждого меша
(`THREE.EdgesGeometry`), цвет — затемнённая версия ТОГО ЖЕ статусного
цвета (`darkerColor`, ×0.55 на канал, не фиксированный чёрный — иначе
терялась бы читаемость на уже тёмных статусах), пересчитываются при
смене статуса/выбора; 3D-подписи — добавлена полупрозрачная белая
подложка на canvas (буквально то, что запрошено). 2D — вместо
подложки (см. риск позиционирования `<rect>` выше) — белый контур
вокруг букв (`stroke`+`paint-order`), тот же эффект контрастности без
риска. Цвет текста подписи — новая персональная настройка
(«Настройки → Цвет подписей марок», доступна всем ролям), единый дефолт
`#222222` вместо двух ранее несогласованных значений (`#333` в 2D,
`#222` в 3D).

### Проверено

Изолированные тесты: каскадный выбор тип/подтип/марка на реальных
данных (5330 элементов, мини-DOM-стаб) — 12/12; `elementSubLabelText` —
9/9 сценариев (все статусы × наличие/отсутствие партии/даты/кода,
включая корректное отсутствие допстроки для Доставлен/Смонтирован/
Принят); `darkerColor` на всех 7 реальных цветах статусов — везде
заметно темнее и не уходит в чистый чёрный.

Живой сервер, полный цикл: контракт с датой/кодом → партия с меткой,
собранной по формуле → назначение элемента (контракт → партия) →
`contract_code`/`batch_planned_date` корректно в ответе PATCH и в
`/plan-data` → мягкое предупреждение при превышении строки партии, БЕЗ
блокировки → жёсткий отказ (400) при несовпадении марки →
`clear_stale_batch_assignment` при смене контракта → `DELETE
/batches/{id}` снимает привязку у элементов → `label-color` 200 себе /
403 чужому. Все тестовые контракты/партии/история удалены после
проверки штатными операциями.

**Файлы**: `app/schema.sql`, `app/db.py`, `app/batches.py` (новый),
`app/contracts.py`, `app/models.py`, `app/main.py`, `app/auth.py`,
`app/users.py`, `app/static/index.html`, `Docs/TZ.md` (разделы 5.1,
6.1, 6.7, 7, 8).

**Не проверено**: живой браузер (визуальное позиционирование
допстроки/каскадных `<select>`, реальный вид рёбер/подложки/контура) —
нет инструмента автоматизации браузера, как и в предыдущих раундах.

## Партия — правки по итогам первой живой проверки (2026-07-17)

Заказчик проверил живьём и нашёл 4 проблемы/доработки.

### 1. Найдена настоящая причина "статус не меняется" из прошлого раунда

При смене статуса на «Контрактация» запись в истории появлялась, но
текущий статус/цвет элемента не менялись, и партию стало нельзя
назначить. Разобрано по БД: `changed_at` новой записи оказался РАНЬШЕ
уже существующей записи «Запланирован» (от импорта чертежа) —
`current_status`/`elements.contract_id` вычисляются как значения самой
ПОЗДНЕЙ по времени записи, так что новая запись корректно "проигрывала"
по хронологии. Причина — поле «Рабочая дата» в тулбаре оставалось
активным (застряло в `localStorage`) со старым значением с прошлой
проверки; кнопка «Сейчас» ЗАПОЛНЯЕТ текущим временем, но не ОЧИЩАЕТ —
поле остаётся "залипшим" на будущее. Не баг логики "последняя запись
побеждает" (осознанное поведение для бэкдейтинга), а UX-ловушка:
пользователь не видел, что рабочая дата ещё активна.

### 2. «Выбрать партию…» должен быть всегда в меню, не только один раз

Диалог смены контракта появляется только на самом первом уходе со
статуса «Запланирован» — поэтому предложение указать партию в НЁМ
физически может сработать только один раз за жизнь элемента. Отдельное
действие «Выбрать партию…» в контекстном меню было условным (только
при `element.contract_id`) — по той же схеме, что и проблема 1
(контракт не применился из-за бэкдейтинга), реально не появлялось.

**Исправлено**: пункт «Партия» теперь ВСЕГДА в контекстном меню (тот же
принцип, что у фильтров — "полный список всегда виден + недоступен
(disabled), не сужается"), недоступен только если у элемента правда нет
контракта, с подсказкой почему. Текст меняется на «Изменить партию…»,
если партия уже назначена — явно даёт понять, что действие работает и
для повторного изменения, не только для первичной установки.

### 3–4. Диалог смены статуса — явные дата/время + кнопка очистки рабочей даты

Заказчик попросил: при установке ЛЮБОГО статуса явно показывать
дату/время, на которые он устанавливается (по умолчанию — рабочая дата
из тулбара, если активна, иначе текущий момент), с возможностью
поменять прямо там же; плюс отдельную кнопку в тулбаре, которая ОЧИЩАЕТ
рабочую дату (а не заполняет текущим временем, как уже существующая
«Сейчас») — тогда сервер всегда использует свой текущий момент на
запись.

**Реализовано**:
- Диалог смены статуса (`#status-contract-backdrop`, был "диалог выбора
  контракта", теперь общий "диалог смены статуса") открывается ВСЕГДА
  на любой клик по статусу в контекстном меню, не только при уходе с
  «Запланирован». Новое поле «Дата и время» (`datetime-local`) + кнопка
  очистки ЭТОГО конкретного значения (не трогает тулбар) — своё,
  одноразовое значение для текущего действия. Секция контракта/партии
  внутри диалога показывается по-прежнему только при уходе с
  «Запланирован» (остальным переходам контракт не нужен — наследуется).
- В тулбаре — новая кнопка «✕» рядом с «Сейчас»: `workdateInput.value = ""`
  + `localStorage.removeItem(...)`. `currentChangedAt()`/дефолт диалога
  читают именно это поле — пустое значение везде трактуется как "сервер
  сам подставит текущий момент" (`datetime('now')`).

### 5. 3D-границы элементов — EdgesGeometry не сработал, заменено на меш

Прошлая попытка (`THREE.EdgesGeometry` + `THREE.LineSegments`) на
реальном скриншоте оказалась практически невидима. Причина:
`THREE.LineBasicMaterial.linewidth` не работает в большинстве браузеров
— WebGL1/ANGLE клэмпит толщину GL-линии к 1 пикселю независимо от
заданного значения (широко известное ограничение, не баг конкретной
реализации) — тонкая 1px линия на масштабе всего чертежа (десятки-сотни
метров) визуально пропадает.

**Исправлено без нового вендоринга** (честный "толстый line" в
Three.js требует `Line2`/`LineMaterial` из `examples/jsm/lines/` —
новый сторонний код, нужно отдельное подтверждение по правилу проекта):
вместо GL-линии — ОТДЕЛЬНЫЙ МЕШ той же формы, что и элемент, но НЕМНОГО
ШИРЕ (`scaledOutline`, равномерное масштабирование контура от
центроида, ×1.08 — не настоящий полигон-offset, который сложнее для
произвольных контуров, а для типичных прямоугольных/выпуклых сечений
ЖБИ визуально даёт тот же эффект) и НИЖЕ (высота меньше на 25мм) —
дочерний объект основного меша. В центре, где оба перекрываются, более
высокий основной меш закрывает "ободок" сверху; виден только по краям,
куда основной меш не доходит — та самая тёмная кайма на стыке соседних
элементов при взгляде сверху. Работает при любом браузере/масштабе,
т.к. это обычная геометрия, а не GL-примитив линии.

**Проверено**: изолированный тест `scaledOutline` на реальном контуре
плиты перекрытия (1200×5300мм) — ширина/высота растут ровно на 8%,
центроид не сдвигается, масштаб 1.0 не меняет контур (5/5). Диспоуз
ободка добавлен в `build3DScene()` (дочерний меш не освобождается
автоматически при диспоузе родителя).

**Файлы**: `app/static/index.html` (контекстное меню, диалог смены
статуса, тулбар рабочей даты, `build3DOutlineMesh`/`scaledOutline`
вместо `EdgesGeometry`).

**Не проверено**: живой браузер (реальный вид "ободка" на скриншоте —
подбор `OUTLINE_MARGIN_SCALE`/`OUTLINE_MARGIN_DEPTH_MM` может
понадобиться после визуальной проверки, диапазон ширины ободка сейчас
зависит от размера элемента — для очень крупных плит 8% может
визуально казаться толще, чем для мелких).

## Партия — 3D-граница: 2 новых проблемы после первой правки (2026-07-19)

Заказчик проверил живьём: (1) между плитами перекрытия граница так и не
появилась, (2) элементы (плиты, ригели) мерцают при взгляде СНИЗУ.

**Причина обеих — геометрия прошлой версии не годилась для элементов,
примыкающих ВПРИТЫК (без реального зазора)**:

1. **Граница не видна у впритык примыкающих элементов** — прошлая
   версия расширяла именно "ободок" (шире истинного контура на 8%). У
   элементов с РЕАЛЬНЫМ зазором расширение никому не мешало — граница
   была видна. У плит перекрытия одного яруса, лежащих ВПРИТЫК (без
   зазора), расширение ободка одного элемента заходило на территорию
   соседа — и там перекрывалось СОБСТВЕННЫМ основным мешом соседа (тот
   выше = ближе к камере сверху). Граница пропадала именно там, где она
   больше всего нужна.
2. **Мерцание снизу** — цветной (статусный) меш и "ободок" границы
   имели ОДИНАКОВУЮ нижнюю грань (оба начинались ровно на
   `elevation_mm`) — при взгляде СНИЗУ это две геометрически совпадающие
   поверхности на одной глубине, классический z-fighting (GPU
   непредсказуемо чередует, какая рисуется поверх, при малейшем сдвиге
   камеры — отсюда мерцание).

**Исправлено — поменяли местами, кто шире/у́же, и раздвинули оба конца
по высоте, не только верх**:
- **"Ободок" границы** теперь РОВНО истинного контура и истинной высоты
  (не шире) — у соседних впритык элементов ободки СОПРИКАСАЮТСЯ по
  стыку без нахлёста на территорию друг друга, граница видна везде, не
  только там, где есть реальный зазор.
- **Цветной (статусный) меш**, наоборот, теперь УЖЕ истинного контура
  на 6% (`OUTLINE_INNER_SCALE=0.94`) и по высоте выступает на 15мм ЗА
  истинный диапазон **с ОБЕИХ сторон** (`OUTLINE_VERTICAL_MARGIN_MM=15`)
  — полностью охватывает "ободок" и сверху, и снизу, поэтому цвет статуса
  остаётся видимым (побеждает по глубине) при взгляде с любой стороны, а
  z-fighting нет ни сверху, ни снизу (обе пары граней теперь заведомо
  разнесены на 15мм, а не совпадают).
- Raycasting (клик/наведение) по-прежнему бьёт по цветному мешу
  (`state.view3d.meshById`) — с чуть более узким контуром (6%), в
  теории клик впритык у самого края элемента может изредка промахнуться
  на несколько мм — принятый компромисс ради видимой границы.

**Проверено**: изолированный тест на реальном контуре плиты перекрытия
(1200×5300мм, высота 300мм) — 8/8: цветной меш начинается на 15мм ниже
и заканчивается на 15мм выше истинного диапазона, ободок — ровно на
истинном диапазоне, охват подтверждён с обеих сторон, контур ободка НЕ
расширен (равен истинному), контур цветного меша УЖЕ истинного на 6%.
Живой сервер — `/health` 200, статика отдаётся.

**Живой браузер по-прежнему не проверен** — в этой сессии инструмент
браузера (`claude-in-chrome`) технически доступен, но браузерные
инструменты не включены (нужно `/chrome` или перезапуск сессии) — не
смог зайти и посмотреть картинку лично, хотя обычно теперь это
возможно и стоит сделать в следующий раз, если пользователь включит.

**Файлы**: `app/static/index.html` (`build3DMeshForElement`,
`build3DOutlineMesh`, новые константы `OUTLINE_INNER_SCALE`/
`OUTLINE_VERTICAL_MARGIN_MM` вместо `OUTLINE_MARGIN_SCALE`/
`OUTLINE_MARGIN_DEPTH_MM`).

## Новый файл 260720 — плиты перекрытия, ещё 3 яруса (2026-07-22)

Пользователь положил `Input/260720_Чертежи для WEB.dxf` (29 МБ) и попросил
проверить загрузку. При старте сервера файл целиком отклонён:
`неизвестный подтип 'на отм. +15.000' для типа 'Плита перекрытия'
(допустимые: ['на отм. +25.800'])` — тот же класс проблемы, что и в
раунде "Новый файл — плиты перекрытия, новые ярусы" (2026-07-17), только
плиты перекрытия выросли ещё на 3 отметки: `+15.000`, `+34.700`,
`+47.000` (в дополнение к уже известной `+25.800`). Изолированный прогон
`parse_layer_name` по всем 31 слою файла показал, что это единственная
проблема — колонны (4 яруса) и ригели (4 варианта) уже целиком покрыты
существующим `allowed_subtypes`.

Плита на `+47.000` — всего 38 элементов (для сравнения: остальные ярусы
по ~2000) при отсутствии в файле каких-либо новых слоёв колонн выше
`+34.700` (который в прошлом раунде был явно зафиксирован как "верх
здания, новых ярусов выше не будет"). Похоже на локальную площадку/крышу
техпомещения, опирающуюся на колонны яруса `+34.700`, а не на полный
новый этаж — стоит иметь в виду, если позже всплывут нестыковки по этой
отметке; отдельного яруса колонн под неё заказчик не прислал.

**Реализовано**: 3 строки в `allowed_subtypes` (Плита перекрытия / на
отм. +15.000, +34.700, +47.000) — тем же способом, что и раньше
(эквивалент `POST /allowed-subtypes`, здесь — прямой SQL по
подтверждению пользователя). Перезапуск сервера переобработал
`Input/260720_...` начисто.

**Проверено**: `[startup] Input/260720_Чертежи для WEB.dxf: 9385
элементов (9385 новых, 0 обновлено)` — 0 ошибок. Разбивка по (тип,
подтип) в БД сошлась день-в-день с прямым подсчётом сущностей по слоям
DXF: колонны 332/332/333/332, плиты перекрытия 1996/1997/2021/38, ригели
634/633/634/103. Марка не распозналась только у 2 элементов из 9385.
Живой браузерной проверки (визуально в UI/3D) не делал — только
серверный лог + прямые SQL-запросы к БД.

**Файлы**: БД — 3 новые строки в `allowed_subtypes` (не файл, но часть
изменения); код не менялся.

## Первая живая браузерная сессия — 5 багов из реального использования (2026-07-22)

Первая в истории проекта живая проверка в браузере (см. предупреждение в
CLAUDE.md — раньше не было доступа к инструменту). Пользователь дал 6
пунктов после живой работы с файлом 260720; почти все оказались
реальными, ранее никогда не пойманными изолированными тестами багами.

**1. Верхний ярус колонн (+34700) не доставал до нового яруса +47000**
— `elementExtrusionHeight` считал высоту верхнего яруса запасным
вариантом (высота предыдущего пролёта), не зная про появившуюся плиту
перекрытия ВЫШЕ верхнего яруса колонн. Добавлена `computeTopColumnCeiling`
— ищет ближайшую `Плита перекрытия` выше верхнего яруса и тянет колонну
до неё; `computeBuildingHeightRange` (объём захватки) переведена на ту
же функцию. Проверено: `elementExtrusionHeight(колонна@34700) === 12300`
(34700+12300=47000), живой 3D-скриншот — колонны стыкуются с балками
верхнего яруса без зазора.

**2. Подписи марок не читались ни в 2D, ни в 3D — на самом деле ДВА
независимых бага**, оба впервые пойманы только благодаря живому
браузеру:
   - **2a. Белый контур текста тонул шрифт при зуме.** `.mark-label`
     имел `stroke-width: 3px` БЕЗ `vector-effect: non-scaling-stroke` —
     значение в мировых мм, масштабируется вместе с viewBox. На дальнем
     зуме незаметно, но на БЛИЖНЕМ (единственном, где вообще можно
     прочитать марку — шрифт зажат `MAX_LABEL_FONT_PX=24` и СЖИМАЕТСЯ в
     мировых единицах при приближении) 3мм-обводка становится толще
     самих букв — белое пятно вместо текста. Фикс — `vector-effect:
     non-scaling-stroke` на `.mark-label`/`.mark-sublabel` (обводка
     фиксирована в экранных px, не растёт при зуме).
   - **2b. Многоярусные конструкции (одна и та же (x,y), разные
     elevation_mm) в 2D — подпись нижнего яруса пряталась ПОД
     непрозрачной фигурой верхнего яруса НАВСЕГДА.** `renderElements`
     клал фигуру и подпись каждого элемента в ОДНУ группу, группы шли в
     `elements-layer` по порядку — у элемента с бо́льшим id (более
     поздний ярус той же колонны) фигура рисовалась ПОЗЖЕ = поверх ВСЕГО
     более раннего, включая подпись. Живой тест (перемещение проблемной
     подписи в конец DOM) подтвердил: подпись валидна, просто
     физически перекрыта. Фикс — новый слой `#labels-layer` ПОСЛЕ
     `#elements-layer` в SVG; `renderElements` кладёт ВСЕ фигуры в
     elements-layer и ВСЕ подписи (+допстроки партии) в labels-layer
     отдельным проходом — подписи теперь всегда поверх всех фигур,
     столкновения подписей друг с другом по-прежнему решает
     `updateLabelCollisionVisibility`. Из 4 подписей на одной точке
     раньше была видна максимум 1 (последняя по DOM), теперь — тоже 1
     (не расширяли collision-логику), но теперь она РЕАЛЬНО видна, а не
     скрыта случайно.

**3. Быстродействие — подписи и зоны включены по умолчанию.** На файле
260720 (9385 элементов, было максимум 5330) `updateSizesForZoom` +
`updateLabelCollisionVisibility` — два O(n) прохода по ВСЕМ элементам на
КАЖДЫЙ тик колёсика мыши, без throttle/rAF — обычный zoom вызывал
многосекундные подвисания браузера. Заказчик подтвердил: подписи и зоны
включать по требованию, не по умолчанию. `label_visibility` (БД,
`ensure_label_visibility` в `scripts/import_elements.py`) — новые типы
заводятся с `visible=0` вместо `1`; существующие 3 строки в БД тоже
переведены в 0 (иначе не подействовало бы на уже загруженные типы).
`state.zoneVisibility` (клиент, не персистится) — дефолт `false` для
всех трёх категорий. UI-тумблеры (Фильтры → Подписи/Отображение зон)
не менялись — просто теперь стартуют выключенными, включаются как
раньше.

**4. Клик по колонне/ригелю в 3D работал только по верхней грани.**
`build3DOutlineMesh` (тёмный "ободок") добавлялся ДОЧЕРНИМ мешем без
`userData.elementId` — на боковых гранях именно он оказывался
ближайшей к камере поверхностью (см. п.5), клик по нему возвращал
`null` в `pick3DElementAt`. На верхней грани его перекрывал цветной меш
(со своим elementId) — оттого клик там работал. Фикс — `outline.
userData.elementId = element.id` при сборке. Проверено: raycast в
центр экранной проекции БОКОВОЙ грани колонны возвращает верный
элемент.

**5. В 3D читались только верхние рёбра, вертикальные грани — сплошным
тёмным пятном без контраста.** Старая геометрия ободка растягивалась
только по высоте (просвет сверху/снизу от истинного диапазона) — на
плане (X-Y) ободок был РОВНО истинного контура, а цветной меш — уже на
6% (`OUTLINE_INNER_SCALE`). На боковых гранях это давало не тонкую
кайму, а СПЛОШНОЕ перекрытие: ободок (шире на плане) был ближе к камере
по всей высоте и ширине грани, цветной меш не проглядывал вообще. Фикс
— приём "инвертированной оболочки": ободок теперь строится по контуру,
раздутому НАРУЖУ на фиксированные `OUTLINE_HULL_MARGIN_MM=35` мм по
ВСЕМ осям (план и высота, через новую `offsetOutlineMM`, аддитивный, не
мультипликативный отступ — иначе у метровой плиты кайма была бы
неоправданно толстой, а у колонны — незаметной), и рисуется ТОЛЬКО
`side: THREE.BackSide` — из-за backface-отбраковки виден только там,
где вылезает за пределы цветного меша, т.е. ровно тонкой каймой по
силуэту с любой стороны обзора, а не сплошным пятном. Живой тест:
одиночная колонна сбоку (видна тёмная кайма по обеим боковым граням,
не было раньше), 6 впритык стоящих плит перекрытия сверху (чёткие
тёмные границы на стыках, БЕЗ мерцания/z-fighting — тот же класс
проблемы, что уже чинили раньше для другой версии ободка).

**6. Уточнение про распределение по стоянкам — оказалось уже работает
верно, без изменений в коде.** Ригель@+5000 и элементы@+47000 не имеют
СВОЕЙ отметки в `zones` (там только один физический слой `Стоянки
крана_ОТМ00000`) — уже существующая "лесенка" стоянок
(`compute_column_tier_elevations`/`_tier_index` в `scripts/
zone_binding.py`) снэпает элемент на БЛИЖАЙШИЙ ярус колонн НИЖЕ его
отметки: 5000→0 (единственный ярус ниже), 47000→34700 (ближайший к
названным пользователем "37.000" — видимо округление на словах).
Прямая проверка в БД: подавляющее большинство элементов на обеих
отметках уже `zone_stance_status='matched'`; 40 ригелей@5000 —
`unmatched`, тот же класс "неполнота исходного чертежа", что уже
задокументирован (заказчик подтвердил, донаполнится).

**Проверено**: впервые в истории проекта — живой браузер (Claude
Browser pane), а не только изолированные Python/Node-скрипты. Логин,
2D/3D переключение, зум (включая JS-инъекцию `setView`/прямую
манипуляцию сценой Three.js для диагностики недоступных через обычный
scroll ситуаций), чтение пикселей через `gl.readPixels`,
`getBoundingClientRect`, перемещение живых DOM-узлов для локализации
z-order бага. Все 5 фиксов подтверждены визуально и/или программным
пересчётом на файле 260720 (9385 элементов, 4 яруса колонн, 4 яруса
плит перекрытия).

**Открытый вопрос**: производительность (п.3) снята с дефолтных
подписей/зон, но сам O(n)-без-throttle цикл в `updateSizesForZoom`/
`updateLabelCollisionVisibility` не тронут — при явном включении
подписей на файле такого размера подвисания при зуме вернутся. Не
исправлено в этом раунде (не запрошено явно), стоит держать в уме.

**Файлы**: `app/static/index.html` (`elementExtrusionHeight`,
`computeTopColumnCeiling` новая, `computeBuildingHeightRange`,
`.mark-label`/`.mark-sublabel` CSS, разметка `<svg>` — новый слой
`#labels-layer`, `renderElements`, `clearWorkspace`, `state.
zoneVisibility` дефолт, `scaledOutline`/`OUTLINE_INNER_SCALE`/
`OUTLINE_VERTICAL_MARGIN_MM` — без изменений по смыслу, `offsetOutlineMM`
новая, `OUTLINE_HULL_MARGIN_MM` новая, `build3DOutlineMesh`,
`build3DMeshForElement`); `scripts/import_elements.py`
(`ensure_label_visibility` — default 0); БД (`label_visibility` — 3
строки visible=0).

## Новый файл 260722 — реальные стоянки на 4 ярусах ломали привязку, подписи слишком мелкие (2026-07-22)

Пользователь заменил файл в `Input/` на `260722_Чертежи для WEB.dxf`
(старый 260720 удалён) — теперь заказчик нарисовал РЕАЛЬНЫЕ полигоны
стоянок крана на ВСЕХ 4 ярусах колонн (`WEB_тех_Стоянки крана_ОТМ00000/
ОТМП15800/ОТМП25800/ОТМП34700`), а не только на нулевом, как раньше.

**Баг 1 (главный) — 8694 из 9385 элементов уходили в `zone_stance_
status='needs_review'`.** `build_stance_level_polygons` (лесенка сужения
стоянки с высотой, см. «Ключевые решения» в CLAUDE.md) была написана
для СТАРОЙ модели данных: один физический полигон стоянки на земле,
верхние ярусы синтезируются программно сужением. Функция группирует
записи `zones` категории "Стоянка" по `(кран, номер_стоянки)`, ИГНОРИРУЯ
`elevation_mm` — с одним физическим ярусом это было безопасно (все
записи и так на одной отметке), но с четырьмя РЕАЛЬНЫМИ ярусами каждая
стоянка теперь даёт 4 отдельные записи (разные DXF-хендлы на разных
слоях), и функция принимает их за "N стоянок подряд в одном ряду" —
путает "номер в ряду" с "ярус", лесенка режет несуществующий длинный
ряд. Итог — искусственная неоднозначность почти у всех элементов.

**Фикс**: `scripts/new_standard_pipeline.py` — синтетическая лесенка
(`build_stance_level_polygons`/`compute_column_tier_elevations`)
включается ТОЛЬКО когда в файле физически один ярус стоянок
(`len({z.elevation_mm for z in zones if category=="Стоянка"}) <= 1`) —
обратная совместимость со старыми файлами (260713…260716, только
`ОТМ00000`) не тронута. Когда ярусов несколько (реальные данные) —
`stance_level_polys=None` передаётся в `bind_element_to_zones`, что уже
существующей веткой этой функции переключает "Стоянка" на путь
`_candidates_for_category` (общий для Захватки/Крана).

**Баг 2, тот же корень** — старая `_candidates_for_category` требовала
ТОЧНОГО совпадения `elevation_mm` элемента с отметкой полигона стоянки.
Для элементов НЕ на ярусе колонн (ригель-парапет +5000, плиты +15000/
+47000) точного совпадения никогда не будет — раньше это лечила именно
лесенка (снэп на ближайший ниже ярус КОЛОНН). Убрав лесенку для
многоярусных файлов, нужно было сохранить снэп — переписал
`_candidates_for_category` (`scripts/zone_binding.py`): теперь ищет
ближайший НИЖЕ РЕАЛЬНО НАРИСОВАННЫЙ ярус стоянок (не ярус колонн — сами
ярусы стоянок теперь и есть источник истины), а не точное совпадение.
Ровно то, что пользователь описал: ригель@+5000 → стоянка@0 (снэп на
ближайший физически существующий ярус ниже), элементы@+47000 →
стоянка@+34700 (пользователь ранее написал «+37.000» — опечатка/
округление, подтвердил: имелось в виду 34700).

**Проверено**: `element_bindings_needs_review['Стоянка']` 8694→0.
Полная переобработка (`process_upload`) — 9385/9385 `matched` (было
691 matched + 8694 needs_review + 0 unmatched). Разбивка по (тип,
отметка) — 100% matched на КАЖДОЙ отметке, включая проблемные +5000
(103/103, было 63 matched + 40 unmatched на предыдущем файле) и +47000
(38/38). Синтетический тест `scripts/verify_zone_pipeline.py` (2 яруса
стоянок, 15800/18800, упражняет НОВЫЙ не-лесенка путь) — все проверки
по-прежнему проходят, включая точное различение "Стоянка A (+15800)"
от "Стоянка B (+18800)" для элемента на +15800.

**Баг 3 — подписи марок нечитаемы на мелком масштабе (весь чертёж на
экране)** — `computeEffectiveMarkerSizing` раньше имела только ВЕРХНИЙ
предел размера шрифта на экране (`MAX_LABEL_FONT_PX=24`), без нижнего:
на мелком масштабе `baseFont` (фиксированное значение в мировых мм)
даёт на экране доли пикселя. Добавлена `MIN_LABEL_FONT_PX=14` — тем же
приёмом (перевод экранного px-предела в мировые единицы под текущий
pxPerUnit), просто снизу: `effectiveFont = Math.max(Math.min(baseFont,
MAX/pxPerUnit), MIN/pxPerUnit)`. Маркер (`effectiveR`) не трогал —
только текст, как и просил пользователь. Живая проверка: марки читаемы
и при 100% (весь чертёж), и при обычном рабочем зуме — раньше на 100%
подпись была практически невидима (доли пикселя).

**Файлы**: `scripts/new_standard_pipeline.py` (условие для лесенки),
`scripts/zone_binding.py` (`_candidates_for_category` — снэп вместо
точного совпадения), `app/static/index.html` (`MIN_LABEL_FONT_PX`,
`computeEffectiveMarkerSizing`); Input/БД — новый файл `260722_Чертежи
для WEB.dxf` загружен (9385 элементов, 175 зон, 0 needs_review).

## Стоянка крана — объём по ярусам вместо плоского маркера, дедуп фильтра (2026-07-22)

Продолжение предыдущего раунда (260722, реальные полигоны стоянок на 4
ярусах): пользователь явно описал желаемое поведение — "Стоянка 1"
крана визуально должна быть ОДНОЙ объёмной фигурой из нескольких
параллелепипедов, поставленных друг на друга (по одному на каждый
реально нарисованный ярус, от своей отметки до отметки следующего яруса
этой же стоянки), а не плоским маркером на земле. И в списке фильтров —
ровно один пункт "Стоянка 1" на кран, отбирающий элементы со ВСЕХ её
ярусов сразу, а не 4 отдельных дублирующих пункта.

**1. 3D-объём стоянки** (`build3DZoneMesh`, `app/static/index.html`) —
новая `stanceTierElevations()` (аналог `computeColumnLevels`, но по
`elevation_mm` РЕАЛЬНО нарисованных зон категории "Стоянка"). Если таких
ярусов больше одного — каждая ярусная зона-стоянка теперь `ExtrudeGeometry`
от своей отметки до отметки СЛЕДУЮЩЕГО яруса (или до `heightRange.top`
для самой верхней, та же логика, что и у верхнего яруса колонн, см.
предыдущий раунд), а не плоский `ShapeGeometry` на Y=10. Если ярус
стоянок в файле всего один (старые файлы, 260713…260716) — прежнее
плоское поведение не тронуто. "Кран" (рабочая зона) остаётся плоским —
своих ярусов не имеет. Проверено: зона "Стоянка 1" крана 4100 — 2 меша
(elevation=0/height=15800 и elevation=15800/height=10000, встык, без
зазора/нахлёста), верхний ярус стоянки — height ровно до
`heightRange.top` (47000, та же ценность, что и у верхнего яруса колонн).

**2. Дедуп в списке фильтров** — раньше значение фильтра "Стоянка" было
сырым `zone.id` (осознанно, не имя — см. старый комментарий у
`zoneFilterValue`: разные краны переиспользуют имена типа "Стоянка 1"
заново). С 4 физическими записями на одну логическую стоянку (по одной
на ярус) это давало 4 ОТДЕЛЬНЫХ пункта "Стоянка 1" в списке, каждый
отбирал элементы только СВОЕГО яруса. Новый `stanceLogicalKey(zoneId)` —
ключ `stance:<id крана>:<имя>` (кран уже различает одноимённые стоянки
РАЗНЫХ кранов, как и раньше; ярус в ключ не входит — как раз то, что
нужно схлопнуть). `stanceFilterValue`/`allValuesFor("stance")` теперь
оперируют этим ключом, а не `zone.id`; `craneIdForStanceId` разбирает id
крана прямо из ключа; новый `stanceNameForLogicalKey` — подпись
чекбокса. Проверено: `allValuesFor('stance')` — 63 уникальных ключа
(было бы существенно больше при подсчёте по сырым id зон), ноль
дублей при группировке по (кран, имя); фильтр по одной "Стоянка 1"
крана 4100 — 197 элементов на 4 РАЗНЫХ отметках (0/5000/15000/15800)
одним пунктом; в самом UI под "Кран 1" — "Стоянка 1"…"Стоянка 11" каждая
ровно один раз.

**Файлы**: `app/static/index.html` (`stanceLogicalKey`,
`stanceNameForLogicalKey`, `stanceTierElevations` — новые;
`stanceFilterValue`, `allValuesFor`, `craneIdForStanceId`,
`stanceLabelFor` в `renderPlacementFilters`, `build3DZoneMesh` и её
вызов в `build3DScene` — изменены).

## Фильтр по крану/стоянке — два реальных бага с живой проверки (2026-07-22)

Пользователь дал конкретный пример (элемент id 28083, dxf handle 1AB76):
считал, что относится к первой стоянке первого крана, но снятие
галочки со ВТОРОГО крана убирало его со схемы; а в 2D снятие галочки с
ПЕРВОГО крана убирало со схемы вообще всё, включая элементы других
кранов. Два независимых бага, один в данных/логике привязки (backend),
один в рендере (frontend, моя же правка из предыдущего раунда).

**Баг 1 — стоянка сопоставлялась без учёта СВОЕГО крана элемента.**
Прямая проверка в БД подтвердила: `zone_crane_id` элемента 28083 = Кран 1,
но его `zone_stance_id` (зона "Стоянка 1" на отм. +34700, куда снэпнула
логика прошлого раунда) на самом деле дочерняя зона КРАНА 2
(`parent_zone_id`) — у Крана 1 своя "Стоянка 1" физически нарисована
только на 0 и +15800, на +34700 её нет, и снэп (не различающий краны)
зацепился за геометрически ближайшую стоянку ЧУЖОГО крана. Задал
пользователю 2 уточняющих вопроса — оба подтверждены: (1) стоянка
должна сопоставляться ТОЛЬКО среди стоянок своего крана; (2) если у
своего крана на нужном ярусе стоянки нет — снэп ещё ниже, до
ближайшего яруса, где она у НЕГО есть (не unmatched).

**Фикс**: `scripts/zone_binding.py` — `_candidates_for_category`
принимает новый параметр `own_crane_handle`, сужает кандидатов до
стоянок этого крана ДО снэпа по высоте. `bind_element_to_zones` —
поскольку `ZONE_CATEGORIES = ("Захватка", "Кран", "Стоянка")` и "Кран"
обрабатывается раньше "Стоянки" в том же вызове, `result["Кран"]` уже
готов к моменту обработки "Стоянки" — передаёт `zone_handle` уже
сматченного крана как `own_crane_handle`. Путь "лесенка"
(`use_tiered_stances`, старые однoярусные файлы) не тронут — он и
раньше был по-крановый через `stances_by_crane` в
`build_stance_level_polygons`, проблема была только в НОВОМ
не-лесенка пути. Проверено: элемент 28083 после переобработки —
`zone_crane_id`/`zone_stance_id` теперь указывают на зоны с ОДНИМ и тем
же `parent_zone_id` (согласовано), снэп ушёл на +15800 (ближайший ярус
СВОЕГО крана), 9385/9385 по-прежнему `matched` (переход на per-crane
снэп не создал новых unmatched в этом файле — у всех элементов свой
кран имеет хоть одну стоянку не выше их отметки). Синтетический тест
`scripts/verify_zone_pipeline.py` — по-прежнему проходит целиком.

**Баг 2 — в 2D снятие любой галочки в фильтре гасило ВСЮ схему.** Моя
собственная регрессия из раунда "Первая живая браузерная сессия" (см.
выше): вынося подписи в отдельный `#labels-layer`, забыл, что
`applyPlacementFilters()` скрывает элемент через
`shape.parentElement.style.display = "none"` — а `parentElement`
фигуры стал ОБЩИМ на все 9385 элементов (`#elements-layer` целиком, не
своя группа на элемент, как было до переноса подписей). Как только хоть
один элемент не проходил фильтр, скрывался общий слой целиком; какой
элемент обработан последним в цикле — то и решало итоговое состояние
общего display. Фикс — скрывать КАЖДЫЙ узел индивидуально: фигуру
напрямую (`shape.style.display`), подпись(+допстроку) — через новую
маленькую обёртку `<g>` НА КАЖДЫЙ элемент внутри `#labels-layer`
(`state.labelGroupById`, создаётся в `renderElements`) — сохраняет и
z-order фикс прошлого раунда (обёртка всё ещё живёт в общем
`labels-layer` ПОСЛЕ `elements-layer`), и точечную скрываемость.
`renderElements` теперь сразу вызывает `applyPlacementFilters()` в
конце — новые узлы не остаются на секунду видимыми до первого
пересчёта. Проверено вживую: снятие ДРУГОГО (не своего) крана у
элемента 28083 — `#elements-layer` остаётся `display:inline`, сам
элемент виден, 6315/9385 видимых (не 0 и не 9385); снятие ЕГО
СОБСТВЕННОГО крана — элемент скрывается (`display:none`), но элемент
ДРУГОГО крана остаётся видим — 6277/9385 (не 0).

**Файлы**: `scripts/zone_binding.py` (`_candidates_for_category`,
`bind_element_to_zones`); `app/static/index.html`
(`state.labelGroupById` новая, `renderElements`, `applyPlacementFilters`,
`clearWorkspace`).

## Кран — тоже объём на всю высоту; иерархия видимости стоянок по кранам (2026-07-22)

Пользователь: (1) зона крана — тоже параллелепипед (контур основания
дан в чертеже, по вертикали — до самой верхней точки здания, не плоский
полигон на земле, как раньше); (2) в разделе "Отображение зон" нужна
иерархия Кран→его Стоянки (как в фильтре), чтобы включить видимость
ОДНОЙ конкретной стоянки конкретного крана сразу на всех её ярусах, а
не всю категорию "Стоянка" целиком.

**1. Кран — полная высота здания.** `build3DZoneMesh` — категория
"Кран" объединена с "Захватка" в одну ветку (`ExtrudeGeometry` на
`heightRange.top - heightRange.bottom`, opacity 0.07) — раньше была в
одной ветке со "Стоянкой" (плоский `ShapeGeometry` на Y=10). Проверено:
все 3 зоны крана — `worldY=0`, `height=47000` (тот же `heightRange.top`,
что и у верхнего яруса колонн/стоянок).

**2. Иерархия видимости стоянок.** Новое `state.stanceZoneVisible` —
Set логических ключей (`stanceLogicalKey`, тот же, что у фильтра),
opt-in (по умолчанию пусто — ничего не видно, та же логика
быстродействия, что и у остальных зон), в отличие от
`state.zoneVisibility` (excluded-семантика через `!== false`) — умышленно
НЕ переиспользовал `buildHierarchicalFilterGroup` (та функция — excluded-
множество, здесь семантика противоположная, opt-in; смешивать в одной
функции рискованнее, чем продублировать вёрстку). Новая
`renderStanceZoneToggles()` — та же визуальная структура (те же CSS-
классы `filter-parent-row`/`filter-children`/`filter-expand-btn`), что
и иерархия в фильтрах: разворачиваемый кран, чекбокс крана
ставит/снимает разом все его стоянки (с `indeterminate`, если
включена только часть), чекбокс стоянки — только её саму, на всех
ярусах разом (той же `renderZones`/`build3DZoneMesh`/
`apply3DZoneVisibility`, что уже умеют объёмную стоянку по ярусам, см.
предыдущий раунд). "Стоянка" убрана из плоского списка
"ОТОБРАЖЕНИЕ ЗОН" (`renderZoneToggles` — теперь только Захватка/Кран),
вместо неё под тем же заголовком — новый блок "Стоянки по кранам".
Проверено вживую: разворот "Кран 1", включение "Стоянка 1" — в 2D
видна её граница на плане; в 3D — обе её ярусные зоны (id 4628@0,
id 4691@15800) `visible:true`, остальные стоянки/краны по-прежнему
скрыты.

**Файлы**: `app/static/index.html` (`state.zoneVisibility` — без
"Стоянка", `state.stanceZoneVisible` новая, `placementGroupsExpanded.
stanceZone` новая, `renderZoneToggles`, `renderStanceZoneToggles` новая,
`renderZones`, `build3DZoneMesh`, `zoneMeshVisible` новая,
`apply3DZoneVisibility`, HTML — новый `#stance-zone-toggles`,
`clearWorkspace`/`loadPlan` — вызовы `renderStanceZoneToggles()`).

## Зона #4754 без названия — не "имя на одном ярусе для всех", а слишком строгий допуск сопоставления (2026-07-23)

Пользователь заметил зону `#4754` (стоянка без имени) и предположил
общую причину: номер стоянки может быть обозначен один раз где-то
сверху и относиться сразу ко всем ярусам/кранам. Проверил гипотезу
напрямую по исходному DXF — не подтвердилась, нашлась другая, куда
более простая причина.

**Что оказалось на самом деле**: подписи названий стоянок — это
`MULTILEADER` (выноска), 168 штук, РОВНО по одной на каждый из 168
полигонов "область" (уже учтено в `_multileader_name`, см. Docs/backlog.md
про MULTILEADER-марки). Сопоставление названия полигону —
`match_names_to_zones()` в `scripts/zone_parser.py`: острие выноски
должно лежать в пределах `NAME_MATCH_TOLERANCE_MM=1.0` мм от полигона.
Для 165 из 168 стоянок так и есть. Для оставшихся 3 (включая #4754)
острие выноски геометрически лежит ЧУТЬ снаружи полигона — не в 10-м
знаке после запятой (как штатный случай округления CAD, ради которого
допуск вообще завели), а на 2.07мм / 2.70мм / 18.27мм — то есть тот же
класс проблемы, что и штатный, просто больше по величине. Прямой замер
расстояний (`shapely.distance`) до ВСЕХ выносок того же яруса
подтвердил: правильный кандидат — 2-18мм, следующий по близости
неверный — 1063-2559мм, то есть даже "непрошедшая" выноска однозначно
понятна, ей просто не хватило 1мм допуска.

Отдельно проверил: у зоны #4754 действительно огромный полигон (Y от
5768 до 134049 — по факту вся длина здания, через все 3 крана), из-за
чего он визуально "заходит" в зоны кранов 1 и 3 в 3D (теперь объёмных
на всю высоту, см. предыдущий раунд) — но `parent_zone_id` у неё и так
корректно указывал на Кран 2 всё это время, привязка к крану была верной
с самого начала, страдало только название.

**Фикс**: `scripts/zone_parser.py` — если острие выноски не попало в
строгий допуск ни у одного полигона, запасной путь: берём БЛИЖАЙШИЙ
полигон, но только если он ЗАМЕТНО (`NAME_NEAR_MISS_MARGIN_MM=200`мм)
ближе любого другого кандидата И сам не дальше `NAME_NEAR_MISS_
TOLERANCE_MM=50`мм — оба порога взяты с большим запасом от реальных
цифр (2-18мм / 1063-2559мм), чтобы не начать угадывать там, где стоянки
стоят вплотную и первый допуск (1мм) отсеивает по делу. Проверено:
`needs_review` зон 3→0, все 168/168 стоянок `matched`, зона #4754 (id
изменился при переимпорте, dxf_handle 2C26B тот же) получила имя
"Стоянка 1". Синтетический тест `verify_zone_pipeline.py` по-прежнему
проходит.

**Файлы**: `scripts/zone_parser.py` (`match_names_to_zones`,
`NAME_NEAR_MISS_TOLERANCE_MM`/`NAME_NEAR_MISS_MARGIN_MM` новые).

## Стоянка = пересечение с зоной крана, разбивка на ярусах "один прямоугольник для всех кранов" (2026-07-23)

Продолжение предыдущего раунда (зона #4754 без имени) — пользователь
задал вопрос "почему зона #4754 заходит в зоны кранов 1 и 3", и по ходу
обсуждения (без сразу переделывать, шаг за шагом) выяснился реальный
факт устройства чертежа: на ярусах 0 и +15800 у каждого крана
нарисован СВОЙ отдельный прямоугольник стоянки (63 = 3×21), а на
+25800 и +34700 заказчик рисует ОДИН вытянутый прямоугольник "Стоянка N"
сразу для всех кранов (21 штука на ярус, не 63) — проверил на данных:
100% стоянок (21/21) обоих верхних ярусов пересекают все 3 крана
заметной площадью (сотни м²), 0% на нижних двух. Раньше связь
"стоянка → кран" бралась по центроиду с требованием ровно ОДНОГО
кандидата — на верхних ярусах центроид с точки зрения кода "проваливался"
в какой-то один кран произвольно, а два других крана эта же самая
стоянка теряла целиком.

Пользователь подтвердил модель: настоящая зона стоянки N крана X на
таком ярусе — это ПЕРЕСЕЧЕНИЕ прямоугольника стоянки с объёмом ИМЕННО
этого крана (не перебор всех кранов заново, тот же кран, что уже
определяется по пересечению), резать по ЛЮБОЙ ненулевой площади
пересечения (без минимального порога), ярусы 0/+15800 не трогать
вообще.

**Фикс**: `scripts/zone_parser.py::_link_stances_to_cranes` — вместо
"один центроид → один кран" теперь считает ПЛОЩАДЬ пересечения с
КАЖДЫМ краном: пересекает ровно один — ведёт себя как раньше (запись не
меняется, контур не режется — так и попросили для 0/+15800); пересекает
несколько — заменяет ОДНУ запись на КАЖДЫЙ кран отдельно, контур каждой
= shapely-пересечение с зоной этого крана (`_polygon_to_outline` новая
— конвертация обратно в список вершин, без замыкающего повтора).
Составной handle (`"{исходный}#{кран}"`) — чтобы не столкнуться с
`UNIQUE(source_file, dxf_handle)`. Мутирует список зон на месте, как и
прежняя версия функции.

Побочный эффект (в плюс): у Крана 1 и Крана 3 раньше НЕ БЫЛО вообще
никаких стоянок на +25800/+34700 (только у Крана 2, которому центроид
исходной стоянки "достался") — элементы этих кранов на верхних ярусах
были вынуждены снэпаться на +15800 (см. предыдущий раунд). Теперь у
всех трёх кранов есть полный, симметричный набор — 21 стоянка на КАЖДОМ
из 4 ярусов (было: у Крана 2 полный набор, у Кранов 1/3 — только 2
яруса). Элемент #28083 (тот самый, с которого началось расследование
двух раундов назад) — плита на +47000, кран 5409 (Кран 1) — раньше
снэпался на стоянку +15800 через 2 яруса, теперь напрямую находит
"Стоянка 1" Крана 1 на +34700 (своей, впритык под своим верхним ярусом
колонн), без пропуска яруса.

**Проверено**: пересчёт файла — зон категории "Стоянка" 168→252 (126
нетронутых на 0/+15800, плюс 21×3 + 21×3 = 126 новых расщеплённых на
+25800/+34700), `needs_review: 0`. У каждого из 3 кранов теперь ровно
по 21 стоянке на КАЖДОМ из 4 ярусов (было — только у Крана 2 полный
набор). 9385/9385 элементов по-прежнему `matched`. Живая проверка в
3D: контур "Стоянка 1" Крана 1 на всех 4 ярусах — X/Y строго внутри
полосы этого крана (Y 5768–42689, та же на всех 4 ярусах), сужается по
X с высотой (43408→34508→25458→16458) — та самая "лесенка", но теперь
у каждого крана СВОЯ, а не общая на всё здание. Синтетический тест
`verify_zone_pipeline.py` (один кран, без множественного пересечения)
по-прежнему проходит без изменений в поведении.

**Файлы**: `scripts/zone_parser.py` (`_link_stances_to_cranes`
переписана, `_polygon_to_outline` новая). Фронтенд не тронут — вся
логика иерархии фильтра/3D-объёма из предыдущих двух раундов рассчитана
на "одна запись зоны = один кран" и заработала на новых данных без
единой правки.

## Подписи в 3D — размер от собственного размера элемента, не фиксированный (2026-07-23)

Пользователь прислал живой скриншот: подписи марок в 3D (белые точки на
плитах перекрытия и колоннах) практически нечитаемы. Причина —
`build3DLabelSprite` считал мировой размер подписи как фиксированную
`state.baseMarkerRadius * 3` (глобальный параметр из
`estimate_marker_radius`, тот же, что и у 2D-маркеров), не зависящую от
размера самого элемента. На этом файле `baseMarkerRadius=25мм` →
подпись высотой ~75мм — при реальном сечении колонны 600×600мм (12.5%
от ширины) и плиты 1200×5300мм (6% от короткой стороны) буква
превращалась в точку.

Заказчик подтвердил правило для плит перекрытия: высота букв — чуть
меньше ширины плиты; для колонн — тоже крупнее, "чтобы было комфортно
считывать". Распространил ОДНО правило на все типы, а не завёл отдельно
для плиты (два разных правила без причины избыточны): новая
`label3DWorldHeight(element)` — `crossSectionWidth(element.outline) * 0.8`
(та же устойчивая к повороту геометрия, что уже даёт высоту экструзии
Ригелю/Панели, ничего заново не мерить), нижний предел
`MIN_LABEL_3D_WORLD_MM=150` — не даёt подписи выродиться в точку на
аномально узком контуре. `build3DLabelSprite` — `worldPerPx` теперь от
этой функции, не от `state.baseMarkerRadius`.

**Проверено**: плита 1200мм ширины → подпись 960мм (80% от ширины,
запрошенное правило); колонна 600мм → подпись 480мм (было 75мм, рост в
6.4 раза). Живой 3D-скриншот на разных дистанциях — марки "4П-15",
"4П-2" и т.п. на плитах, короткие марки колонн — все читаются
уверенно, размер пропорционален элементу, не выглядит ни точкой, ни
избыточно огромным.

**Файлы**: `app/static/index.html` (`label3DWorldHeight`,
`LABEL_3D_WIDTH_FRACTION`, `MIN_LABEL_3D_WORLD_MM` новые;
`build3DLabelSprite` — `worldPerPx`).

## Аудит безопасности перед возможной публикацией в открытый интернет (2026-07-23)

Пользователь попросил оценить готовность к выходу в открытый интернет
(сейчас деплой — обычный HTTP внутри локальной сети, см.
`Docs/DEPLOYMENT_WINDOWS.md`). Полный список находок — критично/высоко/
средне — не переносится сюда целиком (см. историю диалога), но два
самых критичных пункта закрыты сразу же, с усилением по запросу
пользователя (требования к сложности пароля + способ восстановления):

**1. Пустой пароль по умолчанию.** `password_hash IS NULL` раньше
означало "вход с пустым паролем разрешён" — включая дефолтного `admin`,
которого `schema.sql` заводит при первой инициализации БД. В открытом
интернете это открытая дверь: логин `admin` не секрет, пустой пароль —
не требует подбора вовсе. Теперь `verify_password` (`app/auth.py`)
возвращает `False` безусловно при `password_hash IS NULL` — такой
аккаунт просто не может войти, пока пароль не задан явно.

Это создаёт проблему курицы-и-яйца для свежего развёртывания (дефолтный
admin не может войти, чтобы задать себе пароль через UI) — решена
новым `scripts/reset_password.py <domain_login>`: меняет пароль
напрямую в БД в обход API и любой сессии (getpass, без эха в терминал),
с теми же требованиями к сложности, что и `POST
/users/{id}/set-password`. Он же — ответ на "предусмотреть восстановление,
если пароль утрачен": требует доступа к файловой системе сервера (тот
же уровень доверия, что и прямой доступ к `data/zhbi.db`), альтернативы
(почта/SMS) для внутреннего инструмента без адресов сотрудников нет и
не планировалось.

**Требования к сложности** (по явному запросу пользователя, до этого
не было никаких) — `validate_password_strength()` в `app/auth.py`: не
короче 8 символов, буквы и цифры. Применяется в двух местах:
`POST /users/{id}/set-password` и `scripts/reset_password.py` — то есть
везде, где пароль реально устанавливается. НЕ применяется при входе —
на уже принятые (до этого изменения) пароли задним числом не
действует, иначе пользователи с уже заданным коротким паролем оказались
бы заперты снаружи без предупреждения.

Заодно — раз уже трогали `set_password`: смена пароля теперь удаляет
все ДРУГИЕ активные сессии этого пользователя (`DELETE FROM sessions
WHERE user_id = ? AND token != ?`, текущая сессия вызывающего явно
исключена по токену из cookie). Без этого смена пароля (в том числе —
именно потому, что заподозрили компрометацию сессии) не имела бы
практического эффекта: старая cookie продолжала бы работать. Явно
снять пароль (вернуть в "вход запрещён") теперь может только admin над
чужим аккаунтом — не сам пользователь себе (иначе самообслуживание
позволяло бы случайно себя же и запереть; для этого сценария есть
`scripts/reset_password.py`, обычная UI-форма — нет).

На старте сервера (`app/main.py`, `_warn_users_without_password`)
теперь громко логируется список пользователей с `password_hash IS
NULL` — чтобы забытый незалогинившийся аккаунт (в первую очередь —
только что созданный admin) не потерялся молча.

**2. Утечка списка логинов.** `GET /login-users` отдавал ФИО+логин всех
пользователей БЕЗ авторизации — им пользовалась выпадашка на экране
входа. Сам код честно называл это компромиссом, неприемлемым при
публичном доступе. Эндпоинт и модель `PublicUserOut` удалены целиком;
экран входа (`app/static/index.html`) — обычное текстовое поле логина
вместо `<select>`, `loadLoginUsers()` убрана.

**Заодно закрыто** (малая, но раз уж список был под рукой):
`/docs`, `/redoc`, `/openapi.json` раскрывали полную карту API
анонимному клиенту без всякой пользы для внутреннего инструмента —
выключены по умолчанию (`docs_url`/`redoc_url`/`openapi_url=None` в
`app/main.py`), включаются для локальной разработки переменной
`ZHBI_ENABLE_DOCS=1`.

**Проверено живым браузером** (после `python3 scripts/reset_password.py
admin` — иначе войти было уже не с чем): вход с новым паролем — успех;
попытка сменить пароль на `weak` — отклонена валидацией ("Пароль должен
быть не короче 8 символов"); успешная смена на корректный пароль —
диалог закрылся, сессия браузера осталась активной; параллельно
залогиненная через `curl` сессия того же пользователя после смены
пароля — `401` на `/me` (сессия браузера, которой меняли пароль, —
по-прежнему `200`); `GET /login-users` и `GET /docs` — оба `404`.
Старый пароль после смены — `401` на `/login`, как и ожидалось.

**Файлы**: `app/auth.py` (`validate_password_strength` новая,
`verify_password` без байпаса на `NULL`, `/login-users`+`PublicUserOut`
удалены), `app/users.py` (`set_password` — валидация + сброс сессий),
`app/main.py` (`docs_url`/`redoc_url`/`openapi_url` по env,
`_warn_users_without_password`), `scripts/reset_password.py` (новый),
`app/static/index.html` (форма входа, тексты диалогов смены пароля).

Не сделано в рамках этого раунда (см. полный список аудита в истории
диалога, при необходимости попросить снова): TLS/`secure`-cookie по
умолчанию всё ещё выключен (нужен реальный reverse-proxy с сертификатом
на стороне окружения, не только код), лимит размера загружаемых файлов,
CSP/security-заголовки, ротация rate-limit по IP за reverse-proxy.

## Аудит безопасности, часть 2: лимит загрузки, CSP, доверие прокси (2026-07-23)

Продолжение предыдущей записи — оставшиеся пункты, которые можно
закрыть чистым кодом без решений по инфраструктуре (TLS/reverse-proxy
— отдельный вопрос пользователю, ждёт ответа).

**1. Лимит размера загружаемых файлов.** `/import-dxf`,
`/import-history-xlsx`, `/settings/import` читали тело запроса без
всякого ограничения — размер был бы ограничен только памятью/диском
процесса. Новый `app/upload_limits.py`: ASGI-middleware
(`limit_upload_size` в `app/main.py`) отклоняет запрос `413` по
заголовку `Content-Length` ДО чтения тела (для обычной браузерной
multipart-формы этого достаточно — браузер всегда знает размер файла
заранее и передаёт честный `Content-Length`); `read_upload_limited`/
`copy_upload_limited` — второй барьер, читают/пишут поток порциями,
на случай передачи без `Content-Length` (chunked). Лимит —
`ZHBI_MAX_UPLOAD_MB` (по умолчанию 200 МБ — с запасом от текущего
самого большого реального файла, 28 МБ). `save_uploaded_file`
(`app/dxf_import.py`) писал файл через `shutil.copyfileobj` без
лимита — заменён на `copy_upload_limited`, пишет сразу на диск
потоком, не держит файл в памяти.

**Проверено**: `curl` с заголовком `Content-Length: 999999999` на
`/import-dxf` → `413` до какой-либо реальной передачи данных; обычная
загрузка `uploads/sample.dxf` (58 КБ) — по-прежнему `200`.

**2. CSP и security-заголовки.** Новый middleware `security_headers`
(`app/main.py`) добавляет на каждый ответ `Content-Security-Policy`,
`X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`,
`Referrer-Policy: same-origin`, `Permissions-Policy` (без
геолокации/микрофона/камеры). Строгий `script-src 'self'` потребовал
вынести ВЕСЬ прикладной JS из инлайнового `<script>` (был один блок на
~4800 строк прямо в `app/static/index.html`) в отдельный файл
`app/static/app.js`, подключаемый как `<script src="/static/app.js">`
— сам код не менялся, просто переехал. `style-src` оставлен с
`'unsafe-inline'` осознанно: инлайновые `style="..."` для динамических
цветов статусов/зон используются по всему коду, выносить в классы
непропорционально дорого ради разбора с этим конкретным вектором
(CSS-инъекция — куда менее опасный вектор, чем JS).

Единственное, что НЕ удалось вынести — `<script type="importmap">` в
`<head>` (bare-специфайер `"three"` → локальный vendor-файл, см. его
собственный комментарий): внешние import maps (`<script
type="importmap" src="...">`) поддерживаются не всеми браузерами,
инлайновый вариант — единственный надёжный. CSP относится к import map
как к обычному инлайновому скрипту — `'self'` его не пропускает.
Решение — точечный `sha256-...`-хэш именно этого блока (статический
контент, не меняется в рантайме) вместо ослабления `script-src` до
`'unsafe-inline'` целиком.

**Живая ошибка при первой попытке** (стоит записать, чтобы не
наступить снова): посчитал хэш по содержимому regex-группой между
`<script type="importmap">\n` и `\n</script>` — включил в подсчёт
JSON, но НЕ включил сами обрамляющие переводы строк, которые regex
"съел" как context, а не capture group. В браузере `textContent` тега
включает ИХ ТОЖЕ — хэш не совпал, CSP тихо заблокировала import map
(без единой строки в консоли, которую поймал бы `read_console_messages`),
`import("three")` падал с `Failed to resolve module specifier 'three'`
— не ошибка сети (тело даже не запрашивалось, см. пустой
`read_network_requests` по `three`), а ошибка резолва: движок вообще не
считал import map зарегистрированной. Пересчитал хэш по РЕАЛЬНОМУ
`textContent`, полученному через `el.textContent` в браузере (не
регексом по исходнику) — заработало.

**Проверено живым браузером**: `curl -D -` — все 5 заголовков на
месте; `document.querySelectorAll('script')` — инлайновый JS в
`index.html` отсутствует, весь функционал (2D-схема, переключение в
3D, рендер Three.js-сцены через `import("three")`) работает без единой
ошибки в консоли после исправления хэша.

**3. Доверие `X-Forwarded-For`.** Rate-limit на `/login` считал IP по
`request.client.host` безусловно — за реальным reverse-proxy (когда он
появится вместе с TLS) это был бы IP самого прокси у ВСЕХ клиентов
сразу, и 5 неудачных попыток кого угодно блокировали бы вообще всех.
Новая `_client_ip()` (`app/auth.py`) — по умолчанию поведение то же
самое (`request.client.host`), но `ZHBI_TRUST_PROXY_HEADERS=1`
переключает на самый левый адрес из `X-Forwarded-For`. Выключено по
умолчанию совершенно осознанно: доверять этому заголовку МОЖНО только
если перед сервером точно стоит reverse-proxy, который сам его
перезаписывает — иначе любой клиент подделывает заголовок и либо
обходит лимит, либо намеренно запирает чужой IP.

**Файлы**: `app/upload_limits.py` (новый), `app/main.py`
(`limit_upload_size`, `security_headers`, `_CSP`), `app/dxf_import.py`
(`copy_upload_limited` вместо `shutil.copyfileobj`), `app/auth.py`
(`_client_ip`, `TRUST_PROXY_HEADERS`), `app/static/app.js` (новый —
весь JS, вынесенный из `index.html`), `app/static/index.html`
(инлайновый `<script>` заменён на `<script src="/static/app.js">`).

Из полного списка аудита остался только один пункт, требующий решения
пользователя, а не просто кода: TLS/`secure`-cookie (нужен реальный
reverse-proxy с сертификатом — зависит от того, будет ли у сервиса
домен и какой хостинг/прокси выбран).

## Деплой сервера переведён с Windows на Ubuntu Server (2026-07-23)

Пользователь готовит официальный запрос на доступ к корпоративному
Git-серверу и на выделение ВМ под сервис. Уточнение по ходу переписки:
ВМ может быть на любой ОС — выбрана Ubuntu Server LTS (22.04/24.04) как
бесплатная и максимально стабильная (5 лет security-обновлений,
стандартный выбор для серверных ВМ у большинства корп. ИТ). Windows
остаётся только на клиентских машинах — пользователи заходят в браузере,
установка им не нужна.

Ранее вся операционная документация (`Docs/DEPLOYMENT_WINDOWS.md`) и
скрипты (`scripts/windows/*.bat`, от 2026-07-16) были написаны под
Windows 11 как сервер — это стало неактуально с решением ставить сервер
на Linux. Не функциональное изменение приложения — только
переработанная операционная документация/скрипты установки.

**Сделано**:
- `Docs/DEPLOYMENT_LINUX.md` (новый) — install/первый запуск/смена
  пароля admin/доступ по LAN и `ufw`/резервное копирование (`cron`
  вместо Планировщика заданий)/git-схема тест+прод (та же модель:
  `main` + теги релизов, `update-git-test.sh`/`update-git-prod.sh`)/
  обновление копированием файлов/диагностика. Главное архитектурное
  отличие от Windows-версии — постоянная работа сервиса через
  **systemd**, а не через ярлык в автозагрузке/Планировщик
  заданий/сторонний NSSM: `scripts/linux/zhbi.service` (шаблон юнита,
  `Restart=on-failure`), `systemctl enable --now zhbi` переживает
  перезагрузку и падения без стороннего ПО.
- `scripts/linux/{install,run,backup,update-git-test,update-git-prod}.sh`
  (новые, `chmod +x`) — бэш-эквиваленты прежних `.bat`/`.ps1`, та же
  логика (создание `.venv`, `pip install`, метка даты/времени бэкапа
  через `date +%Y-%m-%d_%H-%M-%S` — в отличие от Windows-`.bat`, где
  `%date%`/`%time%` зависели от региональных настроек и потребовали
  обхода через PowerShell, в bash это надёжно и без обходных путей).
- Раздел про GitHub (в старом документе — конкретно VS Code "Publish to
  GitHub", Git Credential Manager) обобщён до "корпоративный Git-сервер"
  — соответствует реальному запросу пользователя (доступ к корп.
  серверу, не GitHub), способ аутентификации (SSH-ключ/токен) оставлен
  открытым — зависит от конкретного продукта (GitLab/Gitea/Bitbucket и
  т.п.), уточнять у ИТ-отдела при выдаче доступа.
- Старые `Docs/DEPLOYMENT_WINDOWS.md` и `scripts/windows/*` удалены
  (`git rm`, обратимо через историю) — сервер на Windows разворачиваться
  больше не будет, держать неактуальную инструкцию как источник путаницы
  смысла нет.
- `CLAUDE.md`, `Docs/TZ.md` (единственная ссылка, §"Защита входа") —
  указатели на `DEPLOYMENT_WINDOWS.md`/`scripts/windows` заменены на
  `DEPLOYMENT_LINUX.md`/`scripts/linux`; в `CLAUDE.md` также обобщена
  формулировка про git-remote (GitHub → корпоративный Git-сервер) и
  добавлен пункт в "Что дальше" про сам запрос доступа/ВМ.

**Не проверено**: как и Windows-версия в своё время, эта переработка не
исполнялась вживую на реальном Ubuntu-сервере (среда разработки —
macOS, нет доступа к живому Linux-серверу в рамках этой сессии) — вся
логика выверена построчно по `bash`/`systemd`/`apt`/`ufw`-семантике, но
не прогнана. `Docs/DEPLOYMENT_LINUX.md` содержит то же явное
предупреждение отдельным разделом, что и была у Windows-версии — перед
боевым использованием нужен один сквозной прогон на тестовой ВМ.

**Файлы**: новый `Docs/DEPLOYMENT_LINUX.md`; новые
`scripts/linux/{install,run,backup,update-git-test,update-git-prod}.sh`,
`scripts/linux/zhbi.service`; удалены `Docs/DEPLOYMENT_WINDOWS.md`,
`scripts/windows/*`; правки `CLAUDE.md`, `Docs/TZ.md`.

## Подписи только верхнего видимого яруса, марки зон в 3D, отметка яруса в имени стоянки (2026-07-23)

Три независимых пункта одним запросом. Попутно в РАБОЧЕМ дереве
обнаружилось, что `app/static/index.html` лишился инлайн-скрипта в
пользу `app/static/app.js` (см. отдельную запись/маркер чата "Переход
на app.js") и логин сменился на форму логин+пароль без выпадашки —
судя по всему, синхронизация с уже описанным в CLAUDE.md состоянием
(вынос JS ради CSP, обязательный пароль), сделанная параллельно в
другом сеансе. Весь код этой сессии в `app.js` цел (проверено по
маркерам — `label3DWorldHeight`, `stanceLogicalKey` и т.д., все на
месте), синтаксис валиден. У admin теперь установлен пароль — живая
проверка в браузере в этом раунде **не сделана** (не гадаю пароль),
только синтаксис + разбор кода; отмечено ниже отдельно.

**1. Подписи только верхнего видимого яруса (2D).** После переноса
подписей в общий `#labels-layer` (см. раунд "Первая живая браузерная
сессия") видны подписи ВСЕХ ярусов многоярусной конструкции сразу, даже
тех, чья фигура реально скрыта под фигурой более верхнего яруса или
выключена фильтром — новая `topVisibleLabelIds()` группирует элементы
по (x,y) (округление до 0.1мм — гасит погрешность float у геометрически
совпадающих точек одной конструкции) и оставляет подпись только у
самого верхнего по отметке элемента среди ПРОШЕДШИХ ТЕКУЩИЙ ФИЛЬТР в
каждой группе; `applyPlacementFilters` — `labelGroup.style.display`
теперь учитывает и это. Чтобы видимая ФИГУРА (цвет статуса) совпадала с
тем, чья подпись показана — `renderElements` теперь сортирует элементы
по отметке (по возрастанию) перед отрисовкой фигур, так верхний ярус
физически рисуется последним = поверх остальных в SVG. Правило только
для 2D — в 3D ярусы разнесены по высоте и видны одновременно без
взаимного перекрытия, там подписи не трогал (сработала бы только помеха).

**2. Марки Крана/Стоянки в 3D.** Новая `build3DZoneLabelSprite(zone,
baseY)` — тот же приём canvas-текстуры/спрайта, что и постоянная марка
элемента (`build3DLabelSprite`), но без допстроки, один раз на зону, в
основании её объёма (для Крана — `heightRange.bottom`, для Стоянки —
собственная `elevation_mm`). Мировой размер — ФИКСИРОВАННЫЙ
(`ZONE_LABEL_3D_WORLD_HEIGHT=2000`мм), не пропорциональный площади зоны
(в отличие от `label3DWorldHeight` у элементов) — зоны разного порядка
величины (пара метров у стоянки, сотня метров у крана), пропорциональный
размер был бы то точкой, то стеной текста. Захватка — без подписи (не
запрошено). Новая карта `state.view3d.zoneLabelSpriteById`, видимость
следует `zoneMeshVisible(zone)` — та же, что и у самого объёма (правки
в `build3DScene`/`apply3DZoneVisibility`/цикл dispose).

**3. Отметка яруса в имени стоянки + защита от наложения в 2D.** Новая
`zoneDisplayName(zone)` — для категории "Стоянка" добавляет `+elevation_mm`
к имени (`"Стоянка 1 +25800"`) — ТОЛЬКО для ОТОБРАЖЕНИЯ (2D-подпись на
плане, новый 3D-спрайт из п.2); `zone.name` САМ не трогается — это поле
всё ещё участвует в `stanceLogicalKey` (группировка одной стоянки по
всем ярусам для фильтра, см. два предыдущих раунда), добавление отметки
туда сломало бы эту группировку обратно на дубли. Отдельно — новая
`updateZoneLabelCollisionVisibility()`, тот же bucket-приём, что и у
`updateLabelCollisionVisibility` для марок элементов, применяется к
подписям ЗОН (`state.zoneLabelEls`, теперь хранит ещё `cx`/`cy`/готовый
текст) — гасит взаимное наложение подписей разных ярусов одной стоянки
на плоском 2D-плане, вызывается из `updateSizesForZoom` на каждый зум,
как и её аналог для марок.

**Проверено**: `node --check app/static/app.js` — синтаксис ок. Живая
браузерная проверка НЕ сделана в этом раунде (сменился пароль admin,
см. выше) — стоит сделать в следующей сессии, когда будут актуальные
учётные данные.

**Файлы**: `app/static/app.js` (`topVisibleLabelIds`, `zoneDisplayName`,
`updateZoneLabelCollisionVisibility`, `build3DZoneLabelSprite`,
`ZONE_LABEL_3D_WORLD_HEIGHT` — новые; `renderElements`,
`applyPlacementFilters`, `renderZones`, `updateSizesForZoom`,
`build3DScene`, `apply3DZoneVisibility`, `state.view3d.zoneLabelSpriteById`
— изменены).

## Favicon, подложка границ проекта в 3D; расследование пропавшего индикатора зума (2026-07-23)

**Favicon** — на вкладке браузера не было иконки вовсе (не было ни
одного `<link rel="icon">`, только `/favicon.ico` — 404, см. сетевые
логи прошлых раундов). Пользователь прислал логотип МСУ-1 (круглый,
с "печатной платой", белый фон) — сохранил как 3 PNG
(`favicon-32/192/512.png`, `app/static/`), белый фон сделан прозрачным
(PIL, порог >245 по каждому каналу — не задевает сам логотип, он весь
серый/красный). Добавлены `<link rel="icon">` (3 размера) +
`apple-touch-icon` в `<head>` `index.html`. `img-src 'self' data:` в
CSP уже разрешает — правок в `app/main.py` не потребовалось. Проверено:
`fetch('/static/favicon-32.png')` → 200, `image/png`.

**Подложка границ всего проекта в 3D** — новая `build3DSiteBaseMesh()`:
прямоугольник по охвату ВСЕХ точек контуров `state.elements` целиком
(не то, что сейчас проходит фильтры или включено в "Отображение зон") —
едва заметная (`opacity: 0.05`, серая), чуть ниже нулевой отметки
(`Y=-5`, тот же приём против z-fighting, что и у выступов ободка
элементов). Добавляется в сцену БЕЗУСЛОВНО в `build3DScene()`, не
зависит ни от каких тумблеров — заказчик подтвердил именно это:
ориентир должен оставаться, даже если фильтрами отключено вообще всё
остальное.

**Расследование "пропал индикатор зума/кнопка сброса"** — пользователь
сообщил, что `#zoom-indicator` (правый нижний угол рабочей области) и
кнопка "⟲" исчезли. Прямое сравнение с `git show HEAD` — разметка
(`#zoom-indicator`/`#zoom-value`/`#zoom-reset`), CSS-позиционирование
(`position:absolute; right:16px; bottom:16px` на `#stage`,
`position:relative`) и JS (`updateZoomIndicator`,
`document.getElementById("zoom-reset")`) — БАЙТ-В-БАЙТ идентичны
рабочей версии, дублей id нет, ничего перекрывающего с более высоким
z-index не найдено. Живьём проверить не смог (см. запись про
переход на app.js и смену пароля admin выше). Вероятная причина —
браузер пользователя показывает УСТАРЕВШУЮ закэшированную копию
`app.js`/`index.html` (тот же класс проблемы, что уже ловил в этой
сессии на СВОЁМ Browser pane — обычная навигация не всегда подхватывала
свежий файл, помогал только принудительный сброс кэша); `StaticFiles`
не выставляет `Cache-Control`, полагается на дефолтные эвристики
браузера. Код-фикса не делал — нечего чинить в файле, который уже
идентичен рабочей версии. Стоит попросить пользователя сделать жёсткую
перезагрузку (Cmd+Shift+R) и, если не поможет — тогда разбираться
дальше по факту (понадобятся актуальные учётные данные, см. выше).

**Файлы**: `app/static/favicon-{32,192,512}.png` новые; `app/static/
index.html` (`<link rel="icon">` в `<head>`); `app/static/app.js`
(`build3DSiteBaseMesh` новая, `state.view3d.siteBaseMesh`,
`build3DScene`).

## Коррекция логики ярусов: Ригель/Плита перекрытия относятся к ярусу ниже (2026-07-23)

**Причина**: пользователь заметил смысловую ошибку — плита перекрытия
физически лежит на колоннах НИЖНЕГО яруса (венчает его сверху), а не
принадлежит тому ярусу, чья отметка записана у неё в имени слоя; колонна
при этом относится к своему собственному ярусу без изменений. Это должно
влиять на привязку к стоянке крана (плита на верхней отметке должна
попадать в ту же стоянку, что и колонны яруса, на котором она физически
стоит) и, через неё, на фильтр Кран/Стоянка на схеме (отдельного фильтра
«ярус» в UI нет).

Перед правкой уточнил у пользователя (`AskUserQuestion`, 4 вопроса) —
проверил гипотезу на реальных данных (`data/zhbi.db`) заранее, это
изменило объём задачи:
- **Тип элементов**: ригель на реальных данных стоит ТОЧНО на отметках
  колонн (15800/25800/34700 — идентично колоннам следующего яруса) —
  та же физическая ситуация, что и у плиты перекрытия. Пользователь
  подтвердил: правка касается ОБОИХ типов (Ригель + Плита перекрытия),
  не только плиты, как было сформулировано изначально.
- **Правило снэпа**: плита перекрытия первого яруса — 15000 мм, НЕ
  15800 (толщина плиты) — точного совпадения с отметкой яруса колонн
  нет. Правило — «ближайший ярус колонн СТРОГО НИЖЕ отметки элемента»
  (строгое `<`, не `<=`), единое что для точного совпадения (ригель),
  что для смещённого (плита).
- **Оба механизма привязки к стоянке**: в коде их два — «лесенка»
  сужения (файлы с ОДНИМ физическим ярусом стоянок, `_tier_index`) и
  прямой снэп на реально нарисованный ярус (файлы с полигонами стоянок
  на каждом ярусе колонн — текущий рабочий файл 260722,
  `_candidates_for_category`). Поправка применяется к ОБОИМ.
- **3D-высоты не трогать**: отдельная логика `computeColumnLevels`/
  `computeTopColumnCeiling` (app.js) — про то, до какой отметки рисовать
  3D-меш колонны/потолка, не про привязку к зоне. Оставлена как есть.

**Реализация**: `scripts/zone_binding.py` — новая константа
`TIER_CAPPING_TYPES = {"Плита перекрытия", "Ригель"}`; `_tier_index()` и
`_candidates_for_category()` получили параметр `strict_below` (`<` вместо
`<=`); `bind_element_to_zones()` сам решает `strict_below` по
`element_type` и прокидывает в оба механизма. Единственный вызывающий
(`new_standard_pipeline.process`) ничего не меняет — правка полностью
внутри `zone_binding.py`.

**Проверено**: юнит-проверка `_tier_index` на реальных ярусах
(0/15800/25800/34700) — колонна остаётся на своём ярусе на всех 4
отметках, ригель/плита корректно уходят на ярус ниже (кровельная плита
на +47000, выше верхнего яруса колонн, остаётся на верхнем ярусе — уходить
некуда). Сервер (`uvicorn --reload`) сам переимпортировал `Input/` с
новым кодом при рестарте — сверил `elements.zone_stance_id` → `zones.
elevation_mm` по факту в БД для файла 260722: колонна 0/15800/25800/34700
→ стоянка той же отметки (не изменилось); плита перекрытия
15000/25800/34700/47000 → стоянка 0/15800/25800/34700 (сдвиг на ярус
вниз, кроме кровли); ригель 5000/15800/25800/34700 → стоянка
0/0/15800/25800 (сдвиг на ярус вниз). Доля `matched` не просела (колонна
100%, плита 100%, ригель 2002/2004 — 2 `needs_review`, похоже на
геометрическую погрешность, не связанную с этой правкой). Живой браузер
не проверял — нет актуальных учётных данных для входа в этой сессии (см.
предыдущую запись); UI не менялся, только серверная логика привязки,
которая уже видна в БД.

**Файлы**: `scripts/zone_binding.py` (`TIER_CAPPING_TYPES`, `_tier_index`,
`_candidates_for_category`, `bind_element_to_zones`); `Docs/TZ.md` §3.7.

## Новый входящий файл 260723 — новый ярус +39200, проверка коррекции ярусов на других отметках (2026-07-24)

**Что пришло**: `Input/260723_Чертежи для WEB.dxf` заменил 260722.
Структура ярусов колонн другая (не просто сдвиг): `Колонна` теперь на
0 / 8050 / 15000 / 25800 (было 0/15800/25800/34700 — старую пару
"нижняя/верхняя" на каждом полуярусе заказчик разбил на 4 подтипа);
`Ригель`/`Плита перекрытия` — 5200 или 15000 / 25800 / 34700 / **39200
(новый, был 47000)**. Реально нарисованные зоны "Стоянка" — только на
0/15000/25800/34700 (на 39200 стоянки нет — по уже согласованному
правилу элемент снэпается на ближайший ниже реально нарисованный ярус).

**Блокер импорта**: `allowed_subtypes` (справочник, "Настройки →
Справочник подтипов", `app/schema.sql` только сеет дефолт для НОВОЙ БД —
для этой рабочей уже наполнен через UI в прошлых раундах) не содержал
новых значений — `LayerNameError` останавливал импорт файла целиком.
Добавлено в БД (`INSERT OR IGNORE`, тот же запрос, что у
`POST /allowed-subtypes`): `Ригель` → `на отм. +15.000` (в этом файле
первый ярус ригеля назван по отметке ПЛИТЫ, не колонны, как раньше — то
есть даже подтип-строка "на отм. +15.800" из старого файла тут не при
чём, это third дословно другое значение) и `на отм. +39.200`;
`Плита перекрытия` → `на отм. +39.200`. Старые значения (включая
`+47.000`, `+15.800`) не удалялись — они остаются валидны для
переимпорта более старых файлов.

**Импорт**: `app.dxf_import.process_upload()` напрямую (та же функция,
что вызывает `/import-dxf` и старт сервера для `Input/`) — не через HTTP
(нет учётных данных в этой сессии для входа в UI). 9422 элемента, 0
ошибок, `needs_review` по стоянке — 2 (Ригель "3Рк1" на +15000, похоже
на геометрическую близость двух смежных стоянок — тот же класс мелких
случаев, что и на 260722, не новая проблема).

**Проверка коррекции ярусов** (см. запись выше про сам фикс) на НОВОЙ,
другой структуре отметок — сверил `elements.zone_stance_id` →
`zones.elevation_mm`: Колонна 0→0, 8050→0 (ближайшая стоянка снизу — на
8050 своей стоянки не нарисовано, это ожидаемо), 15000→15000, 25800→25800
(колонна всегда на своей отметке, без изменений). Ригель/Плита
перекрытия — каждая отметка снэпается на стоянку РОВНО НА ЯРУС НИЖЕ:
5200→0, 15000→0, 25800→15000, 34700→25800, 39200→34700. Подтверждает, что
фикс (`TIER_CAPPING_TYPES`, `strict_below`) корректно обобщается на
структуру ярусов, отличную от той, на которой его проверяли изначально —
никаких зашитых в код чисел не было, всё вычисляется из реальных отметок
файла.

**Файлы**: БД (`allowed_subtypes`, `elements`, `zones` — только данные,
код не менялся в этом раунде); `Docs/TZ.md` §3.7 (актуализирован список
известных подтипов).

## 3D-кайма на торцах ригелей/плит — две независимые причины (живой репорт пользователя, 2026-07-24)

Пользователь прислал скриншоты 3D-схемы: на торцах ригелей и плит
перекрытия — тёмные "ступенчатые"/клиновидные артефакты, выглядящие как
некорректная геометрия. Разобрал БЕЗ живого браузера (логина в сессии не
было — пользователь один раз прислал пароль в чат, использовать отказался,
это жёсткое правило, не ситуативное; вместо этого попросил марку элемента
и считал геометрию руками из реальных данных БД, как и в проверке коррекции
ярусов выше). Причины оказались НЕЗАВИСИМЫМИ, обе в приёме "инвертированной
оболочки" (`build3DOutlineMesh`/`build3DMeshForElement`, `app/static/app.js`):

**Причина 1 — наложение каймы РАЗНЫХ элементов по высоте.** Кайма
(`build3DOutlineMesh`) раздувала силуэт на `OUTLINE_HULL_MARGIN_MM=35`
мм по ВСЕМ трём осям одновременно, включая вертикаль. У ЖБИ элементы
стыкуются друг с другом БЕЗ зазора по конструкции (ригель садится на
колонну ровно на её отметке — подтверждено на реальных данных: колонна
"2КС4.9" на отм. +8050 имеет высоту до следующего яруса +15000, ригель
"2Р20.2" начинается ровно на +15000) — оба независимо раздутых на 35 мм
силуэта пересекались в зоне стыка на 70 мм (35+35), и поскольку оба
рисуются только с обратной стороны (`side: THREE.BackSide`, приём
"invisible outline" — не прозрачность, а обычный непрозрачный меш,
видимый только изнутри своего же раздутого силуэта), в зоне взаимного
пересечения ДВУХ РАЗНЫХ силуэтов ни один нормальный непрозрачный меш их
не перекрывал — отсюда ступенчатый рисунок (z-fighting/само-пересечение
геометрии). Фикс: развёл плановый запас (остался 35 мм, нужен для каймы
на боковых гранях, подтверждён живой проверкой на впритык стоящих
плитах) и вертикальный (`OUTLINE_HULL_VERTICAL_MARGIN_MM=5`, новая,
отдельная константа) — вертикальный запас каймы теперь СТРОГО МЕНЬШЕ
запаса цветного меша (`OUTLINE_VERTICAL_MARGIN_MM=15`), поэтому там, где
кайма соседа залезает в объём другого элемента, его СОБСТВЕННЫЙ
непрозрачный цветной меш всегда перекрывает эту кайму по глубине (обычный
z-buffer, "что скрыто фигурой — не видно", а не мерцание) — пользователь
явно подтвердил именно такое требование. Взаимное наложение на стыке
сократилось с 70 до 10 мм (5+5).

**Причина 2 — офсет контура ломался на вытянутых элементах
(отдельная, более серьёзная проблема).** `offsetOutlineMM` двигал каждую
точку контура НАРУЖУ от центроида на фиксированное расстояние —
приближение, ЗАВЕДОМО помеченное в комментарии как не работающее для
всех форм ("для типичных прямоугольных/выпуклых... не требуя отдельной
геометрической библиотеки"). Ригель "2Р20.2" (id 37926) — контур
600×10510 мм (вытянутый, соотношение сторон ~17.5:1) С ЛИШНИМИ
коллинеарными точками-серединами на длинных сторонах (реальная
особенность DXF-полилиний в этих файлах). Точка-середина смещает
формулу центроида: посчитал руками — центроид сместился на ~750 мм от
геометрического центра (при длине контура 10510 мм) чисто из-за лишних
точек. Офсет от так смещённого центроида толкает угол почти вдоль ДЛИНЫ
(а не по диагонали, как должно быть для прямого угла) — угол каймы
получается перекошенным клином, ровно как на скриншоте пользователя.
Фикс — заменил приближение на настоящий постраничный (per-edge)
полигон-offset: у каждого ребра своя внешняя нормаль (знак — по площади
контура, работает для обоих направлений обхода), ребро сдвигается вдоль
неё на marginMM, новая вершина — пересечение сдвинутых прямых соседних
рёбер (стандартный mitered offset); коллинеарные соседние рёбра (тот же
случай лишних точек-середин) — параллельные прямые не пересекаются,
запасной путь — плоский сдвиг без пересечения. Не зависит от центроида
вовсе, устойчив к лишним точкам любой формы.

**Проверено**: `node --check` — синтаксис ок. Ручной пересчёт офсета
для контура ригеля "2Р20.2" (35 мм) — раньше давал перекошенный
результат (ширина по одной стороне ~+2 мм вместо +35, по другой ~+68 —
асимметрично), после фикса — идеальный прямоугольник +35 мм РОВНО со
всех сторон (670×10580 вместо 600×10510), середины длинных сторон
остались точно на центральной оси. Дополнительно прогнал новую функцию
на ВСЕХ 36885 реальных контурах обоих файлов (`data/zhbi.db`) — 0
исключений/NaN, максимальный неожиданный рост диагонали силуэта ~192 мм
(самый острый угол в данных), ничего похожего на самопересечение.
Живой браузерной проверки НЕ было (см. выше про пароль) — пользователь
проверит сам, попрошу подтверждения в следующий раз, когда будет
возможность зайти вместе.

**Файлы**: `app/static/app.js` (`OUTLINE_HULL_VERTICAL_MARGIN_MM` новая
константа; `build3DOutlineMesh`, `build3DMeshForElement` — используют
её вместо переиспользования планового `OUTLINE_HULL_MARGIN_MM` по
вертикали; `offsetOutlineMM` — полностью переписана на постраничный
offset вместо приближения от центроида).

## Фильтр "Тип элемента/Подтип/Марка" — снятие типа скрывало ЧУЖОЙ тип с тем же текстом подтипа (живой репорт пользователя, 2026-07-24)

Пользователь сообщил: снятие галочки с группы "Ригель" в фильтрах
дополнительно скрывает "Плита перекрытия". Причина — `subtypeFilterValue`
(`app/static/app.js`) возвращал ГОЛЫЙ текст подтипа (`element.subtype`)
без указания типа элемента, а `state.placementFilters.subtype` —
ЕДИНЫЙ глобальный набор исключённых значений по этому голому тексту. В
справочнике allowed_subtypes текст подтипа НАМЕРЕННО переиспользуется
между типами — "на отм. +15.000"/"+25.800"/"+34.700"/"+39.200"
одновременно и у Ригеля, и у Плиты перекрытия (см. запись про новый файл
260723 выше). Клик по родительскому чекбоксу "Ригель" каскадом добавлял
ВСЕ его подтипы (включая эти общие строки) в общий исключённый набор —
а поскольку сам набор не различает, чей это подтип, элементы "Плита
перекрытия" с ТЕМ ЖЕ ТЕКСТОМ подтипа переставали проходить фильтр,
хотя их родительский чекбокс "Плита перекрытия" оставался включён.

Похожий код для диалога "Партия" (`buildBatchDialogMaps`, тот же файл,
чуть ниже) уже решал ровно эту задачу ПРАВИЛЬНО — составным ключом
`` `${type} ${subtype}` ``; фильтр в сайдбаре был единственным местом,
где использовался голый текст.

Фикс — тот же приём составного ключа, что уже применяется для стоянок
(`stanceLogicalKey`, кран+имя): новый `subtypeLogicalKey(elementType,
subtype)` (разделитель `::`), `subtypeFilterValue` возвращает его вместо
голого текста; `subtypeTextForLogicalKey`/`subtypeElementTypeForLogicalKey`
резолвят обратно текст/тип для отображения и для группировки подтипов по
типу в дереве фильтра (заменили отдельный скан `typesForSubtypeValue` по
`state.elements` — больше не нужен, тип уже зашит в ключ). Побочный
эффект: `marksBySubtype` (группировка марок под подтипом) тоже больше не
может перепутать марки разных типов с одинаковым текстом подтипа — та же
причина, раньше явно отмечена в комментарии как "принятый компромисс".

**Проверено**: `node --check` — синтаксис ок; никакой персистентности
фильтров в localStorage нет (сбрасываются при перезагрузке), миграция
сохранённого состояния не нужна. Живой проверки в браузере не было (см.
выше).

**Файлы**: `app/static/app.js` (`subtypeLogicalKey`,
`subtypeTextForLogicalKey`, `subtypeElementTypeForLogicalKey` новые;
`subtypeFilterValue` переписана; `typesForSubtypeValue` удалена как
более не нужная; `renderPlacementFilters` — построение `subtypesByType`
упрощено).

## 3D — цветной меш утапливался на треть метра от торца вытянутых элементов (третья причина, живой репорт пользователя, 2026-07-24)

Пользователь настойчиво указал на "двойной контур" — "как будто каждая
фигура лежит в полупрозрачном параллелепипеде чуть большего размера".
Первое объяснение (наложение нескольких этажей ригелей одновременно,
т.к. отметка не была отфильтрована) пользователь опроверг: показал два
элемента с ФИЛЬТРОМ ПО МАРКЕ и ОДНОЙ отметке — они действительно два
разных физических ригеля (марка переиспользуется в 31 точке здания,
объяснил в предыдущем сообщении), но КАЖДЫЙ из них сам по себе выглядел
как коробка внутри коробки большего размера — то есть это третья,
независимая от первых двух (наложение по высоте между разными
элементами; ломаный офсет каймы на вытянутых контурах) причина, и на
этот раз — в ЦВЕТНОМ (статусном) меше, не в кайме.

`build3DMeshForElement` строил цветной меш через `scaledOutline(outline,
OUTLINE_INNER_SCALE=0.94)` — тот же приём "сдвиг от центроида", только
МУЛЬТИПЛИКАТИВНЫЙ (% от расстояния до центроида), и та же ошибка
приближения, что уже чинили в `offsetOutlineMM` (см. запись выше): для
вытянутого контура (ригель 600×10510 мм) 6% от центроида — это ~18 мм на
короткой стороне, но ~270–360 мм на длинной. Проверил на реальных
данных (марка "2Р20.2", id 37926): цветной меш утапливался на 270 и 360
мм от истинных торцов (на 15–20 мм — от истинных боковых граней). Кайма
(`build3DOutlineMesh`, уже исправленная на постраничный offset в
предыдущем раунде) торчала правильные 35 мм за истинный торец — а
цветной меш в это же время недотягивал до истинного торца на треть
метра. Разница (270+35=305 мм и 360+35=395 мм) — это и есть тот самый
"второй, больший короб": тёмная кайма без цветной заливки поверх,
видимая как отдельная надстройка вокруг короткого цветного меша.

Фикс — заменил процентный `scaledOutline` на фиксированный отступ в мм
(`COLORED_MESH_INSET_MM=15`, то же постраничное смещение по нормалям,
что и у `offsetOutlineMM`, просто с отрицательным знаком — внутрь
вместо наружу). `scaledOutline`/`OUTLINE_INNER_SCALE` удалены как более
не нужные (единственный вызов).

**Проверено**: `node --check` — синтаксис ок. Ручной пересчёт для
контура ригеля "2Р20.2" — раньше 270/360 мм от торцов и 15/21 мм от
боков (асимметрично), после фикса — ровно 15 мм со всех сторон.
Дополнительно прогнал `offsetOutlineMM(outline, -15)` на всех 36885
реальных контурах обоих файлов — 0 исключений/NaN, 0 случаев схлопывания
площади (`newArea <= 0` или `newArea > origArea`, что означало бы
самопересечение/выворот контура), в датасете нет элементов тоньше 40 мм
(отступ 15 мм с каждой стороны безопасен для всех реальных сечений).
Живой браузерной проверки по-прежнему не было (см. выше про пароль) —
пользователь проверит сам после этой правки.

**Файлы**: `app/static/app.js` (`COLORED_MESH_INSET_MM` новая константа
вместо `OUTLINE_INNER_SCALE`; `scaledOutline` удалена; `build3DMeshForElement`
использует `offsetOutlineMM(outline, -COLORED_MESH_INSET_MM)`).

## 3D — приём с раздутым/утопленным контуром убран целиком по требованию заказчика (2026-07-24)

После трёх раундов независимых багов в одном и том же приёме
"инвертированной оболочки" (наложение по высоте у стыкующихся элементов;
сломанный офсет каймы на вытянутых контурах; сломанный отступ цветного
меша на вытянутых контурах — все три записи выше, в течение одного дня)
заказчик прямо указал на четвёртый случай — короткая марка (3Рк2, id
38055, контур 600×830 мм) при сильном зуме (2153%) показывала явную
"коробку в коробке": цветной меш (утоплен на 15 мм) и кайма (выступает
на 35 мм в плане, 5 мм по высоте) в сумме дают ~50 мм зазор на каждую
сторону — на элементе такого размера это ~8% от габарита, при сильном
зуме читается не как тонкая окантовка, а как отдельный второй короб.
Предложил заказчику выбор (уменьшить отступы / убрать приём целиком /
дать живой доступ) — выбрал **убрать целиком**, с точной формулировкой:
"Вместо неё всегда все рёбра у всех 3D объектов должны быть в том же
цвете что и грани, но в три раза темнее чем самая тёмная грань. Сам 3D
объект должен точно соответствовать своей геометрии и координатам из
входящих данных".

**Реализация**: `build3DMeshForElement` строит `THREE.Shape` НАПРЯМУЮ из
`element.outline`, без каких-либо офсетов ни в плане, ни по высоте;
экструзия `depth: height` (не `height + 2×запас`); позиция меша —
буквально `element.elevation_mm` (не `elevation_mm - запас`). Рёбра —
`THREE.EdgesGeometry(geometry)` (строится из ТОЙ ЖЕ геометрии, что и
цветной меш — сама отбрасывает швы между компланарными треугольниками,
например диагонали триангуляции крышки контура, оставляя только
настоящие рёбра силуэта) + `THREE.LineSegments` с
`LineBasicMaterial({color: darkerColor(colorFor(status), 1/3)})` —
дочерний объект основного меша, как и раньше нёс `userData.elementId`
для клика. Полностью удалены: `build3DOutlineMesh`, `offsetOutlineMM`
(постраничный полигон-offset — оказался никому больше не нужен, был
только для каймы/отступа), `scaledOutline` (уже удалена ранее),
константы `OUTLINE_HULL_MARGIN_MM`, `OUTLINE_HULL_VERTICAL_MARGIN_MM`,
`OUTLINE_VERTICAL_MARGIN_MM`, `COLORED_MESH_INSET_MM`, `OUTLINE_INNER_SCALE`
(все — из сегодняшних же правок, прожили меньше суток). Известное,
ПРИНЯТОЕ заказчиком ограничение: `LineBasicMaterial.linewidth` не
работает в большинстве браузеров (WebGL1/ANGLE клэмпит к 1px) — рёбра
тонкие, не "жирные", как была тёмная кайма раньше.

**Открытый риск (не фикшу, заказчик проинформирован не был явно про этот
конкретный пункт, стоит держать в уме)**: без отступа цветного меша
элементы, стыкующиеся ВПРИТЫК без реального зазора (например, плиты
перекрытия одного яруса, ранее защищённые `OUTLINE_INNER_SCALE`/
`COLORED_MESH_INSET_MM`), теперь имеют ТОЧНО совпадающие грани на
границе — потенциальный z-fighting при взгляде почти вдоль этой границы.
Заказчик явно потребовал геометрию 1:1 с исходными данными — это
осознанный компромисс с его стороны, не проверялся живым браузером.
Если всплывёт мерцание на стыках — решать отдельно, не отступом меша (это
то, от чего только что ушли), а, например, `polygonOffset`
материала или явным малым `renderOrder`.

**Проверено**: `node --check` — синтаксис ок. Больше НЕТ офсетной
математики контура в 3D вовсе — соответственно, все три класса багов
предыдущих раундов (наложение по высоте, перекошенный офсет каймы,
непропорциональный отступ цветного меша) устранены структурно, не
подбором констант. Живой браузерной проверки не было (см. историю
выше про пароль) — критично важно, чтобы пользователь проверил именно
эту правку вживую, поскольку это самая крупная структурная правка 3D
за сессию.

**Файлы**: `app/static/app.js` (`build3DOutlineMesh`, `offsetOutlineMM`
удалены; `EDGE_DARKEN_FACTOR` новая константа; `darkerColor` — дефолтный
factor теперь `EDGE_DARKEN_FACTOR` вместо 0.55; `build3DMeshForElement`
переписана — прямая геометрия + `EdgesGeometry`/`LineSegments`;
`update3DElementAppearance`, `build3DScene` — `mesh.userData.outline` →
`mesh.userData.edges`).

## 3D — толстые рёбра силуэта: вендоринг Line2/LineMaterial (2026-07-24)

Сразу после предыдущей правки (обычные `THREE.LineSegments`, 1px)
пользователь сообщил: линию не видно вообще. Ожидаемо — известное
ограничение WebGL1/ANGLE (`LineBasicMaterial.linewidth` игнорируется
браузером, см. историю в Docs/TZ.md §6.7). Настоящая толщина линии в
Three.js требует `Line2`/`LineMaterial`/`LineSegmentsGeometry`
(`examples/jsm/lines/` — та же вендоренная библиотека Three.js 0.160.0,
просто ещё не подключённый модуль) — по стоячему правилу проекта любой
НОВЫЙ вендоренный файл требует отдельного явного подтверждения
пользователя, даже если это часть уже одобренной библиотеки. Спросил —
пользователь подтвердил явно.

**Реализация**: скачал `LineSegmentsGeometry.js`, `LineMaterial.js`,
`LineSegments2.js` с unpkg (`three@0.160.0/examples/jsm/lines/`) —
версия СОВПАДАЕТ с уже вендоренным `three.module.min.js`, чтобы не
рисковать несовместимостью API между минорными релизами. Проверил
импорты внутри каждого файла: бэйр-спецификатор `'three'` (уже покрыт
существующей import map, изменений в ней и в CSP-хэше НЕ потребовалось —
только относительные импорты `../lines/*.js` между новыми файлами, для
них import map не нужна) + друг на друга (`LineSegments2` →
`LineSegmentsGeometry`+`LineMaterial`). Файлы легли в
`app/static/vendor/three/examples/jsm/lines/` (новая директория).

`ensureThreeLoaded()` — три новых динамических импорта, тем же приёмом,
что уже был для `OrbitControls`. `build3DMeshForElement` — та же
`THREE.EdgesGeometry` (нужна только чтобы получить позиции рёбер силуэта
из геометрии элемента, сразу диспозится), но вместо
`THREE.LineSegments`+`LineBasicMaterial` — `LineSegmentsGeometry.
setPositions(edgesGeometry.attributes.position.array)` +
`LineMaterial({color, linewidth: EDGE_LINE_WIDTH_PX=2, resolution})` +
`LineSegments2`. `linewidth` у `LineMaterial` по умолчанию
`worldUnits:false` — толщина в ПИКСЕЛЯХ ЭКРАНА (не в мм мира, не растёт/
не уменьшается при зуме камеры) — именно поэтому нужен uniform
`resolution` (реальный размер вьюпорта в CSS-пикселях, `renderer.
getSize()`), который обязательно пересчитывать при ресайзе окна
(`on3DResize()` теперь ещё и обходит все `mesh.userData.edges.material.
resolution.set(w,h)` — общего материала на все рёбра нет, у каждого
элемента свой из-за собственного цвета статуса).

Raycasting по толстым рёбрам работает "из коробки" — `LineSegments2.
raycast()` требует `raycaster.camera` быть установленным;
`THREE.Raycaster.setFromCamera()` в вендоренной версии его действительно
устанавливает (`this.camera=e`, проверено в исходнике минифицированного
`three.module.min.js`) — специально для поддержки Line2/LineSegments2,
добавлено в API раньше версии 0.160.0.

**Проверено**: `node --check` — синтаксис ок. Прочитал исходники всех 3
новых файлов — импорты и API (`setPositions`, `get/set color`, `raycast`)
соответствуют ожиданиям, `dispose()` не переопределён явно ни в одном из
них, но наследуется от базовых `Material`/`BufferGeometry` (штатное
поведение Three.js, не требует явной реализации в каждом подклассе).
Живой браузерной проверки не было (нет логина в этой сессии) — критично
важно, чтобы пользователь проверил визуально: толщину линии, что она не
меняется с зумом, и что клик по ребру по-прежнему выделяет элемент.

**Файлы**: `app/static/vendor/three/examples/jsm/lines/{LineSegmentsGeometry,
LineMaterial,LineSegments2}.js` новые (вендоринг, подтверждён
пользователем); `app/static/app.js` (`ensureThreeLoaded` — 3 новых
импорта; `EDGE_LINE_WIDTH_PX` новая константа; `build3DMeshForElement` —
`LineSegments2`/`LineMaterial`/`LineSegmentsGeometry` вместо
`THREE.LineSegments`/`LineBasicMaterial`; `on3DResize` — обновляет
`resolution` у материалов рёбер).

## Толстые рёбра всё ещё "не видно" на живом скрине — причина оказалась в кэше браузера, заодно починил корень (2026-07-24)

Пользователь прислал новый живой скриншот (обычный Chrome, не мой
Browser pane) — рёбра колонн по-прежнему не видны, грани сливаются,
попросил "сделать линию ребра жирнее". Проверил код построчно (без
живого доступа — логина по-прежнему нет): `LineSegments2`/`LineMaterial`/
`LineSegmentsGeometry` уже вендорены (`app/static/vendor/three/examples/
jsm/lines/*.js`, файлы реально на диске), `build3DMeshForElement` уже
использует их с `EDGE_LINE_WIDTH_PX=2`, `on3DResize` уже пересчитывает
`resolution` у каждого материала ребра — весь этот раунд был сделан
раньше в этой же сессии (см. предыдущую запись) и до сих пор не
подтверждён живым браузером. Порядок инициализации (`init3DScene()` —
`renderer.setSize(...)` — вызывается ДО первого `build3DScene()`) тоже в
порядке — `resolution` не может быть (0,0) на первом построении сцены.

Раз код на диске корректен, а живой скриншот всё равно показывает старое
поведение (тонкие/невидимые рёбра) — почти наверняка снова устаревшая
закэшированная копия `app.js` в браузере пользователя (этот класс
проблем уже несколько раз путал живую проверку в этой сессии, см. записи
выше). Раз это продолжает возвращаться и стоить кругов согласования —
почитал причину: `StaticFiles` (FastAPI/Starlette) не выставляет
`Cache-Control` вовсе, полагается на дефолтные эвристики браузера
(которые в реальности нередко решают "не перепроверять" даже при
активной разработке).

**Фикс — на уровне сервера, не на уровне "попросите пользователя
перезагрузить"**: добавлен `Cache-Control: no-cache` для всех ответов
под `/static/` в уже существующем middleware `security_headers`
(`app/main.py`). `no-cache` (НЕ `no-store`) — браузер обязан на каждый
запрос спросить сервер, не устарел ли файл (условный `GET` по
`ETag`/`Last-Modified`, которые `StaticFiles` и так уже проставляет
сама) — не полная передекачка при каждой загрузке страницы, но и не
молчаливая раздача устаревшей копии из кэша без обращения к серверу.
Устраняет корень повторяющейся путаницы, а не только этот конкретный
случай.

**Проверено**: `python3 -c "import ast; ast.parse(...)"` — синтаксис ок.
`curl -sI http://localhost:8000/static/app.js` после рестарта (`uvicorn
--reload` подхватил правку `main.py` сам) — заголовок `cache-control:
no-cache` подтверждён в реальном ответе, `etag`/`last-modified`
по-прежнему на месте (условный GET по-прежнему возможен, просто не
молчаливый). Живой проверки в браузере всё ещё не было (нет логина) —
но теперь ОБЫЧНАЯ перезагрузка (F5, не обязательно жёсткая Cmd+Shift+R)
должна забирать актуальный `app.js` — попросил пользователя проверить
именно так, без Cmd+Shift+R, чтобы заодно убедиться, что заголовок
реально решает проблему, а не подстраховаться жёсткой перезагрузкой ещё
раз.

**Файлы**: `app/main.py` (`security_headers` — добавлен `Cache-Control:
no-cache` для `/static/*`).

## Рёбра всё ещё не видно после фикса кэша — z-fighting грани с линией (2026-07-24)

Пользователь обновил страницу обычным F5 (не жёстко) — рёбра колонн
по-прежнему полностью не видны, грани сливаются. Попросил пользователя
проверить консоль браузера (F12 → Console) на ошибки — ошибок нет, JS
выполняется без исключений, все импорты (`LineSegments2`/`LineMaterial`/
`LineSegmentsGeometry`) грузятся успешно (это исключило "модуль не
загрузился" как причину).

Раз кода без ошибок и геометрия строится (см. предыдущие записи —
подробно перепроверено построчно), а линия всё равно НИКОГДА не видна —
осталась одна правдоподобная причина: z-fighting. С 2026-07-24 (см.
запись "приём с раздутым/утопленным контуром убран целиком") линия
рёбер строится из ТОЙ ЖЕ геометрии, что и грань, и лежит РОВНО на её
поверхности — глубина в буфере глубины (z-buffer) у грани и у линии в
этом месте идентична. Без явного смещения GPU-тест глубины для равных
значений (`LEQUAL` по умолчанию в Three.js) — это чистое везение по
порядку отрисовки/округлению float, и грань (закрашивает МНОГО пикселей
экрана) на практике почти всегда выигрывает у линии (закрашивает мало) —
линия просто никогда не проходит тест глубины, отсюда "рёбер не видно
вообще".

**Фикс** — стандартный приём против z-fighting контура/грани в Three.js:
`polygonOffset: true, polygonOffsetFactor: 1, polygonOffsetUnits: 1` на
МАТЕРИАЛЕ ГРАНИ (`MeshStandardMaterial` в `build3DMeshForElement`) — не
физическое смещение геометрии (заказчик явно запретил любые офсеты
контура, см. предыдущие записи), а чисто GPU-шный сдвиг "назад" В
БУФЕРЕ ГЛУБИНЫ при растеризации, так что при сравнении глубин линия
поверх гарантированно выигрывает тест глубины на самой границе.
Начальные значения factor/units=1 — стандартная отправная точка
(из собственных примеров Three.js, `webgl_materials_wireframe`); если
на реальном масштабе здания (буфер глубины растянут на большой диапазон
near/far) этого окажется недостаточно и линия останется невидимой —
следующий шаг — увеличить эти два числа, не трогая саму геометрию.

**Проверено**: `node --check` — синтаксис ок. Живой проверки после этой
конкретной правки ещё не было — критично важно, чтобы пользователь
посмотрел именно на этот момент (та же колонна "ребром", что и на
последнем скриншоте).

**Файлы**: `app/static/app.js` (`build3DMeshForElement` — материал грани
получил `polygonOffset`/`polygonOffsetFactor`/`polygonOffsetUnits`).

## polygonOffset=1/1 недостаточно — усилил вдвое двумя способами (2026-07-24)

Пользователь прислал новый скриншот без комментария — по нему нельзя
уверенно сказать, видны ли рёбра (пара тонких светлых штрихов могут быть
и остатком ребра, и обычной сглаживающей окантовкой силуэта на светлом
фоне сцены). Не стал гадать по пикселям вслепую ещё раз — сразу усилил
ОБЕ вероятные причины недостаточного эффекта прошлого фикса разом
(безопасно комбинируются, не взаимоисключающие):

1. `polygonOffsetFactor`/`polygonOffsetUnits` подняты с 1/1 до 4/4
   (`build3DMeshForElement`) — стандартная величина 1/1 (из примеров
   Three.js) рассчитана на сцены обычного масштаба, а тут модель в мм на
   весь масштаб здания (десятки-сотни тысяч единиц).
2. `logarithmicDepthBuffer: true` на `THREE.WebGLRenderer`
   (`init3DScene`) — камера должна видеть и деталь в упор, и здание
   целиком, near/far (`fit3DCameraToData`) считаются от габарита данных
   и на реальных файлах различаются на 4-5 порядков; обычный (линейный
   после перспективного деления) буфер глубины на таком разбросе имеет
   очень грубую точность вдали от камеры — это НЕЗАВИСИМАЯ от p.1
   причина того же класса проблем (z-fighting ребра с гранью), особенно
   на среднем/дальнем плане, где сделан последний скриншот. Вендоренный
   `LineMaterial` уже содержит штатные чанки `logdepthbuf_*` (общий
   Three.js механизм, был в файле с самого вендоринга) — просто раньше
   не был активирован флагом рендерера; штатные материалы Three.js
   (включая `SpriteMaterial` у подписей марок) поддерживают его так же
   "из коробки", отдельно ничего чинить не пришлось.

**Проверено**: `node --check` — синтаксис ок. Комбинация этих двух
изменений — стандартная связка для крупномасштабных (архитектурных)
Three.js-сцен именно с этим классом проблем; живой проверки по-прежнему
не было — очень важно теперь получить однозначный ответ "да, рёбра
видны" или "нет, всё ещё не видно", а не скриншот без комментария —
дальнейшее гадание по пикселям контрпродуктивно.

**Файлы**: `app/static/app.js` (`init3DScene` — `logarithmicDepthBuffer:
true` у `WebGLRenderer`; `build3DMeshForElement` — `polygonOffsetFactor`/
`polygonOffsetUnits` 1→4).

## Ребро наконец видно, но еле-еле — чёрный цвет + толще (2026-07-24)

Пользователь подтвердил первый однозначный результат в этом раунде: "есть
еле видимая полоска ребра" — значит polygonOffset/logarithmicDepthBuffer
из предыдущей записи действительно решили z-fighting (ребро наконец
проходит тест глубины и рендерится). Осталась чистая проблема
контраста/толщины, попросил явно: "все грани делаем чёрными" и решить
толщину.

**Изменения**: цвет ребра — фиксированный `EDGE_COLOR=0x000000`
(чёрный), больше НЕ зависит от статуса/цвета грани (было — темнее
статусного цвета в 3 раза, `EDGE_DARKEN_FACTOR`/`darkerColor` — убраны
как более не нужные, единственным потребителем были рёбра).
`update3DElementAppearance` (пересчёт цвета при смене статуса) больше не
трогает цвет рёбер — они не должны меняться. Толщина —
`EDGE_LINE_WIDTH_PX` 2→4 (было увеличено с 1 в позапрошлой записи этого
же раунда, но пользователь сообщил, что и 2px чёрным на полутонах серой
схемы всё ещё "еле видимо").

**Проверено**: `node --check` — синтаксис ок, `grep` подтвердил отсутствие
остаточных ссылок на удалённые `darkerColor`/`EDGE_DARKEN_FACTOR`. Живой
проверки этой конкретной правки (чёрный + 4px) ещё не было.

**Файлы**: `app/static/app.js` (`EDGE_COLOR` новая константа вместо
`EDGE_DARKEN_FACTOR`/`darkerColor` — удалены; `EDGE_LINE_WIDTH_PX` 2→4;
`build3DMeshForElement`, `update3DElementAppearance` — используют
фиксированный цвет ребра).

## Докеризация + разделение код/данные + два git-remote (GitHub/GitLab) (2026-07-24)

По запросу пользователя — готовится публикация сервиса внутри
корпоративного контура через Docker-контейнеры, вместо прежней схемы
"git clone + venv + systemd" (`Docs/backlog.md`, запись от 2026-07-23).
Требование: код и данные должны быть физически разделены, чтобы замена/
пересборка контейнера не задевала базу и загруженные чертежи. Отдельно
— нужны два удалённых git-репозитория (GitHub и корпоративный GitLab)
вместо одного гипотетического "корпоративного git-сервера", которого
раньше касалась только предыдущая запись.

Проработано через `EnterPlanMode` (см. `/Users/max/.claude/plans/
generic-jumping-rabbit.md`) с уточняющими вопросами пользователю:
GitHub — приватный, просто резервная копия (не open source); сборка
образа — на сервере из git-чекаута через `docker compose build`, файлы
`Dockerfile`+`docker-compose.yml` обязательны; GitHub — основной для
разработки, корпоративный GitLab — только для развёртывания на тестовом
и продуктовом серверах; один контейнер (не разносить на несколько
сервисов), нужно только разделить код и данные.

Ключевая находка при разведке: пути к данным в коде уже были
относительными (`Path(__file__).resolve().parent.parent / ...` —
`app/db.py` → `data/zhbi.db`, `app/main.py` → `Input/`,
`app/dxf_import.py` → `uploads/`), поэтому разделение код/данные не
потребовало ни строчки правки Python-кода — bind-mount тех же
host-папок на те же относительные пути внутри контейнера
(`/app/data`, `/app/Input`, `/app/uploads`) в `docker-compose.yml`
достаточен. Все три уже были в `.gitignore`.

**Сделано**:
- `Dockerfile` (новый) — `python:3.12-slim`, `pip install` из
  `requirements.txt` (все зависимости — ezdxf/lxml/reportlab/shapely и
  др. — есть готовыми manylinux-колёсами под linux/amd64, компилятор в
  образе не нужен), `COPY . .`, `CMD uvicorn app.main:app --host 0.0.0.0
  --port 8000`.
- `docker-compose.yml` (новый) — один сервис `zhbi`, `build: .`,
  `env_file: .env`, `ports: ["${PORT:-8000}:8000"]`, три bind-mount
  volume (`data/`, `Input/`, `uploads/`), `restart: unless-stopped`
  (заменяет прежний `zhbi.service`/systemd-юнит — не нужно
  поддерживать оба механизма автозапуска).
- `.dockerignore` (новый) — данные/`.venv`/`.git`/`test_data`/`output`
  не попадают в build context/образ; заодно `scripts/linux/`, `Docs/`,
  `CLAUDE.md` — не нужны внутри контейнера для запуска.
- `.env.example` (новый, в git) — шаблон `PORT`/`ZHBI_SECURE_COOKIES`/
  `ZHBI_TRUST_PROXY_HEADERS`/`ZHBI_ENABLE_DOCS`/`ZHBI_MAX_UPLOAD_MB`;
  сам `.env` — в `.gitignore` (машинно-специфичная конфигурация
  конкретного развёртывания, не код).
- `Docs/DEPLOYMENT_LINUX.md` — существенно переработан: установка/
  автозапуск/обновление/бэкап/диагностика — под Docker вместо venv/
  systemd; раздел 3 (git) переписан под ДВА remote вместо одного —
  `origin`=GitHub (приватный, резерв, серверы с ним не
  взаимодействуют)/`gitlab`=корпоративный (единственный источник кода
  для тестового/продуктового серверов, пуш туда — явный шаг, не при
  каждом коммите); явно оговорено, что на самих серверах remote будет
  называться `origin` (обычный результат `git clone`) и указывать на
  GitLab — не путать с именованием на Mac разработчика.
- `scripts/linux/` — удалены venv/systemd-скрипты (`install.sh`,
  `run.sh`, `zhbi.service`, `update-git-test.sh`, `update-git-prod.sh`);
  добавлены `docker-install.sh` (разовая установка Docker Engine +
  Compose plugin из официального apt-репозитория Docker, добавление
  пользователя в группу `docker`), `update-test.sh`/`update-prod.sh`
  (та же логика выбора тега для прод, что была — но `docker compose
  build && up -d` вместо `pip install`+`systemctl restart`). `backup.sh`
  не тронут — bind-mount делает `data/zhbi.db` обычным файлом хоста,
  бэкап работает независимо от того, запущен ли контейнер.
- `.gitignore` — добавлен `.env`.
- `CLAUDE.md` — обновлены разделы "Как продолжить работу с нуля"
  (схема remote), новый пункт в "Ключевые решения" (докеризация), новая
  запись в "Текущий статус", уточнение в "Что дальше".

**Не проверено**: на Mac разработчика, где готовился этот раунд, не
установлены ни Docker, ни Homebrew — попытка проверить сборку/запуск
(`docker compose build`/`up`, persistence данных через `down`+`up`)
живьём не удалась. Пользователь ставит Docker Desktop самостоятельно;
до первого реального прогона (локально или сразу на тестовом сервере)
план и файлы выверены построчно по документации Docker/Docker Compose и
семантике bind-mount, но не исполнялись. `Docs/DEPLOYMENT_LINUX.md`
содержит то же явное предупреждение отдельным разделом (6).

Настройка самих git remote (`git remote add origin/gitlab`, первый
`git push`) сознательно НЕ сделана в этой сессии — ждёт, когда
пользователь создаст оба репозитория (GitHub, корпоративный GitLab) и
даст URL/подтверждение (создание учётных сущностей — не то, что
делается за пользователя без явного запроса).

**Файлы**: новые `Dockerfile`, `docker-compose.yml`, `.dockerignore`,
`.env.example`, `scripts/linux/{docker-install,update-test,
update-prod}.sh`; удалены `scripts/linux/{install,run,
update-git-test,update-git-prod}.sh`, `scripts/linux/zhbi.service`;
правки `.gitignore`, `Docs/DEPLOYMENT_LINUX.md`, `CLAUDE.md`.

## 3D — толщина ребра 4→2px, и первый раунд быстродействия (2026-07-24)

Пользователь подтвердил, что чёрные рёбра толщиной 4px хорошо видны, но
попросил чуть тоньше (2px хватает при чёрном цвете — не хватало именно
раньше, с затемнённым статусным цветом). Заодно сообщил, что вращение и
зум в 3D стали заметно медленнее, попросил заняться быстродействием.

**Причина замедления, скорее всего:** каждый элемент (реальный файл —
9422 элемента) получал СВОЙ собственный экземпляр `LineMaterial` для
рёбер — это было оправдано, пока цвет ребра зависел от статуса
(разный на каждый элемент), но перестало быть нужным ещё в прошлой
записи (переход на фиксированный `EDGE_COLOR`) — просто не был убран
сразу. Три.js группирует/сортирует объекты при рендере по материалу
(меньше переключений состояния GPU между отрисовкой соседних объектов);
~9400 РАЗНЫХ экземпляров материала (даже с одинаковыми значениями
uniform'ов) полностью ломают эту сортировку — каждое ребро вынужденно
отдельное переключение состояния при вращении/зуме (весь кадр
перерисовывается заново). `logarithmicDepthBuffer` (включён в прошлой
записи для фикса z-fighting) тоже добавляет вычисление на каждую
вершину каждого кадра — вклад в замедление тоже вероятен, но трогать
не стал: без него рёбра снова могут перестать быть видны на среднем/
дальнем плане, а измерить эффект раздельно без живого профилирования
не могу.

**Фикс**: один общий `LineMaterial` (`state.view3d.edgeMaterial`,
создаётся один раз в `init3DScene()`) на ВСЕ элементы — `build3DMeshForElement`
использует его вместо создания нового на каждый элемент.
`on3DResize()` обновляет `resolution` только у ОДНОГО материала (была
петля по всем мешам). `build3DScene()` при очистке сцены больше не
диспозит материал ребра на каждом элементе (общий материал не
пересоздаётся между перестройками сцены, живёт всю сессию 3D-режима) —
диспозится только его собственная geometry (позиции рёбер — своя
геометрия на элемент, это не изменилось).

**Проверено**: `node --check` — синтаксис ок, `grep` подтвердил ровно
один вызов `new LineMaterial(...)` во всём файле (было — один на каждый
вызов `build3DMeshForElement`). Живой проверки влияния на FPS не
было — важно, чтобы пользователь сравнил ощущения вращения/зума до и
после этой правки; если быстродействие всё ещё неудовлетворительно —
следующий кандидат на оптимизацию (не сделано, требует более крупной
работы) — слияние геометрии рёбер/граней МНОГИХ элементов в единую
BufferGeometry (кардинально меньше отрисовок за кадр, но потребует
переделать поэлементный клик-выбор через raycasting и точечное
скрытие/показ по фильтрам, сейчас завязанные на отдельный объект на
элемент).

**Файлы**: `app/static/app.js` (`state.view3d.edgeMaterial` новое поле;
`init3DScene` создаёт общий `LineMaterial`; `build3DMeshForElement`
использует его вместо своего; `on3DResize`, `build3DScene` — обновлены
под общий материал; `EDGE_LINE_WIDTH_PX` 4→2).

## Быстродействие 3D, раунд 2 — общий материал ГРАНИ на статус + capped pixel ratio (2026-07-24)

Пользователь подтвердил: общий материал РЕБРА (прошлая запись) помог, но
"ненамного" — попросил продолжить работу над быстродействием. Разобрал
дальше: точно такая же проблема (свой материал на каждый элемент) была
и у ЦВЕТНОГО (статусного) меша — `build3DMeshForElement` создавала
персональный `MeshStandardMaterial` НА КАЖДЫЙ элемент (~9400 на реальном
файле), хотя из различий между ними только цвет статуса (фиксированный
небольшой набор значений, `state.statusOrder`) — та же поломка
сортировки рендера по материалу, что и была у рёбер, только теперь для
ГРАНЕЙ (в разы больше видимых пикселей, чем у тонких линий рёбер).

**Фикс 1 — общий материал НА СТАТУС**: `getStatusMeshMaterial(status)`
(кэш `state.view3d.materialByStatus`, Map статус→материал) — возвращает
общий для ВСЕХ элементов этого статуса материал вместо создания нового.
Подсветка ВЫБРАННОГО элемента больше не может просто менять `emissive`
у своего материала (материал теперь ОБЩИЙ на статус — перекрасило бы
разом ВСЕ элементы этого статуса) — заведён отдельный
`getHighlightMeshMaterial(status)`: один переиспользуемый материал
(`state.view3d.highlightMaterial`), перекрашиваемый под цвет статуса
конкретного выбранного элемента при каждом выборе (выбранный элемент
всегда только один — одного материала достаточно). И
`build3DScene`/`update3DElementAppearance` теперь ПЕРЕКЛЮЧАЮТ
`mesh.material` на нужный общий материал (обычный или highlight) вместо
того, чтобы красить/перекрашивать материал самого меша. Диспоз материала
элемента убран из `build3DScene()` (материалы теперь живут всю сессию,
не пересоздаются на каждую перестройку сцены — та же логика, что уже
была применена к материалу ребра). Смена цветовой схемы статусов
(Настройки → Цвета статусов) раньше просто красила материал элемента
заново на лету — с общим материалом это перекрасило бы правильно, но
только если это ТОТ ЖЕ объект; добавлено явное обновление всех
закэшированных материалов (`for (const [status, material] of
state.view3d.materialByStatus) material.color.set(colorFor(status))`)
в обработчике сохранения цветов — иначе цвета в 3D отстали бы от 2D до
следующей полной перезагрузки страницы.

**Фикс 2 — ограничен pixel ratio рендерера**: `renderer.setPixelRatio`
использовал `window.devicePixelRatio` без ограничения — на Retina/HiDPI
мониторах (обычно 2x) это вчетверо больше пикселей физического буфера
на каждый кадр, чем при 1x, а объём работы фрагментного шейдера растёт
линейно с числом пикселей. Ограничено до 1.5 — компромисс между
плавностью и чёткостью картинки, не отключено совсем.

**Проверено**: `node --check` — синтаксис ок. `grep` подтвердил ровно 2
места создания `MeshStandardMaterial` для элементов во всём файле (было
одно на каждый вызов `build3DMeshForElement`, до 9400 создание на
живом файле) — фабрика материала статуса и единственный highlight-
материал. Живой проверки влияния на FPS по-прежнему не было — важно
получить обратную связь, стало ли ощутимо лучше, или нужен более
крупный шаг (следующий кандидат, не сделано: слияние геометрии МНОГИХ
элементов в единую BufferGeometry — кардинально меньше отрисовок за
кадр, но требует переделать поэлементный клик-выбор через raycasting и
точечное скрытие/показ по фильтрам).

**Файлы**: `app/static/app.js` (`state.view3d.materialByStatus`/
`highlightMaterial` новые поля; `getStatusMeshMaterial`/
`getHighlightMeshMaterial` новые функции; `build3DMeshForElement`,
`build3DScene`, `update3DElementAppearance` — используют общие
материалы вместо создания своего на каждый элемент; обработчик
`settings-save` — обновляет закэшированные 3D-материалы при смене
цветовой схемы; `init3DScene` — `renderer.setPixelRatio` ограничен
до 1.5).

## Фильтр "Тип/Подтип/Марка" — убрана избыточная группировка по отметке у Плиты перекрытия/Ригеля; 2D-подписи — плашка вместо контура (2026-07-24)

Пользователь запросил два независимых изменения.

**1. Фильтр**: внутри типов "Плита перекрытия" и "Ригель" промежуточный
уровень "Подтип" в дереве фильтра почти всегда буквально ОТМЕТКА
("на отм. +15.000" и т.п., см. allowed_subtypes) — избыточно, раз есть
отдельный фильтр "Отметка (высота)". Убрал уровень подтипа для ЭТИХ
ДВУХ типов (у Колонны подтип — смысловое название яруса "нижняя"/
"верхняя", не голая отметка, не трогал): `subtypeFilterValue` для
`FLAT_MARK_TYPES = {"Плита перекрытия", "Ригель"}` теперь возвращает
составной ключ тип+МАРКА (не тип+подтип) — марки становятся ПРЯМЫМИ
детьми типа в дереве (Тип → Марка), без промежуточного уровня. Побочно
понадобилось: `childrenForChild` (3-й уровень, марка под подтипом)
должен возвращать `[]` для этих типов — иначе марка задваивалась бы
сама под собой третьим уровнем (подтип уже и есть марка). Фильтрация по
отметке продолжает работать независимо через `elevationFilterValue` —
никакой функциональности не потеряно, только убран задваивающий уровень
в дереве.

**2. 2D-подписи марок**: заказчик явно сравнил со стилем, уже принятым в
3D (`build3DLabelSprite` — полупрозрачная белая подложка `rgba(255,255,
255,0.72)` под тёмным текстом) — попросил тот же стиль в 2D вместо
белого контура вокруг тёмных букв (`stroke:#fff; paint-order:stroke`,
был отдельно отмечен в комментарии как "рискованно делать rect без
браузера" — теперь сделан). Добавлен `<rect class="mark-label-bg"/
mark-sublabel-bg">` ПЕРЕД каждым `<text class="mark-label"/mark-sublabel">`
в DOM (рисуется позади). Размер бокса — БЕЗ `getBBox()` (форсирует
layout — дорого при тысячах элементов на каждый тик зума, тот же класс
проблем, что и уже отмеченная O(n)-без-throttle зумовая логика) —
оценка по числу символов × 0.62×fontSize, ТА ЖЕ константа, что уже
использует `updateLabelCollisionVisibility` для коллизионных боксов —
согласовано с уже существующей оценкой, не новое приближение с нуля.

Видимость подложки должна следовать за видимостью текста ВЕЗДЕ, где она
меняется (фильтр по типу элемента, коллизии соседних подписей,
создание/пересоздание допстроки партии) — вместо правки каждого из 6
мест по отдельности заведён `labelBgByText` (`WeakMap<текст, rect>`) +
`setLabelDisplay(textEl, value)` — единая точка, все существующие
`label.style.display = ...` заменены на неё.

**Проверено**: `node --check` — синтаксис ок. Живой браузерной проверки
не было (нет логина в этой сессии) — стоит перепроверить: (а) что
подложка не съезжает при зуме/панорамировании, (б) что подложка
корректно появляется/исчезает вместе с текстом во всех сценариях
(тумблер типа, коллизии, появление/исчезание допстроки партии).

**Файлы**: `app/static/app.js` (`FLAT_MARK_TYPES` новая константа,
`subtypeFilterValue` — special-case для неё; `renderPlacementFilters` —
`childrenForChild` учитывает `FLAT_MARK_TYPES`; `updateLabelBgRect`,
`labelBgByText`, `setLabelDisplay`, `appendMarkLabel` новые; все места
создания/обновления `mark-label`/`mark-sublabel` переведены на них);
`app/static/index.html` (`.mark-label`/`.mark-sublabel` — убран
`stroke`/`paint-order`; `.mark-label-bg`/`.mark-sublabel-bg` новые
стили).

## 3D — "наклейка" марки на грани элемента вместо плавающей таблички (2026-07-24/25)

Пользователь запросил: подпись марки для Плиты перекрытия/Ригеля/Колонны
должна быть "наклейкой" прямо на грани элемента (текст вдоль элемента,
по центру длины, буквами во всю ширину), а не плавающей табличкой рядом.
Уточнил явно (см. AskUserQuestion) один неоднозначный момент — для
Колонны (вертикальный элемент, её "длина" в этом смысле высота) —
подтвердил: наклейка на 4 боковых (вертикальных) гранях, текст читается
снизу вверх, а не на верхнем/нижнем торце (маленькое сечение, буквально
как у плит/ригелей было бы непрактично).

**Геометрия**: `footprintDimensions` (ширина+длина контура — тот же
подход площадь/периметр, что и `crossSectionWidth`, но оба корня, не
только меньший), `footprintLongAxisAngle` (направление самой длинной
стороны контура — устойчиво к лишним точкам-серединам, тот же класс
проблемы, что уже решён в `offsetOutlineMM`), `polygonSides` (стороны
контура с объединением коллинеарных соседей — для колонны нужна ОДНА
наклейка на КАЖДУЮ реальную сторону, не на каждый отрезок между
точками), `footprintCentroid`. Ориентация плоскости наклейки —
`orientDecalMesh` — явный ортонормированный базис (right/up/normal)
через `THREE.Matrix4.makeBasis`, не подбор углов поворота вручную (тот
же класс ошибок, что уже ловили на зеркальности всей сцены, см. "3D —
зеркальность" выше) — численно проверено (Node, ручной cross/dot) на
всех используемых комбинациях right/normal: везде получается корректный
ортонормированный базис.

**"Помещается по длине"**: `DECAL_FIT_MARGIN=0.92` — оценка ширины
текста той же константой, что и в 2D (`DECAL_CHAR_WIDTH_RATIO=0.62`,
согласовано с `updateLabelBgRect`); если марка не помещается — `null`,
вызывающий код (`build3DScene`, `rebuild3DLabelSprite`) падает на
прежнюю плавающую табличку (`build3DLabelSprite`) — проверено на
реальном примере из предыдущего раунда (короткая марка "3Рк2", 830 мм
длина) — именно такой случай явно упомянул заказчик.

**Текстура — ОБЩАЯ на марку, не на элемент** (`decalTextureCache`, Map
марка→`THREE.CanvasTexture`) — марки часто повторяются десятками раз по
всему зданию (см. запись "31 экземпляр марки 2Рк2" выше) — отдельная
текстура на каждый экземпляр была бы тем же классом проблемы с
производительностью, что уже чинили для материалов в этом же раунде
(`getStatusMeshMaterial`). Геометрия (`PlaneGeometry`) и материал
(`MeshBasicMaterial`, свой `polygonOffset` от z-fighting с самой гранью)
— по-прежнему свои на каждую грань-наклейку (позиция/размер разные), не
шарятся — этого не избежать без гораздо большей переделки, оставлено
как есть.

**Интеграция с существующей 3D-инфраструктурой подписей**: новая Map
`state.view3d.markDecalById` (параллельно `labelSpriteById`) —
подключена везде, где раньше упоминался только спрайт:
`applyPlacementFilters`, `apply3DLabelVisibility` (видимость по фильтру/
тумблеру типа), `build3DScene` (создание — наклейка первым приоритетом,
спрайт — запасной вариант; очистка сцены — диспоз geometry/material
каждой грани-наклейки, НЕ общей текстуры). `rebuild3DLabelSprite`
(точечная пересборка спрайта при смене допстроки партии) — ранний выход
для элементов с наклейкой: она показывает только марку, от допстроки не
зависит, пересобирать нечего.

**Проверено**: `node --check` — синтаксис ок. Численно (Node, без
Three.js) — `footprintDimensions`/`footprintLongAxisAngle`/`polygonSides`/
`footprintCentroid` на реальных контурах (плита 4П-4: 1200×5300мм, угол
90°; колонна 7Кв3: 4 чистые стороны по 600мм) дают ожидаемые числа;
базис `orientDecalMesh` ортонормирован на всех используемых комбинациях.
НЕ проверено и не может быть проверено без живого браузера: сама
ЧИТАЕМОСТЬ текста (не зеркальный ли, не перевёрнутый ли) с "естественного"
ракурса на каждой из граней — знак вращения текста на нижней грани
плиты/ригеля и на 4 боковых гранях колонны выбран по последовательной
формуле (see orientDecalMesh), но какая из двух возможных ориентаций
(нормальная/зеркальная) на самом деле физически правильна — эмпирический
вопрос, у которого нет единственно верного ответа без осмотра (то же
самое, что уже раз ловили как баг "3D — зеркальность"). Известный
компромисс по быстродействию — подписи по умолчанию выключены (см.
"Ключевые решения"), но если включить на большом файле, наклейки
добавляют 2-4 отрисовки на каждый подходящий элемент вместо 1 у спрайта
— тот же класс "техдолга", что уже отмечен для меток вообще.

**Файлы**: `app/static/app.js` (`DECAL_TYPES`, `DECAL_CHAR_WIDTH_RATIO`,
`DECAL_FIT_MARGIN`, `DECAL_SURFACE_OFFSET_MM`, `decalTextureCache`
новые константы/кэш; `getDecalTexture`, `footprintLongAxisAngle`,
`footprintDimensions`, `polygonSides`, `footprintCentroid`,
`orientDecalMesh`, `buildDecalPlane`, `build3DMarkDecal` новые функции;
`state.view3d.markDecalById` новое поле; `applyPlacementFilters`,
`apply3DLabelVisibility`, `rebuild3DLabelSprite`, `build3DScene` —
подключены к наклейке).

## Ригель — 4 наклейки вместо 2; динамический разворот текста при вращении камеры (2026-07-25)

Пользователь запросил два уточнения к наклейкам из предыдущего раунда.

**1. Ригель — 4 грани, не 2**: сечение ригеля квадратное (ширина
контура = высота экструзии, см. crossSectionWidth) — боковые
(вертикальные) грани вдоль длины ровно того же размера, что верх/низ, и
годятся под ту же наклейку тем же fontSize. Добавлены 2 наклейки на
боковых гранях (`normal` — перпендикуляр к длинной оси в плоскости
контура, ± в обе стороны, `right` — та же ось длины, что у верха/низа).
Плиту перекрытия НЕ трогал — про неё речь шла только про "сверху и
снизу" в прошлом раунде, "четыре стороны" пользователь просил только
для ригелей.

**2. Наклейка не билборд — лежит плоско на грани, не разворачивается к
камере автоматически** (в отличие от плавающей таблички-спрайта) — при
вращении камеры вокруг элемента с некоторого ракурса текст неизбежно
читался бы "вверх ногами" (пользователь подтвердил это на практике).
Фикс — `updateDecalOrientation(mesh, camera)`: каждая грань-наклейка
хранит СВОЮ "базовую" ориентацию (`userData.decalCenter/decalNormal/
decalBaseRight`, записана один раз при постройке в `buildDecalPlane`), и
на каждое движение камеры (`OrbitControls` `change`, `updateAllDecalOrientations`)
пересчитывается: "верх" текста (`cross(normal, right)`) сравнивается с
направлением на камеру (`camera.position - center`) — если их скалярное
произведение отрицательно (верх текста смотрит ОТ камеры, то есть с
текущего ракурса читалось бы вверх ногами), наклейка разворачивается на
180° вокруг своей нормали (просто `right → -right`, тот же
`orientDecalMesh`, что и при первой постройке). Дёшево — только смена
кватерниона у уже существующих мешей, БЕЗ пересборки geometry/texture;
вызывается и один раз сразу после `fit3DCameraToData()` (чтобы начальный
ракурс тоже был читаемым, не только после первого поворота).

Принцип верен независимо от того, какое из двух симметричных
направлений было выбрано "базовым" при постройке — правило "разворачивай,
если верх текста направлен от зрителя" самокорректируется на любом
ракурсе, поэтому остаточный риск неверной ориентации с прошлого раунда
(там, где нельзя было проверить читаемость без браузера) должен
исчезнуть сам собой, а не только для конкретной грани, о которой могло
быть сообщено.

**Проверено**: `node --check` — синтаксис ок. Численно (Node) —
перпендикуляр к длинной оси ригеля ортогонален и единичной длины на
реальном контуре; эвристика разворота (`dot(up, toCamera) < 0`) даёт
ожидаемый результат (не разворачивает, когда камера с "правильной"
стороны, разворачивает с противоположной) на модельных векторах. Живой
проверки — плавности/корректности разворота при реальном вращении
камеры мышью — не было (нет логина в этой сессии).

**Файлы**: `app/static/app.js` (`buildDecalPlane` — сохраняет
`userData.decalCenter/decalNormal/decalBaseRight`; `updateDecalOrientation`,
`updateAllDecalOrientations` новые функции; `build3DMarkDecal` — для
Ригеля добавлены 2 боковые наклейки; `init3DScene` — новый слушатель
`controls.addEventListener("change", updateAllDecalOrientations)`;
`build3DScene` — вызывает `updateAllDecalOrientations()` после
`fit3DCameraToData()`).

## Разворот наклейки был перепутан на обратный знак (2026-07-25)

Пользователь прислал живой скриншот сразу после предыдущей правки —
подписи на ригелях по-прежнему вверх ногами (все, не часть), плюс не
видно наклеек на боковых гранях.

**Разворот текста** — в `updateDecalOrientation` условие `flip =
up.dot(toCamera) < 0` было ПЕРЕПУТАНО НА ОБРАТНОЕ. Перепроверил на
конкретном примере руками (лист на столе: текст читают СО СТОРОНЫ,
противоположной верху страницы — верх "уходит" от читателя, а не
"смотрит" на него; значит верно читаемая позиция даёт `dot(up,toCamera)
< 0`, флип нужен при `> 0`) — то, что было в коде, ровно наоборот. Раз
знак был перепутан систематически (не через раз, а всегда), это и
объясняет, почему ВСЕ подписи оказались вверх ногами на любом ракурсе, а
не только часть — исправлено на `flip = up.dot(toCamera) > 0`, повторно
проверено численно на модельном примере ("читатель на правильной
стороне" → flip=false, "на неправильной" → flip=true — совпадает).

**Боковые наклейки не видны** — перечитал код `build3DMarkDecal`
построчно: позиция и нормаль для боковых граней ригеля построены тем же
способом, что уже верно работает для верха/низа (наружное направление =
`sign×(perpX,-perpY)`, совпадает с направлением от центроида до центра
грани) — багов на бумаге не нашёл. Вероятная причина — ракурс на
присланном скрине: камера смотрит вдоль коридора между ригелями
(параллельно их длине), а боковая грань (перпендикулярна длине) с этого
угла видна почти "с ребра" — геометрически тонкая полоска, легко
потерять на глаз, а не обязательно баг. Не проверялось живьём — попросил
пользователя посмотреть именно сбоку от ригеля (перпендикулярно его
длине) после фикса разворота и подтвердить, видны ли боковые наклейки
там, а не в текущем ракурсе вдоль коридора.

**Проверено**: `node --check` — синтаксис ок. Численно (Node) —
исправленное условие разворота даёт ожидаемый результат на модельном
примере (см. выше). Живой проверки после этого фикса ещё не было.

**Файлы**: `app/static/app.js` (`updateDecalOrientation` — знак условия
`flip` исправлен на противоположный).

## Динамический разворот ошибочно применялся и к боковым (вертикальным) граням (2026-07-25)

Пользователь проверил фикс знака — стало ХУЖЕ по-другому: на части
ригелей боковые (вертикальные) грани вверх ногами, на части — надписи
нет вовсе.

**Причина первой половины (вверх ногами на боковых гранях)**:
`updateDecalOrientation` применяла одну и ту же формулу разворота КО
ВСЕМ наклейкам без разбора — но критерий (сравнение "верха" текста с
направлением на камеру) осмыслен ТОЛЬКО когда "верх" текста
ГОРИЗОНТАЛЬНЫЙ (верх/низ плиты/ригеля — там при обходе камеры по
азимуту вокруг здания верх текста действительно то смотрит на зрителя,
то от него, это и была исходная проблема). На боковых гранях (колонна,
новые боковые грани ригеля) "верх" текста ВЕРТИКАЛЬНЫЙ (буквы стоят
прямо, читаются вдоль длины) — переворачивать его в зависимости от того,
чуть выше или чуть ниже камера относительно ВЫСОТЫ конкретного элемента,
не нужно и неверно — отсюда переворот "через раз", зависящий от
случайного соотношения высоты камеры и высоты каждого конкретного
ригеля по отдельности. Фикс — `updateDecalOrientation` проверяет
`Math.abs(up.y) > 0.5` (вертикальный верх — это боковая грань) и для
таких наклеек просто применяет исходную (заданную при постройке)
ориентацию всегда, без пересчёта по камере; динамический разворот
остался только для горизонтальных верх/низ граней, где он и нужен.

**Вторая половина (на части ригелей боковых надписей нет вовсе)** — НЕ
диагностирована: перечитал `build3DMarkDecal` построчно ещё раз, явного
бага в геометрии/условии создания не нашёл (позиция/нормаль строятся тем
же способом, что и у верха/низа, без дополнительного условия, которое
могло бы их пропустить). Возможные версии — то же самое совпадение
ракурса, что и в прошлой записи (боковая грань "с ребра"), либо
вырожденный случай в `footprintDimensions` для контура нестандартной
формы (не идеальный прямоугольник) — не проверено. Нужен конкретный
пример (марка/ID ригеля, где боковой надписи нет), чтобы поднять его
реальный контур из БД и посчитать точно, как уже делалось несколько раз
в этой сессии для похожих задач.

**Проверено**: `node --check` — синтаксис ок. Живой проверки после этой
конкретной правки ещё не было.

**Файлы**: `app/static/app.js` (`updateDecalOrientation` — динамический
разворот теперь только для горизонтальных наклеек, `Math.abs(up.y) >
0.5` разделяет случаи).

## Боковые наклейки ригеля — доказанная (не эвристическая) ошибка знака "право" (2026-07-25)

Пользователь дал 2 конкретных примера (ID 38989 "3Р17.6", ID 38799
"3Р12.4") и уточнил наблюдения: на первом — грань к зрителю ВООБЩЕ без
надписи; на втором — надпись на грани к зрителю ЕСТЬ, но вверх ногами.
Числа контуров у обоих проверил заранее — марка помещается по длине с
большим запасом (2200 мм текста из 7200-7500 доступных), деградации
`footprintDimensions` не было.

**Причина (доказана аналитически, не подбором)**: для двух
ПРОТИВОПОЛОЖНЫХ боковых граней ригеля (`sign=1`/`sign=-1` в цикле)
использовалось ОДНО И ТО ЖЕ направление "право" на обе стороны. Подставил
формулы `normal`/`right` в `up = cross(normal, право)` и упростил
аналитически (не численным перебором) — получилось `up.y = -sign`
ВСЕГДА, при любом угле длинной оси. То есть одна из двух сторон
(конкретно `sign=1`) гарантированно получала `up`, направленный ВНИЗ
(текст вверх ногами) — не "иногда, в зависимости от ракурса камеры", а
математически детерминированно по тому, какая это из двух сторон.
Правдоподобное объяснение и для "надписи нет вовсе" на другом ригеле:
если у зрителя в кадре оказалась именно перевёрнутая грань под острым
углом, перевёрнутый и обрезанный текст мог визуально не читаться как
"марка" вообще — не проверено полностью, но фикс тот же самый.

**Фикс**: "право" боковой наклейки домножается на `-sign`
(`sideRight = right.clone().multiplyScalar(-sign)`), не берётся общим на
обе стороны. Численно перепроверено (Node, без Three.js) на 4 разных
углах длинной оси (0°, 37°, 90°, 143°) — во всех случаях `up=(0,1,0)`
на ОБЕИХ сторонах одновременно (было — `(0,1,0)` на одной, `(0,-1,0)` на
другой) — доказательство, не эвристика.

**Проверено**: `node --check` — синтаксис ок. Численно (Node) — формула
подтверждена на 4 углах. Живой проверки не было — важно, чтобы
пользователь подтвердил ОБА наблюдения на тех же двух ригелях (38989,
38799): пропала ли надпись "вверх ногами" на обеих боковых гранях и
появилась ли надпись на той грани, где её не было видно вовсе.

**Файлы**: `app/static/app.js` (`build3DMarkDecal` — боковые наклейки
Ригеля используют `sideRight = right × -sign`, не общий `right`).

## Боковые наклейки — центр контура утапливал их ВНУТРЬ тела элемента (2026-07-25)

Разворот текста пользователь подтвердил как исправленный. Но грань, где
надписи не было вовсе (тот же ригель 38989 "3Р17.6") — осталась пустой
без изменений. Пользователь сам предположил: "может быть наклейка на
внутренней поверхности грани, а не на внешней". Предположение верное по
сути, только причина не в направлении (`normal` доказанно правильный —
"от центроида", математически по формуле), а в том, ОТ ЧЕГО отсчитывается
"от центроида".

**Причина**: `footprintCentroid` считала центр как простое среднее
координат вершин контура. Реальные DXF-контуры ригеля почти всегда
содержат лишние коллинеарные точки-середины на длинных сторонах (тот же
класс проблемы, что уже чинили в `offsetOutlineMM`, см. записи выше) —
вершины распределены по контуру НЕРАВНОМЕРНО, и простое среднее
смещается от истинного геометрического центра. Посчитал на конкретных
числах ригеля 38989 (контур 600×8160 мм): среднее по вершинам дало
центр Y=6674.53, истинный центр (по границам) — Y=6631.67, смещение
42.86 мм. При полуширине сечения 300 мм это смещение утопило сторону
`sign=-1` на 42.86 мм ВНУТРЬ тела элемента (координата стороны
6374.53 мм — ВНУТРИ истинных границ 6331.67…6931.67, а не на границе) —
добавленные потом 5 мм выноса (`DECAL_SURFACE_OFFSET_MM`) не спасали,
наклейка оставалась на ~38 мм внутри сплошного непрозрачного меша
самого ригеля, где полностью им закрывалась.

**Масштаб проблемы на реальных данных**: проверил скрипт по ВСЕМ 8094
элементам Ригель/Плита перекрытия текущего файла — смещение центра
больше 20 мм (заметная доля от типовой полуширины 300 мм) у **2097
элементов (26%)**, максимальное смещение — 994 мм. Не редкий частный
случай — системная проблема примерно на четверти реальных ригелей.

**Фикс**: `footprintCentroid` переписана на центр ОГРАНИЧИВАЮЩЕГО
ПРЯМОУГОЛЬНИКА (среднее между min и max по каждой координате), не
среднее по вершинам — устойчиво к любому числу лишних точек на прямых
сторонах. Для ЛЮБОГО прямоугольника (в том числе повёрнутого) это даёт
истинный центр: прямоугольник центрально-симметричен, его
ограничивающий прямоугольник по осям XY всегда центрирован в ту же
точку независимо от угла поворота — доказано, не только для
осеориентированного случая. Сама функция (не только код вызова)
переписана — оба места использования (Колонна и Плита/Ригель) получили
фикс без отдельных правок.

**Проверено**: `node --check` — синтаксис ок. Численно (Node) на
контуре ригеля 38989 — новый центр Y=6631.67, ровно на истинной
границе после вычитания полуширины (расхождение — 0.001 мм,
погрешность округления). Скрипт по всем 8094 реальным контурам —
подтвердил масштаб проблемы (см. выше), та же bbox-логика в проверочном
скрипте попутно подтверждает и сам фикс. Живой проверки после этой правки ещё не было.

**Файлы**: `app/static/app.js` (`footprintCentroid` — реализация
переписана на bbox-центр вместо среднего по вершинам).

## 3D — убрана инерция вращения камеры (2026-07-25)

Пользователь: вращение/зум в 3D ощущались "с оттяжкой" — после отпускания
мыши модель ещё какое-то время докручивалась сама по инерции, "как будто
имеет массу". Попросил убрать — камера должна чётко следовать за
командами и останавливаться сразу же, как оператор прекратил ввод.

`OrbitControls.enableDamping` был включён (`true`, `dampingFactor=0.1`) —
именно это и даёт эффект инерционного "доворота" после отпускания.
Выключен (`enableDamping = false`) — теперь `controls.update()` (уже
вызывается каждый кадр в `animate3D()`) применяет позицию камеры без
сглаживания, движение прекращается мгновенно вместе с вводом.

**Проверено**: `node --check` — синтаксис ок. Живой проверки не было.

**Файлы**: `app/static/app.js` (`init3DScene` — `controls.enableDamping`
true→false, `dampingFactor` убран как более не нужный).

## Колонны верхнего яруса не дотягивались до локальной надстройки выше основного потолка (2026-07-26)

Живой репорт пользователя со скриншотом (карточка колонны 8Кв1.3,
id 30258, отметка 25800): на файле 260723 верхний ярус ригелей/плит —
не 34700 (уже учтённый `computeTopColumnCeiling` как "ближайшая плита
перекрытия выше верхнего яруса колонн"), а ЕЩЁ выше, 39200 — локальная
надстройка (34 ригеля / 42 плиты на 39200, против ~330/~2000 на каждом
из "полноразмерных" ярусов 15000/25800/34700 — явно небольшая кровля
техпомещения, не полный этаж). `computeTopColumnCeiling` умеет учитывать
только ОДИН потолок выше верхнего яруса колонн — 39200 полностью
игнорировался, все колонны верхнего яруса заканчивались на 34700, ригели/
плиты на 39200 оставались висеть в воздухе без опоры.

Требование пользователя, уточнённое явно: удлинять до 39200 нужно
**только те колонны, что стоят под НАЧАЛОМ/КОНЦОМ ригеля** на этой
отметке (ригель опирается концами на колонны, плита лежит на ригеле, не
на колонне напрямую) — колонны под СЕРЕДИНОЙ пролёта такого ригеля высоту
менять не должны.

**Реализовано** (`app/static/app.js`):
- Новая `computeColumnEndExtensions(levels)` — для каждого "Ригель" с
  `elevation_mm` выше базового потолка (`computeTopColumnCeiling`)
  считает 2 конца (центроид контура `footprintCentroid` ± половина
  длины `footprintDimensions` вдоль направления длинной стороны
  `footprintLongAxisAngle` — переиспользованы те же функции, что уже
  строят наклейки марок на гранях, отдельной геометрии не писал), ищет
  ближайшую колонну ВЕРХНЕГО яруса по центроиду ЕЁ СОБСТВЕННОГО контура
  (не по `x`/`y` — это позиция выноски/лидера марки, не геометрический
  центр — источник марки колонны на скриншоте как раз `leader`).
  Допуск `COLUMN_BEAM_END_MATCH_TOLERANCE_MM = 1000` мм — подобран по
  факту (см. проверку ниже), не взят с потолка. Возвращает
  `Map(id колонны -> самая высокая подходящая отметка)`.
- `elementExtrusionHeight(element, levels, columnTopOverrides)` — новый
  3-й параметр: для колонны верхнего яруса, если есть override выше
  базового потолка, используется он вместо `computeTopColumnCeiling`.
  `build3DMeshForElement`/`build3DMarkDecal` — тот же новый параметр,
  прокинут насквозь (высота декали на грани обязана совпадать с высотой
  самого меша). Три места, где раньше считался `computeColumnLevels()`
  (`rebuild3DLabelSprite`, `build3DScene`) — рядом считают и
  `computeColumnEndExtensions(levels)`, передают дальше. Единственное
  место, где НЕ нужно — `computeBuildingHeightRange` (общий диапазон
  высоты здания для объёма захватки, там уже используется глобальный
  потолок напрямую, не по колонкам).

**Проверено** (не "на глаз", а функциями из САМОГО `app.js`, вырезанными
и прогнанными в Node на дампе реальной БД — не переписанная копия
логики):
- `node --check` — синтаксис ок.
- Live-скрипт на живой БД (до кода) подтвердил сам факт: 34 ригеля на
  39200, 68 концов, при сопоставлении по центроиду контура (не по x/y
  марки) — 100% концов находят колонну ближе 1000мм (большинство —
  ближе 400мм); ни у одного из 34 ригелей нет колонны "в середине"
  пролёта в этом конкретном файле (значит требование "не удлинять
  среднюю" сейчас ничего не ломает, но алгоритм и не пытается угадывать
  середину — просто не удлиняет то, что не совпало с концом, это
  побочное свойство, а не отдельная проверка).
- После правки — те же вырезанные функции `computeColumnLevels`/
  `computeTopColumnCeiling`/`computeColumnEndExtensions`/
  `elementExtrusionHeight` прогнаны в Node на том же дампе: колонна
  8Кв1.3 (id 30258, та же с скриншота пользователя) получает override
  39200, итоговая высота 13400мм (25800→39200) — ровно то, что просил
  пользователь. Из 332 колонн верхнего яруса — 30 получили удлинение до
  39200, 302 остались на базовом потолке 34700, 0 неожиданных значений.
- Собран Docker-образ (`docker compose build && up -d`), сервер
  отдаёт обновлённый `app.js` (`grep computeColumnEndExtensions` через
  `/static/app.js` — 4 вхождения, как и в исходнике).
- Живой браузер (визуально, после логина) — НЕ проверено в этой сессии:
  для входа нужен реальный пароль администратора, менять/узнавать его
  не входит в задачу проверки. Верификация через прямой прогон реальной
  функции на реальных данных (см. выше) — более сильное доказательство
  корректности геометрии, чем визуальный осмотр, но сам факт, что новая
  высота КРАСИВО рендерится в 3D (материалы/рёбра/декали), глазами не
  подтверждён.

**Файлы**: `app/static/app.js` (`computeColumnEndExtensions` новая,
`COLUMN_BEAM_END_MATCH_TOLERANCE_MM` новая константа,
`elementExtrusionHeight`/`build3DMeshForElement`/`build3DMarkDecal` —
новый 3-й параметр `columnTopOverrides`, `rebuild3DLabelSprite`/
`build3DScene` — считают и прокидывают его).
