# Docs/DECISIONS.md — ключевые решения проекта

Инварианты, которые нельзя нарушить, и причины, по которым они такие.
Выделено из `CLAUDE.md` 2026-08-04: индекс дорос до 580 строк при потолке
450, и раздел решений был в нём самым большим — теперь он живёт здесь и
читается ПО НАДОБНОСТИ, а не целиком при каждом старте сессии.

Формат прежний: **что** — инвариант; подробности, измерения и история —
`Docs/backlog.md` по указанной дате. Обновлять при значимых изменениях
архитектуры — той же стоячей инструкцией, что и `CLAUDE.md`.

## Данные, объекты, доступ

- **Иерархия Проект → Объект.** У проекта нет своих данных, кроме
  реквизитов; всё живёт внутри объекта. Схема показывает ОДИН объект за раз,
  его выбирает крошка в тулбаре, выбор хранится за ПОЛЬЗОВАТЕЛЕМ. Точка
  перевода «объект → файл» ОДНА — `db.object_source_file`, а не переписанные
  под `object_id` запросы всех 22 эндпоинтов (этапы A–D, 07-31…08-02).
- **Смена объекта сбрасывает шесть вещей**: выделение, групповое выделение,
  ОБЕ структуры фильтров, метку опроса `/changes`, ручной выбор версии.
- **Статус записи справочника** (`active/completed/archived`, 09-07) есть у
  проекта И у объекта: при сотнях позиций списки нечем разгрузить, а удалять
  завершённую стройку нельзя — на ней держатся отчёты. Набор значений держит
  КОД, а не `CHECK`: SQLite не принимает его в `ADD COLUMN` (как и у
  `objects.kind`). **Проект уходит в архив только когда в нём не осталось
  активных объектов** — иначе стройка исчезает из выбора вместе с проектом, а
  искать её человек станет среди объектов.
- **Координаты подставляются автоматически, а не вводятся руками**
  (09-08): по мере складывания адреса виджет спрашивает геокодер
  OpenStreetMap (Nominatim), тот же выключатель, что у подложки карты.
  **Геокодеру передаются РАЗОБРАННЫЕ части адреса, а не отформатированная
  строка с сокращениями** — на живых данных свободный текст «г Москва»
  геокодер прочитал как «гора Москва» и вернул точку в Красноярском крае за
  четыре тысячи километров от настоящей; структурный запрос («город» ищем в
  поле города) этой ошибки не допускает.
- **Автоматика не перезаписывает координаты, если они уже сохранены или
  тронуты вручную** (пин, клик по карте, ручной ввод числа): правка
  опечатки в адресе не должна тайком сдвинуть намеренно уточнённую точку.
  Кнопка «Определить заново» снимает защиту явным действием. Неудачный
  более точный запрос (например, дом не найден) не портит менее точный, но
  успешный прежний результат — геокодер возвращает пусто, а не приблизительно.
- **Адрес — строка ПЛЮС разбор рядом, а не вместо** (09-07). Строка остаётся
  источником для показа, поиска и печати; код классификатора, регион, части,
  индекс и координаты лежат рядом. Причина: адреса, введённые руками до
  классификатора, обязаны работать дальше, а показ адреса не должен ходить в
  базу классификатора и обязан пережить её перезагрузку. **Пустой
  `address_code` — законное состояние**: у стройплощадки почтового адреса
  часто нет вовсе. Пока код заполнен, строка только для чтения — иначе
  строка и разбор разъедутся, и какой из них правда, будет неизвестно.
- **Свой распаковщик 7z вместо библиотеки** (09-07): КЛАДР раздаётся ФНС
  только в 7z, а готовая библиотека тянет девять пакетов, часть с
  C-расширениями, в образ без сборочных инструментов. Разбирается ЧАСТНЫЙ
  случай — сплошной поток, сжатый обычным LZMA (так и собран `BASE.7z`), а
  LZMA умеет стандартный Python. Тот же довод, что у своего разбора DBF и
  своего SVG вместо библиотеки графиков.
- **Файлы классификатора ищутся БЕЗ учёта регистра**: в архиве они названы
  «KLADR.dbf», в коде «KLADR.DBF». На macOS разницы нет, на сервере под
  Linux классификатор молча считался бы незагруженным.
- **Дом выбирается из списка, а не только вводится** (09-08). Первая
  версия отказалась от выпадашки: считалось, что КЛАДР хранит номера
  диапазонами через дефис («1,3,5-9») и раскладывать их в список — значит
  выдумывать номера, которых нет. Проверка на ВСЕЙ стране (все 90
  регионов, 3 891 608 строк `DOMA.DBF`) не нашла НИ ОДНОГО дефиса между
  двумя числами — единственное употребление дефиса (236 002 строки) это
  часть сокращения «г-ж» (гараж). КЛАДР перечисляет номера по одному,
  включая корпуса и строения. Решение пересмотрено по факту, а разбор
  диапазонов (`_номера_из_диапазона`) убран из кода как код на
  гипотетический случай, для которого нашлось железное опровержение —
  сравнение номера дома теперь точное, без попытки что-то разворачивать.
- **Классификатор адресов и подложка карты — ВНЕ основной базы**
  (`data/addr.db`, `data/map/`, 09-07): справочные данные на сотни мегабайт и
  гигабайты, которые не создаются сервисом, а приносятся снаружи. Они не
  входят ни в резервные копии, ни в обезличенную копию, ни в миграции; их
  потеря не теряет ничего рабочего. **Координат в классификаторе нет** — ни в
  КЛАДР, ни в ГАР: их ставит человек пином на карте.
- **Обезличивание трогает и координаты**: точка на карте показывает реальную
  площадку заказчика с точностью до здания, поэтому в копии они заменяются
  условным центром с детерминированным разбросом — карту на копии всё ещё
  можно отлаживать, а место в ней вымышленное.
- **Роль — свойство ГРАНТА** (`user_access`), а не пользователя: один
  человек бывает прорабом на одном здании и наблюдателем на соседнем.
  `users.role` — СИСТЕМНАЯ роль (ведение сервиса). Грант ложится на один из
  трёх уровней (объект → проект → все проекты); до 08-14 частный перекрывал
  общий, теперь они СКЛАДЫВАЮТСЯ (следующий пункт).
  `accessible_object_ids` возвращает `None` = «все», и это НЕ пустое
  множество (`app/access.py`, 08-01/08-02).
- **Роли на объекте НЕЗАВИСИМЫ, разрешения СКЛАДЫВАЮТСЯ** (08-14, решение
  пользователя). Ни одна роль не включает другую; человеку выдают несколько
  ролей на одном объекте, уровень доступа к разделу — максимум по ним.
  Лестница (`view` < `user` < `contract` < `admin`), на которой всё
  держалось с 08-01, снята: слов «выше»/«ниже» у ролей не осталось, `rank`
  задаёт только порядок колонок в матрице.
- **Гранты трёх уровней тоже складываются** (08-14). Частный больше НЕ
  перекрывает общий. Следствие, которое надо знать: понизить человека на
  одном здании сложением нельзя — общесервисные гранты просто не
  выдаются, роль даётся на конкретный объект. Накопленное показывает
  `scripts/check_grant_widening.py`; миграция чужие гранты не трогает.
- **Разрешение — ДАННЫЕ, а не код** (08-14, блок «Настройка ролей»). Роли
  живут в `object_roles`, их разрешения — в `role_features` (60 разделов ×
  роли, уровень `read`/`write`), перечень самих разделов — в
  `app/features.py` (заводит разработчик вместе с проверкой; придумать в
  форме несуществующую проверку нельзя). Проверка адресуется РАЗДЕЛОМ:
  `require_feature("zones", "write")` вместо прежнего
  `require_object_admin`. Заводская раскладка = что каждая из четырёх
  прежних ролей могла до этого дня, поэтому в день выхода не меняется
  ничего.
- **Заводская раскладка ролей накатывается ОДИН РАЗ, за маркером**
  `role_features_seeded` (08-19, разбор инцидента). Условие «у роли нет ни
  одной строки разрешений» на эту роль больше не годится: администратор
  вправе удалить заводскую роль, и тогда миграция вставляла `role_features`
  со ссылкой на несуществующую роль — внешний ключ ронял СТАРТ сервиса, и
  падал при этом любой рестарт, а не только обновление. Откат на прежний
  образ не помогал: ошибка была в коде с 08-14. Отсюда же правило: в
  `role_features` не должна попадать ссылка на роль, которой нет, — цена ей
  не кривая строка, а неподнимающийся сервис. Побочно: снятые
  администратором разрешения больше не возвращаются при перезапуске.
- **Ротация копий: пять последних СЛУЖЕБНЫХ, ручные не трогаются никогда**
  (08-19). Служебная копия — это то, что система сняла сама перед опасным
  действием; её ценность падает с каждым релизом. Ручная копия — решение
  человека, и отменять его уборкой нельзя. Файл без описания приравнен к
  ручному: раз происхождение неизвестно, удалять по догадке нельзя.
- **Холостой прогон миграции — на копии базы ТОГО сервера, куда выкатываем**
  (08-19). `scripts/dry_run_migration.py` этот инцидент ловил, но его гоняли
  на копии локальной базы, где удалённой роли не было: базы серверов
  расходятся, и «похожая» копия отвечает на другой вопрос.
- **Отсутствие строки в `role_features` = «Нет».** Нули не хранятся, и
  новый раздел из будущей версии оказывается закрытым, а не открытым, —
  ошибка в безопасную сторону.
- **Считает ОДНА функция — `access.has_feature`.** Ею отвечают эндпоинты,
  ею же считаются матрица прав и карта прав для клиента. До 08-14 матрица
  разбирала пороги отдельно, «по памяти», и повторяла порядок проверок
  ровно до первой правки в одном из двух мест.
- **Обмен данными делится по той же границе `read`/`write`** (08-19). Раздел,
  который и выгружает данные файлом, и принимает их обратно, отдаёт выгрузку
  «Чтению», а загрузку — «Изменению». Третьего уровня под это не заводится:
  выгрузка ничего не меняет и по смыслу ровно чтение, загрузка меняет и по
  смыслу ровно изменение. Помечается признаком `io` в `app/features.py`
  (`IO_BOTH`), по нему форма гасит шаги загрузки, а обе матрицы подписывают
  клетку. Помечен ОДИН раздел — «Массовая правка через Excel»; «Перенос базы
  целиком» и «Экспорт/импорт настроек» правилу НЕ подчиняются и остаются за
  администратором сервиса (решение пользователя): выгрузка у них — вся база
  вместе с хэшами паролей.
- **Место проверки решает ВЫЗОВ, а не описание раздела.** Объект передали —
  роли ищутся на нём; не передали — берутся все роли человека, где бы они
  ни были выданы. Признак `scope` в реестре — описание для формы, а не
  правило: справочник марок общесервисный, а список марок запрашивается
  ДЛЯ ОБЪЕКТА, и «хотя бы где-то» открыло бы чужие объекты.
- **Запереть систему настройкой нельзя.** Администратор сервиса проходит
  всё в обход, и снять роль администратора сервиса С САМОГО СЕБЯ нельзя
  (`app/users.py`, 409) — это единственная защита, которую настройкой не
  обойти. Отдельного стража от прочего самозапирания намеренно НЕТ: страж,
  который ничего не ловит, со временем начинают считать работающим.
- **Удаление роли — БЕЗ замены**, в отличие от справочников
  (`app/dict_delete.py`). Роли складываются, у человека их обычно
  несколько, и «перевести доступ на другую роль» выдало бы права, которых
  администратор не выдавал. Роль просто исчезает из наборов; сколько это
  людей, форма показывает заранее.
- **У клиента НЕТ своей копии ролей и разрешений** (08-14). `GET
  /me/permissions` отдаёт готовые уровни по разделам, пункт меню помечен
  своим разделом (`data-feature`). Прежние `OBJECT_ROLE_ORDER` и три класса
  гашения (`.admin-only`/`.object-admin-only`/`.object-contract-only`)
  огрубляли картину: за одним классом стояли пункты с разными правилами, и
  с настраиваемыми разрешениями такое огрубление сразу начинает врать.
- **CHECK на `user_access.role` снят, роль вошла в уникальный индекс**
  (08-14). Роли произвольны, а на одном уровне их бывает несколько —
  прежний ключ `(пользователь, проект, объект)` пускал ровно одну. Ловушка
  при этом: старая миграция `_migrate_user_access_contract_role`
  опознавала своё состояние по слову `'contract'` в тексте CHECK, а после
  снятия CHECK признак снова стал истинным — две миграции пересобирали
  таблицу друг за другом на каждом старте. Поймано повторным прогоном на
  копии.
- **Объект нигде не принимается ПАРАМЕТРОМ там, где выводится из сущности** —
  иначе клиент назовёт любой доступный и правит чужое. Отбор недоступного —
  в `projects_tree`, одно место вместо 22.
- **Настройки и справочники — внутри объекта** (`label_visibility`,
  `zone_colors`, `report_notes`, `default_contracts`, `app_settings`).
  `get_setting`/`set_setting` берут `object_id` ОБЯЗАТЕЛЬНЫМ позиционным
  аргументом. **Индексы объектных таблиц — в `_ensure_object_scoped_indexes`,
  НЕ в `schema.sql`**: тот выполняется до миграций, и `CREATE INDEX` по ещё
  не добавленной колонке роняет старт (уже роняло).
- **Идентичность элемента — `element_uid`, а не `(source_file, dxf_handle)`**:
  новая версия чертежа приходит под новым именем, и раньше давала 9422 новых
  строки «Запланирован». Сверка многоуровневая (`app/element_identity.py` +
  `app/element_sync.py`): handle → ключ «тип+отметка+центроид» → ближайший в
  плане (50 мм); отметка в ключ НЕ входит. Импорт двухфазный
  (`/analyze` → сводка → `/apply`), смена марки согласуется. `is_current=0` =
  элемент исчез из чертежа; единое условие видимости —
  `visible_elements_clause()`, не повторять строками.
- **Зоны — справочник уровня Объекта** (`zones` + `zone_levels`): «Стоянка 01»
  на четырёх ярусах — одна запись с четырьмя полигонами. Ключ опознания при
  переимпорте — категория + номер (+ родительский кран), НЕ имя: формат имени
  менялся между версиями чертежа. Запись — `app/zone_sync.py`, не DELETE+INSERT.
- **Пересчёт привязки к зонам — теми же функциями, что импорт**
  (`zone_binding.bind_element_to_zones`), `app/zone_recalc.py`. Один осознанный
  отказ (`can_recalculate`): чертёж с ОДНИМ ярусом стоянок — там ярусы
  строятся «лесенкой», и пересчёт дал бы другую привязку.
- **Права выдаются деревом в форме пользователя**, `PUT /users/{id}/access`
  задаёт СОСТОЯНИЕ, а не разницу (считать разницу на клиенте — надеяться,
  что она совпала с серверной). На каждом уровне — НАБОР ролей галочками
  (08-14), по строке гранта на каждую.
- **План запланированной работы (ЗР) — явный список, неявного «всё» не
  существует** (09-10, `app/block_works.py`, `Docs/block-works-schedule-task.md`
  В1). До этого решения отсутствие настройки блока читалось как «выбрано
  всё» — годилось, пока у операции не было сроков; сроку неявно ставить
  некуда. Не настраивали — список пуст, а не «всё». Снятие операции с
  фактом/сроками — мягкая пометка (`retired_at`), не удаление строки:
  история остаётся адресуемой.
- **Базовый (директивный) срок ЗР меняется только явной правкой с записью
  в журнал** (09-10, `block_work_plan_set`, старое/новое значение) —
  аналог директивных полей графика СМР ЖБИ (см. ниже): обещание, а не то,
  что пересчитывается само.
- **Актуализированный срок ЗР накапливается версиями, старые не правятся**
  (09-10, `block_work_forecasts`) — тот же приём, что у графика СМР ЖБИ
  (`schedule_versions`, 08-14): отдельная таблица версий, текущее значение
  денормализовано на саму ЗР ради скорости отчётов, отклонение считается
  между директивным полем и последней версией.

## Импорт DXF, зоны, геометрия

- **Стоянка крана ≠ точка** — рабочая зона крана в позиции. Элемент без
  стоянки, но с краном — псевдо-значение `no-stance:<craneId>`, дочерним
  пунктом под свой кран.
- **Лесенка сужения зоны стоянки с высотой — ТОЛЬКО когда в файле физически
  один ярус стоянок.** Если заказчик нарисовал реальные полигоны на
  нескольких ярусах, лесенка ВЫКЛЮЧЕНА (путает «номер стоянки» с «ярусом») —
  прямой снэп на ближайший ниже реально нарисованный ярус.
- **Ригель/Плита перекрытия относятся к ярусу НИЖЕ** для привязки к стоянке
  (физически венчают ярус снизу), Колонна — к своему.
  `zone_binding.TIER_CAPPING_TYPES`, снэп СТРОГО ниже (`<`, не `<=`).
- **Этаж — суффикс `_этаж N` в имени слоя, НЕ вывод из `elevation_mm`**
  (соответствие неоднозначно между типами). Пока суффикса в чертежах нет —
  `assign_fallback_floors` считает этаж по отметке «Плита перекрытия»
  (не «Колонна» — та приходит несколькими отметками на одном этаже); трогает
  только `floor IS NULL`.
- **Пустая подпись маркой не считается** (08-21, `build_leader_pool` и
  `resolve_via_polygon_texts`). Сопоставление жадное по расстоянию, и
  пустышка ближе настоящей подписи забирала изделие себе: изделие без
  марки, настоящая подпись не досталась никому, а источник при этом
  «leader» — то есть в сводке «распознано». Пустой текст выбывает из пула
  целиком; изделие честно `unresolved` и видно числом до применения.
- **Справочник подтипов принадлежит ОБЪЕКТУ и наполняется загрузкой**
  (08-21). Подтип — почти всегда отметка перекрытия, то есть свойство
  здания: у соседнего дома свой набор. Новый объект — пустой справочник;
  незнакомый подтип разбор не отвергает, а принимает с написанием ИЗ ФАЙЛА
  и записывает объекту на фазе ПРИМЕНЕНИЯ (анализ не пишет ничего).
  Раскладка накопленного — по факту употребления в `elements`, остаток
  (не подтверждённый ни одним изделием) — объекту с наименьшим id.
- **Незнакомый подтип — единственное исключение из строгой валидации имён
  слоёв** (08-21). Всё прочее — тип, роль, категория, лишние части — по
  прежнему отказ. Отдельно: **токен на `ОТМ` обязан быть отметкой либо
  ошибкой**, подтипом он не становится — иначе опечатка `ОТМП30.750`
  (точка вместо пятизначного числа) молча заводит подтип с таким именем и
  теряет отметку у сотен изделий.
- **Объект разрешается ДО разбора чертежа, а не после** (08-21): разбору
  нужен справочник подтипов ИМЕННО этого объекта. Побочно — отказ «в базе
  несколько объектов» приходит сразу, а не после разбора девяти тысяч
  изделий.
- **Загрузка из `Input/` убрана со старта сервера**: старт — это каждый
  деплой и автоперезапуск, и каждый переписывал геометрию всех 9422
  элементов. Теперь по команде; вместо стартового импорта как детектора
  битой схемы — `_probe_schema_health()` (`PRAGMA foreign_key_check` эту
  поломку НЕ ловит, проверено).
- **У пакетной загрузки (`Input/`) свой выбор объекта — ОДИН на всю
  папку** (08-21). Папка — способ положить на сервер тяжёлый чертёж, а не
  разложить стройку по зданиям, поэтому объект спрашивается один раз, а не
  на каждый файл. Без него на базе с двумя зданиями чертёж ронял весь пакет
  пятисоткой, а график СМР сопоставлялся по всей базе и развозил даты по
  чужому дому. Если объект всё-таки не назван (старый клиент,
  `rebuild_db.py`) — отказ строкой отчёта, а не исключением: одно «нельзя»
  не должно ронять пакет.
- **Контрактация из папки не грузится и после появления выбора объекта**
  (08-12/08-21). Её объект выбирается ОТДЕЛЬНО, своей формой: файл
  приходит от снабжения и вполне может относиться к соседнему зданию, и
  общий выбор на пачку маскировал бы ошибку.
- **`PRAGMA foreign_keys = OFF` — молчаливый no-op внутри открытой
  транзакции**, из-за чего `ALTER TABLE … RENAME` переписывал FK ДРУГИХ
  таблиц навсегда. В `app/db.py` перед PRAGMA стоит commit — первопричина
  двух падений сервера (07-28/07-29).
- **`_attempt_migration_recovery` в `on_startup()` — РАЗОВЫЙ защитный
  механизм** под конкретный инцидент, не архитектура. Убрать, когда станет
  ясно, что сервер стабильно поднимается без него — но **не самостоятельно,
  только по запросу пользователя**.

## Статусы, история, контрактация

- **«Рабочая дата» (backdating)**: `current_status` вычисляется из
  ПОСЛЕДНЕЙ ПО `changed_at` записи `status_history`, не последней по вставке.
  Кнопка «Сейчас» заполняет поле, «✕» очищает; диалог смены статуса всегда
  показывает и даёт поменять момент применения.
- **Контракт — реквизит элемента, а не кэш истории** (08-01).
  `sync_element_contract` держит инвариант «Запланирован ⇒ контракт пуст» в
  одном месте и решает по ЭФФЕКТИВНОМУ статусу. `status_history.contract_id`
  остаётся аудиторским СНИМКОМ; обратное направление
  (`adopt_contract_from_history`) — только там, где историю восстанавливают.
- **Откат на «Запланирован» снимает контракт** безусловно, даже если он
  передан явно.
- **Изделие держится за ПОЗИЦИЮ спецификации** (08-14, решение
  пользователя, страж `app/contract_guard.py`): позиция под (тип, марка)
  обязана существовать, привязанных по ней не больше закупленного. Отсюда
  запреты на привязку без позиции и сверх количества, на уменьшение/
  удаление/переименование позиции под привязанными изделиями и на правку
  марки изделия, уводящую его из-под позиции. Правило одно на ВСЕ пути
  записи (статусы, карандаш, оба Excel-обмена, три импорта, удаление с
  заменой, документы контрактации) — это и есть причина отдельного
  модуля. Отменены прежние решения: «показывать серым, но пускать»
  (08-04) и «предупреждать и согласовывать» по марке (Э5). Проверка
  сравнивает состояние ДО и ПОСЛЕ и ругается только на УХУДШЕНИЕ — иначе
  накопленное нельзя было бы чинить; марки сравниваются без учёта
  регистра, иначе задвоенность написаний давала бы запрет на ровном месте.
  Накопленное показывает `scripts/check_contract_coverage.py`.
- **Контрактация 2.0** (07-28): контракт ссылается на `specification_id`
  (Контрагент → Договор → Спецификация), позиции — тип+МАРКА. «Партии»
  убраны целиком; вместо них у элемента `planned_delivery_date`, плюс
  `project_delivery_date`/`project_smr_start_date` (только импорт графика MS
  Project) и `actual_delivery_date` (кэш ПОСЛЕДНЕГО перехода в «Доставлено»,
  пересчёт — общая `recompute_status_and_actual_date`).
- **Фактическая дата поставки переживает монтаж** (08-06, решение
  пользователя): смонтированное изделие остаётся поставленным. Очищается
  только полным откатом в «Запланирован» (как и контракт) или удалением
  записи «Доставлен» из истории. До 08-06 дата жила, только пока текущий
  статус ровно «Доставлен», — и монтаж стирал её, делая изделие «не
  поставленным» в отчётах. Накопленное доводит обработка
  `2026-08-06-actual-delivery-date-backfill` — то же выражение, что в
  пересчёте, менять только парой.
- **Наименование контракта всегда генерируется** (`build_contract_name`), не
  вводится руками. **Договор заключается НА ОБЪЕКТ**; объект контракта
  ВЫВОДИТСЯ по цепочке и полем не дублируется — это инвариант схемы.
  Договоры без объекта (наследие) видит только администратор сервиса, и в
  каскаде формы контракта тоже (08-03: иначе форма — тупик).
- **Контракты в выпадающих списках — группами «Контрагент · договор»**,
  `contractOptionsHtml`, одна функция на три места выбора (08-03).
- **Контракт при смене статуса отбирается по ПОЗИЦИИ (тип + марка)**, не по
  типу, и показывает план/разнесено/остаток (`GET /contracts/positions` — по
  ТИПУ, клиент раскладывает по маркам и кэширует; 08-04). Ключ сопоставления
  общий с `contract_line_warning`. Список, а не `<select>`: части текста
  `<option>` раскрасить нельзя. ОДИН компонент (`mountContractPicker`) на
  диалог одного изделия, строку массовой формы и раздачу по маркам в её шапке.
- **В массовой смене статуса «доступно» = остаток − РЕЗЕРВ формы** (08-04):
  резерв — что расписано на контракт в ещё не сохранённой форме. Не считаются
  своя строка и строки, где контракт уже стоит в базе (иначе вычитается
  дважды). Пересчёт — только в `updateBulkStatusValidation`, туда сходятся все
  изменения. «Распределить» по марке не трогает выбранное вручную.
- **Порог читаемости подписей — персональная настройка** (`users.min_label_px`,
  дефолт 12; 08-04). В коде только `minLabelPx()`, константу не читать: иначе
  учтётся в 2D и не учтётся в 3D. В 3D после смены обновлять uniform ЯВНО —
  `requestAnimationFrame` в фоновой вкладке не срабатывает.
- **Требования по правам описаны в `app/rights_matrix.py`** (разделы системы →
  минимальная роль на чтение и на запись, у каждой строки перечислены
  эндпоинты). Это ОПИСАНИЕ проверок, а не вторая система прав; правишь
  проверку в эндпоинте — правь и там. Экран «Права пользователей»
  (пользователи × уровни) и отчёт «Матрица прав» считают по нему.
- **Регистронезависимое сравнение кириллицы — только на стороне Python.**
  SQLite без ICU кириллицу к одному регистру не приводит («15КС1.1» vs
  «15кс1.1» — живой баг).
- **Импорт истории: третий режим `sync`** («Скорректировать даты») —
  идентичность записи это (элемент, статус) + номер повторения; попарное
  сопоставление по порядку. `_shift_planned_before_first_event` (во ВСЕХ
  режимах) двигает самую раннюю «Запланирован» на минуту раньше первого
  события другого статуса — иначе правка задним числом откатывает элемент и
  отменяет сама себя. `normalize_changed_at` приводит момент к одному виду:
  даты в `status_history` сравниваются КАК ТЕКСТ.

- **Документ «Смена поставщика» (08-11, `app/supplier_change.py`) статус НЕ
  двигает.** Он меняет `elements.contract_id` и добавляет запись истории ТЕМ
  ЖЕ статусом, с новым контрактом в снимке; момент записи — текущий, а не
  дата документа (запись задним числом перестала бы быть последней, и в силе
  остался бы прежний снимок). Порог «поставлено на площадку» — «Отгружен» и
  выше, считается ОТ порядка `STATUS_ORDER`, а не перечислением кодов.
  Доступное к переносу количество — `план − факт − повреждено` по (тип,
  марка) нового контракта: та же формула, что у остатка в карточке контракта
  и у выбора контракта при смене статуса, расходиться этим трём местам
  нельзя.
- **Документы контрактации ПРОВОДЯТСЯ** (08-11, оба вида). Черновик данные не
  трогает; «Провести» применяет всё разом, «Отменить проведение» возвращает
  исходное состояние по `prev_*` в табличной части и по
  `supplier_change_history_moves`. Проведение — ВСЁ ИЛИ НИЧЕГО: строка,
  переставшая подходить, останавливает его с перечнем причин (документ уже
  сохранён и обязан означать то, что в нём написано). Проведённый документ не
  правится и не удаляется — только через отмену проведения.
- **«Обмен привязками» переставляет ИСТОРИЮ, а не статус** (08-11). Текущий
  статус и фактическая дата производные от истории, и обменять их, оставив
  историю на месте, значило бы получить изделие, чей статус противоречит
  собственным записям. Явно переставляются только живые поля — `contract_id`
  и плановая дата; остальное даёт `recompute_status_and_actual_date` после
  переезда записей. Ограничений по статусу у обмена НЕТ (решение
  пользователя): перепутанную привязку чаще всего обнаруживают у
  смонтированных изделий. Обмен идёт по ОДНОЙ марке и парами «строка против
  строки»; неравные стороны провести нельзя.

## Справочники: марка как запись, удаление с заменой (08-05)

- **Марка — запись справочника, владелец ТИП, область ОБЪЕКТ.** Ключ
  (object_id, element_type, name), `app/marks.py`. Свободный текст расщеплял
  одно изделие на две ветки фильтров, подписей и остатков при разном
  регистре («К-1»/«к-1»), и свернуть его было нечем.
- **Текстовое `elements.mark` живёт РЯДОМ с `mark_id` и остаётся источником
  правды** для фильтров, отчётов, экспорта и импортов, пока разложение не
  сверено (правило «релиз только добавляет»). Отсюда инвариант: **правка
  справочника двигает текст вместе со ссылкой** — переименование марки тем
  же запросом переписывает `elements.mark` и `contract_lines.mark`. Разойтись
  эти два поля не должны; если разойдутся, сверять будет нечего.
- **Обработка наполнения регистр НЕ сворачивает.** «К-1» и «к-1» становятся
  двумя записями, как лежат в данных. Свернуть их — решение человека: какое
  написание правильное, из данных не следует.
- **Удаление записи справочника: проверка → замена → удаление, одной
  транзакцией** (`app/dict_delete.py`, один движок на все справочники).
  Ссылки не обнуляются, а переводятся: внешние ключи в схеме почти везде
  `ON DELETE SET NULL`, то есть база молча оставила бы сотню изделий без
  контракта — это потеря данных, а не удаление записи.
- **Ссылка `mark_id` ПРОИЗВОДНА от текста марки**, вычисляется в одном месте
  (`resolve_mark_id`), через которое идут форма изделия, массовая правка
  через Excel и переимпорт чертежа (`_sync_mark_links`). Марки, которой нет
  в справочнике, запись заводится: справочник — множество марок, которые в
  данных есть, а не отдельный список, способный от них отстать.
- **Полнота проверки ссылок берётся из СХЕМЫ, а не из моего списка.**
  Ссылающиеся места перечисляет `PRAGMA foreign_key_list` по всем таблицам,
  реестр только ОБЪЯСНЯЕТ каждое. Необъяснённая ссылка с ненулевым счётчиком
  останавливает удаление. Иначе следующая миграция добавила бы колонку, а
  модуль молча оставил бы по ней висячие ссылки.
- **Замена обязательна для всего поддерева, и владельцы выбираются сверху
  вниз.** Договор-замена ищется только среди договоров выбранного
  контрагента-замены, спецификация — среди спецификаций выбранного договора.
  Список кандидатов и есть выражение иерархии; сервер сверяет присланную
  замену ИМЕННО С НИМ, а не с фактом существования записи. Смена выбора у
  владельца сбрасывает выбор у подчинённых.
- **Объект и проект удаляются только пустыми, замена для них не
  предлагается.** «Замена объекта» была бы слиянием двух зданий с разными
  чертежами, сетками осей и координатами. Отказ показывает, что именно
  держит запись, — это и есть польза кнопки.
- **Право — администратор сервиса**, независимо от роли на объекте:
  необратимо, а контрагент сквозной для всех строек.

## Excel-обмен

- **Массовая правка реквизитов** (`app/element_bulk_edit.py`): выгрузить →
  править → загрузить → отметить флажками. Ключ — `element_uid`. Валидация
  Excel не гарантия (буфер её обходит) — всё проверяет сервер; `apply` НЕ
  перечитывает файл. `manual_fields` помечает ТОЛЬКО реально изменившиеся
  поля, иначе первая загрузка объявит всю базу правленной вручную. Проверки
  полей — `app/element_fields.py`, на них переведена и одиночная правка
  («в форме нельзя, а через файл прошло»). «Учитывать фильтр схемы» при
  выгрузке (08-04): отбор считает КЛИЕНТ и шлёт список id, поэтому выгрузка
  POST; отсев по id — в PYTHON, `id IN (...)` на тысячах упрётся в лимит
  переменных SQLite. СВЕРКА файла список не получает никогда.
- **Массовая правка ИСТОРИИ СТАТУСОВ** (`app/status_bulk_edit.py`, второй
  режим той же формы): с 08-03 строка на ЗАПИСЬ, а не матрица «изделие ×
  статус» (матрица не показывала автора и время и не умела повтор статуса).
  Ключ — № записи; пустой номер при заполненном UID = новая запись,
  несовпадение номера и UID = отказ. Удалить запись файлом нельзя. Ровная
  полночь → полдень. Порядок статусов считается по ИТОГУ (база + файл), у
  изделий с повтором — предупреждение, а не отказ.
- **Перенос базы ЦЕЛИКОМ — не Excel-обмен, а снимок файла**
  (`app/db_transfer.py`, 08-11, четвёртый режим той же формы). Три первых
  режима ДОПОЛНЯЮТ базу приёмника срезом данных — для переноса боевых
  данных на тестовый сервер это рвёт целостность (чужие id рядом с
  местными, ссылки в пустоту). Здесь ЗАМЕНА: архив .zip = файл базы
  (`Connection.backup()`) + `uploads/` + манифест. Инварианты: (1) загрузка
  в ДВА приёма — сверка «сейчас/приедет» по каждой таблице и только потом
  замена с кодовым словом, числа сверки считаются по самому файлу базы, а
  не по манифесту отправителя; (2) перед заменой ВСЕГДА служебная копия
  (вид `auto_before_transfer`), прежние вложения не удаляются, а отъезжают
  в `data/backups/uploads_<время>`; (3) сразу после замены — `init_db()` и
  обработки релиза, как после восстановления из копии: снимок приезжает с
  более старой схемы. Снимок НОВЕЕ кода приёмника не лечится ничем —
  миграции назад не ходят. Всё, что приходит по HTTP, проверяется ДО того,
  как что-то заменено: имена в архиве (выход за папку, зип-бомба),
  `PRAGMA quick_check`, наличие таблиц сервиса.
- **`ws.max_row` в openpyxl — не счётчик, а `max()` по всем ячейкам.**
  Обращение в цикле по строкам это O(n²): выгрузка реквизитов строилась 88 с
  вместо 1,7 с, «График поставки» до уровня марок — 27,5 с вместо 7,8 с.
  Номер строки в цикле вести СВОИМ счётчиком; во всех четырёх выгрузках
  (`element_bulk_edit`, `reports`, `report_delivery`, `report_my_work`) это
  сделано, новые писать сразу так. Осторожно: `ws.append([])` двигает
  внутренний `_current_row`, но НЕ `max_row` — они расходятся.
- **Формат дат: в базе `ГГГГ-ММ-ДД`, наружу `ДД.ММ.ГГГГ`.** В Excel дата —
  НАСТОЯЩАЯ дата с числовым форматом, а не текст: те же файлы грузятся
  обратно. Исключение — `<input type=date|datetime-local>`.
- **Описание формата живёт рядом с кодом, который его проверяет**
  (`app/import_templates.py` — одно место на все шесть загрузок, заголовки
  образцов берутся из тех же `REQUIRED_HEADERS`/`columns_spec()`).
  Разойдясь, они уже отправили все 671 строку графика MS Project в
  «пропущено».

## Интерфейс: фильтры, подписи, 2D/3D

- **Фильтры на клиенте — «полный список значений всегда виден, недоступное
  посерело»**, а не сужается (фикс двух багов самоблокировки).
  `PLACEMENT_CASCADE_GROUPS` — при расчёте доступности одной категории
  группы фильтр остальных игнорируется. Клик по потомку при выключенном
  родителе включает и родителя.
- **Справочники на сотни позиций — master-detail** (09-07): слева список по
  строке на запись, справа поля ТОЛЬКО выбранной. Прежние две модалки со
  списком раскрытых карточек при двух сотнях объектов переставали быть
  списком: поля всех записей открыты сразу, найти нужную можно лишь
  прокруткой. Оттуда же переключатель объекта в тулбаре: поиск, «недавние»
  (за пользователем на сервере, как `last_object_id`) и свёрнутые группы
  проектов вместо плоского перечня всех объектов всех проектов.
- **Правку в справочнике показывает РАЗДЕЛ ПРАВ, а не системная роль**:
  раздел «Проекты и объекты» с 08-14 выдаётся любой роли, и проверка
  `role === "admin"` оставляла такому человеку форму только для чтения —
  сервер правку принимал, а интерфейс её не предлагал.
- **Правка меняет только присланные поля** (`model_fields_set`, 09-07):
  прежние обработчики писали все колонки подряд, и форма, не знавшая про
  поле, молча его затирала. Так терялось описание проекта.
- **Карта проектов — ОТЧЁТ, а не рабочее место** (09-07): рабочее место это
  раскладка вокруг ТЕКУЩЕГО объекта с проверкой раздела на объекте, а карта
  показывает все доступные сразу. Подложка офлайн, файлом PMTiles на диске:
  тайловых сервисов в закрытом контуре нет. Файла нет или он повреждён —
  карта работает, точки рисуются на пустом фоне.
- **Клик по точке карты открывает свойства, а не переключает объект** сразу
  (09-08): решение пересмотрено против первой версии. С карты смотрят «что
  там за площадка», не всегда намереваясь сейчас на ней работать —
  переключение осталось отдельным явным действием («Перейти к объекту»
  внутри карточки), доступным, только когда доступ на объект у человека
  есть. Карта заведена в тулбар второй кнопкой рядом с крошкой выбора
  объекта — прежде была только в «Отчётах».
- **Каждая карта MapLibre держит свой графический контекст**, а браузер
  выдаёт их считанные десятки: перед пересозданием карты старую обязательно
  уничтожать. Иначе карты перестают строиться молча, без единой ошибки.
- **Справочник «Элементы» — реестр колонок `_EC_SQL`** (`app/main.py`,
  08-04): ключ → выражение SQL, по нему и отбор, и сортировка, и значения
  выпадашки; отбор приходит произвольными парами «ключ=значение», неизвестный
  ключ — 400. Сентинелы `__filled__`/`__none__` годятся ЛЮБОЙ колонке. Наборы
  значений — ОДНИМ проходом по отобранным строкам, не `DISTINCT` на колонку.
- **Ключи значений фильтра — СОСТАВНЫЕ (тип+текст)**, не голый текст:
  подтип «на отм. +15.000» намеренно переиспользуется между типами, и голый
  текст скрывал элементы ДРУГОГО типа. Тот же приём у стоянок; сентинел
  «нет значения» тоже обязан быть составным, иначе марки утекают между
  типами через общий `marksBySubtype`.
- **Фильтр по датам СМР — диапазоном, не чекбоксами** (`state.dateFilters`,
  отдельный предикат): у даты сотни значений, и отбор по ним — диапазон, а
  не набор исключённых. Предикат зовётся из ДВУХ мест (видимость и посерение
  чужих категорий). Фильтр «Изменения» — ТРЕТЬЯ структура: критерий не
  свойство элемента, а событие журнала.
- **Подписи и зоны выключены по умолчанию** — быстродействие.
- **В горячем пути (зум, пан, кадр 3D) не читать раскладку вообще.** Новый
  `viewBox` стоит 0,01 мс, а чтение `clientWidth` СРАЗУ ПОСЛЕ него — 65 мс:
  синхронный пересчёт всех 56 648 узлов SVG, и таких читателей на тик было
  трое. Размер кэширует `readStageMetrics`, читает только `ResizeObserver`
  (вызывается ПОСЛЕ раскладки). 08-04: `setView` 82,75 → 0,15 мс.
- **Скрытый `display:none` узел SVG стоит браузеру столько же, сколько
  показанный.** Замер при зуме 2000 %, одна смена `viewBox`: 9422 подписи в
  дереве — 65,75 мс независимо от того, скрыты они; слой пуст (узлы ВНЕ DOM)
  — 0,13 мс. Поэтому подписи вне кадра и мельче порога читаемости
  ОТСОЕДИНЯЮТСЯ от DOM (`updateViewportCulling`), а не прячутся: первая
  версия прятала классом и не дала ни миллисекунды. Следствие: искать
  подписи `document.querySelectorAll` нельзя — только по картам
  `state.stickerById`/`labelById`/`subLabelById`.
- **Порог читаемости `MIN_LABEL_READABLE_PX`=12 px — ОДНА константа на 2D и
  3D**: подпись либо читается, либо нет, от режима это не зависит.
- Фигуры отсекаются КЛАССОМ `.shape-culled` (их узлы дёшевы — 1,49 мс на все
  9422), не `style.display`: инлайновым стилем уже управляет фильтр
  размещения.
- **2D-подпись — «наклейка» на реальном контуре**: размер шрифта в мировых
  мм от ширины контура, марка и допстрока ОДНОЙ строкой, поворот вдоль
  длинной стороны — с приведением угла к (−90°, 90°], иначе произвольный
  порядок точек контура в DXF даёт θ+180 и подпись вверх ногами (было у 492
  из 9420, 08-04); внутренний отступ подложки входит в лимит ширины (без
  него переполнялись 1322 колонны). Подписи — в отдельном слое
  `#labels-layer` после `#elements-layer`: иначе фигура позднего яруса
  навсегда перекрывала подпись раннего.
- **3D: вся геометрия слита в ОДИН буфер** (07-30): 13 438 вызовов
  отрисовки и 61,3 мс на кадр → 4 вызова и 0,1 мс. Треугольники ничего не
  стоят, стоят вызовы. Цвет статуса — в ВЕРШИНАХ, клик — по таблицам
  «треугольник → элемент», подсветка — отдельным мешем поверх, скрытие —
  пересборкой. Ловушка: `scene.updateMatrixWorld()` обходит детей независимо
  от видимости; помогает только ОТСОЕДИНЕНИЕ контейнера, `matrixWorldAutoUpdate
  = false` — нет.
- **3D-наклейки слиты в общий буфер** (08-04), как до того геометрия: их
  25 568 на файл (плита 2, ригель 4, колонна 4), и меш на каждую давал
  25 531 вызов отрисовки и 105 мс на кадр. Надписи — в атлас 2048×2048
  (`buildDecalAtlas`, 674 уникальные на 5 страниц), буфер — один на пару
  (тип элемента, страница): тип отдельным мешем НАМЕРЕННО, тумблер «Подписи»
  должен быть переключением видимости, а не пересборкой. Стало 24–39 вызовов
  и 0,13–1,2 мс.
- **Вся покадровая работа наклеек — в вершинном шейдере**, на кадр остаётся
  одна константа: отсечение отвёрнутых граней (порядок вершин + `FrontSide`,
  отсекает видеокарта), порог читаемости (мельче `MIN_LABEL_READABLE_PX` —
  схлопнуть квад в точку), разворот текста (поворот на 180° вокруг нормали =
  перестановка UV `u→uMin+uMax−u`). **Правило разворота обратное для грани,
  смотрящей ВНИЗ**: горизонтальная проекция экранного «верха» меняет знак
  при взгляде снизу (дальний край потолка уходит к горизонту, а не вверх
  кадра) — иначе снизу все подписи вверх ногами. В `aUp` кладётся «верх»,
  домноженный на знак `normal.y`; углы квадрата строятся по НЕдомноженному. Непрозрачная подложка при нечитаемом
  тексте застилала модель — это и читалось как «непрорисованные части».
- **Своему `ShaderMaterial` в 3D обязательны чанки `logdepthbuf_*`**: в
  обычном режиме рендерер собран с логарифмическим буфером глубины, и
  материал без них пишет глубину в другой шкале — объект уходит под
  геометрию. Симптом обманчив: в «3D лёгком» (буфер выключен) всё рисуется.
- **На верхнем уровне `app.js` ничего из `THREE` не трогать**: библиотека
  приходит динамическим `import`'ом при первом входе в 3D, до того
  `THREE === null`, и исключение обрывает выполнение ВСЕГО файла. Объявления
  функций всплывают, поэтому симптом обманчив — «приложение не стартует, а
  функции из консоли работают» (08-04).
- **3D-меш строится НАПРЯМУЮ из `element.outline`, без офсетов** (требование
  заказчика: «объект должен точно соответствовать геометрии из входящих
  данных»). Рёбра — `LineSegments2` фиксированной пиксельной толщины, ОДИН
  общий материал на все элементы, чёрный. Ребро лежит на грани, z-fighting
  снимают `polygonOffset` + `logarithmicDepthBuffer`.
- **Сетка осей в 3D** (08-03): линии — ОДИН `LineSegments`, подписи — спрайты
  на ОБОИХ концах. Кегль ЭКРАННЫЙ (`sizeAttenuation: false`, тот же
  `MAX_AXIS_FONT_PX`, что в 2D): мировой размер здесь невозможен в принципе —
  в 2D шрифт оси постоянен в пикселях, то есть 55 мм на общем плане и метры
  на приближении. Прореживание при слипании — по ЭКРАННОМУ расстоянию, в
  `render3DFrame` (в перспективе единого мирового шага не существует).
- **3D-рендер по требованию**, а не безусловный цикл rAF: браузер жёг батарею
  при полном простое. `setViewMode("2d")` обязан ОБНУЛЯТЬ `animationFrameId`.
  Режим «3D лёгкий» = профиль «слабая машина или RDP» (без рёбер,
  `pixelRatio` 1, без antialias и логарифмического буфера, потолок 20 к/с) —
  свойства контекста, поэтому смена профиля пересоздаёт рендерер.
- **Координаты 3D**: `world.X = dxf.x`, `world.Z = -dxf.y` (знак — уже был
  баг зеркальности). Рамка выделения и панорамирование берут масштаб из
  `getScreenCTM()` группы `#flip`: пропорция «пиксель контейнера → единица
  viewBox» игнорирует `preserveAspectRatio` и ловит область уже нарисованной.
- **Содержимое панели «Действия» СОБИРАЕТСЯ из разметки выпадающего меню**
  (`renderActionsPanel`), а не описывается вторым списком тех же сорока
  пунктов. Раскладка — три явные колонки, flex, а не grid: скрытая ролью
  колонка должна выпадать, а не оставлять пустую дорожку.
- **`.admin-only` — ведение СЕРВИСА, `.object-admin-only` — операции внутри
  объекта, `.object-contract-only` — контрактные справочники (08-04, роль
  не ниже «Комплектовщик»; администратору сервиса видно всегда, иначе на
  пустой системе первого контрагента завести некому).** Гасятся разными
  ролями; путать нельзя (08-02 на этом нашлась дыра: пять эндпоинтов
  правки проверяли системную роль и не проверяли объект вовсе).
- **Цвет подписей марок задаёт АДМИНИСТРАТОР СЕРВИСА** (08-04), как и цвета
  статусов; хранится по-прежнему за пользователем (`users.label_color`) —
  сменился только тот, кто вправе менять, самообслуживания больше нет.
- **«Настройки» разбиты на «Общие» / «ЖБИ» / «МФР» вложенными подменю**
  (09-10, живой запрос пользователя) — тот же приём, что у «Справочники →
  Проект и объект/Зоны/…»: пустая подгруппа гасится сама
  (`applyRolePermissions`, счёт снизу вверх по глубине `.submenu`). «Общие»
  — применимо к любому объекту независимо от типа (личная гамма
  интерфейса, пароль, сеансы); «ЖБИ»/«МФР» — только своему контуру учёта.
  Сюда же переехала «Цвета модели» (была кнопкой «Цвета» в тулбаре «Модели
  МФР») — раздел `revit_model` уже гасится на объектах не-МФР
  (`_ТОЛЬКО_МФР`, `app/features.py`), второй раз это правило описывать не
  пришлось.

## Отчёты и журнал

- **Категория события журнала** (08-20) — свойство КОДА ДЕЙСТВИЯ, а не
  решение места вызова: реестр `app/activity_actions.py` держит подпись и
  категорию рядом, полтораста мест вызова о категории не знают. Явно она
  передаётся только там, где зависит от ЗНАЧЕНИЯ, а не от кода: ответ 500 —
  «Ошибка», ответ 403 — «Отказ», действие у них одно. «Ошибка» (сбой
  программы) и «Отказ» (штатно не дали) РАЗДЕЛЕНЫ намеренно — их разбирают
  разные люди. Неудачный вход остаётся в «Безопасности»: подбор пароля
  смотрят одной выборкой с успешными входами.
- **Ошибки регистрируются сквозным обработчиком** (`app/error_log.py`,
  08-20), а не `try/except` по роутам: расставленные по местам ловушки
  ловят ровно те сбои, которых ждали. Ответ пользователю при этом НЕ
  меняется — обработчики вызывают штатные функции FastAPI, а трассировка
  по-прежнему уходит в лог uvicorn. Пишутся все неуспешные ответы (решение
  пользователя), поэтому обязательным становится подавление повторов: окно
  60 секунд на связку «кто — куда — какой код», подавленные считаются и
  попадают в следующую запись.

- **Отчёт «Статусы» и «Динамика»** — одни и те же эндпоинты для формы и для
  панели в сайдбаре: числа не могут разойтись. Панель не считает скрытую
  вкладку. Период графика на панели режется на клиенте, в форме — СЕРВЕРОМ
  (у формы есть XLSX и PDF, три места обрезки разъехались бы); границы
  приводятся к понедельнику. Кривая факта обрывается на отчётной дате —
  иначе полка читается как «факт известен и не растёт».
- **Отчёт «График поставки»** (`app/report_delivery.py`): в ячейке ТРИ числа —
  потребность (дата начала СМР), план, факт: три независимые шкалы дат одного
  изделия. Изделия вне периода не отбрасываются («Ранее»/«Позже»), иначе итог
  не сходится; «Без даты» по ФАКТУ не бывает — изделие без фактической даты не
  поставлено. Непокрытая потребность считается ПО ИЗДЕЛИЮ, а не сравнением
  чисел в ячейке. Больше 400 колонок — отказ с объяснением, не молчаливая
  подмена шага.
- **Признак «отчёт в разработке»** (08-05): `IN_DEVELOPMENT` в модуле отчёта
  и `inDevelopment` в `REPORTS` на клиенте, настройки в интерфейсе НЕТ — это
  состояние разработки, а не решение администратора. Доступ признак **не
  ограничивает** (решение пользователя): отчёт виден тому же кругу, просто с
  пометкой. Пометка обязана дойти до ТРЁХ мест сразу — название (меню,
  вкладка), форма отчёта, выгруженный файл (текст внутри XLSX/PDF + имя
  файла и листа): файл уходит из системы, и предупреждение только на экране
  до его читателя не доходит. Сейчас стоит у «Графика поставки».
- **Отчёт «Статус комплектации»** (`app/report_completion.py`): ПЛОСКИЙ
  перечень по образцу заказчика, строка на КАЖДОЕ изделие (группировки нет,
  08-03), с колонками GUID (`element_uid`) и «Статус». Кран и стоянка
  НОМЕРОМ зоны, «требуемая дата» — начало СМР, не завершение. Статус залит
  цветом ИЗ НАСТРОЕК (`status_colors`, та же таблица, что красит схему),
  цвет текста — по яркости заливки, одним порогом на экран, Excel и PDF; в
  PDF заливка идёт ПРОБЕГАМИ (построчная стоила +5 с на 9422 строках).
  Галочка «учитывать фильтр схемы» включена по умолчанию, состояние её —
  СВОЁ у каждого отчёта (`reportUseFilter`), иначе вкладка меняет отбор
  соседних.
- **«Требуемая дата поставки» = прогноз, а не поле изделия** (08-30, живой
  запрос): оба вида «Статуса комплектации» берут её из последней
  актуализации графика СМР (`schedule_version_dates.smr_start_date`, джойн
  `FORECAST_JOIN` в `app/schedule_versions.py`), а не из
  `project_smr_start_date`. Поле — то, что обещали ИЗНАЧАЛЬНО; снабжение
  работает по действующему графику, и на обезличенной копии эти два числа
  расходились у 9148 изделий из 9151. Версия — всегда последняя, ищется по
  объекту ИЗДЕЛИЯ (в отбор попадают разные объекты). Прогноза нет —
  **ячейка пустая**, подстановка директивной даты смешала бы в одной
  колонке два срока; сколько таких позиций и почему, говорит строка над
  таблицей, в PDF — под заголовком, в XLSX — примечанием на заголовке
  колонки (строкой сверху нельзя: лист начинается с шапки, по нему строят
  сводные). Перечень и сводная обязаны брать ОДНО число: разойдись
  источники, изделие вставало бы в разные месяцы при переключении вида.
- **Потребность ВЕЗДЕ считается по прогнозу** (08-31, решение
  пользователя — продолжение правки 08-30). На тот же `FORECAST_JOIN`
  переведены «График поставки» (`app/report_delivery.py`, шкала
  «Потребность» и подсветка непокрытого), «Аналитическая справка»
  (`app/report_analytics.py` — а с ней и очередь завода `app/capacity.py`,
  она берёт даты оттуда) и «График контрактации и поставки»
  (`app/report_contracting.py`). **«Динамика» НЕ переведена намеренно**: там
  план и прогноз — две РАЗНЫЕ кривые (`cumulative_forecast`), и перевод
  плана слил бы их в одну, а «насколько отстаём» исчезло бы с графика.
  Запасного варианта нет — прогноза нет, значит нет и даты потребности
  (решение пользователя 08-31, выбрано осознанно вместо подстановки
  директивной даты). Практически это касается УЖЕ СМОНТИРОВАННЫХ изделий:
  пересчёт графика от факта исключает их из актуализации, и по закрытым
  этапам «поставлено вовремя» больше не считается. Каждый отчёт говорит об
  этом сам: «График поставки» — колонкой «Без даты», справка — строкой
  «не размечено», «График контрактации» — строкой `warning` (кривая
  потребности до итога не дорастает, и молча это выглядело бы ошибкой
  расчёта).
- **Отчёт строится по АКТИВНОМУ объекту** (08-30, живой запрос): к сужению
  по чертежу добавлено сужение по `object_id`. Один признак был хрупок —
  при пустом `source_file` (объект без чертежа оставляет в `state.sourceFile`
  файл ПРЕДЫДУЩЕГО объекта) администратор сервиса получал перечень по всей
  базе.
- **Отчёт «Аналитическая справка»** (`app/report_analytics.py`, 08-11):
  потребность — из МОДЕЛИ по дате начала СМР; ярус — отметка яруса стоянки
  (`zone_levels.elevation_mm`), этап — кран + стоянка + этаж (блок графика
  MS Project); зачёт контрактации — ПО МАРКАМ, колонка «по типу»
  справочная; выкупленное раскладывается по этапам в порядке дат
  потребности. «На площадке» — фактическая дата, не статус («В
  производстве»/«Отгружено» не ведутся). Позиция, чьи изделия уже на
  площадке, а контракта нет, — «поставлено, не закрыто контрактом»:
  расхождение УЧЁТА, в выводы и в тревожные показатели не идёт. Строится по
  объекту ЦЕЛИКОМ — фильтр схемы не учитывается (решение пользователя).
- **Сроки изготовления считаются ОЧЕРЕДЬЮ завода, а не нормативом в днях**
  (`app/capacity.py`, 08-11): норматива у заказчика нет, есть темп завода
  (шт./календарный день, у контрагента; в контракте — переопределение).
  Пусто ≠ ноль: пусто = «сроки неизвестны», и вывод «успевает / не
  успевает» не делается вовсе. Непоставленный объём завода по типу идёт в
  очередь по датам потребности; плановая дата поставки ГЛАВНЕЕ расчёта, но
  мощность занимает; дефицит без контракта — отдельная очередь со средним
  темпом по типу среди заводов ЭТОГО объекта. Заказы на другие стройки
  системе неизвестны — оценка оптимистичная, и отчёт говорит это сам.
- **Отчёт не должен падать на данных КАРТОЧКИ ОБЪЕКТА** (свободный JSON):
  одно негодное значение роняло «Динамику» в трёх местах сразу
  (`_week_start_or_none`, терпимый `_ru_date_short`); обезличивание даты
  больше не затирает (08-03).
- **Персональные настройки — на СЕРВЕРЕ** (`users.*`, в `/me`), не в
  localStorage: за машиной работают посменно. Цвет подписей (с 08-04 задаёт
  админ сервиса, хранится всё так же за пользователем), гамма, ракурс
  3D (30°/−30°, ноль поворота = числовые оси вертикально), ознакомление с
  «Что нового» (хранится ВЕРСИЯ, не флажок).
- **Отчёт «Моя работа» и фильтр «Изменения»** (`app/report_my_work.py`):
  источник — ЖУРНАЛ, а не `status_history` (её `changed_at` — рабочая дата) и
  не `updated_at` (нет автора). Отбор ОДНОЙ функцией на отчёт и на фильтр.
  Отбор действий — денилист: новое действие обязано попадать в отчёт само.
- **Журнал действий пишется В ФОНЕ** (`app/activity.py`): отдельный поток,
  очередь, пачки до 500 в транзакции; не `BackgroundTasks` (занимают поток
  пула). Запись — внутри `apply_status_change`, а не в эндпоинтах.
- **Журнал покрывает ВСЕ меняющие данные эндпоинты** (82 из 82, проверено
  AST). Массовая операция пишет И сводку, И событие на каждое реально
  изменившееся изделие, связь — общий `request_id`.
- **Отметки времени: сервер пишет UTC, клиент переводит в местное**
  (`activityTimeLocal` — одна функция на все места). Новый код —
  `datetime.now(timezone.utc)`, не `utcnow()`.
- **Автообновление — опрос `/changes` раз в 15 с, а не SSE/WebSocket**:
  синхронные роуты, открытое соединение заняло бы поток пула на каждого.
  Метка времени — ИЗ ОТВЕТА СЕРВЕРА, никогда с часов браузера.

## Обновление и обработки данных

- **Обновление не требует человека на сервере** (08-04). Всё, что новая
  версия должна сделать с накопленными данными, едет вместе с кодом и
  выполняется первым же стартом: копия базы → миграции схемы → обработки
  релиза → версия базы догоняет версию кода. Обработка — запись в реестре
  `app/release_tasks.py`, а не скрипт в `scripts/`: скрипт нужно кому-то
  запустить, а запускать некому.
- **Миграция схемы падает — старт падает; обработка данных падает — старт
  продолжается.** Разная природа: без колонки код не работает вовсе, а без
  дозаполнения — работает, просто часть данных в старом виде. Упавшая
  обработка видна в «Что нового» и повторяется кнопкой администратора
  (решение пользователя 08-04); версия базы при этом НЕ повышается.
- **Версия одна на всё** — верхняя запись `app/changelog.py`; в базе
  `app_settings.db_release_version`. Совпали версии = обновление завершено.
  Но решение «выполнять обработку или нет» принимается ПО ИМЕНИ из
  `release_tasks`, а не сравнением версий: обработка, дописанная в уже
  вышедшую версию, при сравнении номеров была бы пропущена молча.
- **Обработка обязана быть идемпотентной**: условие пишется от состояния
  данных («где ещё не заполнено»), а не от факта «я уже выполнялась» —
  администратор может повторить её кнопкой в любой момент. Проверяется это
  не на глаз: `scripts/dry_run_migration.py` прогоняет обработки на копии
  боевой базы ДВАЖДЫ — второй раз принудительно все, — и расхождение
  счётчиков строк между проходами объявляет проблемой (08-04).
- **Релиз только ДОБАВЛЯЕТ.** Новые поля и таблицы заполняются из старых,
  старое в том же релизе не удаляется: пока данные не сконвертированы у всех
  и не проверены, удалять нечего. Уборка отжившего — отдельный вид
  обработки (`KIND_CLEANUP`), при старте не выполняется никогда, ждёт кнопки
  администратора и снимает перед собой копию базы.
- **Перед обработкой данных всегда есть свежая копия базы** (08-10,
  `backup_before_tasks`). Копия по смене версии (`backup_before_update`)
  прикрывала не все случаи: обработка, ДОПИСАННАЯ в уже вышедшую версию,
  выполняется по имени и на сервере, где версии уже совпали, — а копия по
  версии там не снимается; повтор кнопкой администратора идёт вообще
  спустя дни работы. Поэтому обработки снимают копию сами, ОДНУ на пачку.
  Двух копий подряд при обычном обновлении не бывает: копия по версии
  снята с более раннего состояния (до миграций схемы) и откатывает строго
  больше, поэтому обработки того же старта своей копии не делают. По
  кнопке копия снимается ВСЕГДА — между стартом и нажатием шла работа
  пользователей.
- **Порядок в реестре — хронологический (порядок выполнения), на экране —
  обратный** (08-10): поздняя обработка может опираться на результат
  ранней, а в «Что нового» читают «что приехало последним обновлением».
  Сортируется копия списка в `status()`; сам реестр не трогается. У каждой
  обработки на экране стоят её версия и дата.
- **Копия базы снимается перед ЛЮБОЙ загрузкой данных из файла** (08-21,
  `backup_before_import`, вид `auto_before_import`). Восемь путей: чертёж
  (обе фазы), история, контрактация, график MS Project, массовая правка
  через Excel, настройки, папка `Input` (одна на пакет). Момент — после
  разбора и сверки, ДО первой записи: фаза «что изменится» не пишет и
  копии не делает, отказ на разборе её тоже не оставляет. **Копия не
  снялась — загрузка не начинается** (данные важнее загрузки). Обработчик
  `BackupError` один на всё приложение, а не try/except в каждой из восьми
  точек: восемь одинаковых обёрток разошлись бы на первой правке.
- **Копии «перед загрузкой» — СВОЯ очередь ротации** (десять последних,
  08-21), отдельно от пяти служебных. Правило «пять на всё сразу» (08-19)
  принималось под редкие события — релиз, восстановление, пересборку.
  Загрузка частая: пять загрузок за день вытеснили бы все
  `auto_before_update`, то есть ровно то, ради чего очередь заводилась.
  Разделение сохраняет прежнее решение, а не отменяет его.

## Безопасность и вход

- **`password_hash IS NULL` = вход ЗАПРЕЩЁН** (не «пустой пароль»), включая
  дефолтного `admin` из `schema.sql`. Сложность проверяется при установке,
  НЕ при входе. `set-password` удаляет все другие сессии. Восстановление —
  `scripts/reset_password.py` (в обход API/UI).
- **Два счётчика перебора**: (логин+адрес) 5 и (адрес) 50 — за корпоративным
  NAT сервер видит один адрес на весь офис. Учётная запись не блокируется
  никогда, успешный вход не сбрасывает счётчик АДРЕСА.
  **`must_change_password` проверяется НА СЕРВЕРЕ**: пока поднят,
  `get_current_user` отдаёт 403 на всё, кроме `/me`, смены пароля и выхода;
  доменной учётке не ставится никогда.
- **Доменная авторизация** (`app/ldap_auth.py`): bind ОТ ИМЕНИ ВХОДЯЩЕГО,
  служебной учётки нет. Способ входа — признак КАЖДОГО пользователя, без
  отката «домен → локальный пароль». **Пустой пароль отклоняется ДО
  обращения к домену** — AD отвечает на него УСПЕХОМ. Причина отказа — по
  подкоду AD из текста ошибки (код 49 общий у трёх разных причин). `ldap3`
  импортируется мягко. Аварийный выход — `scripts/reset_password.py`
  возвращает учётку на `local`.
- **Режим «Зайти под пользователем»** (08-05, `app/impersonation.py`) —
  отладка чужих прав. **НЕ подмена cookie**: cookie одна на браузер, и
  подмена превратила бы в подопечного ВСЕ вкладки администратора, включая
  ту, из которой режим включён. Отдельная сессия с заполненным
  `sessions.impersonator_user_id`, токен — в `sessionStorage` (единственное
  хранилище, своё у каждой вкладки) и уходит ЗАГОЛОВКОМ. **По cookie такая
  сессия не подходит принципиально** (`get_user_by_session` фильтрует по
  `IS NULL`): иначе токен, переложенный в cookie, дал бы вход под чужой
  учётной записью без единой отметки в журнале. Заголовок вместо cookie
  попутно закрывает CSRF. Контекст ставит **чистый ASGI-посредник**, а не
  зависимость FastAPI: синхронная зависимость выполняется в потоке пула с
  КОПИЕЙ контекста, и `contextvar` до обработчика не доехал бы — а на нём
  держатся отметки в журнале и в `status_history`. Токен живёт 4 часа и
  приезжает в `#хэше` (на сервер не уходит, в Referer не попадает), после
  чтения стирается из адреса. Отметка автора: журнал — две колонки
  (`user_*` = подопечный, `impersonator_*` = администратор), аудиторские
  снимки (`status_history.changed_by`, вложения, копии) — одна строка
  «Админ (от имени: X)» через `audit_display_name`. Запрещены смена пароля,
  обрыв сеансов и вложенный вход. Отладочный сеанс ВИДЕН подопечному в его
  «Моих сеансах» — скрытый был бы слежкой.
- **Проверять чтение так же, как запись** (аудит 08-03): обе находки второго
  прохода были парами «писать нельзя, читать можно». Списки, которые объект
  не спрашивают, отдавали все стройки любому вошедшему — сужаются
  `accessible_object_ids`. Барьер имён полей стоит в самой `write_fields`, а
  не у вызывающих.
- **CSP `script-src 'self'`**: весь JS вынесен в `app.js`, единственное
  исключение — importmap по точечному sha256 (см. стоячие инструкции).
  `style-src` с `'unsafe-inline'` осознанно. Лимит загрузки —
  `app/upload_limits.py`. `X-Forwarded-For` доверяется только при
  `ZHBI_TRUST_PROXY_HEADERS=1`. `/docs` выключен по умолчанию.
  Карта (09-07) добавила `worker-src`/`child-src` с `blob:` и `blob:` в
  `img-src` — MapLibre заводит рабочие потоки из blob-URL. Внешних адресов в
  политике нет, ПОКА администратор не включил «Карту и адреса из
  интернета» — один выключатель на тайлы карты И геокодирование по адресу
  (09-08): тогда и только тогда добавляются ровно два вшитых в код адреса
  OpenStreetMap. Настройка с произвольным адресом не делается намеренно —
  это дыра в политике размером с любой сайт.
- **`Cache-Control: no-cache` на `/static/*`** — `StaticFiles` его не
  выставляет, и браузер несколько раз путал живую проверку правок.
- **TLS/`secure`-cookie выключены НАМЕРЕННО** (подтверждено 07-23): контур
  корпоративный, без выхода наружу. Вопрос повторно не поднимать без новой
  причины.

## Внешние 3D-модели объекта (благоустройство, 09-10)

- **Модель принадлежит ОБЪЕКТУ, не проекту** (решение пользователя,
  меняет более раннюю версию задания) — проект в системе только
  группирует объекты и собственных данных не имеет, как и везде. Таблица
  `object_external_models`, права — раздел `external_models`
  (`app/features.py`, объектный, по умолчанию не ниже администратора).
  Загрузка — «Действия → Обмен данными → Загрузить из FBX», не карточка
  проекта/объекта.
- **`placement_mode='unreferenced'` — единственный режим.** Ненулевые
  координаты FBX, имя файла, похожие габариты не считаются доказательством
  привязки к координатам объекта. Автогеопривязка отложена.
- **Координатный контракт** (F → C → P → V, мм):
  `C = 1000·(F.x, −F.z, F.y)` — Y-up → Z-up без второго поворота геометрии
  (FBXLoader.matrixWorld уже учёл поворот моделей файла).
  `source_anchor` = центр горизонтального габарита и ВЕРХ модели в C
  (не низ — изменено 2026-09-11, см. ниже), вычисляется ОДИН раз при
  разборе и больше не меняется (не зависит от recenter/offset).
  `object_anchor` = центр горизонтального габарита САМОГО объекта
  (`app/object_geometry_bounds.py`, без текущих фильтров) — считает сервер,
  клиентскому bbox не доверяет; по Z всегда 0 (объект своей высотной
  отметки для этого не даёт). `P = (C − source_anchor)·Rz(поворот) +
  object_anchor + offset` (offset — три компоненты, X/Y/Z). Поворот —
  вокруг ЦЕНТРА модели (вокруг начала координат L-пространства, где она
  уже центрирована), хранится нормализованным в (−180°, 180°]. Адаптер
  ЖБИ: `V=(P.x,P.z,−P.y)` (поворот на -90° вокруг X). Адаптер МФР:
  `V=(P.x−origin.x, P.y−origin.y, P.z−низ)` — чистый перенос, без
  поворота осей.
- **ИСПЫТАНО И ОТКАЧЕНО (2026-09-11, тот же день): попытка убрать
  автоцентрирование целиком, доверившись координатам из FBX как есть**
  (`P = source_anchor + offset`, `source_anchor` = геометрический центр
  габарита по всем осям вместо центра объекта). Мотивация была разумной:
  сдвиг Model в реальном FBX (~13281,142,−10668 мм) визуально похож на
  реальные координаты объекта, а автоцентрирование по bbox объекта
  создавало на живом объекте эффект «модель под неверным углом»/
  «конструктив вне фасада». НО это сходство координат НЕ было проверено
  против настоящих координат объекта (та база — на боевом сервере
  пользователя, порт 8000, ассистенту недоступна) — предположение
  оказалось НЕВЕРНЫМ: после отключения центрирования модель на боевом
  сервере перестала быть видна вообще (уехала далеко от здания), удаление
  и повторная загрузка не помогли. Откачено ревертом коммита
  (`git revert`, коммит «Внешние 3D-модели: убрано автоцентрирование по
  объекту») в тот же день — вернулось `P = object_anchor + offset`,
  `kind`-зависимый anchor по Z, кнопка «Сцентрировать с объектом». Урок:
  визуальное сходство пары чисел — не доказательство совпадения систем
  координат; для объекта, к которому нет прямого доступа, такую гипотезу
  подтверждать ТОЛЬКО у пользователя на реальных данных, а не полагаться
  на правдоподобие. Проблема «модель под неверным углом» (см. следующий
  абзац после `kind='facade'`) на момент отката НЕ решена — нужен другой
  путь (ручной поворот точнее, либо расчёт угла по сетке осей объекта
  `object_grids`, обсуждалось с пользователем, но не реализовано).
- **Решение проблемы угла — «Совместить по точкам» (2026-09-11, тот же
  день, следующая итерация)**: НЕ автоматический расчёт по сетке осей
  (отклонён — сопоставление одной оси грида с направлением в FBX
  неоднозначно без дополнительных данных, задание прямо предупреждает не
  делать `rotation_deg = angle(object_grids)` вслепую), а РУЧНАЯ, но
  ТОЧНАЯ привязка по двум парам физических точек: пользователь кликает
  одну и ту же точку A на модели и на объекте, затем точку B — поворот и
  сдвиг считаются ОДНОЗНАЧНО из этих двух пар, не приближённо и не
  постепенной подгонкой мышью. Математика — `app/static/external-models/
  coordinates.js`:
  ```
  u = c2.xy - c1.xy; v = p2.xy - p1.xy
  theta = atan2(u×v, u·v)                  // math CCW, радианы
  rotation_deg = normalize(-theta·180/π)   // тот же знак, что везде в проекте
  offset.xy = p1.xy - projectAnchor.xy - Rz(theta)·(c1.xy - sourceAnchor.xy)
  ```
  Контрольный численный пример (Docs/fbx-placement-claude-prompt.md §6,
  A=(13280000,10660000), B.xy=(120000,230000), c1=(13281000,10662000),
  c2=(13291000,10662000), p1=(100000,200000), p2≈(108660.25,205000)) даёт
  theta=+30°, rotation_deg=−30°, offset≈(−19866.03,−32232.05) — проверено
  в `scripts/verify_external_model_coordinates.mjs` вместе с третьей
  (контрольной) точкой C=(13284000,10669000)→P≈(99098.08,207562.18).
  `canonicalToProject` (та же функция, что раньше без поворота) получила
  6-й параметр `rotationDeg` (по умолчанию 0, обратная совместимость) —
  контракт теперь `P = Rz(theta)·(C − sourceAnchor) + projectAnchor +
  offset`, ЧТО УЖЕ БЫЛО фактическим поведением `layer.js` (group.position +
  group.quaternion), просто раньше не было отражено в чистой математике.
  Эквивалентность формулы и РЕАЛЬНОГО применения `group.matrixWorld` в
  THREE.js проверена отдельно (не только переписанные формулы, задание
  §6) — тем же verify-скриптом, вендоренным `three.module.min.js`
  напрямую, для обоих адаптеров (МФР и ЖБИ).
  Снап к вершине — только у ТРЕУГОЛЬНИКА, в который попал луч (3 вершины
  хита сравниваются в экранных пикселях с местом клика), не обход всей
  геометрии на каждый pointermove — дёшево, достаточно для клика рядом с
  реальным углом/ребром конструкции. Точка на объекте — раскаст по ВСЕЙ
  сцене ЗА ВЫЧЕТОМ групп внешних моделей (не по конкретному мешу
  «настоящей геометрии» — обжегшись на непроверенном предположении о
  координатах в этой же сессии, решили НЕ предполагать, что известно,
  какие именно объекты в сцене МФР/ЖБИ представляют «объект», а
  исключить только то, что точно знаем — свои же слои).
  Перенос привязки на другую модель того же объекта (два FBX из одного
  источника — например, благоустройство и фасад) — `transferPlacementXY/
  Z` в `coordinates.js`: пересчитывает `t = projectAnchor_i + offset_i −
  Rz(theta)·sourceAnchor_i` (общий мировой сдвиг) и раскладывает его
  обратно в `offset_j` для anchors модели j — обе модели после переноса
  подчиняются одному `P=R·C+t`, несмотря на разные bbox-центры/Z-anchor.
  НЕ проверяет совместимость исходников — явное решение пользователя с
  подтверждением диалогом, не автоматическая догадка.
  Живая проверка: синтетическая сцена (маркеры-кубики вместо реального
  FBX/реальной геометрии объекта — анонимная копия не содержит здание,
  соответствующее тестовым FBX, см. запись выше) в МФР, реальные клики
  через `computer`-инструмент (не `dispatchEvent`) по вычисленным экранным
  позициям — раскаст, снап к вершине, расчёт, предпросмотр поворота/
  сдвига группы, «Применить»→запись в черновик, Esc→откат группы и
  `controls.enabled` — все отработали. Математика (включая контрольный
  пример и THREE.js-эквивалентность) проверена unit-тестами со 100%
  проходом. НЕ проверено: реальный FBX + реальная геометрия объекта на
  боевом сервере (недоступен ассистенту — см. предыдущую запись, тот же
  урок); кнопка «Перенести привязку на другую модель» — проверена только
  как чистая функция (`transferPlacementXY/Z`) в unit-тесте, не через
  живую панель настроек; удобный вид сверху камеры для калибровки —
  НЕ реализован (не критично: пользователь ориентируется имеющимся
  управлением камерой); высота по точке (§4 задания) — НЕ реализована в
  интерактивном UI, только как чистая функция `offsetZFromPoint` (сдвиг
  по Z по-прежнему только числовым полем).
- **Автоматическое совмещение фасада** (2026-09-11,
  `Docs/fbx-auto-placement-claude-prompt.md`, реализовано ПОЛНОСТЬЮ по
  явному указанию пользователя после уточняющего вопроса о масштабе —
  «Сделать полный алгоритм сейчас, как описано») — ТРЕТИЙ, отдельный от
  двух записей выше способ определить поворот/сдвиг, не замена им:
  ручная калибровка по точкам остаётся для случаев, где автоматика не
  справилась (`ambiguous`/`insufficient_geometry`) или где нет модели
  МФР для сравнения. Метод — `app/static/external-models/auto-align.js`
  (чистая геометрия, без THREE/DOM, кроме отдельной функции извлечения
  граней из уже разобранной группы):
  1. Гистограмма направлений отрезков контура (мод 180°, взвешена
     длиной) → пики → кандидаты поворота как разности пиков модели/
     объекта, ОБА варианта (`delta`, `delta+180`) — направление линии
     само по себе не определяет знак поворота (задание прямо
     предупреждает не ограничиваться PCA/bbox и не игнорировать
     неоднозначность 90°/180°).
  2. Разбиение отрезков на полосы по высоте объединением Z-интервалов с
     допуском (`clusterHeightBands`) — БЕЗ жёстких отметок этого
     здания, только по структуре данных; используется, чтобы взять
     центр масс «верхнего», гарантированно не смешанного с соседним
     объёмом (например, пристройкой) блока для начального переноса.
  3. Уточнение методом ICP: на каждой итерации — поиск ближайшей точки
     на отрезках объекта (равномерная сетка-индекс, не полный перебор
     пар), отсечение 20% худших соответствий, пересчёт ГЛОБАЛЬНО
     оптимального поворота+переноса взвешенным 2D Прокрустом
     (`weightedProcrustes2D` — то же `atan2(cross,dot)`, что в
     `calibrateByPointPair`, обобщённое с одной пары векторов на N
     взвешенных пар через центрирование на средних).
  4. Оценка отдельно по каждой полосе высоты (не только общее покрытие)
     — задание прямо требует, чтобы хорошее совпадение плотной башни не
     маскировало промах по низкой части; решение
     `confident`/`ambiguous`/`insufficient_geometry`/`low_confidence` —
     по явным порогам покрытия/невязки/разрыва с следующим кандидатом,
     подобранным на синтетике (не подогнано под один пример).
  Источник геометрии ОБЪЕКТА — ТОЛЬКО категория «Стены» модели МФР
  (`revit_elements.category='Стены'`, новый модуль
  `app/object_geometry_features.py`, эндпоинт `GET
  .../external-models/geometry-features`, права — как у `/bounds`):
  несущие колонны/перегородки/окна/двери либо шум, либо не образуют
  надёжный внешний контур; у объектов ЖБИ (без модели МФР) сравнивать
  не с чем — эндпоинт возвращает `source:'none'`, автоматика для facade
  на таком объекте не запускается вовсе, это ожидаемо, не баг. Источник
  геометрии МОДЕЛИ — грани уже разобранной THREE.js-группы с
  `|normal.z|` меньше порога (вертикальные, не пол/потолок/скат), в
  каноническом C (source_anchor прибавляется обратно к запечённым
  локальным вершинам — `fbx.js` центрирует геометрию при разборе).
  Срабатывает ДВУМЯ явными путями, никогда при просмотре/фильтре этажей/
  повороте камеры/переключении просмотрщика: (а) сразу после загрузки
  `kind='facade'`, результат пишется В ОДНОМ PATCH вместе со сдвигом/
  поворотом сразу за созданием модели — не отдельным запросом, чтобы не
  было окна, где смещение уже применено, а статус попытки ещё старый;
  (б) кнопка «Совместить автоматически» у уже загруженного фасада —
  результат ТОЛЬКО в черновик (как «Совместить по точкам»), сохранение
  — отдельным нажатием «Сохранить», которое передаёт `auto_placement_
  status`/`auto_placement_json` вместе со сдвигом/поворотом в том же
  PATCH. Новые колонки `object_external_models.auto_placement_status`/
  `auto_placement_json` (миграция `app/db.py`) — ТОЛЬКО для показа
  человеку в диагностике, логика приложения на них не полагается.
  Проверено: `scripts/verify_auto_align.mjs` — точное восстановление
  угла (намеренно НЕ кратного 90°) и переноса на синтетическом здании
  «башня (3 этажа) + отдельный объём» без шума и с шумом (±15-40мм),
  устойчивость к неполной геометрии (пропавший верхний объём), отказ от
  ложной уверенности на симметричном квадратном здании и на заведомо
  другом здании, детерминированность повторного запуска, время расчёта.
  ДОПОЛНИТЕЛЬНО — живым запросом браузера (`javascript_tool`) против
  РЕАЛЬНОЙ геометрии стен объекта_id=4 анонимной копии (7620 отрезков,
  настоящее здание «башня+пристройка», см. скриншот в отчёте сессии):
  синтетически повёрнутая на 41° и сдвинутая копия тех же стен
  (имитация ещё не привязанного FBX) была найдена алгоритмом с ошибкой
  положения ~0.7м на дальнем углу здания (80м от опорной точки),
  статус `confident`, ~460мс в браузере. Этой же живой проверкой найден
  и исправлен реальный краевой случай: на настоящих (не идеальных
  синтетических) данных одна стена иногда даёт «плато»-максимум
  гистограммы шириной 10-16° вместо острого пика — узкий порог слияния
  направлений (8°) плодил из одной стены два кандидата поворота и давал
  ЛОЖНУЮ `ambiguous` там, где ответ был однозначным; порог поднят до 15°
  (`findDirectionPeaks`, `mergeDeg`) — после этого тот же живой прогон
  дал `confident` с правильным ответом. **НЕ проверено и не может быть
  проверено в этой сессии: совпадение с РЕАЛЬНЫМ фасадом заказчика** —
  нет пары «настоящий FBX-фасад ↔ настоящая модель МФР одного и того же
  здания» (анонимная копия и тестовые FBX-файлы — разные объекты, боевой
  сервер недоступен ассистенту, тот же урок, что и в двух записях выше).
  Известное принципиальное ограничение метода (не баг): на плотной
  РЕГУЛЯРНОЙ сетке стен (одинаковые пролёты повсюду) поиск методом
  ближайшей точки может дать обманчиво высокое покрытие для неверного
  сдвига на целое число пролётов — статус `ambiguous`/`low_confidence`
  для таких случаев ожидаем и означает «нужна ручная калибровка по
  точкам», а не дефект алгоритма.
- **Исправления после аудита (2026-09-11, тот же день, `Docs/fbx-
  placement-repair-claude-prompt.md`)** — первая версия автосовмещения
  (запись выше) была развёрнута с реальными дефектами: пользователь
  сообщил «положение не изменилось, благоустройство проваливается
  вниз»; аудит прочитал код и подтвердил численными проверками (не
  голословно) несколько независимых ошибок. Подробное описание каждого
  исправления и живая проверка на реальной геометрии — `Docs/OPEN.md`,
  тот же раздел «Внешние 3D-модели», п.2. Кратко, что изменилось и
  ПОЧЕМУ (инварианты на будущее):
  - `clusterHeightBands` → `clusterSpatialParts`: части здания различаются
    СВЯЗНОСТЬЮ КОНТУРА В ПЛАНЕ (XY), а не пересечением Z-диапазонов —
    два объёма от одной земли (башня+пристройка) при группировке по
    высоте неизбежно сливаются в одну «полосу», раз оба начинаются от
    Z=0; при этом связность — по расстоянию «конец-до-ОТРЕЗКА» (не
    «конец-до-конца»), иначе реальные Т-образные стыки Revit (конец
    одной стены в середине другой) остаются несвязанными и дробят
    здание на сотни частей — оба варианта проверены на реальных
    объектах анонимной копии, не только на синтетике.
  - У реальной геометрии всегда есть «мусорные» мелкие части (проёмы,
    авторские дробления) — их дороже оценивать по отдельности, чем они
    стоят; `significantParts` берёт только крупные (по абсолютной и
    относительной доле длины стен) с потолком числа частей — иначе
    поиск на реальном объекте занимал 38+ секунд вместо ~1-2.
  - Оценка кандидата (`icpRefine`) должна отражать ИТОГОВЫЙ transform
    (после последнего обновления), не предпоследний — иначе honesty
    оценки неоднозначности подрывается: решение «этот кандидат хорош»
    принимается по данным, которые уже устарели на один шаг.
  - Неоднозначность — сравнение лучшего решения со ВСЕМИ отличающимися
    (по углу И переносу вместе, не только по углу) результатами, не
    только со вторым по списку: третий-четвёртый кандидат может быть
    ближе к лучшему, чем второй.
  - Автоматика применяет (пишет в БД) только `confident`; `low_confidence`
    — только предложение в черновик и 3D-предпросмотр, сохранение —
    отдельным явным действием пользователя. `ambiguous` даёт выбрать
    один из нескольких кандидатов, не просто останавливается без
    объяснения. Причина ЛЮБОЙ попытки (включая «сравнивать не с чем»)
    сохраняется персистентно на модели, не только тостом.
  - Anchor объекта для расчёта — ВСЕГДА `model.object_anchor_mm` (уже
    сохранённый), никогда не свежий `/bounds` — иначе расчёт и рендер
    молчаливо расходятся, если геометрия объекта изменилась между
    загрузкой модели и повторным запуском кнопки «Совместить
    автоматически».
  - `extractWallSegmentsFromGroup`: бюджет треугольников — заранее
    пропорционально между ВСЕМИ подходящими мешами, не общим счётчиком
    на первый попавшийся (иначе второй и следующие меши пропадают из
    выборки целиком при малом бюджете).
  - Anchor по Z (`fbx.js`) — теперь ОДИНАКОВО нижняя точка габарита у
    ОБОИХ видов (`ground` и `facade`), не «верх габарита» у ground —
    подробности следующей записью ниже (эта правка предшествует ей по
    времени, но описана там подробно как самостоятельное решение).
- **Точное уточнение после грубого поиска (2026-09-12, `Docs/fbx-fine-
  placement-claude-prompt.md`)** — живой отчёт пользователя (скриншоты:
  полоса серого конструктива вдоль края башни) после «Исправления после
  аудита» выше показал, что ОСНОВНОЙ разворот находится верно, но
  положение остаётся не идеально точным. Контрольный численный
  эксперимент подтвердил причину до правки: `icpRefine` с боевым лимитом
  12 итераций останавливался ДО полной сходимости — на синтетике с
  известным точным ответом (+37°/перенос) при лимите 12 максимальная
  ошибка вершины 24мм, при лимите 60 — 0.0004мм (то есть 12 итераций
  объективно недостаточно, это не «неточность метода», а недоработка
  цикла). Три независимых исправления:
  1. **Отдельный этап точного уточнения** (`fineRefine`) — ПОСЛЕ грубого
     поиска (быстрого и намеренно неточного, нужен только чтобы найти
     несколько существенно разных приблизительно верных положений) для
     нескольких (по умолчанию 3) лучших кандидатов запускается уточнение
     с сужающимся радиусом соответствий (3000→1000→400→120мм) и
     адаптивной сходимостью — не тратим время на заведомо плохие старты,
     но и не останавливаемся рано на хороших.
  2. **Сходимость — по смещению САМИХ ТОЧЕК ЗДАНИЯ**, не по «сырому»
     изменению theta/t: при больших абсолютных C-координатах даже
     небольшое изменение угла даёт большой компенсирующий перенос при
     почти неподвижном самом здании — сравнение t напрямую ложно
     считало бы это «не сошлось» (или наоборот). `icpRefine` возвращает
     `iterations`/`stopReason`/`converged` — исчерпание лимита итераций
     ЯВНО отличается от настоящей сходимости.
  3. **Честное сравнение coverage/rms между уточнённым и неуточнённым
     кандидатом** — узкий финальный радиус точного уточнения (120мм) по
     своей природе даёт МЕНЬШИЙ coverage, чем широкий радиус грубого
     поиска (3000мм), даже если итоговое положение точнее; сравнивать их
     напрямую нельзя. `evaluateTransform` — общая функция, уточнённый
     кандидат ПЕРЕОЦЕНИВАЕТСЯ на том же грубом радиусе для честного
     ранжирования (точная невязка на узком радиусе сохраняется отдельно,
     `fineRmsResidualMm`, только для показа человеку). Без этой правки
     более точный кандидат мог нечестно проигрывать менее точному —
     подтверждено живой проверкой на реальном объекте анонимной копии
     (до правки: избранный кандидат rms=342мм; после — rms=44.85мм, тот
     же угол практически точно совпал с истинным +37°).
  4. **Критерий отбора кандидатов** — небольшое (<3 процентных пункта)
     преимущество по coverage больше НЕ перевешивает автоматически явно
     лучшую невязку (`compareResults`, `coverageTieEps`) — раньше даже
     минимальное превосходство по числу точек внутри широкого радиуса
     решало исход, что не отличает «действительно лучше» от «случайно
     зацепило на 1 точку больше».
  `RMS_CONFIDENT_MM=800` НЕ уменьшен — цель была не «отказывать чаще», а
  «находить точнее»; понижение порога подменило бы недостающую точность
  придирчивостью, не решив исходную проблему (явное указание задания).
  Асинхронность: `autoAlignFacade`/`fineRefine` — `async`, с
  `await`-уступками циклу событий между дорогими шагами (не даёт
  расчёту в 5-6 секунд занять поток целиком одним куском) — полноценная
  отмена расчёта НЕ реализована (открытый пункт, `Docs/OPEN.md`).
  Проверено: `scripts/verify_auto_align.mjs`, новый раздел «строгая
  приёмка ≤1мм» — оба знака угла, большие исходные координаты
  (~13 млн мм), разные (не нулевые) anchors источника/объекта,
  повторное вычисление placement (имитация пересохранения) не
  накапливает поправку: максимальная ошибка независимой контрольной
  точки 0.0014мм на точно преобразованной синтетике (допуск по заданию
  — ≤1мм). **Живой запрос против реальной геометрии стен объектов 3 и 4
  анонимной копии** — после исправления точность заметно выросла (угол
  восстановлен с точностью до 4-го знака после запятой в градусах,
  rms снизился в разы), НО оба объекта по-прежнему честно дают
  `ambiguous` — это НЕ дефект: у реальных зданий с повторяющейся сеткой
  стен несколько положений действительно дают сравнимо хорошее
  совпадение, и алгоритм больше не скрывает эту неоднозначность.
  Расчёт занимает ~5-6с на реальном объекте (было мгновенно, но неверно)
  — цена за честную точность на дорогой геометрии; UI не блокируется
  целиком благодаря уступкам, но и не мгновенен — сообщается пользователю
  текстом кнопки «Ищу совмещение…».
  **НЕ реализовано (открыто)**: количественное измерение остатка по
  разнесённым контрольным точкам на РЕАЛЬНОЙ паре «фасад ↔ конструктив
  одного здания» (задание §5) и диагностика «оболочка vs конструктив»
  односторонним/двусторонним запасом (задание §6) — обе требуют
  настоящей такой пары, которой у ассистента нет (см. предыдущие записи,
  тот же урок); полноценная отмена расчёта пользователем — нет,
  реализованы только уступки циклу событий, не прерывание.
- **Проверка соразмерности наружных габаритов (2026-09-12, `Docs/fbx-
  envelope-matching-claude-prompt.md`)** — пользователь проверил все три
  предложенных варианта: во всех конструктив выступал за фасад. Аудит
  дал воспроизводимую демонстрацию: `evaluateTransform` ищет ближайшую
  ЛЮБУЮ стену (включая внутренние — `get_wall_segments` берёт всю
  категорию «Стены», не только наружный контур), и фасад, утопленный на
  несколько метров с одной стороны, всё равно даёт высокий coverage/
  низкий rms — три стороны из четырёх совпадают идеально, четвёртая
  просто не учитывается в среднем. Численно подтверждено на синтетике из
  задания: фасад уже объекта на 4000мм — coverage=100%, rms≈3e-13мм
  (демо), 84-93% и rms 400-500мм (через полный `autoAlignFacade`) — то
  есть штатные пороги confident/ambiguous его пропускали.
  Полноценное восстановление наружного контура (объединение контуров
  перекрытий по уровню через растеризацию — прототип на реальных
  объектах 3/4 анонимной копии дал правдоподобный результат) признано
  СЛИШКОМ ЗАТРАТНЫМ для этой итерации и отложено (см. «Не реализовано»
  ниже) — вместо этого добавлена независимая проверка `checkSizeCompat-
  ibility`: наружный габарит (`orientedExtent` — протяжённость МНОЖЕСТВА
  отрезков вдоль направления кандидата поворота и поперёк, СОБСТВЕННЫЕ
  оси, не axis-aligned bbox) фасада и конструктива должны совпадать с
  точностью до толщины облицовки (по умолчанию max(600мм, 4% габарита),
  жёсткий отказ — при расхождении более чем в 2.5 раза от допуска).
  Кандидат с несовместимым габаритом НЕ предлагается вовсе — ни как
  confident, ни в списке ambiguous (задание §4: «не навязывай выбор
  одного из [заведомо плохих]»); если ВСЕ кандидаты отклонены по этой
  причине — явный статус `insufficient_geometry` с объяснением именно
  этой причины («Наружные габариты... несовместимы»), а не общий отказ.
  Обоснование, почему проверка размера ловит демо-случай сама по себе
  (без перестройки источника геометрии): внутренние стены НИКОГДА не
  выступают за наружный контур, поэтому их присутствие в целевом наборе
  НЕ увеличивает измеренный габарит объекта — утопленный фасад всегда
  измеримо МЕНЬШЕ по своему наружному габариту, чем истинный конструктив,
  вне зависимости от того, что ещё есть в целевом наборе.
  При разработке найдено и исправлено ДВА самостоятельных дефекта:
  1. `orientedExtent` (новая функция) считала габарит с ПРОТИВОПОЛОЖНЫМ
     знаком относительно принятой во всём проекте конвенции поворота
     (`rotateXY`, `P=Rz(theta)*C+t`) — из-за этого буквально ОДНА И ТА ЖЕ
     геометрия под известным углом давала РАЗНЫЕ измеренные габариты
     (подтверждено живой проверкой на реальном объекте: [53302,72101] vs
     [75150,35530] для идентичной геометрии) — проверка ложно отклоняла
     ГЕНУИННО совпадающие данные. Исправлено переиспользованием `rotateXY`
     напрямую вместо самостоятельной формулы проекции.
  2. Проверка изначально запускалась на НЕУТОЧНЁННОМ угле-кандидате (пик
     гистограммы направлений), а не на угле ПОСЛЕ грубого ICP — пик может
     отличаться от истинного угла на несколько градусов (обнаружено на
     синтетике: пик дал 18° при истинных 22°), а измерение габарита ОЧЕНЬ
     чувствительно к точности угла — перенесена на `icp.theta` (после
     уточнения), не на исходный `rc.theta`.
  Попутно найдена и исправлена смежная проблема устойчивости: поиск
  ближайшего соседа в `nearestOnIndexedSegments`/`nearestSegmentIndex-
  Excluding` («нашли — ещё кольцо про запас и стоп») зависел от
  выравнивания сетки-индекса относительно АБСОЛЮТНЫХ координат, которое
  разное у P (объект) и C (FBX, обычно у начала координат) — из-за этого
  `clusterSpatialParts` на буквально идентичной, но по-разному
  расположенной в координатах геометрии могла давать РАЗНОЕ разбиение на
  части (подтверждено: 127 vs 145 частей при чистом повороте той же
  геометрии). Исправлено — обе функции теперь всегда обходят полный
  вычисленный радиус, без ранней остановки; это НЕ было первопричиной
  найденного дефекта размера (сам размер теперь считается по сырым
  отрезкам, минуя кластеризацию), но самостоятельный источник
  недетерминизма, который стоило устранить раз найден.
  Попутно (задание §4, отдельная ошибка согласованности): `fineRefine` в
  конце вызывал `icpRefine({maxIters:1})` «только ради оценки» — тот
  всё равно выполняет один полный шаг Прокруста и СДВИГАЕТ theta/t перед
  оценкой, а функция возвращала СТАРЫЕ theta/t с метриками уже НОВОГО,
  сдвинутого положения. Исправлено — чистая `evaluateTransform` без
  мутации на итоговых theta/t.
  Проверено: `scripts/verify_auto_align.mjs` — точное воспроизведение
  demo аудита (фасад с внутренней стеной в целевом наборе, утоплен на
  4м) теперь корректно отклоняется с явной причиной; устойчивость
  `orientedExtent`/`checkSizeCompatibility` к повороту и большим
  координатам; генуинно совпадающая геометрия с неточным seed-углом
  направления НЕ отклоняется ложно. **Живая проверка на реальной
  геометрии объектов 3 и 4 анонимной копии**: истинная (обратно
  повёрнутая) геометрия каждого объекта корректно проходит проверку
  размера (габариты совпадают с точностью до долей мм) и находится с
  высокой точностью (например, объект 4: 37.30° при истинных 37°,
  rms=43мм); искусственно несовместимые по размеру варианты — надёжно
  отклоняются.
  **НЕ реализовано (осознанно отложено)**: восстановление НАСТОЯЩЕГО
  наружного контура через объединение контуров перекрытий (прототип на
  реальных данных валиден, но требует растеризации на бэкенде + новой
  формы ответа эндпоинта — отдельная по объёму задача); явное измерение
  выступания несущего объёма за оболочку (сигнальная дистанция до
  полигона) — вместо этого проверка размера ловит СИМПТОМ (несовместимый
  агрегатный габарит), не даёт локализованную величину/место выступания
  на схеме; двусторонняя (FBX→конструктив и обратно) отдельная оценка по
  сторонам/частям; направленное (по нормали) сопоставление точек.
  Оставшаяся `ambiguous` неоднозначность на реальных объектах (несколько
  положений со сравнимым качеством) — после проверки размера уже не
  включает грубо несовместимые по габариту варианты, но НЕ проверено,
  устраняет ли она ВСЕ случаи, похожие на исходную жалобу пользователя —
  натурная проверка на паре «этот фасад — этот конструктив» по-прежнему
  недоступна ассистенту.
- **Anchor по Z — ВЕРХНЯЯ точка габарита у ground, НИЖНЯЯ у facade**
  (жила с 2026-09-11 по 2026-09-11 — **ОТМЕНЕНО В ТОТ ЖЕ ДЕНЬ**, см.
  «Исправления после аудита» выше и следующий абзац; оставлено здесь
  как история решения, а не как действующий контракт). Была мотивация:
  типичный случай — благоустройство/рельеф ниже уровня чистого пола, не
  объём, вырастающий вверх от 0. НО реализация была неверной: «верхняя
  точка габарита» — это буквально самая высокая точка ЛЮБОЙ геометрии
  модели, включая деревья/фонари/оборудование; высокое дерево в
  благоустройстве утягивало anchor (и весь уровень площадки) вниз на
  свою собственную высоту — живой отчёт пользователя «благоустройство
  проваливается вниз», подтверждено численно (Docs/fbx-placement-repair-
  claude-prompt.md §3: площадка на C.z=139800 при дереве до 154000 без
  правки давала P.z=-14200 вместо ожидаемых -200). **Исправление
  2026-09-11**: anchor по Z — ВСЕГДА нижняя точка габарита, ОДИНАКОВО
  для `ground` и `facade` (убрана ветка по `kind` в `fbx.js`) — чисто
  ТЕХНИЧЕСКАЯ точка (для точности Float32 при запекании геометрии), НЕ
  несёт смысла «уровень земли/пола» ни для одного из видов: этот смысл
  нельзя вычислить из одного габарита без независимого высотного
  ориентира. Привязка к реальной высоте — по-прежнему числовым полем
  `offset_z_mm`, либо (для случая «оба файла заведомо из одного
  источника координат сцены») переносом уже подтверждённой привязки
  через «Перенести эту привязку на» — `transferPlacementZ`
  (`coordinates.js`) даёт корректный общий `tZ`, эта функция уже
  существовала и не менялась. НЕ требует миграции: `source_anchor_z_mm`
  хранится один раз при разборе КОНКРЕТНОГО файла и не пересчитывается
  позже — правка касается только НОВЫХ загрузок, уже загруженные модели
  сохраняют старое (возможно, «утянутое деревом») значение, пока
  пользователь не перезагрузит файл. «Сцентрировать с объектом» по-
  прежнему пересчитывает только X/Y anchor, `offset_z_mm` не трогает.
- **`kind='facade'` (2026-09-11, живой запрос пользователя) — второй вид в
  ТОМ ЖЕ механизме `object_external_models`**, не отдельная таблица/API:
  `SUPPORTED_KINDS` в `app/external_models.py` расширен с `("ground",)` до
  `("ground", "facade")`. Никакой новой backend-логики — `kind` как был
  просто хранимой строкой, так и остался; вся разница между видами живёт
  на клиенте: (1) `fbx.js` выбирал anchor по Z в зависимости от `kind`
  («Anchor по Z» выше — ground=верх на 0, facade=НИЗ на 0), ЭТА ЧАСТЬ
  ОТМЕНЕНА 2026-09-11 (см. «Исправления после аудита» и «Anchor по Z…»
  выше) — anchor по Z теперь одинаков для обоих видов, `kind` на него не
  влияет; у обоих один и тот же 0 объекта — встречаются на нём, не
  расходятся, живой запрос «благоустройство, фасады и уже загруженные
  элементы должны быть синхронизированы в пространстве»);
  (2) `settings.js` — радиокнопка вида при загрузке, подпись вида на
  карточке модели; (3) `layer.js`/`app.js` — видимость решается ПО ВИДУ
  модели, не одним флагом на все модели объекта: `attachToMfr`/
  `attachToZhbi`/`setVisible` принимают `visibleForKind(kind)` вместо
  булева `visible`, кэш слоя хранит `entry.kind` для `setVisible` без
  повторного attach. X/Y-центрирование и поворот у facade — ТЕ ЖЕ, что у
  ground, никакой отдельной логики не потребовалось.
- **Два независимых чекбокса «Фасады (PDF)» и «Фасады (FBX)», не один
  переключатель источника** (живой запрос пользователя 2026-09-11: «сделай
  просто две галочки... пользователь сам выберет какой фасад выводить»).
  «Фасады (PDF)» — более старая функция (2026-08-31), 4 зашитых в код PNG
  для ОДНОГО конкретного объекта, к `object_external_models` отношения не
  имеет; «Фасады (FBX)» — новый чекбокс, показывает `kind='facade'` модели
  через тот же слой, что и благоустройство. Обе галочки можно включить
  одновременно, они не эксклюзивны.
- **Геометрия разбирается и кэшируется ОДИН раз на модель** (по её id, в
  `app/static/external-models/layer.js`), сдвиг/поворот/фильтр этажа не
  вызывают повторный разбор FBX — меняется только `group.position`/
  `group.quaternion` уже готовой геометрии. МФР и ЖБИ держат НЕЗАВИСИМЫЕ
  экземпляры слоя (Group нельзя делить между двумя сценами с разными
  трансформами).
- **Перетаскивание мышью** — только внутри уже открытой 3D-сцены МФР
  (`beginExternalModelDrag` в `app.js`): диалог настроек на время
  перетаскивания скрывается (не закрывается — черновик остаётся), чтобы
  курсор доставал до canvas позади. ЖБИ таким же перетаскиванием пока не
  покрыт — сцена мутируется на месте, второй адаптер под общий диалог не
  заводили.
- **FBXLoader r160 + fflate + NURBSCurve/Utils вендорены с явного
  подтверждения пользователя** (не текущая заводская индульгенция на
  сторонний код) — `app/static/vendor/three/VENDOR.md`.
- Сервер независимо перечитывает GlobalSettings бинарного FBX
  (`app/fbx_global_settings.py`, зеркало `app/static/external-models/
  fbx-global-settings.js`) и отклоняет любую другую комбинацию осей —
  единственный подтверждённый профиль соответствует первому приёмочному
  файлу `0507_Aviomotornaya_ZU_5_Ground.fbx`.
- **`uploads/external_models/` — ОДИН каталог на диске для ЛЮБОЙ базы**
  (не зависит от `ZHBI_DB_PATH`), в отличие от самих файлов баз
  (`zhbi.db`/`zhbi.anon.db`/`zhbi.check.db` — разные файлы). Инцидент
  2026-09-11: автоматическая уборка файлов-сирот в `on_startup()`
  считала «известными» stored_name только из базы ТЕКУЩЕГО процесса и
  удаляла файлы, реально принадлежащие строкам ДРУГОЙ базы — на боевом
  сервере пользователя (`--reload`, рестарт на каждую правку кода) это
  каждый раз стирало то боевые, то тестовые файлы благоустройства
  (`410 Gone` на скачивании — строка в БД есть, файла нет). Автоматический
  вызов `cleanup_orphan_model_files` из `on_startup()` УБРАН навсегда —
  сверка «файл есть, а строки нет» безопасна только если явно сверять
  файл СРАЗУ со всеми базами, которые могут на него ссылаться, а не
  автоматически при старте одного конкретного процесса. `cleanup_
  orphan_temp_files` (только `.tmp-*`, никогда ничьи) остался — он не
  зависит от того, какая база активна.
- **Перетаскивание мышью конфликтовало с OrbitControls** (тот же
  инцидент, 2026-09-11): её собственный обработчик `pointerdown` на том
  же canvas зарегистрирован раньше нашего (сцена создаётся до открытия
  настроек) и получает событие первым — выключать `controls.enabled`
  внутри СВОЕГО `pointerdown` было уже поздно, OrbitControls успевала
  начать вращение и на отпускании кнопки «откатывала» перетаскивание.
  Правильно: `controls.enabled = false` СРАЗУ при входе в режим
  перетаскивания (до первого возможного `pointerdown`), плюс свои
  обработчики регистрируются с `{capture: true}` и вызывают
  `stopImmediatePropagation()` — двойная, независимая друг от друга
  защита. Синтетические `PointerEvent` через `dispatchEvent` в тестах
  этот конфликт НЕ воспроизводили — итоговая проверка обязана быть
  настоящим перетаскиванием мышью (`computer`/`left_click_drag`), не
  только скриптом.
- **Одна кнопка «Настроить положение» вместо раздельных «Переместить»/
  «Повернуть мышью»** (живой запрос пользователя 2026-09-11): обычная
  протяжка мышью — сдвиг, протяжка с зажатой Control — поворот вокруг
  центра модели. Решается `e.ctrlKey` РОВНО в момент `pointerdown` одного
  конкретного жеста (`startExternalModelPlacementOnPlane`,
  `beginExternalModelPlacement`, `app.js`) и не пересматривается до
  `pointerup` — если Control отпустить/зажать посреди протяжки, текущий
  жест это не переключает (иначе положение прыгало бы посреди движения).
  Раскаст на плоскость общий для обоих видов жеста — что для сдвига, что
  для поворота модель лежит на той же высоте. Проверено прямыми
  `PointerEvent` (с `ctrlKey: true`/`false`) на канвасе живой 3D-сцены
  (МФР) с подставным `THREE.Group` вместо реальной FBX-модели — обычная
  протяжка меняла `position`, протяжка с Control меняла `quaternion` при
  неизменном `position`, `stop()`(отмена) и клик «Готово» (сохранение)
  оба отработали корректно.
- **Режим «Переместить/Повернуть мышью» — ПОСТОЯННЫЙ, не на один жест**
  (живой репорт пользователя 2026-09-11: закрывать режим после каждого
  отпускания мыши не давало нормально отрегулировать положение). Пока
  режим активен, диалог настроек скрыт, а выйти можно только плавающей
  панелью «Готово»/«Отмена» поверх 3D-canvas или клавишей Esc. Позиция/
  поворот при повторных жестах внутри одного захода накапливаются от
  ЗНАЧЕНИЯ НА МОМЕНТ ВХОДА в режим (`armStartGroupPos`/
  `armStartQuaternion`), а не от предыдущего жеста по отдельности — так
  Esc в любой момент откатывает СРАЗУ ко входу в режим, без накопленной
  ошибки округления по числу жестов.
- **Поворот мышью — угол курсора вокруг ЦЕНТРА модели** (`atan2` в
  горизонтальной плоскости, не смещение по одной оси, как у перемещения):
  ось вращения — та же мировая вертикаль, что нормаль плоскости
  перетаскивания (Z у МФР, Y у ЖБИ) — она СОВПАДАЕТ с локальной
  канонической Z модели после адаптера, поворачивать поэтому можно прямо
  в мировых координатах сцены, не пересчитывая обратно в C/P. Знак: рост
  `rotation_deg` — по часовой стрелке при виде на план сверху (см. выше),
  что в математическом (против часовой положительном) угле THREE —
  вычитание: `newDeg = startDeg - THREE_angle*180/π`. Направление
  проверено на внутреннюю непротиворечивость (сохранённый градус после
  Esc/Готово совпадает с тем, что показывает угол мыши), но не сверено
  человеком «по ощущению, куда должно крутиться».

