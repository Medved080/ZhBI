# V2 полный интерфейс — передача работы (handoff)

Обновляется по ходу ночи; читать сверху вниз. Реестр охвата — `Docs/v2-interface-coverage.md` (генерируется скриптом, руками не править). Итоговая сводка для человека — `Docs/v2-morning-report.md` (генерируется `scripts/gen_v2_morning_report.py`).

## Где что
- Рабочий каталог: `/Users/max/zhbi-tool/.claude/worktrees/v2-full-interface`, ветка `worktree-v2-full-interface` (от `a3e8a92`). Слияний, push, deploy — НЕТ и не будет без человека.
- Тестовая БД этого дерева: `data/zhbi.anon.db` (копия обезличенной; `.backup` рядом = исходное состояние на момент ветвления). Настоящий backend: `127.0.0.1:8041` (`/v2` — новый интерфейс, `/?ui=v1` — прежний). В панели браузера есть вход тестового администратора «QA-Overnight Test» (вход выполнял человек; пароли ассистент не вводит и не задаёт).
- Стенд без БД и без входа (фейковый бэкенд): `python3 scripts/v2_test_server.py 8051` → `http://127.0.0.1:8051/tests/run.html` (наборы `shell|ua|po|cp|rd|de|se|ce|pe|vis|a11y`, `?suite=…&only=ID`; полный прогон `?save=all` сохраняет `Docs/v2-acceptance-results/all.json` вместе с версией кода), `…/tests/app.html` — сам интерфейс на имитации API.
- **Скачивание файлов:** сценарии стенда НЕ сохраняют файлы (в `helpers.js` у ссылок с `download` подменён `click`), живые проверки выгрузок — тоже с подменой: macOS спрашивает разрешение на каждое сохранение, а человека может не быть рядом. Не запускать реальные скачивания.
- Браузерная панель: вкладка `tab-1` — прогоны стенда (не уводить её на 8041 во время прогона — прогон прервётся), `tab-2` — живая работа на 8041.
- Чужие/старые процессы, НЕ трогать: 8000 (боевой пользователя), 8020 (обезличенный пользователя), 8010 (чужой). Старые стенды прежнего этапа: 8021 (сервер на БД старого worktree `v2-overnight-migration`), 8031/8032/8033.
- Старый worktree `v2-overnight-migration` (HEAD `a3e8a92`, чистый) — приёмка трёх разделов (Пользователи и доступ / Проекты и объекты / Контрагенты) ОТЛОЖЕНА, не завершена; список остатков — `Docs/v2-night-progress.md` (блок «СОСТОЯНИЕ НА КОНЕЦ ЗАХОДА»). Его тестовая БД возвращена к baseline.

## Порядок работы (задание пользователя)
А — полный охват (все экраны V1 доступны из навигации V2 в едином дизайне) — **завершён**; Б — подключение реальных операций по риску — идёт; В — укрепление и регрессия — начато (полные прогоны стенда, живая регрессия V1 по глубоким ссылкам); затем — отложенная приёмка трёх разделов.

## Что сделано
1. `scripts/inventory_v1_ui.py` — инвентаризация V1 разбором разметки (`html.parser`) + токенизатором JS с самопроверкой; извлекает и структуру форм для каркасов.
2. `app/static/v2/screens.json` — реестр 73 экранов (правится `scripts/v2_screens_tool.py set ID --status N --checks … --limits …`); `scripts/gen_v2_coverage.py` сверяет полноту (падает, если элемент V1 не закреплён) и генерирует реестр и `screen-structure.json`.
3. V1: `applyStartupDeepLink()` — `?ws=`, `?view=`, `?open=menu&item=<id>` (виден роли, не «опасный»).
4. Оболочка V2 (`main.js`, `registry.js`, `screen-view.js`): левая навигация, начальная страница, контекст «Объект» (права и ссылки в V1 — по объекту; на сервер не пишется; смена объекта проходит сторож несохранённого), маршруты `#/экран`, каркасы форм V1 с переходом в V1.
5. Чтение (`read-screen.js`, `reports.js`, `api.readPost`, `api.download`): 29 экранов на статусе 3 — справочники, журналы, настройки, отчёты (статус монтажа, комплектация, аналитика, динамика, моя работа, линейный трек, график работ по блокам, матрица «Учёт по блокам: статусы») с выгрузкой XLSX/PDF, учёт по блокам и журнал факта (объект МФР), документы контрактации, карта проектов (таблица), «Экспорт в XLS/PDF» (`export-form.js`, по чертежу объекта). Правка (статус 4): `dict-edit.js` (СМУ, физлица), `setting-edit.js` (порог опоздания), `color-edit.js` (цвета зон и статусов), `prefix-edit.js` (префиксы марок), `card-edit.js` (карточка объекта — запись целой карточки с проверкой конфликта; события/задачи/вопросы — редакции на дату), `sessions-edit.js` (свои сеансы). Каждый режим правки: барьер безопасности (id из строки/контекста, одна запись за раз, ввод не теряется, успех после повторного чтения, неизвестный исход не повторяется), сценарии на стенде, живая проверка на 8041 с восстановлением данных (кроме реального завершения чужого сеанса — второго сеанса нет).
6. Документы: `Docs/v2-design-notes.md`, `Docs/v2-morning-report.md`, снимки `Docs/v2-full-shots/`, `Docs/TZ.md` §2а, `Docs/OPEN.md`, `Docs/MAP.md`.

## Статус (см. реестр и утренний отчёт)
Статус 4 — 12 экранов (3 раздела прежнего этапа и 9 новых редакторов); статус 3 — 29; статус 2 (каркас + переход в V1) — 32; 5 — нет ни у одного (не проведена итоговая живая приёмка каждого экрана, права не-администраторов не проверялись на настоящем backend).

## Следующий шаг
Проход Б: правка подтипов и зон, операции учёта по блокам и журнала факта на объекте МФР (данные есть на объекте 4: 42 блока, 210 работ, 32 отчёта; каждая операция — с барьером и восстановлением данных), затем рискованное (импорт, восстановление копий, права ячейкой, смена поставщика) — только после отдельной проверки. Проход В: полная живая приёмка каждого экрана статуса 3–4, включая вход под пользователем с урезанными правами (вход выполняет человек); отложенная приёмка трёх разделов по `Docs/v2-night-progress.md`; статус 5 только при выполнении всех критериев.

## Команды проверок
- Полнота реестра: `.venv/bin/python scripts/gen_v2_coverage.py --check`
- Стенд: `python3 scripts/v2_test_server.py 8051` (если не запущен), затем в браузере `http://127.0.0.1:8051/tests/run.html?save=all` (полный прогон ~15 мин, менять `app/static` во время прогона нельзя).
- Снимки: `python3 scripts/v2_shots.py --out <каталог> --port 8051 --scenes shell-home,dict-edit,…`
- Синтаксис модулей: `node --check app/static/v2/<файл>.js`.
- Утренний отчёт: `.venv/bin/python scripts/gen_v2_morning_report.py`.

## Риски и оговорки
- Ограничение инструмента браузера: Enter/Backspace на полях ввода не срабатывают нативно (реальные ввод текста и клики работают); `ctrl+a` не выделяет всё. Значения `<input type=color>` выставлялись программно.
- Эмуляция viewport в панели: после `reload()` скриншот может быть масштабирован неверно — помогает повторная навигация (`navigate`) и `resize_window`.
- На настоящем backend проверялись только права администратора; права остальных ролей — только на стенде (имитация).
- Данные копии БД после живых проверок восстановлены: `smu_catalog` = 6, `mark_type_prefixes` = 12, порог опоздания объекта 2 = 0, цвета кранов объекта 2 и статус `planned` — исходные. В журнале действий остались события проверок; последний выбранный объект пользователя QA-Overnight Test — «Объект-2» (V1-переход по глубокой ссылке записал его).
