# V2 полный интерфейс — передача работы (handoff)

Обновляется по ходу ночи; читать сверху вниз. Реестр охвата — `Docs/v2-interface-coverage.md` (генерируется).

## Где что
- Рабочий каталог: `/Users/max/zhbi-tool/.claude/worktrees/v2-full-interface`, ветка `worktree-v2-full-interface` (от `a3e8a92`). Слияния, push, deploy — НЕТ и не будет без человека.
- Тестовая БД этого дерева: `data/zhbi.anon.db` (копия обезличенной, `.backup` рядом = исходное состояние). Сервер: `127.0.0.1:8041` (лог — в scratchpad `server8041.log`). Вход выполняет человек (`localhost:8041`), пароли ассистент не вводит.
- Чужие/старые процессы, НЕ трогать: 8000 (боевой пользователя), 8020 (обезличенный пользователя), 8010 (чужой). Старые стенды прежнего этапа: 8021 (сервер на БД старого worktree `v2-overnight-migration`), 8031/8032/8033 (стенды без БД).
- Старый worktree `v2-overnight-migration` (HEAD `a3e8a92`, чистый) — приёмка трёх разделов (Пользователи и доступ / Проекты и объекты / Контрагенты) ОТЛОЖЕНА, не завершена. Его тестовая БД возвращена к baseline (14 таблиц-счётчиков, integrity ok, роли 4, `role_features` 216, назначений нет).

## Порядок работы (задание пользователя)
А — полный охват (все экраны V1 доступны из навигации V2 в едином дизайне), Б — подключение реальных операций по риску, В — укрепление и регрессия; затем — отложенная приёмка трёх разделов.

## Что сделано
1. `scripts/inventory_v1_ui.py` — инвентаризация V1 разбором разметки (`html.parser`) + токенизатором JS с самопроверкой (71 пункт меню «Действия», 83 модалки, 38 кнопок панели, 13 областей, 501 обработчик).
2. `app/static/v2/screens.json` — курируемый реестр 73 экранов V2 (группы, закрепление элементов V1, риск, статус); `scripts/gen_v2_coverage.py` сверяет полноту (падает, если элемент V1 не закреплён) и генерирует `Docs/v2-interface-coverage.md` и `app/static/v2/screen-structure.json`.
3. V1: `applyStartupDeepLink()` расширена (`?ws=`, `?view=`, `?open=menu&item=<id>`; только видимые роли, не «опасные» пункты).

## В работе / следующий шаг
Оболочка V2: левая навигация по группам, начальная страница, каркасы экранов из `screen-structure.json`, переходы в V1 с контекстом объекта; затем проход Б.

## Команды проверок
- Полнота реестра: `.venv/bin/python scripts/gen_v2_coverage.py --check`
- Стенд V2 без БД: `python3 scripts/v2_test_server.py 8031`, затем `http://127.0.0.1:8031/run.html` (набор сценариев `shell|ua|po|cp|vis|a11y`).

## Риски
- Статусы в реестре ставятся только по фактически выполненным проверкам; сейчас 70 экранов — «не начат» (каркасов нет), 3 — «подключены операции, проверка не завершена».
