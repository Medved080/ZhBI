# V2: ночной прогон — журнал прогресса

## СОСТОЯНИЕ НА КОНЕЦ ЗАХОДА (2026-09-20) — читать первым

**Задание:** закрытая стабилизация и приёмка трёх разделов V2 (Пользователи и доступ, Проекты и объекты,
Контрагенты) по `Docs/v2-release-acceptance.md`. **Статус: ЗАБЛОКИРОВАНО на уровне L** — нет входа в живую
вкладку `http://127.0.0.1:8021/v2` (`/me` → 401; пароль вводит только пользователь).

**Сделано:** 30 дефектов (D-01…D-30) исправлены в `app/static/v2/*`; собран стенд без входа и БД
(`scripts/v2_test_server.py`, `scripts/v2_tests/`); финальный прогон 266 PASS / 0 FAIL (ревизия `7e372bc`) +
`CP-M-01`; Node-тесты зелёные; матрица заполнена (47 «пройден», 111 «H ✓, ждёт L»); снимки 32 форм × 3 размера.
**Версия кода V2:** без изменений с `78cac49`. Ветка `worktree-v2-overnight-migration`, merge/push не выполнялись.

**Точная точка продолжения:** после входа пользователя выполнить шаги L1–L12 (раздел 3а матрицы) на
`http://127.0.0.1:8021` (сервер из worktree, БД `data/zhbi.anon.db`; перед прогоном `lsof -p <pid>`),
затем `python3 scripts/v2_acceptance_fill.py` не годится для L — статусы L проставляются вручную из результатов SQL/UI.
Если сервер 8021 не запущен: `ZHBI_DB_PATH=<worktree>/data/zhbi.anon.db .venv/bin/python -c "import uvicorn; uvicorn.run('app.main:app', host='127.0.0.1', port=8021)"`
из каталога worktree. Тестовые записи только с префиксом `QA-ACC-`; в конце удалить и подтвердить SQL.

**Состояние тестовых данных:** реальная анонимная БД не менялась в этом заходе (стенд работает на фейковых данных
в памяти). Пользователи `qa_overnight` / `qa_overnight_2` — преднастроенный QA-вход, не удалять.
**Процессы:** стенд 8031 (`scripts/v2_test_server.py`) и 8032 (старый код, проверка «мутацией») — локальные, можно остановить.

---

## Этап 0 — завершение текущего задания (два дефекта)

**Статус: ЗАВЕРШЕНО.** Коммиты `4e5ebf2` (код) и `731dc63` (документация).
Чек-лист `Docs/v2-contract-acceptance-checklist.md`, раздел 7 — все
обязательные сценарии PASS живым браузером, один под-случай
(`confirmContractReplacement`'s defense-in-depth) — NOT TESTED браузером,
верифицирован чтением кода (реальное окно гонки недостижимо через UI).

## Этап 1 — приёмка уже перенесённых форм (в работе)

Разделы для проверки: пользователи, роли, доступ по проектам/объектам,
проверка эффективных прав, проекты и объекты (карточки, адрес, карта,
фото/вложения), контрагенты (договоры/спецификации/контракты — уже
покрыто этапом 0 для контрактов).

Таблица соответствия V1/V2 — см. ниже, заполняется по ходу.

### users-access.js / projects-objects.js — инвентаризация кода

Проведена фоновым агентом (полный список функций, API-эндпоинтов, форм,
черновиков, обработки ошибок чтения, защиты от повторной отправки,
TODO/заглушек) — результат учтён при приёмке ниже. Ключевые выводы:
у обоих модулей есть `hasUnsavedChanges`/`guardLeave` (у
projects-objects.js ещё и `destroy` для мини-карты); у users-access.js
НЕТ отдельного `{loaded,error}` на верхнем уровне (ошибка = замена
содержимого вкладки текстом в `render()` try/catch — рабочий паттерн, не
баг), у projects-objects.js есть явный `state.loaded/state.loadError`.
Старого бага «ошибка чтения = пустой список» не найдено нигде. Найдена
одна асимметрия защиты от двойного клика: кнопка удаления ОДНОГО
вложения в projects-objects.js (~716) не дизейблится перед запросом (в
отличие от соседней кнопки аватара) — низкий риск (общий диалог
подтверждения синглтон), но стоит поправить в рамках Этапа 1/2.
TODO/FIXME технических нет; два сознательных текстовых «не перенесено»:
диагностика/сеансы/вход-от-имени (users-access.js:763) и полный редактор
контракта у объекта (projects-objects.js:21-23) — оба явно ведут в V1.

### Пользователи и доступ → Доступ к объектам — живая проверка (2026-09-19)

Создан тестовый пользователь `qa_night_test1` (id=1600, "QAnight Test")
специально для проверки реального flow выдачи/отзыва доступа, не трогая
права существующих пользователей. Сценарий: «Показать все» → «Выдать»
для Проект-1/Объект-1 → отметить роль «Комплектовщик» → «Сохранить
изменения» → подтверждено И через UI («Доступ сохранён», сводка
«Комплектовщик напрямую», «1 из 2 объектов»), И через SQL
(`user_access`: `26|1600|1|1|contract|...`). Затем снял роль → сохранил →
подтверждено: строка в `user_access` исчезла (0 строк), сводка вернулась
к «0 из 2 объектов». Grant/revoke — PASS, живой браузер + SQL.

Ранее (до этого захода) на РЕАЛЬНОМ пользователе `user2` проверено, что
диалог несохранённых изменений корректно всплывает при переключении
чекбокса роли и попытке уйти со страницы, и что «Остаться»/«Отменить»
не приводят к реальной записи — сохранение НЕ нажималось.

Тестовый пользователь `qa_night_test1` (id=1600) полностью удалён после
проверки (SQL `DELETE FROM users WHERE id=1600`, подтверждено — 0 строк
и в `users`, и в `user_access`; список пользователей в UI показывает
8 записей без него).

Замечено: в БД уже существуют два пользователя с логинами
`qa_overnight`/`qa_overnight_2` («QA-Overnight Test» / «QA-Overnight
Тест2») — это НЕ созданные в рамках этого захода сущности, а
преднастроенный тестовый вход для браузерной сессии (под ним залогинена
текущая вкладка V2 — виден в шапке как «QA-Overnight Test»). Не удалять
— это рабочая учётная запись для автономной проверки, не тестовый мусор
для очистки. У `qa_overnight_2` в поле «должность» нестандартный текст
(«QA тестовая должностX v2ь») — похоже на осознанно введённые тестовые
данные для проверки формы профиля, не путать с багом; отдельно не
разбирался, т.к. запись возникла до текущего окна проверки и не мешает
приёмке.

### «Роли» и «Проверка доступа» — живая проверка (2026-09-19)

Роли (PASS, браузер + SQL): создание роли (`object_roles`), правка ячейки
матрицы и «Сохранить изменения» (`role_features`: `qa_night_role|plan|read`),
переименование, удаление с delete-plan (диалог показал верные счётчики:
0 пользователей, 1 ячейка, 0 грантов), после удаления в обеих таблицах 0
строк. Проверка доступа (PASS, браузер): для пользователя с ролью на
объекте эффективные права показывают источник («Роль «Фамилия4» → проект
«Проект-1»»), для администратора — «Полный доступ». Тестовая роль
`qa_night_role` удалена.

### «Проекты и объекты» — начато (2026-09-19)

Открытие карточки Объект-1 на 1366×768: реквизиты, адрес/координаты,
мини-карта с пином, блок «Фото и вложения» («Файлов нет» + загрузка) —
рендерятся. NOT TESTED: создание/правка/удаление проекта и объекта,
загрузка/просмотр/удаление вложения, фильтры, поиск, выходы с черновиком.

Замечено «+ Проект» ничего не открыл. По коду дефекта не нашёл
(`selectNode` → `withNavGuard` → `render`, обработчики навешиваются в конце
`render()`); наиболее вероятная причина — методика: при эмуляции 1366×768
скриншот масштабируется до 800×449, а ref-клик вернул координаты (195,701)
в пикселях viewport → клик ушёл за кадр. НЕ подтверждено — перепроверить
после входа (скриншот с scale=1 или `#po-add-project`.click()).
Строка консоли `Cannot set properties of null (setting 'disabled')`
(`projects-objects.js:373:34`) — в накопленном за сессию логе, строка 373
не содержит `.disabled` (сверено с файлом, отдаваемым сервером); после
перезагрузки лог чист. Перепроверить на свежем логе.

### Правки кода (НЕ закоммичены, живой проверки ещё нет)

1. `projects-objects.js`, кнопка «удалить вложение»: блокировка всех кнопок
   удаления ДО диалога подтверждения, разблокировка при «Отмена»/ошибке
   (раньше повторный клик мог отправить второй DELETE). NOT TESTED в
   браузере.
2. `api.js` — новый `api.upload(path, formData)` через тот же счётчик записей;
   загрузка вложения в `projects-objects.js` переведена с сырого `fetch` на
   него: теперь идущая закачка блокирует смену раздела/уход в V1
   (`withNavGuard`, `hasPendingWrites`). Автотест без браузера и БД:
   `node scripts/verify_v2_api.mjs` — 19 проверок PASS (на старом `api.js`
   тест падает: `api.upload is not a function`). Живая проверка закачки —
   NOT TESTED.

## БЛОКЕР (2026-09-20) — подробности: нет входа в V2-вкладку браузера

Сервер 8021 перезапущен из worktree (pid 16031; cwd — worktree, БД
`data/zhbi.anon.db` worktree — сверено `lsof`), сессия браузера потеряна
(`/me` → 401). Ввод пароля для аутентификации мне запрещён, сброс пароля
и правка `sessions` в обход входа — тем более. Нужно: пользователь входит
сам в открытой вкладке (`http://127.0.0.1:8021/v2`, логин `qa_overnight`).
Пока входа нет — только работа с кодом и автотестами без браузера.

Следующий шаг в рамках Этапа 1: после входа — «Проекты и объекты»
(создание/правка/удаление тестового проекта и объекта, вложения, выходы с
черновиком), затем «Пользователи» (профиль/пароль/поиск), затем базовый CRUD
контрагентов; параллельно без браузера — инвентаризация V1-форм СМУ /
физлиц / типов / марок и сверка дизайн-референсов (фоновые агенты).

## Этап 2 — закрепление общих компонентов V2

Не начат (реализация). Разведка проведена 2026-09-20 фоновым агентом
(чтение дизайн-референсов и кода; всё ниже — «по коду», в браузере НЕ
проверено).

**Сверка референсов с реальностью.** `FORM-INVENTORY.md`/`MIGRATION-MAP.md`
описывают только V1 (83 `modal-backdrop`, все «Запланировано»); V2 в них не
учтён. Архитектура V2 отличается от плана в `DESIGN-SYSTEM.md §7`
(отдельный `styles.css`, классы `v2-*`, токены `--bg/--ink/--line/…`, а не
`ui-system.css` поверх legacy, `--ui-*`) — принято как есть. Размеры окон
не по стандарту: `.v2-dialog` 420 (стандарт small 480), `.v2-auth-card` 340.
Native `confirm/prompt/alert` в V2: 0.

**Реальное дублирование** (`users-access.js`=UA, `projects-objects.js`=PO,
`counterparties.js`=CP): `escapeHtml` — 6 идентичных копий; `btn()` — 3;
`withNavGuard` — 3; «Удалить нельзя. Мешает: …» — 3 идентичных; подвал
Отменить/Сохранить — 6 РАЗНЫХ вариантов; блокировка полей на время записи —
3 (различаются селектором); текст ошибки — 3 формулы (`err.detail` /
`err.detail||err.message` / `String(err.detail)`) в ~57 местах; `fieldRow` —
одно имя, две разные сигнатуры (PO:429, CP:519 — при выносе ловушка);
каркас страницы — 4 шаблона. Модели dirty (слот / слот+черновики / вычисляемые
части) — три и намеренно разные, каждая уже несёт починенные гонки: НЕ
объединять. Общая часть уже в `dialogs.js`.

**Рекомендованный порядок выносов в `app/static/v2/shared.js`** (каждый —
отдельный коммит, перед ним коммит-точка; после каждого — живой прогон
«правка → Отменить → Сохранить → ошибка сервера → уход с dirty» по UA/PO/CP):
1) `escapeHtml`, `btn`, `describeBlockers`, `optionsHtml` (чистые функции);
2) `errText(err, fallback)` — только строковый `detail`, иначе `message`
   (лечит `[object Object]` при 422; осознанная правка поведения);
3) `createNavGuard(api)` + проверка `api.hasPendingWrites()` в
   `main.js openSection` (сейчас переключатель разделов её не проверяет);
4) `lockControls(root, selector)` с `unlock()`, возвращающим `disabled` только
   тем, у кого он менялся;
5) `mountPageShell` — последним, меняет внешний вид PO и карточки CP
   (закреплённый подвал), проверять на 1920×900 и 1366×768.
Отдельно, без JS: в `styles.css` нет `:focus-visible`, `.v2-danger`, стиля
ошибки поля, `input:disabled`. НЕ выносить: модели dirty/`requestLeave`,
универсальный подвал, `fieldRow`, универсальные поиск/дерево/таблицу, всё
доменное из CP (`contractOpLock`, `_inFlight`, `stillSame`).

**Кандидаты в дефекты по коду (проверить в браузере, затем чинить в V2):**
- Б1. PO: ошибка «Сохранить и продолжить» при выходе с dirty невидима
  (`resolveDirty` пишет только в `state.status_msg`, вызывающие делают `return`
  без `render()`); UA пишет и в `status.textContent`, CP вызывает `render()`.
- Б2. PO: статус «Есть несохранённые изменения» залипает после отказа по
  диалогу «Не сохранять» (`render()` переписывает статус только при непустом
  `status_msg`).
- Б3. Утечка класса `v2-app` между разделами (UA ставит на общий
  `#v2-content`, CP-карточка снимает, PO не ставит, `main.js` не сбрасывает):
  закреплённый подвал у PO зависит от порядка посещения разделов.
- Б4. `main.js openSection` не проверяет `api.hasPendingWrites()`.
- Б5. `[object Object]` в сообщении при 422 (`detail` — список).
- Б6. UA `#ua-access-search`: на каждый `input` перерисовывается вся панель
  вместе с полем, фокус не восстанавливается.
- Б7. UA: ошибка первого чтения без «Повторить»; повторный клик по той же
  вкладке ничего не делает (`goto`: `page === state.page`).
- Б8. Мёртвые слоты ошибок `#cpf-error`/`#pf-error` — ошибки валидации только
  в статусе подвала, хотя `requestLeave` в CP говорит «ошибки показаны в форме».
- Б9. Кнопка «Удалить» в подтверждении — primary-акцент, варианта danger нет.
- Б10. `dialogs.js`: второй вызов диалога получает промис первого; если
  открыт confirm/info, `resolveDirty` трактует чужой ключ как «save».
- Б11. Нет `aria-label` у полей поиска/inline-форм, нет `aria-live` у статусов.

## Этап 3 — перенос простых справочников (СМУ, физлица, типы/подтипы,
марки изделий)

Не начат (реализация). Порядок: СМУ → физлица → типы/подтипы → марки.
Каждая группа идёт только после полной приёмки предыдущей. Переносить
начинаем ТОЛЬКО когда приёмка Этапа 1 не имеет известных потерь данных /
нарушений прав / дублей / сломанных базовых операций.

### Разведка V1 (2026-09-20, чтение кода, фоновый агент; AST для эндпоинтов)

- **СМУ и физлица** — в V1 ОДНА общая модалка «простой справочник»
  (`index.html:7057`, `app.js openSimpleCatalog:11386`), меню «Действия →
  Справочники → Проект и объект». Операции: список (без поиска), добавить
  (Enter), переименовать по месту (blur/Enter), удалить через общий
  `openDictDelete` (delete-plan → обязательная замена, если есть ссылки;
  merge нет). API: `GET/POST /smu`, `PATCH /smu/{id}`; `GET/POST
  /individuals`, `PATCH /individuals/{id}`; удаление только через
  `/dictionaries/{smu|individual}/{id}/delete-plan|candidates|delete`
  (`DELETE /smu` нет). Права: `dict_smu` / `dict_individuals` (read — меню,
  write — POST/PATCH; область service, без `object_id`); удаление —
  `dict_delete`. Уникальность без учёта регистра проверяет код (409). Ссылки:
  `objects.smu_id`; физлицо — две колонки (`smu_director_id`,
  `responsible_id`). Ошибки в V1 через `alert()` — в V2 делать общим диалогом.
- **Типы изделий** — отдельного каталога НЕТ: 4 зашитых значения
  (`models.py:292`), создать/переименовать/удалить нельзя. Есть только
  **подтипы** (объектные; форма выбора объекта в шапке): добавить и удалить
  (крестик на чипе → `openDictDelete("subtype", "<objectId>|<тип>|<подтип>")`),
  переименования подтипа нет ни в UI, ни в API. Права `dict_subtypes`
  write. Наполняются сами при импорте чертежа.
- **Марки** — вложены в ту же модалку подтипов (секция «Марки — N» на тип,
  таблица марка/изделий/позиций, сотни строк — нужен поиск/скролл) + отдельная
  модалка «Префиксы марок» (`dict_mark_prefixes`). Операции: создать,
  переименовать (сервер сам двигает `elements.mark`/`contract_lines.mark`,
  склеивает позиции), удалить с заменой на марку того же объекта и типа;
  префиксы — добавить/удалить. Права: меню `dict_subtypes` write, запросы
  `dict_marks`; `GET /marks` объектный, только ЖБИ (на МФР — 403 даже админу).
- **Серверные дыры, которые V2 НЕ наследует и не чинит (записать как
  замечания к V1/backend):** `POST /marks`, `POST /allowed-subtypes` и
  `GET /allowed-subtypes` не проверяют доступ пользователя к объекту;
  `POST /mark-type-prefixes` не валидирует `element_type` и молча
  переназначает тип у существующего префикса; сырые `DELETE
  /allowed-subtypes/...` и `DELETE /mark-type-prefixes/...` в обход
  `dict_delete` — V2 их звать не должен.
- **Для V2 нужно:** пикер замены на СМУ/физлицо/марку/подтип (сейчас есть
  только контрактный, `counterparties.js:1826-1915`); правило корзины V2 —
  `isSystemAdmin || dictDelete==="write"` (V1 показывает корзину без
  проверки); ключи прав `dict_smu`, `dict_individuals`, `dict_subtypes`,
  `dict_marks`, `dict_mark_prefixes` в `perms` (`main.js:94-105`); флаг
  «выключен» — константа в `main.js` + `available:false` (в навигацию не
  попадёт). «Типы» — подтвердить с пользователем, что редактор типов не
  нужен (типов как справочника нет) — иначе «типы» сводятся к группировке
  подтипов по 4 значениям.
- **Порядок 3→4 осложнён:** подтипы и марки — одна форма V1 с общей шапкой
  выбора объекта. Без `dict_marks` read или на МФР-объекте `GET /marks` даёт
  403, а V1 не ловит ошибку (форма пустая) — в V2 ловить и показывать.

## Этап 4 — сквозная регрессия и визуальная приёмка

Не начат.

## Известные блокеры

(пусто)

## Тестовые сущности на данный момент

БД чиста от тестовых сущностей (все созданные во время этапа 0 удалены,
подтверждено SQL-проверками — см. чек-лист).

## Следующий безопасный шаг

Начать приёмку раздела «Пользователи и доступ» (users-access.js) по
пунктам из задания: пользователи, роли, доступ по проектам/объектам,
проверка эффективных прав.
