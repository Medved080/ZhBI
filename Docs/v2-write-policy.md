# Изменяющие операции V2 в ограниченном выпуске

*Файл создаётся скриптом `scripts/gen_v2_write_policy.mjs` из таблицы политики шлюза `app/static/v2/write-gate.js` — руками не править.*

Шлюз стоит в единственной точке отправки запросов V2 (`api.js`) и работает по принципу «запрещено всё, что не разрешено явно»:
изменяющий запрос, которого нет в первой таблице, до сервера не доходит. Решает не название метода, а фактический эффект:
POST-чтение отчётов (`/reports/*`), выгрузки (`/export.xlsx`, `/export.pdf`) и чтение (GET) шлюзом не ограничиваются; личные настройки
(гамма, цвет подписей, «Ознакомился») помечены как «личная» — они видны и в V1 у того же пользователя. Серверная авторизация действует
на все операции, разрешённые и нет; шлюз её не заменяет.

## Разрешено (20)

| Экран | Действие | Запрос | Риск | Доказательство проверки |
| --- | --- | --- | --- | --- |
| (вход) | Вход в V2 тем же логином и паролем, что в V1 | `POST /login` | аутентификация, данные не меняет | тот же эндпоинт, что у V1 (пароль вводит человек) |
| appearance | Смена личной цветовой гаммы | `PATCH /users/{id}/ui-theme` | личная (общая для V1 и V2 у этого пользователя) | живая проверка: запись → SQL → возврат |
| label-color | Личный цвет подписей марок (сброс — null) | `PATCH /users/{id}/label-color` | личная (V1 и V2) | живая проверка: запись → SQL → сброс |
| changelog | Отметка «Ознакомился» в «Что нового» | `POST /changelog/ack` | личная | живая проверка: запись → SQL |
| dict-smu | СМУ: добавить запись | `POST /smu` | общая (справочник) | живая проверка на синтетической записи |
| dict-smu | СМУ: переименовать | `PATCH /smu/{id}` | общая (справочник) | живая проверка на синтетической записи |
| dict-smu | СМУ: удалить НЕиспользуемую запись (по плану последствий; с зависимостями — отказ) | `POST /dictionaries/smu/{id}/delete` | общая, необратимо | живая проверка: удаление неиспользуемой, отказ для используемой |
| project-card | Карточка объекта: наименование, сроки, вехи (запись целой карточки) | `PUT /settings/project-card` | данные объекта | живая проверка: SQL — JSON целиком, исходный восстановлен |
| report-notes | События, задачи, вопросы: сохранить редакцию на дату | `PUT /settings/report-notes` (только поля: effective_date, key_events, key_tasks, open_questions) | данные отчётов | живая проверка: новая редакция, отказ занятой даты |
| report-notes | События, задачи, вопросы: удалить редакцию (с подтверждением) | `DELETE /settings/report-notes/{дата}` | данные отчётов, необратимо | живая проверка: удаление своей редакции |
| subtypes | Подтипы: добавить подтип типа элемента объекта | `POST /allowed-subtypes` | общая (справочник объекта) | живая проверка на синтетической записи |
| subtypes | Подтипы: удалить неиспользуемый (по плану; используемый — отказ) | `POST /dictionaries/subtype/{ключ}/delete` | общая, необратимо | живая проверка: удаление и отказ для подтипа с 2021 изделием |
| mark-prefixes | Префиксы марок: добавить / заменить тип (с подтверждением) | `POST /mark-type-prefixes` | общая (влияет на разбор марок при импорте) | живая проверка: добавление, замена, возврат |
| mark-prefixes | Префиксы марок: удалить неиспользуемый | `POST /dictionaries/mark_prefix/{ключ}/delete` | общая, необратимо | живая проверка: удаление через план |
| late-threshold | Порог опоздания поставки объекта | `PUT /settings/info-plate` (только поля: late_threshold_days) | общая для объекта | живая проверка: 0→2→0, SQL |
| zone-colors | Цвета кранов объекта (только изменённые) | `PUT /zone-colors` | общая для объекта | живая проверка: цвет → SQL → возврат |
| status-colors | Цвета статусов (только изменённые) | `PUT /status-colors` | общая для всех (видна в V1) | живая проверка: цвет → SQL → возврат |
| marker-shapes | Форма маркера по паре «слой / тип» (только изменённые) | `PUT /element-shapes` | общая для всех (видна в V1) | живая проверка: контур→круг→контур, изменена одна строка |
| mfr-colors | Цветовая схема модели МФР объекта (целиком) | `PUT /revit-plan/colors` | общая для объекта | живая проверка: запись, шаблон, возврат |
| blocks | ЗР: базовый срок (начало / конец) | `PATCH /objects/{id}/block-works/{id}` (только поля: plan_start, plan_end) | данные учёта по блокам | живая проверка: пусто → срок → пусто, SQL, журнал |

## Временно отключено (10 групп; всё остальное, чего нет в таблице выше, отключено тоже)

| Экран | Действие | Запрос | Риск | Почему отключено |
| --- | --- | --- | --- | --- |
| blocks | ЗР: версия прогноза и примечание | `PATCH /objects/{id}/block-works/{id}` | данные, необратимо (версии прогноза копятся и не отменяются) | на настоящем backend не проверялись |
| users-access | Пользователи: создание и правка | `POST/PATCH /users[/{id}]` | права и учётные записи | приёмка раздела не завершена |
| users-access | Задать пароль пользователю | `POST /users/{id}/set-password` | пароли | пароли выполняются только человеком в текущем интерфейсе |
| users-access | Доступ к проектам и объектам (выдача, снятие ролей) | `PUT /users/{id}/access` | права | приёмка раздела не завершена |
| users-access | Роли и их права (создание, порядок, матрица, удаление) | `POST/PUT/DELETE /roles[/…]` | права | приёмка раздела не завершена |
| (вход) | Обязательная смена пароля | `POST /me/change-password` | пароли | смена пароля — в текущем интерфейсе |
| sessions | Мои сеансы: завершение сеансов | `DELETE/POST /me/sessions[/…]` | вход на других устройствах | на настоящем backend не проверялось (нет второго сеанса) |
| dict-individuals | Физлица: добавление, правка, удаление | `POST/PATCH /individuals[/{id}]\|/dictionaries/individual/{id}/delete` | персональные данные | отдельно на настоящем backend не проверялись |
| counterparties | Контрагенты, договоры, спецификации, контракты, привязки, плановая дата поставки | `POST/PATCH/PUT/DELETE /(counterparties\|agreements\|specifications\|contracts\|elements)[/…]\|/dictionaries/(кроме smu, subtype, mark_prefix).+` | данные контрактации | приёмка раздела не завершена |
| projects-objects | Проекты и объекты: создание, правка, удаление, фото, вложения | `POST/PATCH/PUT/DELETE /(projects\|objects\|attachments)[/…]` | данные иерархии, удаление с зависимостями | приёмка раздела не завершена |

Пояснение на экране и переход в текущий интерфейс показывает оболочка (`main.js`); поля и кнопки отключённых операций на экранах
«Физлица», «Мои сеансы» и «Учёт по блокам» (прогноз, примечание) не показываются или заблокированы.
