# Перенос V1 → V2: область «exchange2» (продолжение exchange — обязательный перенос)

Ветка `feature/v2-exchange2` (worktree `/Users/max/zhbi-w-exchange2`, от `feature/v2-next`, после слияния
`feature/v2-exchange`). Задача — то, что в `Docs/v2-progress/exchange.md` осталось «не сделано»: экспорт/импорт
настроек, загрузка из PDF (детально и «только фасады»), панели облицовки шахты, внешние 3D-модели (FBX), остальные
операции обмена (образцы файлов, список чертежей, пользователи «Моей работы», справка отчёта, разбор ячейки
«Графика поставки», отладочная очистка), плюс два известных дефекта V1 (`zone_sync` IntegrityError→500, DXF/Revit
несколькими транзакциями).

Проверки — на НАСТОЯЩЕМ backend (`scripts/real_auth_server.py`, временная копия обезличенной БД, порт 8200, входы
`admin`/`user2`/`user3`/`user4`). Боевые данные, `test_data/`, `Input/`, `uploads/` не читались: PDF и DXF —
синтетические комплекты, построенные ПОД разбор V1 (см. ниже), а не образцы заказчика.

## Что сделано (по операциям)

| Операция | Что в V2 | Статус |
| --- | --- | --- |
| `settings-io` | Сверка → явное подтверждение «файл = копия учётных записей» → применение по digest; перечитывание сверки перед применением | 5 |
| `shaft-panels` | Разбор DXF (профиль ГП1/ГП2) → сводка (панели, замечания флажками) → применение по токену; отмена незавершённого анализа | 5 |
| `pdf-import` | Полный разбор (фоновая задача, прогресс) и «только фасады» (синхронно) → сводка → применение по токену; отладочная очистка справочников | 5 |
| `external-models` | Загрузка FBX (клиентский разбор Three.js/FBXLoader) → список → правка размещения (числа, сверка версии) → перецентровка → удаление | 5 |
| остальные операции обмена | Версии чертежа, печать, справка отчёта, выбор пользователя «Моей работы», разбор ячейки «Графика поставки» | — (добавлено к уже готовым экранам) |

### Экспорт/импорт настроек (`settings-io`)

Backend: `app/settings_import.py` — общая логика (`parse_payload`, `analyze` — сверка без записи и без раскрытия
хэшей/солей, `_digest` — хэш файла и состояния БД (пользователи, цвета, видимость подписей), `apply_payload` —
применение). Использует ОБА входа: одношаговый V1 `POST /settings/import` (поведение не изменилось; добавлена
только блокировка записи первым действием — раньше запись шла без неё, сбой посреди цикла оставлял частично
закоммиченные строки из-за неявной транзакции SQLite) и новый двухфазный V2 `POST /settings/import/analyze` →
`POST /settings/import/apply` (`digest` тела форм — sha256, сверяется заново на apply; несовпадение — 409 «сверка
устарела», ничего не применяется). Права — `require_service_feature("settings_io", "write"/"read")`, как в V1
(только администратор сервиса).

Экран V2 — `app/static/v2/exchange-settings.js`: предупреждение о содержимом файла, кнопка выгрузки (переиспользует
существующий allowlist `api.fetchFile`), форма сверки, таблица расхождений (пользователи create/update — БЕЗ
хэшей/солей, только факт «пароль будет заменён»; цвета статусов; счётчик правок видимости подписей; пропущенные
объекты), обязательный чекбокс подтверждения (блокирует кнопку «Применить», пока не отмечен — независимо от
наличия расхождений), диалог подтверждения перед отправкой, обработка устаревшей сверки/сетевого сбоя/потерянного
ответа по общим правилам области `exchange` (`exchange-common.js`).

Шлюз записи: `settings.analyze` (multipart `.json`, без лишних полей) и `settings.apply` (multipart `.json` +
поле `digest`, проверка формы sha256).

### Панели облицовки шахты (`shaft-panels`)

Backend НЕ менялся: `app/shaft_panels_host.py` (роутер `app/shaft_panels/api.py`, `build_router`) уже был подключён в
`app/main.py` для V1, права те же (`assert_object_feature(conn, user, object_id, 'drawings', 'write')`, как у
загрузки DXF). Экран V2 — `app/static/v2/exchange-shaft-panels.js`: объект (список сужен до `kind='zhbi'` — профиль
работает только с ЖБИ, `storage.analyze` иначе отказывает), файл DXF, необязательная толщина панели, разбор →
таблица панелей (марка, шахта/сторона, габарит, расположение) → замечания к чертежу (каждое — отдельный флажок,
без отметки применение недоступно) → (если указана толщина) сводка для применения (новые/обновлённые/без
изменений/отсутствующие, конфликты блокируют кнопку) → диалог подтверждения → применение. Токен разбора явно
отменяется (`DELETE /shaft-panels/pending/{token}`) при смене файла/объекта/толщины и при уходе с экрана —
сервер держит общий лимит в 8 одновременных разборов на всех пользователей, токен принадлежит только своему
пользователю (чужой/отменённый/потраченный — отклоняется).

Синтетика: `scripts/gen_synthetic_shaft_dxf.py` (варианты `ok`/`no_axes`/`missing_mark`/`overlap`/`bad_level`) —
минимальный DXF, проходящий строгий профиль `app/shaft_panels/parser.py` (две развёртки ГП1/ГП2, восемь сторон,
план шахт, атрибуты осей 5/7/Е/Ж, обязательные отметки 0/+15,000/+25,800/+31,300). Целевая сетка осей — объект 1
тестовой копии (обезличенная копия несёт настоящую геометрию, у объекта уже зарегистрированы оси 5/7/Е/Ж из
реального импорта — ничего в БД дозаписывать не потребовалось).

### Загрузка из PDF (`pdf-import`)

Backend НЕ менялся: `/import-pdf/analyze/start`, `/import-pdf/analyze/progress/{job_id}`, `/import-pdf/apply`,
`/import-pdf-facade/analyze|apply`, `/objects/{id}/clear-import-data` уже были в V1. Экран V2 —
`app/static/v2/exchange-pdf.js`: переключатель режима (полный/только фасады), объект (список сужен до `kind='mfr'`),
файл, «Разобрать». Полный режим опрашивает фоновую задачу (`pollJob`, интервал 700мс, та же логика прогресса, что в
V1 — два этапа «Разбираю помещения»/«Разбираю стены и перекрытия» дают вклад по 50%), показывает сводку (счётчики,
предупреждения, по этажам) и применяет по токену; «только фасады» — синхронно (секунды), сводка по этажам
(ширина×глубина секций). Отдельный блок (`<details>`, свёрнут по умолчанию) — отладочная очистка справочников по
трём группам (помещения / секции-этажи / виды работ и статусы блоков), с явным подтверждением текста последствий и
предупреждением о необратимости через интерфейс.

Синтетика: `scripts/gen_synthetic_pdf_set.py` (варианты `ok`/`small`/`no_rooms`/`bad_scale`) — 22-страничный PDF под
жёсткий профиль `app/pdf_rooms.py`/`app/pdf_facade_import.py` (слой OCG помещений, подписи масштаба и осей,
фасадные отметки). Целевой объект тестовой копии — `object_id=3` (МФР, есть только разделы АР/КР из Revit, раздел
`PDF` пуст — не пересекается с загружаемыми данными).

### Внешние 3D-модели объекта — FBX (`external-models`)

Backend НЕ менялся: `POST/PATCH/DELETE /objects/{id}/external-models[…]`, `recenter`, `geometry-features`, `bounds` уже
были в V1. Перенесено то, что не требует 3D-сцены: **список** (таблица — название/вид/размер/сводка размещения),
**загрузка** (файл .fbx → клиентский разбор через ДИНАМИЧЕСКИ импортируемые вендоренные `three` (importmap),
`FBXLoader.js` и `app/static/external-models/fbx.js` — ТОТ ЖЕ код, что считает `source_anchor_mm`/`bbox_size_mm`/
`mesh_count` в V1; неподдерживаемый профиль осей отклоняется уже на этом шаге, до сети — `fbx.js` использует тот же
ограниченный ридер GlobalSettings, что и сервер, `app/static/external-models/fbx-global-settings.js`), диалог
подтверждения называет РЕАЛЬНО разобранную геометрию, **правка размещения** числовыми полями (смещение X/Y/Z,
поворот, масштаб X/Y/Z, название) со сверкой `expected_revision` (оптимистичная блокировка — устаревшая версия даёт
409), **перецентровка**, **удаление** (с подтверждением необратимости). 3D-сцена, визуальная калибровка на плане и
автосовмещение фасада (`auto_placement_status`/`auto_placement_json` — вычисляются в 3D V1) в V2 не перенесены и не
разрешены шлюзом записи (только явно используемые V2-формой поля).

Синтетика: `scripts/gen_synthetic_fbx.py` — минимальный, но НАСТОЯЩИЙ бинарный FBX (формат Kaydara FBX Binary 7400)
с одним треугольным мешем, проходит и серверный ограниченный ридер, и НАСТОЯЩИЙ FBXLoader Three.js (проверено и в
Node, и в браузере — оба дали идентичный результат: 1 меш, 3 вершины, 1 треугольник). Варианты `bad_axis`/`bad_unit`
— валидный бинарный FBX с иным профилем осей/некорректным масштабом, отклоняются что клиентом (та же проверка), что
сервером независимо. Самая трудоёмкая часть генератора — воспроизвести условие остановки чтения узлов верхнего
уровня (`endOfContent()` в FBXLoader.js вызывается ПЕРЕД каждым узлом, не только в конце файла) и порядок узлов
(`GlobalSettings` строго раньше `Objects`/`Connections` — этого требует серверный ридер).

### Остальные операции обмена (пп. 4 задания)

Добавлены к уже перенесённым экранам общими модулями (не новый экран):

* **Версии чертежа объекта** (`GET /objects/{id}/drawings`) — сворачиваемая справка «Версии чертежа объекта» на
  экране «Загрузить чертёж» (`exchange-drawing.js`): файл, дата загрузки, число изделий, текущая версия помечена.
  Перечитывается при смене объекта.
* **Печать отчёта** — кнопка «Печать» на КАЖДОМ отчёте (`read-screen.js`, `paintReport()`), вызывает `window.print()`
  (как в V1, `app.js:23022`); печатная CSS (`styles.css`, `@media print`) убирает шапку/навигацию/панель действий —
  печатается сам отчёт.
* **Справка отчёта** (`GET /report-help/{key}`) — кнопка «Справка» там, где в `screens.json` у секции указан
  `helpKey`; текст показывается через `showInfoDialog` (диалог V2, не `alert`). Добавлено девяти отчётам (все, у
  кого справка есть в `app/report_help.py`).
* **Выбор пользователя в «Моей работе»** (`GET /objects/{id}/activity-users`) — новый тип контрола `"users"` в
  `read-screen.js`: обычному пользователю (`can_choose: false`) выпадающий список не показывается вовсе (сервер и
  так отдаёт только его самого); у кого есть право «Чужие действия» — «Я» / «Все» / поимённо, смена перестраивает
  отчёт (`all_users`/`user_ids` в теле запроса).
* **Разбор ячейки «Графика поставки» по маркам** (`POST /reports/delivery-schedule/cell`) — ячейки РАСКРЫТЫХ (не
  свёрнутых) строк дерева кликабельны (`deliveryCell`/`deliveryRows`, `reports-exchange.js`, несут `gkey`-путь от
  корня и ключ колонки); щелчок показывает разбор по маркам (нужно/план/факт, что не перекрыто, откуда можно взять
  — тот же ответ, что у наведения в V1). Требует тех же дат/шага, что уже построили показанный отчёт — сервер их не
  подставляет сам для этого запроса (в отличие от самого отчёта), взяты из ответа отчёта (`s.data`), если человек
  их не менял. `api.js` (`readPost`) и write-gate не потребовали правки прав — это чтение, тот же путь, что у
  остальных отчётов.
* **Образцы файлов импорта** — уже были доступны во всех формах загрузки (`mountTemplates`, `exchange-common.js`,
  кнопка «Скачать образец» у каждой). Отдельного независимого экрана эта операция не требует — не делалось ничего.

## Найденное и исправленное

* `POST /settings/import` (V1, одношаговый): запись шла без `begin_write` — сбой ПОСРЕДИ цикла (например,
  невалидный цвет статуса не на первой строке) оставлял ранее выполненные INSERT/UPDATE в БД (неявная транзакция
  SQLite не гарантирует «всё или ничего» без явного `BEGIN`). Теперь — блокировка записи первым действием,
  коммит один раз в конце; поведение успешного пути не изменилось (проверено сравнением ответа и по журналу).
* **`app/zone_sync.py` (дефект из `Docs/v2-progress/exchange.md`, «Найденное, но НЕ исправленное»): исправлено.**
  Повторная загрузка того же чертежа, где полигон зоны теряет подпись (опознание вне допуска — та же зона, тот же
  `dxf_handle`, но по имени/номеру больше не сопоставляется с записью справочника), пыталась вставить НОВУЮ запись
  с уже занятым `dxf_handle` → `UNIQUE(source_file, dxf_handle)` → необработанный `IntegrityError` → 500, при этом
  этап изделий уже мог быть записан. Теперь `sync_zones()` ловит именно эту `IntegrityError`, откатывает свою
  собственную (ещё не закоммиченную) часть работы и бросает `ZoneSyncConflict` — понятное сообщение с чертежом,
  handle полигона и категорией; `app/dxf_import.py` превращает его в `DxfProcessingError(409, …)`, уже подключённый
  к существующей обработке ошибок `/import-dxf/apply`. **Смысл зон не изменён** — не решается, какую запись считать
  правильной (это по-прежнему решение пользователя, как и было записано в exchange.md), просто сбой не роняет
  сервер и не оставляет БД в частично вставленном состоянии. Семантика «сбой посреди загрузки оставляет
  завершённые этапы, повтор тем же токеном догружает» — та же, что у всего остального применения чертежа, не
  менялась. Проверено `scripts/verify_zone_sync_conflict.py` (напрямую вызывает `zone_sync.sync_zones()` дважды на
  временной копии обезличенной БД — воспроизводит конфликт без разбора настоящего DXF, `ZoneRecord` тот же
  dataclass, что использует `scripts/zone_parser.py`): **7 проверок, 0 не пройдено** (конфликт даёт
  `ZoneSyncConflict` с понятным текстом вместо падения; после отказа в БД осталась ровно исходная запись без
  дублей/порчи; повторная загрузка с восстановленной подписью обновляет ту же запись). Существующие 26 проверок
  `scripts/verify_exchange.py drawing` (включая штатный сценарий staged-failure «сбой на этапе сетки осей» → повтор
  тем же токеном догружает) по-прежнему проходят без изменений — обычный путь загрузки не затронут.

## Не исправлено (осознанно, из пятого пункта задания)

**Применение DXF/Revit несколькими транзакциями** (тот же пункт `exchange.md`, где написано «сделать атомарным —
отдельная задача»): НЕ переделано в одну транзакцию. Это не просто технический долг, а задокументированное и уже
ИСПОЛЬЗУЕМОЕ поведение: «сбой посреди загрузки оставляет завершённые этапы, повтор с тем же токеном догружает» —
эта семантика явно показана пользователю в подтверждении V2 (`exchange-drawing.js`), описана в `Docs/TZ.md` и
покрыта отдельным тестом (`scripts/verify_exchange.py drawing`, «сбой на этапе сетки осей»). Слияние этапов в одну
транзакцию убрало бы саму возможность «повтора с тем же токеном» (при любом сбое откатывалось бы ВСЁ, а не только
незавершённый этап) — это смена наблюдаемого поведения восстановления после сбоя, а не «совместимое исправление
без смены бизнес-логики». Сделать это безопасно означало бы переписать порядок `commit()` в файле на полтысячи с
лишним строк (`app/dxf_import.py`, аналогично для Revit) и заново проверить каждый сценарий отказа — ровно то
«отдельная задача», о которой сказано в `exchange.md`.

## Чем проверено

* `scripts/verify_exchange.py <порт> <work.db> settings` — HTTP на настоящем backend: права (403 у `user2`/`user4`
  на экспорт, сверку, применение), валидация файла (пустой/не JSON/не объект), сверка не пишет и не раскрывает
  хэш, устаревшая сверка (прямое изменение БД между analyze/apply) → 409 без изменений, откат при сбое ВНУТРИ
  операции (временный триггер на `status_colors`: ни пользователь, ни цвет не остались, события в журнале нет),
  успешное применение (пользователь создан без пароля, роль `view`; цвет применён; журнал не содержит хэш пароля),
  идемпотентность повторной сверки (уже применённое не показывается снова), совместимость одношагового
  `/settings/import` (включая его новую атомарность). **40 проверок, 0 не пройдено.**
* Браузер (`scripts/cdp.mjs`, настоящие клики): `scripts/v2_tests/exchange/chk_settings.mjs` — скачивание без
  исключений; битый файл — отказ текстом; сверка показывает расхождения БЕЗ хэшей; кнопка «Применить» заблокирована
  до отметки чекбокса ДАЖЕ при наличии расхождений; диалог подтверждения называет файл и последствия (пароли/роли);
  успешное применение → результат виден → сохраняется после перезагрузки страницы → виден в V1; устаревшая сверка
  (второй администратор МЕНЯЕТ базу настоящим запросом между сверкой и применением) → 409 текстом, форма сброшена,
  ничего не создано; неизвестный исход (потерянный ответ) → «Результат неизвестен» без автоповтора, операция
  фактически выполнена на сервере, сверка по журналу это подтверждает; роли — `user2`/`user4` получают 403.
  **25 проверок, 0 не пройдено.**
* Шлюз записи: `node scripts/v2_tests/exchange-gate.selftest.mjs` — добавлены проверки формы `settings.analyze`/
  `settings.apply` (расширение, лишние поля, отсутствие/неверный формат `digest`); одношаговый `/settings/import`
  по-прежнему НЕ разрешён шлюзу V2 (только через `/analyze`+`/apply`). **61 проверка, 0 не пройдено** (после
  добавления `shaft.analyze`/`shaft.apply`/`shaft.cancel`).
* `shaft-panels` — HTTP: `scripts/verify_exchange.py <порт> <work.db> shaft` — права (403 у `user2`/`user4`),
  битый DXF, все четыре нарушения профиля (без осей/марки/перекрытие/отметки) понятным текстом и без изменений в
  БД, разбор без толщины не пишет и не даёт применить, разбор с толщиной даёт корректную сводку, применение без
  подтверждения замечаний отклоняется, успешное применение (14 панелей, событие в журнале с `kind: shaft_panels`),
  повтор потраченного токена и применение отменённого токена отклоняются, повторный разбор идемпотентен (0 новых),
  чужой токен нельзя отменить (403). **29 проверок, 0 не пройдено.** Браузер —
  `scripts/v2_tests/exchange/chk_shaft.mjs`: те же сценарии настоящими кликами (флажки замечаний, диалог
  подтверждения называет последствия, результат после перезагрузки страницы и в V1, отмена анализа). **20
  проверок, 0 не пройдено.**
* `pdf-import` — HTTP: `scripts/verify_exchange.py <порт> <work.db> pdf` — права (403), валидация (без файла,
  не-PDF, комплект без помещений — фоновая задача завершается ошибкой понятным текстом), полный разбор не пишет,
  применение (счётчики совпадают с разбором), повтор токена отклонён, повторный разбор идемпотентен (0 новых), «только
  фасады» (разбор не пишет, применение), отладочная очистка (счётчики, журнал, 403). **34 проверки, 0 не пройдено.**
  Браузер — `scripts/v2_tests/exchange/chk_pdf.mjs`: полоса прогресса во время фоновой задачи, сводка, диалог
  подтверждения называет последствия, применение → результат → после перезагрузки страницы сводка не висит и данные
  видны в V1, «только фасады», очистка справочников с подтверждением необратимости, права. **21 проверка, 0 не
  пройдено.** Раздел `pdf_import` помечен `not_applicable` у объектов не-МФР (скрывает экран даже админу) — проверки
  сперва выбирают объект МФР в шапке V2, как и для Revit.
* `external-models` — HTTP: `scripts/verify_exchange.py <порт> <work.db> external_models` — загрузка синтетического
  FBX с реальным мешем (права 403, неподдерживаемый профиль осей/масштаб отклонены сервером понятным текстом, успех,
  поля модели верны, журнал), список, правка размещения (сверка версии — 409 при устаревшей и при чужой правке
  после неё, отказ невалидного масштаба), перецентровка (сверка версии, сброс смещения), удаление (404 на повтор и
  на правку удалённой). **35 проверок, 0 не пройдено.** Браузер — `scripts/v2_tests/exchange/chk_external_models.mjs`:
  загрузка через НАСТОЯЩИЙ файловый ввод и НАСТОЯЩИЙ клиентский разбор (Three.js/FBXLoader) — неподдерживаемый профиль
  осей отклоняется клиентом ДО сети, диалог подтверждения называет реально разобранную геометрию (1 меш, 1
  треугольник), правка/перецентровка/удаление, результат после перезагрузки страницы и в V1, права. **19 проверок,
  0 не пройдено** (разбор без текстур занимает ~20с — таймаут ожидания текстур в `fbx.js`, тот же, что у V1; проверка
  ждёт дольше этого срока).
* «Остальные операции обмена» — HTTP: `scripts/verify_exchange.py <порт> <work.db> misc` — образцы файлов (список,
  скачивание, 404 неизвестного ключа), версии чертежа (список, текущая помечена, 404 несуществующего объекта),
  справка (шесть отчётов, заглушка неизвестного ключа не падает, доступна роли `view`), активность (администратору
  — все с правом, обычному — только он сам), разбор ячейки (форма ответа, 400 без периода). **21 проверка, 0 не
  пройдено.** Браузер — `scripts/v2_tests/exchange/chk_misc.mjs`: версии чертежа на экране, диалог справки, `window.
  print()` вызывается кнопкой «Печать» ровно один раз (подмена функции), выбор пользователя реально перестраивает
  «Мою работу» (второй запрос к `/reports/my-work`), клик по ячейке «Графика поставки» открывает разбор по маркам.
  **8 проверок, 0 не пройдено.**

## Изменённые файлы общего пользования

`app/static/v2/write-gate.js` (блок «область: exchange» — 14 новых строк: `settings.analyze`, `settings.apply`,
`shaft.analyze`, `shaft.apply`, `shaft.cancel`, `pdf.analyze.start`, `pdf.apply`, `pdf.facade.analyze`,
`pdf.facade.apply`, `pdf.clear`, `em.upload`, `em.patch`, `em.recenter`, `em.delete`, плюс валидаторы и проверки
формы — «остальные операции обмена» шлюза НЕ потребовали, это чтение), `app/static/v2/exchange.js` (регистрация
`"settings-io"`, `"shaft-panels"`, `"pdf-import"`, `"external-models"`), `app/static/v2/screens.json` (`settings-io`:
`impl: "exchange"`, `feature: [["settings_io","read"]]` — как у `import-input`, нав-пункт V1 использует
`class="admin-only"` вместо `data-feature`, автоматически не захватывается; `shaft-panels`, `pdf-import`,
`external-models`: `impl: "exchange"` — `feature` не нужен, `data-feature` у V1-кнопок уже захвачен генератором;
у отчётов с `helpKey` и у `report-mywork` обновлены `checks`/`limits`), `app/static/v2/index.html` (importmap для
`three`, идентичный V1 — тот же CSP sha256), `app/static/v2/read-screen.js` (кнопки «Печать»/«Справка», тип контрола
`"users"`, обработчик разбора ячейки «Графика поставки»), `app/static/v2/reports-exchange.js` (ячейки дерева «Графика
поставки» несут `gkey`-путь и ключ колонки), `app/static/v2/api.js` (`readPost` пропускает `/reports/…/cell`),
`app/static/v2/styles.css` (`@media print`), `Docs/TZ.md` (раздел «Обмен данными в V2»). Backend: `app/main.py`
(рефактор `import_settings` на общий `app/settings_import.py`, два новых эндпоинта; остальной backend не менялся —
все операции пп. 3–4 уже существовали в V1). Пятый пункт (дефекты): `app/zone_sync.py` (`ZoneSyncConflict` вместо
необработанного `IntegrityError`), `app/dxf_import.py` (ловит `ZoneSyncConflict`, превращает в `DxfProcessingError`
409), `Docs/DECISIONS.md` (инвариант зон дополнен строкой про отказ вместо падения).

## Итог по заданию

Обязательный перенос (пп. 1–4) завершён: настройки, PDF (детально + фасады), панели облицовки шахты, внешние
3D-модели, остальные операции обмена (образцы файлов, версии чертежа, справка, печать, активность «Моей работы»,
разбор ячейки «Графика поставки»). Пятый пункт (дефекты) — `zone_sync` исправлен и проверен; атомарность
DXF/Revit ОСОЗНАННО не переделана (см. «Не исправлено (осознанно…)» выше — это смена семантики восстановления
после сбоя, а не совместимое исправление, и сама `exchange.md` называет это отдельной задачей).

Точка продолжения (следующая сессия/область), если решат делать дальше:
* Не перенесена очистка справочников Revit (`source: "revit"`) в экран `revit-import` — реализована только для
  `pdf-import` (та же серверная операция `POST /objects/{id}/clear-import-data`, общий backend). Если понадобится —
  добавить строку шлюза со `screen: "revit-import"` на тот же путь (`clearImportDataProblem` уже написан, переиспользуется)
  и кнопку/блок в `exchange-revit.js` (скопировать блок `#pf-clear-*` из `exchange-pdf.js`, заменить `source: "pdf"` на
  `"revit"` и подписи).
* Атомарность применения DXF/Revit — если пользователь решит, что «повтор тем же токеном» больше не нужен и предпочтительна
  полная отмена при любом сбое, это отдельная задача: убрать промежуточные `conn.commit()` в `app/dxf_import.py`
  (аналогично для Revit-модуля), взять `begin_write()` один раз в начале, перепроверить КАЖДЫЙ сценарий отказа заново
  (включая уже существующий тест «сбой на этапе сетки осей» — его придётся переписать на «полный откат», а не «этап
  остался»), обновить пользовательский текст подтверждения в `exchange-drawing.js`/`exchange-revit.js` и `Docs/TZ.md`.
* Отчёты, не перенесённые целиком в V2 (график/диаграммы «Динамики» и «Графика поставки», отбор «Учитывать текущий
  фильтр схемы», вид «сводная» у «Статуса комплектации», период «весь срок») — из `Docs/v2-progress/exchange.md`,
  этой сессией не трогались, вне области пятого пункта задания.
