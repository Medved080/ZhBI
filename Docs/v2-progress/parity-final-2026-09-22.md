# Итог: «V2 до полного соответствия V1» — сводный отчёт по четырём параллельным областям (2026-09-22)

Сводит `map.md`, `access.md`, `charts.md`, `graphics.md` в одну таблицу по требованию задания: не
объявлять перенос завершённым по наличию API/кнопок/разрешений шлюза, а сравнивать сценарий V1 →
сценарий V2 → тот же ли результат. Публикация кода — `Docs/v2-workspaces.md` §0 (публикации №7–9).

## 1. Метод

Для каждого пункта задания (карта, графики отчётов, графические операции, вход под пользователем) —
настоящий backend (копия обезличенной БД, `scripts/real_auth_server.py`), настоящий вход, настоящий
браузер (`scripts/cdp.mjs`, headless Chrome, реальные события мыши — `Input.dispatchMouseEvent`), а
не вызовы внутренних JS-функций. Каждая ветка проверена дважды: своим исполнителем и НЕЗАВИСИМО ещё
раз при слиянии (см. эту сессию: 41+38+69+38 проверок веток, затем 251/251/38/38/69 при интеграции —
backend 7/7, модель 189/189, оболочка 87/87 повторены на каждой из трёх публикаций).

## 2. Сценарии: способ в V1 → способ в V2 → тот же результат?

| Сценарий | V1 | V2 | Тот же результат? | Чем проверено |
| --- | --- | --- | --- | --- |
| Общая карта проектов | MapLibre: метки, кластеры, поиск/фильтр, легенда, попап объекта | То же, тот же модуль `map.js` целиком; кнопка попапа — сразу на рабочее место (в V1 — на карточку) | ДА, отличие раскрыто явно (см. `map.md`) | `verify_map_ui.mjs`+`verify_map_ui2.mjs`, 41/41, реальные клик/драг/колесо |
| График «Динамика поставки и монтажа» | SVG, план/факт/прогноз по неделям, легенда, вехи, период | Тот же SVG-код перенесён как есть, период тем же механизмом | ДА, числа сверены с самим V1 на одном сервере | `chk_dynamics.mjs`, `chk_v1_compat.mjs` |
| «Аналитическая справка» §2.3 | 4 накопительные кривые | То же | ДА | `chk_analytics.mjs`, `chk_v1_compat.mjs` |
| «Учитывать текущий фильтр схемы» | Читает состояние схемы в той же вкладке | Снимок отбора через мост `embed-bridge.js` (кадр схемы — отдельный экран V2, живого канала быть не может архитектурно) | Функционально ДА, механизм честно другой (снимок, не жизненный канал) — задокументировано как открытая архитектурная особенность, не дефект | `chk_filter.mjs`, `chk_filter_delivery.mjs` |
| График СМР — ширина колонки/полный экран | Не было в V1 (задание требовало добавить сверх переноса) | Добавлено: перетаскивание мышью+клавиатурой, полноэкранный CSS-оверлей | Новая возможность, не перенос | `chk_schedule.mjs`, `chk_layout.mjs` |
| МФР: привязка секции к осям | Два `<select>` | То же | ДА | `verify_graphics_mfr.mjs` |
| МФР: геометрия блока | Мышь (угол/ребро/тело) + числовые поля, живой клампинг о соседа | То же, тот же алгоритм клампинга перенесён | ДА, включая «сосед не меняется побочно» | `verify_graphics_mfr.mjs` |
| Подбор изделий обмена поставщика | SVG-схема: клик/рамка/панорама/масштаб + список | То же, список остался вторым способом | ДА, синхронизация списка и схемы подтверждена | `verify_graphics_picker.mjs` |
| «Зайти под пользователем» | Открывает V1 (для V2 раньше тоже открывало V1 — дефект) | Открывает именно V2, токен/права/журнал прежние | ДА | `verify_access_impersonation.mjs`, 12/12 |

## 3. Консолидированная классификация А/Б/В/Г (по пункту 6 задания)

Источники: `access.md` §2 (сплошной аудит 76 экранов + сверка `screens.json`↔`ops`/`checks`↔матрица),
плюс находки `map.md`/`charts.md`/`graphics.md` по своим областям.

| № | Находка | Категория | Действие/статус |
| --- | --- | --- | --- |
| 1 | «Учёт по блокам: статусы» — правка % в ячейке, текст утверждал «только V1» | А | Исправлен текст `screens.json`; живой прогон подтвердил — работает |
| 2 | «Резервные копии» — три операции, текст утверждал «только просмотр» | А | Исправлен текст; живой прогон (создание+удаление копии) |
| 3 | «Карта: подложка и источник» — вкл/выкл подложки, загрузка PMTiles | А | Исправлен текст по коду |
| 4 | «Журнал действий» — очистка записей раньше даты | А | Исправлен текст по коду (не запускал — необратимо) |
| 5 | Impersonation открывал V1 вместо V2 | Б → исправлено | `feature/v2-access`, опубликовано №7 |
| 6 | Общая карта — таблица координат вместо карты | Б → исправлено | `feature/v2-map`, опубликовано №7 |
| 7 | Графики отчётов отсутствовали (были таблицы/числа) | Б → исправлено | `feature/v2-charts`, опубликовано №8 |
| 8 | МФР: привязка осей и геометрия блока — только числовые поля | Б → исправлено | `feature/v2-graphics`, опубликовано №9 |
| 9 | Подбор изделий обмена — только список | Б → исправлено | `feature/v2-graphics`, опубликовано №9 |
| 10 | 20 записей `REPLACED_BY` (карта соответствий старый→новый маршрут) | В | Проверены поштучно — ни одна не урезает функциональность |
| 11 | «Статус комплектации»: у V1 есть режим «сводная таблица», в V2 только «перечень позиций» | **Б** (найдено charts, не в её задании — не чинила) | Требует отдельной небольшой доработки, вне этой сессии |
| 12 | «Динамика»: инлайн-блоки «события/задачи/вопросы» с кнопкой правки | В | Тот же смысл в карточке объекта V2 (`card-edit.js`); буквальный перенос — отдельная доработка, если нужен именно ВИД как в V1 |
| 13 | «График контрактации и поставки»: галочка фильтра схемы есть, но backend её не учитывает | Г (это дефект САМОГО V1, не пробел переноса) | В V2 галочку намеренно не показывали, чтобы не дублировать нерабочую |
| 14 | «Документы контрактации» недоступны роли «только чтение» — экран целиком требует права записи | Г (унаследовано от разметки меню V1, не создано этой сессией) | Нужно решение: считать ли это правильным поведением |
| 15 | `PATCH /users/{id}/menu-prefs` — панель «Действия» V1, аналога нет в V2 (у левой навигации V2 свой независимый порядок/избранное) | Г (повтор, известно с 08-21) | Нужен ли V2-аналог порядка именно панели «Действия» сверх уже сделанного |
| 16 | Критерий `status: 5` в `screens.json` расходится между ветками (READ-отчёты) | Г | Нужен единый критерий на весь реестр, не по одной области |
| 17 | «Планируемый поставщик» как отдельная сущность | Г → **закрыто пользователем 2026-09-22**: не нужно | Хватает «Замены поставщика»/«Распределения» |

Сплошной обход 76 экранов (64 не-admin/12 admin) — 0 необработанных исключений консоли, редиректы по
роли совпадают с ожиданием. Новых пробелов «ломается под нормальными правами» не найдено.

## 4. Публикации на 8000 (2026-09-22)

| № | Ветка | Коммит `main` | Откат |
| --- | --- | --- | --- |
| 7 | `feature/v2-map` + `feature/v2-access` | `6f54224` | `pre-v2-map-access` |
| 8 | `feature/v2-charts` | `f777986` | `pre-v2-charts` |
| 9 | `feature/v2-graphics` | `51c709b` | `pre-v2-graphics` |

Все три публикации сохранили незакоммиченную работу пользователя (подложка карты, PMTiles/цвета) без
единой потери байта (`git stash`/`merge`/`stash pop`, хэш диффа сверен на каждом шаге).

## 5. Что осталось открытым (для решения пользователем)

1. №11 — «Статус комплектации»: добавлять ли «сводную таблицу» (отдельная доработка).
2. №14 — «Документы контрактации» недоступны роли «только чтение» целиком — так и должно быть, или нужен просмотр?
3. №15 — нужен ли V2-аналог порядка панели «Действия» V1 (сверх уже сделанного порядка левой навигации)?
4. №16 — единый критерий `status: 5` для READ-отчётов в реестре экранов.
5. №13 — чинить ли в V1 нерабочую галочку фильтра у «Графика контрактации и поставки» (дефект самого V1, не переноса).

Ничего из перечисленного не блокирует то, что уже опубликовано — все пять пунктов независимы друг от
друга и от остального объёма.

## 6. Вторая волна (2026-09-23): аудит статусов, исправления, решения пользователя

Решения пользователя по пп. 1–5 раздела 5: «делай» (сводная таблица), «нужен просмотр» (документы контрактации),
«аналог — Начало/левая навигация» (порядок меню «Действия» V1 не переносится), «ничего не понял» (статусы — моя
забота, см. ниже), «чинить» (фильтр схемы «Графика контрактации»). Отдельным заданием — причина дефекта подложки карты.

| Что | Итог | Доказательство |
| --- | --- | --- |
| «Статус комплектации» — сводная таблица, колонка GUID | Б → сделано, статус 5 | `reports2.md`; `chk_pivot` 29/29, `chk_v1_parity` 16/16, `chk_completion_guid` 6/6 |
| «График контрактации» — фильтр схемы на сервере (дефект V1) | Б → сделано, V1 и V2 | без фильтра побайтово прежний 36/36; `chk_contracting_*` 20/20, 18/18 |
| «Документы контрактации» — просмотр при праве «Чтение» | Б → сделано (V2; V1 не менялся) | `docsview.md`; `verify_docsview` 67/67 |
| Подложка карты — «прямоугольники» | причина найдена и устранена (глифы → пустые тайлы; OSM под офлайн-слоями; порядок файлов; кэш) | `basemap.md`; приёмка 86/86 |
| Пометки «Начала» — 21 рабочий раздел помечен «в V1» | А (текст неверен) → исправлено | `home_chips` 18/18 |
| Плашка статуса у заголовка — оранжевая у проверенных | исправлено: зелёная у статуса 5 | `status_chip` — обход всех экранов |
| Аудит 27 экранов со статусом < 5 | 25 → статус 5; осталось 2 (см. ниже) | `audit-set.md` (254 проверки), `audit-work.md` (119) |
| Реализовано аудитами (Б) | 3D-предпросмотр зоны; тени/солнце 3D; внешние модели на «Виде»; удаление подтипа с заменой; «Состояние БД» целиком; «Элементы» — справочник с правкой вместо перехода в V1; АРМ комплектовщика — групповые операции, «Распределение по марке», вкладка «Статус»; «Модель МФР» — «Сбросить все», «Сроки», легенда; «Динамика» — ежедневный вид с правкой заметок; «Моя работа» → изделие на схеме; «Учёт по блокам» — режимы; «График работ» — группировка и Гант | те же документы |
| Сеансовые «Подписи» на схеме рабочих мест ЖБИ (найдено аудитом) | Б → сделано | `check_labels` 22/22 |
| Дефект оболочки: после смены объекта раздел оставался с данными прежнего | найден при сверке, исправлен | `object_switch_race` старый 8/12, новый 12/12 |
| Дефект: FBX без текстур грузилась лишние 20 с (V1 и V2) | найден при сверке, исправлен | `verify_fbx_texture_wait` 20 004 мс → 2 мс |
| Исправлено аудитами попутно | «[object Object]» в справке; 500 строк вместо 375; доля выполненных ×100; «Видимость подписей» оставалась «изменённой»; итоги удаления зоны/подтипа | `audit-*.md` |

Статусы реестра после волны: **73 из 76 — «рабочий и проверенный»**. Не 5: «АРМ комплектовщика» (4 — нет мини-отчётов
вкладки «Статус» по срезу), «Внешний вид» (4 — палитры гаммы в оболочке, вопрос), `shell-context` (2 — служебная запись
реестра об оболочке, в интерфейсе не показывается).

Регрессия итогового кода (публикации №13–14): оба аудита, отчёты 79/79, графики 69/69, оболочка, документы 67/67,
графические операции 23/23 и 17/17, карта 41/41, backend 7/7 наборов, модель 189/189 — без нарушений.

### Открытые вопросы пользователю (сводно, по состоянию на 2026-09-23)

Карта: (1) режим по умолчанию при включённой интернет-подложке — «из интернета» (сейчас) или «с сервера»;
(2) довендорить недостающие диапазоны глифов Noto Sans (символы «№», «–», «’» в подписях); (3) передавать ли OSM адрес
страницы (Referrer) — предложение: нет. Отчёты: (4) раскладывать ли «законтрактовано» по датам потребности при фильтре
схемы; (5) нерабочие кнопки XLSX/PDF «Графика контрактации» в V1 — убрать или сделать выгрузку; (6) «Учёт по блокам:
статусы» — по умолчанию только операции с данными (V2) или всё дерево (V1). Документы контрактации: (7) отбор списка по
праву на вид документа на сервере; (8) таблица «свободно в контракте» — читателю. Рабочие места: (9) мини-отчёты вкладки
«Статус» по текущему отбору («Модель», «Прораб», АРМ). Настройки: (10) палитры гаммы V1 в оболочке V2; (11) справочник зон —
отбор по объекту из шапки (нужен необязательный `object_id` у `GET /zones`); (12) «Состояние БД» — одно право вместо двух.
V1: (13) после переключения ЖБИ → МФР отчёты по блокам уходят с прежним чертежом (403) — чинить? (14) «Назад» из «Моей
работы» — запоминать период? Данные: (15) в базе остались таблицы старой контрактации `batches`, `batch_lines` и колонка
`contracts.code` — убрать обработкой «уборка отжившего» по кнопке администратора? (16) автоматические копии базы при
обновлении (`data/backups`, на 8000 — 321 файл, 10 ГБ) — нужна ли ротация?

## 7. Продолжение 2026-09-23

Пункты (9), (10) и (14) закрыты: мини-отчёты «Статуса» работают в трёх рабочих местах, семь палитр применяются к оболочке V2, период «Моей работы» сохраняется в пределах вкладки отдельно для каждого объекта. Дополнительно массовая правка Excel выгружает снимок отбора схемы выбранного объекта, а строка «без контракта» АРМ ведёт себя как V1 при «только с остатком». Проверки на обезличенной копии: `verify_workspace_mini_reports.mjs` 8/8, `verify_picker_remainder_v2.mjs` 4/4, `audit_work/check_finish_v2.mjs` 20/20. В реестре 75 из 76 записей имеют статус 5; единственная запись со статусом 2 — служебная `shell-context`, её нет в навигации. Это не является автоматическим основанием снимать предупреждение об экспериментальном интерфейсе: перед этим нужен отдельный сквозной приёмочный прогон операций и проверка оставшихся ограничений.
