# V2: АРМ прораба, модель ЖБИ, операции над изделиями (область `model`)

Ветка `feature/v2-model` (worktree `/Users/max/zhbi-w-model`, от `feature/v2-next` `30331d6`). Push и публикация на 8000 не выполнялись. Порты проверок — 8110 (настоящий сервер на копии БД),
8112 (стенд). Дата — 2026-09-21. Пароли и содержимое рабочих данных в документ не входят.

## 1. Что сделано

| Что | Где |
| --- | --- |
| Backend: безопасные маршруты `POST /element-ops/status-batch` (предпросмотр `preview` и запись `apply`), `POST /element-ops/planned-date-batch`, `POST /element-ops/contract`, `GET /element-ops/state?ids=` (текущее состояние пачки одним чтением — сверка после потерянного ответа) | `app/element_ops.py` (новый), подключён одной строкой в `app/main.py` рядом с `allocation_router` |
| Интерфейс: карточка изделия (статус, плановая дата, контракт, комментарий, правка/удаление записей истории, форма реквизитов) и панель группового выделения (групповая смена статуса с предпросмотром последствий, групповая плановая дата) | `app/static/v2/element-ops.js` (новый), `element-ops-rules.js` (правила/тексты, проверки тела для шлюза), `element-ops.css`; в `workspace.js` — минимальные вставки (создание модуля, вызов вместо прежней формы статуса, событие `contracts`, баннер, права) |
| Кадр сцены: Ctrl/⌘ + щелчок добавляет/убирает изделие (2D), команды `getContracts`, `applyElements` (для ЖБИ, кроме комплектовщика), `patchComment`, состав выделения несёт плановую дату | `app/static/embed-bridge.js` |
| Шлюз записи: 7 отдельных строк `allowed: true` в блоке области `model` (все с проверкой формы тела); прежняя строка `element.status` удалена (заменена `element.status.batch`) | `app/static/v2/write-gate.js` |
| Экраны: `ws-model`, `ws-foreman`, `element-ops` (теперь рабочее место, открывает «Модель») → статус 5; `fill-scope` → чтение (таблица «что не заполнено»), статус 3 | `app/static/v2/screens.json` |
| Проверки | `scripts/verify_element_ops.py` (HTTP, копия БД), `scripts/verify_model_ui.mjs` (браузер, настоящий backend), стенд: `scripts/v2_tests/fake-backend.js` (маршруты `/element-ops/*`, комментарий, позиции контрактов) и `scenarios/ws.js` (WS-08 переписан, WS-09 поправлен, новые WS-13…WS-15) |
| Документы | `Docs/TZ.md` (подраздел «Операции над изделиями на схеме в V2»), `Docs/v2-workspaces.md` (§9б — пометка об обновлении) |

### Семантика групповой смены статуса (безопасный режим)

* Клиент не присылает контракты «для записи»: у каждого изделия только ожидаемое состояние (`expected_status`, `expected_contract_id`). `PATCH /elements/bulk-status` из V2 не используется (шлюз его не пропускает).
* Контракт СОХРАНЯЕТСЯ. Меняется только по правилам V1: возврат на «Запланирован» снимает контракт и очищает фактическую дату поставки (`sync_element_contract`, `recompute_status_and_actual_date`), либо при явном
  `assign_contract_id` для изделий, у которых контракта нет (страж остатка `contract_guard`; в интерфейсе — только при выходе одного изделия из «Запланирован», окно выбора контракта).
* Режим `preview` выполняет ту же операцию и откатывает: сервер отдаёт последствия (сколько контрактов и с каких снимется, у скольких очистится фактическая дата, сколько изделий уйдёт с «Запланирован» без
  контракта, что найдёт страж); интерфейс показывает их (панель и диалог подтверждения). `apply` требует подтверждения увиденных чисел (`expect`), иначе 409 `consequences_changed`.
* После потерянного ответа интерфейс не выдаёт текущее состояние за подтверждение запроса (сервер не хранит идентификатор операции): сверяется КАЖДОЕ изделие пачки одним чтением `GET /element-ops/state`, итог — «состояние
  соответствует запросу» (без слов «сервер подтвердил») / «не подтверждено» / «неоднозначно»; автоповтора нет. (Тот же подход лидер применил к распределению — `alloc-verify.js`, `GET /allocation-state`.)
* Расхождение состояния — 409 с перечнем причин, без частичных изменений; повтор (потерянный ответ) — 200 `already_applied`; частичный повтор — 409. Всё под `begin_write`, права — на всю пачку, журнал —
  `defer_*` (только после commit). Лимит 2000 изделий. Формат даты/времени проверяется строго (мусор — 400).

## 2. Что проверено и чем

Способ: настоящий `app.main:app` с настоящим входом по паролю (`scripts/real_auth_server.py`) на КОПИИ обезличенной БД, безголовый Chrome по протоколу DevTools (`scripts/cdp.mjs`, настоящие события мыши),
SQL к копии для сверки «до/после». Боевая БД, порты 8000/8010/8020 и `/Users/max/zhbi-tool` не использовались.

| Проверка | Результат |
| --- | --- |
| `scripts/verify_element_ops.py <копия>` — HTTP на копии: предпросмотр без записи; контракт сохраняется; возврат на «Запланирован» (последствия, подтверждение, снятие контракта и фактической даты); устаревшее состояние (409, чужой контракт цел); недопустимые пачки (дубли, пустая, нет изделия, чужой объект, тот же статус, формат даты, лимит 2000); права (403 целиком, смешанные объекты, 401); повтор и двойной клик; назначение контракта (страж остатка, откат всей пачки, журнал `discarded`); конкуренция (одинаковые вызовы, разные, за остаток); плановая дата; контракт изделия; освобождение блокировки, 503 при занятой БД; совместимость прежних операций V1 | 0 нарушений (см. вывод `нарушений нет`) |
| `scripts/verify_model_ui.mjs 8110 <каталог>` — браузер, полный прогон разделов `gest single group plan cont hist form 3d foreman rights v1` на свежей копии | итог — раздел 3 ниже |
| Стенд `suite=ws` (фейковый backend, `run_stand.sh`) | 15 PASS / 0 FAIL (WS-01…WS-15) на 1920×1080 и на 1366×768 (при одновременной нагрузке от других прогонов встречался разовый «Не дождались: загрузка стенда» — на повторе проходит) |
| Стенд `suite=ws,shell,gt,ua,a11y,lc,ap,rd` | в полном прогоне 118 PASS, 16 FAIL — все 16 «Не дождались: загрузка стенда (20000 мс)» (нагрузка на машину при параллельной работе), повторный прогон именно этих 16 — 17 PASS / 0 FAIL / 1 отключён шлюзом |

Проверка каждой изменяющей операции (по перечню BRIEF): успех и результат после перезагрузки; 403 при недостаточных правах (user2, урезанная роль «только статусы» и «только чтение» на копии — роли и гранты созданы SQL
в копии; серверные коды проверены прямыми запросами из сеанса пользователя); серверная валидация (даты, неверные поля, `floor=8.5`); конфликт устаревших данных; двойная отправка (один запрос); обрыв связи
(`Network.emulateNetworkConditions offline`) и потеря ответа ПОСЛЕ записи (запрос выполняется, `fetch` в браузере отклоняется) — без автоповтора, сверка с сервером; отсутствие побочных изменений (сравнение строк
БД и числа записей истории до/после); журнал (`activity_log`: событие только для сохранённого, автор, без дублей); совместимость V1 (тот же сервер: `state.byId` V1 показывает статус и комментарий,
собственная операция V1 работает); для пачек — конкуренция (два пользователя одновременно) и полный откат при отказе внутри пачки.

### Какие права нужны операциям (раздел на объекте изделия, порог «запись» — `role_features`)

| Операция | Раздел (ключ) | user2 (роль `user`) | user4 в копии |
| --- | --- | --- | --- |
| смена статуса (одного и пачки), контракт изделия, распределение | `status` | запись | у роли `view` в копии стоит запись `status`/`plan` (след прежних проверок лидера) — для проверок отказа на ВРЕМЕННОЙ копии заменена чтением |
| плановая дата (одного и пачки) | `planned_date` | запись | чтение |
| комментарий | `comment` | запись | чтение |
| правка и удаление записей истории | `history` | запись | чтение |
| форма реквизитов изделия | `element_fields` | ТОЛЬКО чтение (запись — администратор объекта) | чтение |
| чтение карточки, состояния пачки (`/element-ops/state`) | `plan` | чтение | чтение |

Неполные роли для проверок созданы SQL в КОПИИ (`object_roles`/`role_features`/`user_access`, скрипт — раздел «rights» в `verify_model_ui.mjs`): `t_status_only` (только `status` — запись) и `t_readonly` (всё на чтение).

## 3. Результаты полного прогона браузерной проверки

Итоговая сборка — коммит `5abc323` плюс правка списка «у каких изделий снимется контракт» (см. ниже). Свежая копия обезличенной БД, порт 8110, настоящий вход:

| Раздел | Что проверялось (настоящие жесты мыши, SQL до/после, журнал) | Итог |
| --- | --- | --- |
| G жесты | 1920×1080 и 1366×768: щелчок, ⌘+щелчок (настоящий) добавляет/убирает, Ctrl+щелчок (событие `click` с `ctrlKey`), рамка Shift+перетаскивание (и вторая рамка добавляет), колесо, сдвиг без изменения выбора, «+»/«Вписать», щелчок по пустому месту, страница без прокрутки | 36 ✓ |
| S смена статуса одного | успех, дата, комментарий, контракт сохранён, история/журнал, после перезагрузки; возврат на «Запланирован» (последствия до записи, отмена, подтверждение); выход из «Запланирован» с выбором контракта; конфликт; двойная отправка; обрыв связи; потеря ответа после записи | 34 ✓ |
| Q пачка | предпросмотр ничего не пишет; запись; показатели; возврат пачки на «Запланирован» (последствия, отмена, подтверждение, 4 контракта снято, фактическая дата очищена); конфликт — ПОЛНЫЙ ОТКАТ; двойной щелчок; потеря ответа; обрыв связи; конкуренция (два пользователя, одинаковые и разные изменения) | 31 ✓ |
| P плановая дата | одного (успех, после перезагрузки, конфликт, потеря ответа, двойной щелчок, снятие) и пачки (диалог, БД, журнал, конфликт — откат, «только без даты») | 22 ✓ |
| K/C контракт и комментарий | снять/назначить, после перезагрузки, идемпотентность, конфликт, потеря ответа; комментарий (успех, после перезагрузки, потеря ответа, двойной щелчок) | 16 ✓ |
| H история | правка (статус пересчитан), удаление (диалог, отмена, пересчёт), журнал, серверный запрет удаления последней записи | 11 ✓ |
| E форма изделия | сохранение (только изменённые поля, `manual_fields`, журнал), схема перечитана, выбор возвращён, серверная валидация, потеря ответа | 8 ✓ |
| D 3D | WebGL (swiftshader): масштаб колесом, вращение перетаскиванием, выбор щелчком, сброс | 6 ✓ |
| F АРМ прораба | панель отбора, полоса показателей, групповая запись, обновление полосы, отбор слева, страница без прокрутки на двух размерах | 12 ✓ |
| R права | user2 (запись статуса/даты/комментария/истории, без реквизитов, 403 на `/fields`), роль «только статусы» (403 на дату/комментарий/реквизиты/историю), «только чтение» (нет форм, 403 на статус/контракт), шлюз | 13 ✓ |
| V1 | V1 на том же сервере показывает результат V2, собственная операция V1 работает | 3 ✓ |
| **Всего** | | **188 ✓, нарушений 0** (полный прогон); после добавления списка «у каких изделий снимется контракт» раздел Q прогнан повторно на свежей копии: 32 ✓, нарушений 0 |

`scripts/verify_element_ops.py`: 117 проверок, нарушений нет (2000 изделий: предпросмотр 0,5 с, запись 2,4 с; конфликт на последнем изделии — все 2000 нетронуты).

## 4. Найденные дефекты и решения

* Исходная семантика `PATCH /elements/bulk-status` (явная перезапись контракта из тела) не годится для V2 — заменена специализированным маршрутом (раздел 1).
* `PATCH /elements/bulk-planned-delivery-date` не проверяет формат даты, не сверяет прежнее значение и не берёт блокировку — в V2 не используется, вместо него `planned-date-batch`.
* Ctrl + щелчок на macOS — системный жест контекстного меню: настоящим щелчком его создать нельзя (в Windows, где работают пользователи, это обычный щелчок). Поэтому ⌘ + щелчок проверен настоящим жестом,
  Ctrl + щелчок — событием `click` с `ctrlKey` (в проверке помечено). Ошибка «Ctrl + щелчок открывает меню статуса вместо добавления» была замечена именно так и относится только к macOS.
* После сохранения реквизитов (тип/марка/адрес) схема перечитывается целиком и выбор V1 сбрасывается — интерфейс возвращает выбор на то же изделие.
* Итог групповой операции при расхождении показывался в форме, исчезавшей вместе со сбросом выделения после перечитывания схемы — вынесен в баннер вверху панели.
* Фантомная проверка «показатели обновились» в первой версии проверки заменена настоящей (вкладка «Статус» и полоса показателей прораба).

## 5. Ограничения и вопросы к пользователю

1. **Не перенесено**: вложения изделия (карточка/форма V1), построчные (разные) плановые даты в таблице «массовая плановая дата» V1 (в V2 одна дата на выбранные изделия, флажок «только у изделий без даты»),
   назначение контрактов ПО СТРОКАМ при групповой смене статуса (в V2 контракты сохраняются; назначить их можно распределением на АРМ комплектовщика или в карточке изделия), групповое выделение в 3D
   (в V1 его тоже нет). Вопрос: нужны ли построчные даты и контракты при групповой смене статуса — это потребует отдельного интерфейса таблицы; сервер `assign_contract_id` уже умеет один контракт на все изделия без контракта.
2. **`fill-scope`** («Заполнить пустые Объект и Проект», временная): в V2 подключено только чтение (что не заполнено). Само заполнение — необратимая массовая административная обработка, которая
   по документации самого модуля подлежит удалению, сервер не сверяет ожидаемое состояние. Не переносил без решения пользователя.
3. **`chess-flat`** (плоская шахматка) — это шахматка работ по блокам МФР (Docs/TZ.md §3в.1), не ЖБИ: не моя область, не трогал.
4. Групповые операции ограничены 2000 изделий за раз (3000 — предел состава выделения в кадре); при большем выборе интерфейс просит уменьшить выбор.
5. Групповые формы отключены на рабочем месте комплектовщика (там выбор нужен распределению; его вкладки и `allocSubmit` не трогал).
6. В шлюзе и на сервере остались прежние V1-маршруты для комментария, реквизитов и истории (без сверки ожидаемого состояния): это последовательность V1, права проверяет сервер. Если нужна такая же защита от устаревших
   данных для правки записи истории — отдельная задача.

## 6. Точка продолжения

* Пользователь принимает результат на 8000 после интеграции лидером. При интеграции проверить конфликты: `workspace.js` (мои вставки: импорт, `ops`, `propsHtml`→`propsInner`, `bindPanel` → `ops.bind`, `paintPanel`
  фокус, `loadStatusRights` → `ops.setRights`, `capabilities`, `hasUnsavedChanges/guardLeave`, `onObjectChange`), `write-gate.js` (блок `область: model`, строка импорта в начале файла, удалена строка `element.status`),
  `embed-bridge.js` (блок `COMMANDS_EL`, Ctrl/⌘-щелчок), `screens.json` (четыре строки), `main.js` не менялся.
* Если решено переносить `fill-scope`: скрипт проверки по образцу `verify_element_ops.py` (S-сценарии), затем интерфейс по `scan`/`apply` с подтверждением чисел (по образцу `expect`).
