# Аудит V2 ↔ V1: настройки, справочники, служебные экраны (audit-set)

Ветка `feature/v2-audit-set`, worktree `/Users/max/zhbi-w-audit-set`. Даты работы: 2026-09-22…23.
Стенд — `scripts/real_auth_server.py` (НАСТОЯЩИЙ `app.main:app`, временная копия обезличенной БД в каталоге scratchpad, порты 8360–8362),
вход настоящей формой V2 (`admin`, `user2` — роль user на объекте 1, `user3` — второй администратор, `user4` — просмотр). Браузер — только
`scripts/cdp.mjs` (headless Chrome, настоящие события мыши и клавиатуры; программный WebGL swiftshader для 3D). В `/Users/max/zhbi-tool`, боевую БД,
порты 8000/8010/8020 и корпоративный сервер ничего не писалось. Проверки — `scripts/audit_set/*.mjs`, общий прогон
`sh scripts/audit_set/run_all.sh <копия БД> <порт>`; итоговый прогон на свежей копии — раздел «Итоговый прогон».

Категории: **А** — уже работало, текст реестра был неверен (доказано прогоном, текст исправлен); **Б** — не было, реализовано в V2;
**В** — заменено равноценным механизмом; **Г** — нужно решение пользователя.

## Таблица соответствия

| Экран | V1 (окно, действия) | V2 (как делается) | Результат | Кат. | Действие | Доказательство |
|---|---|---|---|---|---|---|
| `report-notes` | `report-notes-backdrop`: список редакций (дата, «пунктов: N · автор»), при открытии — свежая; «+» — новая на сегодня; сохранить (PUT, смена даты = копия с молчаливой перезаписью); удалить (confirm); открыть из «Динамики» с датой отчёта | тот же экран; дата существующей редакции закрыта (перенос — новая + удалить старую), занятая дата не перезаписывается молча, проверка чужой правки | тот же набор данных; без молчаливой перезаписи | А + Б + В | Б: свежая редакция при открытии, новая — на сегодня, число пунктов, приёмная сторона даты из отчёта (`v2.reportNotes.date`); В: смена даты → новая + удалить | `check_report_notes.mjs` 22/22 (SQL, журнал, сторож, 403 user2/user4) |
| `fill-scope` | `fill-scope-backdrop` (только admin): сводка, отметки, проект/объект, confirm, отчёт | то же; подтверждение вводом слова «ЗАПОЛНИТЬ» | тот же | А (+В: слово вместо confirm) | текст реестра | `check_fill_scope.mjs` 19/19 (валидация 400, 403, обрыв связи без автоповтора, двойной щелчок, SQL, журнал, устаревший предпросмотр) |
| `zones` | `zones-backdrop` + `zone-edit-backdrop`: 3 категории, «нет в чертеже», правка (номер, имя, кран, ярусы, точки), предпросмотр **2D/3D**, откат, удаление по плану | то же, 3D-предпросмотра не было | теперь тот же | Б | 3D-предпросмотр (`zone-preview-3d.js`), прилипание предпросмотра; дефект: итог удаления стирался перерисовкой | `check_zones3d.mjs` 34/34, `check_zone_delete.mjs` 11/11, `check_v1_smoke.mjs` (3D V1 — эталон) |
| `subtypes` | `subtypes-backdrop`: подтипы (добавить, ✕ — `openDictDelete` с заменой/переносом), марки (добавить, переименовать, удалить) | подтипы: используемый — **отказ «в текущем интерфейсе»** | теперь тот же | Б | удаление используемого подтипа с заменой (общий `delete-plan.js`); строка шлюза сужена до `replacements/mode` | `check_subtype_replace.mjs` 16/16 (SQL ровно 3 изделия, журнал, 403, двойной щелчок) |
| `mark-prefixes` | `mark-type-prefixes-backdrop`: добавить (перезапись типа молча), ✕ | то же; замена типа — после подтверждения | тот же | А (+В) | текст реестра | `check_roles_misc.mjs` (цикл, SQL, журнал, 403, нет экрана у user2/user4) |
| `address-classifier` | `address-backdrop`: скачать с ФНС, загрузить файл (сразу при выборе), распаковать, регионы, **два флага «дома»** (распаковка / загрузка) | один флаг на всё | теперь тот же | Б (+В: загрузка файла кнопкой) | два независимых флага | `check_address.mjs` 9/9 (синтетический КЛАДР, тело `houses=false`, 403) |
| `db-status` | `db-status-backdrop` + `db-table-backdrop`: сводка, **красная плашка расхождения**, области/объём/гистограмма, описания полей и связи, строки с подписями колонок, 50/100/200/500 | плоский список таблиц + строки по 100 | теперь тот же (+поиск) | Б | окно перенесено целиком | `check_db_status.mjs` 19/19 (403 и нет экрана у user2/user4) |
| `db-transfer` | 4-й раздел `bulk-edit-backdrop`: выгрузка, приём и сверка, применение (кодовое слово + confirm), forget | отдельный экран; второе слово «ЗАМЕНИТЬ» | тот же | А (+В) | текст реестра | `admin2_verify/check_dbtransfer_ui.mjs` 11/11 повторно, `check_roles_misc.mjs` (403, нет экрана) |
| `admin-guide` | `admin-guide-backdrop`: памятка, копировать, скачать .md (сообщение об итоге), скопировать всё | ошибка скачивания молча терялась | теперь тот же | Б (мелкое) | сообщение об итоге | `check_admin_guide.mjs` 12/12 (обрыв связи, 403 user2/user4, user3 — есть) |
| `appearance` | `menu-view-backdrop`: гамма (7), ракурс 3D, **благоустройство/фасады**, **тени**, **солнце**, порог подписей, вид меню «Действия» | гамма (оболочка — светлая/тёмная), ракурс, порог | см. справа | Б + В + Г | Б: тени и солнце (ключи localStorage V1), флажки внешних моделей — на вкладке «Вид» рабочего места (`setExternalVisible`); В: меню «Действия» → левая навигация (решение пользователя); **Г**: полная палитра гаммы в оболочке | `check_appearance.mjs` 29/29, `check_scheme_settings.mjs` (ракурс в 3D = 40°) |
| `label-color` | `label-color-backdrop`: цвет, сброс; схема перерисовывается сразу | тот же PATCH; реестр писал «в V2 схем нет» | цвет на схеме рабочих мест V2 (сцена V1 в кадре) при следующем входе в рабочее место | А | текст реестра | `check_scheme_settings.mjs` (CSS-переменная кадра, 403 user4) |
| `status-colors` | `settings-backdrop` | тот же PUT | цвет изделий на схеме V2 и в панели «Статус» | А | текст реестра | `check_scheme_settings.mjs` (заливка фигуры `#123456`, панель, 403 user2/user4) |
| `marker-shapes` | `shapes-backdrop` | тот же PUT; реестр писал «перерисовка — в V1» | фигура на схеме V2 (`data-shape=square`) | А | текст реестра | `check_scheme_settings.mjs` |
| `zone-colors` | `zone-colors-backdrop` | тот же PUT | цвет крана в сцене V2 | А | текст реестра | `check_scheme_settings.mjs` |
| `label-visibility` | формы нет: поля задаёт «Импорт настроек»; сеансовый переключатель «Подписи» в панели схемы | экран V2 по объекту | стартовое значение на схеме V2 | А (+В) | дефект: после сохранения экран оставался «изменённым» | `check_scheme_settings.mjs` (`state.labelVisibility` кадра) |
| `late-threshold` | `info-plate-settings-backdrop` | тот же PUT | порог объекта в сцене V2 | А | текст реестра («права не проверялись» — проверены) | `check_scheme_settings.mjs` (403 user2 и user4) |
| `mfr-colors` | `revit-colors-backdrop`: шаблон, цвет, прозрачность, свечение | тот же PUT; реестр писал «в V2 схем нет» | цвет категории на схеме «Модель МФР» V2 | А | текст реестра | `check_scheme_settings.mjs` (объект 4, `revitColors` кадра) |

Про «сразу или после перезагрузки»: в V1 окно настройки лежит поверх схемы и перерисовывает её немедленно; в V2 схема — на другом экране,
кадр сцены создаётся заново при каждом входе в рабочее место и читает настройки с сервера (и `localStorage` для теней/солнца) — изменение
видно при следующем открытии рабочего места, перезагружать страницу не нужно. Проверено для каждой настройки чтением состояния движка
в кадре (DevTools `Runtime.callFunctionOn` в основном мире кадра — CSP страницы запрещает `eval` из кода) и отрисованных элементов.

Статусы в реестре: **5** — 16 экранов; **4** — `appearance` (вопрос о палитре гаммы).

## Реализовано (коммиты)

* `d61d16f` — зоны: 3D-предпросмотр (`app/static/v2/zone-preview-3d.js`, `zones-edit.js`).
* `07307de` — «Видимость подписей и дат»: дефект «изменён после сохранения»; проверка применения настроек на схеме V2.
* `bcabe1c` — «Внешний вид»: тени и солнце; флажки внешних 3D-моделей на вкладке «Вид» (`embed-bridge.js`, `workspace.js`).
* `0d51222` — подтипы: удаление используемого с заменой; результат «exists» у марок и зон больше не «Удалено»; сценарий стенда SB-04.
* `9385a97` — «Состояние БД»: окно V1 целиком.
* `288e777` — адресный классификатор: два флага «дома».
* `f6d8ac8` — «События, задачи, вопросы»: поведение V1 при открытии, число пунктов, дата новой редакции, приём даты из отчёта.
* `1832437` — памятка: сообщение об итоге скачивания.
* `af3570f` — зоны: итог удаления не стирается; проверка удаления зоны.
* `c0f9050` — реестр (17 записей).
* Проверки без правок кода: `9a8e4d8` (fill-scope), `30deafb` (роли: перенос базы, префиксы), `dfd690c` (совместимость V1), `7a6fc01`
  (ожидание данных сцены, `run_all.sh`), последний коммит — этот отчёт и устойчивость проверки классификатора к повторному прогону.

Общие файлы: `app/static/embed-bridge.js` (команда `setExternalVisible{kind,on}`, поле снимка `external`), `app/static/v2/workspace.js`
(вкладка «Вид»: два флажка и их привязка), `app/static/v2/write-gate.js` (строка `subtype.delete` — текст и `onlyKeys`),
`app/static/v2/delete-plan.js` (`KIND_TEXT.subtype`), `app/static/v2/screens.json` (только 17 своих записей), `Docs/TZ.md`,
`scripts/v2_tests/scenarios/sb.js` (SB-04). Backend, схема БД, `app/changelog.py` — не менялись.

## Итоговый прогон (свежая копия БД, порт 8361, итоговая сборка ветки)

Сервер перезапущен на СВЕЖЕЙ копии обезличенной БД (порт 8362), `sh scripts/audit_set/run_all.sh` на итоговой сборке ветки:

| Проверка | Итог |
|---|---|
| `check_v1_smoke.mjs` — 15 окон V1 и 3D-предпросмотр зоны V1 | 18/18 |
| `check_zones3d.mjs` — 3D-предпросмотр зоны, 1920×1080 и 1366×768, user4 | 34/34 |
| `check_scheme_settings.mjs` — 9 настроек на схеме V2 (ЖБИ и МФР), 403 | 35/35 |
| `check_appearance.mjs` — тени/солнце, V1↔V2, гамма, внешние модели, user4 | 29/29 |
| `check_subtype_replace.mjs` — удаление используемого подтипа с заменой | 16/16 |
| `check_db_status.mjs` — окно «Состояние БД», роли | 19/19 |
| `check_address.mjs` — два флага «дома», роли | 9/9 (первый прогон упал на самой проверке: регион, загруженный прежним прогоном, приходит отмеченным, и щелчок снимал отметку; проверка исправлена, повтор — 9/9) |
| `check_report_notes.mjs` — заметки, роли | 22/22 |
| `check_admin_guide.mjs` — памятка, роли | 12/12 |
| `check_fill_scope.mjs` — временная обработка, роли, обрыв связи, устаревший предпросмотр | 19/19 |
| `check_roles_misc.mjs` — перенос базы (роли), префиксы (цикл и роли) | 19/19 |
| `check_zone_delete.mjs` — удаление крана с переносом стоянок, роли | 11/11 |
| `admin2_verify/check_dbtransfer_ui.mjs` — перенос базы целиком (на порту 8360, та же сборка кода экрана) | 11/11 |

Всего 254 проверки. Ветка не содержит исправления оболочки из `feature/v2-next` 53d7ae9 (перерисовка раздела после смены объекта при
фоновой записи `/me/last-object`); сценарии со сменой объекта в шапке (`setObject` в проверках зон, настроек схемы, заметок, МФР) на этой
ветке проходили устойчиво. Сбоев из-за чужого завершения Chrome не было.

## Вопросы пользователю (Г)

1. **Палитра гаммы в оболочке V2 — закрыто 2026-09-23.** Все семь гамм V1 перенесены в цветовые токены оболочки V2;
   «Базовая» — синяя, как в V1. Геометрия и типографика V2 сохранены. Проверка на копии базы — `scripts/audit_work/check_finish_v2.mjs`.
2. **Справочник зон показывает зоны всех доступных объектов** (у администратора — всех объектов, одноимённые «Кран 1» разных зданий
   неотличимы) — и в V1, и в V2 (`GET /zones` не отбирает по объекту). Отбирать по объекту из шапки V2? Это совместимая правка: необязательный
   параметр `object_id` у `GET /zones` (V1 его не шлёт) и одна строка в `zones-edit.js`.
3. **Права «Состояния БД»**: пункт меню (и экран V2) открывается по разделу «Резервные копии» (чтение), сервер проверяет «Состояние БД»
   (чтение) — расхождение унаследовано от V1. Привести к одному разделу?

## Находки для соседних областей (не мои экраны, не правил)

* **Рабочее место «Модель»**: нет сеансового переключателя «Подписи» боковой панели V1 (подпись марки и допстрока дат по типу, только в
  открытой вкладке). В V2 скрыть подписи можно лишь настройкой объекта «Видимость подписей и дат» — она для всех пользователей объекта.
* **Отчёт «Динамика»** (`report-dynamics`): нет кнопки правки заметок с датой отчёта (V1 `.dyn-edit`). Приёмная сторона готова: записать
  дату в `sessionStorage["v2.reportNotes.date"]` (ГГГГ-ММ-ДД) и перейти на `#/report-notes`.
* **Стенд `scripts/v2_tests` устарел** относительно левой навигации: наборы падают на `.v2-nav [data-section]` и ожидании загрузки —
  в том числе нетронутый `lc` (значит, до моих правок). SB-04 переписан под новое поведение, прогнать стенд нельзя.
* **Описание структуры БД отстало**: плашка «Состояния БД» на копии показывает `batches`, `batch_lines`, `contracts.code` без описания в
  `app/db_schema_doc.py`.

## Точка продолжения

Все 17 экранов разобраны; незакрытое — только вопросы Г выше (после решения: палитры — `appearance-edit.js` + токены гамм в CSS V2;
отбор зон — `app/main.py:list_zones` + `zones-edit.js`).
