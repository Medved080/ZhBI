Реализуй загрузку и размещение FBX благоустройства в проекте /Users/max/zhbi-tool по полному заданию Docs/fbx-ground-implementation-task.md. Сначала прочитай этот документ целиком, затем CLAUDE.md и указанные в задании участки действующего кода. Это задача реализации, а не ещё одного проектирования.

Первый обязательный файл для проверки: Input/0507_Aviomotornaya_ZU_5/0507_Aviomotornaya_ZU_5_Ground.fbx. Второй FBX пока не импортируй. Реальные файлы и их текстуры в git не добавляй.

Выполняй этапы A–F по порядку и фиксируй результаты каждого. До интеграции проверь единицы, оси и вложенные трансформации на реальном файле. Сохрани разницу между абсолютными координатами проекта и координатами сцены МФР: смена этажа не должна перемещать благоустройство. Начальное положение — центр полного проекта, нижняя точка FBX на отметке 0; ручной сдвиг только по X/Y, с сохранением на сервере.

Пиши небольшие модули с явными зависимостями; не размножай загрузчик и математику в двух просмотрщиках. Обязательны права на content, ограничение размера, проверка finite, optimistic concurrency, cancel/cleanup, сохранение текстур и проверка восстановления данных. Не рефактори несвязанные участки приложения.

Не считай ненулевые FBX-координаты подтверждением привязки. Не сплющивай рельеф. Не применяй масштаб/поворот дважды. Не вычисляй центр из текущего фильтра, камеры или самой внешней модели. Не лечи неверное положение подгонкой коэффициентов на глаз.

В плане отмечена необходимость согласования нового vendor-кода по CLAUDE.md. Если подтверждение ещё не дано в этой сессии, подготовь конкретный минимальный список r160-файлов с лицензиями и запроси его до вендоринга. Остальную независимую работу продолжай. Не спрашивай повторно, если пользователь уже подтвердил именно этот набор.

Не трогай чужие изменения и боевую БД. Работай с отдельной копией обезличенной базы и при параллельной работе — в отдельном worktree. Не утверждай, что функция готова, пока Ground.fbx не показан в живом браузере, текстуры не проверены, положение не сохраняется после reload и не выдержаны фильтр этажа и ручное перемещение. Если проверка реально недоступна, явно укажи конкретный пробел и не выдумывай результат.
