# Матрица пользовательских операций V1 → V2

> Файл **генерируется** скриптом `scripts/gen_v2_operations.py`; руками не править. Машинный вид — `Docs/v2-operations-matrix.json`.
> Пересоздать: `.venv/bin/python scripts/gen_v2_operations.py --ref HEAD --published-ref main` (после каждого слияния).

- Анализируется: **HEAD@5c9b543** (`--ref HEAD`). «Опубликовано на 8000» — по **main@59bafcd** (`--published-ref main`): операция опубликована, если в этой ветке шлюз её разрешает, интерфейс V2 её вызывает и маршрут есть в backend. Дата: 2026-09-22.
- **Внимание:** в рабочем дереве есть незакоммиченные изменения (1 файл.: scripts/gen_v2_operations.py); матрица построена по `HEAD` и их не учитывает (`--ref WORKTREE` — учесть).
- Операций (строк): **384**, из них пользовательских 372, служебных 12 (вход, оболочка, версия сборки — в сводку не входят).
- **Ссылка в V1, заглушка и отключённая кнопка не считаются реализованной функцией V2.** «Только переход в V1» — в V2 есть экран, который лишь ведёт в V1; «в V2, но отключена шлюзом» — форма есть, но `write-gate.js` не разрешает запрос, пользователь её выполнить не может.

**Перечень проверок изменяющей операции** (условие статуса «5» и включения строки в шлюзе; на настоящем backend, в браузере, на временной копии БД): успех и результат ПОСЛЕ перезагрузки страницы; отказ 403 у ограниченной роли (`user2`, `user4`); серверная валидация; конфликт устаревших данных; двойная отправка — один запрос; сетевой сбой и неизвестный исход без автоповтора, сверка с сервером; отсутствие частичных и побочных изменений (SQL до/после); правдивый журнал (`activity_log`); совместимость V1; для групповых опасных — ещё конкуренция и полный откат пачки.

## Сводка

Число пользовательских операций по областям переноса и состоянию в V2:

| Область | реализована в V2 и опубликована | в V2 (в ветке), не опубликована | в V2, но отключена шлюзом | только переход в V1 | нет в V2 | не вызывается интерфейсами (нет в V1 и V2) | Всего |
| --- | --- | --- | --- | --- | --- | --- | --- |
| model | 6 | 9 | 0 | 5 | 0 | 4 | 24 |
| mfr | 17 | 18 | 2 | 19 | 0 | 0 | 56 |
| picker | 14 | 19 | 0 | 20 | 0 | 1 | 54 |
| admin | 63 | 61 | 2 | 29 | 0 | 8 | 163 |
| exchange | 25 | 18 | 0 | 29 | 0 | 2 | 74 |
| прочее | 0 | 1 | 0 | 0 | 0 | 0 | 1 |
| **Итого** | **125** | **126** | **4** | **102** | **0** | **15** | **372** |

Из них **изменяющие** операции (запись и разбор файла — то, что проходит через шлюз):

| Область | реализована в V2 и опубликована | в V2 (в ветке), не опубликована | в V2, но отключена шлюзом | только переход в V1 | нет в V2 | не вызывается интерфейсами (нет в V1 и V2) | Всего |
| --- | --- | --- | --- | --- | --- | --- | --- |
| model | 0 | 8 | 0 | 4 | 0 | 0 | 12 |
| mfr | 2 | 7 | 2 | 12 | 0 | 0 | 23 |
| picker | 1 | 9 | 0 | 13 | 0 | 0 | 23 |
| admin | 17 | 40 | 2 | 16 | 0 | 5 | 80 |
| exchange | 0 | 11 | 0 | 19 | 0 | 1 | 31 |
| **Итого** | **20** | **75** | **4** | **64** | **0** | **6** | **169** |

По разделам V1:

| Раздел | реализована в V2 и опубликована | в V2 (в ветке), не опубликована | в V2, но отключена шлюзом | только переход в V1 | нет в V2 | не вызывается интерфейсами (нет в V1 и V2) | Всего |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Рабочие места | 13 | 9 | 0 | 5 | 0 | 4 | 31 |
| Пользователи и роли | 7 | 21 | 0 | 2 | 0 | 1 | 31 |
| Проекты и объекты | 7 | 11 | 0 | 2 | 0 | 1 | 21 |
| Контрагенты | 2 | 2 | 0 | 3 | 0 | 0 | 7 |
| Договоры | 2 | 2 | 0 | 2 | 0 | 0 | 6 |
| Спецификации | 2 | 2 | 0 | 2 | 0 | 0 | 6 |
| Контракты | 4 | 2 | 0 | 4 | 0 | 0 | 10 |
| Документы | 3 | 13 | 0 | 0 | 0 | 0 | 16 |
| Справочники | 23 | 12 | 2 | 11 | 0 | 2 | 50 |
| Отчёты | 21 | 4 | 0 | 4 | 0 | 0 | 29 |
| Импорт и экспорт | 4 | 14 | 0 | 25 | 0 | 2 | 45 |
| Настройки | 16 | 0 | 0 | 3 | 0 | 3 | 22 |
| Служебные функции | 8 | 16 | 0 | 11 | 0 | 1 | 36 |
| Учёт по блокам (МФР) | 12 | 18 | 2 | 19 | 0 | 0 | 51 |
| График СМР | 1 | 0 | 0 | 9 | 0 | 1 | 11 |

## Экраны V2: статус и состав операций

Экран операции — где она реализована в V2, а если не реализована, то экран, который лишь ведёт в V1 (по правилу пути и точкам входа V1). Статус — из `screens.json` (1 не начат … 5 рабочий и проверенный).

| Экран | Название | Статус | Реализация | реализована в V2 и опубликована | в V2 (в ветке), не опубликована | в V2, но отключена шлюзом | только переход в V1 | нет в V2 | не вызывается интерфейсами (нет в V1 и V2) |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `ws-model` | Модель: схема 2D/3D | 5 | workspace | 8 | 0 | 0 | 1 | 0 | 4 |
| `element-ops` | Операции над элементами | 5 | workspace | 0 | 8 | 0 | 4 | 0 | 0 |
| `ws-mfr` | Модель МФР | 4 | workspace | 1 | 0 | 0 | 0 | 0 | 0 |
| `ws-picker` | АРМ комплектовщика | 3 | workspace | 2 | 0 | 0 | 0 | 0 | 0 |
| `ws-foreman` | АРМ прораба | 5 | workspace | 0 | 0 | 0 | 0 | 0 | 0 |
| `chess-flat` | Плоская шахматка | 5 | chess-flat-edit | 0 | 2 | 0 | 3 | 0 | 0 |
| `map` | Карта проектов | 3 | read | 1 | 0 | 0 | 1 | 0 | 0 |
| `blocks` | Учёт по блокам | 5 | blocks-edit | 10 | 10 | 2 | 17 | 0 | 0 |
| `fact-journal` | Журнал факта | 5 | fact-journal-edit | 1 | 6 | 0 | 1 | 0 | 0 |
| `counterparties` | Контрагенты | 5 | module:counterparties | 7 | 8 | 0 | 9 | 0 | 0 |
| `contracts` | Контракты | 5 | contracts-list | 4 | 1 | 0 | 2 | 0 | 0 |
| `supplier-change` | Документы контрактации | 5 | supplier-docs | 1 | 11 | 0 | 0 | 0 | 0 |
| `schedule` | График СМР | 3 | read | 1 | 0 | 0 | 9 | 0 | 1 |
| `report-status` | Статус монтажа | 3 | read | 3 | 0 | 0 | 2 | 0 | 0 |
| `report-dynamics` | Динамика поставки и монтажа | 3 | read | 3 | 0 | 0 | 0 | 0 | 0 |
| `report-delivery` | График поставки (в разработке) | 5 | read | 0 | 3 | 0 | 1 | 0 | 0 |
| `report-completion` | Статус комплектации | 3 | read | 3 | 0 | 0 | 0 | 0 | 0 |
| `report-mywork` | Моя работа | 3 | read | 3 | 0 | 0 | 1 | 0 | 0 |
| `report-contracting` | График контрактации и поставки (в разработке) | 5 | read | 0 | 1 | 0 | 0 | 0 | 0 |
| `report-analytics` | Аналитическая справка | 3 | read | 3 | 0 | 0 | 0 | 0 | 0 |
| `report-block-status` | Учёт по блокам: статусы | 3 | read | 1 | 0 | 0 | 0 | 0 | 0 |
| `report-block-schedule` | График работ по блокам | 3 | read | 3 | 0 | 0 | 0 | 0 | 0 |
| `report-linear-track` | Линейный трек | 3 | read | 2 | 0 | 0 | 0 | 0 | 0 |
| `projects-objects` | Проекты и объекты | 5 | module:projects-objects | 10 | 11 | 0 | 1 | 0 | 1 |
| `project-card` | Карточка объекта | 5 | card-edit | 2 | 0 | 0 | 0 | 0 | 0 |
| `dict-smu` | СМУ | 5 | dict-edit | 5 | 5 | 2 | 0 | 0 | 0 |
| `dict-individuals` | Физлица | 5 | dict-edit | 2 | 4 | 0 | 0 | 0 | 0 |
| `report-notes` | События, задачи, вопросы | 4 | notes-edit | 3 | 0 | 0 | 0 | 0 | 0 |
| `fill-scope` | Заполнить пустые «Объект» и «Проект» (временная) | 3 | read | 0 | 1 | 0 | 1 | 0 | 0 |
| `zones` | Зоны: захватки, краны, стоянки | 3 | read | 1 | 0 | 0 | 3 | 0 | 0 |
| `subtypes` | Типы и подтипы элементов | 4 | subtypes-edit | 4 | 1 | 0 | 2 | 0 | 1 |
| `mark-prefixes` | Префиксы марок | 4 | prefix-edit | 5 | 1 | 0 | 0 | 0 | 1 |
| `element-catalog` | Элементы | 3 | read | 1 | 0 | 0 | 0 | 0 | 0 |
| `upload-drawing` | Загрузить чертёж | 5 | exchange | 0 | 2 | 0 | 1 | 0 | 1 |
| `shaft-panels` | Панели облицовки шахты | 2 | v1 | 0 | 0 | 0 | 3 | 0 | 0 |
| `revit-import` | Загрузить из Revit | 5 | exchange | 0 | 2 | 0 | 1 | 0 | 0 |
| `pdf-import` | Загрузить из PDF | 2 | v1 | 0 | 0 | 0 | 5 | 0 | 0 |
| `external-models` | Загрузить из FBX | 2 | v1 | 2 | 0 | 0 | 5 | 0 | 1 |
| `export-xls` | Экспорт в XLS | 5 | export-form | 2 | 0 | 0 | 0 | 0 | 0 |
| `export-pdf` | Экспорт в PDF | 5 | export-form | 0 | 0 | 0 | 0 | 0 | 0 |
| `history-import` | Импорт истории из XLS | 5 | exchange | 0 | 0 | 0 | 1 | 0 | 0 |
| `status-restore` | Восстановить статусы из выгрузки | 5 | exchange | 0 | 0 | 0 | 0 | 0 | 0 |
| `contracting-import` | Импорт контрактации из XLS | 5 | exchange | 0 | 0 | 0 | 1 | 0 | 0 |
| `objects-import` | Загрузка справочника объектов из Excel | 5 | exchange | 0 | 2 | 0 | 0 | 0 | 0 |
| `schedule-import` | Импорт графика MS Project из XLS | 5 | exchange | 0 | 0 | 0 | 1 | 0 | 0 |
| `import-input` | Загрузить из папки Input | 5 | exchange | 0 | 2 | 0 | 0 | 0 | 0 |
| `settings-io` | Экспорт/импорт настроек | 2 | v1 | 0 | 0 | 0 | 2 | 0 | 0 |
| `bulk-edit` | Массовая правка через Excel | 5 | exchange | 0 | 2 | 0 | 5 | 0 | 0 |
| `blk-bulk` | Массовая правка ЗР через Excel (МФР) | 5 | block-bulk-edit | 0 | 3 | 0 | 1 | 0 | 0 |
| `users-access` | Пользователи и доступ | 5 | module:users-access | 4 | 13 | 0 | 2 | 0 | 1 |
| `access-matrix` | Права пользователей | 5 | access-view | 4 | 1 | 0 | 0 | 0 | 0 |
| `sessions` | Сеансы | 5 | sessions-edit | 1 | 5 | 0 | 0 | 0 | 0 |
| `ldap` | Доменная авторизация | 5 | admin:ldap | 0 | 2 | 0 | 0 | 0 | 0 |
| `backups` | Резервные копии | 5 | admin:backups | 1 | 3 | 0 | 1 | 0 | 0 |
| `address-classifier` | Адресный классификатор | 3 | read | 1 | 0 | 0 | 6 | 0 | 0 |
| `map-admin` | Карта: подложка и источник | 5 | admin:map | 0 | 2 | 0 | 0 | 0 | 0 |
| `activity` | Журнал действий | 5 | read | 1 | 1 | 0 | 1 | 0 | 0 |
| `db-status` | Состояние БД | 3 | read | 1 | 0 | 0 | 1 | 0 | 0 |
| `admin-guide` | Памятка администратора | 3 | read | 1 | 0 | 0 | 3 | 0 | 0 |
| `reset-history` | Очистить историю статусов | 5 | admin:reset-history | 0 | 1 | 0 | 0 | 0 | 0 |
| `appearance` | Внешний вид | 4 | appearance-edit | 1 | 0 | 0 | 3 | 0 | 0 |
| `label-color` | Цвет подписей марок | 4 | label-color-edit | 1 | 0 | 0 | 0 | 0 | 0 |
| `status-colors` | Цвета статусов | 4 | color-edit | 2 | 0 | 0 | 0 | 0 | 0 |
| `marker-shapes` | Форма маркеров | 4 | shape-edit | 2 | 0 | 0 | 0 | 0 | 0 |
| `zone-colors` | Цвета зон | 4 | color-edit | 2 | 0 | 0 | 0 | 0 | 0 |
| `late-threshold` | Порог опоздания поставки | 4 | setting-edit | 2 | 0 | 0 | 0 | 0 | 0 |
| `mfr-colors` | Цвета модели МФР | 4 | revit-colors-edit | 3 | 0 | 0 | 0 | 0 | 0 |
| `change-password` | Сменить пароль | 5 | admin:password | 0 | 2 | 0 | 1 | 0 | 0 |
| `my-access` | Мой доступ | 5 | admin:my-access | 0 | 0 | 0 | 0 | 0 | 0 |
| `training` | Обучение: инструкция и тест | 5 | read | 1 | 5 | 0 | 0 | 0 | 0 |
| `training-history` | Как учатся сотрудники | 5 | read | 1 | 0 | 0 | 0 | 0 | 0 |
| `changelog` | Что нового | 5 | read | 2 | 2 | 0 | 0 | 0 | 0 |
| `statuslog` | Сообщения за сеанс | 5 | admin:statuslog | 0 | 0 | 0 | 0 | 0 | 0 |
| `shell-context` | Общие элементы оболочки | 2 | v1 | 0 | 1 | 0 | 0 | 0 | 0 |

## Самопроверки генератора

- Маршрутов backend: `ast` **346** = независимый подсчёт декораторов токенами `tokenize` **346** — совпало. После разворота `/dictionaries/{kind}` по видам справочника (12) строк: 379.
- `index.html`: встроенных скриптов (кроме importmap) 0, обращений к API в них 0, атрибутов-обработчиков `on*=` 0 — весь код V1 в `app.js` и соседних скриптах. Вызовов `api()`/`fetch()` в V1 всего 350; найдено обращений к путям: V1 387 (включая строки-адреса и таблицу отчётов), V2 412 (модуль `read-screen.js` раскрыт по `screens.json`).
- **Проблемы:**
  - маршрутов без правила области/раздела: 6
- маршруты, не отнесённые к области/разделу правилами: **6**
  - `POST /element-ops/contract`
  - `POST /element-ops/planned-date-batch`
  - `GET /element-ops/state`
  - `POST /element-ops/status-batch`
  - `GET /admin/reset-status-history/preview`
  - `GET /password-policy`
- маршруты V1 без экрана V2 (ни правило пути, ни точка входа V1, ни V2 экрана не дали): **0**
- обращения V1 к путям, которых нет в backend (мёртвые или ошибочные): **0**
- обращения V2 к путям, которых нет в backend (мёртвые или ошибочные): **0**
- вызовы V2, путь которых не удалось определить статически (проверить вручную): **1**
  - `exchange-import.js:98 api.upload(cfg.path(v))`
- вызовы V1 api()/fetch() с путём не литералом (разобраны по присваиваниям и настройкам объектов, остальное — слепое пятно): **15**
  - `app.js:421 fetch(url)`
  - `app.js:455 fetch(path)`
  - `app.js:747 api(адрес)`
  - `app.js:1185 api(адрес)`
  - `app.js:11246 api(url)`
  - `app.js:11442 api(simpleCatalogConfig . path)`
  - `app.js:11505 api(simpleCatalogConfig . path)`
  - `app.js:22596 api(def . endpoint)`
  - `app.js:23024 fetch(REPORTS [ currentReport ] . endpoint + suffix)`
  - `app.js:23127 api(endpoint)`
  - `app.js:24710 fetch(url)`
  - `app.js:29251 api(запрос)`
  - `app.js:34365 api(url)`
  - `app.js:34370 api(url + ?force=true)`
  - `map.js:76 fetch(config . geocode_url + ? + параметры . toString ( ))`
- обращения V1, метод которых не определён: **0**
- маршруты, которых не вызывает ни V1, ни V2 (внешние клиенты, служебные или мёртвые): **15**
  - `GET /axis-grid`
  - `GET /elements`
  - `GET /layers`
  - `GET /status-summary`
  - `GET /users/{user_id}/access-summary`
  - `DELETE /projects/{project_id}`
  - `DELETE /allowed-subtypes/{element_type}/{subtype}`
  - `DELETE /mark-type-prefixes/{prefix}`
  - `POST /import-dxf`
  - `GET /objects/{object_id}/external-models/bounds`
  - `GET /label-dates-visibility`
  - `PUT /label-dates-visibility`
  - `PUT /label-visibility`
  - `GET /admin/db-transfer/current`
  - `GET /schedule-versions/deviation`
- строки шлюза, не совпадающие ни с одним маршрутом backend: **0**
- разделы V1 без единой операции: **0**

## Первые отсутствующие операции по областям (эвристика важности)

Отсутствующие = состояние «нет в V2», «только переход в V1» или «отключена шлюзом»; порядок — по эвристике (вес раздела, запись/отчёт, есть ли точка входа в V1, редкие служебные вниз). Это подсказка очерёдности, а не решение.

### model — прораб, модель ЖБИ, операции над элементами, групповая смена статуса, даты, комментарии, контракты изделий

Отсутствует 5 из 24. Первые 5:

| № | Раздел | Операция | Сценарий | V2 |
| --- | --- | --- | --- | --- |
| 10 | Рабочие места | `PATCH /elements/bulk-planned-delivery-date` | Изменение: массовая плановая дата | только переход в V1 |
| 11 | Рабочие места | `PATCH /elements/bulk-status` | Массовая смена статуса (выделение рамкой в 2D, см. Docs/backlog.md). | только переход в V1 |
| 17 | Рабочие места | `PATCH /elements/{element_id}/contract` | Назначить изделию контракт, НЕ трогая статус (2026-08-06, живой запрос). | только переход в V1 |
| 22 | Рабочие места | `PATCH /elements/{element_id}/status` | Изменение: статус | только переход в V1 |
| 16 | Рабочие места | `GET /elements/{element_id}/context` | Мини-карта изделия (2026-08-05, запрос пользователя): где оно стоит — оси, основание объекта, зоны и сам конт… | только переход в V1 |

### mfr — МФР, учёт по блокам, факт, сроки, шахматка, динамика

Отсутствует 21 из 56. Первые 20:

| № | Раздел | Операция | Сценарий | V2 |
| --- | --- | --- | --- | --- |
| 355 | Учёт по блокам (МФР) | `PUT /objects/{object_id}/blocks/{block_id}/work-progress-cell` | Правка одной ячейки в отчёте «Учёт по блокам: статусы» (2026-09-05, живой запрос пользователя) — попадает в т… | только переход в V1 |
| 334 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks/chess-flat-export.pdf` | Выгрузка в PDF: плоская шахматка | только переход в V1 |
| 335 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks/chess-flat-export.xlsx` | Выгрузка в XLSX: плоская шахматка | только переход в V1 |
| 332 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks` | Создание: блоки объекта | только переход в V1 |
| 340 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks/recalc-membership` | Кнопка «Обновить принадлежность» («Учёт по блокам»): пересчитать этаж и секцию элементов модели заново — вруч… | только переход в V1 |
| 347 | Учёт по блокам (МФР) | `PUT /objects/{object_id}/blocks/{block_id}/boxes` | Экран «Учёт по блокам → Блоки»: геометрия блока руками — набором прямоугольников, поверх вычисленных по осям/… | только переход в V1 |
| 362 | Учёт по блокам (МФР) | `POST /objects/{object_id}/levels` | Создание: этажи и уровни | только переход в V1 |
| 364 | Учёт по блокам (МФР) | `PATCH /objects/{object_id}/levels/{level_id}` | Изменение: этажи и уровни | только переход в V1 |
| 369 | Учёт по блокам (МФР) | `POST /objects/{object_id}/sections` | Создание: секции | только переход в V1 |
| 371 | Учёт по блокам (МФР) | `PATCH /objects/{object_id}/sections/{section_id}` | Изменение: секции | только переход в V1 |
| 323 | Учёт по блокам (МФР) | `КЛИЕНТ chess-flat.js:660` | Печать бланка обхода шахматки, форматы A4/A3 (окно печати браузера) | только переход в V1 |
| 337 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/fact-changes` | Список: изменения факта | только переход в V1 |
| 341 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/track-progress` | Список: выполнение по трекам | только переход в V1 |
| 345 | Учёт по блокам (МФР) | `DELETE /objects/{object_id}/blocks/{block_id}` | Удаление: блоки объекта | только переход в V1 |
| 346 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/{block_id}/boxes` | Список: геометрия блока | только переход в V1 |
| 363 | Учёт по блокам (МФР) | `DELETE /objects/{object_id}/levels/{level_id}` | Удаление: этажи и уровни | только переход в V1 |
| 370 | Учёт по блокам (МФР) | `DELETE /objects/{object_id}/sections/{section_id}` | Удаление: секции | только переход в V1 |
| 372 | Учёт по блокам (МФР) | `GET /objects/{object_id}/work-progress` | Список: выполнение работ | только переход в V1 |
| 328 | Учёт по блокам (МФР) | `POST /objects/{object_id}/block-works/bulk-preview` | Что сделает `PUT /objects/{id}/block-works/bulk` с этим набором — БЕЗ записи. | в V2, но отключена шлюзом |
| 373 | Учёт по блокам (МФР) | `PUT /objects/{object_id}/work-progress/cell` | Сохранение: Виды работ и статус по блокам | только переход в V1 |

### picker — комплектовщик, контрагенты, договоры, спецификации, контракты, замена поставщика, обмен привязками

Отсутствует 20 из 54. Первые 20:

| № | Раздел | Операция | Сценарий | V2 |
| --- | --- | --- | --- | --- |
| 85 | Контрагенты | `POST /counterparties` | Контрагенты: создать контрагента (наименования, реквизиты, код, нормативы производительности) | только переход в V1 |
| 87 | Контрагенты | `PATCH /counterparties/{counterparty_id}` | Контрагенты: правка карточки контрагента целиком (с проверкой версии записи) | только переход в V1 |
| 92 | Договоры | `POST /agreements` | Создание: договоры — Новый договор — ОБЯЗАТЕЛЬНО на объект (2026-08-03). | только переход в V1 |
| 93 | Договоры | `PATCH /agreements/{agreement_id}` | Договоры: правка договора (номер, дата, объект; с проверкой версии) | только переход в V1 |
| 101 | Спецификации | `POST /specifications` | Спецификации: создать спецификацию договора | только переход в V1 |
| 102 | Спецификации | `PATCH /specifications/{specification_id}` | Спецификации: правка спецификации (номер, дата; с проверкой версии) | только переход в V1 |
| 104 | Контракты | `POST /contracts` | Контракты: создать контракт под спецификацией (тема, позиции, инциденты, нормативы) | только переход в V1 |
| 106 | Контракты | `PUT /contracts/default-map` | Сохранение: контракт по умолчанию по типу | только переход в V1 |
| 108 | Контракты | `PATCH /contracts/{contract_id}` | Контракты: правка контракта целиком — позиции и количество (не ниже привязанного), инциденты, архив, норматив… | только переход в V1 |
| 86 | Контрагенты | `GET /counterparties/full` | Вложенное дерево Контрагент -> Договоры -> Спецификации одним запросом — для каскадных селектов в форме контр… | только переход в V1 |
| 105 | Контракты | `GET /contracts/default-map` | Список: контракт по умолчанию по типу | только переход в V1 |
| 374 | График СМР | `POST /schedule-calc` | Создание: расчёт графика СМР | только переход в V1 |
| 376 | График СМР | `PUT /schedule-calc/inputs` | Сохранение: исходные данные расчёта — Замена обеих таблиц целиком. | только переход в V1 |
| 380 | График СМР | `POST /schedule-versions/deviation` | Создание: отклонение графика | только переход в V1 |
| 382 | График СМР | `GET /schedule-versions/gantt.pdf` | Выгрузка в PDF: диаграмма Ганта | только переход в V1 |
| 383 | График СМР | `GET /schedule-versions/gantt.xlsx` | Выгрузка в XLSX: диаграмма Ганта | только переход в V1 |
| 375 | График СМР | `GET /schedule-calc/inputs` | Исходные данные расчёта: виды работ (темп и порядок) и поток. | только переход в V1 |
| 381 | График СМР | `GET /schedule-versions/gantt` | Дерево для диаграммы Ганта. version_id не задан — текущий прогноз. | только переход в V1 |
| 384 | График СМР | `DELETE /schedule-versions/{version_id}` | Удаление версии. Базовую удалить нельзя: на ней держится сравнение, и «отклонение неизвестно от чего» — не то… | только переход в V1 |
| 377 | График СМР | `POST /schedule-calc/inputs/parse` | Разобрать файл обработки и вернуть значения В ФОРМУ, не записывая. | только переход в V1 |

### admin — пользователи, роли, доступы, пароли, сеансы, проекты и объекты, справочники, настройки, резервные копии, служебные

Отсутствует 31 из 163. Первые 20:

| № | Раздел | Операция | Сценарий | V2 |
| --- | --- | --- | --- | --- |
| 77 | Проекты и объекты | `GET /objects/{object_id}/avatar` | Отдаёт саму картинку — единственное место, где вложение возвращается СВОИМ content_type для показа в <img>, а… | только переход в V1 |
| 148 | Справочники | `POST /dictionaries/mark/{key}/delete` | Удаление записи справочника с заменой ссылок «Марка» | в V2, но отключена шлюзом |
| 160 | Справочники | `POST /dictionaries/zone/{key}/delete` | Удаление записи справочника с заменой ссылок «Зона» | в V2, но отключена шлюзом |
| 170 | Справочники | `POST /marks` | Создание: марки | только переход в V1 |
| 171 | Справочники | `PATCH /marks/{mark_id}` | Переименование ведёт за собой ТЕКСТ марки везде, где он лежит копией: у изделий (`elements.mark`) и в позиция… | только переход в V1 |
| 176 | Справочники | `PATCH /zones/{zone_id}` | Правка зоны справочника: номер, наименование, кран-владелец и ГЕОМЕТРИЯ ярусов (решения З9, З13, З14). | только переход в V1 |
| 178 | Справочники | `POST /zones/{zone_id}/undo` | Откат последней правки зоны ЦЕЛИКОМ (решение З12): реквизиты, ярусы и привязки элементов, которые изменил пер… | только переход в V1 |
| 45 | Пользователи и роли | `PATCH /roles/{key}` | Переименование. Ключ НЕ меняется: за него держатся выданные доступы и разрешения, а смена ключа ради косметик… | только переход в V1 |
| 134 | Справочники | `GET /address/regions-in-file` | Регионы в положенном KLADR.DBF — чтобы человек отмечал их по названиям, а не по двузначным кодам. | только переход в V1 |
| 177 | Справочники | `GET /zones/{zone_id}/geometry` | Геометрия зоны плюс КОНТЕКСТ для предпросмотра (решение З13): габариты объекта, сетка осей, соседние зоны той… | только переход в V1 |
| 270 | Настройки | `PATCH /users/{user_id}/menu-prefs` | Личная настройка меню «Действия»: порядок пунктов внутри блоков и избранное. | только переход в V1 |
| 271 | Настройки | `PATCH /users/{user_id}/min-label-px` | Тот же guard самообслуживания, что у set_ui_theme и set_view3d: менять можно только себе, если ты не админист… | только переход в V1 |
| 273 | Настройки | `PATCH /users/{user_id}/view3d` | Тот же guard самообслуживания, что у set_ui_theme и set_label_color: менять можно только себе, если ты не адм… | только переход в V1 |
| 46 | Пользователи и роли | `GET /roles/{key}/delete-plan` | Что произойдёт при удалении роли — до того, как оно произойдёт. | только переход в V1 |
| 73 | Проекты и объекты | `GET /map/tiles/{name}` | Файл подложки. Читается браузером по диапазонам байт — Starlette отдаёт Range сам, своего кода это не требует. | только переход в V1 |
| 130 | Справочники | `POST /address/fetch` | Скачать классификатор с сайта ФНС и распаковать — одной кнопкой. | только переход в V1 |
| 132 | Справочники | `POST /address/load` | Загрузка: адресный классификатор | только переход в V1 |
| 139 | Справочники | `POST /address/unpack` | Распаковка: адресный классификатор | только переход в V1 |
| 276 | Служебные функции | `КЛИЕНТ app.js:16577` | Копирование текста в буфер обмена (памятка администратора) | только переход в V1 |
| 277 | Служебные функции | `КЛИЕНТ app.js:16578` | Копирование текста в буфер обмена (памятка администратора) | только переход в V1 |

### exchange — импорт, экспорт, документы, отчёты

Отсутствует 29 из 74. Первые 20:

| № | Раздел | Операция | Сценарий | V2 |
| --- | --- | --- | --- | --- |
| 215 | Импорт и экспорт | `POST /import-contracting-xlsx` | Файл "Контрактация" (см. app/contracting_import.py, Docs/backlog.md, "Контрактация 2.0") — создаёт/находит Ко… | только переход в V1 |
| 219 | Импорт и экспорт | `POST /import-history-xlsx` | Импорт истории статусов из XLSX по чертежу объекта (скорректировать / дополнить / заменить) | только переход в V1 |
| 224 | Импорт и экспорт | `POST /import-pdf/apply` | Применение сверенных изменений: загрузка планировок из PDF — Фаза 2: применяет уже показанную сводку. | только переход в V1 |
| 212 | Импорт и экспорт | `POST /elements/bulk-edit/export` | Снимок реквизитов элементов ОДНИМ файлом — для правки в Excel и обратной загрузки (см. app/element_bulk_edit.… | только переход в V1 |
| 221 | Импорт и экспорт | `POST /import-pdf-facade/apply` | Фаза 2 упрощённой загрузки — применяет уже показанную сводку. | только переход в V1 |
| 227 | Импорт и экспорт | `POST /import-schedule-xlsx` | Файл графика MS Project (см. app/schedule_import.py) — сопоставляется с изделиями по блоку Захватка/Кран/Стоя… | только переход в V1 |
| 229 | Импорт и экспорт | `GET /import-templates/{key}/sample` | Образец файла: образцы файлов импорта | только переход в V1 |
| 233 | Импорт и экспорт | `POST /objects/{object_id}/block-works/bulk-edit/apply` | Применение сверенных изменений: массовая правка через Excel | только переход в V1 |
| 236 | Импорт и экспорт | `POST /objects/{object_id}/clear-import-data` | Отладочная очистка справочников объекта перед повторной загрузкой Revit/PDF (app/import_reset.py) — не двухфа… | только переход в V1 |
| 239 | Импорт и экспорт | `POST /objects/{object_id}/external-models` | Создание: внешние 3D-модели | только переход в V1 |
| 243 | Импорт и экспорт | `PATCH /objects/{object_id}/external-models/{model_id}` | Изменение: внешние 3D-модели | только переход в V1 |
| 245 | Импорт и экспорт | `POST /objects/{object_id}/external-models/{model_id}/recenter` | Перецентровка: внешние 3D-модели | только переход в V1 |
| 247 | Импорт и экспорт | `POST /objects/{object_id}/work-types/apply` | Применение сверенных изменений: виды работ — Фаза 2: применяет уже показанную сводку по токену. | только переход в V1 |
| 248 | Импорт и экспорт | `GET /settings/export` | Выгрузка: настройки | только переход в V1 |
| 249 | Импорт и экспорт | `POST /settings/import` | Создание / действие: Экспорт и импорт настроек | только переход в V1 |
| 179 | Отчёты | `КЛИЕНТ app.js:23021` | Печать отчёта (окно печати браузера) | только переход в V1 |
| 180 | Отчёты | `GET /objects/{object_id}/activity-users` | Кого можно выбрать в «Моей работе» и в фильтре «Изменения». | только переход в V1 |
| 181 | Отчёты | `GET /report-help/{key}` | Справка по отчёту (2026-08-14): что показывает и по каким правилам считает. | только переход в V1 |
| 222 | Импорт и экспорт | `GET /import-pdf/analyze/progress/{job_id}` | Фаза 1, шаг 2: состояние фоновой задачи разбора. `status` — running/done/error; при `done` в `result` та же с… | только переход в V1 |
| 237 | Импорт и экспорт | `GET /objects/{object_id}/drawings` | Список: чертежи — Версии чертежа объекта, новые сверху (этап B). | только переход в V1 |

## Матрица по областям

Для каждой области — две таблицы: **К выполнению** (состояния «в V2, но отключена шлюзом», «только переход в V1», «нет в V2», «в ветке, не опубликована») — это рабочий список исполнителя; **Уже в V2 и прочее** (опубликованные, не вызываемые интерфейсами, служебные).

Столбцы: **Операция** (метод и путь backend); **Сценарий** (докстринг маршрута, при его отсутствии — по адресу); **Права** (проверки backend: раздел прав и уровень, «условно» — ветвление по праву); **V1** (точка входа в интерфейсе — по графу вызовов, приближённо — и место вызова в `app.js`); **V2** (где вызывается интерфейсом V2); **Вид · проверка** (чтение/запись и чем подтверждено — по `screens.json` и строке шлюза); **Шлюз** (строки `write-gate.js`); **Опубл.** (в `main`); **Что осталось**.

### model — прораб, модель ЖБИ, операции над элементами, групповая смена статуса, даты, комментарии, контракты изделий

**К выполнению: 14**

| № | Раздел | Операция | Сценарий | Права | V1 | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 5 | Рабочие места | `POST /element-ops/contract` | Карточка изделия: назначить, сменить или снять контракт без смены статуса (со сверкой ожидаемого со… | «Статусы изделий: смена по одному и группой»: изменение | не вызывается из V1 | есть: element-ops.js:305 · экран `element-ops` | запись · настоящий backend: scripts/verify_element_ops.py S11 и браузер на на… | разрешена `element.contract.set`, проверка тела | разрешена шлюзом (element.contract.set) и вызывается из V2 (element-ops.js:305) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 6 | Рабочие места | `POST /element-ops/planned-date-batch` | Схема: плановая дата поставки изделия или пачки — одна дата (или снятие), сверка прежней даты, всё… | «Плановая дата поставки изделия»: изменение | не вызывается из V1 | есть: element-ops.js:241, element-ops.js:697 · экран `element-ops` | запись · настоящий backend: scripts/verify_element_ops.py S10 и браузер на на… | разрешена `element.planned.batch`, проверка тела | разрешена шлюзом (element.planned.batch) и вызывается из V2 (element-ops.js:241, element-ops.js:697) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 7 | Рабочие места | `GET /element-ops/state` | Текущее состояние ВСЕЙ пачки изделий ОДНИМ запросом — для сверки после неопределённого исхода запис… | «Схема, карточка изделия, фильтры, 3D»: чтение | не вызывается из V1 | есть: element-ops.js:185 | чтение · не проверено | не ограничивается (чтение) | подключено в этой ветке (element-ops.js:185); в опубликованной нет: публикация |
| 8 | Рабочие места | `POST /element-ops/status-batch` | Схема: смена статуса изделия или пачки — контракт СОХРАНЯЕТСЯ (клиент шлёт только ожидаемое состоян… | «Статусы изделий: смена по одному и группой»: изменение | не вызывается из V1 | есть: element-ops.js:509, element-ops.js:520 · экран `element-ops` | запись · настоящий backend: scripts/verify_element_ops.py (HTTP: предпросмотр… | разрешена `element.status.batch`, проверка тела | разрешена шлюзом (element.status.batch) и вызывается из V2 (element-ops.js:509, element-ops.js:520) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 10 | Рабочие места | `PATCH /elements/bulk-planned-delivery-date` | Изменение: массовая плановая дата | «Плановая дата поставки изделия»: изменение | окно «Плановая дата поставки» (`bulk-planned-date-backdrop`) — app.js:9376 | только переход в V1 (экран `element-ops`, статус 5) | запись · — | отключена `counterparties.write` | нет формы: экран «Операции над элементами» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области model и проверки (см. «Перечень проверок»). Реестр экранов: «Вложения изделия и построчные плановые даты (таблица V1) — в текущем интерфейсе; групповая смена статуса не назначает к…» |
| 11 | Рабочие места | `PATCH /elements/bulk-status` | Массовая смена статуса (выделение рамкой в 2D, см. Docs/backlog.md). | «Статусы изделий: смена по одному и группой»: изменение | окно «Массовая смена статуса» (`bulk-status-backdrop`) — app.js:9271 | только переход в V1 (экран `element-ops`, статус 5) | запись · — | отключена `counterparties.write` | нет формы: экран «Операции над элементами» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области model и проверки (см. «Перечень проверок»). Реестр экранов: «Вложения изделия и построчные плановые даты (таблица V1) — в текущем интерфейсе; групповая смена статуса не назначает к…» |
| 15 | Рабочие места | `PATCH /elements/{element_id}/comment` | Произвольный комментарий к элементу (2026-08-02, живой запрос). | «Комментарий к изделию»: изменение | окно «Элемент» (`element-form-backdrop`); окно «Плановая дата поставки» (`planned-date-backdrop`) — app.js:79… | есть: element-ops.js:279 · экран `element-ops` | запись · настоящий backend: браузер на настоящем backend; права раздела «Комм… | разрешена `element.comment`, проверка тела; вне формы разрешённой строки — отключена `cou… | разрешена шлюзом (element.comment) и вызывается из V2 (element-ops.js:279) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 16 | Рабочие места | `GET /elements/{element_id}/context` | Мини-карта изделия (2026-08-05, запрос пользователя): где оно стоит — оси, основание объекта, зоны… | «Схема, карточка изделия, фильтры, 3D»: чтение | точка входа не установлена статически — app.js:14292 | только переход в V1 (экран `ws-model`, статус 5) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Модель: схема 2D/3D» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Групповой выбор — в 2D (в 3D выбор одиночный, как в V1); вложения изделия и построчные даты в групповой плановой дате —…» |
| 17 | Рабочие места | `PATCH /elements/{element_id}/contract` | Назначить изделию контракт, НЕ трогая статус (2026-08-06, живой запрос). | «Статусы изделий: смена по одному и группой»: изменение | окно «Контракт изделия» (`contract-pick-backdrop`) — app.js:8906 openCardContractPicker() | только переход в V1 (экран `element-ops`, статус 5) | запись · — | отключена `counterparties.write` | нет формы: экран «Операции над элементами» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области model и проверки (см. «Перечень проверок»). Реестр экранов: «Вложения изделия и построчные плановые даты (таблица V1) — в текущем интерфейсе; групповая смена статуса не назначает к…» |
| 18 | Рабочие места | `PATCH /elements/{element_id}/fields` | Изменение: реквизиты — Ручная правка полей элемента (решения Э2, Э4, Э5). | «Реквизиты изделий: правка полей»: изменение | окно «Элемент» (`element-form-backdrop`) — app.js:14330 | есть: element-ops.js:442 · экран `element-ops` | запись · настоящий backend: браузер на настоящем backend; права раздела «Рекв… | разрешена `element.fields`, проверка тела; вне формы разрешённой строки — отключена `coun… | разрешена шлюзом (element.fields) и вызывается из V2 (element-ops.js:442) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 19 | Рабочие места | `DELETE /elements/{element_id}/history/{history_id}` | Карточка изделия: удаление записи истории статусов (пересчёт текущего статуса, последнюю запись уда… | «История статусов: правка и удаление записей»: изменение | окно «Плановая дата поставки» (`planned-date-backdrop`); окно «Смена статуса» (`status-contract-backdrop`) —… | есть: element-ops.js:376 · экран `element-ops` | запись · настоящий backend: браузер на настоящем backend; права раздела «Исто… | разрешена `element.history.delete` | разрешена шлюзом (element.history.delete) и вызывается из V2 (element-ops.js:376) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 20 | Рабочие места | `PATCH /elements/{element_id}/history/{history_id}` | Правка ЗАПИСИ истории статусов: статус, дата/время применения, автор, комментарий (живой запрос: «у… | «История статусов: правка и удаление записей»: изменение | окно «Элемент» (`element-form-backdrop`); окно «Смена статуса» (`status-contract-backdrop`) — app.js:14000 ef… | есть: element-ops.js:355 · экран `element-ops` | запись · настоящий backend: браузер на настоящем backend; права раздела «Исто… | разрешена `element.history.edit`, проверка тела; вне формы разрешённой строки — отключена… | разрешена шлюзом (element.history.edit) и вызывается из V2 (element-ops.js:355) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 21 | Рабочие места | `PATCH /elements/{element_id}/planned-delivery-date` | Плановая дата поставки — независимое действие, НЕ привязанное к смене статуса (партии убраны, см. D… | «Плановая дата поставки изделия»: изменение | окно «Плановая дата поставки» (`planned-date-backdrop`) — app.js:8638, app.js:20062 renderContractExpandedVie… | есть: counterparties.js:2648 · экран `counterparties` | запись · не проверено: scripts/picker_verify/cp.mjs: успех и SQL, очистк… | разрешена `element.planned-date`, поля: planned_delivery_date, проверка тела; вне формы р… | разрешена шлюзом (element.planned-date) и вызывается из V2 (counterparties.js:2648) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 22 | Рабочие места | `PATCH /elements/{element_id}/status` | Изменение: статус | «Статусы изделий: смена по одному и группой»: изменение | окно «Смена статуса» (`status-contract-backdrop`) — app.js:8599 doApplyStatus() | только переход в V1 (экран `element-ops`, статус 5) | запись · — | отключена `counterparties.write` | нет формы: экран «Операции над элементами» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области model и проверки (см. «Перечень проверок»). Реестр экранов: «Вложения изделия и построчные плановые даты (таблица V1) — в текущем интерфейсе; групповая смена статуса не назначает к…» |

**Уже в V2 и прочее: 10**

| № | Раздел | Операция | Сценарий | Состояние | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2 | Рабочие места | `GET /axis-grid` | Список: сетка осей | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `ws-model`) | чтение · — | не ограничивается (чтение) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 3 | Рабочие места | `GET /changes` | Что изменилось у элементов чертежа после момента `since` — для автоматического обновления… | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `ws-model` | чтение · настоящий backend: настоящий backend (копия обезличенной БД, вход по… | не ограничивается (чтение) | чтение опубликовано |
| 9 | Рабочие места | `GET /elements` | Список: элементы | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `ws-model`) | чтение · — | не ограничивается (чтение) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 12 | Рабочие места | `POST /elements/changed` | Элементы объекта, чьи реквизиты или история статуса менялись за период. | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `ws-model` | чтение (POST) · настоящий backend: настоящий backend (копия обезличенной БД, вход по… | не ограничивается (чтение) | чтение опубликовано |
| 13 | Рабочие места | `GET /elements/{element_id}` | Просмотр: элементы | реализована в V2 и опубликована | есть: element-ops.js:363, element-ops.js:382 · экран `ws-model` | чтение · настоящий backend: настоящий backend (копия обезличенной БД, вход по… | не ограничивается (чтение) | чтение опубликовано |
| 14 | Рабочие места | `GET /elements/{element_id}/activity` | История изменений ОДНОГО изделия — все события журнала о нём (живой запрос 2026-08-03, бл… | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `ws-model` | чтение · настоящий backend: настоящий backend (копия обезличенной БД, вход по… | не ограничивается (чтение) | чтение опубликовано |
| 23 | Рабочие места | `GET /layers` | Слои файла для выбора произвольного набора под источником (п.5 третьего раунда). | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `ws-model`) | чтение · — | не ограничивается (чтение) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 24 | Рабочие места | `POST /plan-data` | Всё, что нужно интерактивной схеме одним запросом: элементы, сетка осей, радиус маркера,… | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `ws-model` | чтение (POST) · настоящий backend: настоящий backend (копия обезличенной БД, вход по… | не ограничивается (чтение) | чтение опубликовано |
| 30 | Рабочие места | `GET /source-files` | Файлы, реально присутствующие в Input/ прямо сейчас (см. INPUT_DIR выше) — сканируется на… | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `ws-model` | чтение · настоящий backend: настоящий backend (копия обезличенной БД, вход по… | не ограничивается (чтение) | чтение опубликовано |
| 31 | Рабочие места | `GET /status-summary` | Разбивка по статусам. Всегда в пределах доступного пользователю. | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `ws-model`) | чтение · — | не ограничивается (чтение) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |

### mfr — МФР, учёт по блокам, факт, сроки, шахматка, динамика

**К выполнению: 39**

| № | Раздел | Операция | Сценарий | Права | V1 | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 323 | Учёт по блокам (МФР) | `(без API) chess-flat.js:660` | Печать бланка обхода шахматки, форматы A4/A3 (окно печати браузера) | как у экрана V1 | Действия › Шахматка › Плоская шахматка; chess-flat.js:660 doPrintAction | только переход в V1 (экран `chess-flat`, статус 5) | клиентская · — | — | нет в V2: добавить клиентскую операцию на экране «Плоская шахматка» (печатная форма / буфер обмена, без API — шлюз не нужен) |
| 326 | Учёт по блокам (МФР) | `GET /objects/{object_id}/block-works/active-counts` | block_id -> число активных ЗР, одним запросом — колонка счётчика в списке блоков «Запланированные р… | «Виды работ и статус по блокам»: чтение | окно «Сроки» (`block-works-dates-backdrop`) — app.js:34130 loadBlkPlansBlocks(), app.js:34299 refreshPlansTab… | есть: blocks-screen.js:73, mfr-block-panel.js:42 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (blocks-screen.js:73, mfr-block-panel.js:42); в опубликованной нет: публикация |
| 327 | Учёт по блокам (МФР) | `PUT /objects/{object_id}/block-works/bulk` | ЗР: групповая правка сроков (сдвиг плана/прогноза; прогноз = план) — только по предпросмотру, с отп… | «Виды работ и статус по блокам»: изменение | окно «Сроки» (`block-works-dates-backdrop`) — app.js:32544 runBlockWorksBulk() | есть: mfr-dialogs.js:232 · экран `blocks` | запись · не проверено: HTTP и браузер: предпросмотр = результат; 409 при… | разрешена `block-work.bulk`, поля: block_work_ids/op/field/days/expected, проверка тела | разрешена шлюзом (block-work.bulk) и вызывается из V2 (mfr-dialogs.js:232) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 328 | Учёт по блокам (МФР) | `POST /objects/{object_id}/block-works/bulk-preview` | Что сделает `PUT /objects/{id}/block-works/bulk` с этим набором — БЕЗ записи. | «Виды работ и статус по блокам»: изменение | не вызывается из V1 | форма есть: mfr-dialogs.js:219 · экран `blocks` | запись · настоящий backend: настоящий backend с настоящим входом (копия обезл… | нет строки (запрещено по умолчанию) | форма есть (mfr-dialogs.js:219), шлюз отключён (строки в POLICY нет — запрещено по умолчанию); нужны проверки (см. «Перечень проверок») и строка `allowed: true` в блоке области mfr |
| 332 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks` | Создание: блоки объекта | «Секции, этажи и блоки объекта»: изменение | окно «Факт: отчёт о выполнении» (`block-fact-backdrop`); Действия › Справочники › Учёт по блокам — app.js:349… | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 333 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks/chess-flat-batch` | Шахматка: пакетный ввод факта по нескольким блокам (одна транзакция, ключ идемпотентности, сверка «… | «Виды работ и статус по блокам»: изменение | Действия › Шахматка › Плоская шахматка — chess-flat.js:380 doCommit() | есть: chess-flat-screen.js:199 · экран `chess-flat` | запись · не проверено: HTTP и браузер: пакет целиком, повтор с тем же кл… | разрешена `chess.batch`, поля: report_date/track_code/idempotency_key/items, проверка тела | разрешена шлюзом (chess.batch) и вызывается из V2 (chess-flat-screen.js:199) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 334 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks/chess-flat-export.pdf` | Выгрузка в PDF: плоская шахматка | «Виды работ и статус по блокам»: чтение | Действия › Шахматка › Плоская шахматка — chess-flat.js:698 doExportAction() | только переход в V1 (экран `chess-flat`, статус 5) | чтение (POST) (файл) · — | не ограничивается (чтение) | экран «Плоская шахматка» реализован в V2 модулем `chess-flat-edit`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: «Печать бланка обхода А4/А3 — в текущем интерфейсе.» |
| 335 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks/chess-flat-export.xlsx` | Выгрузка в XLSX: плоская шахматка | «Виды работ и статус по блокам»: чтение | Действия › Шахматка › Плоская шахматка — chess-flat.js:698 doExportAction() | только переход в V1 (экран `chess-flat`, статус 5) | чтение (POST) (файл) · — | не ограничивается (чтение) | экран «Плоская шахматка» реализован в V2 модулем `chess-flat-edit`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: «Печать бланка обхода А4/А3 — в текущем интерфейсе.» |
| 336 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/chess-flat-layout` | Список: плоская шахматка (раскладка) | «Виды работ и статус по блокам»: чтение | Действия › Шахматка › Плоская шахматка — chess-flat.js:308 loadLayout(), chess-flat.js:416 refreshLayoutSilen… | есть: chess-flat-screen.js:78, chess-flat-screen.js:201 · экран `chess-flat` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (chess-flat-screen.js:78, chess-flat-screen.js:201); в опубликованной нет: публикация |
| 337 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/fact-changes` | Список: изменения факта | «Виды работ и статус по блокам»: чтение | кнопка «Сбросить все фильтры» (`mfr-reset-all-filters`); окно «Шахматка: выбор доски» (`mfr-chess-modal-backd… | только переход в V1 (экран `fact-journal`, статус 5) | чтение · — | не ограничивается (чтение) | экран «Журнал факта» реализован в V2 модулем `fact-journal-edit`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. |
| 340 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks/recalc-membership` | Кнопка «Обновить принадлежность» («Учёт по блокам»): пересчитать этаж и секцию элементов модели зан… | «Секции, этажи и блоки объекта»: изменение | окно «Учёт по блокам» (`blocks-backdrop`) — app.js:34461 | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 341 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/track-progress` | Список: выполнение по трекам | «Виды работ и статус по блокам»: чтение | окно «Шахматка: выбор доски» (`mfr-chess-modal-backdrop`) — app.js:32736 selectMfrChessTrack() | только переход в V1 (экран `blocks`, статус 5) | чтение · — | не ограничивается (чтение) | экран «Учёт по блокам» реализован в V2 модулем `blocks-edit`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 342 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/work-types-settings` | Список: настройки видов работ | «Виды работ и статус по блокам»: чтение | кнопка «Настройки» (`block-progress-settings-btn`) — app.js:31682 | есть: mfr-dialogs.js:67, mfr-dialogs.js:148 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (mfr-dialogs.js:67, mfr-dialogs.js:148); в опубликованной нет: публикация |
| 343 | Учёт по блокам (МФР) | `PUT /objects/{object_id}/blocks/work-types-settings` | Состав работ ГРУППЫ блоков — по предпросмотру, все блоки одной транзакцией (всё или ничего) | «Виды работ и статус по блокам»: изменение | окно «Настройки: операции блока» (`block-settings-backdrop`) — app.js:31743 | есть: mfr-dialogs.js:143 · экран `blocks` | запись · не проверено: HTTP и браузер: атомарность (отказ на блоке — отк… | разрешена `block-settings.group`, поля: block_ids/work_type_ids/expected, проверка тела | разрешена шлюзом (block-settings.group) и вызывается из V2 (mfr-dialogs.js:143) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 344 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks/work-types-settings/preview` | Что сделает сохранение состава работ (`PUT .../work-types-settings`) — БЕЗ записи: добавится, вернё… | «Виды работ и статус по блокам»: изменение | не вызывается из V1 | форма есть: mfr-dialogs.js:126 · экран `blocks` | разбор файла · настоящий backend: настоящий backend с настоящим входом (копия обезл… | нет строки (запрещено по умолчанию) | форма есть (mfr-dialogs.js:126), шлюз отключён (строки в POLICY нет — запрещено по умолчанию); нужны проверки (см. «Перечень проверок») и строка `allowed: true` в блоке области mfr |
| 345 | Учёт по блокам (МФР) | `DELETE /objects/{object_id}/blocks/{block_id}` | Удаление: блоки объекта | «Секции, этажи и блоки объекта»: изменение | окно «Факт: отчёт о выполнении» (`block-fact-backdrop`) — app.js:34991 renderBlkMatrix() | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 346 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/{block_id}/boxes` | Список: геометрия блока | «Секции, этажи и блоки объекта»: чтение | окно «Факт: отчёт о выполнении» (`block-fact-backdrop`) — app.js:35051 openBlkGeoEditor() | только переход в V1 (экран `blocks`, статус 5) | чтение · — | не ограничивается (чтение) | экран «Учёт по блокам» реализован в V2 модулем `blocks-edit`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 347 | Учёт по блокам (МФР) | `PUT /objects/{object_id}/blocks/{block_id}/boxes` | Экран «Учёт по блокам → Блоки»: геометрия блока руками — набором прямоугольников, поверх вычисленны… | «Секции, этажи и блоки объекта»: изменение | окно «Учёт по блокам» (`blocks-backdrop`) — app.js:35445 | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 349 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/{block_id}/fact-reports` | Список: отчёты о выполнении (факт) | «Виды работ и статус по блокам»: чтение | окно «Новый отчёт: выбор блока» (`fj-new-report-backdrop`); окно «Журнал факта —» (`fact-journal-backdrop`) —… | есть: fact-form.js:28, fact-form.js:183 · экран `fact-journal` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (fact-form.js:28, fact-form.js:183); в опубликованной нет: публикация |
| 350 | Учёт по блокам (МФР) | `POST /objects/{object_id}/blocks/{block_id}/fact-reports` | Факт: новый документ на дату (проценты по работам блока) | «Виды работ и статус по блокам»: изменение | окно «Факт: отчёт о выполнении» (`block-fact-backdrop`) — app.js:31973 | есть: fact-form.js:177 · экран `fact-journal` | запись · настоящий backend: браузер и HTTP на настоящем backend: документ и с… | разрешена `fact.create`, поля: report_date/items, проверка тела | разрешена шлюзом (fact.create) и вызывается из V2 (fact-form.js:177) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 351 | Учёт по блокам (МФР) | `DELETE /objects/{object_id}/blocks/{block_id}/fact-reports/{report_id}` | Удаление документа «Факт» целиком (этап 4, В6, решение пользователя 2026-09-10) — порог тот же, что… | «Виды работ и статус по блокам»: изменение | окно «Журнал факта —» (`fact-journal-backdrop`) — app.js:31934, app.js:32204 fjRenderTable() | есть: fact-form.js:218, fact-journal-screen.js:146 · экран `fact-journal` | запись · не проверено: браузер и HTTP: удаление → SQL (документ, строки,… | разрешена `fact.delete`, проверка тела | разрешена шлюзом (fact.delete) и вызывается из V2 (fact-form.js:218, fact-journal-screen.js:146) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 352 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/{block_id}/fact-reports/{report_id}` | Просмотр: отчёты о выполнении (факт) | «Виды работ и статус по блокам»: чтение | окно «Новый отчёт: выбор блока» (`fj-new-report-backdrop`); окно «Журнал факта —» (`fact-journal-backdrop`) —… | есть: fact-form.js:59, fact-form.js:185 · экран `fact-journal` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (fact-form.js:59, fact-form.js:185); в опубликованной нет: публикация |
| 353 | Учёт по блокам (МФР) | `PUT /objects/{object_id}/blocks/{block_id}/fact-reports/{report_id}` | Факт: исправить документ (с отпечатком документа; построчная история правок пишется сервером) | «Виды работ и статус по блокам»: изменение | окно «Факт: отчёт о выполнении» (`block-fact-backdrop`) — app.js:31968 | есть: fact-form.js:177 · экран `fact-journal` | запись · не проверено: браузер и HTTP: правка → SQL, work_fact_item_hist… | разрешена `fact.update`, поля: report_date/items/expected_rev, проверка тела | разрешена шлюзом (fact.update) и вызывается из V2 (fact-form.js:177) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 354 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/{block_id}/progress` | Ход выполнения: блоки объекта | «Виды работ и статус по блокам»: чтение | окно «Настройки: операции блока» (`block-settings-backdrop`); кнопка «Факт» (`block-progress-fact-btn`) — app… | есть: fact-form.js:42, mfr-block-panel.js:54 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (fact-form.js:42, mfr-block-panel.js:54); в опубликованной нет: публикация |
| 355 | Учёт по блокам (МФР) | `PUT /objects/{object_id}/blocks/{block_id}/work-progress-cell` | Правка одной ячейки в отчёте «Учёт по блокам: статусы» (2026-09-05, живой запрос пользователя) — по… | «Виды работ и статус по блокам»: изменение | точка входа не установлена статически — app.js:35831 | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 356 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/{block_id}/work-types-settings` | Список: настройки видов работ | «Виды работ и статус по блокам»: чтение | окно «Учёт по блокам» (`blocks-backdrop`) — app.js:31681, app.js:31795 openBlockFactForm() | есть: fact-form.js:28, mfr-dialogs.js:68 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (fact-form.js:28, mfr-dialogs.js:68); в опубликованной нет: публикация |
| 357 | Учёт по блокам (МФР) | `PUT /objects/{object_id}/blocks/{block_id}/work-types-settings` | Состав работ блока (ЗР): добавить / снять (с историей — мягко) — по предпросмотру, с отпечатком сос… | «Виды работ и статус по блокам»: изменение | окно «Настройки: операции блока» (`block-settings-backdrop`) — app.js:31738 | есть: mfr-dialogs.js:144 · экран `blocks` | запись · настоящий backend: браузер и HTTP на настоящем backend: мягкое сняти… | разрешена `block-settings.set`, поля: work_type_ids/expected, проверка тела | разрешена шлюзом (block-settings.set) и вызывается из V2 (mfr-dialogs.js:144) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 358 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/{block_id}/work-types/{work_type_id}/fact-history` | Просмотр: Виды работ и статус по блокам | «Виды работ и статус по блокам»: чтение | точка входа не установлена статически — app.js:31483 showBpHistoryTooltip() | есть: fact-form.js:130 · экран `fact-journal` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (fact-form.js:130); в опубликованной нет: публикация |
| 361 | Учёт по блокам (МФР) | `GET /objects/{object_id}/levels` | Список: этажи и уровни | «Секции, этажи и блоки объекта»: чтение | окно «Факт: отчёт о выполнении» (`block-fact-backdrop`); окно «Учёт по блокам» (`blocks-backdrop`) — app.js:3… | есть: fact-journal-screen.js:37 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (fact-journal-screen.js:37); в опубликованной нет: публикация |
| 362 | Учёт по блокам (МФР) | `POST /objects/{object_id}/levels` | Создание: этажи и уровни | «Секции, этажи и блоки объекта»: изменение | окно «Учёт по блокам» (`blocks-backdrop`) — app.js:34878 | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 363 | Учёт по блокам (МФР) | `DELETE /objects/{object_id}/levels/{level_id}` | Удаление: этажи и уровни | «Секции, этажи и блоки объекта»: изменение | окно «Учёт по блокам» (`blocks-backdrop`); Действия › Справочники › Учёт по блокам — app.js:34836 renderBlkLe… | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 364 | Учёт по блокам (МФР) | `PATCH /objects/{object_id}/levels/{level_id}` | Изменение: этажи и уровни | «Секции, этажи и блоки объекта»: изменение | окно «Учёт по блокам» (`blocks-backdrop`); Действия › Справочники › Учёт по блокам — app.js:34827 renderBlkLe… | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 367 | Учёт по блокам (МФР) | `GET /objects/{object_id}/planning-tracks` | Весь справочник треков объекта (не только доски «Шахматки») — для вкладки «Учёт по блокам → Виды ра… | «Виды работ и статус по блокам»: чтение | окно «Учёт по блокам» (`blocks-backdrop`); окно «Факт: отчёт о выполнении» (`block-fact-backdrop`) — app.js:3… | есть: blocks-screen.js:62 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (blocks-screen.js:62); в опубликованной нет: публикация |
| 368 | Учёт по блокам (МФР) | `GET /objects/{object_id}/sections` | Список: секции | «Секции, этажи и блоки объекта»: чтение | окно «Факт: отчёт о выполнении» (`block-fact-backdrop`); окно «Учёт по блокам» (`blocks-backdrop`) — app.js:3… | есть: fact-journal-screen.js:37 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (fact-journal-screen.js:37); в опубликованной нет: публикация |
| 369 | Учёт по блокам (МФР) | `POST /objects/{object_id}/sections` | Создание: секции | «Секции, этажи и блоки объекта»: изменение | окно «Учёт по блокам» (`blocks-backdrop`) — app.js:34780 | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 370 | Учёт по блокам (МФР) | `DELETE /objects/{object_id}/sections/{section_id}` | Удаление: секции | «Секции, этажи и блоки объекта»: изменение | окно «Учёт по блокам» (`blocks-backdrop`); Действия › Справочники › Учёт по блокам — app.js:34396 renderBlkSe… | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 371 | Учёт по блокам (МФР) | `PATCH /objects/{object_id}/sections/{section_id}` | Изменение: секции | «Секции, этажи и блоки объекта»: изменение | окно «Учёт по блокам» (`blocks-backdrop`); Действия › Справочники › Учёт по блокам — app.js:34421 save() | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 372 | Учёт по блокам (МФР) | `GET /objects/{object_id}/work-progress` | Список: выполнение работ | «Виды работ и статус по блокам»: чтение | окно «Учёт по блокам» (`blocks-backdrop`); окно «Факт: отчёт о выполнении» (`block-fact-backdrop`) — app.js:3… | только переход в V1 (экран `blocks`, статус 5) | чтение · — | не ограничивается (чтение) | экран «Учёт по блокам» реализован в V2 модулем `blocks-edit`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 373 | Учёт по блокам (МФР) | `PUT /objects/{object_id}/work-progress/cell` | Сохранение: Виды работ и статус по блокам | «Виды работ и статус по блокам»: изменение | точка входа не установлена статически — app.js:35854 | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области mfr и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |

**Уже в V2 и прочее: 17**

| № | Раздел | Операция | Сценарий | Состояние | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 25 | Рабочие места | `GET /revit-plan/colors` | Список: цвета модели МФР — Цветовая схема объекта плюс перечень шаблонов. | реализована в V2 и опубликована | есть: revit-colors-edit.js:116, revit-colors-edit.js:150 · экран `mfr-colors` | чтение · настоящий backend: стенд RC-01…06: запись ЦЕЛОЙ схемы (все 12 цветов… | не ограничивается (чтение) | чтение опубликовано; экран «Цвета модели МФР» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 26 | Рабочие места | `PUT /revit-plan/colors` | Цветовая схема модели МФР объекта (целиком) | реализована в V2 и опубликована | есть: revit-colors-edit.js:160 · экран `mfr-colors` | запись · настоящий backend: живая проверка: запись, шаблон, возврат | разрешена `revit.colors` | готово и опубликовано (шлюз: revit.colors); экран «Цвета модели МФР» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 27 | Рабочие места | `GET /revit-plan/element` | Список: карточка элемента модели МФР | реализована в V2 и опубликована | есть: workspace.js:235 · экран `ws-model` | чтение · настоящий backend: настоящий backend (копия обезличенной БД, вход по… | не ограничивается (чтение) | чтение опубликовано |
| 28 | Рабочие места | `GET /revit-plan/elements` | Список: элементы — Контуры модели для плана. | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `ws-mfr` | чтение · настоящий backend: рабочее место в V2: сцена V1 в кадре только для ч… | не ограничивается (чтение) | чтение опубликовано; экран «Модель МФР» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 29 | Рабочие места | `GET /revit-plan/filters` | Что есть у объекта из модели: этажи, секции, разделы, категории. | реализована в V2 и опубликована | есть: revit-colors-edit.js:116 · экран `mfr-colors` | чтение · настоящий backend: стенд RC-01…06: запись ЦЕЛОЙ схемы (все 12 цветов… | не ограничивается (чтение) | чтение опубликовано; экран «Цвета модели МФР» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 324 | Учёт по блокам (МФР) | `GET /objects/{object_id}/block-work-types` | Справочник BLOCK_UNITS объекта («эт/сек», «кв.эт/сек»), без привязки к блоку — источник д… | реализована в V2 и опубликована | есть: blocks-screen.js:246, fact-journal-screen.js:37 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | чтение опубликовано |
| 325 | Учёт по блокам (МФР) | `GET /objects/{object_id}/block-works` | Список: запланированные работы | реализована в V2 и опубликована | есть: blocks-screen.js:83, fact-form.js:28 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | чтение опубликовано |
| 329 | Учёт по блокам (МФР) | `GET /objects/{object_id}/block-works/{bw_id}` | Просмотр: запланированные работы | реализована в V2 и опубликована | есть: block-work-form.js:89, block-work-form.js:118 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | чтение опубликовано |
| 330 | Учёт по блокам (МФР) | `PATCH /objects/{object_id}/block-works/{bw_id}` | ЗР: базовый срок (начало / конец) — с отпечатком работы (конкуренция проверяется сервером) | реализована в V2 и опубликована | есть: block-work-form.js:117 · экран `blocks` | запись · настоящий backend: браузер и HTTP на настоящем backend (scripts/veri… | разрешена `block-work.plan`, поля: plan_start/plan_end/expected_rev, проверка т… | готово и опубликовано (шлюз: block-work.plan) |
| 331 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks` | Список: блоки объекта | реализована в V2 и опубликована | есть: blocks-screen.js:62, fact-journal-screen.js:37 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | чтение опубликовано |
| 338 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/geometry` | Параллелепипеды блоков для слоя «Блоки» в «Модели МФР» (Docs/TZ.md, «Геометрия блока») —… | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | чтение опубликовано |
| 339 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/planning-tracks` | Список: треки планирования | реализована в V2 и опубликована | есть: chess-flat-screen.js:69 · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | чтение опубликовано |
| 348 | Учёт по блокам (МФР) | `GET /objects/{object_id}/blocks/{block_id}/card` | Карточка: блоки объекта | реализована в V2 и опубликована | есть: workspace.js:235, workspace.js:235 · экран `ws-model` | чтение · настоящий backend: настоящий backend (копия обезличенной БД, вход по… | не ограничивается (чтение) | чтение опубликовано |
| 359 | Учёт по блокам (МФР) | `GET /objects/{object_id}/fact-journal` | Список: журнал факта | реализована в V2 и опубликована | есть: fact-journal-screen.js:52 · экран `fact-journal` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | чтение опубликовано |
| 360 | Учёт по блокам (МФР) | `GET /objects/{object_id}/grids` | Сетка осей для слоя «Оси» в «Модели МФР» — тем же правом, что сами элементы: сетка не отд… | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | чтение опубликовано |
| 365 | Учёт по блокам (МФР) | `GET /objects/{object_id}/plan-images` | Слой «Планы» «Модели МФР» (2026-09-02, app/pdf_plan_images.py): у каких этажей есть карти… | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `blocks` | чтение · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | чтение опубликовано |
| 366 | Учёт по блокам (МФР) | `GET /objects/{object_id}/plan-images/{level_id}.png` | Доступ — «Модель Revit» ИЛИ «Учёт по блокам», см. plan_images_list_endpoint. | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `blocks` | чтение (файл) · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | чтение опубликовано |

### picker — комплектовщик, контрагенты, договоры, спецификации, контракты, замена поставщика, обмен привязками

**К выполнению: 39**

| № | Раздел | Операция | Сценарий | Права | V1 | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 85 | Контрагенты | `POST /counterparties` | Контрагенты: создать контрагента (наименования, реквизиты, код, нормативы производительности) | «Справочник контрагентов»: изменение | окно «Контрагент» (`counterparty-edit-backdrop`) — app.js:18151 | только переход в V1 (экран `counterparties`, статус 5) | запись · — | разрешена `counterparty.create`, проверка тела; вне формы разрешённой строки — отключена… | нет формы: экран «Контрагенты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Привязка контракта к одному изделию, групповая плановая дата, «контракт по умолчанию» и режим «свёртка дублей» — V1» |
| 86 | Контрагенты | `GET /counterparties/full` | Вложенное дерево Контрагент -> Договоры -> Спецификации одним запросом — для каскадных селектов в ф… | администратор сервиса (условно) | окно «Контрагент» (`counterparty-edit-backdrop`); окно «Контрагенты» (`counterparties-backdrop`) — app.js:203… | только переход в V1 (экран `counterparties`, статус 5) | чтение · — | не ограничивается (чтение) | экран «Контрагенты» реализован в V2 модулем `counterparties`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: «Привязка контракта к одному изделию, групповая плановая дата, «контракт по умолчанию» и режим «свёртка дублей» — V1» |
| 87 | Контрагенты | `PATCH /counterparties/{counterparty_id}` | Контрагенты: правка карточки контрагента целиком (с проверкой версии записи) | «Справочник контрагентов»: изменение | окно «Контрагент» (`counterparty-edit-backdrop`) — app.js:18149 | только переход в V1 (экран `counterparties`, статус 5) | запись · — | разрешена `counterparty.update`, проверка тела; вне формы разрешённой строки — отключена… | нет формы: экран «Контрагенты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Привязка контракта к одному изделию, групповая плановая дата, «контракт по умолчанию» и режим «свёртка дублей» — V1» |
| 88 | Контрагенты | `GET /dictionaries/counterparty/candidates` | Кандидаты для замены записи справочника «Контрагент» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Контрагент» (`counterparty-edit-backdrop`); Действия › Справочники › Контрактация › Контрагенты (и др.)… | есть: delete-plan.js:69 · экран `counterparties` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69); в опубликованной нет: публикация |
| 89 | Контрагенты | `POST /dictionaries/counterparty/{key}/delete` | Удаление записи справочника с заменой ссылок «Контрагент» | «Удаление записей справочников с заменой ссылок»: изменение | окно «Удаление записи справочника» (`dict-delete-backdrop`) — app.js:11396 | есть: delete-plan.js:145 · экран `counterparties` | запись · не проверено: scripts/picker_verify/cp.mjs: предпросмотр, удале… | разрешена `counterparty.delete`, проверка тела; вне формы разрешённой строки — отключена… | разрешена шлюзом (counterparty.delete) и вызывается из V2 (delete-plan.js:145) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 92 | Договоры | `POST /agreements` | Создание: договоры — Новый договор — ОБЯЗАТЕЛЬНО на объект (2026-08-03). | «Договоры и спецификации»: изменение | окно «Контрагент» (`counterparty-edit-backdrop`) — app.js:18001 | только переход в V1 (экран `counterparties`, статус 5) | запись · — | разрешена `agreement.create`, проверка тела; вне формы разрешённой строки — отключена `co… | нет формы: экран «Контрагенты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Привязка контракта к одному изделию, групповая плановая дата, «контракт по умолчанию» и режим «свёртка дублей» — V1» |
| 93 | Договоры | `PATCH /agreements/{agreement_id}` | Договоры: правка договора (номер, дата, объект; с проверкой версии) | системная роль admin (проверка в коде; может допускать и «свою запись… | окно «Контрагент» (`counterparty-edit-backdrop`); окно «Контрагенты» (`counterparties-backdrop`) — app.js:178… | только переход в V1 (экран `counterparties`, статус 5) | запись · — | разрешена `agreement.update`, проверка тела; вне формы разрешённой строки — отключена `co… | нет формы: экран «Контрагенты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Привязка контракта к одному изделию, групповая плановая дата, «контракт по умолчанию» и режим «свёртка дублей» — V1» |
| 94 | Договоры | `GET /dictionaries/agreement/candidates` | Кандидаты для замены записи справочника «Договор» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Контрагент» (`counterparty-edit-backdrop`); Действия › Справочники › Контрактация › Контрагенты (и др.)… | есть: delete-plan.js:69 · экран `counterparties` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69); в опубликованной нет: публикация |
| 95 | Договоры | `POST /dictionaries/agreement/{key}/delete` | Удаление записи справочника с заменой ссылок «Договор» | «Удаление записей справочников с заменой ссылок»: изменение | окно «Удаление записи справочника» (`dict-delete-backdrop`) — app.js:11396 | есть: delete-plan.js:145 · экран `counterparties` | запись · не проверено: scripts/picker_verify/cp.mjs | разрешена `agreement.delete`, проверка тела; вне формы разрешённой строки — отключена `co… | разрешена шлюзом (agreement.delete) и вызывается из V2 (delete-plan.js:145) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 97 | Спецификации | `GET /dictionaries/specification/candidates` | Кандидаты для замены записи справочника «Спецификация» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Контрагент» (`counterparty-edit-backdrop`); Действия › Справочники › Контрактация › Контрагенты (и др.)… | есть: delete-plan.js:69 · экран `counterparties` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69); в опубликованной нет: публикация |
| 98 | Спецификации | `POST /dictionaries/specification/{key}/delete` | Удаление записи справочника с заменой ссылок «Спецификация» | «Удаление записей справочников с заменой ссылок»: изменение | окно «Удаление записи справочника» (`dict-delete-backdrop`) — app.js:11396 | есть: delete-plan.js:145 · экран `counterparties` | запись · не проверено: scripts/picker_verify/cp.mjs | разрешена `specification.delete`, проверка тела; вне формы разрешённой строки — отключена… | разрешена шлюзом (specification.delete) и вызывается из V2 (delete-plan.js:145) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 101 | Спецификации | `POST /specifications` | Спецификации: создать спецификацию договора | системная роль admin (проверка в коде; может допускать и «свою запись… | окно «Контрагент» (`counterparty-edit-backdrop`); окно «Контрагенты» (`counterparties-backdrop`) — app.js:179… | только переход в V1 (экран `counterparties`, статус 5) | запись · — | разрешена `specification.create`, проверка тела; вне формы разрешённой строки — отключена… | нет формы: экран «Контрагенты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Привязка контракта к одному изделию, групповая плановая дата, «контракт по умолчанию» и режим «свёртка дублей» — V1» |
| 102 | Спецификации | `PATCH /specifications/{specification_id}` | Спецификации: правка спецификации (номер, дата; с проверкой версии) | системная роль admin (проверка в коде; может допускать и «свою запись… | окно «Контрагент» (`counterparty-edit-backdrop`); окно «Контрагенты» (`counterparties-backdrop`) — app.js:178… | только переход в V1 (экран `counterparties`, статус 5) | запись · — | разрешена `specification.update`, проверка тела; вне формы разрешённой строки — отключена… | нет формы: экран «Контрагенты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Привязка контракта к одному изделию, групповая плановая дата, «контракт по умолчанию» и режим «свёртка дублей» — V1» |
| 104 | Контракты | `POST /contracts` | Контракты: создать контракт под спецификацией (тема, позиции, инциденты, нормативы) | системная роль admin (проверка в коде; может допускать и «свою запись… | окно «Контракт» (`contract-edit-backdrop`) — app.js:20455 | только переход в V1 (экран `counterparties`, статус 5) | запись · — | разрешена `contract.create`, проверка тела; вне формы разрешённой строки — отключена `cou… | нет формы: экран «Контрагенты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Привязка контракта к одному изделию, групповая плановая дата, «контракт по умолчанию» и режим «свёртка дублей» — V1» |
| 105 | Контракты | `GET /contracts/default-map` | Список: контракт по умолчанию по типу | «Контракт по умолчанию по типу изделия»: чтение | окно «Контракт» (`contract-edit-backdrop`); Действия › Справочники › Контрактация › Контракты — app.js:18690… | только переход в V1 (экран `contracts`, статус 5) | чтение · — | не ограничивается (чтение) | экран «Контракты» реализован в V2 модулем `contracts-list`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: «Создание и правка выполняются в карточке контрагента («Контрагенты»); групповые операции и «контракт по умолчанию» — V1» |
| 106 | Контракты | `PUT /contracts/default-map` | Сохранение: контракт по умолчанию по типу | «Контракт по умолчанию по типу изделия»: изменение | окно «Контракт» (`contract-edit-backdrop`); Действия › Справочники › Контрактация › Контракты — app.js:18709… | только переход в V1 (экран `contracts`, статус 5) | запись · — | отключена `counterparties.write` | нет формы: экран «Контракты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Создание и правка выполняются в карточке контрагента («Контрагенты»); групповые операции и «контракт по умолчанию» — V1» |
| 107 | Контракты | `GET /contracts/positions` | Позиции контрактов по ТИПУ изделия — все марки сразу, с планом, фактом и остатком по каждой. | администратор сервиса (условно) | общий код V1 (вызывается из многих мест) — app.js:8332 contractPositions() | есть: element-ops.js:124 · экран `contracts` | чтение · настоящий backend: настоящий backend (копия БД, вход формой): script… | не ограничивается (чтение) | подключено в этой ветке (element-ops.js:124); в опубликованной нет: публикация |
| 108 | Контракты | `PATCH /contracts/{contract_id}` | Контракты: правка контракта целиком — позиции и количество (не ниже привязанного), инциденты, архив… | системная роль admin (проверка в коде; может допускать и «свою запись… | окно «Контракт» (`contract-edit-backdrop`) — app.js:20453 | только переход в V1 (экран `counterparties`, статус 5) | запись · — | разрешена `contract.update`, проверка тела; вне формы разрешённой строки — отключена `cou… | нет формы: экран «Контрагенты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Привязка контракта к одному изделию, групповая плановая дата, «контракт по умолчанию» и режим «свёртка дублей» — V1» |
| 111 | Контракты | `POST /dictionaries/contract/{key}/delete` | Удаление записи справочника с заменой ссылок «Контракт» | «Удаление записей справочников с заменой ссылок»: изменение | окно «Удаление записи справочника» (`dict-delete-backdrop`) — app.js:11396 | есть: counterparties.js:1459, delete-plan.js:145 · экран `counterparties` | запись · не проверено: scripts/picker_verify/cp.mjs: замена, отказ страж… | разрешена `contract.delete`, проверка тела; вне формы разрешённой строки — отключена `cou… | разрешена шлюзом (contract.delete) и вызывается из V2 (counterparties.js:1459, delete-plan.js:145) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 118 | Документы | `POST /supplier-changes` | Создаёт ЧЕРНОВИК. Данные изделий при этом не меняются — документ пока только намерение; применяет е… | «Документ «Замена поставщика»», «Документ «Обмен привязками»»: измене… | окно «Документ» (`supplier-change-form-backdrop`) — app.js:19787 scdSaveDraft() | есть: supplier-docs.js:183 · экран `supplier-change` | запись · не проверено: scripts/picker_verify/docs.mjs на копии БД | разрешена `supplier-doc.create`, проверка тела | разрешена шлюзом (supplier-doc.create) и вызывается из V2 (supplier-docs.js:183) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 119 | Документы | `GET /supplier-changes/candidates` | Что и в каком количестве можно перенести ЗАМЕНОЙ ПОСТАВЩИКА: позиции (тип, марка) с перечнем самих… | раздел не определён: изменение | окно «Документ» (`supplier-change-form-backdrop`); Действия › Документы › Смена поставщика — app.js:18863 scd… | есть: supplier-docs.js:134, supplier-docs.js:235 · экран `supplier-change` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | подключено в этой ветке (supplier-docs.js:134, supplier-docs.js:235); в опубликованной нет: публикация |
| 120 | Документы | `GET /supplier-changes/contract-marks` | Марки, изделия которых стоят на ЭТОМ контракте, с количествами. | раздел не определён: изменение | окно «Документ» (`supplier-change-form-backdrop`); Действия › Документы › Смена поставщика — app.js:19016 scd… | есть: supplier-docs.js:142 · экран `supplier-change` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | подключено в этой ветке (supplier-docs.js:142); в опубликованной нет: публикация |
| 121 | Документы | `GET /supplier-changes/mark-contracts` | Контракты объекта, на которых стоят изделия ЭТОЙ марки, с количествами. | раздел не определён: изменение | окно «Документ» (`supplier-change-form-backdrop`); Действия › Документы › Смена поставщика — app.js:19061 scd… | есть: supplier-docs.js:151 · экран `supplier-change` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | подключено в этой ветке (supplier-docs.js:151); в опубликованной нет: публикация |
| 122 | Документы | `GET /supplier-changes/refs` | Справочные данные формы: контракты объекта. | раздел не определён: изменение | окно «Документы контрактации» (`supplier-change-backdrop`); окно «Документ» (`supplier-change-form-backdrop`)… | есть: supplier-docs.js:58 · экран `supplier-change` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | подключено в этой ветке (supplier-docs.js:58); в опубликованной нет: публикация |
| 123 | Документы | `GET /supplier-changes/swap-elements` | Изделия одной марки, стоящие на этом контракте, — материал для подбора рамкой на схеме. | раздел не определён: изменение | окно «Подбор изделий» (`scd-picker-backdrop`); окно «Документ» (`supplier-change-form-backdrop`) — app.js:195… | есть: supplier-docs.js:289 · экран `supplier-change` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | подключено в этой ветке (supplier-docs.js:289); в опубликованной нет: публикация |
| 124 | Документы | `DELETE /supplier-changes/{doc_id}` | Удалить можно только ЧЕРНОВИК: проведённый документ — основание, на которое ссылаются движения по и… | «Документ «Замена поставщика»», «Документ «Обмен привязками»»: измене… | окно «Документ» (`supplier-change-form-backdrop`) — app.js:19888 | есть: supplier-docs.js:206 · экран `supplier-change` | запись · не проверено: scripts/picker_verify/docs.mjs; scripts/verify_pi… | разрешена `supplier-doc.delete` | разрешена шлюзом (supplier-doc.delete) и вызывается из V2 (supplier-docs.js:206) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 125 | Документы | `GET /supplier-changes/{doc_id}` | Просмотр: документы контрактации | «Документ «Замена поставщика»», «Документ «Обмен привязками»»: чтение | окно «Документ» (`supplier-change-form-backdrop`); Действия › Документы › Смена поставщика — app.js:19665 scd… | есть: supplier-docs.js:102, supplier-docs.js:193 · экран `supplier-change` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | подключено в этой ветке (supplier-docs.js:102, supplier-docs.js:193); в опубликованной нет: публикация |
| 126 | Документы | `PATCH /supplier-changes/{doc_id}` | Правка ЧЕРНОВИКА. Проведённый документ не правится: его движения уже разошлись по изделиям, и подме… | «Документ «Замена поставщика»», «Документ «Обмен привязками»»: измене… | окно «Документ» (`supplier-change-form-backdrop`) — app.js:19783 scdSaveDraft() | есть: supplier-docs.js:183 · экран `supplier-change` | запись · не проверено: scripts/picker_verify/docs.mjs; scripts/verify_pi… | разрешена `supplier-doc.update`, проверка тела | разрешена шлюзом (supplier-doc.update) и вызывается из V2 (supplier-docs.js:183) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 127 | Документы | `POST /supplier-changes/{doc_id}/post` | Документы контрактации: ПРОВЕСТИ документ — переносит привязки изделий (всё или ничего; остаток про… | «Документ «Замена поставщика»», «Документ «Обмен привязками»»: измене… | окно «Документ» (`supplier-change-form-backdrop`) — app.js:19844 | есть: supplier-docs.js:258 · экран `supplier-change` | запись · не проверено: scripts/picker_verify/docs.mjs: перенос и SQL, от… | разрешена `supplier-doc.post`, поля: expected_version, проверка тела | разрешена шлюзом (supplier-doc.post) и вызывается из V2 (supplier-docs.js:258) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 128 | Документы | `POST /supplier-changes/{doc_id}/unpost` | Отмена проведения: изделия возвращаются в состояние до документа. | «Документ «Замена поставщика»», «Документ «Обмен привязками»»: измене… | окно «Документ» (`supplier-change-form-backdrop`) — app.js:19871 | есть: supplier-docs.js:258 · экран `supplier-change` | запись · не проверено: scripts/picker_verify/docs.mjs: возврат и SQL (вк… | разрешена `supplier-doc.unpost`, поля: expected_version, проверка тела | разрешена шлюзом (supplier-doc.unpost) и вызывается из V2 (supplier-docs.js:258) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 374 | График СМР | `POST /schedule-calc` | Создание: расчёт графика СМР | «График СМР: версии, исходные данные, расчёт, визуализация»: изменение | окно «График СМР» (`schedule-backdrop`) — app.js:29161 | только переход в V1 (экран `schedule`, статус 3) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «График СМР» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 375 | График СМР | `GET /schedule-calc/inputs` | Исходные данные расчёта: виды работ (темп и порядок) и поток. | «График СМР: версии, исходные данные, расчёт, визуализация»: чтение | Действия › Документы › График СМР — app.js:28986 loadScheduleInputs() | только переход в V1 (экран `schedule`, статус 3) | чтение · — | не ограничивается (чтение) | нет в V2: экран «График СМР» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 376 | График СМР | `PUT /schedule-calc/inputs` | Сохранение: исходные данные расчёта — Замена обеих таблиц целиком. | «График СМР: версии, исходные данные, расчёт, визуализация»: изменение | окно «График СМР» (`schedule-backdrop`) — app.js:29135 | только переход в V1 (экран `schedule`, статус 3) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «График СМР» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 377 | График СМР | `POST /schedule-calc/inputs/parse` | Разобрать файл обработки и вернуть значения В ФОРМУ, не записывая. | «График СМР: версии, исходные данные, расчёт, визуализация»: изменение | окно «График СМР» (`schedule-backdrop`) — app.js:29094 | только переход в V1 (экран `schedule`, статус 3) | разбор файла · — | нет строки (запрещено по умолчанию) | нет формы: экран «График СМР» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 380 | График СМР | `POST /schedule-versions/deviation` | Создание: отклонение графика | «График СМР: версии, исходные данные, расчёт, визуализация»: чтение | окно «График СМР» (`schedule-backdrop`); Действия › Документы › График СМР — app.js:23136 loadSidebarReports() | только переход в V1 (экран `schedule`, статус 3) | чтение (POST) · — | не ограничивается (чтение) | нет в V2: экран «График СМР» лишь ведёт в V1; подключить отчёт/выгрузку в экран. Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 381 | График СМР | `GET /schedule-versions/gantt` | Дерево для диаграммы Ганта. version_id не задан — текущий прогноз. | «График СМР: версии, исходные данные, расчёт, визуализация»: чтение | Действия › Документы › График СМР — app.js:29249 loadScheduleGantt() | только переход в V1 (экран `schedule`, статус 3) | чтение · — | не ограничивается (чтение) | нет в V2: экран «График СМР» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 382 | График СМР | `GET /schedule-versions/gantt.pdf` | Выгрузка в PDF: диаграмма Ганта | «График СМР: версии, исходные данные, расчёт, визуализация»: чтение | окно «График СМР» (`schedule-backdrop`) — app.js:29642 downloadGantt() | только переход в V1 (экран `schedule`, статус 3) | чтение (файл) · — | не ограничивается (чтение) | нет в V2: экран «График СМР» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 383 | График СМР | `GET /schedule-versions/gantt.xlsx` | Выгрузка в XLSX: диаграмма Ганта | «График СМР: версии, исходные данные, расчёт, визуализация»: чтение | окно «График СМР» (`schedule-backdrop`) — app.js:29642 downloadGantt() | только переход в V1 (экран `schedule`, статус 3) | чтение (файл) · — | не ограничивается (чтение) | нет в V2: экран «График СМР» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 384 | График СМР | `DELETE /schedule-versions/{version_id}` | Удаление версии. Базовую удалить нельзя: на ней держится сравнение, и «отклонение неизвестно от чег… | «График СМР: версии, исходные данные, расчёт, визуализация»: изменени… | окно «График СМР» (`schedule-backdrop`); Действия › Документы › График СМР — app.js:28969 loadScheduleVersion… | только переход в V1 (экран `schedule`, статус 3) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «График СМР» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области picker и проверки (см. «Перечень проверок»). Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |

**Уже в V2 и прочее: 15**

| № | Раздел | Операция | Сценарий | Состояние | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Рабочие места | `GET /allocation-state` | Текущее состояние ВСЕЙ пачки изделий — для сверки после неопределённого исхода распределе… | реализована в V2 и опубликована | есть: alloc-verify.js:40 · экран `ws-picker` | чтение · настоящий backend: рабочее место в V2: сцена V1 в кадре только для ч… | не ограничивается (чтение) | чтение опубликовано; экран «АРМ комплектовщика» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 4 | Рабочие места | `POST /contracts/{contract_id}/allocations` | Комплектовщик: распределение изделий одной позиции на контракт — одна пачка (запланирован… | реализована в V2 и опубликована | есть: workspace.js:520 · экран `ws-picker` | запись · настоящий backend: проверено на копии БД по HTTP (scripts/verify_all… | разрешена `element.allocate`, проверка тела; вне формы разрешённой строки — отк… | готово и опубликовано (шлюз: element.allocate); поля/тело вне разрешённой формы отклоняет строка counterparties.write (операции вне перечня разрешённых выше не… |
| 84 | Контрагенты | `GET /counterparties` | Список: контрагенты | реализована в V2 и опубликована | есть: contracts-list.js:67, counterparties.js:437 · экран `contracts` | чтение · настоящий backend: настоящий backend (копия БД, вход формой): script… | не ограничивается (чтение) | чтение опубликовано |
| 90 | Контрагенты | `GET /dictionaries/counterparty/{key}/delete-plan` | План последствий удаления записи справочника «Контрагент» | реализована в V2 и опубликована | есть: delete-plan.js:148, delete-plan.js:159 · экран `counterparties` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | чтение опубликовано |
| 91 | Договоры | `GET /agreements` | Список: договоры | реализована в V2 и опубликована | есть: contracts-list.js:32, counterparties.js:574 · экран `contracts` | чтение · настоящий backend: настоящий backend (копия БД, вход формой): script… | не ограничивается (чтение) | чтение опубликовано |
| 96 | Договоры | `GET /dictionaries/agreement/{key}/delete-plan` | План последствий удаления записи справочника «Договор» | реализована в V2 и опубликована | есть: delete-plan.js:148, delete-plan.js:159 · экран `counterparties` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | чтение опубликовано |
| 99 | Спецификации | `GET /dictionaries/specification/{key}/delete-plan` | План последствий удаления записи справочника «Спецификация» | реализована в V2 и опубликована | есть: delete-plan.js:148, delete-plan.js:159 · экран `counterparties` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | чтение опубликовано |
| 100 | Спецификации | `GET /specifications` | Список: спецификации | реализована в V2 и опубликована | есть: contracts-list.js:33, counterparties.js:575 · экран `contracts` | чтение · настоящий backend: настоящий backend (копия БД, вход формой): script… | не ограничивается (чтение) | чтение опубликовано |
| 103 | Контракты | `GET /contracts` | Список: контракты | реализована в V2 и опубликована | есть: contracts-list.js:22, counterparties.js:576 · экран `contracts` | чтение · настоящий backend: настоящий backend (копия БД, вход формой): script… | не ограничивается (чтение) | чтение опубликовано |
| 109 | Контракты | `GET /contracts/{contract_id}/elements` | Развёрнутый вид контракта (см. Docs/backlog.md, "Контрактация 2.0", п.3) — одна строка на… | реализована в V2 и опубликована | есть: counterparties.js:2554 · экран `counterparties` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | чтение опубликовано |
| 110 | Контракты | `GET /dictionaries/contract/candidates` | Кандидаты для замены записи справочника «Контракт» | реализована в V2 и опубликована | есть: counterparties.js:2123, delete-plan.js:69 · экран `counterparties` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | чтение опубликовано |
| 112 | Контракты | `GET /dictionaries/contract/{key}/delete-plan` | План последствий удаления записи справочника «Контракт» | реализована в V2 и опубликована | есть: counterparties.js:1473, counterparties.js:2108 · экран `counterparties` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | чтение опубликовано |
| 117 | Документы | `GET /supplier-changes` | Список: документы контрактации | реализована в V2 и опубликована | есть: supplier-docs.js:53 · экран `supplier-change` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | чтение опубликовано |
| 378 | График СМР | `GET /schedule-versions` | Список: версии графика СМР | реализована в V2 и опубликована | есть: read-screen.js:205 · экран `schedule` | чтение · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «График СМР» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 379 | График СМР | `GET /schedule-versions/deviation` | Список: отклонение графика | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `schedule`) | чтение · — | не ограничивается (чтение) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |

### admin — пользователи, роли, доступы, пароли, сеансы, проекты и объекты, справочники, настройки, резервные копии, служебные

**К выполнению: 92**

| № | Раздел | Операция | Сценарий | Права | V1 | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 32 | Пользователи и роли | `POST /ldap-search` | Отвечает 200 и при неудаче: это подсказка в форме, а не операция — администратору нужна причина, а… | «Доменная авторизация»: чтение | окно «Найти пользователя в домене» (`ldap-search-backdrop`) — app.js:17557 runDomainSearch() | есть: users-access.js:879 · экран `users-access` | чтение (POST) · настоящий backend: раздел «Пользователи»: настоящий backend и вход (… | не ограничивается (чтение) | подключено в этой ветке (users-access.js:879); в опубликованной нет: публикация |
| 34 | Пользователи и роли | `PUT /ldap-settings` | Доменная авторизация: сохранить настройки (с проверкой «устарело» на клиенте) | «Доменная авторизация»: изменение | окно «Доменная авторизация» (`ldap-backdrop`) — app.js:17602 | есть: admin-service.js:279 · экран `ldap` | запись · не проверено: HTTP+браузер: успех и перезагрузка, валидация 422… | разрешена `ldap.save`, поля: enabled/host/port/use_ssl/start_tls/verify_certificate/login… | разрешена шлюзом (ldap.save) и вызывается из V2 (admin-service.js:279) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 35 | Пользователи и роли | `POST /ldap-settings/test` | Пробная привязка. Отвечает 200 и при неудаче: это диагностика, а не попытка входа, и администратору… | «Доменная авторизация»: чтение | окно «Доменная авторизация» (`ldap-backdrop`) — app.js:17628 | есть: admin-service.js:296 · экран `ldap` | запись · не проверено: HTTP+браузер: недоступный сервер — причина показа… | разрешена `ldap.test`, поля: login/password/config | разрешена шлюзом (ldap.test) и вызывается из V2 (admin-service.js:296) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 36 | Пользователи и роли | `POST /me/change-password` | Смена СВОЕГО пароля с подтверждением ТЕКУЩЕГО. | любой вошедший пользователь | окно «Сменить пароль» (`change-password-backdrop`) — app.js:1128 | есть: password-form.js:46 · экран `change-password` | запись · не проверено: HTTP+браузер на тестовом пользователе копии: неве… | разрешена `me.password`, поля: current_password/new_password | разрешена шлюзом (me.password) и вызывается из V2 (password-form.js:46) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 38 | Пользователи и роли | `POST /me/sessions/close-others` | «Завершить все, кроме текущего» — одно нажатие вместо перебора списка: именно это делают, заподозри… | любой вошедший пользователь | окно «Мои сеансы» (`sessions-backdrop`) — app.js:1282 | есть: sessions-edit.js:96 · экран `sessions` | запись · не проверено: HTTP+браузер: остальные завершены, текущий жив | разрешена `sessions.own.close-others` | разрешена шлюзом (sessions.own.close-others) и вызывается из V2 (sessions-edit.js:96) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 39 | Пользователи и роли | `DELETE /me/sessions/{public_id}` | Оборвать свой сеанс, в том числе ТЕКУЩИЙ (это просто выход). | любой вошедший пользователь | окно «Мои сеансы» (`sessions-backdrop`); Действия › Настройки › Общие › Мои сеансы — app.js:1215 renderSessio… | есть: sessions-edit.js:80 · экран `sessions` | запись · не проверено: HTTP+браузер: свой другой сеанс завершается, повт… | разрешена `sessions.own.end` | разрешена шлюзом (sessions.own.end) и вызывается из V2 (sessions-edit.js:80) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 41 | Пользователи и роли | `POST /roles` | Создание: роли — Новая роль заводится ПУСТОЙ — без единого разрешения. | «Настройка ролей и разрешений»: изменение | окно «Пользователи и доступ» (`users-backdrop`) — app.js:16340 | есть: users-access.js:1621 · экран `users-access` | запись · не проверено: HTTP+браузер: успех, дубль 409, пустое 400, 403 | разрешена `roles.create`, поля: name | разрешена шлюзом (roles.create) и вызывается из V2 (users-access.js:1621) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 42 | Пользователи и роли | `PUT /roles/features` | Сохранение: матрица прав ролей — Ячейки матрицы: роль × раздел → Нет/Чтение/Изменение. | «Настройка ролей и разрешений»: изменение | окно «Пользователи и доступ» (`users-backdrop`) — app.js:16360 | есть: users-access.js:1488 · экран `users-access` | запись · не проверено: HTTP+браузер: успех, устаревшее «было» 409 без за… | разрешена `roles.features`, поля: items, проверка тела | разрешена шлюзом (roles.features) и вызывается из V2 (users-access.js:1488) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 43 | Пользователи и роли | `PUT /roles/order` | Сохранение: порядок ролей — Порядок колонок в матрице. | «Настройка ролей и разрешений»: изменение | точка входа не установлена статически — app.js:16299 | есть: users-access.js:1658 · экран `users-access` | запись · не проверено: HTTP+браузер: успех, неполный список 400 | разрешена `roles.order`, поля: keys | разрешена шлюзом (roles.order) и вызывается из V2 (users-access.js:1658) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 44 | Пользователи и роли | `DELETE /roles/{key}` | Удаление: роли — Удаление роли: её разрешения и все её выдачи исчезают. | «Настройка ролей и разрешений»: изменение | точка входа не установлена статически — app.js:16320 | есть: users-access.js:1780 · экран `users-access` | запись · не проверено: HTTP+браузер: успех по актуальному плану, устарев… | разрешена `roles.delete` | разрешена шлюзом (roles.delete) и вызывается из V2 (users-access.js:1780) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 45 | Пользователи и роли | `PATCH /roles/{key}` | Переименование. Ключ НЕ меняется: за него держатся выданные доступы и разрешения, а смена ключа рад… | «Настройка ролей и разрешений»: изменение | точка входа не установлена статически — app.js:16307 | только переход в V1 (экран `users-access`, статус 5) | запись · — | разрешена `roles.rename`, поля: name/expected_name | нет формы: экран «Пользователи и доступ» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: ««Зайти под пользователем» открывает вкладку в текущем интерфейсе (только он подставляет токен во все запросы; в V2 рабо…» |
| 46 | Пользователи и роли | `GET /roles/{key}/delete-plan` | Что произойдёт при удалении роли — до того, как оно произойдёт. | «Настройка ролей и разрешений»: чтение | точка входа не установлена статически — app.js:16312 | только переход в V1 (экран `users-access`, статус 5) | чтение · — | не ограничивается (чтение) | экран «Пользователи и доступ» реализован в V2 модулем `users-access`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: ««Зайти под пользователем» открывает вкладку в текущем интерфейсе (только он подставляет токен во все запросы; в V2 р… |
| 47 | Пользователи и роли | `GET /sessions` | Кто сейчас в системе. Текущий сеанс администратора помечен — чтобы он не оборвал сам себя, не поняв… | «Сеансы пользователей»: чтение | окно «Мои сеансы» (`sessions-backdrop`); Действия › Настройки › Общие › Мои сеансы — app.js:1182 renderSessio… | есть: sessions-edit.js:129 · экран `sessions` | чтение · настоящий backend: сеансы: настоящий backend и вход (real_auth_serve… | не ограничивается (чтение) | подключено в этой ветке (sessions-edit.js:129); в опубликованной нет: публикация |
| 48 | Пользователи и роли | `POST /sessions/close-others` | Оборвать ВСЕ сеансы всех пользователей, кроме своего текущего. | «Сеансы пользователей»: изменение | окно «Мои сеансы» (`sessions-backdrop`) — app.js:1283 | есть: sessions-edit.js:161 · экран `sessions` | запись · не проверено: HTTP+браузер: с подтверждением и числом; свой сеа… | разрешена `sessions.admin.close-others` | разрешена шлюзом (sessions.admin.close-others) и вызывается из V2 (sessions-edit.js:161) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 49 | Пользователи и роли | `DELETE /sessions/{public_id}` | Удаление: сеансы — Оборвать ОДИН любой сеанс по отпечатку. | «Сеансы пользователей»: изменение | окно «Мои сеансы» (`sessions-backdrop`); Действия › Настройки › Общие › Мои сеансы — app.js:1216 renderSessio… | есть: sessions-edit.js:155, users-access.js:969 · экран `sessions` | запись · не проверено: HTTP+браузер: успех, повтор 404, 403 у не-админис… | разрешена `sessions.admin.end` | разрешена шлюзом (sessions.admin.end) и вызывается из V2 (sessions-edit.js:155, users-access.js:969) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 51 | Пользователи и роли | `POST /users` | Пользователи: создание учётной записи | «Пользователи и выдача доступов»: изменение | окно «Пользователь» (`user-edit-backdrop`) — app.js:15806 | есть: users-access.js:501 · экран `users-access` | запись · не проверено: HTTP+браузер: успех и БД, 403 у user2/user4, дубл… | разрешена `users.create`, поля: last_name/first_name/domain_login/role | разрешена шлюзом (users.create) и вызывается из V2 (users-access.js:501) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 52 | Пользователи и роли | `POST /users/access-bulk` | Групповая правка доступа: всё или ничего. `dry_run` — только предпросмотр (без записи и без журнала… | «Пользователи и выдача доступов»: изменение; администратор сервиса (у… | не вызывается из V1 | есть: access-view.js:183, access-view.js:196 · экран `access-matrix` | запись · не проверено: HTTP+браузер: предпросмотр без записи, применение… | разрешена `users.access-bulk`, поля: changes/dry_run, проверка тела | разрешена шлюзом (users.access-bulk) и вызывается из V2 (access-view.js:183, access-view.js:196) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 54 | Пользователи и роли | `PATCH /users/{user_id}` | Пользователи: правка карточки (с проверкой «запись устарела») | «Пользователи и выдача доступов»: изменение; администратор сервиса (у… | окно «Пользователь» (`user-edit-backdrop`) — app.js:15804, app.js:16427 | есть: users-access.js:669 · экран `users-access` | запись · не проверено: HTTP+браузер: успех, устаревшая версия 409 без за… | разрешена `users.update`, поля: last_name/first_name/patronymic/position/department/domai… | разрешена шлюзом (users.update) и вызывается из V2 (users-access.js:669) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 56 | Пользователи и роли | `PUT /users/{user_id}/access` | Доступ: пользователи — Заменяет ВЕСЬ набор грантов пользователя присланным. | «Пользователи и выдача доступов»: изменение; «Пользователи и выдача д… | окно «Пользователь» (`user-edit-backdrop`) — app.js:15812, app.js:16437 | есть: users-access.js:1247 · экран `users-access` | запись · не проверено: HTTP+браузер: успех и перезагрузка, валидация 400… | разрешена `users.access`, поля: grants/expected_grants, проверка тела | разрешена шлюзом (users.access) и вызывается из V2 (users-access.js:1247) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 58 | Пользователи и роли | `POST /users/{user_id}/impersonate` | Зайти под пользователем (отладочный сеанс; вкладка открывается в текущем интерфейсе) | «Пользователи и выдача доступов»: изменение | окно «Пользователь» (`user-edit-backdrop`) — app.js:15404 startImpersonation() | есть: users-access.js:989 · экран `users-access` | запись · не проверено: HTTP+браузер: токен, заголовок X-Impersonate-Toke… | разрешена `users.impersonate` | разрешена шлюзом (users.impersonate) и вызывается из V2 (users-access.js:989) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 60 | Пользователи и роли | `DELETE /users/{user_id}/sessions` | Оборвать ВСЕ сеансы пользователя — то, что делают, когда человек уволился или потерял ноутбук. | «Сеансы пользователей»: изменение | окно «Мои сеансы» (`sessions-backdrop`) — app.js:1284 | есть: users-access.js:975 · экран `users-access` | запись · не проверено: HTTP+браузер: успех, пароль цел, 403, 404 | разрешена `sessions.admin.user-all` | разрешена шлюзом (sessions.admin.user-all) и вызывается из V2 (users-access.js:975) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 61 | Пользователи и роли | `GET /users/{user_id}/sessions` | Активные сеансы ЛЮБОГО пользователя — для администратора сервиса. | «Сеансы пользователей»: чтение | окно «Мои сеансы» (`sessions-backdrop`); Действия › Настройки › Общие › Мои сеансы — app.js:1183 renderSessio… | есть: users-access.js:940 · экран `users-access` | чтение · настоящий backend: раздел «Пользователи»: настоящий backend и вход (… | не ограничивается (чтение) | подключено в этой ветке (users-access.js:940); в опубликованной нет: публикация |
| 62 | Пользователи и роли | `POST /users/{user_id}/set-password` | Пароль пользователя: задать или заблокировать вход (пустой пароль — только чужому) | системная роль admin (проверка в коде; может допускать и «свою запись… | окно «Задать пароль» (`user-password-backdrop`) — app.js:15835 | есть: users-access.js:706, users-access.js:1095 · экран `users-access` | запись · не проверено: HTTP+браузер на тестовых пользователях копии: пол… | разрешена `users.password`, поля: password/must_change_password, проверка тела | разрешена шлюзом (users.password) и вызывается из V2 (users-access.js:706, users-access.js:1095) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 63 | Проекты и объекты | `GET /dictionaries/object/candidates` | Кандидаты для замены записи справочника «Объект» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Проекты и объекты» (`catalog-backdrop`) (и др.) — app.js:11244 ddBuildNode() | есть: delete-plan.js:69 · экран `projects-objects` | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69); в опубликованной нет: публикация |
| 64 | Проекты и объекты | `POST /dictionaries/object/{key}/delete` | Удаление записи справочника с заменой ссылок «Объект» | «Удаление записей справочников с заменой ссылок»: изменение | окно «Удаление записи справочника» (`dict-delete-backdrop`) — app.js:11396 | есть: delete-plan.js:145, projects-objects.js:950 · экран `projects-objects` | запись · не проверено: HTTP+браузер: удаление пустого со всем каскадом,… | разрешена `objects.delete`, поля: replacements/mode; вне формы разрешённой строки — отклю… | разрешена шлюзом (objects.delete) и вызывается из V2 (delete-plan.js:145, projects-objects.js:950) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 66 | Проекты и объекты | `GET /dictionaries/project/candidates` | Кандидаты для замены записи справочника «Проект» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Проекты и объекты» (`catalog-backdrop`) (и др.) — app.js:11244 ddBuildNode() | есть: delete-plan.js:69 · экран `projects-objects` | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69); в опубликованной нет: публикация |
| 67 | Проекты и объекты | `POST /dictionaries/project/{key}/delete` | Удаление записи справочника с заменой ссылок «Проект» | «Удаление записей справочников с заменой ссылок»: изменение | окно «Удаление записи справочника» (`dict-delete-backdrop`) — app.js:11396 | есть: delete-plan.js:145, projects-objects.js:950 · экран `projects-objects` | запись · не проверено: HTTP+браузер: удаление пустого, отказ для проекта… | разрешена `projects.delete`, поля: replacements/mode; вне формы разрешённой строки — откл… | разрешена шлюзом (projects.delete) и вызывается из V2 (delete-plan.js:145, projects-objects.js:950) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 71 | Проекты и объекты | `PUT /map/online-tiles` | Включить или выключить подложку из интернета. | «Карта проектов»: изменение | точка входа не установлена статически — app.js:13025 | есть: admin-service.js:333 · экран `map-admin` | запись · не проверено: HTTP+браузер: успех, SQL, перезагрузка, 403 | разрешена `map.online`, поля: enabled | разрешена шлюзом (map.online) и вызывается из V2 (admin-service.js:333) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 72 | Проекты и объекты | `POST /map/tiles/upload` | Загрузка файла: tiles — Принять файл подложки PMTiles, выбранный в браузере. | «Карта проектов»: изменение | точка входа не установлена статически — app.js:13052 | есть: admin-service.js:348 · экран `map-admin` | запись · не проверено: HTTP+браузер: успех, не-PMTiles отказ без следа н… | разрешена `map.upload` | разрешена шлюзом (map.upload) и вызывается из V2 (admin-service.js:348) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 73 | Проекты и объекты | `GET /map/tiles/{name}` | Файл подложки. Читается браузером по диапазонам байт — Starlette отдаёт Range сам, своего кода это… | любой вошедший пользователь | точка входа не установлена статически — плитки карты: адрес формирует библиотека карты по настройкам GET /map… | только переход в V1 (экран `map`, статус 3) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Карта проектов» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Карта MapLibre остаётся в V1; здесь — таблица тех же данных» |
| 75 | Проекты и объекты | `POST /objects` | Создание: объекты — Завести объект ЗАРАНЕЕ, до загрузки чертежа. | «Справочники «Проекты» и «Объекты»»: изменение | окно «Проекты и объекты» (`catalog-backdrop`) — app.js:12614 saveCatalog() | есть: projects-objects.js:415 · экран `projects-objects` | запись · не проверено: HTTP+браузер: успех и перезагрузка, пустое/дубль/… | разрешена `objects.create`, поля: name/project_id/status/description/kind/smu_id/smu_dire… | разрешена шлюзом (objects.create) и вызывается из V2 (projects-objects.js:415) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 76 | Проекты и объекты | `PATCH /objects/{object_id}` | Правка реквизитов объекта: наименование, проект, тип учёта, статус, адрес и координаты. | «Справочники «Проекты» и «Объекты»»: изменение | окно «Проекты и объекты» (`catalog-backdrop`) — app.js:12619 saveCatalog() | есть: projects-objects.js:415 · экран `projects-objects` | запись · не проверено: HTTP+браузер: успех, устаревшая версия 409 без за… | разрешена `objects.update`, поля: name/project_id/status/description/kind/smu_id/smu_dire… | разрешена шлюзом (objects.update) и вызывается из V2 (projects-objects.js:415) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 77 | Проекты и объекты | `GET /objects/{object_id}/avatar` | Отдаёт саму картинку — единственное место, где вложение возвращается СВОИМ content_type для показа… | «Вложения: просмотр и добавление»: чтение | окно «Проекты и объекты» (`catalog-backdrop`); Действия › Справочники › Проект и объект › Проекты и объекты —… | только переход в V1 (экран `projects-objects`, статус 5) | чтение · — | не ограничивается (чтение) | экран «Проекты и объекты» реализован в V2 модулем `projects-objects`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: «Непустой объект/проект не удаляется (правило V1: за объектом стоят изделия и история); загрузка справочника объектов… |
| 78 | Проекты и объекты | `PUT /objects/{object_id}/avatar` | Превью: объекты — Назначить или снять превью объекта в дереве справочника. | «Вложения: просмотр и добавление»: изменение | окно «Проекты и объекты» (`catalog-backdrop`); Действия › Справочники › Проект и объект › Проекты и объекты —… | есть: projects-objects.js:795 · экран `projects-objects` | запись · не проверено: HTTP+браузер: назначить, снять, не изображение 40… | разрешена `objects.avatar`, поля: attachment_id | разрешена шлюзом (objects.avatar) и вызывается из V2 (projects-objects.js:795) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 80 | Проекты и объекты | `POST /projects` | Проекты: создание | «Справочники «Проекты» и «Объекты»»: изменение; администратор сервиса… | окно «Проекты и объекты» (`catalog-backdrop`) — app.js:12614 saveCatalog() | есть: projects-objects.js:415 · экран `projects-objects` | запись · не проверено: HTTP+браузер: успех и перезагрузка, пустое/дубль,… | разрешена `projects.create`, поля: name/status/description/address/address_note/address_c… | разрешена шлюзом (projects.create) и вызывается из V2 (projects-objects.js:415) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 83 | Проекты и объекты | `PATCH /projects/{project_id}` | Изменение: проекты — Правка реквизитов проекта. | «Справочники «Проекты» и «Объекты»»: изменение; администратор сервиса… | окно «Проекты и объекты» (`catalog-backdrop`) — app.js:12619 saveCatalog() | есть: projects-objects.js:415 · экран `projects-objects` | запись · не проверено: HTTP+браузер: успех, устаревшая версия 409 без за… | разрешена `projects.update`, поля: name/status/description/address/address_note/address_c… | разрешена шлюзом (projects.update) и вызывается из V2 (projects-objects.js:415) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 114 | Документы | `POST /attachments` | Вложения проекта и объекта: приложить файл | «Вложения: просмотр и добавление»: изменение | окно «Проекты и объекты» (`catalog-backdrop`); Действия › Справочники › Проект и объект › Проекты и объекты —… | есть: projects-objects.js:846 · экран `projects-objects` | запись · не проверено: HTTP+браузер: успех и перезагрузка, слишком больш… | разрешена `attachments.add` | разрешена шлюзом (attachments.add) и вызывается из V2 (projects-objects.js:846) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 115 | Документы | `DELETE /attachments/{attachment_id}` | Вложения проекта и объекта: удалить | «Вложения: удаление»: изменение | окно «Проекты и объекты» (`catalog-backdrop`) — app.js:7809 перерисовать() | есть: projects-objects.js:816 · экран `projects-objects` | запись · не проверено: HTTP+браузер: удаление и файл на диске, повтор 40… | разрешена `attachments.delete` | разрешена шлюзом (attachments.delete) и вызывается из V2 (projects-objects.js:816) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 130 | Справочники | `POST /address/fetch` | Скачать классификатор с сайта ФНС и распаковать — одной кнопкой. | «Загрузка адресного классификатора КЛАДР»: изменение | окно «Адресный классификатор» (`address-backdrop`) — app.js:13225, app.js:13250 | только переход в V1 (экран `address-classifier`, статус 3) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Адресный классификатор» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «ОСТАЁТСЯ В V1: загрузка классификатора КЛАДР — многошаговая операция (скачать/распаковать архив на сервере, выбрать рег…» |
| 132 | Справочники | `POST /address/load` | Загрузка: адресный классификатор | «Загрузка адресного классификатора КЛАДР»: изменение | окно «Адресный классификатор» (`address-backdrop`) — app.js:13286 | только переход в V1 (экран `address-classifier`, статус 3) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Адресный классификатор» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «ОСТАЁТСЯ В V1: загрузка классификатора КЛАДР — многошаговая операция (скачать/распаковать архив на сервере, выбрать рег…» |
| 133 | Справочники | `GET /address/load-status` | Ход загрузки: адресный классификатор — Прогресс фоновой загрузки. | «Загрузка адресного классификатора КЛАДР»: изменение | окно «Адресный классификатор» (`address-backdrop`); Действия › Администрирование › Адресный классификатор — a… | только переход в V1 (экран `address-classifier`, статус 3) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Адресный классификатор» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «ОСТАЁТСЯ В V1: загрузка классификатора КЛАДР — многошаговая операция (скачать/распаковать архив на сервере, выбрать рег…» |
| 134 | Справочники | `GET /address/regions-in-file` | Регионы в положенном KLADR.DBF — чтобы человек отмечал их по названиям, а не по двузначным кодам. | «Загрузка адресного классификатора КЛАДР»: изменение | окно «Адресный классификатор» (`address-backdrop`); Действия › Администрирование › Адресный классификатор — a… | только переход в V1 (экран `address-classifier`, статус 3) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Адресный классификатор» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «ОСТАЁТСЯ В V1: загрузка классификатора КЛАДР — многошаговая операция (скачать/распаковать архив на сервере, выбрать рег…» |
| 139 | Справочники | `POST /address/unpack` | Распаковка: адресный классификатор | «Загрузка адресного классификатора КЛАДР»: изменение | окно «Адресный классификатор» (`address-backdrop`); Действия › Администрирование › Адресный классификатор — a… | только переход в V1 (экран `address-classifier`, статус 3) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Адресный классификатор» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «ОСТАЁТСЯ В V1: загрузка классификатора КЛАДР — многошаговая операция (скачать/распаковать архив на сервере, выбрать рег…» |
| 140 | Справочники | `POST /address/upload` | Загрузка файла: адресный классификатор — Принять файл классификатора, выбранный в браузере. | «Загрузка адресного классификатора КЛАДР»: изменение | точка входа не установлена статически — app.js:13244 | только переход в V1 (экран `address-classifier`, статус 3) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Адресный классификатор» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «ОСТАЁТСЯ В V1: загрузка классификатора КЛАДР — многошаговая операция (скачать/распаковать архив на сервере, выбрать рег…» |
| 144 | Справочники | `GET /dictionaries/individual/candidates` | Кандидаты для замены записи справочника «Физлицо» | «Удаление записей справочников с заменой ссылок»: чтение | общий код V1 (вызывается из многих мест) — app.js:11244 ddBuildNode() | есть: delete-plan.js:69, dict-edit.js:184 · экран `dict-individuals` | чтение · настоящий backend: справочник физлиц: настоящий backend и вход (real… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69, dict-edit.js:184); в опубликованной нет: публикация |
| 145 | Справочники | `POST /dictionaries/individual/{key}/delete` | Удаление записи справочника с заменой ссылок «Физлицо» | «Удаление записей справочников с заменой ссылок»: изменение | окно «Удаление записи справочника» (`dict-delete-backdrop`) — app.js:11396 | есть: delete-plan.js:145, dict-edit.js:192 · экран `dict-individuals` | запись · не проверено: HTTP+браузер: удаление неиспользуемой, отказ для… | разрешена `individuals.delete`, поля: replacements/mode; вне формы разрешённой строки — о… | разрешена шлюзом (individuals.delete) и вызывается из V2 (delete-plan.js:145, dict-edit.js:192) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 147 | Справочники | `GET /dictionaries/mark/candidates` | Кандидаты для замены записи справочника «Марка» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Удаление записи справочника» (`dict-delete-backdrop`); окно «Справочник» (`simple-catalog-backdrop`) (и… | есть: delete-plan.js:69 · экран `dict-smu` | чтение · настоящий backend: справочник СМУ: настоящий backend и вход (real_au… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69); в опубликованной нет: публикация |
| 148 | Справочники | `POST /dictionaries/mark/{key}/delete` | Удаление записи справочника с заменой ссылок «Марка» | «Удаление записей справочников с заменой ссылок»: изменение | окно «Удаление записи справочника» (`dict-delete-backdrop`) — app.js:11396 | форма есть: delete-plan.js:145 · экран `dict-smu` | запись · настоящий backend: справочник СМУ: настоящий backend и вход (real_au… | отключена `counterparties.write` | форма есть (delete-plan.js:145), шлюз отключён (counterparties.write: операции вне перечня разрешённых выше не проверялись в новом интерфейсе); нужны проверки (см. «Перечень проверок») и строка `allowed: true` в блоке области admin |
| 149 | Справочники | `GET /dictionaries/mark/{key}/delete-plan` | План последствий удаления записи справочника «Марка» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Удаление записи справочника» (`dict-delete-backdrop`); окно «Справочник» (`simple-catalog-backdrop`) (и… | есть: delete-plan.js:148, delete-plan.js:159 · экран `dict-smu` | чтение · настоящий backend: справочник СМУ: настоящий backend и вход (real_au… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:148, delete-plan.js:159); в опубликованной нет: публикация |
| 150 | Справочники | `GET /dictionaries/mark_prefix/candidates` | Кандидаты для замены записи справочника «Префикс марки» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Справочник префиксов марок» (`mark-type-prefixes-backdrop`); Действия › Справочники › Классификация › П… | есть: delete-plan.js:69 · экран `mark-prefixes` | чтение · настоящий backend: настоящий backend (тестовая копия БД): реальный в… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69); в опубликованной нет: публикация |
| 153 | Справочники | `GET /dictionaries/smu/candidates` | Кандидаты для замены записи справочника «СМУ» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Удаление записи справочника» (`dict-delete-backdrop`); окно «Справочник» (`simple-catalog-backdrop`) (и… | есть: delete-plan.js:69, dict-edit.js:184 · экран `dict-smu` | чтение · настоящий backend: справочник СМУ: настоящий backend и вход (real_au… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69, dict-edit.js:184); в опубликованной нет: публикация |
| 156 | Справочники | `GET /dictionaries/subtype/candidates` | Кандидаты для замены записи справочника «Подтип» | «Удаление записей справочников с заменой ссылок»: чтение | общий код V1 (вызывается из многих мест) — app.js:11244 ddBuildNode() | есть: delete-plan.js:69 · экран `subtypes` | чтение · настоящий backend: настоящий backend (тестовая копия БД, объект 1):… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69); в опубликованной нет: публикация |
| 159 | Справочники | `GET /dictionaries/zone/candidates` | Кандидаты для замены записи справочника «Зона» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Удаление записи справочника» (`dict-delete-backdrop`); окно «Справочник» (`simple-catalog-backdrop`) (и… | есть: delete-plan.js:69 · экран `dict-smu` | чтение · настоящий backend: справочник СМУ: настоящий backend и вход (real_au… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:69); в опубликованной нет: публикация |
| 160 | Справочники | `POST /dictionaries/zone/{key}/delete` | Удаление записи справочника с заменой ссылок «Зона» | «Удаление записей справочников с заменой ссылок»: изменение | окно «Удаление записи справочника» (`dict-delete-backdrop`) — app.js:11396 | форма есть: delete-plan.js:145 · экран `dict-smu` | запись · настоящий backend: справочник СМУ: настоящий backend и вход (real_au… | отключена `counterparties.write` | форма есть (delete-plan.js:145), шлюз отключён (counterparties.write: операции вне перечня разрешённых выше не проверялись в новом интерфейсе); нужны проверки (см. «Перечень проверок») и строка `allowed: true` в блоке области admin |
| 161 | Справочники | `GET /dictionaries/zone/{key}/delete-plan` | План последствий удаления записи справочника «Зона» | «Удаление записей справочников с заменой ссылок»: чтение | окно «Удаление записи справочника» (`dict-delete-backdrop`); окно «Справочник» (`simple-catalog-backdrop`) (и… | есть: delete-plan.js:148, delete-plan.js:159 · экран `dict-smu` | чтение · настоящий backend: справочник СМУ: настоящий backend и вход (real_au… | не ограничивается (чтение) | подключено в этой ветке (delete-plan.js:148, delete-plan.js:159); в опубликованной нет: публикация |
| 164 | Справочники | `POST /individuals` | Физлица: добавить запись | «Физлица»: изменение | Действия › Справочники › Проект и объект › Физлица — app.js:11526 | есть: dict-edit.js:123 · экран `dict-individuals` | запись · не проверено: HTTP+браузер: успех, дубль 409, пустое 400, 403 б… | разрешена `individuals.create`, поля: name | разрешена шлюзом (individuals.create) и вызывается из V2 (dict-edit.js:123) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 165 | Справочники | `PATCH /individuals/{individual_id}` | Физлица: переименовать | «Физлица»: изменение | Действия › Справочники › Проект и объект › Физлица — app.js:11526 | есть: dict-edit.js:149 · экран `dict-individuals` | запись · не проверено: HTTP+браузер: успех, дубль 409, 404, 403 | разрешена `individuals.rename`, поля: name | разрешена шлюзом (individuals.rename) и вызывается из V2 (dict-edit.js:149) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 169 | Справочники | `GET /marks` | Список: марки — Марки объекта — все или одного типа. | «Марки»: чтение | окно «Элемент» (`element-form-backdrop`); окно «Смена статуса» (`status-contract-backdrop`) — app.js:11565 re… | есть: element-ops.js:404 · экран `element-ops` | чтение · настоящий backend: настоящий backend (копия обезличенной БД): полный… | не ограничивается (чтение) | подключено в этой ветке (element-ops.js:404); в опубликованной нет: публикация |
| 170 | Справочники | `POST /marks` | Создание: марки | «Марки»: изменение | Действия › Справочники › Классификация › Типы и подтипы элементов — app.js:11630 добавитьМарку() | только переход в V1 (экран `subtypes`, статус 4) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Типы и подтипы элементов» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «Марки типов и удаление подтипа с изделиями (с заменой) — V1» |
| 171 | Справочники | `PATCH /marks/{mark_id}` | Переименование ведёт за собой ТЕКСТ марки везде, где он лежит копией: у изделий (`elements.mark`) и… | «Марки»: изменение | Действия › Справочники › Классификация › Типы и подтипы элементов — app.js:11677 переименовать() | только переход в V1 (экран `subtypes`, статус 4) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Типы и подтипы элементов» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «Марки типов и удаление подтипа с изделиями (с заменой) — V1» |
| 176 | Справочники | `PATCH /zones/{zone_id}` | Правка зоны справочника: номер, наименование, кран-владелец и ГЕОМЕТРИЯ ярусов (решения З9, З13, З1… | «Зоны: захватки, краны, стоянки»: изменение | окно «Зона» (`zone-edit-backdrop`) — app.js:15028 | только переход в V1 (экран `zones`, статус 3) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Зоны: захватки, краны, стоянки» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 177 | Справочники | `GET /zones/{zone_id}/geometry` | Геометрия зоны плюс КОНТЕКСТ для предпросмотра (решение З13): габариты объекта, сетка осей, соседни… | «Зоны: захватки, краны, стоянки»: чтение | окно «Зона» (`zone-edit-backdrop`); окно «Зоны» (`zones-backdrop`) — app.js:14592 openZoneEditor() | только переход в V1 (экран `zones`, статус 3) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Зоны: захватки, краны, стоянки» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 178 | Справочники | `POST /zones/{zone_id}/undo` | Откат последней правки зоны ЦЕЛИКОМ (решение З12): реквизиты, ярусы и привязки элементов, которые и… | «Зоны: захватки, краны, стоянки»: изменение | окно «Зоны» (`zones-backdrop`) — app.js:15081 | только переход в V1 (экран `zones`, статус 3) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Зоны: захватки, краны, стоянки» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 270 | Настройки | `PATCH /users/{user_id}/menu-prefs` | Личная настройка меню «Действия»: порядок пунктов внутри блоков и избранное. | системная роль admin (проверка в коде; может допускать и «свою запись… | окно «Действия» (`actions-panel-backdrop`); кнопка «Действия ▾» (`btn-settings-menu`) — app.js:10468 saveMenu… | только переход в V1 (экран `appearance`, статус 4) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Внешний вид» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «Параметры 3D-сцены (ракурс, тени, подписи), меню «Действия», направление солнца — V1; полная палитра гаммы в V2 не пере…» |
| 271 | Настройки | `PATCH /users/{user_id}/min-label-px` | Тот же guard самообслуживания, что у set_ui_theme и set_view3d: менять можно только себе, если ты н… | системная роль admin (проверка в коде; может допускать и «свою запись… | окно «Внешний вид» (`menu-view-backdrop`) — app.js:10885 | только переход в V1 (экран `appearance`, статус 4) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Внешний вид» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «Параметры 3D-сцены (ракурс, тени, подписи), меню «Действия», направление солнца — V1; полная палитра гаммы в V2 не пере…» |
| 273 | Настройки | `PATCH /users/{user_id}/view3d` | Тот же guard самообслуживания, что у set_ui_theme и set_label_color: менять можно только себе, если… | системная роль admin (проверка в коде; может допускать и «свою запись… | окно «Внешний вид» (`menu-view-backdrop`) — app.js:10848 | только переход в V1 (экран `appearance`, статус 4) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Внешний вид» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «Параметры 3D-сцены (ракурс, тени, подписи), меню «Действия», направление солнца — V1; полная палитра гаммы в V2 не пере…» |
| 276 | Служебные функции | `(без API) app.js:16577` | Копирование текста в буфер обмена (памятка администратора) | как у экрана V1 | app.js:16577 copyText | только переход в V1 (экран `admin-guide`, статус 3) | клиентская · — | — | нет в V2: добавить клиентскую операцию на экране «Памятка администратора» (печатная форма / буфер обмена, без API — шлюз не нужен) |
| 277 | Служебные функции | `(без API) app.js:16578` | Копирование текста в буфер обмена (памятка администратора) | как у экрана V1 | app.js:16578 copyText | только переход в V1 (экран `admin-guide`, статус 3) | клиентская · — | — | нет в V2: добавить клиентскую операцию на экране «Памятка администратора» (печатная форма / буфер обмена, без API — шлюз не нужен) |
| 281 | Служебные функции | `POST /activity/cleanup` | Очистка: журнал действий — Очистка журнала за период. | «Журнал действий сервиса и его очистка»: изменение | окно «Журнал действий» (`activity-backdrop`) — app.js:23864 | есть: admin-service.js:394 · экран `activity` | запись · не проверено: HTTP+браузер: счёт, очистка, факт очистки в журна… | разрешена `activity.cleanup` | разрешена шлюзом (activity.cleanup) и вызывается из V2 (admin-service.js:394) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 282 | Служебные функции | `GET /activity/stats` | Сколько журнал накопил и сколько места занимает (живой запрос 2026-08-20). | «Журнал действий сервиса и его очистка»: чтение | окно «Журнал действий» (`activity-backdrop`); Действия › Администрирование › Журнал действий — app.js:23793 l… | только переход в V1 (экран `activity`, статус 5) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Журнал действий» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Только просмотр: изменяющие операции — переход в V1 с контекстом объекта» |
| 284 | Служебные функции | `GET /admin-guide.md` | Список: памятка администратора | «Памятка администратора»: чтение | окно «Памятка администратора» (`admin-guide-backdrop`) — app.js:16645, app.js:16664 | только переход в V1 (экран `admin-guide`, статус 3) | чтение (файл) · — | не ограничивается (чтение) | нет в V2: экран «Памятка администратора» лишь ведёт в V1; подключить чтение (GET) в экран. |
| 286 | Служебные функции | `POST /admin/backups` | Копия по кнопке. Записывается, КЕМ создана — в отличие от служебных, которые система снимает сама п… | «Резервные копии»: изменение | окно «Резервные копии базы данных» (`backups-backdrop`) — app.js:23499 | есть: admin-service.js:63, admin-service.js:131 · экран `backups` | запись · не проверено: HTTP+браузер: успех, файл и описание, двойной кли… | разрешена `backups.create`, поля: comment | разрешена шлюзом (backups.create) и вызывается из V2 (admin-service.js:63, admin-service.js:131) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 287 | Служебные функции | `DELETE /admin/backups/{name}` | Резервные копии: удалить копию | «Резервные копии»: изменение | точка входа не установлена статически — app.js:23536 | есть: admin-service.js:147 · экран `backups` | запись · не проверено: HTTP+браузер: удаление файла и описания, повтор 4… | разрешена `backups.delete` | разрешена шлюзом (backups.delete) и вызывается из V2 (admin-service.js:147) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 288 | Служебные функции | `POST /admin/backups/{name}/restore` | Восстановление из копии: резервные копии — Восстановление на выбранный момент. | «Резервные копии»: изменение | точка входа не установлена статически — app.js:23523 | есть: admin-service.js:167 · экран `backups` | запись · не проверено: HTTP+браузер на копии БД: восстановление, служебн… | разрешена `backups.restore` | разрешена шлюзом (backups.restore) и вызывается из V2 (admin-service.js:167) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 290 | Служебные функции | `GET /admin/db-status/tables/{table}` | Просмотр: строки таблицы БД | «Состояние БД»: чтение | окно «Таблица» (`db-table-backdrop`) — app.js:17367 loadDbTable() | только переход в V1 (экран `db-status`, статус 3) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Состояние БД» лишь ведёт в V1; подключить чтение (GET) в экран. |
| 291 | Служебные функции | `POST /admin/db-transfer/apply` | Применение сверенных изменений: перенос базы — ПОЛНАЯ ЗАМЕНА базы и вложений содержимым снимка. | «Перенос базы целиком между серверами»: изменение | окно «Массовая правка через Excel» (`bulk-edit-backdrop`) — app.js:28861 | только переход в V1 (экран `bulk-edit`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Массовая правка через Excel» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: ««Перенос базы» (замена базы целиком) остаётся только в V1; выгрузка по фильтру схемы — в V1 (в V2 — все или выбранный о…» |
| 293 | Служебные функции | `POST /admin/db-transfer/export` | Снимок ВСЕЙ базы и вложений одним архивом — то, что увозят с боевого сервера. | «Перенос базы целиком между серверами»: изменение | окно «Массовая правка через Excel» (`bulk-edit-backdrop`) — app.js:28798 | только переход в V1 (экран `bulk-edit`, статус 5) | чтение (POST) (файл) · — | не ограничивается (чтение) | экран «Массовая правка через Excel» реализован в V2 модулем `exchange`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: ««Перенос базы» (замена базы целиком) остаётся только в V1; выгрузка по фильтру схемы — в V1 (в V2 — все или выбран… |
| 294 | Служебные функции | `POST /admin/db-transfer/forget` | Сброс приёма файла: перенос базы — Убрать загруженный снимок из очереди, не применяя. | «Перенос базы целиком между серверами»: изменение | окно «Массовая правка через Excel» (`bulk-edit-backdrop`); Действия › Обмен данными › Массовая правка через E… | только переход в V1 (экран `bulk-edit`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Массовая правка через Excel» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: ««Перенос базы» (замена базы целиком) остаётся только в V1; выгрузка по фильтру схемы — в V1 (в V2 — все или выбранный о…» |
| 295 | Служебные функции | `POST /admin/db-transfer/stage` | Приём файла: перенос базы — Принять снимок и СВЕРИТЬ его с текущей базой. | «Перенос базы целиком между серверами»: изменение | окно «Массовая правка через Excel» (`bulk-edit-backdrop`) — app.js:28824 | только переход в V1 (экран `bulk-edit`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Массовая правка через Excel» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: ««Перенос базы» (замена базы целиком) остаётся только в V1; выгрузка по фильтру схемы — в V1 (в V2 — все или выбранный о…» |
| 296 | Служебные функции | `GET /admin/disk-space` | Свободное место отдельным запросом — для предупреждения при входе. | «Резервные копии»: чтение | окно «Сменить пароль» (`change-password-backdrop`); кнопка «Войти» (`login-submit`) — app.js:1025 warnAboutDi… | только переход в V1 (экран `backups`, статус 5) | чтение · — | не ограничивается (чтение) | экран «Резервные копии» реализован в V2 модулем `backups`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: «Только просмотр списка копий. Создание и восстановление копии БД, удаление копии — в V1: это высокорисковые операции, д…» |
| 297 | Служебные функции | `GET /admin/fill-empty-scope` | Что сейчас не заполнено. В базу не пишет — это предпросмотр. | «Заполнить пустые «Объект» и «Проект» (временная)»: чтение | окно «Заполнить пустые «Объект» и «Проект»» (`fill-scope-backdrop`); Действия › Справочники › Проект и объект… | есть: read-screen.js:205 · экран `fill-scope` | чтение · не проверено: чтение подключено: таблица из GET /admin/fill-emp… | не ограничивается (чтение) | подключено в этой ветке (read-screen.js:205); в опубликованной нет: публикация |
| 298 | Служебные функции | `POST /admin/fill-empty-scope/apply` | Применение сверенных изменений: заполнение пустых объекта и проекта — Заполнить выбранные поля выбр… | «Заполнить пустые «Объект» и «Проект» (временная)»: изменение | окно «Заполнить пустые «Объект» и «Проект»» (`fill-scope-backdrop`) — app.js:13430 | только переход в V1 (экран `fill-scope`, статус 3) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Заполнить пустые «Объект» и «Проект» (временная)» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области admin и проверки (см. «Перечень проверок»). Реестр экранов: «Само заполнение остаётся в текущем интерфейсе: временная необратимая массовая административная обработка (подлежит удал…» |
| 299 | Служебные функции | `POST /admin/reset-status-history` | Массовый сброс истории статусов ВСЕХ элементов — только для тестирования (живой запрос пользователя… | «Очистка истории статусов»: изменение | Действия › Администрирование › Очистить историю статусов — app.js:20524 | есть: admin-service.js:69 · экран `reset-history` | запись · не проверено: HTTP+браузер: предпросмотр, устаревший предпросмо… | разрешена `reset.history` | разрешена шлюзом (reset.history) и вызывается из V2 (admin-service.js:69) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 300 | Служебные функции | `GET /admin/reset-status-history/preview` | Что затронет `POST /admin/reset-status-history` — до того, как оно выполнено (только чтение, ничего… | «Очистка истории статусов»: изменение | не вызывается из V1 | есть: admin-service.js:46 | чтение · не проверено | не ограничивается (чтение) | подключено в этой ветке (admin-service.js:46); в опубликованной нет: публикация |
| 309 | Служебные функции | `GET /me/access-summary` | То же о самом себе — для любого вошедшего (без права на раздел «Пользователи»). | администратор сервиса (условно) | не вызывается из V1 | есть: admin-screens.js:46 · экран `shell-context` | чтение · настоящий backend: каркас: открывается из навигации, 3 размера окна… | не ограничивается (чтение) | подключено в этой ветке (admin-screens.js:46); в опубликованной нет: публикация |
| 313 | Служебные функции | `GET /release-status` | Список: состояние обновления — Состояние обновления для формы «Что нового». | системная роль admin (проверка в коде; может допускать и «свою запись… | окно «Что нового» (`changelog-backdrop`); кнопка «?» (`btn-changelog`) — app.js:24083 renderReleaseStatus() | есть: admin-service.js:419 · экран `changelog` | чтение · настоящий backend: «Что нового»: отметка «Ознакомился» (стенд CL, на… | не ограничивается (чтение) | подключено в этой ветке (admin-service.js:419); в опубликованной нет: публикация |
| 314 | Служебные функции | `POST /release-tasks/{name}/run` | Повтор упавшей обработки или запуск уборки — без подключения к серверу. | «Обработки данных при обновлении»: изменение | окно «Что нового» (`changelog-backdrop`); кнопка «?» (`btn-changelog`) — app.js:24107 renderReleaseStatus() | есть: admin-service.js:437 · экран `changelog` | запись · не проверено: HTTP+браузер: повтор выполненной обработки идемпо… | разрешена `release.run` | разрешена шлюзом (release.run) и вызывается из V2 (admin-service.js:437) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 315 | Служебные функции | `GET /training/attempts` | История попыток: своя — всегда, чужая — по разделу «История прохождения тестов другими людьми». | «Инструкция и тест по своим разделам»: чтение; «История прохождения т… | окно «Обучение» (`training-backdrop`); Действия › Настройки › МФР › Справка — app.js:17138 trainingRenderAtte… | есть: training-ui.js:73, training-ui.js:150 · экран `training` | чтение · настоящий backend: обучение: настоящий backend и вход (real_auth_ser… | не ограничивается (чтение) | подключено в этой ветке (training-ui.js:73, training-ui.js:150); в опубликованной нет: публикация |
| 316 | Служебные функции | `POST /training/attempts` | Создание: попытки теста — Начать попытку. Незавершённая уже есть — возвращается она. | «Инструкция и тест по своим разделам»: чтение; раздел не определён: у… | окно «Обучение» (`training-backdrop`); Действия › Настройки › МФР › Справка — app.js:17038 trainingStartAttem… | есть: training-ui.js:86 · экран `training` | запись · не проверено: HTTP+браузер: старт, возобновление вместо второй… | разрешена `training.start`, поля: object_id/role_key/feature_key | разрешена шлюзом (training.start) и вызывается из V2 (training-ui.js:86) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 317 | Служебные функции | `GET /training/attempts/{attempt_id}` | Разбор попытки по ответам: что спрашивали, что человек выбрал, верно ли и сколько думал. | «Инструкция и тест по своим разделам»: чтение; «История прохождения т… | окно «Обучение» (`training-backdrop`); Действия › Настройки › МФР › Справка — app.js:17175 trainingToggleAnsw… | есть: training-ui.js:125, training-ui.js:157 · экран `training` | чтение · настоящий backend: обучение: настоящий backend и вход (real_auth_ser… | не ограничивается (чтение) | подключено в этой ветке (training-ui.js:125, training-ui.js:157); в опубликованной нет: публикация |
| 318 | Служебные функции | `POST /training/attempts/{attempt_id}/answer` | Зафиксировать ответ и вернуть разбор вместе со следующим вопросом. | «Инструкция и тест по своим разделам»: чтение; раздел не определён: у… | окно «Обучение» (`training-backdrop`); Действия › Настройки › МФР › Справка — app.js:17094 trainingAnswer() | есть: training-ui.js:103 · экран `training` | запись · не проверено: HTTP+браузер: верный/неверный ответ и разбор, пов… | разрешена `training.answer`, поля: question_key/option | разрешена шлюзом (training.answer) и вызывается из V2 (training-ui.js:103) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 321 | Служебные функции | `GET /training/state` | Что показать на экране теста до его начала: незавершённая попытка, если она есть, и собственный ито… | «Инструкция и тест по своим разделам»: чтение; раздел не определён: у… | окно «Обучение» (`training-backdrop`); Действия › Настройки › МФР › Справка — app.js:17010 trainingRenderTest… | есть: training-ui.js:62 · экран `training` | чтение · настоящий backend: обучение: настоящий backend и вход (real_auth_ser… | не ограничивается (чтение) | подключено в этой ветке (training-ui.js:62); в опубликованной нет: публикация |

**Уже в V2 и прочее: 82**

| № | Раздел | Операция | Сценарий | Состояние | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 33 | Пользователи и роли | `GET /ldap-settings` | Список: настройки домена | реализована в V2 и опубликована | есть: admin-service.js:250, admin-service.js:271 · экран `users-access` | чтение · настоящий backend: раздел «Пользователи»: настоящий backend и вход (… | не ограничивается (чтение) | чтение опубликовано |
| 37 | Пользователи и роли | `GET /me/sessions` | Список: сеансы | реализована в V2 и опубликована | есть: sessions-edit.js:63 · экран `sessions` | чтение · настоящий backend: сеансы: настоящий backend и вход (real_auth_serve… | не ограничивается (чтение) | чтение опубликовано |
| 40 | Пользователи и роли | `GET /roles` | Всё, что нужно матрице, одним запросом: роли, разделы, разрешения. | реализована в V2 и опубликована | есть: users-access.js:87 · экран `users-access` | чтение · настоящий backend: раздел «Пользователи»: настоящий backend и вход (… | не ограничивается (чтение) | чтение опубликовано |
| 50 | Пользователи и роли | `GET /users` | Список: пользователи | реализована в V2 и опубликована | есть: access-view.js:84, users-access.js:83 · экран `access-matrix` | чтение · настоящий backend: сводка и групповая выдача: настоящий backend и вх… | не ограничивается (чтение) | чтение опубликовано |
| 53 | Пользователи и роли | `GET /users/access-matrix` | Гранты ВСЕХ пользователей разом — для групповой настройки прав. | реализована в V2 и опубликована | есть: access-view.js:84, users-access.js:117 · экран `access-matrix` | чтение · настоящий backend: сводка и групповая выдача: настоящий backend и вх… | не ограничивается (чтение) | чтение опубликовано |
| 55 | Пользователи и роли | `GET /users/{user_id}/access` | Доступ: пользователи — Гранты пользователя с расшифровкой названий. | реализована в V2 и опубликована | есть: users-access.js:1193, users-access.js:1252 · экран `users-access` | чтение · настоящий backend: раздел «Пользователи»: настоящий backend и вход (… | не ограничивается (чтение) | чтение опубликовано |
| 57 | Пользователи и роли | `GET /users/{user_id}/access-summary` | Просмотр: Пользователи и выдача доступов | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `users-access`) | чтение · — | не ограничивается (чтение) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 59 | Пользователи и роли | `GET /users/{user_id}/rights-matrix` | «На что у человека есть права» — разделы системы против объёма прав. | реализована в V2 и опубликована | есть: users-access.js:1933, users-access.js:1933 · экран `users-access` | чтение · настоящий backend: раздел «Пользователи»: настоящий backend и вход (… | не ограничивается (чтение) | чтение опубликовано |
| 65 | Проекты и объекты | `GET /dictionaries/object/{key}/delete-plan` | План последствий удаления записи справочника «Объект» | реализована в V2 и опубликована | есть: delete-plan.js:148, delete-plan.js:159 · экран `projects-objects` | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | чтение опубликовано |
| 68 | Проекты и объекты | `GET /dictionaries/project/{key}/delete-plan` | План последствий удаления записи справочника «Проект» | реализована в V2 и опубликована | есть: delete-plan.js:148, delete-plan.js:159 · экран `projects-objects` | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | чтение опубликовано |
| 69 | Проекты и объекты | `GET /map/config` | Что нужно клиенту до отрисовки: есть ли подложка и куда смотреть. | реализована в V2 и опубликована | есть: admin-service.js:324, projects-objects.js:28 · экран `projects-objects` | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | чтение опубликовано |
| 70 | Проекты и объекты | `GET /map/objects` | Список: объекты — Объекты с координатами и сводкой по каждому. | реализована в V2 и опубликована | есть: read-screen.js:205, projects-objects.js:247 · экран `map` | чтение · настоящий backend: настоящий backend (тестовая копия БД, администрат… | не ограничивается (чтение) | чтение опубликовано; экран «Карта проектов» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 74 | Проекты и объекты | `GET /objects` | Список: объекты — Объекты со счётчиками элементов. | реализована в V2 и опубликована | есть: counterparties.js:456, projects-objects.js:185 · экран `counterparties` | чтение · настоящий backend: настоящий backend (копия БД, вход формой, реальны… | не ограничивается (чтение) | чтение опубликовано |
| 79 | Проекты и объекты | `GET /projects` | Справочник проектов. Сроки СВОДЯТСЯ из объектов (решение П5), а не хранятся: раннее начал… | реализована в V2 и опубликована | есть: access-view.js:84, projects-objects.js:185 · экран `access-matrix` | чтение · настоящий backend: сводка и групповая выдача: настоящий backend и вх… | не ограничивается (чтение) | чтение опубликовано |
| 81 | Проекты и объекты | `GET /projects-tree` | Проекты со своими объектами — источник для переключателя в тулбаре (этап B). | реализована в V2 и опубликована | есть: access-view.js:84, main.js:162 · экран `access-matrix` | чтение · настоящий backend: сводка и групповая выдача: настоящий backend и вх… | не ограничивается (чтение) | чтение опубликовано |
| 82 | Проекты и объекты | `DELETE /projects/{project_id}` | Удаление: проекты — Удалять можно только ПУСТОЙ проект. | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `projects-objects`) | запись · — | нет строки (запрещено по умолчанию) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 113 | Документы | `GET /attachments` | Список: вложения | реализована в V2 и опубликована | есть: projects-objects.js:854, projects-objects.js:864 · экран `projects-object… | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | чтение опубликовано |
| 116 | Документы | `GET /attachments/{attachment_id}/download` | Скачивание: вложения | реализована в V2 и опубликована | есть: projects-objects.js:730 · экран `projects-objects` | чтение (файл) · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | чтение опубликовано |
| 129 | Справочники | `GET /address/check-house` | Проверка дома: адресный классификатор — Есть ли такой дом в классификаторе. | реализована в V2 и опубликована | есть: projects-objects.js:384 · экран `projects-objects` | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | чтение опубликовано |
| 131 | Справочники | `GET /address/houses` | Номера домов на улице (или в населённом пункте, если улицы нет) — подсказка к полю «Дом». | реализована в V2 и опубликована | есть: projects-objects.js:357 · экран `projects-objects` | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | чтение опубликовано |
| 135 | Справочники | `GET /address/resolve` | Разбор кода: адресный классификатор | реализована в V2 и опубликована | есть: projects-objects.js:278, projects-objects.js:303 · экран `projects-object… | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | чтение опубликовано |
| 136 | Справочники | `GET /address/settlements` | Список: населённые пункты (классификатор адресов) | реализована в V2 и опубликована | есть: projects-objects.js:274 · экран `projects-objects` | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | чтение опубликовано |
| 137 | Справочники | `GET /address/status` | Список: статус — Что загружено и что лежит в каталоге. | реализована в V2 и опубликована | есть: read-screen.js:205, projects-objects.js:37 · экран `address-classifier` | чтение · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Адресный классификатор» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 138 | Справочники | `GET /address/streets` | Список: улицы (классификатор адресов) | реализована в V2 и опубликована | есть: projects-objects.js:298 · экран `projects-objects` | чтение · настоящий backend: проекты и объекты, вложения, превью: настоящий ba… | не ограничивается (чтение) | чтение опубликовано |
| 141 | Справочники | `GET /allowed-subtypes` | Справочник подтипов ОДНОГО объекта по новому стандарту имён слоёв (см. Docs/backlog.md). | реализована в V2 и опубликована | есть: element-ops.js:405, subtypes-edit.js:58 · экран `subtypes` | чтение · настоящий backend: настоящий backend (тестовая копия БД, объект 1):… | не ограничивается (чтение) | чтение опубликовано; экран «Типы и подтипы элементов» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 142 | Справочники | `POST /allowed-subtypes` | Подтипы: добавить подтип типа элемента объекта | реализована в V2 и опубликована | есть: subtypes-edit.js:76 · экран `subtypes` | запись · настоящий backend: живая проверка на синтетической записи | разрешена `subtype.add` | готово и опубликовано (шлюз: subtype.add); экран «Типы и подтипы элементов» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 143 | Справочники | `DELETE /allowed-subtypes/{element_type}/{subtype}` | Удаление: подтипы элементов | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `subtypes`) | запись · — | нет строки (запрещено по умолчанию) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 146 | Справочники | `GET /dictionaries/individual/{key}/delete-plan` | План последствий удаления записи справочника «Физлицо» | реализована в V2 и опубликована | есть: delete-plan.js:148, delete-plan.js:159 · экран `dict-individuals` | чтение · настоящий backend: справочник физлиц: настоящий backend и вход (real… | не ограничивается (чтение) | чтение опубликовано |
| 151 | Справочники | `POST /dictionaries/mark_prefix/{key}/delete` | Удаление записи справочника с заменой ссылок «Префикс марки» | реализована в V2 и опубликована | есть: delete-plan.js:145, prefix-edit.js:127 · экран `mark-prefixes` | запись · настоящий backend: живая проверка: удаление через план | разрешена `prefix.delete` | готово и опубликовано (шлюз: prefix.delete); экран «Префиксы марок» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 152 | Справочники | `GET /dictionaries/mark_prefix/{key}/delete-plan` | План последствий удаления записи справочника «Префикс марки» | реализована в V2 и опубликована | есть: delete-plan.js:148, delete-plan.js:159 · экран `mark-prefixes` | чтение · настоящий backend: настоящий backend (тестовая копия БД): реальный в… | не ограничивается (чтение) | чтение опубликовано; экран «Префиксы марок» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 154 | Справочники | `POST /dictionaries/smu/{key}/delete` | Удаление записи справочника с заменой ссылок «СМУ» | реализована в V2 и опубликована | есть: delete-plan.js:145, dict-edit.js:192 · экран `dict-smu` | запись · настоящий backend: живая проверка: удаление неиспользуемой, отказ дл… | разрешена `smu.delete` | готово и опубликовано (шлюз: smu.delete) |
| 155 | Справочники | `GET /dictionaries/smu/{key}/delete-plan` | План последствий удаления записи справочника «СМУ» | реализована в V2 и опубликована | есть: delete-plan.js:148, delete-plan.js:159 · экран `dict-smu` | чтение · настоящий backend: справочник СМУ: настоящий backend и вход (real_au… | не ограничивается (чтение) | чтение опубликовано |
| 157 | Справочники | `POST /dictionaries/subtype/{key}/delete` | Удаление записи справочника с заменой ссылок «Подтип» | реализована в V2 и опубликована | есть: delete-plan.js:145, subtypes-edit.js:101 · экран `subtypes` | запись · настоящий backend: живая проверка: удаление и отказ для подтипа с 20… | разрешена `subtype.delete` | готово и опубликовано (шлюз: subtype.delete); экран «Типы и подтипы элементов» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 158 | Справочники | `GET /dictionaries/subtype/{key}/delete-plan` | План последствий удаления записи справочника «Подтип» | реализована в V2 и опубликована | есть: delete-plan.js:148, delete-plan.js:159 · экран `subtypes` | чтение · настоящий backend: настоящий backend (тестовая копия БД, объект 1):… | не ограничивается (чтение) | чтение опубликовано; экран «Типы и подтипы элементов» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 162 | Справочники | `GET /element-catalog` | Табличный справочник элементов с отбором по колонкам и сортировкой. | реализована в V2 и опубликована | есть: read-screen.js:205 · экран `element-catalog` | чтение · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Элементы» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 163 | Справочники | `GET /individuals` | Список: физлица | реализована в V2 и опубликована | есть: dict-edit.js:95, projects-objects.js:185 · экран `dict-individuals` | чтение · настоящий backend: справочник физлиц: настоящий backend и вход (real… | не ограничивается (чтение) | чтение опубликовано |
| 166 | Справочники | `GET /mark-type-prefixes` | Список: префиксы марок | реализована в V2 и опубликована | есть: prefix-edit.js:75 · экран `mark-prefixes` | чтение · настоящий backend: настоящий backend (тестовая копия БД): реальный в… | не ограничивается (чтение) | чтение опубликовано; экран «Префиксы марок» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 167 | Справочники | `POST /mark-type-prefixes` | Префиксы марок: добавить / заменить тип (с подтверждением) | реализована в V2 и опубликована | есть: prefix-edit.js:98 · экран `mark-prefixes` | запись · настоящий backend: живая проверка: добавление, замена, возврат | разрешена `prefix.set` | готово и опубликовано (шлюз: prefix.set); экран «Префиксы марок» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 168 | Справочники | `DELETE /mark-type-prefixes/{prefix}` | Удаление: префиксы марок | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `mark-prefixes`) | запись · — | нет строки (запрещено по умолчанию) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 172 | Справочники | `GET /smu` | Список: СМУ | реализована в V2 и опубликована | есть: dict-edit.js:95, projects-objects.js:185 · экран `dict-smu` | чтение · настоящий backend: справочник СМУ: настоящий backend и вход (real_au… | не ограничивается (чтение) | чтение опубликовано |
| 173 | Справочники | `POST /smu` | СМУ: добавить запись | реализована в V2 и опубликована | есть: dict-edit.js:123 · экран `dict-smu` | запись · настоящий backend: живая проверка на синтетической записи | разрешена `smu.create` | готово и опубликовано (шлюз: smu.create) |
| 174 | Справочники | `PATCH /smu/{smu_id}` | СМУ: переименовать | реализована в V2 и опубликована | есть: dict-edit.js:149 · экран `dict-smu` | запись · настоящий backend: живая проверка на синтетической записи | разрешена `smu.rename` | готово и опубликовано (шлюз: smu.rename) |
| 175 | Справочники | `GET /zones` | Список: зоны — Справочник зон одной категории (этап 2). | реализована в V2 и опубликована | есть: read-screen.js:205, read-screen.js:205 · экран `zones` | чтение · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Зоны: захватки, краны, стоянки» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 254 | Настройки | `PUT /element-shapes` | Форма маркера по паре «слой / тип» (только изменённые) | реализована в V2 и опубликована | есть: shape-edit.js:120 · экран `marker-shapes` | запись · настоящий backend: живая проверка: контур→круг→контур, изменена одна… | разрешена `shape.set` | готово и опубликовано (шлюз: shape.set); экран «Форма маркеров» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 255 | Настройки | `GET /label-dates-visibility` | Список: видимость дат в подписях | не вызывается интерфейсами (нет в V1 и V2) | нет | чтение · — | не ограничивается (чтение) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 256 | Настройки | `PUT /label-dates-visibility` | Сохранение: видимость дат в подписях | не вызывается интерфейсами (нет в V1 и V2) | нет | запись · — | нет строки (запрещено по умолчанию) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 257 | Настройки | `GET /label-visibility` | Список: видимость подписей | реализована в V2 и опубликована | есть: prefix-edit.js:76 · экран `mark-prefixes` | чтение · настоящий backend: настоящий backend (тестовая копия БД): реальный в… | не ограничивается (чтение) | чтение опубликовано; экран «Префиксы марок» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 258 | Настройки | `PUT /label-visibility` | Сохранение: видимость подписей | не вызывается интерфейсами (нет в V1 и V2) | нет | запись · — | нет строки (запрещено по умолчанию) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 259 | Настройки | `GET /layer-type-combinations` | Для экрана настроек формы маркера (п.11 третьего раунда) — все встреченные пары (слой, ти… | реализована в V2 и опубликована | есть: shape-edit.js:91, shape-edit.js:114 · экран `marker-shapes` | чтение · настоящий backend: стенд SP-01…04: в PUT только изменённые пары (пар… | не ограничивается (чтение) | чтение опубликовано; экран «Форма маркеров» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 260 | Настройки | `GET /settings/info-plate` | Список: порог опоздания поставки | реализована в V2 и опубликована | есть: setting-edit.js:69, setting-edit.js:92 · экран `late-threshold` | чтение · настоящий backend: настоящий backend (тестовая копия БД): для объект… | не ограничивается (чтение) | чтение опубликовано; экран «Порог опоздания поставки» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 261 | Настройки | `PUT /settings/info-plate` | Порог опоздания поставки объекта | реализована в V2 и опубликована | есть: setting-edit.js:85 · экран `late-threshold` | запись · настоящий backend: живая проверка: 0→2→0, SQL | разрешена `late.threshold`, поля: late_threshold_days | готово и опубликовано (шлюз: late.threshold); экран «Порог опоздания поставки» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 262 | Настройки | `GET /settings/project-card` | Список: карточка объекта | реализована в V2 и опубликована | есть: card-edit.js:126, card-edit.js:145 · экран `project-card` | чтение · настоящий backend: карточка объекта: настоящий backend и вход (real_… | не ограничивается (чтение) | чтение опубликовано |
| 263 | Настройки | `PUT /settings/project-card` | Карточка объекта: наименование, сроки, вехи (запись целой карточки) | реализована в V2 и опубликована | есть: card-edit.js:156, card-edit.js:315 · экран `project-card` | запись · настоящий backend: живая проверка: SQL — JSON целиком, исходный восс… | разрешена `card.save` | готово и опубликовано (шлюз: card.save) |
| 264 | Настройки | `GET /settings/report-notes` | Список: события, задачи, вопросы — Без on_date — весь список редакций (для формы ведения). | реализована в V2 и опубликована | есть: card-edit.js:126, card-edit.js:145 · экран `report-notes` | чтение · настоящий backend: настоящий backend (тестовая копия БД, объект 1):… | не ограничивается (чтение) | чтение опубликовано; экран «События, задачи, вопросы» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 265 | Настройки | `PUT /settings/report-notes` | События, задачи, вопросы: сохранить редакцию на дату | реализована в V2 и опубликована | есть: card-edit.js:156, card-edit.js:315 · экран `report-notes` | запись · настоящий backend: живая проверка: новая редакция, отказ занятой даты | разрешена `notes.save`, поля: effective_date/key_events/key_tasks/open_questions | готово и опубликовано (шлюз: notes.save); экран «События, задачи, вопросы» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 266 | Настройки | `DELETE /settings/report-notes/{effective_date}` | События, задачи, вопросы: удалить редакцию (с подтверждением) | реализована в V2 и опубликована | есть: card-edit.js:346 · экран `report-notes` | запись · настоящий backend: живая проверка: удаление своей редакции | разрешена `notes.delete` | готово и опубликовано (шлюз: notes.delete); экран «События, задачи, вопросы» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 267 | Настройки | `GET /status-colors` | Список: цвета статусов | реализована в V2 и опубликована | есть: color-edit.js:85, color-edit.js:106 · экран `status-colors` | чтение · настоящий backend: настоящий backend (тестовая копия БД): изменение… | не ограничивается (чтение) | чтение опубликовано; экран «Цвета статусов» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 268 | Настройки | `PUT /status-colors` | Цвета статусов (только изменённые) | реализована в V2 и опубликована | есть: color-edit.js:111 · экран `status-colors` | запись · настоящий backend: живая проверка: цвет → SQL → возврат | разрешена `status.colors` | готово и опубликовано (шлюз: status.colors); экран «Цвета статусов» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 269 | Настройки | `PATCH /users/{user_id}/label-color` | Цвет подписей марок — настройка АДМИНИСТРАТОРА СЕРВИСА (2026-08-04, решение пользователя)… | реализована в V2 и опубликована | есть: label-color-edit.js:75 · экран `label-color` | запись · настоящий backend: живая проверка: запись → SQL → сброс | разрешена `label-color.set` | готово и опубликовано (шлюз: label-color.set); экран «Цвет подписей марок» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 272 | Настройки | `PATCH /users/{user_id}/ui-theme` | Персональная цветовая гамма — тот же guard самообслуживания, что у set_label_color и set_… | реализована в V2 и опубликована | есть: appearance-edit.js:51 · экран `appearance` | запись · настоящий backend: живая проверка: запись → SQL → возврат | разрешена `appearance.theme` | готово и опубликовано (шлюз: appearance.theme); экран «Внешний вид» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 274 | Настройки | `GET /zone-colors` | Для экрана настроек «Цвета зон» — цвет каждого крана ОБЪЕКТА (см. Docs/backlog.md, item 7… | реализована в V2 и опубликована | есть: color-edit.js:85, color-edit.js:106 · экран `zone-colors` | чтение · настоящий backend: настоящий backend (тестовая копия БД): изменение… | не ограничивается (чтение) | чтение опубликовано; экран «Цвета зон» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 275 | Настройки | `PUT /zone-colors` | Цвета кранов объекта (только изменённые) | реализована в V2 и опубликована | есть: color-edit.js:111 · экран `zone-colors` | запись · настоящий backend: живая проверка: цвет → SQL → возврат | разрешена `zone.colors` | готово и опубликовано (шлюз: zone.colors); экран «Цвета зон» в статусе 4: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 278 | Служебные функции | `GET /` | Корневой адрес — ВСЕГДА V1; V2 открывается только явно (2026-09-21, ограниченный выпуск). | служебная | служебный | чтение · — | не ограничивается (чтение) | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |
| 279 | Служебные функции | `GET /activity` | Поиск по журналу. Только админу: журнал показывает, кто что делал, — это не то, что должн… | реализована в V2 и опубликована | есть: admin-service.js:378, exchange-common.js:123 · экран `activity` | чтение · настоящий backend: журнал и очистка: настоящий backend и вход (real_… | не ограничивается (чтение) | чтение опубликовано |
| 280 | Служебные функции | `POST /activity` | Пачка клиентских событий («нажал кнопку», «форма открылась», «запись выполнена») — сервер… | служебная | служебный | запись · — | нет строки (запрещено по умолчанию) | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |
| 283 | Служебные функции | `GET /admin-guide` | Список: памятка администратора | реализована в V2 и опубликована | есть: read-screen.js:205 · экран `admin-guide` | чтение · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Памятка администратора» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 285 | Служебные функции | `GET /admin/backups` | Все резервные копии на диске, новые сверху — из этого списка выбирается точка, на которую… | реализована в V2 и опубликована | есть: admin-service.js:120 · экран `backups` | чтение · настоящий backend: резервные копии: настоящий backend и вход (real_a… | не ограничивается (чтение) | чтение опубликовано |
| 289 | Служебные функции | `GET /admin/db-status` | Список: состояние БД | реализована в V2 и опубликована | есть: admin-service.js:160, read-screen.js:205 · экран `db-status` | чтение · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Состояние БД» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 292 | Служебные функции | `GET /admin/db-transfer/current` | Что сейчас в базе приёмника — левая колонка сверки «было/приедет». | не вызывается интерфейсами (нет в V1 и V2) | нет | чтение · — | не ограничивается (чтение) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 301 | Служебные функции | `GET /app-build` | Отпечаток сборки фронтенда — вкладка сверяет его со своим и, если он сменился, показывает… | служебная | служебный | чтение · — | не ограничивается (чтение) | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |
| 302 | Служебные функции | `GET /changelog` | Список: журнал версий | реализована в V2 и опубликована | есть: read-screen.js:205 · экран `changelog` | чтение · настоящий backend: «Что нового»: отметка «Ознакомился» (стенд CL, на… | не ограничивается (чтение) | чтение опубликовано |
| 303 | Служебные функции | `POST /changelog/ack` | Отметка «Ознакомился»: журнал версий — «Ознакомился» (2026-08-03, живой запрос). | реализована в V2 и опубликована | есть: read-screen.js:235 · экран `changelog` | запись · настоящий backend: живая проверка: запись → SQL | разрешена `changelog.ack` | готово и опубликовано (шлюз: changelog.ack) |
| 304 | Служебные функции | `GET /health` | Список: проверка работоспособности сервера | служебная | служебный | чтение · — | не ограничивается (чтение) | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |
| 305 | Служебные функции | `POST /login` | Вход в V2 тем же логином и паролем, что в V1 | служебная | служебный | запись · — | разрешена `auth.login` | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |
| 306 | Служебные функции | `GET /login-users` | Список для выпадающего списка на экране входа. | служебная | служебный | чтение · — | не ограничивается (чтение) | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |
| 307 | Служебные функции | `POST /logout` | Выход из системы (завершает свой сеанс) | служебная | служебный | запись · — | разрешена `auth.logout` | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |
| 308 | Служебные функции | `GET /me` | Список: текущий пользователь | служебная | служебный | чтение · — | не ограничивается (чтение) | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |
| 310 | Служебные функции | `PUT /me/last-object` | Запоминает выбранный объект ЗА ПОЛЬЗОВАТЕЛЕМ, а не в localStorage: человек садится за дру… | служебная | служебный | запись · — | нет строки (запрещено по умолчанию) | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |
| 311 | Служебные функции | `GET /me/permissions` | Что МОЖЕТ САМ спрашивающий на показываемом объекте: {раздел: уровень}. | служебная | служебный | чтение · — | не ограничивается (чтение) | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |
| 319 | Служебные функции | `GET /training/guide` | Инструкция под права спрашивающего (или под выбранную роль). | реализована в V2 и опубликована | есть: training-ui.js:62, read-screen.js:205 · экран `training` | чтение · настоящий backend: обучение: настоящий backend и вход (real_auth_ser… | не ограничивается (чтение) | чтение опубликовано |
| 320 | Служебные функции | `GET /training/ratings` | Итоги обучения всех людей — колонка «Обучение» в списке пользователей. | реализована в V2 и опубликована | есть: training-ui.js:141, read-screen.js:205 · экран `training-history` | чтение · настоящий backend: история попыток сотрудников: настоящий backend и… | не ограничивается (чтение) | чтение опубликовано |
| 322 | Служебные функции | `GET /v2` | Прямая точка входа в V2 — не зависит от cookie-предпочтения: по этому адресу V2 открывает… | служебная | служебный | чтение · — | не ограничивается (чтение) | служебный маршрут (вход, оболочка, версия сборки): не пользовательская операция, отдельно не переносится |

### exchange — импорт, экспорт, документы, отчёты

**К выполнению: 47**

| № | Раздел | Операция | Сценарий | Права | V1 | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 179 | Отчёты | `(без API) app.js:23021` | Печать отчёта (окно печати браузера) | как у экрана V1 | кнопка `report-print`; app.js:23021 | только переход в V1 (экран `report-status`, статус 3) | клиентская · — | — | нет в V2: добавить клиентскую операцию на экране «Статус монтажа» (печатная форма / буфер обмена, без API — шлюз не нужен) |
| 180 | Отчёты | `GET /objects/{object_id}/activity-users` | Кого можно выбрать в «Моей работе» и в фильтре «Изменения». | «Журнал действий по объекту: свои действия»: чтение; «Журнал и «Моя р… | Действия › Отчёты › Моя работа — app.js:3068 ensureMyWorkUsers() | только переход в V1 (экран `report-mywork`, статус 3) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Моя работа» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Только просмотр и выгрузка файла; печать, график, отбор фильтром схемы — V1» |
| 181 | Отчёты | `GET /report-help/{key}` | Справка по отчёту (2026-08-14): что показывает и по каким правилам считает. | любой вошедший пользователь | окно «Отчёт» (`reports-backdrop`) — app.js:22796 | только переход в V1 (экран `report-status`, статус 3) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Статус монтажа» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Только просмотр и выгрузка файла; печать, график, отбор фильтром схемы — V1» |
| 192 | Отчёты | `POST /reports/contracting-schedule` | Отчёт «График контрактации и поставки» (2026-08-06): насколько потребность стройки закрыта контракт… | «График контрактации и поставки»: чтение | Действия › Отчёты › График контрактации и поставки (в разработке) — app.js:21172 | есть: read-screen.js:204 · экран `report-contracting` | чтение (POST) · настоящий backend: настоящий backend + браузер (CDP): таблица, свёрт… | не ограничивается (чтение) | подключено в этой ветке (read-screen.js:204); в опубликованной нет: публикация |
| 193 | Отчёты | `POST /reports/delivery-schedule` | Отчёт «График поставки» — календарь потребности в поставке по дате начала СМР против фактической по… | «График поставки»: чтение | Действия › Отчёты › График поставки (в разработке) — app.js:21131 | есть: read-screen.js:204 · экран `report-delivery` | чтение (POST) · настоящий backend: настоящий backend + браузер (CDP): таблица, свёрт… | не ограничивается (чтение) | подключено в этой ветке (read-screen.js:204); в опубликованной нет: публикация |
| 194 | Отчёты | `POST /reports/delivery-schedule.pdf` | Отчёт «График поставки»: выгрузка в PDF | «График поставки»: чтение | Действия › Отчёты › График поставки (в разработке) — app.js:21131 | есть: read-screen.js:180 · экран `report-delivery` | чтение (POST) (файл) · настоящий backend: настоящий backend + браузер (CDP): таблица, свёрт… | не ограничивается (чтение) | подключено в этой ветке (read-screen.js:180); в опубликованной нет: публикация |
| 195 | Отчёты | `POST /reports/delivery-schedule.xlsx` | Отчёт «График поставки»: выгрузка в XLSX | «График поставки»: чтение | Действия › Отчёты › График поставки (в разработке) — app.js:21131 | есть: read-screen.js:180 · экран `report-delivery` | чтение (POST) (файл) · настоящий backend: настоящий backend + браузер (CDP): таблица, свёрт… | не ограничивается (чтение) | подключено в этой ветке (read-screen.js:180); в опубликованной нет: публикация |
| 196 | Отчёты | `POST /reports/delivery-schedule/cell` | Разбор одной ячейки по маркам: чего не хватает и откуда это можно переставить. | «График поставки»: чтение | точка входа не установлена статически — app.js:22156 showDeliveryTip() | только переход в V1 (экран `report-delivery`, статус 5) | чтение (POST) · — | не ограничивается (чтение) | нет в V2: экран «График поставки (в разработке)» лишь ведёт в V1; подключить отчёт/выгрузку в экран. Реестр экранов: «Разбор ячейки по маркам при наведении, выбор порядка группировки, печать и отбор фильтром схемы — в текущем интерфейсе» |
| 208 | Импорт и экспорт | `POST /admin/import-input` | Импорт всех файлов из папки Input/ на сервере — по явной команде из меню. | «Загрузка чертежей из папки Input»: изменение; «Чертежи: загрузка DXF… | окно «Загрузка из папки Input» (`import-input-backdrop`) — app.js:23922 | есть: exchange-import.js:414 · экран `import-input` | запись · настоящий backend: настоящий backend: построчный отчёт, отказ 403 у… | разрешена `input.import`, проверка тела | разрешена шлюзом (input.import) и вызывается из V2 (exchange-import.js:414) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 209 | Импорт и экспорт | `GET /admin/input-files` | Что сейчас лежит в Input/ — для диалога подтверждения перед импортом. | «Загрузка чертежей из папки Input»: чтение | Действия › Обмен данными › Загрузить из папки Input — app.js:23895 | есть: exchange-import.js:385 · экран `import-input` | чтение · настоящий backend: настоящий backend (тестовая копия БД, входы admin… | не ограничивается (чтение) | подключено в этой ветке (exchange-import.js:385); в опубликованной нет: публикация |
| 210 | Импорт и экспорт | `POST /elements/bulk-edit/analyze` | Сверяет загруженный файл с базой и возвращает список расхождений. | «Массовая правка через Excel»: изменение | окно «Массовая правка через Excel» (`bulk-edit-backdrop`) — app.js:28612 | есть: exchange-bulk.js:162 · экран `bulk-edit` | разбор файла · настоящий backend: настоящий backend: расхождения по трём режимам, 4… | разрешена `bulk.analyze`, проверка тела; вне формы разрешённой строки — отключена `counte… | разрешена шлюзом (bulk.analyze) и вызывается из V2 (exchange-bulk.js:162) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 211 | Импорт и экспорт | `POST /elements/bulk-edit/apply` | Применение сверенных изменений: массовая правка через Excel — Применяет отмеченные изменения. | «Массовая правка через Excel»: изменение | окно «Массовая правка через Excel» (`bulk-edit-backdrop`) — app.js:28674 | есть: exchange-bulk.js:229 · экран `bulk-edit` | запись · настоящий backend: настоящий backend: применение отмеченного, откат… | разрешена `bulk.apply`, проверка тела; вне формы разрешённой строки — отключена `counterp… | разрешена шлюзом (bulk.apply) и вызывается из V2 (exchange-bulk.js:229) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 212 | Импорт и экспорт | `POST /elements/bulk-edit/export` | Снимок реквизитов элементов ОДНИМ файлом — для правки в Excel и обратной загрузки (см. app/element_… | «Массовая правка через Excel»: чтение | окно «Массовая правка через Excel» (`bulk-edit-backdrop`) — app.js:28582 | только переход в V1 (экран `bulk-edit`, статус 5) | чтение (POST) (файл) · — | не ограничивается (чтение) | экран «Массовая правка через Excel» реализован в V2 модулем `exchange`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: ««Перенос базы» (замена базы целиком) остаётся только в V1; выгрузка по фильтру схемы — в V1 (в V2 — все или выбран… |
| 215 | Импорт и экспорт | `POST /import-contracting-xlsx` | Файл "Контрактация" (см. app/contracting_import.py, Docs/backlog.md, "Контрактация 2.0") — создаёт/… | «Импорт файла контрактации»: изменение | окно «Импорт контрактации из XLS» (`contracting-import-backdrop`) — app.js:18265 | только переход в V1 (экран `contracting-import`, статус 5) | запись · — | разрешена `import.contracting`, проверка тела | нет формы: экран «Импорт контрактации из XLS» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Предпросмотра до записи нет и в V1: перед отправкой — подтверждение последствий» |
| 217 | Импорт и экспорт | `POST /import-dxf/analyze` | Фаза 1 импорта (решение И3): что изменится, если применить чертёж. | «Чертежи: загрузка DXF и переимпорт версии»: изменение; администратор… | окно «Чертежи» (`upload-backdrop`) — app.js:24346 | есть: exchange-drawing.js:129 · экран `upload-drawing` | разбор файла · настоящий backend: настоящий backend: разбор синтетического DXF, отк… | разрешена `drawing.analyze`, проверка тела | разрешена шлюзом (drawing.analyze) и вызывается из V2 (exchange-drawing.js:129) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 218 | Импорт и экспорт | `POST /import-dxf/apply` | Применение сверенных изменений: загрузка чертежа DXF — Фаза 2: применяет уже показанную пользовател… | «Чертежи: загрузка DXF и переимпорт версии»: изменение | окно «Применение чертежа: сверка с прежней версией» (`import-review-backdrop`) — app.js:24618 | есть: exchange-drawing.js:172 · экран `upload-drawing` | запись · настоящий backend: настоящий backend: применение по токену, повтор т… | разрешена `drawing.apply`, проверка тела | разрешена шлюзом (drawing.apply) и вызывается из V2 (exchange-drawing.js:172) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 219 | Импорт и экспорт | `POST /import-history-xlsx` | Импорт истории статусов из XLSX по чертежу объекта (скорректировать / дополнить / заменить) | «Импорт истории статусов»: изменение | окно «Импорт истории статусов из XLS» (`history-import-backdrop`) — app.js:20568, app.js:23960 | только переход в V1 (экран `history-import`, статус 5) | запись · — | разрешена `import.history`, проверка тела; разрешена `status.restore`, проверка тела | нет формы: экран «Импорт истории из XLS» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Чертёж — текущий чертёж выбранного объекта (как в V1: чертёж на схеме)» |
| 220 | Импорт и экспорт | `POST /import-pdf-facade/analyze` | Упрощённая загрузка — «только фасады» (Docs/TZ.md §3а, Docs/ backlog.md 2026-09-01): блоки (этаж×се… | «Загрузка помещений из PDF»: изменение | окно «Загрузка из PDF» (`pdf-import-backdrop`) — app.js:36582 | только переход в V1 (экран `pdf-import`, статус 2) | разбор файла · — | нет строки (запрещено по умолчанию) | нет формы: экран «Загрузить из PDF» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: двухшаговая загрузка с фоновой задачей разбора, рассчитанная на один комплект чертежей заказчика; у…» |
| 221 | Импорт и экспорт | `POST /import-pdf-facade/apply` | Фаза 2 упрощённой загрузки — применяет уже показанную сводку. | «Загрузка помещений из PDF»: изменение | окно «Применение: что изменится» (`pdf-review-backdrop`) — app.js:36692 | только переход в V1 (экран `pdf-import`, статус 2) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Загрузить из PDF» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: двухшаговая загрузка с фоновой задачей разбора, рассчитанная на один комплект чертежей заказчика; у…» |
| 222 | Импорт и экспорт | `GET /import-pdf/analyze/progress/{job_id}` | Фаза 1, шаг 2: состояние фоновой задачи разбора. `status` — running/done/error; при `done` в `resul… | любой вошедший пользователь | окно «Загрузка из PDF» (`pdf-import-backdrop`) — app.js:35901 pollPdfAnalyzeJob() | только переход в V1 (экран `pdf-import`, статус 2) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Загрузить из PDF» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Не перенесено в V2: двухшаговая загрузка с фоновой задачей разбора, рассчитанная на один комплект чертежей заказчика; у…» |
| 223 | Импорт и экспорт | `POST /import-pdf/analyze/start` | Фаза 1 загрузки помещений из PDF, шаг 1: запускает разбор в фоновом потоке (~30с на реальном компле… | «Загрузка помещений из PDF»: изменение | окно «Загрузка из PDF» (`pdf-import-backdrop`) — app.js:36595 | только переход в V1 (экран `pdf-import`, статус 2) | разбор файла · — | нет строки (запрещено по умолчанию) | нет формы: экран «Загрузить из PDF» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: двухшаговая загрузка с фоновой задачей разбора, рассчитанная на один комплект чертежей заказчика; у…» |
| 224 | Импорт и экспорт | `POST /import-pdf/apply` | Применение сверенных изменений: загрузка планировок из PDF — Фаза 2: применяет уже показанную сводк… | «Загрузка помещений из PDF»: изменение | окно «Применение: что изменится» (`pdf-review-backdrop`) — app.js:36692 | только переход в V1 (экран `pdf-import`, статус 2) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Загрузить из PDF» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: двухшаговая загрузка с фоновой задачей разбора, рассчитанная на один комплект чертежей заказчика; у…» |
| 225 | Импорт и экспорт | `POST /import-revit/analyze` | Фаза 1 загрузки пакетов Revit: что появится в справочниках объекта. | «Загрузка выгрузок из Revit: пакеты разделов»: изменение | окно «Загрузка из Revit» (`revit-backdrop`) — app.js:29843 | есть: exchange-revit.js:120 · экран `revit-import` | разбор файла · настоящий backend: настоящий backend: разбор синтетического пакета К… | разрешена `revit.analyze`, проверка тела | разрешена шлюзом (revit.analyze) и вызывается из V2 (exchange-revit.js:120) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 226 | Импорт и экспорт | `POST /import-revit/apply` | Применение сверенных изменений: загрузка из Revit — Фаза 2: применяет уже показанную сводку. | «Загрузка выгрузок из Revit: пакеты разделов»: изменение | окно «Применение выгрузки: что изменится» (`revit-review-backdrop`) — app.js:29949 | есть: exchange-revit.js:144 · экран `revit-import` | запись · настоящий backend: настоящий backend: применение по токену, повтор т… | разрешена `revit.apply`, проверка тела | разрешена шлюзом (revit.apply) и вызывается из V2 (exchange-revit.js:144) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 227 | Импорт и экспорт | `POST /import-schedule-xlsx` | Файл графика MS Project (см. app/schedule_import.py) — сопоставляется с изделиями по блоку Захватка… | «Импорт графика MS Project»: изменение | окно «Импорт графика MS Project из XLS» (`schedule-import-backdrop`) — app.js:18573 | только переход в V1 (экран `schedule-import`, статус 5) | запись · — | разрешена `import.schedule`, проверка тела | нет формы: экран «Импорт графика MS Project из XLS» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Прогноз при повторной загрузке добавляет ещё одну версию (как в V1)» |
| 228 | Импорт и экспорт | `GET /import-templates` | Описания форматов всех загружаемых файлов — одним запросом на все формы загрузки (их шесть, ходить… | любой вошедший пользователь | окно «Сменить пароль» (`change-password-backdrop`) — app.js:28080 initImportTemplates() | есть: exchange-common.js:55 · экран `change-password` | чтение · настоящий backend: смена пароля: настоящий backend и вход (real_auth… | не ограничивается (чтение) | подключено в этой ветке (exchange-common.js:55); в опубликованной нет: публикация |
| 229 | Импорт и экспорт | `GET /import-templates/{key}/sample` | Образец файла: образцы файлов импорта | любой вошедший пользователь | окно «Сменить пароль» (`change-password-backdrop`) — app.js:28055 downloadTemplateSample() | только переход в V1 (экран `change-password`, статус 5) | чтение (файл) · — | не ограничивается (чтение) | экран «Сменить пароль» реализован в V2 модулем `password`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. |
| 230 | Импорт и экспорт | `POST /objects-import/analyze` | Сверяет файл «Объекты на карте» со справочником и возвращает список расхождений построчно (см. app/… | «Загрузка справочника объектов из Excel»: изменение | окно «Загрузка справочника объектов из Excel» (`objects-import-backdrop`) — app.js:18485 | есть: exchange-import.js:278 · экран `objects-import` | разбор файла · настоящий backend: настоящий backend: расхождения построчно, 422 на… | разрешена `objects.analyze`, проверка тела | разрешена шлюзом (objects.analyze) и вызывается из V2 (exchange-import.js:278) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 231 | Импорт и экспорт | `POST /objects-import/apply` | Применяет отмеченные изменения — создаёт новые объекты (и проекты под них) и правит реквизиты уже с… | «Загрузка справочника объектов из Excel»: изменение | окно «Загрузка справочника объектов из Excel» (`objects-import-backdrop`) — app.js:18511 | есть: exchange-import.js:330 · экран `objects-import` | запись · настоящий backend: настоящий backend: применение отмеченного, повтор… | разрешена `objects.apply`, проверка тела | разрешена шлюзом (objects.apply) и вызывается из V2 (exchange-import.js:330) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 232 | Импорт и экспорт | `POST /objects/{object_id}/block-works/bulk-edit/analyze` | Сверяет загруженный файл с базой и возвращает список расхождений. | «Виды работ и статус по блокам»: изменение | окно «Массовая правка ЗР через Excel» (`blk-bulk-backdrop`) — app.js:34570 | есть: block-bulk-screen.js:57 · экран `blk-bulk` | разбор файла · не проверено: HTTP и браузер: сверка на копии, БД до/после не и… | разрешена `bulk-edit.analyze` | разрешена шлюзом (bulk-edit.analyze) и вызывается из V2 (block-bulk-screen.js:57) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 233 | Импорт и экспорт | `POST /objects/{object_id}/block-works/bulk-edit/apply` | Применение сверенных изменений: массовая правка через Excel | «Виды работ и статус по блокам»: изменение | окно «Массовая правка ЗР через Excel» (`blk-bulk-backdrop`) — app.js:34755 | только переход в V1 (экран `blk-bulk`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Массовая правка ЗР через Excel (МФР)» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). |
| 234 | Импорт и экспорт | `POST /objects/{object_id}/block-works/bulk-edit/apply-strict` | Применение Excel-правки ЗР, «всё или ничего». | «Виды работ и статус по блокам»: изменение | не вызывается из V1 | есть: block-bulk-screen.js:123 · экран `blk-bulk` | запись · не проверено: HTTP и браузер: применение → SQL; 409 при изменен… | разрешена `bulk-edit.apply`, поля: changes, проверка тела | разрешена шлюзом (bulk-edit.apply) и вызывается из V2 (block-bulk-screen.js:123) в этой ветке; в опубликованной ветке нет: слияние и публикация, затем проверка на 8000 |
| 235 | Импорт и экспорт | `POST /objects/{object_id}/block-works/bulk-edit/export` | Выгрузка: массовая правка через Excel | «Виды работ и статус по блокам»: чтение | окно «Массовая правка ЗР через Excel» (`blk-bulk-backdrop`) — app.js:34545 | есть: block-bulk-screen.js:43 · экран `blk-bulk` | чтение (POST) (файл) · настоящий backend: настоящий backend с настоящим входом (копия обезл… | не ограничивается (чтение) | подключено в этой ветке (block-bulk-screen.js:43); в опубликованной нет: публикация |
| 236 | Импорт и экспорт | `POST /objects/{object_id}/clear-import-data` | Отладочная очистка справочников объекта перед повторной загрузкой Revit/PDF (app/import_reset.py) —… | раздел не определён: изменение; «Секции, этажи и блоки объекта»: изме… | окно «Загрузка из Revit» (`revit-backdrop`) — app.js:29795 runClearImportData() | только переход в V1 (экран `revit-import`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Загрузить из Revit» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Применение этапами (как в V1); отладочная очистка справочников загрузки — только в V1» |
| 237 | Импорт и экспорт | `GET /objects/{object_id}/drawings` | Список: чертежи — Версии чертежа объекта, новые сверху (этап B). | «Чертежи: загрузка DXF и переимпорт версии»: чтение | Действия › Обмен данными › Загрузить чертёж — app.js:1883 renderFileSelectMenu() | только переход в V1 (экран `upload-drawing`, статус 5) | чтение · — | не ограничивается (чтение) | экран «Загрузить чертёж» реализован в V2 модулем `exchange`, но это чтение он не вызывает — вероятно, заменено другим запросом; сверить с V1 и подключить, если данные нужны пользователю. Реестр экранов: «Применение идёт этапами и не является одной транзакцией (как в V1) — об этом сказано в подтверждении; воспроизводится д…» |
| 239 | Импорт и экспорт | `POST /objects/{object_id}/external-models` | Создание: внешние 3D-модели | «Внешние 3D-модели (благоустройство и т.п.)»: изменение | Действия › Обмен данными › Загрузить из FBX — external-models/settings.js:822 wire() | только переход в V1 (экран `external-models`, статус 2) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Загрузить из FBX» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: это интерфейс размещения и калибровки FBX-моделей в 3D, а не форма загрузки — выполняется в V1» |
| 241 | Импорт и экспорт | `GET /objects/{object_id}/external-models/geometry-features` | Контуры стен объекта в абсолютных P (мм) — для автоматического совмещения фасада из FBX (Docs/fbx-a… | «Внешние 3D-модели (благоустройство и т.п.)»: изменение | Действия › Обмен данными › Загрузить из FBX — external-models/settings.js:139 computeAutoAlignment() | только переход в V1 (экран `external-models`, статус 2) | чтение · — | не ограничивается (чтение) | нет в V2: экран «Загрузить из FBX» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Не перенесено в V2: это интерфейс размещения и калибровки FBX-моделей в 3D, а не форма загрузки — выполняется в V1» |
| 242 | Импорт и экспорт | `DELETE /objects/{object_id}/external-models/{model_id}` | Удаление: внешние 3D-модели | «Внешние 3D-модели (благоустройство и т.п.)»: изменение | Действия › Обмен данными › Загрузить из FBX — external-models/settings.js:773 wire() | только переход в V1 (экран `external-models`, статус 2) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Загрузить из FBX» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: это интерфейс размещения и калибровки FBX-моделей в 3D, а не форма загрузки — выполняется в V1» |
| 243 | Импорт и экспорт | `PATCH /objects/{object_id}/external-models/{model_id}` | Изменение: внешние 3D-модели | «Внешние 3D-модели (благоустройство и т.п.)»: изменение | Действия › Обмен данными › Загрузить из FBX — external-models/settings.js:531 wire(), external-models/setting… | только переход в V1 (экран `external-models`, статус 2) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Загрузить из FBX» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: это интерфейс размещения и калибровки FBX-моделей в 3D, а не форма загрузки — выполняется в V1» |
| 245 | Импорт и экспорт | `POST /objects/{object_id}/external-models/{model_id}/recenter` | Перецентровка: внешние 3D-модели | «Внешние 3D-модели (благоустройство и т.п.)»: изменение | Действия › Обмен данными › Загрузить из FBX — external-models/settings.js:750 wire() | только переход в V1 (экран `external-models`, статус 2) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Загрузить из FBX» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: это интерфейс размещения и калибровки FBX-моделей в 3D, а не форма загрузки — выполняется в V1» |
| 246 | Импорт и экспорт | `POST /objects/{object_id}/work-types/analyze` | Сверка файла перед применением: виды работ — Фаза 1: разбор xlsx со справочником видов работ. | «Виды работ и статус по блокам»: изменение | окно «Учёт по блокам» (`blocks-backdrop`) — app.js:35467 | только переход в V1 (экран `blocks`, статус 5) | разбор файла · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 247 | Импорт и экспорт | `POST /objects/{object_id}/work-types/apply` | Применение сверенных изменений: виды работ — Фаза 2: применяет уже показанную сводку по токену. | «Виды работ и статус по блокам»: изменение | окно «Учёт по блокам» (`blocks-backdrop`) — app.js:35503 | только переход в V1 (экран `blocks`, статус 5) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Учёт по блокам» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Секции, этажи, блоки и геометрия блоков заводятся и правятся в текущем интерфейсе; справочник видов работ — только прос…» |
| 248 | Импорт и экспорт | `GET /settings/export` | Выгрузка: настройки | «Экспорт и импорт настроек»: чтение | окно «Экспорт/импорт настроек проекта» (`settings-io-backdrop`) — app.js:20481 | только переход в V1 (экран `settings-io`, статус 2) | чтение (файл) · — | не ограничивается (чтение) | нет в V2: экран «Экспорт/импорт настроек» лишь ведёт в V1; подключить чтение (GET) в экран. Реестр экранов: «Не перенесено в V2: файл экспорта содержит хэши и соли паролей, импорт перезаписывает пользователей и роли; пароли и пр…» |
| 249 | Импорт и экспорт | `POST /settings/import` | Создание / действие: Экспорт и импорт настроек | «Экспорт и импорт настроек»: изменение | окно «Экспорт/импорт настроек проекта» (`settings-io-backdrop`) — app.js:20491 | только переход в V1 (экран `settings-io`, статус 2) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Экспорт/импорт настроек» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: файл экспорта содержит хэши и соли паролей, импорт перезаписывает пользователей и роли; пароли и пр…» |
| 250 | Импорт и экспорт | `POST /shaft-panels/analyze` | Сверка файла перед применением: панели облицовки шахты | «Чертежи: загрузка DXF и переимпорт версии»: изменение | точка входа не установлена статически — shaft-panels.js:159 mountShaftImport() | только переход в V1 (экран `shaft-panels`, статус 2) | разбор файла · — | нет строки (запрещено по умолчанию) | нет формы: экран «Панели облицовки шахты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: разбор по профилю семейства чертежей (слой, оси 5–7 / Е–Ж, шаблон марки); без реального чертежа усп…» |
| 251 | Импорт и экспорт | `POST /shaft-panels/apply` | Применение сверенных изменений: панели облицовки шахты | «Чертежи: загрузка DXF и переимпорт версии»: изменение | точка входа не установлена статически — shaft-panels.js:146 drawPreview() | только переход в V1 (экран `shaft-panels`, статус 2) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Панели облицовки шахты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: разбор по профилю семейства чертежей (слой, оси 5–7 / Е–Ж, шаблон марки); без реального чертежа усп…» |
| 252 | Импорт и экспорт | `DELETE /shaft-panels/pending/{token}` | Удаление: cancel_endpoint | любой вошедший пользователь | точка входа не установлена статически — shaft-panels.js:69 cancel() | только переход в V1 (экран `shaft-panels`, статус 2) | запись · — | нет строки (запрещено по умолчанию) | нет формы: экран «Панели облицовки шахты» лишь ведёт в V1; нужны форма/диалог V2, строка `allowed: true` в блоке области exchange и проверки (см. «Перечень проверок»). Реестр экранов: «Не перенесено в V2: разбор по профилю семейства чертежей (слой, оси 5–7 / Е–Ж, шаблон марки); без реального чертежа усп…» |

**Уже в V2 и прочее: 27**

| № | Раздел | Операция | Сценарий | Состояние | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 182 | Отчёты | `POST /reports/analytics` | Отчёт «Аналитическая справка» (2026-08-11): контрактация под ближайшие этапы СМР и критич… | реализована в V2 и опубликована | есть: read-screen.js:204 · экран `report-analytics` | чтение (POST) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Аналитическая справка» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 183 | Отчёты | `POST /reports/analytics.pdf` | Отчёт «Аналитическая справка»: выгрузка в PDF | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-analytics` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Аналитическая справка» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 184 | Отчёты | `POST /reports/analytics.xlsx` | Отчёт «Аналитическая справка»: выгрузка в XLSX | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-analytics` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Аналитическая справка» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 185 | Отчёты | `POST /reports/block-schedule` | Отчёт «График работ по блокам» — ЗР (запланированные работы) с планом, прогнозом, процент… | реализована в V2 и опубликована | есть: read-screen.js:204 · экран `report-block-schedule` | чтение (POST) · настоящий backend: настоящий backend (тестовая копия БД, объект 4 ти… | не ограничивается (чтение) | чтение опубликовано; экран «График работ по блокам» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 186 | Отчёты | `POST /reports/block-schedule.pdf` | Отчёт «График работ по блокам»: выгрузка в PDF | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-block-schedule` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, объект 4 ти… | не ограничивается (чтение) | чтение опубликовано; экран «График работ по блокам» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 187 | Отчёты | `POST /reports/block-schedule.xlsx` | Отчёт «График работ по блокам»: выгрузка в XLSX | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-block-schedule` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, объект 4 ти… | не ограничивается (чтение) | чтение опубликовано; экран «График работ по блокам» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 188 | Отчёты | `POST /reports/block-status` | Отчёт «Учёт по блокам: статусы» (2026-09-05, живой запрос пользователя) — бывшая вкладка… | реализована в V2 и опубликована | есть: read-screen.js:204 · экран `report-block-status` | чтение (POST) · настоящий backend: настоящий backend (тестовая копия БД, администрат… | не ограничивается (чтение) | чтение опубликовано; экран «Учёт по блокам: статусы» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 189 | Отчёты | `POST /reports/completion` | Отчёт «Статус комплектации» в одном из двух видов (`view`): плоский перечень «кран · стоя… | реализована в V2 и опубликована | есть: read-screen.js:204 · экран `report-completion` | чтение (POST) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Статус комплектации» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 190 | Отчёты | `POST /reports/completion.pdf` | Отчёт «Статус комплектации»: выгрузка в PDF | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-completion` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Статус комплектации» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 191 | Отчёты | `POST /reports/completion.xlsx` | Отчёт «Статус комплектации»: выгрузка в XLSX | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-completion` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Статус комплектации» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 197 | Отчёты | `POST /reports/dynamics` | Отчёт «Динамика поставки и монтажа»: данные для экрана — Ежедневный «Отчёт о динамике пос… | реализована в V2 и опубликована | есть: read-screen.js:204 · экран `report-dynamics` | чтение (POST) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Динамика поставки и монтажа» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 198 | Отчёты | `POST /reports/dynamics.pdf` | Отчёт «Динамика поставки и монтажа»: выгрузка в PDF | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-dynamics` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Динамика поставки и монтажа» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 199 | Отчёты | `POST /reports/dynamics.xlsx` | Отчёт «Динамика поставки и монтажа»: выгрузка в XLSX | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-dynamics` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Динамика поставки и монтажа» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 200 | Отчёты | `POST /reports/linear-track` | Отчёт «Линейный трек» — полный список позиций WBS объекта, без привязки к блоку/секции (с… | реализована в V2 и опубликована | есть: read-screen.js:204 · экран `report-linear-track` | чтение (POST) · настоящий backend: настоящий backend (тестовая копия БД, объект 4 ти… | не ограничивается (чтение) | чтение опубликовано; экран «Линейный трек» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 201 | Отчёты | `POST /reports/linear-track.xlsx` | Отчёт «Линейный трек»: выгрузка в XLSX | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-linear-track` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, объект 4 ти… | не ограничивается (чтение) | чтение опубликовано; экран «Линейный трек» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 202 | Отчёты | `POST /reports/my-work` | Отчёт «Моя работа» — что текущий пользователь изменил за период. | реализована в V2 и опубликована | есть: read-screen.js:204 · экран `report-mywork` | чтение (POST) · настоящий backend: настоящий backend (тестовая копия БД): за 20.09 п… | не ограничивается (чтение) | чтение опубликовано; экран «Моя работа» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 203 | Отчёты | `POST /reports/my-work.pdf` | Отчёт «Моя работа»: выгрузка в PDF | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-mywork` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД): за 20.09 п… | не ограничивается (чтение) | чтение опубликовано; экран «Моя работа» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 204 | Отчёты | `POST /reports/my-work.xlsx` | Отчёт «Моя работа»: выгрузка в XLSX | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-mywork` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД): за 20.09 п… | не ограничивается (чтение) | чтение опубликовано; экран «Моя работа» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 205 | Отчёты | `POST /reports/status` | Отчёт «Статус монтажа»: данные для экрана — Отчёт «Статус монтажа» — данные для экрана. | реализована в V2 и опубликована | есть: read-screen.js:204 · экран `report-status` | чтение (POST) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Статус монтажа» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 206 | Отчёты | `POST /reports/status.pdf` | Отчёт «Статус монтажа»: выгрузка в PDF | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-status` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Статус монтажа» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 207 | Отчёты | `POST /reports/status.xlsx` | Отчёт «Статус монтажа»: выгрузка в XLSX | реализована в V2 и опубликована | есть: read-screen.js:180 · экран `report-status` | чтение (POST) (файл) · настоящий backend: настоящий backend (тестовая копия БД, сессия адми… | не ограничивается (чтение) | чтение опубликовано; экран «Статус монтажа» в статусе 3: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 213 | Импорт и экспорт | `GET /export.pdf` | Выгрузка в PDF: выгрузка | реализована в V2 и опубликована | есть: export-form.js:67 · экран `export-xls` | чтение (файл) · настоящий backend: настоящий backend + браузер (CDP): выгрузка по че… | не ограничивается (чтение) | чтение опубликовано |
| 214 | Импорт и экспорт | `POST /export.xlsx` | Выгрузка в XLSX: выгрузка | реализована в V2 и опубликована | есть: export-form.js:63 · экран `export-xls` | чтение (POST) (файл) · настоящий backend: настоящий backend + браузер (CDP): выгрузка по че… | не ограничивается (чтение) | чтение опубликовано |
| 216 | Импорт и экспорт | `POST /import-dxf` | Создание: загрузка чертежа DXF | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `upload-drawing`) | запись · — | нет строки (запрещено по умолчанию) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 238 | Импорт и экспорт | `GET /objects/{object_id}/external-models` | Список: внешние 3D-модели | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `external-models` | чтение · каркас, не проверено: каркас недостаточен (динамическое содержимое V1 н… | не ограничивается (чтение) | чтение опубликовано; экран «Загрузить из FBX» в статусе 2: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |
| 240 | Импорт и экспорт | `GET /objects/{object_id}/external-models/bounds` | Просмотр: Внешние 3D-модели (благоустройство и т.п.) | не вызывается интерфейсами (нет в V1 и V2) | нет (ближайший экран `external-models`) | чтение · — | не ограничивается (чтение) | не вызывается ни V1, ни V2 (API без интерфейса: внешний клиент, устаревший или служебный маршрут): переносить нечего; решить, нужен ли маршрут |
| 244 | Импорт и экспорт | `GET /objects/{object_id}/external-models/{model_id}/content` | Содержимое: внешние 3D-модели | реализована в V2 и опубликована | есть: сцена V1 в кадре · экран `external-models` | чтение (файл) · каркас, не проверено: каркас недостаточен (динамическое содержимое V1 н… | не ограничивается (чтение) | чтение опубликовано; экран «Загрузить из FBX» в статусе 2: до «5» — права других ролей, регрессия V1, прогон на итоговой сборке |

### прочее — не отнесено ни к одной из пяти областей

**К выполнению: 1**

| № | Раздел | Операция | Сценарий | Права | V1 | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 312 | Служебные функции | `GET /password-policy` | Требования к новому паролю. | любой вошедший пользователь | не вызывается из V1 | есть: password-form.js:30, users-access.js:899 · экран `users-access` | чтение · настоящий backend: раздел «Пользователи»: настоящий backend и вход (… | не ограничивается (чтение) | подключено в этой ветке (password-form.js:30, users-access.js:899); в опубликованной нет: публикация |

**Уже в V2 и прочее: 1**

| № | Раздел | Операция | Сценарий | Состояние | V2 | Вид · проверка | Шлюз | Что осталось |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 253 | Настройки | `(без API) настройки вида в браузере (localStorage)` | Запоминание в браузере настроек вида: режим вида, скин, ширина панелей, свёрнутые секции,… | служебная | служебный | клиентская · — | — | сводная строка, в счёт не входит: в V1 20 мест записи настроек вида, в V2 4 (собственные); совпадение набора не проверялось — при переносе экрана сверить, каки… |

