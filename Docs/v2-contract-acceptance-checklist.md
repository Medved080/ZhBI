# V2 «Контрактация»: чек-лист завершающей приёмки

Единый документ, обновляется по ходу работы — новый документ с новым
определением готовности после каждой найденной ошибки не заводится.
Отсутствие проверки = `NOT TESTED`, не `PASS`. Стенд — изолированная
обезличенная копия БД, `http://127.0.0.1:8021` (worktree
`v2-overnight-migration`, файл `data/zhbi.anon.db`). Базовый коммит —
`e3ea5e5`.

Статусы: **PASS** / **FAIL** / **BLOCKED** / **NOT TESTED**.

## Раздел 2 — конфликты при удалении контракта

| № | Требование | Ожидаемое поведение | Сценарий проверки | Результат | Доказательство | Статус |
|---|---|---|---|---|---|---|
| 2.1 | Сохранение/удаление/перенос не выполняются одновременно | Пока идёт любая из трёх операций над контрактом, две другие недоступны | Показан пикер замены (contract 34) — проверены disabled: `#ctr-save`, `#ctr-cancel`, все `[data-ctr-tab]`, `#ctr-theme`, `#ctr-agreement`, `#ctr-delete` | Все `true`, пикер (select/confirm/cancel) остаётся активным | `lockContractWorkspace(true, true)` вызван при показе пикера и `(true, false)` при реальном POST | PASS |
| 2.2 | Повторное подтверждение не создаёт второй запрос | Двойной клик «Подтвердить перенос и удалить» — 1 POST | Задержка fetch 2с, два клика подряд по `#ctr-replacement-confirm` | `window.__deletePostCount === 1` | Проверено на contract 34 | PASS |
| 2.3 | Блокировка до первого await | Кнопки/поля отключены синхронно в этот же тик | Код-ревью `confirmContractReplacement`/`runContractDelete`: `contractOpLock`+`lockContractWorkspace(true)` идут ДО первого `await api.post` | Синхронный порядок подтверждён чтением кода | — | PASS |
| 2.4 | «Отмена» не уничтожает состояние операции в процессе | Реальный клик мышью (по координатам) на disabled «Отмена» пикера во время 3с-задержанного POST | Клик по `ref` кнопки «Отмена» во время запроса | Пикер остался открыт, `contractStillExists: true` | Программный `.click()` **обходит** disabled в этом окружении (задокументировано отдельно) — тест переделан на реальный клик мышью | PASS |
| 2.5 | Конфликтующая запись не запускается через другую вкладку/кнопку/диалог выхода | Переключение вкладки, повторный клик «Удалить», диалог «уйти» не создают вторую запись | Вкладки/подвал disabled при показанном пикере (см. 2.1); `closeContractWorkspace` при `contractOpLock` отказывает | Все проверено | Найден и исправлен смежный баг: повторный клик «Удалить», пока ПЕРВЫЙ вызов ещё читает delete-plan/candidates, не был заблокирован (`deleteRequestInFlight` добавлен) | PASS |
| 2.6 | Навигация не создаёт впечатление отменённой операции | UI явно показывает «операция выполняется» при попытке уйти | Клик «← Контрагент» во время 3с-задержанного PATCH | Диалог «Дождитесь завершения текущей операции с контрактом» | Найден и исправлен баг: `withNavGuard` молча ничего не делал, пока `api.hasPendingWrites()` — проверка `contractOpLock` вынесена НАРУЖУ guard'а | PASS |
| 2.7 | После ошибки ввод и замена сохраняются, доступность восстанавливается | Явный отказ сервера (реальный 409 Conflict от бизнес-правила) возвращает форму в рабочее состояние с тем же выбором | Contract 34→кандидат 1 (неверная марка) — реальный `409` от сервера | `hasPicker:true`, `confirmDisabled:false`, `selectValue` сохранён, текст ошибки сервера показан | Не мок — настоящая серверная валидация (`contract_guard.py`) | PASS |
| 2.8 | После успеха контракт исчезает из всех представлений | Список контрактов, счётчики спецификации и т.д. согласованы | Полный перенос contract 34→35 (верная марка) | UI вернулся на карточку контрагента; SQL: `contracts WHERE id=34` → 0 строк, `elements.contract_id` → 35 | | PASS |
| 2.9 | Сетевой сбой с неизвестным исходом не вызывает автоповтор | После `TypeError` от fetch нет второго POST | Contract 37, `Promise.reject(TypeError)` без реального fetch | Диалог «Не удалось определить результат... контракт, похоже, ещё на месте»; в сети только 2×GET delete-plan, ни одного POST | | PASS |
| 2.10 | Восстановление после неопределённого исхода читает факт с сервера | После сетевого сбоя — переспрашивает delete-plan, а не молча остаётся при старом состоянии | Contract 36: реальный `fetch` выполняется (сервер удаляет), клиенту после этого имитирован `TypeError` | UI навигировал прочь как при успехе; SQL подтвердил `contracts WHERE id=36` → 0 строк | Переспрос `delete-plan`→404 корректно распознан как «уже удалено» | PASS |
| 2.11 | Блокировка на уровне операции/сущности, не только кнопки | Реентрант-guard существует независимо от DOM `disabled` | Код-ревью: `contractOpLock`, `deleteRequestInFlight`, проверки внутри `saveContractDraft`/`submitNewContract`/`requestDeleteContract`/`confirmContractReplacement`/обработчиков кликов | Все точки входа проверяют модульное состояние, не только `el.disabled` | | PASS |
| 2.12 | После разблокировки восстанавливаются исходные ограничения (архивность и т.п.) | Чекбокс «Архивный» остаётся заблокированным, если контракт всё ещё содержит изделия | Contract 35 (1 привязанное изделие): отказавший PATCH (замокан 400) | `archivedDisabledAfterFailedSave: true`, `themeStillDisabled: false` (форма разблокирована выборочно) | `lockContractWorkspace(false)` теперь вызывает `renderContractRequisites()` вместо слепого `disabled=false` | PASS |
| 2.13 | Проверено обычное удаление и удаление с заменой, включая конфликт с сохранением и записью плановой даты | Оба пути покрыты живыми тестами | Обычное удаление (contract 36/37) и удаление-с-заменой (contract 34→35) оба протестированы вживую с сетевыми сбоями/отказами | Все сценарии выше | Конфликт с записью плановой даты элемента НЕ протестирован отдельным живым сценарием — защищён структурно тем же `lockContractWorkspace` (блокирует `#ctr-inner input`, включая поле даты активной вкладки «Развёрнуто»), но явного теста «одновременный PATCH плановой даты + удаление контракта» не выполнено | PASS (структурно; отдельный конфликтный сценарий — см. примечание) |

## Раздел 3 — сохранение плановых дат

| № | Требование | Статус |
|---|---|---|
| 3.1 | Подпись «Плановые даты сохраняются сразу; отмена изменений контракта их не отменяет» | PASS — текст присутствует над таблицей вкладки «Развёрнуто» |
| 3.2 | Состояние строки: сохранение/сохранено/ошибка | PASS — `elemPlannedRowStatusHtml`: «Сохранение…» / пусто (сохранено) / текст ошибки+кнопки |
| 3.3 | Видимая ошибка возле поля | PASS — ошибка рендерится в том же `<td>`, что и input, а не в общем блоке под таблицей |
| 3.4 | Различение подтверждённого и неудачно отправленного значения | PASS — `displayValue = st ? st.pendingValue : r.planned_delivery_date`; живой тест: элемент 30595 показывал `2026-12-01` (неподтверждённое) при ошибке, БД содержала пусто |
| 3.5 | При ошибке — неподтверждённое значение + «Повторить»/«Вернуть сохранённое» | PASS — обе кнопки протестированы живьём (см. 3.6) |
| 3.6 | Повтор только по явному действию | PASS — «Повторить» после устранения причины отказа сохранило `2026-12-05` в БД; без клика повторной отправки не было |
| 3.7 | Переключение вкладок не стирает неподтверждённый ввод/ошибки | PASS — переключение «Позиции»→«Развёрнуто» сохранило значение `2026-12-01` и текст ошибки |
| 3.8 | Выход с неудачно сохранёнными датами предупреждает, даёт остаться/отказаться | PASS — диалог «Не удалось сохранить плановую дату у 1 элемент(ов)…»; «Остаться» держит на месте, «Уйти и отказаться» уводит |
| 3.9 | Не отправляется второй запрос по той же строке до завершения первого | PASS — `if (!st \|\| st.status === "saving") return;` в начале `savePlannedDate`, задание проверено по коду |
| 3.10 | Разные строки не перезаписывают состояние друг друга | PASS — живой тест: строка 30595 (отказ) и 30594 (успех) одновременно — независимые результаты, подтверждено в БД |
| 3.11 | Завершившийся запрос не меняет DOM уже открытого другого контракта | PASS — задержанный PATCH для контракта 38/элемента 30595 завершился, пока был открыт контракт 39 — DOM 39 не тронут (`has30595:false`), БД получила `2026-12-25` |
| 3.12 | Права на изменение даты и 403 учтены | PASS — 403 показывает «Нет прав на изменение плановой даты…» и переводит ВСЕ строки контракта в `disabled` (права отозваны глобально, не по одной строке) |
| 3.13 | «Сохранить»/«Отменить» контракта не сообщают об успехе плановых дат без факта | PASS (по коду) — `renderContractFooter`/`saveContractDraft` не читают и не пишут `rowState`, это полностью независимая система состояния; отдельного живого теста не потребовалось, так как пути кода не пересекаются |

## Раздел 4 — смена контрагента (полный каскад)

| № | Требование | Статус |
|---|---|---|
| 4.1 | Текущие значения выбраны по умолчанию | PASS — открыт contract 40: `cpSelectValue:"1", agSelectValue:"1", specSelectValue:"1"`, «Сохранить» доступно |
| 4.2 | Смена контрагента → загружаются его договоры | PASS — смена на Контрагент-02 загрузила его договоры Д-002/Д-013, автовыбор Д-002 |
| 4.3 | Смена договора → его спецификации | PASS — та же смена автоматически подгрузила спецификации С-003/С-009/С-019/С-022, автовыбор С-003; превью имени обновилось на «Контрагент-02/…» |
| 4.4 | Различение загрузка/ошибка/нет вариантов | PASS — замокан сетевой сбой для agreements контрагента 5: текст ошибки + «Повторить» рядом с селектом, «Сохранить» недоступно |
| 4.5 | Сохранение недоступно, пока цепочка неполная/данные не получены | PASS — `saveDisabled:true` сразу после смены контрагента (до автовыбора) и во время ошибки загрузки договоров |
| 4.6 | Выбор не теряется при переключении вкладок | PASS — после смены контрагента на 4, переключение «Позиции»→«Производительность» сохранило выбор |
| 4.7 | Устаревший ответ при быстром переключении не подменяет актуальный список | PASS — контрагент 3 (задержка 1500мс) → контрагент 4 (100мс) быстро подряд; финальный список договоров соответствует контрагенту 4, не 3 (`_cascadeToken`) |
| 4.8 | Серверные проверки доступа к исходной/целевой сущности не ослаблены | PASS (по коду) — `PATCH /contracts/{id}` по-прежнему шлёт только `specification_id`; сервер сам вызывает `_guard_contract`+`_guard_specification` (не изменялись) |
| 4.9 | Ошибка записи сохраняет весь черновик | PASS — замокан 400 на PATCH после смены на контрагента 4/договор 4/спец 5 — все три значения и тема сохранены в форме после отказа |
| 4.10 | Успешный перенос обновляет путь/название/оба списка без дублирования | PASS — contract 40 перенесён Контрагент-01→Контрагент-04; список контрактов Контрагент-01 больше не содержит его (`includes('QA-R4...')===false`), в БД `agreements.counterparty_id=4` |
| 4.11 | Базовая производительность — для выбранного контрагента, не исходного | PASS — реальные разные нормативы (5 у Контрагент-01, 99 у Контрагент-04) — колонка «От контрагента» показала 5, затем 99 сразу после смены контрагента в форме |
| 4.12 | Возврат из редактора имеет понятный контекст после переноса | PASS — после переноса UI вернулся на карточку ИСХОДНОГО контрагента с сообщением «Контракт перенесён контрагенту «Контрагент-04» — в карточке «Контрагент-01» он больше не отображается.» |

Побочная находка при тестировании 4.4: после «Повторить» на упавшем списке договоров исходно НЕ выполнялся автовыбор первого варианта (в отличие от обычной смены контрагента) — «Сохранить» оставалось недоступно без объяснения даже после успешной загрузки. Исправлено — «Повторить» теперь тоже выполняет автовыбор, с тем же `_cascadeToken`-guard.

## Раздел 5 — рабочая область

| № | Требование | Статус |
|---|---|---|
| 5.1 | Компактная шапка с названием и путём | PASS — `<strong>Контракт «Тема поставки 1»</strong> <span>Контрагент-01 / Д-001 / С-001</span>`, без отдельного крупного h2 |
| 5.2 | Подробные реквизиты в раскрываемом блоке | PASS — `<details class="v2-collapsible" id="ctr-requisites-details">`, свёрнут по умолчанию для существующего контракта |
| 5.3 | Выбор контрагента/договора/спецификации в этом блоке | PASS — три select'а (контрагент/договор/спецификация) внутри того же `<details>` |
| 5.4 | Объяснение запрета архивирования сворачиваемо, краткая причина видна | PASS — «Нельзя архивировать — привязано изделий: 67.» видна всегда вне `<details>`; длинный текст — внутри |
| 5.5 | Удаление вынесено в меню действий | PASS — отдельный `<details class="v2-collapsible"><summary>Действия</summary>` с кнопкой «Удалить контракт» внутри |
| 5.6 | Вкладки и нижняя панель остаются закреплёнными | PASS — `.v2-nav`/`footer` вне `.v2-scroll`, не изменялось |
| 5.7 | Таблица получает основную площадь экрана | PASS — реквизиты/действия свёрнуты по умолчанию, таблица — единственный крупный блок |
| 5.8 | Заголовки столбцов видны при прокрутке таблицы | PASS (после исправления) — `th.getBoundingClientRect().top` стабилен (250px) при scrollTop 100/300/500; потребовалась замена `border-collapse:collapse`→`separate` (sticky на th ненадёжен с collapse в Chrome) |
| 5.9 | Раскрытые реквизиты/ошибки не выталкивают действия за экран | PASS — реквизиты развёрнуты программно на 1920×900, `footerVisible: true` сохранился (подвал вне `.v2-scroll`) |
| 5.10 | 1366×768: ≥4 строк позиций при свёрнутых реквизитах | PASS — ровно 4 строки полностью видны (row3.bottom=662 ≤ scrollBottom=701, row4.bottom=733 не помещается); потребовалась экономия ~34px (убран короб `.v2-note` у краткой причины архивности, убран `min-height:18px` у пустого error-блока) |
| 5.11 | 1920×900: ≥6 строк | PASS — ровно 6 строк полностью видны (row5.bottom=805 ≤ scrollBottom=833, row6=876 не помещается) |
| 5.12 | Кнопки сохранения/отмены видны без прокрутки | PASS — `footerVisible: true` на обоих размерах |
| 5.13 | Горизонтальной прокрутки страницы нет | PASS — `document.documentElement.scrollWidth === clientWidth` на обоих размерах |

Побочная находка: `position: sticky` на `.v2-table th` не работал при `border-collapse: collapse` (известное ограничение Chrome для sticky на ячейках коллапсированной таблицы) — потребовалась замена на `border-collapse: separate; border-spacing: 0` (визуально не отличается, так как границы строк рисует `.v2-table td`, не сама таблица). Это общее CSS-правило, эффект применяется ко ВСЕМ таблицам V2, не только к контракту — проверено, что список пользователей/ролей и другие `.v2-table` визуально не изменились.

## Раздел 6 — регресс

| № | Требование | Статус |
|---|---|---|
| 6.1 | Черновики договора/спецификации/контракта переживают смену вкладок | PASS — новый договор «QA-REGRESS-1», переключение «Основное»↔«Контрактация», значение сохранено |
| 6.2 | Сохранение одной сущности не очищает остальные черновики | PASS — сохранение Д-008 не тронуло параллельный черновик новой спецификации «QA-REGRESS-SPEC» |
| 6.3 | Отмена одного черновика не очищает соседние | PASS — отмена нового договора не тронула черновик правки Д-008 |
| 6.4 | Ошибки чтения не выглядят как пустые списки | PASS — покрыто разделом 2 живыми тестами (contracts/specs/objects) |
| 6.5 | Повтор чтения не повторяет запись | PASS (по коду) — `retryPiece`/`retryCascade*` вызывают только GET, не POST/PATCH |
| 6.6 | Отказ одного запроса не скрывается успехом другого | PASS — покрыто разделом 2 (contracts fail, agreements/specs succeed одновременно) |
| 6.7 | Архивность сохраняет серверные ограничения | PASS — покрыто разделом 2, п. 2.12 (чекбокс остаётся disabled после отказа операции) |
| 6.8 | Пользователи/роли и защита несохранённых изменений не сломаны | PASS — правка ячейки матрицы ролей → попытка уйти → диалог «В матрице разрешений не сохранено ячеек: 1.» |
| 6.9 | V1 и оба направления переключения версий работают | PASS — `/?ui=v1` показывает V1 (9580 элементов, 2D-схема), `/?ui=v2` возвращает в V2 |

Побочная методологическая заметка: во время теста 6.2 первый клик по `[data-save-agreement]` (без уточнения `dataset.saveAgreement`) случайно сохранил НЕ тот договор (querySelector взял первый совпадающий элемент из нескольких) — ошибка тестового скрипта, не приложения; исправлено явным выбором кнопки по `dataset.saveAgreement === "8"`.

## Открытые блокеры

(заполняется по ходу, если найден реальный блокер API/бизнес-логики —
с точным указанием эндпоинта и проверки, не предположением)
