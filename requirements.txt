ezdxf==1.4.2
fastapi==0.128.8
pydantic==2.13.4
pydantic_core==2.46.4
starlette==0.49.3
uvicorn==0.39.0
openpyxl==3.1.5
python-multipart==0.0.20
lxml==6.1.1
reportlab==5.0.0
svglib==2.0.2
shapely==2.0.7
# Разбор PDF (слои/OCG векторных примитивов) для загрузки помещений из
# чертежа — app/pdf_rooms.py. Лицензия AGPL v3 (Artifex), не MIT/BSD, как
# остальные зависимости — осознанный выбор для внутреннего инструмента,
# согласован с пользователем 2026-08-26.
PyMuPDF==1.26.5
# Доменная (корпоративная) авторизация — LDAP-bind к контроллеру домена,
# см. app/ldap_auth.py. Чистый Python (единственная зависимость — pyasn1),
# без C-расширений: в образ python:3.12-slim встаёт без сборочных пакетов.
ldap3==2.9.1
