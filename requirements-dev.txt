# Зависимости ТОЛЬКО для локальной разработки. В образ (Dockerfile ставит
# requirements.txt) не входят намеренно: на сервере uvicorn запускается без
# --reload, и сторож файлов там не нужен.
-r requirements.txt

# Сторож файлов для `uvicorn --reload`. Без него uvicorn молча падает на
# запасной StatReload (опрос mtime), который на этом проекте не срабатывал
# вовсе: сервер на 8000 крутил код трёхчасовой давности и продолжал отдавать
# уже исправленную ошибку (см. Docs/backlog.md 2026-08-03).
watchfiles==1.1.1
